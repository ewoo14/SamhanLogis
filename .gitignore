# ----- Gradle -----
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/

# ----- Maven (in case any module uses it) -----
target/
.mvn/wrapper/maven-wrapper.jar

# ----- IDE -----
.idea/
*.iml
*.iws
*.ipr
.vscode/
*.code-workspace
.project
.classpath
.settings/
out/
bin/
nbproject/private/
nbbuild/
nbdist/
.nb-gradle/

# ----- OS -----
.DS_Store
Thumbs.db
desktop.ini

# ----- Logs -----
*.log
logs/
# QA 증거 로그는 보고서가 원문을 인용하므로 저장소에 보존한다. 그 밖의 위치는 위 규칙을 유지한다.
!docs/qa/**/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# `logs/` 및 npm/yarn 디버그 로그 규칙의 예외는 docs/qa 전체 하위 경로에 적용한다.
!docs/qa/
!docs/qa/**/
!docs/qa/**/*.log

# ----- Node -----
node_modules/
dist/
dist-ssr/
*.local
.turbo/
.parcel-cache/

# ----- Python ----- (2026-07-27 하네스 흡수 H2 — docs/qa/**/*.py PNG 생성 스크립트가
# scripts/lib/qa_shots_dir.py 를 import 하면서 실행할 때마다 __pycache__/*.pyc 가 생긴다)
__pycache__/
*.pyc

# ----- Env -----
.env
.env.local
.env.*.local
!.env.example
!.env.dev-seed

# ----- Test outputs -----
coverage/
*.lcov
.nyc_output/
playwright-report/
playwright-json/
test-results/
# QA 일회용 — 실 캡처 PNG만 추적, 캡처 스크립트·결과 json·진단 스크립트 제외
**/qa-*-capture.mjs
docs/qa/**/_results.json
# 라이브QA 재캡처 스크래치 — 스펙 재실행 기본 출력지(resolveQaShotsDir, clients/desktop/playwright/
# support/qa-screenshot-dir.ts 및 .mjs 버전). 커밋된 확정 증거(docs/qa/<slug>/*.png,
# clients/desktop/playwright/<slug>/screenshots/*.png, docs/manual/screenshots/*.png 등)와
# 경로를 분리해 재실행이 그 증거를 덮어쓰지 못하게 한다 (2026-07-25, #902 슬라이스 — 덮어쓰기
# 위험 때문에 리뷰어가 라이브QA 실행을 포기한 사고 재발 방지). resolveQaShotsDir 는 임의의
# committedDir 인자를 받아 그 밑에 `_local/` 을 만들 수 있으므로(2026-07-26 하네스 재수렴
# 라운드 G4 — docs/manual/screenshots/_local/ 이 이 규칙 밖이라 미추적 잔재로 남던 결함 실측)
# 특정 디렉토리를 나열하지 않고 저장소 전역에서 `_local/` 이름의 디렉토리를 전부 제외한다.
**/_local/
**/.diag-*.cjs
**/.diag-*.mjs
# 로컬 세션 QA 아티팩트 — CI testIgnore 대상·커밋 대상 아님 (git status 정리, 2026-07-05 개발책임자 요청)
clients/desktop/playwright/manual/
clients/desktop/playwright/coedit-s3-1-live/
clients/desktop/playwright/coedit-s3-3-accounting/
clients/desktop/playwright/dispatch-collab-real-qa/
clients/desktop/playwright/n1b-native-qa/
clients/desktop/playwright/n3b-fcm-push-real-qa/
clients/desktop/playwright/e2-partner-list-real-qa/
clients/desktop/playwright/1001-r5-ledger-real-qa/
clients/desktop/playwright/1001-r6-ledger-real-qa/
# playwright 루트에 바로 놓인 라이브QA 드라이버 — H-2 가드는 QA 스펙이 디렉터리 안에 있기를
# 요구하므로 루트 직하 real-qa 파일은 커밋되면 CI 가 깨진다(2026-08-03 실측). 증거는 보고서와
# 캡처이지 드라이버가 아니다. ※ 디렉터리형(`<slug>-real-qa/`)은 커밋 증거라 무시하지 않는다.
clients/desktop/playwright/*-real-qa.spec.ts
clients/desktop/playwright/*-real-qa.mjs
clients/desktop/playwright/*-real-qa-result.json
clients/desktop/iotxn-*.config.ts
clients/desktop/docs/

# 병합 PR(#760/#761/#714) 선행 브랜치(#756/#755/#711) 잔재 QA 스샷 — 실 증적은
# docs/qa/*-retro/·714-journal-detail-1024px/·e2-rollout-* 에 이미 커밋됨. 로컬 중복 고아
# (재캡처 가능·커밋 대상 아님). 물리 삭제는 개발책임자 확인 후. (2026-07-11 집PC 정리)
docs/qa/e2-partner-list/gui-*.png
docs/qa/e3-s4d-coedit/asis-broken/
docs/qa/e3-s4d-coedit/fixed-*.png
docs/qa/journal-detail-column-widths/screenshots/*-1024-minwidth-*.png

# ----- Misc -----
*.tmp
*.bak
*.swp
*~
test_raw.json
tempit_raw.json
*.home-bak

# ----- 머신-로컬 설정 (커밋 금지 — PC별 상이) -----
.mcp.json
.codex/

# ----- pnpm 미사용 (clients 는 npm — package-lock.json) -----
pnpm-lock.yaml
pnpm-workspace.yaml

# ----- Codex 로컬 캐시/워크스페이스 (회사 PC 핸드오프 산출물 — 커밋 금지) -----
.gradle-codex/
_codex_commit_repo/

# ----- 병렬 트랙 전용 Gradle 홈 (트랙별 캐시 경합 회피용 — 커밋 금지) -----
.gradle-t[0-9]*/
# `--gradle-user-home` 로 지정한 트랙별 홈도 같은 성격 (2026-08-05 집PC — .gradle-user-t1008b/)
.gradle-user-*/

# ----- 이카운트 raw (민감) -----
# 거래처 사업자번호 / 미수금 / 매입매출 등 민감 정보 포함, .gitkeep 만 보존
docs/migration/ecount-data/raw/*
!docs/migration/ecount-data/raw/.gitkeep

# ----- Tesseract OCR (PR-F2) — 로컬 다운로드 traineddata 무시 -----
# 한국어 모델 (kor.traineddata) 약 50MB; 환경별 설치 가이드 docs/dev-environment/tesseract-setup.md 참조.
# system 경로 (C:\Program Files\Tesseract-OCR\tessdata, /usr/share/tesseract-ocr/...) 사용 권장이며,
# 프로젝트 root 에 별도 다운로드 시 본 규칙으로 VCS 격리.
tessdata/
**/tessdata/*.traineddata

# ----- Electron / 빌드 산출물 -----
release/
**/release2/
**/release-codex-review/
clients/desktop/legacy-assets/estimate/*.html

# ----- Claude / Agent local state -----
.claude/settings.local.json
.claude/settings.json
.claude/scheduled_tasks.lock
.claude/.cache/
.claude/worktrees/
.claude/tmp/
.superpowers/

# ----- PR/Issue/TM/PM draft (gh CLI body-file 일회용) -----
.pr-body*.md
.issue-*.md
.tm-*.md
.pm-*.md
.tmp/
.tmp-*
commit-msg.txt

# ----- 멀티모델 워크플로우 스크래치 (codex 콘솔로그·프롬프트·출력·커밋메시지 일회용) -----
docs/tmp/

# ----- legacy GAS (소스 코드는 추적, 운영 데이터·자격은 제외) -----
# .js/.html 소스는 회사 PC 코드 조회용으로 추적(Notion/Google 토큰은 redact 됨).
# 민감 운영 데이터(거래처 DC·단톡방·발송금지 등 Notion export)는 VCS 추적 금지.
tools/legacy-gas/_notion-export/
tools/legacy-gas/**/*.csv
tools/legacy-gas/**/*.zip
tools/legacy-gas/**/*.xlsx

# ----- Terraform -----
infrastructure/terraform/.terraform/
infrastructure/terraform/.terraform.lock.hcl
infrastructure/terraform/terraform.tfstate
infrastructure/terraform/terraform.tfstate.backup
infrastructure/terraform/tfplan
.tmp-qa/
infrastructure/docker-compose.qa-override.yml
.tmp-qa-2-6c.sh
*.orig
_tmp_*
.pr327.*
*.class

# C5 cutover DB backups (자격 포함 — 커밋 금지)
backups/

# 종합견적서 clasp 라이브 클론 (평문 자격 포함 — redact 전 커밋 금지)
tools/legacy-gas/종합견적서-live/
.clasp.json

# 양식 샘플 드롭 폴더 — 실거래처/개인정보/계좌 포함 가능 → public repo 커밋 금지 (README 만 추적)
docs/sample/*
!docs/sample/README.md
.codex-gradle-cache/
.codex-temp/
.gradle-codex-cache/
.gstack/

# Capacitor / Android 빌드 산출물
clients/desktop/dist/capacitor/
clients/desktop/android/.gradle/
clients/desktop/android/app/build/
clients/desktop/android/build/
clients/desktop/android/local.properties
clients/desktop/android/.idea/
clients/desktop/android/app/src/main/assets/public/
clients/desktop/android/app/google-services.json
clients/desktop/ios/App/GoogleService-Info.plist
clients/desktop/ios/App/App/GoogleService-Info.plist
clients/desktop/ios/**/*.p8
services/notification-service/config/*service-account*.json
services/notification-service/config/*firebase*.json
services/notification-service/config/*fcm*.json
services/notification-service/src/main/resources/*service-account*.json
services/notification-service/src/main/resources/*firebase*.json
services/notification-service/src/main/resources/*fcm*.json
