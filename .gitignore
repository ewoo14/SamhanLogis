# ----- Gradle -----
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/

# ----- Maven (in case any module uses it) -----
target/
.mvn/wrapper/maven-wrapper.jar

# ----- IDE -----
.idea/
*.iml
*.iws
*.ipr
.vscode/
*.code-workspace
.project
.classpath
.settings/
out/
bin/
nbproject/private/
nbbuild/
nbdist/
.nb-gradle/

# ----- OS -----
.DS_Store
Thumbs.db
desktop.ini

# ----- Logs -----
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ----- Node -----
node_modules/
dist/
dist-ssr/
*.local
.turbo/
.parcel-cache/

# ----- Env -----
.env
.env.local
.env.*.local
!.env.example
!.env.dev-seed

# ----- Test outputs -----
coverage/
*.lcov
.nyc_output/
playwright-report/
playwright-json/
test-results/

# ----- Misc -----
*.tmp
*.bak
*.swp
*~
test_raw.json
tempit_raw.json
*.home-bak

# ----- 머신-로컬 설정 (커밋 금지 — PC별 상이) -----
.mcp.json

# ----- pnpm 미사용 (clients 는 npm — package-lock.json) -----
pnpm-lock.yaml
pnpm-workspace.yaml

# ----- Codex 로컬 캐시/워크스페이스 (회사 PC 핸드오프 산출물 — 커밋 금지) -----
.gradle-codex/
_codex_commit_repo/

# ----- 이카운트 raw (민감) -----
# 거래처 사업자번호 / 미수금 / 매입매출 등 민감 정보 포함, .gitkeep 만 보존
docs/migration/ecount-data/raw/*
!docs/migration/ecount-data/raw/.gitkeep

# ----- Tesseract OCR (PR-F2) — 로컬 다운로드 traineddata 무시 -----
# 한국어 모델 (kor.traineddata) 약 50MB; 환경별 설치 가이드 docs/dev-environment/tesseract-setup.md 참조.
# system 경로 (C:\Program Files\Tesseract-OCR\tessdata, /usr/share/tesseract-ocr/...) 사용 권장이며,
# 프로젝트 root 에 별도 다운로드 시 본 규칙으로 VCS 격리.
tessdata/
**/tessdata/*.traineddata

# ----- Electron / 빌드 산출물 -----
release/
clients/desktop/legacy-assets/estimate/*.html

# ----- Claude / Agent local state -----
.claude/settings.local.json
.claude/settings.json
.claude/scheduled_tasks.lock
.claude/.cache/
.claude/worktrees/
.claude/tmp/
.superpowers/

# ----- PR/Issue/TM/PM draft (gh CLI body-file 일회용) -----
.pr-body*.md
.issue-*.md
.tm-*.md
.pm-*.md
.tmp/
.tmp-*
commit-msg.txt

# ----- legacy GAS (소스 코드는 추적, 운영 데이터·자격은 제외) -----
# .js/.html 소스는 회사 PC 코드 조회용으로 추적(Notion/Google 토큰은 redact 됨).
# 민감 운영 데이터(거래처 DC·단톡방·발송금지 등 Notion export)는 VCS 추적 금지.
tools/legacy-gas/_notion-export/
tools/legacy-gas/**/*.csv
tools/legacy-gas/**/*.zip
tools/legacy-gas/**/*.xlsx

# ----- Terraform -----
infrastructure/terraform/.terraform/
infrastructure/terraform/.terraform.lock.hcl
infrastructure/terraform/terraform.tfstate
infrastructure/terraform/terraform.tfstate.backup
infrastructure/terraform/tfplan
.tmp-qa/
infrastructure/docker-compose.qa-override.yml
.tmp-qa-2-6c.sh
*.orig
_tmp_*
.pr327.*
*.class

# C5 cutover DB backups (자격 포함 — 커밋 금지)
backups/

# 종합견적서 clasp 라이브 클론 (평문 자격 포함 — redact 전 커밋 금지)
tools/legacy-gas/종합견적서-live/
.clasp.json

# 양식 샘플 드롭 폴더 — 실거래처/개인정보/계좌 포함 가능 → public repo 커밋 금지 (README 만 추적)
docs/sample/*
!docs/sample/README.md
.codex-gradle-cache/
.codex-temp/
.gradle-codex-cache/
