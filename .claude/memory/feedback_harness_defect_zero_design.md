---
name: feedback_harness_defect_zero_design
description: 🚨 하네스 설계 정본 (2026-07-23 개발책임자 전면 수용) — 궁극 목적은 결함 0. 설계 목표는 fix-유발률 r < 0.45. 진단 확증 · 표면 분할 리뷰 · fix 최소 변경 · 회귀 울타리 · 계열 전수 sweep · 효용 게이트(U-gate) · r 기반 적응 규칙. 3턴은 권고
metadata:
  type: feedback
---

# 🚨 하네스 설계 정본 (2026-07-23)

> 개발책임자: *"결함의 증가여부에 따라 PM이 규칙을 변경해도 될 것 같아. 기본은 지금 규칙으로 하되, 계속 순증하고 역효과라면 예전 규칙대로 해도 좋아. **어쨌든 가장 궁극적인 목적은 결함을 0으로 만드는 것**이기 때문이야. **해당 기능이 쓸모있는가에 대한 질문도 중요**하긴해. PM이 가장 효율적인 하네스를 제시 바라."*
> 개발책임자: *"3턴은 그냥 권고로 하고 PM의 제안을 모두 수용."*

**궁극 목적 = 결함 0.** 라운드 수·속도는 그 수단이지 목적이 아니다.

---

## 0. 설계 목표를 숫자로 — **수렴비 c < 0.45**

지표는 **둘**이다. 하나는 판정용(애매함 없음), 하나는 진단용.

| 지표 | 정의 | 용도 |
|---|---|---|
| **수렴비 `c`** | **이번 라운드 도달가능 건수 ÷ 직전 라운드 도달가능 건수** | **판정** — 적응 규칙은 이 값으로만 움직인다. 귀속 논쟁이 없다 |
| **fix-유발률 `r`** | 이번 라운드 `[NEW]` 도달가능 중 **직전 fix 가 만든 것** ÷ 이번 라운드 도달가능 건수 | **진단** — c 가 높을 때 원인이 fix 인지 리뷰 심화인지 가른다 |

미해결 N 건이 3라운드 안에 1건 미만이 되려면:
```
N · c³ < 1        N=10 ⟹ c < 0.46
```
이 레포 실측은 **c ≥ 1**(#907 이 4건 → 10건으로 늘었다). **그 값이면 라운드를 아무리 돌려도 수렴하지 않는다.**
⟹ 하네스의 유일한 설계 목표는 **라운드를 늘리는 것이 아니라 c 를 0.45 아래로 떨어뜨리는 것**이다. 나머지는 전부 부차적이다.

⚠️ **c 가 1을 넘어도 곧바로 실패가 아니다.** 리뷰 브리프를 도달성으로 좁히면 같은 비용으로 더 깊이 파므로 **일시적으로 발견이 는다**(#907 실측 4→10). 그때는 `r` 을 보고 가른다 — **r 이 낮으면 리뷰가 깊어진 것(정상)**, **r 이 높으면 fix 가 결함을 낳는 것(개입 필요)**.

**매 라운드 c 와 r 을 계산해 PR 코멘트에 기록한다.** 기록하지 않으면 적응 규칙이 작동하지 않는다.

---

## 1. r 을 만드는 원인 (2026-07-23 세션 실측)

| # | 원인 | 실측 |
|---|---|---|
| ① | **fix 가 기존 고지·경로를 조용히 죽인다** | 관측된 fix-유발 결함 **2건 중 2건**. #907 `partnerIdExact` 가 안내 배너를 구조적 렌더 불가로 · #914 빈값 fallback 을 진단 문구로 바꿔 **실 결재문서 인쇄면**에 오류 문구를 실음 |
| ② | **fix 가 계열의 한 곳만 고친다** | #907 뿌리 **B·C·D 전부** 직전 fix 의 sweep 누락에서 나옴(신선도·무효화 키·escape) |
| ③ | **fix 부피가 곧 다음 라운드 감사면** | #854 R4 신규 1,400줄 → R5 HIGH 5건이 거의 전부 그 안 |
| ④ | **PM 진단이 틀려 불필요한 변경을 유발** | #914 — 기획서의 *"ASCII 정규식이 원인"* 이 오진(**label↔key 혼동**). 실서버 key 는 전부 ASCII 였다. 구현자가 진짜 원인을 고치면서 **불필요하게 정규식까지 넓혀** FE/BE 계약 불일치를 만듦 |

---

## 2. 하네스 5부

### ① 기획 — **PM 진단 확증 의무**
불변식을 쓰기 전에 **원인을 실행으로 확증**한다. 코드 읽기 추정으로 쓰지 않는다.
확증하지 못하면 **"원인 미확정"** 이라고 적고 구명을 구현자에게 맡긴다. 🚫 추정을 단정으로 쓰지 말 것 — 원인 ④ 차단.

### ② 리뷰 — **5차원 고정 폐기, 표면 분할 3~4 에이전트**
차원(역할)이 아니라 **표면(파일군)** 으로 나눈다. 각 에이전트에게 주는 질문은 **도달성 단일 질문** 하나뿐.
- 실측 근거: #914 5차원에서 도달가능 5건이 나왔는데 **A-1·A-2 가 3~4개 차원에서 중복 발견**됐다. 중복은 비용이다. #907 마감 라운드는 **3차원으로 10건**을 찾았다.
- **도달성 판정 + 종합 + 라이브QA 는 루트**가 맡는다(도달성 = 라이브QA 그 자체).

### ③ fix — 🔴 **최소 변경 원칙** (가장 강한 레버)
> **불변식을 만족시키는 최소 변경만 한다. "온 김에" 금지.**

부피가 곧 다음 라운드 감사면이므로(원인 ③), 구현자는 **변경 줄 수를 보고**하고 늘어난 이유를 설명한다. #914 의 정규식 확장이 정확히 이 규칙에 걸렸을 변경이다.

### ④ fix — **회귀 울타리 + 계열 전수 sweep**
- **회귀 울타리**: fix 지시마다 **"이것은 계속 보여야/동작해야 한다"** 목록을 함께 준다 — 원인 ① 차단.
- 🚨 **회귀 울타리는 표면을 명시한다** (2026-07-24 실측). #907 의 `F-2`(`%`·`_` escape)가 **3라운드 연속 PASS** 였는데, 그것은 **거래처 검색(partner-service)** 을 잰 것이었다 — escape 가 원래 거기 있었으니 당연히 통과. 정작 깨진 **판매관리 검색(slip-service)** 은 한 번도 측정되지 않았다. 울타리가 *"실패 시 배너 노출"* 처럼 **어느 화면·어느 서비스인지 안 적으면**, fix 를 넣은 곳만 재고 결함이 있는 곳은 안 잰다 → **양성 대조군과 똑같은 거짓 green**(2-B③). ⟹ 울타리 항목은 **"거래처·판매관리·구매관리 *각각*에서 측정"** 처럼 표면을 전부 나열한다.
- **계열 전수 sweep**: 한 곳을 고치면 **같은 패턴이 있는 곳 전부**를 grep 으로 확인하고 **실제로 조작해** 목록을 보고한다(코드 근거로 갈음 금지 — 그것이 F-2 가 3번 놓친 이유). 보고 단위는 "고친 곳"이 아니라 **"같은 패턴이 있는 곳 전부"** — 원인 ② 차단. sweep 이 슬라이스 **표면 밖**의 같은 결함을 찾으면(#907 은 5개 서비스의 escape 누락 발견), 범위 동결상 흡수하지 않되 **은폐하지 말고 PR 에 기록 + 개발책임자 판단**을 구한다. → [[feedback_defect_family_sweep_fix]] · [[feedback_expanded_scope_reinstate_review]]

### ⑤ 🚨 **효용 게이트 (U-gate)** — *"기능이 쓸모있는가"*
도달성 축은 *"코드가 사용자에게 거짓말하는가"* 는 잡지만 *"기능이 실제로 쓸모 있는가"* 는 못 잡는다. 적대 라운드에 넣으면 범위가 다시 터지므로 **양 끝에만** 둔다.
- **기획 시 1문장** — *"이 슬라이스가 끝나면 사용자가 **실데이터로** 무엇을 할 수 있게 되는가"* + 구체 시나리오 1개
- **머지 직전 PM 라이브QA 1회** — 그 시나리오를 **실데이터로 실행**. 실행하지 못하면 **왜 못 하는지 명시하고 개발책임자 판단**을 받는다(조용히 머지 금지).

🚩 실증 — **#907**: 실 DB 주문 **2,020건 전부 `partner_id IS NULL`** 이라 legacy fail-closed 결정상 **머지돼도 병합을 실행할 수 없다.** 도달성 축만으로는 도달가능 0 으로 수렴해 **조용히 머지된다.** U-gate 가 정확히 이 지점을 잡는다.

---

## 2-B. 🚨 불변식을 쓸 때의 3가지 함정 (2026-07-24 실측 · 전부 PM 기획 실패)

### ① **"X 를 없애라"는 "X 를 담은 것 전체를 없애라"로 만족된다**
#914 — PM 이 *"내부 용어(`envelope`)를 화면에 내지 마라 · 클래스 단위로 고쳐라"* 라고 냈다. 구현자는 **10개 오류 코드 전부를 일반 문구로 치환**해 이를 만족시켰다. 클래스 단위도 맞고 용어도 사라졌다. **그런데 원인 정보가 같이 버려졌다.**
```
문구 4,096자 초과 → "문서 요소 설정을 확인하세요."
문구 빈 값       → "문서 요소 설정을 확인하세요."   ← 같은 문구로 붕괴
```
파서는 여전히 `문구는 4096자 이하여야 합니다` 를 만드는데 **소비처가 버린다.** 입력칸에 `maxLength` 도 카운터도 없어 사용자는 **무엇을 줄여야 하는지 알 수단이 0**.
🔑 **어휘를 바꾸라는 요구는 반드시 "정보를 보존하라"와 함께 낸다.** 그리고 **"서로 다른 두 원인이 같은 문구로 붕괴하지 않는다"** 를 별도 불변식으로 세운다 — 이것이 없으면 일반화로 만족된다.

### ② **위치를 옮기는 fix 는 문제를 옮길 뿐이다**
#909 — 오버레이 알림이 3라운드 연속 새 피해자를 만들었다.
```
좌상단 fixed  → 사이드바·제목을 가림
우하단 fixed  → 다른 토스트를 가림          (겹침 84.8%)
in-flow static → 인쇄물을 밀어 A4 1장이 2장  (고객 문서의 합계행·서명란이 넘어감)
```
🔑 **오버레이는 어디에 두든 무언가와 부딪힌다.** 위치가 불변식이 되면 fix 는 영원히 자리를 옮긴다. 불변식은 **"무엇과 충돌하지 않는가"** 가 아니라 **"어느 표면에 참여하지 않는가"**(예: 인쇄 미디어에 참여하지 않는다)로 세워야 종료된다.
🔑 sweep 도 관점을 바꿔야 한다 — *"같은 결함이 더 있는가"* 가 아니라 **"내 변경이 이것들과 부딪히는가"**, 인쇄 결함이면 **"화면 전용 요소 중 인쇄물에 나타나는 것 전부"**.

### ③ 🚨 **양성 대조군은 "결함 부재"의 증거가 아니다**
#907 — 리뷰가 구매관리를 **양성 대조군**으로 제시했다(*"화면 8행 = Excel 8행"*). PM 이 그것을 근거로 fix 대상에서 뺐다. **계열 전수 sweep 이 뒤집었다** — 검색을 걸면 `화면 1건 / Excel 38행`. 양성 대조군은 **그 조건에서 결함이 안 드러난다는 증거**였을 뿐이다.
🔑 **양성 대조군으로 표면을 제외하지 말 것.** 대조군은 *측정이 유효함*을 보이는 장치이지 *그 표면이 건강함*을 보이는 장치가 아니다. 제외는 **sweep 이 그 표면을 실제로 조작해 본 뒤**에만.

---

## 3. 적응 규칙 — 언제 무엇으로 바꾸는가

**PM 이 r 을 보고 하네스를 바꿀 권한을 위임받았다.**

| 상태 (2라운드 연속) | 조치 |
|---|---|
| **c < 0.45** | 현행 유지 |
| **c ≥ 0.45 이고 r 이 낮다** | 유지 — 리뷰가 깊어진 것이지 fix 가 낳은 게 아니다. 발견이 소진되면 c 는 내려간다 |
| **c ≥ 0.45 이고 r ≥ 0.5** | 🔧 **fix 분할 동결** — 한 턴에 **뿌리 1개만** fix 한다. 턴 수는 늘지만 표면이 작아 c·r 이 함께 떨어진다 |
| **c ≥ 1 이 3라운드 연속** | PM 이 원인을 규명해 **개발책임자께 보고 후** 규칙 변경 |

### 🚫 "예전 규칙 복귀"는 fallback 이 아니다
개발책임자가 *"역효과면 예전 규칙대로 해도 좋다"* 고 허용했으나, **실측상 예전 규칙(전 심각도 0 + 검증품질 이월)이 더 나빴다** — 검증품질이 도달가능의 **2~9배**로 나와 PM 판정·게시·이월 결정을 전부 소모했고 **밤새 3트랙 머지 0** 이었다.
🔑 **r 이 안 떨어지는 원인은 리뷰 범위가 아니라 fix 부피다.** 되돌리면 노이즈만 돌아오고 r 은 그대로다. 그래서 fallback 을 **fix 분할 동결**로 잡는다.

### 📌 첫 발동 기록 — #909 (2026-07-24)
```
SOL 라운드   c=2.00  r=1.00   1회째
OPUS 라운드  c=1.00  r=1.00   2회째  -> 발동
```
조치 = **발견 2건 중 뿌리 1개(인쇄)만 fix**, 나머지 1개는 다음 턴으로 명시 이월(PR 에 공개 기록 — 숨김 아님).
⚠️ **관측** — #909 의 두 fix 는 각각 30줄·63줄로 **작았는데도** r=1.00 이었다. 즉 **부피가 아니라 "문제를 옮기기만 한 것"이 원인**이었다(위 2-B②). 분할 동결은 부피 레버라 이 실패 모드에 직접 듣지 않는다 — 함께 **불변식을 "표면 비참여"로 다시 세우는 것**이 실제 레버다. 다음 라운드 c·r 로 어느 쪽이 들었는지 판별할 것.

---

## 4. 3턴은 **권고**다 (2026-07-23 개발책임자 하향)

1턴 = `OPUS 라운드 + CODEX 라운드`(각 fix 포함). **재수렴까지 3턴을 지향하되 절대 규칙은 아니다.**
- 넘겨도 임의로 끌지 말고 **남은 도달가능 목록 + 각각의 흡수 난이도 + PM 권고**를 개발책임자께 올려 판단을 받는다.
- 3턴을 못 지키는 것 자체는 위반이 아니다. **r 을 기록하지 않고 원인 없이 라운드를 반복하는 것**이 위반이다.
- 참고 고정비(2026-07-23 실측): OPUS 표면분할 병렬 11~20분 · mock 게이트 11분 · CI 15분 · 라이브QA 3~6분 · fix 수십 분 ⟹ **1턴 ≈ 2~4시간**. 3트랙 병렬 시 6~12시간에 3슬라이스가 산술 상한.

## 5. 새 이슈 금지 (유지)
개발책임자 사전 허락 없이 새 이슈를 만들지 않는다. 흡수가 디폴트이고 기준은 파일 소유가 아니라 **흡수 비용**이다. → [[feedback_backlog_burndown_issue_bar]]

관련: [[feedback_canonical_workflow]] · [[feedback_pm_verify_what_measurement_proves]] · [[feedback_reconvergence_before_merge]]
