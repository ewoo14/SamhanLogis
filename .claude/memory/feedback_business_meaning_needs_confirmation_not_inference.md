---
name: feedback_business_meaning_needs_confirmation_not_inference
description: 🚨 측정 가능한 사실은 PM 이 재고, 업무 의미는 확인받는다 — 데이터·코드에서 업무 규칙을 추론해 브리핑에 넣으면 그 결함은 PM 이 만든 것 (2026-08-06 개발책임자)
metadata:
  type: feedback
---

# 🚨 **업무 의미는 추론하지 말고 확인받는다**

> 개발책임자 (2026-08-06)
> *"전제가 여러 번 틀렸다면 나한테 확인을 받아야 하는 것 아닌가? 함부로 추측한 것은 추후 설계를 다시 변경해야할 수 있어."*

## 한 세션에 다섯 번 틀렸고, 전부 같은 성격이었다

| PM 이 브리핑에 넣은 전제 | 실제 | 어떻게 틀렸나 |
|---|---|---|
| 세트 `bundle_component` 13행 = **13행 전개** | 계열별 **선택지** · `is_default` 로 4행이 정답 | 행이 있으면 다 쓰는 줄 알았다 |
| 전역DC `500` = **제품 결함** | **8일 낡은 배포본** (소스엔 매핑 존재) | 증상을 원인으로 단정 |
| `has_variable_discount = true` → **할인된다** | **원칙적 자격**일 뿐 · `categoryKey=singleSets` 엔 적용 율이 없다 | 불리언 이름에서 업무 규칙을 읽었다 |
| UUID `v3` = **구형 dev 시드** 표식 | `UUID.nameUUIDFromBytes`(MD5) 결과 — **시드 세대와 무관** | 기술 표식으로 정체를 단정 |
| `RegionalService` 의 **17개 시도 방식이 틀렸다** | 선별은 이미 `deliveryTag=REGION` · 시도는 **표시 그룹 키** | 메모리 규칙을 코드 확인 없이 적용 |

🔑 **전부 "데이터·코드에서 업무 의미를 추론" 한 것이다.** 다섯 번 다 검증자가 반려해 살았지만, **반려되지 않았으면 그대로 설계에 박혔다.**

## 🆕 2026-08-07 여섯 번째 — **근거는 옳았는데 "어느 화면인가" 를 안 물었다**

`#1077` 이 데스크톱 견적·판매전표에서 세트 옵션 picker 를 지운 것을 PM 이 **회귀로 판정**하고
`#1078` 안에서 복원(S34)까지 시켰다. 근거는 **레거시 원문 직독**이었고 그 자체는 정확했다.

```text
tools/legacy-gas/종합견적서/index.html:7413-7418   SHA-256 대조까지 마침
  renderSingleOptions() 가 견적 화면 #singleOpts 에 옵션 picker 를 생성한다   ← 사실이다
```

개발책임자 정정:
> *"여기서 말하는 옵션은 **웹 종합견적서 및 주문서**를 말함. **그 외는 없애는게 맞음**"*

그리고 **이미 이슈 `#896` 본문에 적혀 있었다**:
```text
"데스크톱 폼에는 이 로직이 없습니다 — 본 이슈는 레거시 GAS 앱(웹 주문서·견적서) 전용입니다.
 데스크톱/모바일은 범위 밖입니다."
```

### 무엇이 새로운가

앞의 다섯은 *"근거가 약한데 단정"* 이었다. 이번은 **근거가 강했다** — 레거시 원문 · 줄 번호 · SHA-256.
틀린 것은 **그 근거가 어느 화면에 적용되는가**였다.

🔑 **레거시 원문 직독은 "무엇을 하는가" 만 답한다. "우리 어느 화면이 그것을 계승하는가" 는 답하지 않는다.**
   레거시는 GAS 앱(웹 견적서·주문서)이고 우리는 데스크톱·모바일·웹이 따로 있다. **1:1 이 아니다.**

### How to apply

```text
레거시를 근거로 쓸 때 두 가지를 함께 확정한다
  ① 레거시의 **어느 문서·앱**인가        (종합견적서 / 주문서 / 거래처 발송 주문서 / …)
  ② 우리의 **어느 화면**이 그 계승자인가  ← 이것을 안 물으면 엉뚱한 화면에 이식한다

🚨 착수 전 **관련 이슈 본문을 먼저 읽는다.** 이번 답은 #896 본문에 이미 있었다.
   "이는 이미 이슈로 요청한 사항" 이라는 정정을 두 번 받았다.
```

이것은 [[feedback_freight_cutting_amount_first_entry]] 의 *"어느 레거시 문서인지 먼저 확정할 것"*
과 같은 계열의 **두 번째**다. 그때는 문서를 안 봤고, 이번은 문서는 봤으나 **대응 화면**을 안 물었다.

## 규칙 — 두 종류를 섞지 않는다

```text
측정 가능한 사실        PM 이 직접 잰다. 확인받지 않는다.
  행 수 · 채움률 · 배포 생성 시각 · CI 결과 · git diff · 파일 존재 · 참조 grep

업무 의미               추론하지 않는다. 셋 중 하나로만 확정한다.
  ① 레거시 원문을 **PM 이 직접 읽는다**
  ② 코드의 명시적 계약(Javadoc·계약 테스트·enum 정의)을 **직접 읽는다**
  ③ 개발책임자께 **확인받는다**
  예: 이 컬럼이 무엇을 뜻하나 · 이게 결함인가 · 무엇이 정본인가 ·
      어느 것이 권위 원본인가 · 이 동작이 의도된 것인가
```

## 특히 위험한 형태

- **후보를 권위로 승격**하는 것. 조사 보고서가 *"후보 A~E, 확정하지 않음"* 으로 올린 것을 PM 이 하나 골라 브리핑에 *"권위 원본은 X 다"* 로 쓰면, 그 선택이 설계에 박힌다. **고르는 것은 개발책임자 몫이다.**
- **"이 방향이 맞는지도 당신이 판단하십시오"** 로 헤지하는 것. 구현자는 대개 주어진 방향으로 간다. 헤지는 확인이 아니다.
- 메모리 규칙을 **해당 코드를 안 읽고** 적용하는 것. 규칙은 다른 맥락에서 만들어졌을 수 있다.

## How to apply

브리핑에 문장을 쓰기 전에 자문한다 — **"이 문장은 내가 잰 것인가, 내가 해석한 것인가?"**

```text
잰 것    그대로 쓴다. SQL·명령 원문을 함께 넣는다
해석한 것 ①레거시 원문 ②명시적 계약 ③개발책임자 확인 — 셋 중 하나를 붙이거나,
          붙일 수 없으면 **브리핑에 넣지 말고 질문으로 올린다**
```

그리고 **이미 설계에 박힌 추측은 소급해서 올린다.** 되돌리는 비용이 라운드 하나보다 크다.

관련: [[feedback_pm_verifies_round_and_directs_next_fix]] · [[feedback_pm_gives_coordinates_not_means]] · [[feedback_pm_means_instruction_creates_defect]] · [[feedback_conflict_is_mostly_one_sided_blank]] · [[feedback_gas_full_inheritance_definition]]
