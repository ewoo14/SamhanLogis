# (주)삼한공조시스템 자체 물류 프로그램 - 종합 개발 기획서

> **프로젝트명**: SamhanLogis (삼한로지스)
> **작성일**: 2026-05-01
> **버전**: v2.0 (팀 조직 개편 + 프로세스 강화)
> **상태**: 🟢 대표 승인 완료 — Phase 1 착수 대기

---

## 1. 프로젝트 개요

### 1.1 배경
- (주)삼한공조시스템은 삼성 시스템에어컨 공식 파트너사
- 현재 이카운트 + 구글 스크립트(견적서/주문서)로 업무 수행 중
- 자체 물류 프로그램(회계, 그룹웨어, 대시보드)으로 행정 전환 목표

### 1.2 핵심 요구사항 요약
| 구분 | 내용 |
|------|------|
| 아키텍처 | MSA, 분산 Docker 서버, **서비스별 독립 DB**, Eureka 기반 게이트웨이/로드밸런싱 |
| 배포 형태 | 내부용: EXE(Electron), 외부 거래처용: 웹/앱 |
| 조직 구조 | 대표실 → 사무실(행정) / 물류창고(초월·상일·가상창고) / 거래처 |
| 권한 체계 | 마스터, 개발자, 매니저, 영업원, 회계원, 창고원, 재고원 (7단계 세분화) |

---

## 2. 시스템 아키텍처

### 2.1 전체 아키텍처 개요

```
┌─────────────────────────────────────────────────────────┐
│                    클라이언트 계층                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ Desktop  │  │   Web    │  │  Mobile  │              │
│  │(Electron)│  │ (React)  │  │(RN/앱)   │              │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘              │
└───────┼──────────────┼─────────────┼────────────────────┘
        │              │             │
┌───────┴──────────────┴─────────────┴────────────────────┐
│                   게이트웨이 계층                         │
│  ┌──────────────┐  ┌──────────────┐                     │
│  │ Eureka Server│  │  API Gateway │                     │
│  │ (Discovery)  │  │(Spring Cloud)│                     │
│  └──────────────┘  └──────┬───────┘                     │
└────────────────────────────┼────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────┐
│                    서비스 계층                            │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐         │
│  │ Auth │ │ User │ │ Prod │ │ Inv  │ │ Slip │         │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘         │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐         │
│  │ Acct │ │ Part │ │ GrpW │ │ Noti │ │  Log │         │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘         │
│  ┌──────┐                                               │
│  │ Dash │                                               │
│  └──────┘                                               │
└────────────────────────────┼────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────┐
│                    데이터 계층                            │
│  ┌──────────┐ ┌──────┐ ┌────────────┐ ┌──────┐        │
│  │PostgreSQL│ │Redis │ │Elasticsrch │ │MinIO │        │
│  │(독립DB)  │ │Cache │ │  (Logs)    │ │(File)│        │
│  └──────────┘ └──────┘ └────────────┘ └──────┘        │
│  ┌──────────┐                                           │
│  │ RabbitMQ │                                           │
│  └──────────┘                                           │
└─────────────────────────────────────────────────────────┘
```

### 2.2 마이크로서비스 목록 (13개)

| # | 서비스명 | 기술 | 독립 DB | 핵심 기능 |
|---|---------|------|--------|----------|
| 1 | API Gateway | Spring Cloud Gateway | - | 라우팅, 인증 필터, Rate Limiting |
| 2 | Eureka Server | Spring Cloud Netflix | - | 서비스 디스커버리, 로드밸런싱 |
| 3 | Auth Service | Spring Boot + JWT | auth_db | 로그인, 토큰, 권한 검증 |
| 4 | User Service | Spring Boot | user_db | 사용자 CRUD, 조직도, 역할 |
| 5 | Product Service | Spring Boot | product_db | 품목, UUID, 태그/스펙 |
| 6 | Inventory Service | Spring Boot | inventory_db | 창고 재고, FIFO, UUID 추적, **창고 간 재고 이동** |
| 7 | Slip Service | Spring Boot | slip_db | 전표 CRUD, 수정이력, 번호관리, **이동전표** |
| 8 | Accounting Service | Spring Boot | accounting_db | 입출금, 잔액, 세금계산서, 원장, **홈택스 API**, **오픈뱅킹 API** |
| 9 | Partner Service | Spring Boot | partner_db | 거래처 관리, 거래처 원장, **링크 공유 주문** |
| 10 | Groupware Service | Spring Boot | groupware_db | 메신저, 일정, 전자결재 |
| 11 | Notification Service | Spring Boot | Redis | 푸시, 알림, WebSocket |
| 12 | Logging Service | Spring Boot | Elasticsearch | 전체 감사로그, 작업 추적 |
| 13 | Dashboard Service | Spring Boot | dashboard_db | 집계, 통계, 리포트 |
| 14 | Migration Service | Spring Boot | migration_db | **이카운트 데이터 마이그레이션, 음수 수량 변환** |

---

## 3. 핵심 기능 상세 설계

### 3.1 전표 관리 시스템

#### 전표 번호 체계
- 형식: `YYYY/MM/DD - {전표번호}` (내림차순 정렬)
- 모든 전표에 UUID 부여

#### 출고일 변경 로직
1. 출고일 변경 시 → 새 날짜에서 마지막 번호+1 부여
2. 기존 날짜의 전표번호 → 자동 폐기 처리
3. 원래 날짜로 복원 시 → UUID 기반 선택권 제공:
   - **(A)** 기존 번호 복원
   - **(B)** 해당 날짜 마지막 번호+1 새로 부여

#### 수정 이력 추적 (Google Docs 스타일)
- 최초 저장 후 수정 시 수정 카운트 증가 (1차, 2차, ... N차)
- 수정 전 데이터: 취소선 표시
- 수정 후 데이터: 수정자별 고유 색상으로 표시
- 우측에 수정자 정보 + 색상 범례 표시
- 수정 이력 전체 조회 및 특정 시점 복원 기능

#### 전표 종류
- 출고전표, 입고전표, 입금전표, 출금전표, **이동전표**

#### 전표 상태 라이프사이클

전표는 아래 상태를 순서대로 거치며, 각 상태에 따라 수정 가능 여부가 결정됨:

```
[작성중] → [저장완료] → [전송완료] → [수락] → [처리중] → [처리완료]
   │          │            │                        │
   │          │            └── 반려 → [작성중]       └── [배송중] → [배송완료] → [확정]
   │          │                                          │
   │          └── 수정 가능 (수정 이력 카운트 증가)         └── 서명 링크 발행
   │                                                         (배송기사 + 인수자 서명)
   └── 자유 수정 (이력 미기록)
```

| 상태 | 수정 가능 | 설명 |
|------|----------|------|
| 작성중 | ✅ 자유 수정 | 최초 작성, 수정 이력 미기록 |
| 저장완료 | ✅ 이력 기록 수정 | 저장 후 수정 시 수정 카운트 증가 |
| 전송완료 | ⚠️ 긴급 수정만 | 창고로 전송 완료, 일반 수정 불가 |
| 수락 | 🔒 **수정 불가** | 창고 직원이 전표 수락, **데이터 락 발동** |
| 처리중 | 🔒 **수정 불가** | 출고 작업 진행 중 |
| 처리완료 | 🔒 **수정 불가** | 출고 완료, **서명 링크 자동 발행** |
| 배송중 | 🔒 **수정 불가** | 배송 진행 중, 서명 대기 |
| 배송완료 | 🔒 **수정 불가** | 배송기사 + 인수자 **전자서명 2건 완료** |
| 확정 | 🔒 **수정 불가** | 최종 확정 (세금계산서 연계 가능) |

#### 데이터 락킹 메커니즘

**목표**: 창고 직원이 전표를 수락하고 출고 처리 시 해당 전표의 수정을 즉시 차단

##### 락킹 전략 (2단계)

**1단계: 낙관적 락킹 (Optimistic Locking)**
- 전표 엔티티에 `version` 필드 적용
- 동시 수정 시도 시 버전 충돌 감지 → 나중 수정 거부
- 작성중/저장완료 상태에서 적용

**2단계: 비관적 락킹 (Pessimistic Locking)**
- 전표가 `수락` 이상 상태로 전환되면 DB 레벨 Row Lock 적용
- 수정 API 호출 시 상태 체크 → 즉시 `403 Forbidden` 반환
- Redis 분산 락으로 다중 서버 환경에서도 동기화 보장

```
[영업원 수정 시도]
    │
    ▼
상태 확인 ──→ 작성중/저장완료? ──→ 수정 허용 (낙관적 락)
    │
    └──→ 전송완료 이상? ──→ 🔒 수정 차단
                              │
                              └──→ "긴급 수정 요청" 버튼만 표시
```

##### 실시간 동기화 (WebSocket + SSE)
- 전표 상태 변경 시 **WebSocket으로 즉시 브로드캐스트**
- 영업원 화면: 전표가 `수락`됨 → 실시간으로 수정 버튼 비활성화
- 창고원 화면: 새 전표 전송 → 실시간 푸시 알림 + 목록 자동 갱신
- 네트워크 지연 대비 **Polling 폴백** (3초 간격)

#### 긴급 수정 요청 워크플로우

전표가 `수락` 이후 상태일 때 영업원이 수정이 필요한 경우:

```
[영업원]                    [시스템]                  [창고원/매니저]
   │                          │                          │
   ├── 긴급 수정 요청 ───────→│                          │
   │   (수정 사유 + 변경내용)  │                          │
   │                          ├── 알림 전송 ────────────→│
   │                          │   (푸시 + WebSocket)      │
   │                          │                          │
   │                          │←── 승인/반려 ────────────┤
   │                          │                          │
   │   ┌──────────────────────┤                          │
   │   │                      │                          │
   │   ▼                      │                          │
   │ [승인 시]                 │                          │
   │  · 전표 상태 → 일시해제   │                          │
   │  · 30분 타임아웃 설정     │                          │
   │  · 수정 완료 → 재락킹    │                          │
   │                          │                          │
   │ [반려 시]                 │                          │
   │  · 반려 사유 전달         │                          │
   │  · 전표 상태 유지 (락 유지)│                         │
```

##### 긴급 수정 요청 규칙
1. **수정 요청 시 필수 입력**: 수정 사유, 변경할 항목 + 변경 내용 미리보기
2. **승인 권한**: 해당 전표를 수락한 창고원 또는 매니저 이상
3. **타임아웃**: 승인 후 **30분** 내 수정 미완료 시 자동 재락킹
4. **이력 기록**: 긴급 수정 요청/승인/반려 전체 감사 로그 기록
5. **처리완료/확정 상태**: 긴급 수정 불가, 별도 **정정 전표** 발행으로 대체
6. **실시간 알림**: 요청 즉시 창고원 모바일/데스크톱에 푸시


### 3.2 재고 관리 (FIFO + UUID)

- 모든 물품에 고유 UUID 부여
- 입고 시 UUID 생성 + 타임스탬프 기록
- 출고 시 가장 오래된 UUID부터 자동 선택 (FIFO)
- 물품 이동 이력 전체 추적 가능
- 창고 구분:
  - **초월창고**: 자체 소유, 내부 물류 적재/재고관리/입출고 (창고장 중심 팀)
  - **상일창고**: 임대창고
  - **온라인창고 / 서초창고**: 삼성 직배건 처리 가상창고

### 3.3 배송 태그 시스템

| 태그 | 입/출고 | 자동 규칙 |
|------|--------|----------|
| 당일 | 출고 | - |
| 야적 | 출고 | 특이사항에 `{출고일}상차 {다음날}하차` 자동 태깅 |
| 지방 | 출고 | 특이사항에 `{출고일}상차 {다음날}하차` 자동 태깅 |
| 로젠택배 | 출고 | - |
| 경동택배 | 출고 | - |
| 경동화물 | 출고 | - |
| 회차 | **입고** | - |
| 반품 | **입고** | - |
| 차용 | **입고** | - |
| 대여 | 출고 | - |
| 반납 | 출고 | - |

### 3.4 권한 체계 (7단계)

| 역할 | 접근 범위 | 비고 |
|------|----------|------|
| 마스터 | 전체 시스템 | 권한 조정 유일 담당 |
| 개발자 | 마스터 필적 | 마스터 승인 필요 |
| 매니저 | 위임 권한 범위 | 마스터가 위임할 권한 설정 |
| 영업원 | 견적서, 발주, 출고전표 | - |
| 회계원 | 입출금, 세금계산서, 원장 | - |
| 창고원 | 물류 입출고 처리 | - |
| 재고원 | 창고원 + 재고 심화 | 더 넓은 재고 권한 |

- 각 역할 내에서도 세부 접근 범위 분할 가능
- 권한 조정은 **오로지 마스터만** 가능

### 3.5 품목 관리
- 품목별 고유 UUID
- 필드: 품목명, 모델명, 출고가, 납품가
- 상세 스펙: 태그 형식 추가, 견적서에서 조회 가능

### 3.6 회계 시스템
- 법인계좌 입출금 내역/잔액 조회 (**오픈뱅킹 API 직접 연동**)
- 세금계산서 일괄/개별 발행 (**홈택스 API 직접 연동**)
- 거래처별 원장 관리
- 권한별 데이터 표시 차등

#### 한국 표준 계정과목 체계 (일반기업회계기준 준거)

> ⚠️ **회계 처리에서 매우 중요하므로 각별히 주의하여 적용할 것**
> 아래 계정과목은 한국 세무서(국세청)에서 인정하는 일반기업회계기준에 따른 표준 분류이며,
> 시스템 초기 세팅 시 전체 삽입 필수. 추후 회사 실정에 맞게 추가/세분화 가능.

##### 1. 자산 (100번대)

| 분류 | 코드 | 계정과목 |
|------|------|----------|
| **유동자산 — 당좌자산** | 101 | 현금 |
| | 102 | 보통예금 |
| | 103 | 당좌예금 |
| | 104 | 정기예금 |
| | 105 | 단기투자자산 |
| | 108 | 외상매출금 (매출채권) |
| | 109 | 받을어음 |
| | 110 | 미수금 |
| | 111 | 미수수익 |
| | 112 | 선급금 |
| | 113 | 선급비용 |
| | 114 | 가지급금 |
| | 115 | 부가세대급금 |
| | 116 | 대손충당금 (차감) |
| **유동자산 — 재고자산** | 120 | 상품 |
| | 121 | 제품 |
| | 122 | 반제품 |
| | 123 | 재공품 |
| | 124 | 원재료 |
| | 125 | 부재료 |
| | 126 | 저장품 |
| | 127 | 미착품 |
| **비유동자산 — 투자자산** | 131 | 장기투자증권 |
| | 132 | 장기대여금 |
| | 133 | 투자부동산 |
| **비유동자산 — 유형자산** | 141 | 토지 |
| | 142 | 건물 |
| | 143 | 구축물 |
| | 144 | 기계장치 |
| | 145 | 차량운반구 |
| | 146 | 비품 |
| | 147 | 건설중인자산 |
| | 148 | 감가상각누계액 (차감) |
| **비유동자산 — 무형자산** | 151 | 영업권 |
| | 152 | 산업재산권 (특허권/상표권) |
| | 153 | 소프트웨어 |
| | 154 | 개발비 |
| **기타비유동자산** | 161 | 임차보증금 |
| | 162 | 장기선급비용 |
| | 163 | 이연법인세자산 |

##### 2. 부채 (200번대)

| 분류 | 코드 | 계정과목 |
|------|------|----------|
| **유동부채** | 201 | 외상매입금 (매입채무) |
| | 202 | 지급어음 |
| | 203 | 단기차입금 |
| | 204 | 미지급금 |
| | 205 | 미지급비용 |
| | 206 | 선수금 |
| | 207 | 예수금 |
| | 208 | 부가세예수금 |
| | 209 | 미지급법인세 |
| | 210 | 유동성장기부채 |
| | 211 | 미지급배당금 |
| **비유동부채** | 221 | 장기차입금 |
| | 222 | 사채 |
| | 223 | 퇴직급여충당부채 |
| | 224 | 장기미지급금 |
| | 225 | 임대보증금 |
| | 226 | 이연법인세부채 |

##### 3. 자본 (300번대)

| 분류 | 코드 | 계정과목 |
|------|------|----------|
| **자본금** | 301 | 보통주자본금 |
| | 302 | 우선주자본금 |
| **자본잉여금** | 311 | 주식발행초과금 |
| | 312 | 기타자본잉여금 |
| **자본조정** | 321 | 자기주식 (차감) |
| **기타포괄손익누계액** | 331 | 매도가능증권평가이익 |
| **이익잉여금** | 341 | 이익준비금 |
| | 342 | 임의적립금 |
| | 343 | 미처분이익잉여금 |

##### 4. 수익 (400번대)

| 분류 | 코드 | 계정과목 |
|------|------|----------|
| **매출** | 401 | 상품매출 |
| | 402 | 제품매출 |
| | 403 | 공사수익 |
| | 404 | 용역매출 |
| | 405 | 매출에누리/환입/할인 (차감) |
| **영업외수익** | 901 | 이자수익 |
| | 902 | 배당금수익 |
| | 903 | 임대료수익 |
| | 904 | 유형자산처분이익 |
| | 905 | 외환차익 |
| | 906 | 외화환산이익 |
| | 907 | 잡이익 |

##### 5. 비용 (500/800/900번대)

| 분류 | 코드 | 계정과목 |
|------|------|----------|
| **매출원가** | 501 | 기초상품재고액 |
| | 502 | 당기상품매입액 |
| | 503 | 기말상품재고액 (차감) |
| | 510 | 기초제품재고액 |
| | 511 | 당기제품제조원가 |
| | 512 | 기말제품재고액 (차감) |
| **판매비와관리비** | 801 | 급여 |
| | 802 | 퇴직급여 |
| | 803 | 복리후생비 |
| | 804 | 여비교통비 |
| | 805 | 접대비 |
| | 806 | 통신비 |
| | 807 | 수도광열비 |
| | 808 | 세금과공과 |
| | 809 | 감가상각비 |
| | 810 | 지급임차료 |
| | 811 | 수선비 |
| | 812 | 보험료 |
| | 813 | 차량유지비 |
| | 814 | 운반비 |
| | 815 | 교육훈련비 |
| | 816 | 도서인쇄비 |
| | 817 | 소모품비 |
| | 818 | 지급수수료 |
| | 819 | 광고선전비 |
| | 820 | 대손상각비 |
| | 821 | 무형자산상각비 |
| | 822 | 잡비 |
| **영업외비용** | 951 | 이자비용 |
| | 952 | 유형자산처분손실 |
| | 953 | 외환차손 |
| | 954 | 외화환산손실 |
| | 955 | 기부금 |
| | 956 | 잡손실 |
| **법인세비용** | 991 | 법인세비용 |

> 위 계정과목은 시스템 초기 구축 시 **시드 데이터**로 전체 삽입하며,
> 회사 운영 중 필요에 따라 계정과목을 추가할 수 있으나 **코드 체계는 유지**한다.

#### 거래처 채권 관리 시스템

> ⚠️ **입금 처리는 회계권한자(회계원) 이상만 가능**

##### 채권 데이터 구조

거래처마다 **채권(매출채권) 데이터**를 자동 생성·관리한다.
출고전표 확정 시 해당 금액이 채권에 자동 반영되고, 입금 완료 시 차감된다.

| 필드 | 타입 | 설명 |
|------|------|------|
| receivableId | UUID | 채권 고유 ID |
| partnerId | UUID | 거래처 ID (FK) |
| slipId | UUID | 관련 전표 ID (FK, 출고전표) |
| receivableDate | DATE | 채권 발생일 (출고일) |
| amount | DECIMAL | 채권 금액 |
| paidAmount | DECIMAL | 입금 완료 금액 |
| remainingAmount | DECIMAL | 잔액 (amount - paidAmount, 계산 필드) |
| status | ENUM | `OPEN` / `PARTIAL` / `PAID` / `OVERDUE` |

##### 입금 처리 및 입금보고서 워크플로우

```
[입금 확인]                 [회계권한자]              [시스템]
    │                          │                      │
    ├── 입금 내역 확인 ─────────→│                      │
    │   (오픈뱅킹 자동 매칭      │                      │
    │    또는 수동 입력)         │                      │
    │                          │                      │
    │                          ├── 거래처/전표 매칭     │
    │                          ├── 입금 금액 입력       │
    │                          ├── 입금 처리 확정 ──────→│
    │                          │                      │
    │                          │                      ├── 입금보고서 자동 생성
    │                          │                      ├── 채권 잔액 차감
    │                          │                      ├── 거래처 원장 반영
    │                          │                      └── 감사 로그 기록
```

##### 입금보고서 구조

| 필드 | 설명 |
|------|------|
| 보고서 ID | UUID |
| 거래처명 | 입금한 거래처 |
| 입금일 | 실제 입금 날짜 |
| 입금액 | 입금 금액 |
| 입금 방법 | 계좌이체 / 현금 / 어음 등 |
| 매칭 전표 | 해당 입금이 매칭된 출고전표 목록 |
| 처리자 | 회계권한자 ID |
| 비고 | 특이사항 |

> 입금보고서 작성 완료 시 → 해당 거래처의 채권에 입금 기록 자동 반영

##### 거래처 채권 조회 화면

거래처 채권을 조회하면 **날짜별로 전표 출고 내용, 금액, 입금 내역, 금액이 모두 표시**되고,
하단에 **남은 잔액**이 요약 표시된다.

```
┌──────────────────────────────────────────────────────────────────┐
│ 거래처: ○○전기  │ 총 채권: ₩15,000,000 │ 입금 합계: ₩8,000,000  │
│                 │ 잔액: ₩7,000,000                              │
├──────┬─────────┬──────────┬──────────┬──────────┬───────────────┤
│ 날짜 │ 구분    │ 전표번호 │ 내용     │ 금액     │ 잔액          │
├──────┼─────────┼──────────┼──────────┼──────────┼───────────────┤
│ 4/01 │ 출고    │ S-0401-1 │ 에어컨 3 │ +5,000,000 │ 5,000,000   │
│ 4/10 │ 출고    │ S-0410-2 │ 에어컨 2 │ +3,000,000 │ 8,000,000   │
│ 4/15 │ 입금    │ P-0415-1 │ 계좌이체 │ -5,000,000 │ 3,000,000   │
│ 4/20 │ 출고    │ S-0420-1 │ 실외기 4 │ +7,000,000 │ 10,000,000  │
│ 4/25 │ 입금    │ P-0425-1 │ 계좌이체 │ -3,000,000 │ 7,000,000   │
├──────┴─────────┴──────────┴──────────┴──────────┼───────────────┤
│                                       남은 잔액 │ ₩7,000,000   │
└─────────────────────────────────────────────────┴───────────────┘
```

> ⚠️ 채권 데이터도 **BaseEntity** 상속 (감사 필드 7종 포함)

### 3.8 거래처 주문 시스템
- **링크 공유 방식**: 공식 홈페이지 도메인 사용 (예: `order.samhan.co.kr/{token}`)
- 거래처별 고유 주문 링크 생성 → 링크 접속 시 주문 페이지 표시
- 로그인 불필요, 토큰 기반 인증으로 간편 접근
- 주문 완료 시 내부 시스템에 자동 반영 → 출고전표 생성 연계

### 3.8-A 그룹웨어 — 전자결재 시스템

#### 결재라인 체계

| 규칙 | 설명 |
|------|------|
| **최종 결재자** | 모든 기안서의 결재라인 최종은 반드시 **마스터(대표)**의 결재가 포함되어야 함 |
| **결재라인 설정 권한** | **대표실(대표, 전무)만** 결재라인을 설정·변경할 수 있음 |
| **결재 순서** | 기안자 → 중간 결재자(들) → **마스터 최종 결재** |
| **결재라인 템플릿** | 대표실이 부서/문서유형별 결재라인 템플릿을 사전 등록 |

#### 결재 프로세스

```
[기안자]               [중간 결재자]            [마스터(대표)]           [시스템]
   │                      │                      │                    │
   ├── 기안서 작성         │                      │                    │
   ├── 결재라인 자동 적용  │                      │                    │
   ├── 기안 상신 ─────────→│                      │                    │
   │                      ├── 검토                │                    │
   │                      ├── 승인 ──────────────→│                    │
   │                      │   (전자서명 자동 입력)  │                    │
   │                      │                      ├── 최종 검토         │
   │                      │                      ├── 최종 승인 ────────→│
   │                      │                      │  (전자서명 자동 입력) │
   │                      │                      │                    ├── 결재 완료 처리
   │                      │                      │                    ├── 전자서명 이미지 삽입
   │                      │                      │                    └── 감사 로그 기록
```

#### 전자서명 시스템

| 항목 | 내용 |
|------|------|
| 서명 등록 | 각 결재자가 사전에 **전자서명 이미지**를 등록 (터치/업로드) |
| 자동 입력 | 결재 승인 시 등록된 서명이 기안서에 **자동으로 삽입** |
| 서명 위치 | 기안서 내 결재란의 해당 결재자 칸에 서명 이미지 표시 |
| 결재 취소 | 최종 결재 전까지 중간 결재자 취소 가능 (서명 자동 제거) |

#### 전자서명 보안 (강화보안)

| # | 보안 항목 | 구현 방법 |
|---|----------|----------|
| 1 | **서명 이미지 암호화** | AES-256-GCM으로 서명 이미지 저장 시 암호화, 표시 시에만 복호화 |
| 2 | **접근 제어** | 서명 데이터는 **본인만 등록/수정/삭제** 가능 (마스터도 타인 서명 수정 불가) |
| 3 | **서명 위변조 방지** | 서명 등록 시 **SHA-256 해시값** 생성 → 사용 시마다 해시 검증 |
| 4 | **서명 사용 로그** | 서명이 사용될 때마다 사용 일시, 문서 ID, IP를 감사 로그에 기록 |
| 5 | **2FA 인증** | 서명 등록/변경 시 **2단계 인증** 필수 (SMS 인증코드) |
| 6 | **세션 격리** | 서명 이미지는 클라이언트 메모리에 캐시하지 않음, 매번 서버에서 복호화하여 전송 |
| 7 | **전송 보안** | 서명 데이터 전송 시 별도 암호화 채널 사용 (TLS 1.3 + 추가 페이로드 암호화) |
| 8 | **서명 이미지 격리 저장** | MinIO 내 별도 격리 버킷에 저장, 서비스 계정만 접근 가능 |

#### 서명 데이터 저장

| 필드 | 타입 | 설명 |
|------|------|------|
| signatureId | UUID | 서명 고유 ID |
| userId | UUID | 소유자 ID (FK) |
| signatureImageEncrypted | BLOB (MinIO) | AES-256 암호화된 서명 이미지 |
| signatureHash | VARCHAR | SHA-256 해시값 (위변조 검증용) |
| encryptionKeyId | VARCHAR | 암호화 키 ID (HSM/Vault 참조) |
| registeredAt | TIMESTAMP | 등록 일시 |
| lastUsedAt | TIMESTAMP | 마지막 사용 일시 |

> ⚠️ 서명 데이터도 **BaseEntity** 상속 (감사 필드 7종 포함)

### 3.9 모바일 앱 (듀얼 구성)

#### 거래처용 앱
- 주문 링크 접속 → 주문서 작성/조회
- 배송 현황 추적
- 주문 이력 확인

#### 내부 직원용 앱
- **권한별 메뉴 차등 표시** (역할에 따라 접속 가능 메뉴가 다름)
- 창고원: 출고전표 수신 → 확인 → 출고 처리 (핵심 워크플로우)
- 영업원: 견적/주문 현황 조회, 긴급 전표 확인
- 매니저: 승인 대기 항목 처리, 현황 대시보드
- 알림 푸시 (전표 도착, 승인 요청, **결재 요청** 등)

### 3.10 이카운트 데이터 마이그레이션

#### 마이그레이션 대상
- 품목 마스터 데이터
- 거래처 정보
- 입출고 이력
- 재고 현황
- 회계 데이터 (입출금, 세금계산서)

#### 음수 수량 변환 규칙
이카운트에서는 음수 수량으로 반대 방향 처리를 하지만, SamhanLogis에서는 명시적 전표 분리:

| 이카운트 원본 | 변환 후 (SamhanLogis) |
|-------------|---------------------|
| 출고전표 수량 < 0 (마이너스 출고) | → **입고전표** 생성 (양수 수량) |
| 입고전표 수량 < 0 (마이너스 입고) | → **출고전표** 생성 (양수 수량, 삼성 착하판정 회수 등) |

#### 변환 프로세스
1. 이카운트 데이터 CSV/API 추출
2. 데이터 정합성 검증 (누락, 불일치 확인)
3. 음수 수량 레코드 분류 및 전표 유형 변환
4. UUID 신규 발행 + 기존 데이터 매핑 테이블 생성
5. 스테이징 DB 적재 → 검증 → 프로덕션 반영
6. 변환 리포트 생성 (원본 vs 변환 결과 대조표)

### 3.11 창고 간 재고 이동 (Stock Transfer)

#### 개요
창고 간 재고를 이동할 때 **이동전표**를 발행하여 출발 창고에서 출고, 도착 창고에서 입고를 하나의 트랜잭션으로 처리한다.

#### 이동 가능 경로
모든 창고 간 양방향 이동 가능:

| # | 출발 창고 | 도착 창고 | 비고 |
|---|----------|----------|------|
| 1 | 초월창고 | 상일창고 | 실물 이동 (차량 배송) |
| 2 | 상일창고 | 초월창고 | 실물 이동 (차량 배송) |
| 3 | 초월창고 | 온라인창고/서초창고 | 가상 이동 (삼성 직배건 전환) |
| 4 | 상일창고 | 온라인창고/서초창고 | 가상 이동 (삼성 직배건 전환) |
| 5 | 온라인창고/서초창고 | 초월창고 | 가상 → 실물 전환 |
| 6 | 온라인창고/서초창고 | 상일창고 | 가상 → 실물 전환 |

#### 이동 사유 체계
이동전표 작성 시 **이동 사유** 필수 선택:

| 사유 코드 | 사유명 | 설명 |
|----------|--------|------|
| `REBALANCE` | 재고 재배치 | 창고 간 재고 균형 조정 |
| `URGENT` | 긴급 출고 대응 | 특정 창고에서 긴급 출고 필요 시 타 창고에서 이동 |
| `CONSOLIDATE` | 재고 통합 | 분산된 재고를 하나의 창고로 집중 |
| `MAINTENANCE` | 창고 정비 | 창고 정비/점검으로 인한 임시 이동 |
| `SAMSUNG_DIRECT` | 삼성 직배건 전환 | 삼성 직배건 처리를 위한 가상창고 이동 |
| `OTHER` | 기타 | 직접 입력 (사유 상세 필수) |

#### 이동전표 상태 라이프사이클

```
[이동요청] → [승인대기] → [승인완료] → [출고처리] → [운송중] → [입고완료] → [이동확정]
    │           │                                                    │
    │           └── 반려 → [이동요청] (수정 후 재요청)                  │
    │                                                                │
    └── 취소 가능 (승인 전)                                     최종 확정 (수정 불가)
```

| 상태 | 설명 | 수정 가능 |
|------|------|----------|
| 이동요청 | 이동전표 작성, 사유 입력 | ✅ 자유 수정 |
| 승인대기 | 창고장(출발 창고) 승인 대기 | ⚠️ 요청자만 취소 가능 |
| 승인완료 | 출발 창고 창고장 승인 | 🔒 수정 불가 |
| 출고처리 | 출발 창고에서 물품 출고 완료 | 🔒 수정 불가, 출발 창고 재고 차감 |
| 운송중 | 실물 이동 중 (가상창고 이동 시 즉시 스킵) | 🔒 수정 불가 |
| 입고완료 | 도착 창고에서 물품 입고 확인 | 🔒 수정 불가, 도착 창고 재고 증가 |
| 이동확정 | 최종 확정, 감사 로그 기록 | 🔒 수정 불가 |

#### 승인 프로세스

```
[요청자]                  [출발 창고장]              [도착 창고장]           [시스템]
   │                          │                        │                    │
   ├── 이동전표 작성 ─────────→│                        │                    │
   │   (출발/도착 창고,         │                        │                    │
   │    품목, 수량, 사유)       │                        │                    │
   │                          │                        │                    │
   │                          ├── 재고 확인             │                    │
   │                          ├── 승인/반려 ─────────────────────────────────→│
   │                          │                        │                    │
   │                          │   [승인 시]             │                    │
   │                          │                        │                    ├── 알림 전송
   │                          ├── 출고 처리             │                    │   (도착 창고장에게)
   │                          │                        │                    │
   │                          │                        ├── 입고 확인         │
   │                          │                        ├── 입고 완료 ───────→│
   │                          │                        │                    ├── 이동 확정
   │                          │                        │                    ├── 재고 반영
   │                          │                        │                    └── 감사 로그
```

##### 승인 규칙
1. **승인 권한**: 출발 창고의 **창고장** 또는 **매니저 이상**
2. **재고 검증**: 승인 시 출발 창고의 가용 재고 자동 확인 (부족 시 승인 차단)
3. **도착 확인**: 도착 창고 담당자가 실물 확인 후 입고 처리
4. **가상창고 이동**: 온라인창고/서초창고 이동 시 운송 단계 자동 스킵, 즉시 재고 반영
5. **FIFO 유지**: 이동 시 UUID 및 입고 타임스탬프 보존 → 도착 창고에서도 FIFO 순서 유지
6. **전체 감사 로그**: 이동 요청/승인/반려/출고/입고 전 과정 기록

#### 재고 반영 규칙

| 시점 | 출발 창고 | 도착 창고 | 비고 |
|------|----------|----------|------|
| 이동요청 | 변동 없음 | 변동 없음 | 이동 예정 수량으로 표시만 |
| 승인완료 | 가용 재고에서 **예약 차감** | 변동 없음 | 다른 출고에 해당 수량 배정 방지 |
| 출고처리 | **실제 재고 차감** | 변동 없음 | 물품이 출발 창고를 떠남 |
| 입고완료 | - | **실제 재고 증가** | 물품이 도착 창고에 도착 |
| 이동확정 | 확정 | 확정 | 최종 정산 |

### 3.7 전체 감사 로깅
- **모든 작업**에 대해 감사 로그 기록
- 로그 항목: 작업자, 작업 내용, 실행 위치, 타임스탬프, IP, 변경 전/후 데이터

### 3.12 전자서명 및 배송완료 시스템

#### 개요
출고전표가 `처리완료` 상태가 되면 **서명 전용 링크**를 자동 발행한다.
배송기사는 해당 링크를 통해 모바일에서 **인수자에게 전자서명**을 받고, **배송기사 본인도 서명**한다.
양측 서명이 모두 완료되면 전표는 `배송완료`로 자동 전환된다.

#### 서명 링크 발행

| 항목 | 내용 |
|------|------|
| 발행 시점 | 출고전표 `처리완료` 전환 즉시 |
| 링크 형식 | `https://sign.samhan.co.kr/{token}` |
| 토큰 | 1회용 JWT (전표 UUID + 배송기사 ID 포함) |
| 유효 기간 | 발행 후 **72시간** (만료 시 재발행 가능) |
| 접근 방식 | 로그인 불필요, 토큰 기반 인증 |

#### 서명 워크플로우

```
[처리완료]                  [배송기사 모바일]           [인수자]              [시스템]
    │                          │                      │                    │
    ├── 서명 링크 자동 발행 ──→│                      │                    │
    │   (SMS/푸시 알림)        │                      │                    │
    │                          │                      │                    │
    │                          ├── 링크 접속            │                    │
    │                          ├── 전표 내용 확인       │                    │
    │                          │                      │                    │
    │                          ├── ① 배송기사 서명 ─────────────────────────→│
    │                          │   (터치 서명 패드)     │                    │
    │                          │                      │                    │
    │                          ├── 인수자에게 기기 전달 →│                    │
    │                          │                      ├── ② 인수자 서명 ───→│
    │                          │                      │   (터치 서명 패드)   │
    │                          │                      │                    │
    │                          │                      │                    ├── 서명 2건 검증
    │                          │                      │                    ├── 전표 → [배송완료]
    │                          │                      │                    └── 감사 로그 기록
```

#### 서명 완료 후 전표 사본 전송

| 항목 | 내용 |
|------|------|
| 전송 기능 | 서명 완료 후 배송기사가 **인수자 전화번호 입력** → 서명된 전표 사본 이미지를 **MMS/알림톡** 전송 |
| **1회 전송 제한** | 전송 버튼은 **1회만 사용 가능**, 전송 완료 후 버튼 비활성화 |
| 전송 내용 | 서명 2건이 포함된 전표 이미지 (워터마크 포함) |
| 전송 기록 | 전송 시각, 수신 번호, 전송 상태를 감사 로그에 기록 |

#### 보안 정책

| # | 보안 항목 | 구현 방법 |
|---|----------|----------|
| 1 | **스크린샷 방지** | CSS `user-select: none` + JavaScript `visibilitychange` 감지 + 워터마크 오버레이 |
| 2 | **이미지 다운로드 차단** | 우클릭 비활성화, 이미지를 `<canvas>` 렌더링 (URL 직접 접근 불가) |
| 3 | **캡처 감지** | `navigator.mediaDevices` API 차단 + 화면 녹화 감지 시 콘텐츠 블러 처리 |
| 4 | **링크 1회 서명** | 서명 완료 후 토큰 무효화, 재접근 시 서명 완료 안내만 표시 |
| 5 | **사본 전송 1회** | 전송 완료 후 전송 API 재호출 차단 (서버 측 상태 관리) |
| 6 | **토큰 만료** | 72시간 후 자동 만료, 만료 링크 접근 시 안내 페이지 표시 |
| 7 | **워터마크** | 전표 이미지에 "삼한공조시스템 / {날짜} / {배송기사명}" 동적 워터마크 |
| 8 | **HTTPS 전용** | 모든 서명 링크는 TLS 1.3 이상 강제 |

#### 서명 데이터 저장

| 필드 | 타입 | 설명 |
|------|------|------|
| signatureId | UUID | 서명 고유 ID |
| slipId | UUID | 전표 ID (FK) |
| signerType | ENUM | `DRIVER` / `RECEIVER` |
| signerName | VARCHAR | 서명자 이름 |
| signatureImage | BLOB (MinIO) | 서명 이미지 (PNG, 투명 배경) |
| signedAt | TIMESTAMP | 서명 시각 |
| ipAddress | VARCHAR | 서명 시 IP |
| deviceInfo | VARCHAR | 기기 정보 (User-Agent) |
| copysentAt | TIMESTAMP | 사본 전송 시각 (NULL이면 미전송) |
| copySentTo | VARCHAR | 사본 수신 전화번호 |

> ⚠️ 서명 데이터도 **BaseEntity**를 상속하여 감사 필드 7종 포함

#### 전표 상태 전환 조건

| 전환 | 조건 |
|------|------|
| 처리완료 → 배송중 | 서명 링크 발행 완료 시 자동 전환 |
| 배송중 → 배송완료 | **배송기사 서명 + 인수자 서명** 2건 모두 완료 시 자동 전환 |
| 배송완료 → 확정 | 매니저 이상 수동 확정 또는 자동 확정 (24시간 후) |

---

## 4. 기술 스택

| 계층 | 기술 |
|------|------|
| Backend | Java 17+, Spring Boot 3.x, Spring Cloud (Eureka, Gateway) |
| Database | PostgreSQL 16 (**서비스별 독립 DB**), Redis (캐시), Elasticsearch (로그) |
| 외부 API | 홈택스 API (세금계산서), 오픈뱅킹 API (법인계좌) |
| Message Queue | RabbitMQ |
| Frontend (Desktop) | Electron + React + TypeScript |
| Frontend (Web) | React + TypeScript + Vite |
| Frontend (Mobile) | React Native |
| 파일 저장소 | MinIO (S3 호환) |
| 컨테이너 | Docker, Docker Compose |
| CI/CD | GitHub Actions |
| 모니터링 | Prometheus + Grafana |

---

## 5. 에이전트 팀 구성 — 마이크로서비스 단위 (총 52명)

> **원칙**: 마이크로서비스마다 **팀장(TL) 1 + 백엔드(BE) 1 + 프론트엔드(FE) 1 + QA 1** = 4명 편성
> **소규모 서비스 팀 통합**: 개발 상황에 따라 PM이 판단하여 결정

### 5.1 서비스팀 편성표

| # | 팀명 | 담당 서비스 | TL | BE | FE | QA | 교차 QA 대상 |
|---|------|-----------|-----|-----|-----|------|-------------|
| 1 | **Team-Infra** | API Gateway + Eureka | TL-00 | BE-00 | FE-00 | QA-00 | Team-Auth, Team-Log |
| 2 | **Team-Auth** | Auth + User Service | TL-01 | BE-01 | FE-01 | QA-01 | Team-Slip, Team-GrpW |
| 3 | **Team-Product** | Product Service | TL-02 | BE-02 | FE-02 | QA-02 | Team-Inventory |
| 4 | **Team-Inventory** | Inventory Service | TL-03 | BE-03 | FE-03 | QA-03 | Team-Product, Team-Slip |
| 5 | **Team-Slip** | Slip Service | TL-04 | BE-04 | FE-04 | QA-04 | Team-Inventory, Team-Acct |
| 6 | **Team-Acct** | Accounting Service | TL-05 | BE-05 | FE-05 | QA-05 | Team-Slip, Team-Partner |
| 7 | **Team-Partner** | Partner Service | TL-06 | BE-06 | FE-06 | QA-06 | Team-Acct, Team-Slip |
| 8 | **Team-GrpW** | Groupware Service | TL-07 | BE-07 | FE-07 | QA-07 | Team-Auth, Team-Noti |
| 9 | **Team-Noti** | Notification Service | TL-08 | BE-08 | FE-08 | QA-08 | Team-GrpW |
| 10 | **Team-Log** | Logging Service | TL-09 | BE-09 | FE-09 | QA-09 | Team-Dash |
| 11 | **Team-Dash** | Dashboard Service | TL-10 | BE-10 | FE-10 | QA-10 | Team-Log |
| 12 | **Team-Migrate** | Migration Service | TL-11 | BE-11 | FE-11 | QA-11 | Team-Inventory, Team-Slip |

### 5.2 지원 팀 (독립 편성)

| 팀명 | 인원 | 구성 | 역할 |
|------|------|------|------|
| **PM** | 1명 | PM | 총괄 프로젝트 매니저, 대표 보고 |
| **UI/UX** | 3명 | TL-UX + UX-01 + UI-01 | 디자인 시스템, UX 리서치, UI 디자인 |
| **DevOps** | 2명 | DO-01 + DO-02 | Docker, CI/CD, 모니터링, DB 관리 |

> UI/UX팀은 독립 유지하며 각 서비스팀에 디자인 가이드를 제공한다.

### 5.3 인원 합산표

| 구분 | 팀 수 | 팀당 인원 | 소계 |
|------|--------|---------|------|
| 서비스팀 | 12 | 4 (TL+BE+FE+QA) | **48명** |
| PM | - | - | **1명** |
| UI/UX | 1 | 3 | **3명** |
| DevOps | 1 | 2 | **2명** |
| **합계** | | | **총 54명** |

### 5.4 조직도

```
PM (총괄 프로젝트 매니저) ─── 대표에게 보고
 │
 ├── [서비스팀 × 12] ─── 각 팀: TL + BE + FE + QA
 │    ├── Team-Infra (API Gateway + Eureka)
 │    ├── Team-Auth (Auth + User)
 │    ├── Team-Product
 │    ├── Team-Inventory
 │    ├── Team-Slip
 │    ├── Team-Acct
 │    ├── Team-Partner
 │    ├── Team-GrpW
 │    ├── Team-Noti
 │    ├── Team-Log
 │    ├── Team-Dash
 │    └── Team-Migrate
 │
 ├── UI/UX Team (독립) ── 3명
 │    ├── TL-UX (UI/UX 리드)
 │    ├── UX-01: UX 리서처/설계자
 │    └── UI-01: UI 디자이너
 │
 └── DevOps Team (독립) ── 2명
      ├── DO-01: Docker/CI/CD 엔지니어
      └── DO-02: DB/모니터링 엔지니어
```

### 5.5 QA 교차 검증 체계

각 서비스팀의 QA는 **자기 팀 내부 QA** 외에, 위 편성표의 **교차 QA 대상 팀**의 기능도 검증한다.
상세 프로세스: [`docs/PM/qa_cross_validation_process.md`](qa_cross_validation_process.md) 참조

### 5.6 의사결정 체인

```
개발자(BE/FE) → TL(팀장) 승인 → PM 승인 → 대표 최종 승인 → 개발 착수
```

---

## 6. 서비스팀 필수 문서 체계

> **핵심 원칙**: 매번 전체 코드를 학습하지 않고, 기획서 및 개발 리포트를 통해 현재 상황을 파악
> **템플릿**: `docs/templates/` 디렉토리의 표준 템플릿 사용 필수

### 6.1 필수 문서 3종 (모든 작업에 적용)

| # | 문서명 | 파일 경로 | 작성 시점 | 작성자 | 템플릿 |
|---|--------|----------|----------|--------|--------|
| 1 | **기획서** | `docs/{팀명}/plans/{기능명}_plan.md` | 개발 착수 전 | 팀장(TL) | `plan_template.md` |
| 2 | **중간 보고서** | `docs/{팀명}/interim/{기능명}_interim_{N차}.md` | 스프린트 중간 | TL + 개발자 | `interim_report_template.md` |
| 3 | **개발 보고서** | `docs/{팀명}/dev_reports/{기능명}_dev_report.md` | 개발 완료 시 | BE + FE | `dev_report_template.md` |

### 6.2 QA 보고서 (스크린샷 필수)

| 항목 | 내용 |
|------|------|
| 파일 경로 | `docs/{팀명}/qa_reports/{기능명}_qa_report.md` |
| 스크린샷 저장 | `docs/{팀명}/qa_reports/screenshots/` |
| **필수 포함** | 테스트 시나리오, 결과, **스크린샷(1장 이상 필수)**, 버그 목록 |
| 교차 QA 보고서 | `docs/{팀명}/qa_reports/{기능명}_cross_qa_{교차팀명}.md` |
| 템플릿 | `qa_report_template.md`, `cross_qa_report_template.md` |

> ⚠️ **QA 보고서에 스크린샷이 없으면 보고서 미제출로 간주한다.**

### 6.3 토론 기록

| 항목 | 내용 |
|------|------|
| 파일 경로 | `docs/{팀명}/discussions/{YYYY-MM-DD}_{주제}.md` |
| **필수 포함** | 참여자, 안건, **각 참여자 발언 상세 기록**, **최종 결론**, 후속 액션 |
| 템플릿 | `discussion_template.md` |

> ⚠️ **토론의 결론은 반드시 명확히 기술해야 하며, 결론 없는 토론 기록은 반려한다.**

### 6.4 팀별 폴더 구조

```
docs/{팀명}/
 ├── plans/                    # 기획서
 ├── interim/                  # 중간 보고서
 ├── dev_reports/              # 개발 보고서
 ├── qa_reports/               # QA 보고서
 │    └── screenshots/         # 스크린샷 저장
 ├── discussions/              # 토론 기록
 └── function_docs/            # 함수별 상세 문서
```

### 6.5 기타 문서

| 문서 | 경로 | 설명 |
|------|------|------|
| Phase 보고서 | `docs/{팀명}/phase_reports/` | 단계별 개발 요약 + 상세내역 |
| 함수 문서 | `docs/{팀명}/function_docs/` | 함수별 상세 설명 |
| API 문서 | `docs/{팀명}/api_docs/` | API 명세서 |

---

## 7. 데이터 표준: 감사 필드 (BaseEntity)

> ⚠️ **모든 마이크로서비스의 모든 엔티티는 아래 7개 감사 필드를 필수로 포함해야 한다.**

### 7.1 BaseEntity 구조

```java
@MappedSuperclass
@EntityListeners(AuditingEntityListener.class)
public abstract class BaseEntity {

    @CreatedDate
    @Column(nullable = false, updatable = false)
    private LocalDateTime createdAt;

    @CreatedBy
    @Column(nullable = false, updatable = false)
    private String createdBy;

    @LastModifiedDate
    private LocalDateTime modifiedAt;

    @LastModifiedBy
    private String modifiedBy;

    private LocalDateTime deletedAt;

    private String deletedBy;

    @Column(nullable = false)
    private Boolean isDeleted = false;
}
```

### 7.2 적용 규칙

| # | 규칙 | 설명 |
|---|------|------|
| 1 | **BaseEntity 상속 필수** | 모든 엔티티는 `extends BaseEntity` |
| 2 | **물리 삭제 금지** | `DELETE` 쿼리 금지, Soft Delete만 허용 |
| 3 | **Soft Delete** | `isDeleted = true` + `deletedAt` + `deletedBy` 설정 |
| 4 | **자동 필터링** | API 조회 시 `isDeleted = false` 조건 자동 적용 (`@Where` 또는 글로벌 필터) |
| 5 | **사용자 자동 주입** | `createdBy` / `modifiedBy` / `deletedBy`는 JWT 토큰의 사용자 ID 자동 주입 |
| 6 | **타임스탬프 자동** | `createdAt` / `modifiedAt`은 JPA Auditing으로 자동 설정 |

### 7.3 DB 컬럼 매핑

| 필드 | 컬럼명 | 타입 | NOT NULL | 기본값 |
|------|--------|------|----------|--------|
| createdAt | `created_at` | `TIMESTAMP` | ✅ | - |
| createdBy | `created_by` | `VARCHAR(50)` | ✅ | - |
| modifiedAt | `modified_at` | `TIMESTAMP` | ❌ | NULL |
| modifiedBy | `modified_by` | `VARCHAR(50)` | ❌ | NULL |
| deletedAt | `deleted_at` | `TIMESTAMP` | ❌ | NULL |
| deletedBy | `deleted_by` | `VARCHAR(50)` | ❌ | NULL |
| isDeleted | `is_deleted` | `BOOLEAN` | ✅ | `false` |

---

## 8. QA 교차 검증 프로세스

상세 프로세스: [`docs/PM/qa_cross_validation_process.md`](qa_cross_validation_process.md)

### 8.1 핵심 규칙

1. 각 팀 QA는 자기 팀 내부 QA 완료 후, **교차 QA 대상 팀**의 기능도 검증
2. 교차 QA 보고서에 **스크린샷 1장 이상 필수 첨부**
3. 교차 QA **FAIL** 시 해당 기능은 `develop` 머지 차단
4. 교차 QA 요청 접수 후 **48시간 이내** 보고서 제출

### 8.2 교차 검증 시점

| 시점 | 범위 |
|------|------|
| 기능 단위 | 각 기능 개발 완료 후, 연관 서비스 QA가 검증 |
| Phase 단위 | Phase 완료 전, 해당 Phase 모든 팀 QA 통합 검증 |
| 릴리스 전 | main 머지 전 전체 서비스 통합 검증 |

---

## 9. 개발 로드맵 (7단계, 총 33주)

### Phase 1: 기반 인프라 (4주)
- Docker 환경 구성, Eureka Server, API Gateway
- PostgreSQL 클러스터, Redis, RabbitMQ 설정
- 인증/권한 서비스 (JWT, 7단계 역할 기반 접근 제어)
- 로깅/감사 서비스 기본 구축
- 디자인 시스템 확립

### Phase 2: 핵심 도메인 (6주)
- 사용자/조직 서비스 (조직도, 역할 관리)
- 품목 서비스 (UUID, 태그, 스펙)
- 재고/창고 서비스 (FIFO, UUID 추적, **창고 간 재고 이동**)
- Desktop(Electron) 앱 기본 프레임

### Phase 3: 전표 시스템 (6주)
- 전표 서비스 (출고·입고·입금·출금·**이동** 전표)
- 전표 번호 체계 + 출고일 변경 로직
- 수정 이력 추적 (Google Docs 스타일)
- 배송 태그 시스템

### Phase 4: 회계/거래처 (4주)
- 회계 서비스 (입출금, 잔액, 원장)
- 거래처 서비스 + 거래처별 원장
- 세금계산서 발행 (일괄/개별)
- 권한별 데이터 표시 차등

### Phase 5: 그룹웨어/대시보드 (4주)
- 그룹웨어 (메신저, 일정, 전자결재)
- 대시보드 (통계, 리포트, 시각화)
- 알림 서비스 (WebSocket, 푸시)

### Phase 6: 외부 배포 + 모바일 (5주)
- 거래처 주문 링크 시스템 (공식 도메인 기반)
- 모바일 앱 듀얼 구성 (거래처용 + 내부 직원용)
- 내부 앱 권한별 메뉴 차등 구현
- 창고원 모바일 출고전표 수신/처리 워크플로우

### Phase 7: 이카운트 마이그레이션 + 최적화 (4주)
- 이카운트 데이터 추출 및 정합성 검증
- 음수 수량 변환 처리 (출고↔입고 전환)
- UUID 매핑 및 스테이징 검증
- 전체 통합 테스트, 성능 최적화
- 운영 배포 + 모니터링

---

## 10. 대표 검토 결정사항 (2026-05-01 확정)

| # | 질문 | 결정 |
|---|------|------|
| 1 | DB 분산 전략 | ✅ **서비스별 독립 DB** (각 마이크로서비스가 자체 DB 소유) |
| 2 | 거래처 주문 웹 | ✅ **링크 공유 방식** (공식 홈페이지 도메인 사용) |
| 3 | 세금계산서 연동 | ✅ **홈택스 API 직접 연동** |
| 4 | 법인계좌 연동 | ✅ **오픈뱅킹 API 직접 연동** |
| 5 | 이카운트 마이그레이션 | ✅ **필요** (음수 수량 → 반대 전표 변환 처리 포함) |
| 6 | 모바일 앱 | ✅ **거래처용 + 내부 직원용 듀얼** (내부용은 권한별 메뉴 차등) |

---

## 11. GitHub 활용 계획

### 11.1 브랜치 전략 (서비스팀별 독립 브랜치)

```
main (운영 배포)
 │
 └── develop (통합 개발)
      │
      ├── team/infra        (Team-Infra 브랜치)
      ├── team/auth         (Team-Auth 브랜치)
      ├── team/product      (Team-Product 브랜치)
      ├── team/inventory    (Team-Inventory 브랜치)
      ├── team/slip         (Team-Slip 브랜치)
      ├── team/acct         (Team-Acct 브랜치)
      ├── team/partner      (Team-Partner 브랜치)
      ├── team/grpw         (Team-GrpW 브랜치)
      ├── team/noti         (Team-Noti 브랜치)
      ├── team/log          (Team-Log 브랜치)
      ├── team/dash         (Team-Dash 브랜치)
      ├── team/migrate      (Team-Migrate 브랜치)
      │    └── {팀명}/feature/{기능명}
      │    └── {팀명}/bugfix/{이슈번호}
      │
      ├── team/uiux         (UI/UX 팀 브랜치)
      ├── team/devops       (DevOps 팀 브랜치)
      │
      └── hotfix/{이슈번호} (긴급 수정, PM 직접 관리)
```

#### 브랜치 규칙
| 브랜치 | 관리자 | 머지 대상 | 보호 설정 |
|--------|--------|----------|----------|
| `main` | 대표 승인 | `develop` → `main` | 강제 푸시 금지, PR 필수 |
| `develop` | PM | 팀 브랜치 → `develop` | PM 승인 필수, CI 통과 필수, **교차 QA PASS 필수** |
| `team/*` | 각 TL | 개인 브랜치 → `team/*` | TL 승인 필수 |
| 개인 브랜치 | 개발자 | - | 자유 푸시 |

### 11.2 머지 요청 프로세스 (TL → PM)

```
[개발자]              [TL (팀장)]              [PM]                [대표]
   │                     │                      │                    │
   ├── 개인 브랜치 작업   │                      │                    │
   ├── PR 생성 ────────→│                      │                    │
   │                     ├── 코드리뷰            │                    │
   │                     ├── 승인/수정요청        │                    │
   │                     │                      │                    │
   │                     ├── team/* 머지 완료    │                    │
   │                     ├── develop 머지 요청 ─→│                    │
   │                     │   (PR + 변경사항 요약) │                    │
   │                     │                      ├── 충돌 검토         │
   │                     │                      ├── 타 팀 영향 분석   │
   │                     │                      ├── 승인 / 반려       │
   │                     │                      │                    │
   │                     │                      │ [Phase 완료 시]     │
   │                     │                      ├── main 머지 요청 ─→│
   │                     │                      │                    ├── 최종 승인
```

#### PM 머지 검토 체크리스트

PM은 `develop` 머지 요청 시 아래 항목을 **모두 확인** 후 승인/반려 결정:

| # | 검토 항목 | 설명 |
|---|----------|------|
| 1 | **타 팀 충돌 여부** | 다른 팀 브랜치와 코드 충돌이 없는가? |
| 2 | **API 호환성** | 변경된 API가 다른 서비스의 기존 호출에 영향을 주지 않는가? |
| 3 | **DB 스키마 영향** | 독립 DB 간 데이터 정합성에 문제가 없는가? |
| 4 | **테스트 통과** | CI 파이프라인 (빌드 + 테스트)이 모두 통과했는가? |
| 5 | **문서 업데이트** | 관련 기획서/API 문서/함수 문서가 함께 업데이트되었는가? |
| 6 | **기획서 준수** | 기획서에 명시된 스펙과 일치하는가? |
| 7 | **필수 문서 제출** | 기획서, 중간 보고서, 개발 보고서가 모두 작성되었는가? |
| 8 | **QA 보고서** | 내부 QA + 교차 QA 보고서 제출 + 스크린샷 첨부 완료? |
| 9 | **BaseEntity 준수** | 모든 엔티티가 감사 필드 7종을 포함하는가? |

#### PM 반려 시 필수 제공 항목

PM이 머지 요청을 반려할 경우 **반드시 아래 3가지를 PR 코멘트에 명시**:

```markdown
### 🔴 머지 반려

**1. 반려 사유**
- (구체적인 문제점 기술)

**2. 충돌/영향 분석**
- 어떤 팀/서비스와 충돌 또는 영향이 있는지 명시
- 관련 파일 및 라인 번호 지정

**3. 수정 방향 제시**
- 어떻게 수정하면 승인 가능한지 구체적 가이드 제공
- 필요 시 참고 코드/문서 링크 첨부
```

### 11.3 머지 프로세스 예시

#### ✅ 승인 케이스
```
TM-BE: "전표 서비스 CRUD API 구현 완료, develop 머지 요청합니다."
PM: "✅ 승인. 타 팀 충돌 없음, CI 통과, API 명세 일치 확인."
→ develop 머지 완료
```

#### 🔴 반려 케이스
```
TM-FE: "전표 수정 이력 UI 구현 완료, develop 머지 요청합니다."
PM: "🔴 반려.
  1. 반려 사유: 전표 상태 API 응답 필드명이 백엔드 스펙과 불일치
     (FE: 'slipStatus' / BE: 'status')
  2. 충돌 분석: Slip Service API v2 응답 스키마 참조 필요
     (docs/backend/api_docs/slip_api.md#L45)
  3. 수정 방향: API 클라이언트의 필드 매핑을 BE 스펙에 맞춰 수정하거나,
     BE 팀과 협의하여 필드명 통일 후 재요청 바랍니다."
→ TM-FE가 수정 후 재요청
```

### 11.4 Issue & Discussion 활용
- **Issues**: 버그 리포트, 기능 요청, 작업 트래킹 (팀 라벨 필수)
- **Discussions**: 설계 토론, 리뷰 승인, 의사결정 기록
- **Projects**: 칸반 보드로 Phase별 진행 현황 관리
- **Wiki**: 개발 가이드, 온보딩 문서, API 규약

### 11.5 라벨 체계

| 라벨 | 용도 |
|------|------|
| `team:infra` ~ `team:migrate` | 각 서비스팀 이슈 |
| `team:uiux` | UI/UX 팀 이슈 |
| `team:devops` | DevOps 팀 이슈 |
| `cross-qa` | 교차 QA 이슈 |
| `priority:critical` | 긴급 |
| `priority:high` | 높음 |
| `priority:normal` | 보통 |
| `status:review` | PM 검토 대기 |
| `status:approved` | 승인 완료 |
| `status:rejected` | 반려 (사유 첨부) |
| `doc:plan` | 기획서 관련 |
| `doc:qa-report` | QA 보고서 관련 |
| `phase:1` ~ `phase:7` | 해당 Phase |

---

*최종 수정: 2026-05-03 12:22 | 작성자: PM | 버전: v2.0 (팀 조직 개편 + 프로세스 강화 + 데이터 표준)*
