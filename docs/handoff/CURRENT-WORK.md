# 현재 작업 — 2026-08-10 야간 세션 종료 (집 PC → 회사 PC)

> 이 문서만 읽으면 이어받을 수 있습니다.
> 현황판: https://claude.ai/code/artifact/8a33a8b9-00fe-4ed0-8628-6ee28f5af09f

---

## 🚨🚨 회사 PC 에서 가장 먼저 — **수치를 다시 세십시오**

**이 문서의 모든 숫자는 집 PC 실측입니다.** 회사 PC 는 데이터가 다릅니다.
→ [`feedback_home_office_seed_data_differs`](../../.claude/memory/feedback_home_office_seed_data_differs.md)

> **수치가 다른 것보다 나쁜 것은 발화 조건이 아예 없는 것**입니다. 표본 0 = "결함 0" 이 아니라 **"판정 불가"**.

### 착수 전 재측정 목록 (그대로 복사해 돌리십시오)

```sql
-- ① 재고 연결 끊김  (#1128 · #1157 둘 다 이 수치에 걸려 있음)
\c inventory_db
SELECT count(*) AS balances, count(DISTINCT product_id) AS product_uuids FROM stock_balances;
SELECT count(*) FROM stock_instances;

-- ② 품목 모델코드 부재  (#1157)
\c product_db
SELECT count(*) FROM products WHERE is_deleted=false AND (model_code IS NULL OR btrim(model_code)='');

-- ③ 이관 주문 (#1159 — 메뉴 제거의 근거)
\c accounting_db
SELECT count(*) AS orders_total FROM orders;          -- 집PC: 0행

-- ④ 판매전표 (#1131)
\c slip_db
SELECT count(*) FILTER (WHERE is_deleted=false) AS active_slips FROM slips WHERE slip_type='SALES';

-- ⑤ 수량 동기화 규칙 (#1126)
\c product_db
SELECT rule_key, estimate_category, enabled, is_deleted, created_by, created_at
FROM quantity_sync_rule ORDER BY created_at;
```

### 집 PC 실측값 (대조용 — 회사 PC 가 이래야 한다는 뜻이 **아닙니다**)

| 항목 | 집 PC 2026-08-10 |
|---|---|
| `stock_balances` | 202행 · product UUID 102개 · **삭제 product 연결 100개** |
| `stock_instances` | 20행 (삭제 참조 1) |
| `GET /inventory/balances` | 🚨 **HTTP 404 전체 실패** |
| 모델코드 없는 활성 품목 | 33개 · 참조 line 4,225 |
| `accounting_db.orders` | **0행** (= `#1159` 메뉴 제거 근거) |
| 활성 판매전표 | 403건 · 라인 774행 · 거래처 3필드 빈 전표 63건 |
| `quantity_sync_rule` | 6행 중 **활성 1** — 그 1건은 **우리 QA 산물** (`created_by=dev_master`, 08-09 20:30) |
| 채팅 관련 | 계정 32 · 직원 24 · 그룹 10 · 부서 5 · 메시지 598(활성 160) · 알림 69,178 |

⚠️ **회사 PC 에 `quantity_sync_rule` 이 0행일 수 있습니다.** 그러면 `#1126` 의 "규칙 1건" 조건이 없어 검증이 성립하지 않습니다 — **실 관리자 UI 로 만들어서** 밟으십시오(🚫 DB 직접 INSERT 금지).

### 첫 셋업

```powershell
git pull
.\scripts\sync-claude-memory.ps1
```

DB 이관은 [`docs/handoff/DB-MIGRATION-RUNBOOK.md`](DB-MIGRATION-RUNBOOK.md).
⚠️ 런북 보완 — 보정 endpoint 는 **partner-service 와 slip-service 가 같은 Docker network** 에 있어야 합니다(Eureka 가 내부 주소를 반환해 호스트에서 안 닿습니다).

---

## ✅ 이번 세션 머지 (3건)

```text
#1133  시트 단종·미판매·품절 → 품목 상태     6c73926c7   이슈 #1095 CLOSED   라운드 12
#1154  기초거래처 7,253건 멱등 적재          e6f7878f1   에픽 #896 OPEN 유지  라운드 25
#1156  INBOUND partner_code + 세금계산서     b2d53a092   이슈 #1155 CLOSED   라운드 14
```

`#1156` 은 원 결함 외에 **세금계산서에 존재하지 않는 등록번호가 찍히던 것**(`P-2026-0001` → `20260001`)까지 닫았습니다. 우리 fix 가 만든 표면이었습니다.

---

## 진행 중 — `#1126` 하나

| | |
|---|---|
| HEAD | `935d9af70` |
| 게이트 | ① R12 까지 결함 닫음 · ② CI 확인 필요 · ③ 라이브 확인됨 |
| 다음 한 수 | **R13 적대검증 재수렴** → 0 이면 머지 |

🚨 **R13 을 발주했다가 세션 종료로 중단했습니다.** 산출물 없음. 다시 발주하십시오.

📌 개발책임자 결정: **이 PR 은 선재 결함까지 닫고 머지**합니다. R12 가 마지막 선재(리모컨 4→6 누적)를 닫았습니다.

R13 이 확인할 것 — **잃으면 안 되는 것 5개**:
```text
규칙 0건이면 순수 레거시와 exact diff 0
규칙 소비는 살아 있다
비상품 납품가 입력 시 수량 1                       (R10)
#1133 품목 상태 축 (단종·미판매 미표시 · 품절 잠금)  (R10 병합)
multiselect-chip-count 하위호환                     (R8)
```

---

## 🆕 이번 세션에 연 트랙 (5개 · 전부 착수 전)

| PR | 이슈 | 등록 | 상태 |
|---|---|---|---|
| **#1159** | `#826` 주문서 관리(이관) 메뉴 제거 | 07-16 | 🚨 **차단 판정 뒤집음** |
| **#1158** | `#845` 문서 양식 디자이너 다음 슬라이스 | 07-18 | 정찰 미실시 |
| **#1157** | `#1051`+`#1086` 품목 식별자 정합 | 08-01 | 정찰 미실시 |
| — | `#901` 클로드 대화 | 07-22 | 브랜치만 (`feat/901-claude-conversation`) · **PR 미개설** |
| — | `#1111`+`#1143` 세트 구성품 | 08-07 | **미개설** (문서 작성 중 중단) |

### 🚨 `#826` — 제가 뒤집은 판정입니다

2026-07-24 정찰이 *"Step A 미수행 ⟹ 착수 불가"* 로 적었고 제가 그대로 옮겼는데, 개발책임자께서 *"826은 진행할 수 있지 않나?"* 라고 물으셔서 다시 쟀습니다.

```text
accounting_db.orders = 0행   ← 이관 대상 자체가 없다
```

런북의 금지 근거가 *"미편입분 사용자 접근 손실"* 이므로 **미편입분이 0행이면 잃을 접근이 없습니다.** *"미수행"* 과 *"할 것이 없음"* 을 같게 옮긴 제 잘못입니다.

개발책임자 지시: *"주문서 메뉴가 2개인데 뒤에 (이관)이라고 되어있는 부분이 걸려서 그래. **가급적 해당 메뉴는 삭제하기를 원해.**"*

제거 대상 경로는 PR #1159 본문에 실측으로 적어 두었습니다.

---

## 🚨 완료한 정찰 3건 — 착수 전에 읽으십시오

### `#1128` 재고 — **재고 조회가 전체 404 입니다**

```text
GET /inventory/balances  →  HTTP 404 전체 실패
```

**재고 인스턴스를 설계하기 전에 이것부터**입니다. 그리고 삭제 product 참조 100건은 **`#1157` 과 같은 뿌리**라 처리 방침은 **`#1157` 에서 한 번만** 정하십시오.

### `#1125` 내부 채팅 — 새로 만들 뻔한 것을 막았습니다

```text
🚫 별도 채팅 도메인 신설 금지 — #894 명시 결정대로 기존 messages 를 방 모델로 확장
실시간   공용 SSE broker/client 재사용 · Yjs coedit 아닌 방 단위 계약 필요
ChatRoomsPage 는 외부 단톡방 매핑이라 재사용 불가
데스크톱·모바일 모두 삼한이 런처와 독립 채팅 라우트 필요
```

### `#1131` 판매전표 — 전제 확인 완료

```text
#1071 전제 맞음 — 수정 표는 있으나 품목 선택기·신규 행 생성 경로 없음
전잔·후잔 정본 — 실행 accounting-service · 산식 PartnerLedgerContract.fold
자동 빈행 — 트랙 문서의 좌표가 전부 이동했고 공통 함수 3개를 특정 (🚫 재구현 금지)
```

🚨 **`#1131` 에 중단된 WIP 커밋이 있습니다** — `8fe285a72`. **완성본이 아닙니다.** main 병합 없이 만든 것이라 신뢰하지 말고, 병합 후 RED 부터 다시 쓰거나 revert 하십시오.

---

## 📌 개발책임자 결정 (2026-08-10)

| 건 | 결정 |
|---|---|
| `#896` 순서 | **A→B→C→D** — 주문서 소비 경로 → 옵션 평가기 → 계열별 세팅 → 서버·데스크톱 정합 |
| `#1126` 범위 | **선재 결함까지 닫고 머지** |
| `#1156` 자가등록 | **예외로 명시 유지** — `PartnerAuthService:144,326` 이 `partnerCode` 를 사업자번호 mirror 로 두는 것을 주석·계약테스트로 고정 (미실행) |
| `#1133` 품절 BUNDLE | **부모가 남고 잠긴다** — 전개하지 않고 부모 행으로 남겨 수량 잠금 (미실행 · `#1111` 트랙이 흡수 예정) |
| `#826` | **메뉴 삭제 원함** |

🚨 아래 둘은 **결정만 되고 구현 안 됐습니다**:
- `#1156` 자가등록 예외 명시
- `#1133` 품절 BUNDLE 부모 잠금

---

## 🚨 `#896` 의 실제 범위 — 45계열

📄 `docs/dev-reports/2026-08-10-896-quantity-sync-full-inventory.md`

> **개념은 스키마에 있고, 그것을 계산하는 코드가 저장소에 없습니다.**

```text
주문서는 서버 규칙을 수량에 전혀 반영하지 않는다   grep -c "applyServer" order-app = 0
옵션 조건 평가기가 없다                            clients/ 전체에 optionEquals 0건
order-app 은 조건 붙은 규칙을 거부한다             quantitySync.ts:126-129
같은 규칙이 네 곳에 독립 구현                      estimate-app · order-app · 서버 BundleExpander · 데스크톱
업무용 규칙 0건                                    활성 1건은 우리 QA 산물
```

⚠️ 세팅 최대 함정 — `evaluateRule` 이 **하나라도 `null` 이면 전체 `null`**(`quantitySync.js:61`). 45개 중 품목코드 하나가 카탈로그에 없으면 **45개가 다 죽고** 조용히 레거시로 돌아갑니다.

---

## ⏸️ 차단된 이슈 (이유와 함께)

```text
#883   UI/디자인 전면 개편   개발책임자 지시 "최후순위 · AWS 이관 직전"
#922   CODEF→바로빌          자격 확보 대기
#935   아로로지스 자동 업데이트  코드서명·피드 선행
#1098  알리고 실호출          자격 확보 대기
```

## 아직 트랙 없는 이슈 (오래된 순)

```text
#910  (07-22) 클라이언트 버전 정책 확대 — 8앱 자동 업데이트 · N2 잠복 결함 문서화됨
#1072 (08-05) 계정과목 정본 이카운트 체계 — 🚨 무결성 도메인 · 착수 전 선확인
#1085 (08-06) QA797 잔재 정리 (chore) — 🚨 #1157 과 겹치는 행이 있는지 먼저 가를 것
#1092 (08-06) 견적서 메뉴 정본 재정의
#1094 (08-06) 상세 진입 하이퍼링크 · 뒤로가기 (전 화면 공통)
#1140 (08-08) '인상 전 단가' → '단가변동' 통일
```

---

## 이 세션에서 굳힌 절차

### 🚨 식별자·경로·필드명을 바꾸면 **의존 측 전수 표**를 요구한다

같은 실수를 네 번 했고 넷 다 CI 가 찾았습니다.

```text
resolveQaShotsDir   파일에 "있는지" 만 확인   ·  모든 경로 상수 경유 미확인
partnerCode         write 축만 확인          ·  read 축 미확인
multiselect testid  고친 지점만 확인         ·  소비자 미확인
mock 필드명         고친 지점만 확인         ·  같은 파일 안 참조 미확인
```

### 🚨 QA 증거 커밋 전 2줄

```bash
grep -nE "path\.(join|resolve)\([^)]*docs/qa" <spec> | grep -v resolveQaShotsDir
git check-ignore -v <캡처 경로>
```

`_local` 로 증거가 날아갈 뻔한 것이 **여섯 번**이었습니다(과거 라운드 10개 소급 회수 완료).

### 🚨 결함 보고를 받으면 **계약을 먼저 읽는다**

`#1133` 의 409 는 **정상 동작**이었는데 제가 결함으로 판정해 고치게 했고 CI 를 깼습니다.

> **"검증을 막는 것" 은 결함처럼 보인다.** 표본을 못 만들게 하는 가드는 거의 항상 결함으로 신고되는데, **가드가 옳고 표본 설계가 틀린 경우**가 있다.

### 🚨 브리핑은 좁게 — 넓으면 7200초 타임아웃

`#1126` R9 가 5각도라 타임아웃했습니다(산출물은 남아 손실 없음). 이후 라운드는 2~4각도로 좁혔습니다.

---

## 워크트리

세션 종료 시 전부 제거를 시도했습니다. **파일 잠금으로 디렉터리가 남았을 수 있습니다** — 회사 PC 에는 영향 없습니다(로컬 경로).
