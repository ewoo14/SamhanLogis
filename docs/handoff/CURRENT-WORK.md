# 현재 작업 — 2026-08-09 새벽 (야간 자율 진행 중)

> 개발책임자 취침 · PM 자율 진행. **이 문서만 읽으면 즉시 이어갈 수 있습니다.**

---

## 0. 🚨 한 화면 요약 — 질문 10개와 PM 권장안

동의하시면 번호만 답해 주시면 됩니다. **이견 있는 것만** 아래 본문을 보시면 됩니다.

| # | 질문 | PM 권장 | 근거 한 줄 |
|---|---|---|---|
| **1** | 회계전표(매출·매입)를 `MANAGER`·`SALES` 도 봐야 하나 | — | 업무 정책이라 권장 없음. MANAGER 는 **원래도 서버에서 403** 이었습니다 |
| **2** | 수신 세금계산서도 같은가 | — | 대상: 실 계정 3명 + `매니저` 그룹 1개 |
| **3** | MANAGER 가 입고 검수 완료를 할 수 있어야 하나 | — | 1·2·3 은 같은 축입니다. 한 번에 답하시면 두 트랙이 풀립니다 |
| **4** | 관리자가 전표가 참조 중인 품목을 삭제할 수 있어야 하나 | — | 지금은 막는 게 없고, 삭제해도 화면은 `200 OK` 로 정상처럼 보입니다 |
| **5** | 시트 전용 모델 2쌍이 오타 alias 인가 별도 모델인가 | — | 출력은 어느 쪽이든 재현되지만 **재고 식별 의미**가 달라집니다 |
| **6** | `JHJ` 14세트 + 그룹명 4세트가 판매 BUNDLE 인가 계산용 중간키인가 | — | 판단 전엔 conflict 로 두고 활성 승격 안 합니다 |
| **7** | `#1127` `updated` 카운터 — 그대로 vs 별도 카운터 | **별도 카운터** | `nameDrift` 와 같은 형태. 작습니다 |
| **8** | `#1129` 선재 HIGH — 흡수 vs 분리 | **흡수** | 상수 2개 범위 조정. 분리하면 담을 곳을 정해야 합니다 |
| **9** | `#1142` 역연산 — 93.3% 거부를 어떻게 | — | 재배정 규칙은 **업무 판단**입니다. 범위 축소(lot 우선)도 후보 |
| **10** | `#1141` A2 로 착수해도 되나 | **예** | 시간창은 실측으로 죽었고, A2 는 타이밍과 무관합니다 |

> 5·6·9 는 업무 의미라 권장안을 만들지 않았습니다. 추측이 설계에 박히면 나중에 다시 뜯어야 합니다.

---

## 1. 🚨 아침에 결정해 주실 것 — 상세

### ① #1145 회계전표 권한 — **이 PR 은 여기서 막혀 있습니다**

SOL 이 실 계정으로 라이브 로그인해 확인했습니다.

```
login=manager@samhan.test  loginStatus=200   role=MANAGER
accounting.sales-slip.accounting=            ← 비어 있음
/admin/sales-slips   status=403
/admin/purchase-slips status=403

kind     blocked_view_accounts immediately_loginable
purchase 13                    12
sales    13                    12
```

이 PR 은 *"FE/BE PageCode 불일치라서 13명이 403"* 이라는 진단으로 시작했는데, **그 13명은 애초에 `.accounting` 권한이 없습니다.** `V37` 이 그렇게 정한 것입니다. 진단이 업무 의미를 추론한 것이었을 수 있습니다.

**질문 1 — 회계전표(매출·매입)를 `MANAGER`·`SALES` 도 볼 수 있어야 합니까?**

| | 결과 |
|---|---|
| 예 | 권한을 부여하는 migration 이 필요합니다 (업무 정책 변경) |
| 아니오 | 지금 상태가 맞습니다. 403 나는 화면을 메뉴에 띄우던 것이 오히려 결함이었고, 이 PR 은 그대로 머지 가능합니다 |

**질문 2 — 수신 세금계산서도 같습니까?** 대상: `dev_manager` · `janyeonggu` · `manager@samhan.test` 3명 + `매니저` 그룹 1개

### ② `#1064` 도 **같은 축**에서 멈췄습니다 — 함께 답해 주시면 됩니다

입고 lifecycle 정찰 결과, 원래 결함(*"GUI 만으로 입고 완료 불가"*)은 **이미 해소**돼 있습니다. 도메인↔화면 액션 양방향 차집합이 입고 0 · 출고 0 입니다.

남은 것은 이것 하나입니다.
```
MASTER · WAREHOUSE   GUI 로 입고 완료 가능
MANAGER              inbound.inspection:UPDATE=false 라 검수 완료가 막힘
```

**질문 3 — MANAGER 가 입고 검수 완료를 할 수 있어야 합니까?**

> 질문 1·2·3 은 전부 *"MANAGER 가 어디까지 할 수 있는가"* 입니다. 한 번에 답해 주시면 두 트랙이 같이 풀립니다.

### ③ `#1051` — 복구할 실 데이터가 **0행**이었습니다

기존 집계 *"복구대상 549전표 + 63견적"* 은 정확히 재현되는데, 그 정체가 **`QA797` 품목 3개를 참조하는 라인**이었습니다. 실 데이터는 0행, 판정불가도 0행입니다.

🚨 **되살리면 QA 산물이 실 데이터가 됩니다.** 관리자 `reactivate` 로 `QA797` 을 살리면 최근단가 8행과 `9,999,999,999,999원`(`created_by=qa-r9-estimate-bait`)도 함께 정상 품목처럼 작동합니다.

트랙 성격이 **복구 → 예방·관측성**으로 바뀝니다.

**질문 4 — 관리자가 전표가 참조 중인 품목을 삭제할 수 있어야 합니까?**
지금은 막는 것이 없고, 삭제해도 상세 화면은 `200 OK` 로 저장된 snapshot 을 정상처럼 보여줘 사용자가 이상을 못 느낍니다.

### ④ `#896` — 이관 착수 직전, 식별 질문 2개만 남았습니다

불일치 83건은 **판단 83개가 아니라 계산 순서 차이 하나**였고(세트 총액 차이 0원), 개발책임자께서 이미 말씀하신 *"세트 납품가가 중요하고 내부는 비율대로"* 와 맞아 **고를 것이 없습니다.** P2 적재 설계도 끝났습니다.

**질문 5 — 시트 전용 모델 2쌍이 오타 alias 입니까, 별도 모델입니까?**
`AP110RNPPHH1 ↔ AP110RNPPBH1` · `PC6NUNK1N ↔ PC6NUNK1NW`
어느 쪽이든 출력은 재현되지만 **재고 식별 의미가 달라집니다.**

**질문 6 — `JHJ` 14세트 + 그룹명 4세트가 판매용 BUNDLE 입니까, 계산용 중간키입니까?**
판단 전에는 적재본에서 conflict 로 두고 활성 승격하지 않습니다.

### ⑤ `#1127` — fix 라운드 상한 3 에 닿았습니다

`#978 D-1` 은 세 라운드(캐시 제거 → name 제외 → 구조 전환)를 거쳤고, 마지막 검증에서 **데이터 정합성 결함 0**·**집계 의미 결함 1**이 남았습니다.

```
PriceHistory · exposure · spec 등 Product 밖 엔티티만 변경되면
실제 DB 쓰기가 있어도 updated=0 · unchanged=1 로 집계된다
(쓰기·롤백 자체는 정상 — 데이터 유실 아님)
```

**질문 7 — 그대로 둘까요, 별도 카운터를 뺄까요?**

| 선택지 | 결과 |
|---|---|
| 그대로 | `updated` 는 "Product master 행" 을 센다는 뜻으로 확정 · 문서 명시 후 머지 |
| **별도 카운터 (권장)** | `nameDrift` 와 같은 형태로 Product 밖 쓰기를 따로 드러냄 · 작은 변경 |

상한에 닿았으므로 임의로 한 라운드 더 돌지 않고 판단을 여쭙니다.

### ⑥ `#1089` 축은 현재 게이트로 검증할 수 없습니다

`#1146` 을 만들며 나온 발견입니다. `#1089` 는 *"세트 전개는 기본 구성품만"* 인데, **골든의 `isDefault` 가 852행 전부 `false`** 입니다. 원천에는 존재하고 **P0 파생 캡처에서 소실**됩니다. (`#1089` 는 레거시 GAS 를 명시적으로 제외합니다.)

### ⑦ `#1129` 도 상한 3 에 닿았습니다 — 선재 결함 하나

이 PR 범위의 결함은 **0** 입니다. 다만 검증이 **선재 HIGH 하나**를 드러냈습니다.

```
fresh 정상 시드에서도 SlipLockSeeder 처리 건수가 0건
CONFIRMED 5건 날짜가 2월·4월인데 lock 조회 범위는 1월
→ origin/main:SlipLockSeeder.java:51-52 에 그대로 있는 선재 결함
  (이 PR 은 의존 게이트만 +9줄 추가했습니다)
```

**질문 8 — 흡수할까요, 분리할까요?**

| 선택지 | 결과 |
|---|---|
| **흡수 (권장)** | 상수 두 개 범위 조정 · 작다 · 다만 이 PR 이 4라운드가 됨 |
| 분리 | 이 PR 은 닫고 별도로 — 다만 **새 이슈를 안 만들기로** 했으니 어디에 담을지 정해 주셔야 합니다 |

### ⑧ 🚨 `#1142` 역연산 설계 — 이대로 구현하면 **93.3% 가 거부**됩니다

구현 전에 무너뜨려 본 결과입니다. 재고는 되돌리기 어려운 도메인이라 지금 잡는 게 훨씬 쌉니다.

```
설계 엄격 적용 시 56/60건(93.3%) 거부
관측상 통과 가능한 lot 입고는 4건뿐
```

거부 사유의 실체:
> 시리얼 입고 중 **5전표·12인스턴스가 다른 OUTBOUND 9전표로 재배정**됐다.
> 재배정된 인스턴스는 원 작업을 유일하게 되짚을 수 없다.

설계가 불완전한 곳도 나왔습니다.
```
정방향 API 가 역연산 잠금을 공유하지 않아 동시성 계약이 성립하지 않는다
전체 saga 에 회계 취소·재생성 단계가 없다
누락된 거부 조건: 진행 중 실사 · 마감 3층 · source 별 reversal 유일성
```

**질문 9 — 어느 쪽으로 갈까요?**

| 선택지 | 결과 |
|---|---|
| 그대로 | 4건만 되돌릴 수 있음. 사실상 기능이 없는 것과 비슷 |
| **재배정 규칙을 따로 만든다** | 재배정된 인스턴스를 어떻게 다룰지 정해야 함 — **업무 판단** |
| 범위 축소 | lot 입고만 우선 지원하고 시리얼은 후속 |

인용 사실 3개(역연산 API 4개 부재 · DRAFT/SAVED 제약 · 후보 60건)는 모두 재현됐습니다.

### ⑨ `#1141` 자동확정 엣지 — 권장안이 나왔습니다 (승인만 주시면 착수)

`#1120` 이 시간창(100ms)으로 가르려다 발산했던 건입니다. 실측이 그 접근을 죽였습니다.
```
자동확정 후  33ms 에 친 정상 Q  →  삼켜짐 (사용자 입력 손실)
자동확정 후 140ms 에 친 같은 Q  →  라벨 뒤에 붙어 통과
분당 100타면 글자 간격이 약 100ms — 두 경우가 겹친다
```

**권장안 A2 — 사용자가 친 접두는 유지하고, 자동 생성된 라벨 suffix 만 선택**
다음 키가 suffix 를 덮어씁니다. 브라우저 인라인 자동완성의 표준 동작이고 **타이밍과 무관**합니다.

빈도가 낮지 않습니다.
```
전체 코드 전에 단건이 되는 창고   6/7 (85.7%)
전체 이름 전에 단건이 되는 창고   7/7 (100%)
자동확정 활성 화면 7개 · 동일 위험 공유 화면 5개
```

**질문 10 — A2 로 착수해도 되겠습니까?** UX 동작 변경이라 여쭙니다.

### ⑩ 그 밖의 대기 목록

```
#1144  P0-A ✅ 진단으로 전제가 뒤집혀 착수·PR #1148
         저장은 정확하고 표시만 틀렸다 · 소급 변경 불요 · 산수라 업무 판단 불요
       P0-D 진단 완료 — "명세와 반대" 가 아니라 **묶음 발행만 분개를 안 한다**
         개별 세금계산서 발행은 110 채권 분개를 생성한다
         OUTBOUND 40건/359,003,920원은 이미 채권 원장에 반영됨 (명세 만족)
         INBOUND 20건/12거래처/116,747,400원 누락은 그대로 (= P0-C)
       🚨 P0-C 와 P0-D 는 직접 원인은 다르나 **중복계상 방지 source identity
         계약을 함께 설계**해야 한다 — 따로 고치면 한쪽이 다른 쪽을 두 번 센다
       🚨 기존 데이터 **소급 게시·연결**은 되돌리기 어려워 개발책임자 판단 사항
#1142  역연산 API 4개 — PM 판단으로 #1142 안에서 함께 갑니다
       (이 저장소 규칙: 리뷰 fix 는 현재 PR 내 처리 · 별도 PR 분리 금지)
#1102  ✅ SlipFormPage 쪽은 해소 — PR #1147 머지 (2cf2835f9)
       계약 감시 손실 0 (뮤테이션 2건 RED · 강제 경합으로 옛/새 코드 갈림)
       ⚠️ HeaderAuthenticationFilterTest flaky 는 **원인 미확정으로 남았습니다**
          추측으로 대기를 넣는 것이 #1103 이 한 일이고 그래서 재발했습니다
       (아래는 해소 전 기록)
#1102  CLOSED 인데 flaky 가 재발했습니다 (SlipFormPage 단가 IDREF).
       ✅ 원인 확정: 선행 테스트가 남긴 미해결 Promise 가 5초 뒤 발화해
          대상 테스트의 mockResolvedValueOnce(200000) 를 선소비 → 카탈로그
          fallback 1000 이 나온다. CI 실패 1/2 · 로컬 0/36 (관측 불가)
       수정 방향도 확정됐습니다(대기 추가가 아니라 인자별 deferred mock).
       🚨 어디서 고칠까요? 새 이슈/PR 을 열지 않기로 했고, 머지 근접한
          #1127 을 넓히기도 애매해 흡수처만 정해 주시면 바로 착수합니다.
#922   바로빌 계약·자격증명 (착수 블로커)
```

---

## 2. 밤 사이 한 일

### 머지 8건
| PR | 이슈 | 머지 SHA |
|---|---|---|
| #1138 QA 증거 덮어쓰기 가드 | #1116 ✅ | `70677436d` |
| #1137 적용 Flyway 마이그레이션 편집 가드 | #1136 ✅ | `e027bf6bf` |
| #1120 전역 입력 UX | #825 ✅ | `6e05ec9f3` |
| #1124 마감날짜 신규 전표 차단 | #1123 ✅ | `d09d5db15` |
| #1146 #896 P2 세트 전개 parity 게이트 | (#896 진행) | `46dc07721` |
| #1147 SlipFormPage flaky 결정화 | (#1102 재발) | `2cf2835f9` |
| #1148 #1144 P0-A VAT 표시 (저장은 정확) | (#1144 진행) | `e9fbdee7d` |
| #1149 nginx healthcheck IPv6 거짓 unhealthy | — | `c0b7a643a` |

### 백로그가 실제로 줄었습니다 — 고쳐서가 아니라 **이미 고쳐진 것을 골라내서**
```
#978   이월 3건이 이미 해결돼 있었다 (#989 · #987 · #990)
#1064  결함이 현재 HEAD 에 없다 — 차집합 양방향 0
#1051  복구 대상 실 데이터 0행 — 전건 QA/시드 잔재
#896   판단 83개가 아니라 원인 하나 · 결정 질문 0개
```

### 🚨 `#978 D-1` 은 살아 있었고, 고치니 인접 결함이 드러났습니다
어제 머지한 `#1139` 는 **별도 계층**(`ProductLookupSheetSyncService`)의 캐시만 고쳤고,
`ProductSheetSyncService.lastKnownRowHash` 는 그대로입니다. 실 PostgreSQL IT 로 재현됐습니다.
```
expected: 1200000
 but was: 1000000.00
```
**롤백된 단가 변경이 다음 실행에서 영구히 건너뛰어집니다.** 고쳤고(`a4f139f2d`), SOL 적대검증이 **인접 결함 1건**을 찾았습니다.

> 캐시를 없애니 비교기가 **이름을 보는데 갱신 분기가 이름을 안 씁니다.** 실 데이터 **481모델**이 매 sync 마다 거짓 `updated` 로 반복 집계됩니다.
> PM 코드 확인: `applyAttributes()` 는 이름에서 **파생된** `panelType`/`remoteType` 만 쓰고 `name` 자체는 저장하지 않습니다.

되돌림 여부를 규칙대로 세었습니다.
```
되돌리면   1건 — 롤백된 단가가 영구히 반영 안 됨 (금액이 틀림)
유지하면   1건 — 거짓 updated 반복 (중복 쓰기·오해, 금액은 정확)
```
수는 같으나 **금액이 틀리는 쪽이 나쁘므로 유지하고 고칩니다.**
🚫 481개 품목명을 시트 값으로 덮는 방향은 택하지 않았습니다 — 어느 쪽이 권위 정본인지는 **업무 판단**이고 되돌리기 어렵습니다. 필요하면 아침에 여쭙겠습니다.

### 잡은 결함 셋 — 셋 다 원문을 직접 열어서 나왔습니다

**#1124 출고 마감시각 게이트가 자정 넘어 통째로 풀렸다** (`bc00649af`)
```java
// 결함
if (!slipDate.equals(LocalDate.now(clock.getZone()))) return;
// clock 에서 존만 꺼내 쓰고 시각은 실제 시스템 시계
```
테스트가 `TODAY = 2026-08-08` 을 고정해 두어, 날짜가 넘어가자 첫 관문에서 조기 return. **마감을 초과해도 아무것도 막지 않았습니다.** 어제까지 우연히 green 이던 테스트입니다. `LocalDate.now(clock)` 으로 고쳐 `tests=20 failures=0` 확인(PM 직접 `--rerun-tasks`).

**#1145 migration 이 MANAGER 에게 회계전표 전권을 줄 뻔했다** (`0e31a5933`)
```
role       .list   .accounting
MANAGER    1111    0000     ← 복제하면 0000 → 1111
SALES      1000    0000
```
V99 이 `.list` 비트를 정본 코드로 복제하는 구조였습니다. **적용 전이라 머지 전에 잡았습니다.**

**main 을 깬 것은 PM 이었다**
`#896` P0 스크립트를 하네스 가드 확인 없이 커밋했습니다. 지금 고치는 중입니다.

---

## 3. 🚨 PM 이 낸 오류 — 같은 함정 세 번째

구현자가 `const baselineDir = 'docs' + '/qa/...'` 로 문자열을 쪼개 놓은 것을 **"스캐너 회피"** 로 판단해 되돌리고 커밋했습니다.

근거는 로컬 실행이었는데, **위반 목록이 124건이라 vitest 메시지가 잘렸고 잘린 앞부분에 없다는 이유로 "없다" 고 결론냈습니다.** CI 는 정확히 그 파일을 잡았습니다.

구현자의 쪼개기는 회피가 아니라 **유일한 수단**이었습니다 — 가드가 `copyFileSync(원본, 목적지)` 의 **인자 전체**를 쓰기 대상으로 세고 전이 폐포까지 있어 중간 변수로도 못 피합니다.

지금은 문자열로 숨기는 대신 **가드가 목적지만 세도록 정밀화**하는 라운드가 돌고 있습니다. 메모리에 남겼습니다 → `feedback_exit_code_measurement_traps.md` 함정 3.

---

## 4. 트랙별 상태

| 트랙 | PR | HEAD | 상태 |
|---|---|---|---|
| 마감날짜 | #1124 | — | ✅ **머지** `d09d5db15` · 이슈 #1123 close |
| 회계전표 P0-B | #1145 | `2c8ff777e` | CI ✅ · **질문 1·2 답만 기다림** |
| 이월 배치 | #1127 | `61935ce32` | D-1 세 라운드 → **구조 전환** · SOL 재수렴 중 |
| **#896 P2 게이트** | ~~#1146~~ | — | ✅ **머지** `46dc07721` |
| **flaky 머지게이트** | **#1147** | `0a48c2669` | **신규 개설** · SlipFormPage 결정화 |
| 입고 lifecycle | #1130 | `f3cb1ffb4` | 정찰 완료 · **질문 3 대기** |
| 끊긴 연결 | #1129 | `c4b57f1cf` | 정찰 완료 · **질문 4 대기** |
| 구글 의존성 제거 | (main) | `841e5da81` | P1·P2 설계 완료 · **질문 5·6 대기** |

### #1145 회계전표 P0-B
R1 → R2 → R3 완료, 코멘트 3건 게시. 🚫 **더 이상 fix 라운드 돌지 않습니다** — 남은 것은 엔지니어링이 아니라 결정입니다.

### #896 구글 의존성 제거
```
수식 인벤토리   3,391건 → 2,648그룹
시트 탭         27개 중 code-read 17개 전부 CSV 확보
열 계약         납품가-2 = 문맥 가격 · 141개는 배분 "가중치"
불일치 83건     ✅ 계산 순서 차이 하나로 판명 · 결정 질문 0개
P2 적재 설계    ✅ 기존 6테이블 재사용 + 신규 3
합격 관문       explodeSetParts() 반환 상세행이 골든과 일치 (원시 셀 아님)
```
🚨 앱 기본 소스는 **이미 DB** (`CATALOG_SOURCE` 기본값 `db`).

### #1142 되돌리기 — B 범위 확정
```
🚨 품목·금액 수정은 DRAFT/SAVED 에서만 가능 → 거기까지 되돌려야 함
🚨 역연산 없는 재고 API 4개: deduct · ship-batch · lots/inbound · instances/batch
되돌림 대상 60건 (INBOUND 20 + OUTBOUND 40)
설계안: docs/dev-reports/2026-08-08-1142-inverse-ops-design.md
PM 판단: 별도 트랙으로 빼지 않고 #1142 안에서 함께 간다
```

## 5. 운영 규칙 — 이번 세션에서 배운 것

```
🚨 부재를 증명하려면 목록이 온전한지부터 확인한다
   잘린 출력에 grep 해서 "없다" 는 근거가 아니다. 판별축은 git ls-files
🚨 구현자가 이상해 보이는 코드를 썼으면 무엇을 피하는지부터 확인한다
   되돌리기 전에 되돌린 상태를 실제로 재라 — CI 로
🚨 이슈를 늘리지 말고 기존에 흡수한다 (개발책임자 지시)
🚨 codex 잔재를 죽이기 전에 워크트리를 먼저 본다
🚨 "구현됐다" 는 기록을 그대로 믿지 않는다
🚨 docker compose up --build <svc> 는 의존 서비스까지 빌드하려다 실패한다
   → --no-deps 를 붙인다 (eureka jar 없어 재배포가 조용히 안 됐던 실측)
```

---

## 6. 환경

```
Docker   slip-service · auth-service · api-gateway = main 빌드 (d09d5db15, 02:5x KST)
         override 필수: -f docker-compose.yml -f docker-compose.local-all.yml
                        -f docker-compose.slip-port-override.yml
         재배포는 --no-deps 와 함께
SA 키     C:\dev\samhan-homepage-260f8ae469cc.json  (프로젝트 상위 폴더)
시트 CSV  scratchpad/live_sheet* · live_sheet_more
```

🚨 **백엔드를 바꿨으면 push 직후 PM 이 재배포한다.**
