# 현재 작업 — 2026-08-08 세션 종료 시점

> 세션을 정리하고 종료했습니다. 이 문서만 읽으면 즉시 이어갈 수 있습니다.

---

## 0. 재시작 전 반드시 알 것 — 이번 세션에서 배운 운영 규칙

### 🚨 codex 세션이 죽지 않고 쌓인다

```
타임아웃(7200초) → 클라이언트만 끊김 → 세션 프로세스는 생존
  → 슬롯 점유 → 새 라운드가 굶음 → 또 타임아웃 → 또 누적
```

이번에 **15개**가 쌓여 있었고 가장 묵은 것이 **12시간 초과**였습니다. codex 타임아웃 7건 중 5건이 산출물 0 이었던 이유입니다.

```powershell
# 재시작 후 먼저 확인
Get-CimInstance Win32_Process -Filter "Name='node.exe'" |
  Where-Object { $_.CommandLine -match 'OpenAI\\Codex\\runtimes' } | Measure-Object
# 라운드 수보다 많으면 잔재 — Stop-Process 로 회수
```

### 🚨 Workflow 서브에이전트도 codex 와 같은 부하다

`codex 4 + Workflow 2` 는 과부하였습니다. **codex 3 + Workflow 1** 또는 **codex 2 + Workflow 2** 까지.

### 🚨 Workflow 스키마에 "원문 인용 전수" 를 넣으면 터진다

```
API Error: Claude's response exceeded the 64000 output token maximum
```

GAS 수식 sweep 에서 `quote`(원문 인용) × 수백 건이 **한 응답 64,000 토큰**을 넘겨 에이전트 4개가 죽었습니다.

```
🚫 에이전트 1개 = 파일 1개 전체 = 수식 수백 개
✅ 에이전트 1개 = 파일 1개 × 구간 1개
✅ 위치(파일:줄)는 정밀하게 · 인용은 짧게  (원문은 나중에 다시 뽑으면 된다)
```

---

## 1. 진행 중 PR 4건 — 전부 push 완료 · 라운드만 재발주하면 됨

| PR | 이슈 | HEAD | CI | 다음 한 수 |
|---|---|---|---|---|
| **#1120** | #825 전역입력UX | `b306bfaca` | **51/51** ✅ | S16 재발주 — 확정 시 표시값 미설정 |
| **#1124** | #1123 마감날짜 | `b7d04da56` | **44/44** ✅ | S12 재발주 — 게이트웨이 라우트 |
| **#1137** | #1136 Flyway | `949274d6e` | **41/41** ✅ | S9 재발주 — fail-open 재발 여부 |
| **#1138** | #1116 증거가드 | `64705274c` | 1 red | S9 재발주 — `cygpath` |

### #1120 — 진단 확보됨

되돌림(S14) 후에도 **확정 후 표시값이 `""` 로 사라지는 결함**이 남습니다. S11·S13 이 만든 회귀가 **아니고** 그 이전부터 있던 것입니다. S12 의 "확정 후 Enter 무음 변경" 도 같은 원인(표시값이 비어 dropdown 이 다시 열림).

⚠️ **워크트리 `t825` 에 미커밋 2건이 있습니다** — 다음 라운드에서 이어 쓸 것이니 지우지 마십시오.
```
WarehouseAutocomplete.test.tsx   +30/-1  RED 테스트 작성 중이던 것
825-s15-final-reconvergence.spec.ts      드라이버 — 커밋하지 말 것
```

🚫 검증 범위를 좁힐 것: `WarehouseAutocomplete` 테스트 + typecheck 만. **Desktop mock 전체 스위트 금지**(7~10분, 타임아웃 원인).

### #1124 — 진단 확보됨

```
배포 slip-service OpenAPI 에는 신규 관리자 API 존재
gateway 실 경로에서 GET /admin/slip-closing-baselines 가 404
기존 /admin/slip-cutoffs 는 같은 계정으로 200  -> gateway 라우트 누락
```
정본은 `/admin/slip-cutoffs` 설정. `GET`/`POST`/`DELETE` 3개를 **축으로** 대조표와 함께.
🚫 fix 라운드에서 재배포·실 호출 금지(타임아웃 원인) — PM 이 배포 후 별도 QA 라운드.

### #1138 — 진단 확보됨

```
qa-output-path-guard.test.cjs:192
  assert.ok(GITBASH_EXE, 'cygpath를 제공하는 Git Bash …')   ← CI(Linux)에 cygpath 없음
```
`qa-shots-dir.sh` 는 순수 Linux 동작 설계(주석 `:29` `:130` `:201`). `cygpath` 는 **UNC·subst 등 Windows 전용 정규화에만** 필요.
→ Linux 는 `bash` 로 protect/regenerate 계약을 실제 검증, Windows 전용 케이스는 "검증 불가" 를 **리포트**(조용히 skip 도, 실패 처리도 아님).

---

## 2. #896 — 설계 잠김 · 값 도출만 남음

개발책임자 확인 **13건 전부 답변 완료**. BLOCKING 9/9 해소.

### 확정된 설계 축

| | 결정 |
|---|---|
| 특징 정본 | `bundle_component.component_variant` — 현재 '기본' 188행을 **재라벨링**(선행) |
| 기본값 | `is_default` = "이 세트에서 기본 전개되는 구성품" · **계열당 개수 제약 없음** |
| 전개 | 카테고리 분기 없이 **기본만 전개** · 모든 세트에 기본값 지정 |
| 동기화 방향 | 종속 1 ← 기준 N · 기준은 **구성품도 세트도** 가능 |
| 배수 | 칩마다 `factor`(기본 1) · 대상 `multiplier`(기본 1) — **V24 에 이미 있음, 신규 테이블 0** |
| '제외' | 모든 품목변경 옵션에 자동 포함 · **데이터로** 표현 |
| 대상 앱 | **레거시 웹앱**(종합견적서·주문서) |
| 설정 UI | 견적품목 — **구성은 기초품목 · 쓰임은 견적품목** |
| 견적품목 추가 3항목 | 납품가 오버라이드 · 구성품 특징 · 수량 동기화 (표시·순서는 이미 있음 = 무훼손) |
| 접힌 구성품 | **데이터 오류** — 실제 대수로 (괄호HP 84건 중 31건 수량 변동, 금액 대조 필수) |
| 리모컨 어휘 | 통일 + 옛 저장값 매칭 이관 |
| 홈멀티 | **세트화한다** (현재 세트 0 · 구성품 0) |
| 완료 조건 | 레거시 두 앱에서 **DB 를 바꾸면 화면이 따라온다** · 금액 회귀 0 |
| 반영 경로 | **Flyway 마이그레이션** · 도출 스크립트도 커밋 · 회사PC 재현 |

### 🚨 라이브 GAS 정본 대조 — 완료됐고 결과가 중요합니다

저장소 사본이 실제로 낡았습니다(`clasp` clone + SHA-256 대조).

```
Code.js      repo 2835750aad33e2fe -> live be373be13eb58a96   3,203 -> 3,577줄
index.html   repo 2321353ee221fb0f -> live 85bb61405e67bebf   19,182 -> 19,301줄
```

**🚨🚨 정본 시트 탭이 스왑됐습니다**
```
저장소 Code.js:48-52    '홈멀티_단가인상' · '싱글 세트_단가인상' … 가 정본
라이브 Code.js:423-427  '홈멀티' · '싱글 세트' · '싱글 구성품' · '상업멀티' · '상업멀티 구성'  (접미사 없음)
```
`_단가인상` 은 **인상단가 오버레이로 강등**. 캐시키 `V3 -> V4`.
⟹ **구조 도출은 접미사 없는 탭에서** 해야 합니다.

**트랙에 닿는 변경 7건**
```
① 홈멀티 '360판넬' 신설 (옵션 배열 변경은 이것 단 1건)   live index.html:7429 · 기본값 '사각'
② 유선리모컨 키트 기준 = 1-Way 실내기 수량만              live :7892-7893 · cnt1Way :7857
③ 세트 스코프 조회 전환 "다른 세트 값 혼입 방지"          live :11049-11062 · :9347
④ 드레인펌프 PUMP_MAP 개편 (구성품->구성품)               live :8086-8088
⑤ 분기관 코드 산정 전면 재작성 (< -> <=, 누적 방향 반전)  live :12416-12489
⑥ 방진가대 S2 중/대 — 냉난방탑 30 이 대->중              live :3774 :3780
⑦ 상업멀티 실내기 판정 확대                               live :3636 :12393
```
**무변경 확인**: `recomputeHomeDerived`(SHA 동일) · 상업멀티 세트 전개 전체(SHA 동일) · 나머지 옵션 배열 9개(문자 단위 일치).

🔑 ③이 확정 설계를 뒷받침합니다 — 레거시가 스스로 *"동일 모델 구성품이 세트마다 다른 값을 갖는다"* 를 인정하고 세트 스코프로 옮겼습니다.

⚠️ ②는 확정 모델에 **안 맞는 예외**입니다. 기준이 특정 품목이 아니라 *"조건에 맞는 품목들의 집합"* 이라 칩 지목으로 표현이 안 됩니다.

### 남은 일

```
1  GAS 수식 전수 sweep 재실행   ← 스키마를 고쳐서(위 0절 참조). 이번엔 max_output_tokens 로 4개 실패
2  값 도출 A/B/C               세트·구성품 / 특징·기본값 / 수량동기화
3  기존 DB 대조                일치·보완·충돌 3분류 · 근거 없는 행 분리
4  개발책임자 확인
5  Flyway 반영
6  레거시 두 앱 라이브QA
7  회사PC 재현 절차 문서화
```

**실측 규모**: 세트 344 · 구성품 1,585행 · 기본 855행 / 기본 없는 세트 **73개(구성품 138행, OUTDOOR 137 + ACCESSORY 1)**

**부분 결과 위치**(재실행 시 참고, 캐시 재사용 가능)
```
값 도출     .../tasks/w9qdinmiz.output          (완료 — 정본 대조 + A/B/C/시트)
수식 sweep  .../tasks/wmdb7pcx3.output          (중단 — Inventory 2/4 만 성공)
스크립트    scratchpad/derive-896.js · gas-formula-sweep.js
```

---

## 3. 신규 이슈 #1140 — '단가변동' 옵션 통일

개발책임자 지시로 오늘 신설. **아직 착수 전.**

```
'인상 전 단가' -> '단가변동' 표기 통일 · 기본값 카테고리별 설정 · oldProducts 토글 신설
결정: 의미 보존 (단가변동_기본값 = NOT default_pre_change)
  homemulti TRUE->FALSE · 나머지 3종 FALSE->TRUE
🚨 착수 전 실측: oldProducts 가 지금 이 플래그를 참조하는지 (구형 금액이 조용히 바뀌면 안 됨)
```

**단가 전환의 두 경로** (2026-08-08 확인)
```
주문서       일자 기준 자동 전환   price_change_schedule · EstimatePricingConfigPage
종합견적서   웹 옵션 토글          chkSingleInc · 일자 개념 없음
```

---

## 4. 신규 10트랙 — 정찰 완료 · 착수 대기

**무차단 4개**: `#978`(PR #1127) · `#1051`(#1129) · `#1064`(#1130) · `#1068+#1071`(#1131)

🚨 **`#978` 슬1(SA 키)이 최우선** — product-service 에 `/etc/samhan/` 부재 · `GOOGLE_*` 0개라 **시트 동기화가 죽어 있고 `#1095` 라이브QA 를 막습니다.**

**표면 충돌 (동시 진행 금지)**
```
SlipFormPage.tsx (2,691줄)   #1068/#1071 선점 · #1095 S4 · #896 · #1089 는 직렬
Modal.tsx                     PR #1120 진행 중 -> #1091 · #896 슬C 는 머지 후
SlipService.java              PR #1124 진행 중 -> #1089 S2 · #1051 S3 는 머지 후
마이그레이션 번호              auth V97+ · slip V119+ (V95·V96·V118 점유)
#978 슬4                      PR #1138 과 같은 6파일 -> 머지 전 착수 금지
```

**오늘 받은 결정** — `#894` 쪽지를 채팅으로 통합 · 방 생성은 전 직원 / `#999` 시리얼 = 분류접두+연월+일련번호 · 정상만 자동 출고후보 · 인스턴스 관리 대상 플래그 신설 · QR 은 모바일+USB 둘 다 / `#1051` 삭제 허용·읽기 계속 / `#1095` 사용자 선택 화면에서만 숨김 · col7 = 비고 / `#1091` 필드별 diff까지 / `#978` 손입력 수동잠금 넣음

---

## 5. 오늘 머지 10건 · 세션 누적 19건

`#1078` `#1083` `#1066` `#1082` `#1103` `#1097` `#1109` `#1107` `#1104` `#1105` `#1074` `#1110` `#1111` `#875` `#1092`×2 `#1113` `#1135` `#978`D-1

---

## 6. 환경 상태

```
Docker      slip-service = #1124 브랜치 빌드 (slip_db 118 · auth_db 96)
            ⚠️ 다른 트랙 라이브QA 전에 재배포 필요
            override 필수: -f docker-compose.yml -f docker-compose.local-all.yml
                           -f docker-compose.slip-port-override.yml
Flyway      auth·arologis 체크섬 복구 완료 (#1136 S1) — 94 migrations validated
프로세스    codex 런타임 0 · Gradle 데몬 0 · 총 0.59GB
```

🚨 **백엔드를 바꿨으면 push 직후 PM 이 재배포한다** — 오늘 이것으로 QA 라운드 3개를 버렸습니다. 이 PR 이 마이그레이션을 넣은 **서비스를 먼저 세고** 전부 올릴 것.
