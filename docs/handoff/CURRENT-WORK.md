# 현재 진행 상황 (2026-08-18 새벽 세션 종료 시점)

> 🚨 새 세션은 이 파일만 읽으면 컨텍스트가 회복된다.

## 즉시 결정이 필요한 것 — 개발책임자 3건

### ① #1269 되돌릴 것인가 (가장 중요)

싱글중대형 가격 옵션 축. **fix 4라운드를 썼고 상한 3을 넘겼다.**

```text
1차  세트상세 리모컨 45,375원 (배분 중간값)        → 도달 1건
2차  헤더만 고쳐지고 상세는 16,000원              → 도달 1건
     🚩 보고서가 "56,000원" 이라 적었으나 거짓이었다 (PM 이 results.json 으로 적발)
3차  4way 판넬 104,060 → 150,040 중간값 노출      → 도달 1건
4차  판넬 차액 60,000 중 50,000 만 반영            → 도달 1건
5차 판정  🚨 도달 3건 — 결함 수가 늘었다
```

**5차에서 새로 나온 3건**

| | 내용 |
|---|---|
| ① | 컬러유선리모컨 75,000원 미반영 — 적용 **59세트 전부** |
| ② | `AC060CS6PBH1SY` 의 360 할인 미반영 |
| ③ | 실외기 받침대 포함 토글 무효 |

**정상으로 확인된 것 (되돌리면 함께 사라진다)**

```text
리모컨 무선 1,660,000 / 유선 1,700,000 — 차액 40,000 정확
판넬   블랙 60,000 전액 반영 · 변형 16개 납품가 지도 보강 · 중간값 노출 0건
자재   1,200,000 → 1,330,000
전수   207세트 x 18상태 = 3,726회 헤더=상세합 불일치 0
       로컬 371 tests · CI 44/44
```

**선택지**

1. **fix 5라운드 — 분모를 닫는다.** 지금까지 매 라운드가 증상 하나씩만 닫았다. 가격에 영향을 주는 **옵션 전체를 열거해 한 번에** 검증한다. 근거: 3건 모두 같은 계열(옵션→가격 반영 누락)이고 원인 구조가 이미 밝혀져 있다.
2. **되돌린다.** 기록된 규칙(`결함 수가 같거나 늘면 fix 를 쌓지 말고 되돌려라`)에 부합한다. 대신 위 "정상 확인" 성과가 사라진다.
3. **분리한다.** 리모컨·판넬·자재(검증된 축)만 머지하고 컬러유선·할인·받침대는 새 PR 로 뺀다.

🚩 **PM 권장: 3.** 검증된 성과를 지키면서 상한 규칙도 지킨다.

### ② #1238 A-2 일마감 탭 분류 — 업무 의미 확인 필요

```text
레거시   회계반영일자가 **있는** 행 = 선발행 · **없는** 행 = 결과
현행     정확히 반대
실측     현재 대상 24행 전부 반대 탭 (반영 1 + 미반영 23)
기존작업 분류는 PR #1219 · #1264·#1270 이 같은 화면을 고치는 중이나 분류는 안 건드림
```

**선택지**
1. 레거시 의미로 뒤집는다 (`accountingPostedAt != null` → 선발행). #1264 버튼 이동 + #1270 QA 갱신 필요.
2. 현행 유지하고 탭명을 명시화 (`결과→회계반영 완료`, `선발행→회계반영 전`).
3. 하지 않는다 — "선발행 = 아직 회계전표 발행 전" 을 공식 채택할 때만.

🚩 **SOL 권장: 1.** 다만 이건 **업무 의미**라 PM 이 추론하면 안 된다.

### ③ #1238 나머지 3건 — 할 일 없음 확인만

```text
A-1 Notion 적재    이미 구현 (PR #115·#453·#233) · 전체 재적재는 #1245 진행 중 → 중복
A-3 I호스 1,000원  8,000원이 PR #967 에 이미 구현 · Drive 라이브 정본도 8,000원
                   레거시 7,000원이 옛것 · 저장 라인 영향 0건
A-4 영업수수료      실행 경로가 PR #1248 에 이미 구현 → 중복
```

---

## 열린 PR 상태 (2026-08-18 02:50 기준)

| PR | 판정 | 남은 것 |
|---|---|---|
| **#1262** 시트 식별자 마스킹 | ✅ **머지 준비 완료** | 도달 0 · 증거 0 · 축 닫힘. CI 48성공/1실패인데 그 1건은 PR 무관 flake(Mobile-Staff `SalesTabNavigator` 5초 타임아웃, mobile 변경 0건). REST 로 재실행 걸어둠 — 결과 확인 후 머지 |
| **#1264** 일마감 회계전표 | 라이브 증명 완료 | 매출→매입 생성 성공 · 중복 차단 유지 · 네 자리 11,000원 일치. **SOL 재판정 3회차 미발주** |
| **#1266** UUID wire/표시 | 확인 범위 내 도달 0 | 전표정리 복원 라이브 성공. 인쇄·tooltip 전수 미검증. **SOL 재판정 미발주** |
| **#1269** 싱글 가격 옵션 | ❌ 도달 3건 | 위 ① 결정 대기 |
| **#1272** 카테고리 설정 이전 | 도달 0 · CI fix 완료 | 실제 HTTP 403 · 주문서웹 UI · 동일 부모 다중 카테고리 **미검증**. **SOL 재판정 3회차 미발주** |
| **#1265** 웹→전표 | ❌ 도달 3건 | 공급가/VAT 1원 회귀 · 원천 추적률 0/27 · 주문서웹 품목 0행 |
| #1267 거래처 마스터·importer | 미착수 | CI 2건 실패 |
| #1268 수량동기화 규칙 | 미착수 | 🚨 **#1272 머지 후** 착수 (옮긴 자리에 규칙을 채워야 두 번 안 옮긴다) |
| #1270 일마감 열 정합 | 미착수 | CI 3건 실패 |
| #1271 DPS 입고비교 | 라이브 캡처 성공 | 🚩 집계 카드 라벨이 아직 「출고전표 라인」 (부제는 입고전표) |
| #1245 · #1251 · #1188 | 정체 | 별도 트랙 |

---

## 이번 세션에서 배운 것 (메모리에 기록됨)

```text
feedback_uuid_no_user_visibility          UUID 캐논은 표시 금지이고 wire 는 허용
feedback_stale_deployment_looks_like_defect  공유 컨테이너는 main 이미지 — 404 는 배포본 나이
feedback_no_cd_compound_use_git_dash_c    cd 복합은 모드와 무관하게 항상 권한창
feedback_git_add_all_swallows_concurrent_round  경로 좁힌 add 는 내 것을 빠뜨린다
```

### 🚩 세션 중 반복된 함정 3가지

1. **라이브 검증이 계속 막혔다** — 공유 컨테이너가 main 이미지라 브랜치 신규 경로가 404/401. 해법은 **브랜치 JAR 를 별도 포트로 기동**(#1271 이 28085/28086 으로 성공, #1264·#1266·#1272 가 뒤따름). #1266 의 401 은 `SAMHAN_GATEWAY_ATTESTATION` 미주입이 원인이었다.
2. **GitHub 이 밤새 불안정** — GraphQL 이 503 이어도 **REST 는 살아 있다**.
   ```bash
   gh api repos/ewoo14/Samhan-Public/issues/<N>/comments -F body=@<보고서> --jq '.html_url'
   gh api repos/ewoo14/Samhan-Public/actions/runs/<ID>/rerun-failed-jobs -X POST
   ```
3. **보고서가 실측과 어긋난 사례 2건** — #1269 라운드1(56,000원이라 적었으나 실제 16,000원, 스크린샷은 빈 화면), #1272(LUNA 가 SOL 판정서 경로에 덮어씀). **PM 이 results.json 과 PNG 를 직접 열어야 잡힌다.**

---

## 환경 상태

```text
권한 모드     bypassPermissions (.claude/settings.local.json) — 2026-08-18 개발책임자 변경
워크트리      11개 · 전부 미커밋 0
공유 컨테이너  24개 유지 · 격리 컨테이너 0
QA 포트       잔여 listener 0
브랜치        전부 origin/main 병합 후 push 완료
🚩 wdc02 제거함 — wdcp 와 같은 PR #1264 를 잡아 wdcp 가 detached HEAD 였다
```
