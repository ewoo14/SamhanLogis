# 현재 작업 — 2026-08-11 집PC 세션 (진행 중 · 자율 운행)

> 이 파일만 읽으면 이어받을 수 있습니다.
> 이전 회차: `docs/handoff/` 의 날짜 앞선 파일들.

---

## 0. 🚨 이어받는 사람이 가장 먼저 할 것

```text
1  docker ps -a --filter "name=samhan-" --format "{{.Names}}\t{{.Status}}"
   🚨 있는 것만 읽지 말고 **없는 것을 세십시오.** 이전 세션에서 그걸 놓쳐
      product-service·partner-service 가 죽은 채로 라이브QA 여러 라운드가 돌았습니다
2  git pull && .\scripts\sync-claude-memory.ps1
3  수치는 그 PC 에서 다시 세십시오 (양 PC 시드 상이)
```

### 🔴 배포·복구 시 `--no-deps` 를 빠뜨리지 마십시오

```bash
./gradlew :services:<svc>:bootJar --no-daemon
docker compose -f infrastructure/docker-compose.yml -f infrastructure/docker-compose.local-all.yml \
  up -d --build --no-deps <svc>          # ← --no-deps 없으면 postgres·eureka·gateway 가 재생성됩니다
```

`--no-deps` 를 빼서 스택 전체를 `Created` 로 만들어 놓은 적이 있습니다(데이터는 무사).

### 🔴 포트 8086 을 `influxd` 가 가져갔습니다

slip-service 를 재생성하는 순간 포트가 잠깐 풀리고 InfluxDB 가 낚아챕니다.
개발책임자 프로세스라 죽이지 않고 **로컬 전용 오버라이드**로 우회했습니다:

```text
infrastructure/docker-compose.local-portfix.yml   ← 🚫 커밋하지 마십시오
  ports: !override 로 host 8186 → container 8086
  (서비스 간 통신은 docker 네트워크의 http://slip-service:8086 이라 영향 없음)
```

---

## 1. 개발책임자 결정 (이번 세션)

| # | 결정 |
|---|---|
| **수량동기화 방향** | 주품목 → 부자재를 타겟으로. 칩은 `품목명:수량` |
| **수량의 출처** | 🔑 구성품·이름에서 추론하지 않는다. **오로지 수량동기화 설정값이 정한다** |
| **세트 기본구성품** | 기본 0건 세트 **72개를 지정해 준다** → `V37` 로 이행 완료 |
| **정액DC 분류 우선순위** | 가장 하위 분류가 이긴다 (S > M > L) · 품목 개별 override 최우선 |
| **결정 형식** | 결정이 필요하면 **항상 선택지를 제시** (AskUserQuestion · 예시 포함 · 대가 명시) |
| **조사 규칙** | 🆕 조사·제안 전에 **①코드 ②이슈 ③기존결정 3축 대조** |
| **`#1162`** | 집PC 진행 안 함. env 파일 전달 완료 |

### GAS 조사 확정 결정 → `docs/dev-reports/2026-08-11-gas-sweep-devlead-decisions.md`

```text
D-G1  영업수수료 정산 도메인 신설
      + 문서번호 YYYY/MM/DD-N 채번 (저장소 표준 · 일자별 atomic 시퀀스)
      + ApprovalReferenceDocType 에 7번째 값 추가 → 지출결의서에 참조 첨부
      + 정산 화면에 그룹웨어 연결 버튼 (저장소 최초 사례 — 참고 패턴 0건)
D-G2  DC 상한을 주문서 48% 로 통일  ⚠️ PM 권고 A(문서별 보존)와 다른 선택
      🚨 구현 전 "49% 로 이미 나간 견적" 건수를 먼저 셀 것. 소급 변경은 그 숫자 보고 재판단
D-G3  원장 특례 9199/9549/1089 — 영향액 산출 후 명시 매핑 (집PC 실측 0건 = 판정 불가)
      조사자 권고 정련: 1089 는 #1072 에서 확정 · 레거시 수입분에만 provenance 기반 특례
D-G4  거래처 4자리 비밀번호 — 한시 허용 후 강제 재설정 (제한기간 길이는 착수 시 확인)
```

**잔여 상정 대기 9건** — 같은 파일 §2.

---

## 2. 트랙 상태판

| 트랙 | PR | 상태 | 다음 한 수 |
|---|---|---|---|
| 수량동기화 칩 | `#1126` | ✅ **머지 `2c62202c6`** | — |
| 버전이력 모달 | `#1134` | ✅ **머지 `97da5590`** · 워크트리 정리 완료 | — |
| 판매전표 계보 | `#1131` | R9 `7a4b2612d` (V119 이관) · SOL R10 중 | R10 결과 |
| 세트전개·정액DC | `#1132` | V37 `6524e5083` + 정액DC 프런트 `5857da4ac` · SOL R2 중 | R2 결과 |
| UUID 잔여노출 | `#1164` | R6·R7 `f47ebcbfe` · SOL R8 중 | R8 결과 |
| 자격 노출 | `#1162` | ⏸️ 회사PC 이월 | §5 |

### 🚩 이번 세션의 지배적 결함 패턴

**모든 SOL 라운드가 "직전 fix 가 정상 경로를 막았다/가렸다" 를 찾았습니다.**

```text
#1134 R13  UUID 판별이 너무 넓어 '1-1-1-1-1' 을 은폐
#1134 R16  actorId 동일성 미확인으로 과잉 은닉
#1163 R2/R3 이름 소실 → 이중 decode
#1131 R2~R8 계보 검증을 느슨하게 하려다 매 라운드 새 표면
⟹ 재수렴 첫 각도를 **"차단되면 안 되는 것이 차단되는가"** 로 고정하십시오.
   그리고 좁히는 쪽 / 넓히는 쪽 **양방향**으로 물으십시오
```

---

## 3. GAS 전수조사 — 결론에 도달

```text
분모 이력   889 → 3,200 → (Critic 반증) AST 기준 최소 3,595
   🚩 두 번 연속 틀렸습니다. Critic 을 붙인 것이 값을 했습니다
   v1 은 **포팅본을 조사하고 원본 GAS 를 안 봤습니다**(원본 2,096 · order-app 358 이 0건)

🔑 진짜 산출물 = 원본에만 있는 업무규칙 이름 **257개**
      유실 22 · 대체 109 · 불필요 77 · 판정보류 49
   🔑 **109개가 '대체'** — 이름만 다르고 기능은 이미 있었습니다.
      "레거시 이름으로 grep 해서 없다" 를 "기능이 없다" 로 읽으면 안 됩니다

산출물   2026-08-11-gasv2-CRITIC.md · -gasv3-remainder.md ·
         -gas-sweep-devlead-decisions.md · (진행) -gasv4-lost-rules.md
```

---

## 4. 🚨 메모리 인덱스가 잘려 나가고 있었습니다 (고침)

```text
.claude/memory/MEMORY.md   98,829 bytes   ← 읽기 한도는 24KB
⟹ 239개 항목 중 앞 30개만 로드되고 209개는 매 세션 보이지 않았습니다
   훅을 전부 걷어내고 제목만 남겨 21,528 bytes 로 축소 (커밋 3fe20030b)
⚠️ 파일명만으로 239줄 × 약 55 bytes = 13KB 라 여유가 없습니다.
   항목이 더 늘면 훅이 아니라 **항목 자체를 병합·폐기**해야 합니다
```

---

## 5. `#1162` — 회사PC 로 이월

```text
✅ jar BOOT-INF 평문 0건 (378파일 스캔) · 소스 잔존 28건은 전부 docs/·.claude/memory/
🔴 미해결 — 컨테이너 제거 + postgres_data 볼륨 유지 시 부트스트랩이 새 랜덤 자격을
   만들어 전 서비스 DB 인증 실패 (격리 실험으로 실증)
   좌표 scripts/ensure-local-env.sh:86 · infrastructure/scripts/ensure-local-env.ps1:51
🔑 개발책임자께 전달한 infrastructure/.env 를 회사PC 에 넣으면 이 상황 자체가 안 생김
```

---

## 6. 이 세션에서 굳힌 절차

```text
🚨 슬롯이 비면 즉시 채운다 — 순서 = 결과 회수 → 다음 라운드 발주 → 그다음 커밋·게시
🚨 라운드마다 커밋한다 — #1131 은 R4~R9 다섯 라운드가 미커밋으로 쌓여 있었다
🚨 SOL 판정을 릴레이하지 않는다 — #1164 는 SOL 이 머지 차단을 냈으나
   PM 이 실 DB 를 직접 세어(발화 0건) 게이트 ①에서 뺐다
🚨 codex 브리핑에 감사·우회·보안 어휘를 쓰면 콘텐츠 필터에 걸린다 (#1131 R10 1회 실패)
   → 중립적 표현으로 바꿔 재발주하면 통과한다
🚨 docker ps 는 **없는 것을 세는 도구**다
🚨 새 워크트리는 의존성이 없다 — npm install 후 package-lock 은 커밋 전 되돌릴 것
```
