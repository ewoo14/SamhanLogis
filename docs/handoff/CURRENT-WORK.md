# 현재 작업 — 2026-08-08 야간 (진행 중)

> 개발책임자 취침 · PM 자율 진행 중. 이 문서만 읽으면 즉시 이어갈 수 있습니다.

---

## 0. 오늘 머지 3건

| PR | 이슈 | 머지 SHA |
|---|---|---|
| #1138 QA 증거 덮어쓰기 가드 | #1116 ✅close | `70677436d` |
| #1137 적용 Flyway 마이그레이션 편집 가드 | #1136 ✅close | `e027bf6bf` |
| #1120 전역 입력 UX | #825 ✅close | `6e05ec9f3` |

---

## 1. #1124 마감날짜 — **머지 직전**

브랜치 `feat/1123-closed-date-guard` · 워크트리 `t1123` · HEAD `9f369dfa3`

```
S17  중앙 가드 SlipClosedDateGuard.assertAllowed() 도입
S19  inspect() 누락 + CI red(가드 mock)
S21  권한 없음이 마감 409 로 바뀌던 것 → 권한 검증을 먼저
S23  권한 화면 노출 (계열 확인으로 1개 더 발견)
S25  폐기 권한 제거 (V91·V92 근거)
S27  🔑 전수 차집합으로 전환 — PUT/DELETE 외 mutation 8개 추가 발견
     gateway 공개 4경로 no-strip · period-lock 회수 UI
S29  MASTER 권한 캐시 미구축 (EffectivePermissionMaterializer) · 회수 3축
S30  가드가 verifyVersion 보다 앞이라 낙관적 잠금 충돌 → 순서 교정
main 병합  #1120 을 품어 Frontend Desktop red 해소 · 충돌 0
```

**남은 것**
```
1  CI green 확인 (9f369dfa3)      백그라운드 감시 중
2  라이브QA 재실시                 규칙이 크게 확대돼 필수
3  머지
```
재배포 완료: `slip-service` · `auth-service` · `api-gateway`

🔑 **이 트랙의 교훈**: `inspect()` 하나(S19) → 복원·revision(S17) → PUT/DELETE(S27) 로 **세 번 반복된 뒤에야** 전수 차집합으로 바꿨고, 그러자 8개가 한 번에 나왔다. 하나씩 발견될 때마다 고치는 방식을 일찍 그만뒀어야 했다.

---

## 2. #1144 회계전표 — 명세 16규칙 · 조사 완료 · 계획 수립됨

개발책임자 명세를 16개 규칙으로 분해하고 **전수 조사 + 실행 계획**을 마쳤습니다.

**보고서**
```
docs/dev-reports/2026-08-08-1144-accounting-spec-gap-survey.md    규칙 11개 차집합
docs/dev-reports/2026-08-08-1144-implementation-plan.md           17묶음 · 의존 그래프 · 우선순위
docs/dev-reports/2026-08-08-1144-deposit-withdrawal-survey.md     입출금 실측
docs/dev-reports/2026-08-08-1144-cash-ledger-menu-survey.md       메뉴 3분할 실측
```

**우선순위 — 없는 기능보다 "지금 틀린 값" 을 앞에**
```
P0-A  VAT 표시 정정      330,000 저장인데 363,000 표시 (합계 33,000원 차이)  ← 가장 위험
P0-B  PageCode 정합      13개 계정 GET 403 · 3개 저장 403                    ← 진행 중
P0-C  채무 원장 누락      INBOUND 20건/12거래처가 원장 밖                      ← 조사 중
P0-D  분개 인과 정렬      현행은 "세금계산서 발행 → 채권 분개" 로 명세와 반대   ← 가장 위험
```

**확정된 결정**
```
자기 회사 구분 플래그 불요 — 기초거래처에 있음 ((주)삼한공조시스템 · 2148720659)
규칙 15·16(메뉴 3분할)은 바로빌 자격과 무관하게 지금 가능 (화면에 탭이 이미 있음)
활성 체크 + 10분 바로빌 자동수집은 #922 소관 (자격 확보 대기)
CODEF 는 DRY_RUN — 실 연동이 켜진 적 없음. 마스터 0건의 이유
```

워크트리 `t1144` · 브랜치 `feat/1144-accounting-slip-spec` (main 에서 신규)

---

## 3. #896 구글 의존성 제거 — 조사 완료 · P0 완료

**개발책임자 방향**: 품목 데이터 원천이 구글 시트면 안 된다 · 시트는 **1회성 이전 다리**로만 · 합격 기준은 **"이관 후 지금 레거시처럼 그대로 출력되는가"**

**완료**
```
P0   골든·열 계약·적재 manifest 고정   diff 0 · 재현성 2회 일치 · 절대경로 0
     scripts/generate-896-p0-golden-manifest.mjs
     docs/qa/896-p0-golden-manifest/
```

**수집된 사실 (13개 보고서)**
```
수식 인벤토리     3,391건 → 2,648그룹 (DATA_OK 524 / PARTIAL 551 / CODE_ONLY 1,560 / UNKNOWN 14)
parity            불합격 확정 (같은 커밋 재측정으로 confound 제거)
                  카탈로그 2,326 · 세트전개 681 · 가격셀 979
                  다중문맥 sheet 134 ↔ DB 2 · 토글 sheet 215셀 ↔ DB 2셀
시트 탭           27개 중 code-read 17개 전부 CSV 확보
열 계약           납품가-2 = 문맥 가격 · 141개는 "가격" 이 아니라 배분 "가중치"
비율 기제         싱글세트 6:4/4:6 구현됨 · 데이터 아님 · 상업멀티는 재배분 없음
재현 검증         일치 22 · 불일치 83 · 판정불가 36
누락 열           싱글 할인 247곳 실제 영향 / 할인·1WAY할인 유효값 0건
셀 의미           $L$2·$I$1 = 변동DC 여부 · '용량' 단위 = kW  (개발책임자 답변)
```

🚨 **앱 기본 소스는 이미 DB** (`CATALOG_SOURCE` 기본값 `db`, 시트는 명시 opt-out). 초기 판단("호스팅만 우리 것")은 과장이었음.

**선행**: #1143 (세트 구성품 비중·반올림을 데이터로) — P1 설계안 작성 중

---

## 4. #1142 되돌리기 — B 범위 확정 · 선행 조건 규명됨

```
회계·세금계산서는 CONFIRMED 이후 별도 명령
  → #1144 규칙 11(회계전표는 삭제 후 재생성)과 맞물려 되돌리기가 회계를 역처리할 필요 없음

🚨 품목·금액 수정은 DRAFT/SAVED 에서만 가능
  → 목적(품목·금액 수정)을 이루려면 거기까지 되돌려야 함
🚨 역연산이 없는 재고 API 4개 (전수 검색으로 확정)
  deduct · ship-batch · lots/inbound · instances/batch
되돌림 대상 60건 (INBOUND 20 + OUTBOUND 40)
```

**개발책임자 판단 대기**: 역연산 4개를 별도 트랙으로 뺄지 함께 갈지

---

## 5. 개발책임자 판단 대기 목록

```
#1142   역연산 API 4개를 별도 트랙으로 뺄지
#1144   P0-A/P0-C/P0-D 착수 승인 (금액·원장에 닿아 PM 이 보류)
#1143   비중 재현 불일치 83건을 어떻게 다룰지 (P1 설계안에서 선택지 제시 예정)
#922    바로빌 계약·자격증명 (착수 블로커)
```

---

## 6. 운영 규칙 — 이번 세션에서 배운 것

```
🚨 이슈를 늘리지 말고 기존에 흡수할 것 (개발책임자 지시 2026-08-08)
🚨 codex 잔재를 죽이기 전에 워크트리를 먼저 볼 것
   — 이번에 #1111 S11 보고서 213줄 + 캡처 5장을 그렇게 살렸다 (PR #1117 은 이미 머지된 상태였다)
🚨 Workflow 산출을 응답이 아니라 파일로 나를 것
   — 64k 초과가 치명적 사건에서 회복 가능한 사건이 된다 (에이전트 8개가 전부 겪고 전부 회복)
🚨 "구현됐다" 는 기록을 그대로 믿지 말 것
   — #1093 이 COMPLETED 로 닫혔는데 데이터 모델이 없었고,
     핸드오프의 "정본 탭" 기록이 실제 앱과 달랐다
```

---

## 7. 환경

```
Docker   slip-service · auth-service · api-gateway = #1124 브랜치 빌드 (9f369dfa3 시점)
         override 필수: -f docker-compose.yml -f docker-compose.local-all.yml
                        -f docker-compose.slip-port-override.yml
SA 키     C:\dev\samhan-homepage-260f8ae469cc.json  (프로젝트 상위 폴더)
시트 CSV  scratchpad/live_sheet* · live_sheet_more (거래처·담당자·추천실외기·구형_템플릿)
```

🚨 **백엔드를 바꿨으면 push 직후 PM 이 재배포한다.**
