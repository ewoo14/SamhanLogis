# 현재 작업 핸드오프 노트

## 2026-08-05 집PC 야간 세션 마감 — **머지 1건** · 3트랙 CI green · 회사PC 인계

> **다음 세션(회사 PC)은 이 절만 읽으면 된다.**

### 0. 한 줄 결론

`#1067` 을 머지했고, `#1061`·`#1063`·`#1045` 를 **전부 CI green + mergeable** 상태로 올려 놓았다. 세 트랙 모두 **최종 SOL 재수렴 하나씩**만 남았다.

```text
머지  7ba5f00f8  [FIX] #1055 재고 0 창고 소실 (VIRTUAL 합성 행 · 표시/복사 계약)  → 이슈 #1055 close
```

---

## 1. 🚨 회사 PC 에서 먼저 할 것

```powershell
git pull
.\scripts\sync-claude-memory.ps1
```

⚠️ **집PC 실측 수치는 회사PC 에 그대로 쓰면 안 된다.** 아래 6절 참조.

**착수 순서** — `#1063`(가장 가까움) → `#1061` → `#1045` → `#1057`·`#1066`

---

## 2. 트랙별 상태

| PR | HEAD | ① 결함0 | ② CI | ③ QA | 다음 한 수 |
|---|---|---|---|---|---|
| `#1067` | — | ✅ | ✅ | ✅ | **머지됨** `7ba5f00f8` |
| `#1063` | `614450c0b` | R19 6건 → R20 으로 범위 분리 | ✅ 49 | R3·R4 실시 | **최종 SOL → 머지** |
| `#1061` | `65296fe20` | R43 2계열 (실 데이터 0건) | ✅ 49/49 | ✅ R14 PASS | **도달성 판정 → 머지** |
| `#1045` | `20eac9d0c` | 미실시 | ✅ 49 | ②③ PASS · ④ 403 | **S10 권한 축 분리** |
| `#1057` | `a66c84c01` | fix 3/3 소진 | ✅ 42 | 미완 | QA 완주 → SOL |
| `#1066` | `53c6f1e05` | 미실시 | ✅ 42 | 미실시 | 적대검증 → QA |

---

## 3. 🚨 각 트랙의 "다음 한 수" 상세

### `#1063` — 최종 SOL 만 남음

R20 에서 **편집 라우트를 분리**했다(개발책임자 결정). `SlipDetailPage`·`CollaborativeSlipInput`·`slip-collab-panel.spec.ts` 가 `origin/main` 과 **바이트 단위로 동일**함을 `git diff` 로 확인했고 잔재 0건이다. **main 병합도 완료**(뒤진 0).

```text
머지되는 것   자동완성 판독성 · 후보 2건+ 모달 · 견적/분개/이동 자동 빈행
              autoBlankRow 유틸 · 빈행 저장 제외 규약
분리된 것     /sales/:id/edit — 별도 이슈로 등록 필요 (아직 미등록)
```

🚨 **회사PC 에서 먼저 할 것: 편집 라우트 후속 이슈 등록.** SOL R19 가 낸 6건이 그대로 사양이다.

```text
· 무수정 저장만으로 VAT 포함 단가가 공급단가로 재해석돼 금액이 오른다
  hydrate  unitPriceWithVat ?? unitPrice → 화면 unitPrice  (SlipFormPage.tsx:165-186)
  BE       받은 unitPrice 를 VAT 제외 공급단가로 정의       (SlipLine.java:153-174)
  VAT포함 1,100 → 공급단가 1,100 / 부가세 110 / VAT포함 1,210
· 거래처 교체 시 새 ID·이름과 기존 코드·사업자번호 혼재
· 창고·출고구분·기사·주소 변경이 저장 성공 후 폐기
· 저장 직후 상세가 5분 캐시로 수정 전 값 표시
· 기존 라인 비고가 hydrate 되지 않아 저장 시 소실
· BUNDLE 옵션 UI 는 열리는데 PUT 계약에 옵션이 없어 평면 라인 저장
```

⚠️ **판매전표 수정 화면에서 품목 추가는 여전히 불가**(main 과 같은 상태). 그게 후속 이슈의 존재 이유다.

### `#1061` — 도달성 판정만 남음

R43 이 결함 2계열을 냈으나 **실 데이터에서 전부 0건**이다. "코드상 가능"과 "실제 도달"을 갈라야 한다.

```text
낸 것    다중 거래처 journal 금액 중복 귀속 / 기초잔액에 R42 전체라인 계약 미적용
실측     다중 거래처 journal 0건 · 식별자 공백/중복 0건 · 금액 부풀림 0건
유지     R39 48·44·51 구간 산식 · R14 세 경로 일치 (P-2026-0028 = 60,267,900)
```

📌 **D2·D3 는 개발책임자 결정으로 종결** — `created_by='system'` SLIP 분개 29건·502,700,000원 **포함 유지**.

### `#1045` — S10 권한 축 분리 (중단된 상태에서 재개)

라이브QA S9 가 ④에서 막혔다.

```text
CarrierAdminController 의 모든 엔드포인트가 hr.carriers 로 막혀 있다 — VIEW 까지
인사 카테고리는 대표실 부서 + MASTER 게이트라 배차담당자(DISPATCH/MANAGER)는 도달 불가
⟹ 배차담당자가 운송사를 조회조차 못 해 ④ 운송사 지정이 구조적으로 불가능
```

📌 **PM 브리핑 탓이다** — S1 에서 *"마스터는 `hr.carriers`"* 만 걸고 *"배차 화면에서의 운송사 조회는 배차 권한"* 을 걸지 않았다.

**S10 불변식**

```text
1  배차담당자가 그룹에 운송사를 지정할 수 있다 (목록·단건 조회 = dispatch.board 축)
2  운송사 마스터 변경(등록·수정·비활성화·삭제)은 hr.carriers 유지
3  인사 > 운송사 목록 화면은 종전대로 hr.carriers 게이트 (S6 계약 유지)
4  권한 세 층 일치 — FE 키 · BE 가드 · catalog
```

⚠️ S10 을 시작했다가 세션 종료로 **중단했고 부분 변경은 되돌렸다**(브랜치 깨끗함).

**그 밖 남은 것** — ⑥ 아로로지스 GUI 로그인 후속 오류(원인 미규명) · ① 가배차 8모드(A안으로 기존 전표 미발화)

### 🆕 `#1045` 가배차 화면 설계 (개발책임자 결정 · 미구현)

> *"화면을 2:1로 나누고 좌측에는 가배차된 내역을, 우측에는 생성된 배차그룹을. 좌측 전표행을 우측 그룹으로 드래그 또는 더블클릭으로 넣는다."*
> *"배차그룹 테두리는 차량 형태로, 차종에 따라 테두리 디자인을 다르게."*

```text
차종 실측 (VehicleTonnage)
  UI 노출    1 · 1.5 · 2.5 · 3 · 5 · 10 · 20톤   (7종)
  UI 비노출  TONNAGE_1_4 · TONNAGE_BIG (카톡 파싱 호환 · @deprecated)
```

S10(권한) 다음에 구현한다.

---

## 4. ✅ 개발책임자 결정 (2026-08-04~05) — 전부 반영

| 항목 | 결정 |
|---|---|
| `#1045` 범위 | **후속 분리 취소 — 이번 트랙에서 ①~⑥ 전부** |
| 가배차 위치 | 화면·데이터는 **삼한**, 분류 계산도 **삼한으로 이전**(S2 완료) |
| 아로로지스 | **수신 표시 전용** · legacy 8모드 UI 제거(S4 완료) |
| 운송사 마스터 | **신설** · 메뉴명 **"운송사 목록"** · **인사** 하위 |
| 외부 운송사 | 전송 안 함 · **기록만** |
| 구매전표 | **가배차 미경유** · 검색으로 그룹 편입만 |
| `#1061` D2 | `system` SLIP 분개 29건 **포함 유지** |
| `#1063` 편집 | **별도 이슈로 분리** · 검증된 것만 머지 |
| 가배차 화면 | **2:1 · 드래그/더블클릭 · 차량 형태 테두리(차종별)** |

---

## 5. 🔑 이 세션의 방법론 — 다음 세션도 이대로

### ① fix 3연속 실패면 **진단으로 전환**한다 (세 트랙에서 확인)

```text
#1061  R33·R34·R36 → R37 결함 4건 → R38 진단 → R39 계약 재설계 → R14 실측이 예측과 원 단위 일치
#1063  R5·R7·R9 → R11 진단 → R12 분리 → R15 write 경계 → 뿌리 제거
```

덧대는 fix 보다 **한 번 멈추고 진단하는 쪽이 라운드를 줄인다.**

### ② 🚨 라이브QA 가 PASS 낸 숫자를 기대값으로 굳히지 마라

`#1061` R13 이 `P-2026-0028 = 30,567,900원` 으로 PASS 했는데, 그 값은 **orphan 매출 29,700,000원이 누락된 결함 하 계산값**이었다. PM 이 그것을 RED-B 로 걸어 **결함을 정본으로 만들 뻔했다.**

```text
불변식이어야 할 것    세 경로가 서로 일치한다 · 산식이 성립한다
불변식이 아닌 것      특정 금액 값 자체
```

### ③ 브리핑에 **"제 전제가 틀렸다면 고치지 말고 중단·보고"** 를 항상 넣어라

**하루에 네 번 작동했다.**

```text
· "모드 4·5 창고 조건은 계승 이탈" → 레거시 원문이 반박. 계승 훼손 방지
· "S3 에서 전송까지"                → 전송 API 부재 지적. 추측 구현 방지
· "시산표가 새 계약 때문에 깨졌다"   → 시산표는 그 서비스를 안 씀. 진짜는 mock 배선 누락
· "sales.slip.edit 는 이 PR 신규"    → 기존 권한(cc030f67c·V36)이라 되돌리지 않음
```

### ④ 새 메모리 규칙 (`main` `3d3ab7fe8`)

`feedback_pm_verifies_round_and_directs_next_fix` — 라운드마다 PM 이 ①산출물 실물 확인 ②핵심 주장 1개 이상 재확인 ③다음 fix 브리핑 5절 ④커밋→즉시 게시.

---

## 6. 🚩 PM 이 만든 낭비 — 반복하지 말 것

- **전제를 실측하지 않고 지시해 결함을 만들었다.** *"수정 모드에 빈행 유지"* 가 **그 화면에 품목 선택기가 없다**는 전제를 안 본 채 들어가 도달 결함 2건(입력값 소실·협업 lineId 재부착, 각 2,174건)을 낳았다.
- **A안(빈행 요구 제외)을 권장했는데 틀렸다.** 개발책임자 *"빈행이 없으면 품목을 어떻게 추가해?"* 로 드러났다 — `origin/main` 에는 **기존 전표에 품목을 추가하는 경로가 아예 없었다**(행 추가 버튼은 alert 만, SlipFormPage 는 신규 전용).
- **CI 판정에서 `CANCELLED` 를 놓쳤다.** `실패 0 · 진행 0` 만 보고 green 으로 읽었는데 실제는 `SUCCESS 48 + CANCELLED 1`. SOL 이 증거 무결성으로 잡았다. **앞으로 `성공 수 = 전체 수` 로 센다.**
- **구현자 보고를 대조 없이 믿을 뻔했다.** `#1045` S5 가 *"Playwright 가설 A 확정"* 이라 했는데 `git status` 를 보니 **스펙이 하나도 안 바뀌어** 있었다. 릴레이했으면 CI red 를 한 번 더 돌았다.
- **QA 산출물 경로를 안 봤다.** `#1061` R14 가 캡처를 `clients/desktop/clients/desktop/...` 중첩 경로에 만들고 `.mjs` 드라이버를 남겼다. 옮기고 드라이버는 커밋에서 뺐다.
- **게이트웨이 라우트를 안 걸었다.** S1·S3 가 컨트롤러만 만들어 `/admin/dispatch-groups`·`/admin/carriers` 가 404 였고 QA 가 ②~⑤를 통째로 못 봤다. **CI 의 `Config Audit Guard` 도 이 축은 안 본다.**

---

## 7. 환경 메모 — ⚠️ 집PC 실측 (회사PC 에서 재측정)

```text
배포본 (전 트랙 공유 · 마지막 상태)
  api-gateway        2026-08-04T22:34:18Z  ← S8 새 라우트 포함
  slip-service       2026-08-04T22:13:20Z  ← #1045 S1~S6 코드
  arologis-service   2026-08-04T22:13:20Z  ← #1045 S4 수신 코드
  accounting-service 2026-08-04T14:14:35Z  ← #1061 R39 코드
  inventory-service  2026-08-04T10:39:09Z  ← #1067(머지됨) 코드

마이그레이션 적용 최고
  slip_db 105 (이 브랜치 V104·V105) · auth_db 93 (#1045 가 V94 추가)
  arologis_db 24 (#1045 가 V25 추가) · accounting_db 96 · product_db 30

집PC 시드 (회사PC 와 다름)
  delivery_tag  DAY 52 · REGION 12 · STACK 11 · RETURN_RENTAL 10 · (NULL) 2270
  slips         활성 OUTBOUND 2,309 (수정가능 DRAFT/SAVED 2,174) · INBOUND 46
  창고          HQ-001 · CS-001 · VH-001 · VR-001 + QA-1039-SANGIL · QA-1039-CHOWOL
  운송사        AROLOGIS 1건 (is_arologis=true)
  원장 확정상태  32전표 / 93라인 / VAT포함 355,861,000원

계정
  삼한        dev_manager · dev_master · dev_accountant  모두 dev_p05_pass!
  아로로지스   admin / admin1234  (자체 인증 · API 127.0.0.1:8097 · 삼한 계정 안 통함)

함정
  slip-service 는 docker-compose.slip-port-override.yml 을 함께 줘야 기동 (호스트가 8086 선점)
  compose up -d --build <svc> 는 depends_on 까지 빌드하려다 실패 → build 후 up -d --no-deps
  Playwright 로컬 확인은 CI=1 로 새 서버 강제 기동 (stale 5173 재사용 시 오판)
```

## 8. ⚠️ 커밋하면 안 되는 미추적 파일

```text
t1001b  clients/desktop/playwright/1001-r5-ledger-real-qa/ · 1001-r6-ledger-real-qa/
        clients/desktop/playwright/1001-r14-real-qa.mjs · 1001-r14-real-qa-result.json
        (앞의 둘은 로컬 real-QA 집합 게이트를 건드려 전체 typecheck 가 중단된다. CI 무영향)
t1062   docs/qa/1062-line-input-real-qa/renderer-real-qa*.log  (잔여 renderer 가 잡고 있음)
main    .gradle-user-t1008b/ · docs/dev-reports/2026-08-03-874-live-qa.md
```

---

## 2026-08-04 회사PC 세션 마감 — **머지 0건** · 🚨 라운드 상한 3 도입 · 집PC 인계

> **다음 세션(집PC)은 이 절만 읽으면 된다.**

### 0. 한 줄 결론

머지는 없다. 대신 **라운드가 무한증식하던 원인을 규명해 상한 3과 fix 자기표면 종료조건을 규칙으로 박았고**, 6트랙 중 5트랙이 게이트 ②(CI green)를 채웠다. 병목은 전부 ③ 라이브QA 로 옮겨졌다.

---

### 1. 🚨🚨 집PC 에서 먼저 — **시드 데이터가 다르다**

> 개발책임자: *"집PC는 회사PC와 다르게 시드 데이터(테스트 데이터)에서 차이가 있으므로 주의."*

**이 문서의 모든 수치는 회사PC 실측이다. 집PC 에서 그대로 쓰면 안 된다.** 오늘 이 계열로 세 번 틀렸다(`미삭제 127`→118 · `dev_master`→`dev_manager` · `REGION 12건`→0건).

```powershell
git pull
.\scripts\sync-claude-memory.ps1
```

**착수 전 반드시 다시 셀 것**

```sql
-- 전표
SELECT slip_type, status, COUNT(*) FROM slips WHERE is_deleted=false GROUP BY 1,2;
SELECT delivery_tag, COUNT(*) FROM slips WHERE is_deleted=false GROUP BY 1;
-- 분개
SELECT created_by, source_type, COUNT(*) FILTER (WHERE is_deleted=false) FROM journals GROUP BY 1,2;
-- 창고
SELECT type, COUNT(*) FROM warehouses WHERE is_deleted=false GROUP BY 1;
```

⚠️ 회사PC 에서 개발책임자 승인 하에 **soft delete** 한 것이 있다(집PC 엔 해당 없을 수 있음).

```text
accounting_db  SYSTEM_SEED MANUAL 분개 19건 + journal_lines 44건  (미래 날짜 시드)
slip_db        2026/06/24-901 · 902  ·  2026/08/03-QA-1013-* 5건
보존           dev_master 2026-05-22 일괄분 (MANUAL 2,106 · CASH_RECEIPT 370 · CASH_DISBURSEMENT 26)
```

> 🔑 이관분은 **테스트 데이터가 맞지만 보존**했다. 데이터를 지워서 없앤 결함은 AWS 실배포에 진짜 데이터가 들어오면 그대로 재발하기 때문이다. 코드가 journal-only 행을 처리하게 만드는 것이 맞다.

---

### 2. 🚨 오늘 확정된 규칙 — **fix 라운드 상한 3** (개발책임자 결정)

→ [`feedback_fix_round_self_closure_3cap`](../../.claude/memory/feedback_fix_round_self_closure_3cap.md) · MEMORY.md 최상단

**진단**: 라운드를 늘린 것은 결함을 못 찾아서가 아니다. 결함은 매번 첫 라운드에 나왔고, **fix 가 만든 새 표면을 다음 라운드에서 발견**한 것이다.

```text
fix 브리핑 필수 3절
  ① 새로 가능해진 상태·화면 조합을 열거하고 각각 밟아라
  ② 제거·이동·개명한 식별자를 grep 으로 전수 조사하라
  ③ 바꾼 파일을 참조하는 테스트를 전부 실행하라
     3-1 생성자·빈 배선을 바꿨으면 컨텍스트 로드 IT 를 최소 1개 (오늘 실증)

PM 브리핑 3절
  ④ 수치는 SQL 원문 그대로 — 요약 라벨 금지
  ⑤ QA 시나리오마다 발화 조건 카운트, 0 이면 "만들 수 있나" 부터
  ⑥ 환경 변경 전 호출 관계를 코드로 확인
```

🚫 **상한은 절차를 줄이는 것이지 결함 허용이 아니다.** 닿으면 현 상태 + 엣지 목록을 올려 판단받는다.

---

### 3. 트랙별 상태 — 게이트 ②는 5트랙이 찼다

| PR | HEAD | ① 결함0 | ② CI | ③ QA | fix | 다음 한 수 |
|---|---|---|---|---|---|---|
| `#1057` #874 riUsage | `a66c84c01` | R29 3건 → R30 해소 | ✅ 42/42 (`9ac2586f0`) | ⚠️ **미완** | **3/3 소진** | **QA 이어서** → SOL 재수렴 → 머지 판정 |
| `#1061` #1001 원장 | `d64ad5c57` | R31 8계열 → R33 4뿌리 | 🔴 **10잡 fail** | 🔴 무효 | 1/3 | **R34 배선 수정** |
| `#1045` #1039 가배차 | `09778d88b` | R15 진단 → R16 fix | ✅ **49/49** | ❌ 8모드 미발화 | 1/3 | arologis 재배포 → QA |
| `#1063` #1062 라인UX | `0347d3fc0` | 미실시 | ✅ **49/49** | ❌ | 1/3 | **QA 먼저** |
| `#1067` #1055 창고 | `11d51ae3e` | 미실시 | ✅ **38/38** | ❌ | 1/3 | **QA 먼저** |
| `#1066` #1065 검수결재 | `53c6f1e05` | 미실시 | ✅ **42/42** | ❌ | 1/3 | 적대검증 → QA · **FE 2단계는 #1057 머지 후** |

#### `#1061` R34 가 먼저 — 원인 확정됨

```text
Caused by: BeanCreationException → BeanInstantiationException → NoSuchMethodException
```

R33 이 `LedgerSnapshotService` 에 `LedgerSnapshotBatchNoGenerator` 를 주입하고 `PartnerLedgerReadModelService` 도 바꿨는데, **옛 생성자 시그니처로 인스턴스화하는 곳이 남아 Spring 컨텍스트가 통째로 안 뜬다.** 무관한 IT 까지 전부 죽었다. **제품 로직 변경 없이 배선만** 잡으면 된다.

---

### 4. ✅ 개발책임자 결정 (2026-08-04 회사PC) — 전부 반영

| 항목 | 결정 |
|---|---|
| **라운드 상한** | **3** · 닿으면 현 상태+엣지 목록으로 판단 요청 |
| `#1065` 결재선 권한 | **문서유형 · 액션 · 상태로 경계** (`OUTBOUND × INSPECTING × 검수 결재자`) |
| `#1061` 채권 잔액 | **기초 포함 잔액** (기초 + 기간매출 − 기간수금) |
| `#1061` 판매전표 금액 | **VAT 포함 문서금액** (기획·레거시에 이미 확정 — 선택지 아님) |
| `#1061` 시드 정리 | **이관분 보존 · 시드·QA 잔재만 삭제** |
| `#1061` 계약 재설계 | fix 로 덧대지 말고 **계약에 불변식** · 상한 리셋 |
| `#1068` 판매전표 헤더 | **프로젝트 불필요 · 전표구분 필요** |

---

### 5. 🆕 새 이슈 — `#1068` 판매전표 헤더 (개발책임자 지적)

이카운트 실화면과 대조해 **거래처 자동채움이 사실상 없음**을 확정했다.

```text
전잔 · 후잔 · 미수잔액   0건 (아예 미구현)
대표이사 · 전화번호 · 주소 · 거래처 특이사항 · 담당자 · 할인율   전부 0건
```

**기사명·기사 연락처 건 — 계승은 잘 됐고 추가물이 문제였다.**

```text
✅ 레거시 배송기사내역 표 → DispatchSmsPage.tsx:423 에 정확히 계승 (매칭 로직까지 호환)
❌ 그런데 판매전표에도 기사 입력 UI 가 있다 (b1334dc4a · notification-slice-B)
   resolveDriverPhone: 레거시 표 우선 → 전표 필드 폴백 → 진실원이 둘
조치: 판매전표의 기사 입력 UI 제거 · 입력은 배차 메뉴 하나로 통일
```

⚠️ `SlipFormPage.tsx` 는 `#1063` 이 고치는 중이라 **그 PR 머지 후 착수**.

🔑 **계승 PR 의 검증 각도 제안** — 적대검증이 *"도달성"* 만 물어서 **"레거시에 없는 것이 추가됐는가"** 를 아무도 안 물었다. `#1013` 은 머지된 트랙인데 이탈이 남아 있었다. 메모리 규칙화 검토 필요.

---

### 6. 🚩 PM 이 만든 낭비 — 반복하지 말 것

- **컨테이너를 "무관" 으로 단정해 중지했다.** `arologis-service` 를 내렸는데 **가배차가 그 서비스를 쓴다**(`/admin/arologis/dispatches/pre-classify`). QA 가 503 → `exit=137` 로 짚어 재기동. **내리기 전에 호출 관계를 코드로 확인할 것.**
- **QA 잔재로 지운 전표가 다른 트랙의 QA 표본이었다.** `2026/06/24-901·902` 가 `#1057` 의 유일한 `COMPLETED` 출고전표였다. **지우기 전에 "이 데이터를 쓰는 트랙이 있나" 를 물을 것.**
- **수치를 요약하다 두 번 틀렸다.** `COUNT(*)` 를 "미삭제" 로 라벨링(127→118) · QA 캡처 계정을 `dev_master` 로 오기(실제 `dev_manager`). **SQL 원문을 그대로 넘길 것.**
- **codex `cwd` 를 빠뜨렸다.** 잘못된 워크트리에서 돌 뻔했다(중단·재발주로 무손실). **매 발주마다 `cwd` 확인.**
- **QA 브리핑에 launch config 를 안 적었다.** `virtual:pwa-register` 오류로 한 번 헛돌았다. 데스크톱 QA 는 **`vite.renderer.dev.config.ts`** 로 띄워야 한다(`vite.config.ts` 는 dev serve 에서 그 가상 모듈을 안 준다).
- **틀린 전제로 삭제 지시를 받았다.** 실측으로 범위가 `journals 2,521건` → `19건` 으로 좁혀졌다. **지우기 전 실측은 계속 유지할 것.**

---

### 6-1. 🆕 새 이슈 2건 — 둘 다 `SlipFormPage.tsx` · `#1063` 머지 후 직렬 처리

```text
#1068  판매전표 헤더 거래처 자동채움 + 전잔·후잔
       전잔/후잔/대표이사/전화번호/주소/거래처 특이사항/담당자/할인율  전부 0건
       개발책임자 결정: 프로젝트 불필요 · 전표구분 필요
       ⏸ 확인 필요: `전표구분` 이 현행 `출고구분`(delivery_tag) 의 정칭인가 별개 축인가

#1069  세트 전개가 입력 화면에 반영되지 않는다
       서버는 전개함 (SlipService.create → addSlipLinesExpanded)
       화면은 전개 없음 → 사용자는 세트 1줄만 보고 저장
```

**`#1069` 사양 확정 (개발책임자)**

```text
① 세트품목 입력  →  구성품 N개로 전개 · 세트품목 라인은 사라진다
                    ⚠️ 현재 구현은 `첫 setHead + parentSetModel` 로 헤드를 남긴다 — 바꿔야 함
② 구성품만 단독 입력도 허용
③ 전개 참조원 = 견적품목
   세트품목 식별  product_estimate_exposure 에 등록된 product_type='BUNDLE'
   구성품 조회    bundle_component (bundle_product_id → component_product_code)
④ 세트품목은 전표 라인으로 저장 불가 (①과 같은 이야기)
```

실측 (회사PC · 집PC 재측정 필요)

```text
BUNDLE 343개  →  견적 노출 343개 (100%)      ← 세트품목은 전부 견적품목이 맞다
bundle_component 1,584건 · 구성품 보유 세트 343개 (전부 보유)
구성품 코드 → products 조인  1,519 성공 / 65 실패   ← 유효한 결손. #1051 계열
```

---

### 6-2. 🚨 PM 수치 오류 4건 — 전부 **권위 아닌 것으로 셈**

오늘 같은 계열로 네 번 틀렸다. 메모리 규칙 ④(*"수치는 SQL 원문 그대로"*)에 **"권위 테이블·권위 구분을 먼저 확정하라"** 를 더해야 한다.

```text
① COUNT(*) 를 "미삭제" 로 라벨링           127 → 실제 미삭제 118
② QA 캡처 계정을 dev_master 로 오기        실제 dev_manager (SOL 이 정정)
③ 세트 구성품을 products.parent_bundle_set_model 로 계수
   "세트 111개 구성품 미등록" → 실제 권위 테이블 bundle_component 로는 343개 전부 보유
④ products 에 BUNDLE 행이 있다 → "세트품목이 기초품목에 실재한다"
   실제 products 는 저장 테이블일 뿐이고 업무 구분은 product_estimate_exposure 가 한다
   개발책임자가 맞았고 PM 이 저장 위치를 업무 구분으로 오독했다
```

🔑 **네 건 모두 "숫자를 잘못 셌다" 가 아니라 "무엇을 세는지 틀렸다" 이다.** 계수 전에 **권위 원본이 무엇인지 먼저 확정**할 것.

---

### 7. 환경 메모 — ⚠️ 회사PC 실측 (집PC 에서 재측정)

```text
Docker         컨테이너 19개 가동 (arologis·dashboard·groupware 는 메모리 확보로 중지했다가 arologis 만 복구)
               ⚠️ 총 RAM 15.9GB · 여유 1~3GB 로 빠듯. codex 3~4개 + 브라우저가 한계
배포본         slip-service 2026-08-04T01:39:40Z (#1057 코드 · sourceType 심볼 확인)
               api-gateway 2026-08-03T08:02:47Z (main)
               arologis-service 2026-07-14 (재기동만 함 · 재빌드 안 함)
DB 백업        스크래치패드 dbdump/ 에 accounting_db · slip_db pg_dump -Fc
               ※ 세션 종료 시 소실 — 집PC 는 다시 뜰 것
마이그레이션    accounting_db V95 적용 · #1061 이 V96 추가 (ADD COLUMN only · DROP 없음)
계정           dev_manager(MANAGER) · dev_master(시스템 마스터 그룹, account row 없음)
전표 실재       QA 가 만든 2026/08/04-2·3·4 (DAY·DAY·STACK) 보존 — #1045 8모드 QA 에 필요
```

---


> **다음 세션(회사 PC)은 이 절만 읽으면 된다.**

### 0. 한 줄 결론

회사PC 종일 세션이 **25+라운드 머지 0건**으로 끝난 상태를 이어받아 **2건을 닫았고**, 나머지 4트랙을 전부 마지막 관문까지 밀어 올렸다. 라운드 70여 건을 커밋·게시했다.

```text
머지
  2dc4f7915  [FIX] 이카운트 품목 임포트 — 모델코드 일치 품목 병합 (#984)
  (#1059)    [FEAT] 배차안내문자 완전계승 — 하차일별 그룹 문구 + 레거시 매칭 계승
```

---

## 1. 🚨 회사 PC 에서 먼저 할 것

```powershell
git pull
.\scripts\sync-claude-memory.ps1
```

**그다음 이슈 #1064·#1065 부터 착수한다** (개발책임자 결정).

⚠️ **Docker·DB 상태는 집PC 것이라 회사PC 에 그대로 적용되지 않는다.** 아래 6절의 수치는 집PC 실측이므로 회사PC 에서 다시 재야 한다.

---

## 2. ✅ 개발책임자 결정 (2026-08-04) — 전부 반영 완료

| 항목 | 결정 | 반영 |
|---|---|---|
| `#1057` 시나리오 2~5 | **A — 후속 분리하고 현 검증 범위로 머지** | PR 기록 완료 |
| `#1061` 원장 상태 집합 | **`INSPECTING`·`SHIPPING` 포함** | R22 로 구현 변경 완료 |
| `#1061` slip 없는 journal 매출 | **`SALE_SUMMARY` 문서로 표시** | 현재 구현 유지 |
| 이슈 #1064·#1065 | **다음 세션에서 먼저 처리** | 이 문서 3절 |

`#1061` 상태 집합 변경이 만든 것:

```text
변경 전  CONFIRMED·DELIVERED·COMPLETED       21전표 / 62라인 / 197,476,400원
변경 후  + INSPECTING 5전표 87,841,600
         + SHIPPING   5전표 68,803,900
         = 31전표 / 89라인 / 354,121,900원
```

> R7 라이브QA 가 화면에서 본 `354,121,900원` 이 **개발책임자 기준으로는 맞는 값**이었다. 당시 PM 이 SOL 수치를 기대값으로 삼아 "불일치" 로 판정했으나, 실제 문제는 **경로마다 집합이 달랐던 것**이지 값 자체가 아니었다.

**정의는 `shared/common/.../ledger/PartnerLedgerContract.java` 한 곳**이고, 집계·상세·인쇄가 그 계약을 소비한다. 다르게 판단하시면 거기만 바꾸면 세 경로가 따라온다.

---

## 3. 🚨 다음 세션 최우선 — 이슈 2건

둘 다 **전표 lifecycle 을 막아 여러 트랙의 라이브QA 를 동시에 차단**한다. 먼저 뚫으면 이후 처리량이 올라간다.

```text
#1064  입고 전표 lifecycle 버튼과 API 전이 역전 — GUI 만으로 입고 완료 불가
       완료(검수 시작) → /inspect 호출 · BE 는 /complete 요구 → 409
       완료(처리 완료) → /complete 호출 · BE 는 /process 요구 → 409

#1065  출고 전표 검수 불가 — 결재선 계정도 INSPECTING 에서 진행 못 함
       dev_manager        "출고 검수 권한이 없습니다 — 검수자 결재자(그룹/개인)만"
       kimgicheol         검수 완료 버튼 disabled
       kimeunji           전표 상세 "전표를 불러오지 못했습니다"
       → CONFIRMED 도달 불가 → 회계 배분 불가(422 SAS_SOURCE_SLIP_NOT_CONFIRMED)
```

⚠️ `#1057` 의 시나리오 2~5 는 **#1065 가 풀려야** 실행 가능하다.

---

## 4. 트랙별 상태와 다음 한 수

| PR | HEAD | ① 결함 0 | ② CI | ③ 라이브QA | 다음 한 수 |
|---|---|---|---|---|---|
| `#984` | — | ✅ | ✅ | ✅ | **머지됨** |
| `#1059` | — | ✅ | ✅ | ✅ | **머지됨** |
| `#1057` | `81129d14a` | R23 SOL 2건 → R24 로 해소 | ✅ **42/42** | 10차까지 · 2~5 는 #1065 로 미발화 | **R24 재수렴 1회 → 머지** |
| `#1061` | `d8b4e4071` | R25 SOL 1건 → R26 으로 해소 | 확인 중 | ✅ **R12 전부 PASS** | **R26 재수렴 1회 → 머지** |
| `#1045` | `bb892e99d` | — | ✅ 49/49 | 미실시 | **QA 먼저** — 적요 라이브 재현 |
| `#1063` | `cac258c3a` | 미검증 | 🔴 1잡 | 미실시 | **QA 먼저** |

### `#1057` — 머지까지 한 걸음

개발책임자 A안 결정 + CI 42/42 green. **R24(오늘 밤 제품 코드 변경)만 재수렴하면 머지**다.

```text
R21  PROCESSING 다음 액션 inspect → complete       (제품 결함이었음)
R22  전 상태 전이 매핑 전수 sweep — INSPECTING 수정
R23(SOL)  INSPECTING 반려 누락 · 모바일 라벨 의미 불일치
R24  상태별 허용 액션 전수 대조 · reject 노출 · 라벨 "재고 반영 후 검수 대기"
```

⚠️ 라벨 문구는 사용자 노출 텍스트다. 개발책임자께서 다른 표현을 원하시면 그 한 곳만 바꾸면 된다.

### `#1061` — 진자를 구조로 끝냈다

```text
R13 SOL 5건 → R14 → R15 → R16   증상 fix 3연속 라이브 BLOCK
R17 진단(코드 무변경) → R18 공통 산출기 통합 → R8 전부 PASS
R19 SOL 2건 → R20 → R9 PASS → R21 SOL 0건
R22(결정 반영) → R10 오판(환경) → R23 진단 → R24 → R11 PASS
R25 SOL 1건 → R26 → R12 전부 PASS (snapshot 12→13→13 · 세 경로 일치)
```

---

## 5. 🔑 이 세션의 방법론 — 다음 세션도 이대로

### ① 라이브QA 를 먼저 (회사PC 결정이 옳았다)

```text
#1061 R14 fix  CI 42/42 + 로컬 전부 통과  →  라이브 BLOCK (모든 거래처 오염)
#1061 R15 fix  로컬 통과                  →  라이브 BLOCK (정상 금액 0)
#1061 R16 fix  46 tests 0 failed          →  라이브 BLOCK (상세·인쇄 어긋남)
#1061 R22 fix  로컬 통과                  →  라이브 BLOCK
```

**PM 은 세션 초반에 순서를 거꾸로 했다**(적대검증 → fix → … → QA 마지막). `#1059` 는 R18~R22 다섯 라운드를 돌고서야 첫 라이브QA 를 했다. `#1045`·`#1063` 은 **QA 먼저**.

### ② QA 통과는 결함 0 이 아니다 — SOL 을 반드시 붙인다

```text
#1061 R8 QA "전부 PASS"  →  R19 SOL 도달 결함 2건 (무필터 차단 87,562,200원 · UUID 노출 351,000,000원)
#1061 R11 QA "전부 PASS" →  R25 SOL 도달 결함 1건 (snapshot 저장 전면 도달 불가)
#1057 QA 10차 후          →  R23 SOL 도달 결함 2건
```

**SOL 이 세 트랙에서 8건을 잡았고 전부 QA 가 PASS 를 낸 뒤였다.**

### ③ fix 브리핑에 **양방향 RED** 를 걸 것

한쪽만 보는 fix 가 반대 방향 결함을 만든 사례가 **여섯 번**이다.

```text
#984  R7   오차단 160건 막으려다 정상 주문 3,489건 거부
#1061 R14  오염 ↔ R15 정상 금액 0 ↔ R16 부분
#1059 R22  날짜 반영 ↔ R29 교차 날짜 섞임
#1061 R18  세 경로 통합 ↔ R19 무필터 차단 + UUID 노출
#1061 R24  GET 자동저장 제거 ↔ R25 저장 기능 전면 소실
#1057 R22  전이 매핑 ↔ R23 반려 버튼 소실
```

**RED-A(기능이 동작한다) / RED-B(결함이 재발 안 한다) 를 각각 테스트로 고정하고 동시 GREEN 을 요구할 것.**

### ④ QA 브리핑에 **환경 확인 절**을 넣을 것

"결함처럼 보이는 것" 이 환경 문제였던 사례가 **다섯 번**이다.

```text
#1057  404              다른 PR 빌드가 배포돼 있었음
#984   422 미재현       배포본이 커밋보다 앞섬 (SOL 이 image·jar·commit 시각 대조로 적발)
#984   GUI 없음         운영/API 작업 · PM 브리핑 오류
#1057  409              임포트 전 데이터 상태
#1061  R10 전면 BLOCK   QA 가 다른 워크트리의 mock 프런트를 띄우고 측정
```

마지막 건은 인쇄에 뜬 금액이 **구 정적 mock 과 원 단위로 일치**해서 잡혔다. QA 브리핑에 **워크트리 경로 · 포트 · 호출 API** 를 먼저 기록하게 할 것.

### ⑤ 계승 PR 은 레거시 원문에서 답을 찾는다

```text
Index.html:421-424 · 1351-1354   배송기사내역 입력 표     → #1059 R19 계승 본체
Code.js:313-328                  전표 매칭 4형태          → #1059 R27
Index.html:407·415·424           날짜별 패널 구조         → #1059 R30
```

*"우리 구조가 레거시와 어떻게 다른가"* 를 물으면 매번 근본 원인이 나왔다.

### ⑥ fix 3연속 새 표면이면 **구조 진단**으로 전환

`#1061` 이 R14·R15·R16 을 전부 되돌려받은 뒤 **R17 진단(코드 무변경) → R18 통합**으로 한 번에 닫혔다. R23 도 같은 방식으로 R10 오판을 규명했다.

---

## 6. 🚩 PM 이 만든 낭비 — 반복하지 말 것

- **`git add -- clients` 가 미추적 QA 하네스를 삼켜 CI 가 깨졌다.** 커밋 전 `git status --porcelain` 을 눈으로 대조하고 **파일을 지목**할 것.
- **전수 sweep 을 시키면서 축을 하나만 줬다.** `#1057` R22 에 *"상태 → 다음 전이"* 만 물어 **반려 버튼이 통째로 사라진 채** 통과했다. *"도메인이 허용하는 액션 전부 vs 화면이 노출하는 액션 전부"* 로 물을 것.
- **좁힌 브리핑에서 Playwright fallback 안내를 잘라내** 라운드가 `No browser is available` 로 멈췄다. QA 브리핑에는 **항상** 그 절을 넣을 것.
- **틀린 전제로 개발책임자께 두 번 판단을 요청**했다(고아 분개 29건 — 실제로는 참조 전표 전부 실재, `source_ref_id` 가 v3 UUID). **지우기 전에 반드시 실측.**
- **모호한 측정을 PASS 로 읽었다.** `snapshot 12 → 12` 는 *"조회가 저장 안 함"* 과 *"저장 경로가 없음"* 을 구분하지 못한다. **같은 수치가 정반대 두 상황에서 나오는지** 먼저 물을 것.
- **집PC 자원**: Docker 21개 + gradle + Playwright + codex 4개를 동시에 돌려 사용자 게임이 죽었다(여유 13.5GB). **집PC 는 개발 전용이 아니다.**

---

## 7. SOL 이 PM 인용 수치를 네 번 정정했다

```text
관계 XLSX "제목 행 1개"      → 실제 160행·1,424셀 (PM 이 역으로 정정 · 트랙 정지를 막음)
활성 OUTBOUND 2,307/2,288    → 실제 2,309/2,290
CI "42/42 green"             → 그 시점 41/42 + 1 진행 중
"활성 거래처 50곳"            → 실제 ACTIVE 45 + SUSPENDED 5
```

**적대검증에 증거 무결성 각도를 남겨 둔 것이 값을 했다.**

---

## 8. 환경 메모 — ⚠️ 집PC 실측이라 회사PC 에서 다시 잴 것

```text
accounting_db  V68 이 빠진 채 V95 까지 올라간 구멍 → 개발책임자 승인으로 out-of-order 적용해 메움
               ※ 회사PC QA 가 flyway 를 끄고 돌 수밖에 없었던 근본 원인
auth_db        V90 → V93 (#1059 배포로 V91·V92·V93 적용)
product_db     V26 → V30 · 품목코드 공백 1,120/1,220 → 387/3,183 (#984 임포트)
slip-service   호스트 influxd 가 8086 선점 → docker-compose.slip-port-override.yml 로 18086
inventory      전 품목 inventory_qty_mgmt=true — 재고 없이 출고 불가 (실 입고 필요)
계정           dev_manager·dev_dispatch = V5 시드 비밀번호 / janyeonggu = 기존 QA 자격
               회계 배분은 dev_accountant (accounting.sales-slip.accounting 보유)
전표 실재 날짜  2026-08-03 6건 · 08-01 4건 · 07-27 66건 · 07-17 73건 (모두 -1 전표 있음)
세트           구성품 전부 코드 보유: AC060CS1DBC1SY · AF60F19D11WRS · AF70F17D11LRS
QA throwaway   2026/08/03-1~6 · 2026/06/24-901·902 · 2026/08/03-QA-1013-* · QA-874-*
               accounting_db source_type='SLIP' AND created_by='system' 29건 (2026-06-23)
               → 집계·카운트에서 제외할 것
```

---

## 9. ⚠️ 커밋하면 안 되는 미추적 파일

```text
t874     clients/desktop/playwright/874-riusage-real-qa.spec.ts
           디렉터리 없이 놓여 있어 H-2 가드 위반 · 오늘 실수로 커밋돼 CI 가 깨졌다
t1001b   clients/desktop/playwright/1001-r5-ledger-real-qa/
         clients/desktop/playwright/1001-r6-ledger-real-qa/
           로컬 real-QA 집합 게이트를 건드려 전체 typecheck 가 중단된다 (CI 는 영향 없음)
main     .gradle-user-t1008b/ · docs/dev-reports/2026-08-03-874-live-qa.md
```

---

## 2026-08-04 집PC 야간 세션 (진행 중) — 머지 1건 · 두 트랙 머지 임박

> **다음 세션은 이 절부터 읽으면 된다.**

### 0. 트랙 현황

| PR | HEAD | ① 결함 0 | ② CI | ③ 라이브QA | 남은 것 |
|---|---|---|---|---|---|
| `#984` | — | ✅ | ✅ | ✅ | **머지됨** `2dc4f7915` |
| `#1061` | `bb16f6416` | R19 SOL 진행 | ✅ **49/49** | ✅ R8 전부 PASS | SOL 결과 → 머지 |
| `#1059` | `b602ffe34` | R26 1건 → R27 닫음 | ✅ 42/42 | ⑤ 하나 남음 | R29 ⑤ → 머지 |
| `#1057` | `e9898181b` | ✅ | ✅ 42/42 | 시나리오 2~5 미발화 | **바운드** — A/B/C 판단 대기 |
| `#1045` | `bb892e99d` | — | ✅ | 미실시 | 적요 라이브 재현 |
| `#1063` | `cac258c3a` | 미검증 | — | 미실시 | QA 먼저 |

### 1. ✅ 개발책임자 결정 (2026-08-04) — 전부 답 옴

| 항목 | 결정 |
|---|---|
| **`#1057`** 시나리오 2~5 | **A — 후속 분리하고 현 검증 범위로 머지** |
| **`#1061`** 원장 상태 집합 | **`INSPECTING`·`SHIPPING` 도 포함** (구현 변경 필요 → R22) |
| **`#1061`** slip 없는 journal 매출 | **`SALE_SUMMARY` 문서로 표시** (현재 구현 유지) |
| **이슈 #1064·#1065** | **다음 세션에서 먼저 처리** |

🚨 **다음 세션은 이슈 #1064·#1065 부터 시작한다.** 둘 다 전표 lifecycle 을 막아 여러 트랙의 라이브QA 를 동시에 차단한다.

```text
#1064  입고 lifecycle 버튼과 API 전이 역전 — GUI 만으로 입고 완료 불가
#1065  출고 검수 불가 — 결재선 계정(kimgicheol·kimeunji)도 INSPECTING 에서 진행 못 함
```

`#1061` 상태 집합 결정이 바꾸는 것:

```text
현재 정본   CONFIRMED · DELIVERED · COMPLETED       21전표 / 62라인 / 197,476,400원
결정 반영   + INSPECTING 5전표 87,841,600 + SHIPPING 5전표 68,803,900
            = 31전표 / 89라인 / 354,121,900원
```

> R7 라이브QA 가 화면에서 본 `354,121,900원` 이 **개발책임자 기준으로는 맞는 값**이었다. 당시 PM 이 SOL R13 수치를 기대값으로 삼아 "불일치" 로 판정했으나, 실제 문제는 **경로마다 집합이 달랐던 것**이지 값 자체가 아니었다.

**참고 — 취소된 판단 요청**: `accounting_db` 의 system SLIP 분개 29건은 "고아" 가 아니라 **조인 키가 어긋난** 것이다(참조 전표 전부 실재 · `source_ref_id` 가 v3 UUID). PM 이 삭제 직전 실측으로 막았다.

### 2. 🔑 이 세션의 방법론 — 두 가지가 결정적이었다

**① 라이브QA 가 CI green PR 을 네 번 되돌렸다**

```text
#1061 R14 fix  CI 42/42 green + 로컬 전부 통과  →  라이브 BLOCK (모든 거래처 오염)
#1061 R15 fix  로컬 통과                        →  라이브 BLOCK (정상 금액 0)
#1061 R16 fix  46 tests 0 failed                →  라이브 BLOCK (상세·인쇄 어긋남)
#1059 R19 fix  통과                             →  R21 SOL 이 날짜 드리프트 발견
```

회사PC 결정 `feedback_live_qa_first_not_last` 가 옳았다. **PM 은 이 세션 초반에 순서를 거꾸로 했다**(적대검증 → fix → … → QA 마지막). 남은 트랙(`#1045`·`#1063`)은 **QA 먼저**.

**② fix 3연속 새 표면이면 증상을 쫓지 말고 구조를 봐야 한다**

`#1061` 이 R14·R15·R16 을 전부 라이브에서 되돌려받은 뒤 **R17 진단(코드 무변경) → R18 구조 통합** 으로 한 번에 닫혔다.

```text
원인  집계·상세·인쇄가 식별·필터 로직을 세 벌 갖고 있었다
조치  PartnerLedgerReadModelService 공통 산출기 · 세 경로가 같은 계약 소비
결과  R8 에서 결함 1~4 전부 PASS · 세 곳 금액 동일
```

### 3. 🚩 PM 이 만든 낭비 — 반복하지 말 것

- **`git add -- clients` 가 미추적 QA 하네스를 삼켜 CI 가 깨졌다.** 커밋 전 `git status --porcelain` 을 눈으로 대조하고 **파일을 지목**할 것.
- **계열 전수 sweep 을 지시하지 않아** `#1057` R21 이 한 칸만 고쳤고 QA 한 사이클을 더 썼다(R22 가 전 상태 대조표로 정리).
- **좁힌 브리핑에서 Playwright fallback 안내를 잘라내** 라운드가 `No browser is available` 로 멈췄다. QA 브리핑에는 **항상** 이 절을 넣을 것.
- **틀린 전제로 개발책임자께 두 번 판단 요청**(고아 분개 29건). 지우기 전에 반드시 실측.
- **집PC 자원**: Docker 21개 + gradle + Playwright + codex 4개를 동시에 돌려 사용자 게임이 죽었다(여유 13.5GB). 집PC 는 개발 전용이 아니다.

### 4. SOL 이 PM 수치를 세 번 정정했다

```text
관계 XLSX "제목 행 1개" → 실제 160행·1,424셀 (PM 이 정정 · 트랙 정지를 막음)
활성 OUTBOUND 2,307/2,288 → 실제 2,309/2,290
CI "42/42 green" → 그 시점 41/42 + 1 진행 중
```

**적대검증에 증거 무결성 각도를 남겨 둔 것이 값을 했다.**

### 5. 등록한 이슈

```text
#1064  입고 전표 lifecycle 버튼과 API 전이 역전 — GUI 만으로 입고 완료 불가
#1065  출고 전표 검수 불가 — 결재선 계정(kimgicheol·kimeunji)도 INSPECTING 에서 진행 불가
```

둘 다 라이브QA 가 아니면 발견되지 않았을 것들이다.

### 6. 환경 메모 (집PC)

```text
accounting_db  V68 이 빠진 채 V95 까지 올라간 구멍 → 개발책임자 승인으로 out-of-order 적용해 메움
               (회사PC QA 가 flyway 를 끄고 돌 수밖에 없었던 근본 원인)
auth_db        V90 → V93 (#1059 배포로 V91·V92·V93 적용)
slip-service   호스트 influxd 가 8086 선점 → slip-port-override.yml 로 18086
product_db     V26 → V30 · 품목코드 공백 1,120/1,220 → 387/3,183 (#984 임포트)
inventory      전 품목 inventory_qty_mgmt=true — 재고 없이 출고 불가
계정           dev_manager·dev_dispatch = V5 시드 비밀번호 / janyeonggu = 기존 QA 자격
전표 실재 날짜  2026-08-03 6건 · 08-01 4건 · 07-27 66건 · 07-17 73건
```

---

## 2026-08-03 집PC 야간 세션 — **머지 1건** · 라이브QA 가 CI green PR 을 세 번 되돌림

> **다음 세션은 이 절부터 읽으면 된다.** (아래 종일 세션 절은 회사PC 기록)

### 0. 한 줄 결론

회사PC 종일 세션이 25+라운드 머지 0건으로 끝난 상태를 이어받아, `#984` 를 닫고 나머지 4트랙을 전부 마지막 관문까지 밀어 올렸다.

```text
머지   2dc4f7915  [FIX] 이카운트 품목 임포트 — 모델코드 일치 품목 병합 (#984)
```

### 1. 🚨 이 세션이 확인한 것 — "라이브QA 가 보고한 결함" 의 절반은 결함이 아니다

QA 가 올린 차단 중 **다섯 건이 제품 결함이 아니었다.**

```text
#1057  /internal/slips/by-period 404   →  다른 PR(#1059) 빌드가 배포돼 있었음
#984   R13 의 422 미재현                →  배포본이 커밋보다 앞섬 (SOL 이 image·jar·commit 시각 대조로 적발)
#984   GUI 임포트 진입점 없음 BLOCKER   →  임포트는 운영/API 작업. PM 브리핑 오류
#1057  409 productCode 는 필수          →  임포트 전 데이터 상태
#984   검색 120초 미완료                →  화면 진입 실패. 백엔드는 15~30ms
```

🔑 **QA 결과를 집계하기 전에 ①배포본 나이 ②그 PR 이 그 표면을 건드렸는지 ③데이터 조건**을 먼저 확인할 것.

반대로 **SOL 적대검증은 CI green 인 PR 에서 도달 결함 8건**을 냈다. 이 세션 SOL 3회 = 결함 8건.

### 2. 트랙별 상태

| PR | HEAD | CI | 라이브QA | 다음 한 수 |
|---|---|---|---|---|
| `#984` | — | — | — | ✅ **머지됨** |
| `#1057` | `2028f9fc8` | 확인 중 | 8차까지 미발화 | **QA 9차** — PROCESSING→CONFIRMED 완주 후 배분·전기 |
| `#1061` | `7a48b50d4` | ✅ 42/42 | R6 진행 중 | R6 결과 → 재수렴 → 머지 |
| `#1059` | `f174dfd22` | ✅ 42/42 | 미실시 | auth·notification 재배포 → 라이브QA |
| `#1045` | `bb892e99d` | ✅ | 미실시 | slip 재배포 후 적요 라이브 재현 |

### 3. `#1057` — 여덟 관문을 하나씩 걷어냈다

```text
1차  /internal/slips/by-period 404   배포 불일치      해소
2차  409 productCode 는 필수          #984 머지로 충전  해소
3차  409 재고 부족                    실 입고로 생성    해소
4차  수락 성공                                          —
5차  403 배분 권한 없음               dev_accountant    해소
6차  422 전표 상태 CONFIRMED 요구     상태 전이 필요    —
7차  PROCESSING→INSPECTING 경로 없음  R21 fix          해소
8차  INSPECTING→COMPLETED 도 뒤집힘   R22 전수 sweep   해소
```

🔑 **7·8차는 제품 결함이었다.** 프런트가 호출하는 엔드포인트와 BE 가 요구하는 것이 어긋나 있었다. R22 가 `DRAFT`~`CONFIRMED` 전 상태를 INBOUND/OUTBOUND 로 나눠 대조표를 만들었고 불일치는 `INSPECTING` 하나였다.

⚠️ **별건 관찰** — 백엔드 엔드포인트 명명이 의미와 반대다.
```text
/complete  =  PROCESSING → INSPECTING
/inspect   =  INSPECTING → COMPLETED
```
전이 의미를 바꾸면 기존 데이터·테스트가 흔들려 이번엔 프런트만 맞췄다.

### 4. `#1061` — 라이브QA 가 fix 회귀를 잡았다

```text
R13 SOL      도달 결함 5건        (CI green 상태)
R14 fix      4건 처리             CI 42/42 · 로컬 전부 통과
R5 라이브QA  BLOCK                세 거래처 전부 197,476,400원 오염 (방향만 바뀜)
R15 fix      회귀 차단 + 사업자번호 조회
R6 라이브QA  진행 중
```

🔑 **CI 와 로컬 테스트로는 어느 쪽도 못 잡는다.** 개발책임자의 "라이브QA 를 첫 라운드로" 결정 근거가 그대로 재현됐다.

### 5. 🚩 PM 이 만든 낭비 — 다음 세션이 반복하지 말 것

- **`git add -- clients` 가 미추적 QA 하네스를 삼켰다** (`874-riusage-real-qa.spec.ts`). CI 가 H-2 가드로 잡음. 메모리에 이미 있는 계열인데 경로를 안 좁혔다. **커밋 전 `git status --porcelain` 을 눈으로 대조하고 파일을 지목할 것.**
- **계열 전수 sweep 을 지시하지 않아** R21 이 한 칸만 고쳤고 QA 한 사이클을 더 썼다.
- **틀린 전제로 개발책임자께 두 번 판단을 요청**했다 — "참조 전표 없는 분개 29건" 은 실제로 29건 전부 참조 전표가 실재했다(v3 UUID 라 조인만 안 됨). 삭제 직전에 확인해서 막았다. **지우기 전에 반드시 실측.**
- **집PC 자원**: Docker 21개 + gradle + Playwright + codex 4개를 동시에 돌려 사용자 게임이 죽었다. 여유 13.5GB 까지 떨어졌다. **집PC 는 개발 전용이 아니다.**

### 6. 환경 메모 (집PC)

```text
accounting_db  V68 이 빠진 채 V95 까지 올라간 이력 구멍 → 개발책임자 승인으로 out-of-order 1회 적용해 메움
               ※ 회사PC QA 가 flyway.enabled=false 로 돌 수밖에 없었던 근본 원인이었다
slip-service   호스트 influxd 가 8086 선점 → docker-compose.slip-port-override.yml 로 18086 매핑
product_db     V26 → V30 · 품목코드 공백 1,120/1,220 → 387/3,183 (#984 임포트)
inventory      stock_instances AVAILABLE 1 → 실 입고로 생성해야 출고 가능
               products 3,183건 전부 inventory_qty_mgmt=true (우회 불가)
```

### 7. 등록한 이슈

- **#1064** 입고 전표 lifecycle 버튼과 API 전이 역전 — GUI 만으로 입고 완료 불가

### 8. 개발책임자 판단 대기 (막지 않음)

- `accounting_db` 의 `source_type='SLIP' AND created_by='system'` 분개 **29건** (생성일 2026-06-23)
  참조 전표는 **전부 실재**하나 `source_ref_id` 가 v3 결정론 UUID 라 연결되지 않음. `source_ref` 는 전 분개 124건에서 비어 있어 대체 조인도 없음.
  링크를 고쳐도 `POSTED` 라 매출 집계에는 그대로 들어감 → **"DRAFT/SAVED 전표에 POSTED 분개가 있는 것이 맞는가"** 가 진짜 질문. `#1061` 을 막지 않음.

---


## 2026-08-03 종일 세션 — **25+라운드 · 머지 0건** · 🚨 워크플로우 순서 개편 결정

> **다음 세션은 이 절만 읽으면 된다.**

### 0. 한 줄 결론

6트랙이 **전부 게이트 ①(도달 결함 0)·②(CI green)를 통과했고, 전부 ③ 라이브QA 에서 되돌아왔다.** QA 를 돌린 **5트랙에서 5번 다** 코드 리뷰가 못 본 결함이 나왔다. 그래서 개발책임자께서 **라이브QA 를 첫 라운드로 옮기는 것**을 결정하셨다.

### 1. 🚨 개발책임자 결정 (2026-08-03)

| 결정 | 내용 |
|---|---|
| **워크플로우 순서** | **전부 완주 — 다만 QA 를 앞으로.** 구현 → **라이브QA 1회** → 적대검증 → fix → 재수렴 → 머지 전 QA 재실시. 단축하는 것은 검증 깊이가 아니라 **순서** → [[feedback_live_qa_first_not_last]] |
| **#984 병합 규칙** | 필드별 **"비어 있지 않은 쪽이 이긴다"** · `AP110RNPPHH1` 싱글 **662,000** |
| **#1062 빈행** | 빈 행 = **품목코드가 확정되지 않은 행**. **수정 모드는 항상 아래 빈행 유지**, 저장 시 미확정 행 자동 삭제 |
| **#1062 자동완성** | **둘 다** — 폭 넓히고 + **후보 2건 이상이면 모달**(이카운트 방식) |

### 2. 트랙별 상태 — 전부 ③에서 막혀 있다

```text
PR      HEAD        ①      ②       ③              다음 한 수
#984    eb9ab2482   ✅0    ✅green  ❌ 임포트 409   🚨 개발책임자 판단 대기
#1057   94fa7c88d   ✅0    대기     ❌ 2~5 미실시   slip 재배포 → QA 재실시
#1059   8c1cea73f   fix완료 ❌red    ❌ PASS아님     R15 CI 2원인 닫기 → 재배포 → QA
#1061   28815b3c9   fix완료 대기     ❌ PASS아님     재배포 → QA R4
#1045   806c8953f   후보1   ✅green  ❌ 부분         적요 판별 + 8모드 표본
#1063   cac258c3a   미검증  대기     ❌ 미실시       🆕QA 먼저(새 순서 첫 적용)
```

### 3. ✅ `#984` — 개발책임자 결정으로 **판단 종결**

📌 **개발책임자 결정 (2026-08-03)**

> **"어쨌든 코드가 다르다면 다른 품목이 맞지."**
> (선행 정정) *"규격은 병합 키에 넣으면 안 된다. 창고 직원들이 헷갈리지 말라고 임의로 넣은 간단한 설명이다."*

```text
품목코드가 다르면  →  다른 품목
동명이코드 164그룹  →  전부 별개. 이름이 같다는 이유로 합치지 않는다
병합               →  관계 원본(품목관계리스트)이 명시한 관계로만
```

**이 결정이 `409 MIG2_ALIAS_DUPLICATE` 를 푼다** — `AAAA-00004`↔`AAAA-00005` 를 이름으로 합치지 않으므로 alias 충돌이 성립하지 않는다. 앞서 정리한 충돌 8건도 판단할 필요가 없어졌다.

🚨 **다음 세션이 먼저 판별할 것 — R11 과의 충돌.** R11 이 넣은 *"공백 포함 라벨은 활성 Product 의 유일한 정확한 `name` 으로도 해소"* 가 이름 기반 경로다. R11 은 `guard 가 정상 주문 3,489건을 전부 막던 것`을 풀려고 라벨 정규화를 넓혔는데, **그 넓힘이 이름 축이면 이 결정과 충돌**한다.

**여섯 번의 진자가 끝났다** — R2 이름 병합→값 소실 / R5 fingerprint→우회 / R7 gate→3,489건 차단 / R9 관계 우선→값 소실 / R11 라벨 확장→이름 경로 잔존 / **결정: 이름 축 제거**. R2 의 33품목 값 소실도 이 결정으로 소급 설명된다 — 애초에 합치면 안 되는 것을 합쳤던 것이다.

**다음 한 수**: ①R11 이름 경로 판별 ②병합 키를 관계 원본 기준으로 확정 ③임포트 재시도 ④`422 MIG2_CSV_HEADER_MISMATCH`(XLSX 직접 업로드 미지원) 확인

### 3-0. (참고) 결정 전에 정리해 둔 분류

📌 **개발책임자 정정 (2026-08-03)** — *"규격은 병합 키에 넣으면 안 된다. 창고 직원들이 헷갈리지 말라고 임의로 넣은 간단한 설명이다."* PM 이 규격명을 업무값으로 세어 분류한 것은 **틀렸다.**

규격명을 빼고 재분류한 결과:

```text
동명이코드 164그룹
  업무값 완전 동일 141 · 한쪽만 값 있음 15 · 실제 값 충돌 8   ← 판단 대상
```

충돌 8건은 전부 `출하가 0 · 입고단가만` 있는 항목(설치 자재·작업 원가)이고 두 갈래다.

```text
A. AAAA 끼리 · 원가만 다름
   삼성추가배관(벽걸이) 12,277 ↔ 13,914 · (스탠드) 13,914 ↔ 16,370
   고소작업차(스카이)   148,512 ↔ 247,521

B. AAAA ↔ ZENG · 코드 체계가 다르고 원가도 다름 (일관되게 AAAA 가 높음)
   바람막이 45,017↔30,000 · 배수펌프 110,496↔80,000 · 천공 16,370↔10,000
   유니온 22,099↔14,000 · 실외기받침대 [상품]↔[무형상품]
```

**B 5건이 더 의심스럽다** — 서로 다른 시기·경로로 등록된 중복 가능성.

**다음 한 수**: 8건 판단 → 병합 키 확정 → 임포트 재시도. `422 MIG2_CSV_HEADER_MISMATCH`(XLSX 직접 업로드 미지원)도 함께 볼 것.

### 3-1. 임포트 실패 원문

실 임포트가 **409 로 중단**됐다. guard 가 fail-closed 로 막아 데이터 오염은 0.

```text
409 MIG2_ALIAS_DUPLICATE
    "동일 alias_code 가 다른 main 에 매핑됩니다: aliasCode=AAAA-00005, sourceRowNo=124"

원인   품목등록.xlsx 의 동명이코드
       AAAA-00004  삼성추가배관(벽걸이)
       AAAA-00005  삼성추가배관(벽걸이)      ← 같은 품목명, 다른 코드
전수   같은 품목명·다른 품목코드  164그룹 / 328품목코드
```

> **품목명이 같고 품목코드가 다른 164그룹을 같은 물건으로 볼 것인가.**
> 같은 물건이면 병합하고 alias 충돌을 규칙으로 풀어야 하고, 다른 물건이면 이름 기반 병합을 좁혀야 한다. 표본을 보면 **두 성격이 섞여 있다**(`방진가대 S2 소`↔`방진가대S2소` 는 표기 차이로 보이고, `00131 AR-EH03`↔`SAR-00006` 은 판단 필요).

R2 의 *"이름만으로 병합 → 33품목 값 소실"* 과 같은 축이다.

### 4. 라이브QA 가 잡은 것 — 코드 리뷰가 못 본 것들

```text
#1059  R13 이 DB 행으로 "V92 권한 회수됨" 확인 → 화면은 그 행을 안 봄 (그 계정이 실제 접근)
#1061  R12 SOL 이 "GUI 클릭 안 함" 명시 → 그 표면에서 422 · 인쇄 미개방
#1061  2차 QA — 집계 금액 전부 공란인데 상세엔 245,612,215원
#1045  적요 입력값 저장 안 됨
#1057  회계 매출전표 배분이 실 모드에서 아예 동작 안 함 (mock 이 덮고 있던 main 선재 결함)
```

### 5. 🚩 PM 이 만든 낭비 — 다음 세션이 반복하지 말 것

- **QA 하네스 커밋이 CI 를 깼다**(#1059 3잡). `docs/qa` 경로 하드코딩 → H-2 가드 위반. #1057 은 워크트리 절대경로라 커밋에서 제외했다 → [[feedback_qa_harness_commit_breaks_ci]]
- **브리핑 오류 3건**(`#1061` 한 PR 에서만) — ①화면 표시 코드를 API 코드로 줌 ②라인 없는 기간 지정 ③*"전표번호 미노출"* 이라는 없는 기준
- **QA 전 발화 조건 미확인 3회** — `#1061` 기간·`#1045` 8모드·`#1057` `model_rows=0`. 전부 미실시로 끝났다

### 6. 스택 배포 조합 (증거를 섞지 말 것)

```text
product-service       #984    08:31:27Z
accounting-service    #1057   07:17:49Z
slip·auth·notification #1059  07:0x
api-gateway           main    08:02:47Z
```

DB 백업 — `slip_db`·`accounting_db`·`product_db`·`partner_db`·`auth_db`·`notification_db` 를 `pg_dump -Fc` 로 확보(스크래치패드 `dbdump/`). **세션 종료 시 소실되므로 다음 세션은 다시 뜰 것.**

### 6-1. 🚩 QA 가 남긴 throwaway 전표 6건 (공유 `slip_db`)

```text
2026/08/03-1                  QA-1039-배차 scheduler throwaway    DRAFT
2026/08/03-QA-1013-MAP-01     … QA-1013-MAP-01                    DRAFT
2026/08/03-QA-1013-MAP-02     … QA-1013-MAP-02                    DRAFT
2026/08/03-QA-1013-PHONE-01   … QA-1013-PHONE-01                  DRAFT
2026/08/03-QA-1013-PHONE-02   … QA-1013-PHONE-02                  DRAFT
2026/08/03-QA-1013-ROW-01     … QA-1013-ROW-01                    DRAFT
```

전부 `DRAFT` 이고 표식이 붙어 있다. 기존 실 데이터는 건드리지 않았다.

🚨 **다음 세션이 집계·카운트를 셀 때 이 6건을 빼고 세라.** 오늘 `QA-GATE-A/B`(6/24 생성 잔재) 때문에 *"원장에 2,400,000원이 왜 뜨나"* 를 한 바퀴 조사했다. → [[feedback_seed_and_qa_residue_pollute_aggregates]]

### 7. 적용된 마이그레이션 (공유 DB)

```text
slip_db V103 · accounting_db V95 · auth_db V92 · notification_db V7 · product_db V30
```

배포 전에는 `slip_db` V59 · `accounting_db` V66 이었다. **이제 공유 DB 가 코드에 근접했으므로 다음 라이브QA 는 마이그레이션 대량 적용 없이 시작한다.**

---

## 2026-08-03 오전 세션 — 4트랙 병렬 · 각 1라운드 진행

> **직전 세션이 PC 다운으로 끊겨 복구 후 재개한 세션이다. 이 절이 최신이다.**

### 0. 한 줄 결론

4트랙을 **간섭 없는 범위에서 병렬**로 돌려 각각 한 라운드씩 진행했다. 머지는 아직 0건이고, `#984` 는 **개발책임자 판정이 닫혀** 착수 조건이 해소됐다.

```text
이번 세션 커밋
  #1061  208b01a0e  R10 재수렴(SOL)   도달 결함 1 + 증거 무결성 1
  #1061  9f09fbde8  R11 fix(LUNA)     후보 집합 확장 · R9 수치 정정
  #1057  4cd9c0505  R14(LUNA)         레거시 분기 15개 통째 이식   ← CI 42/42 green
  #1045  8ec6024ce  R12 fix(LUNA)     CI red + 큐 기아 + claim
  #984   5554def49  재계수(PM)         판정 대상 25그룹 → 1그룹
```

### 1. 🚨 이번 세션에서 확정된 사실 3건

**① `#1061` R9 의 `[실데이터]` 는 이 DB 를 가리키지 않았다.**
R9 는 *"19건 292,000,000원 → 299,498,000원, 근거 있는 3건만 변경"* 으로 안심시켰는데, 근거로 든 2~3월 출고 slip 3건이 실 `slip_db` 에 **존재한 적이 없다**(전표 109건 전부 6~7월 생성). SOL 재산출은 **18건·264,000,000원·변경 0건**. R11 이 R9 보고서에 정정 절을 넣었다(원문 보존).

**② `#984` 는 문제의 성격이 바뀌었다.**
R10 은 *"관계가 맞나 업무값이 맞나"* 양자택일로 제시했으나, 재취득 원본으로 26그룹을 열어 보니

```text
한쪽만 값 있음(공백/0)   23      ← 고를 필요가 없다. 값이 하나뿐
공백 문자 차이만          2      ← 'T 분기관' ↔ 'T분기관'
실제 값 충돌              1      ← 개발책임자 결정: AP110RNPPHH1 싱글 662,000
```

R10 이 B안 대가로 든 *"21품목/37필드 값 소실"* 은 병합 자체가 아니라 **대표품목 값을 그대로 쓰는 병합 규칙**의 대가다. 규칙을 **필드별 "비어 있지 않은 쪽이 이긴다"** 로 바꾸면 26그룹 전체가 소실 0으로 병합된다.

**③ 실 DB 에 시드·QA 잔재가 섞여 집계에 잡힌다.**

```text
accounting_db  source_type='SLIP' 분개 29건   전건 2026-06-19 'system' 생성
               참조 전표가 UUID·전표번호 둘 다 slip_db 에 없음 (삭제분 포함 0)
               참조 UUID 가 version-3(이름 기반 결정론) — 실 생성이면 v4
slip_db        QA-GATE-A/B (2026/06/24-901·902) dev_master 생성 — QA 하네스 잔재
```

`#1061` 원장 화면이 이것들을 매출로 집계한다. **라이브QA 수치를 업무 금액으로 읽지 말 것.** (별건 보고의 *"slip 없는 회계분개 30건·412,300,000원"* 은 어느 정의로도 재현되지 않는다 — 401 기준 POSTED 24건 363,000,000원 / 전 상태 29건 457,000,000원.)

### 2. 트랙별 다음 한 수

| 트랙 | 마지막 | 다음 |
|---|---|---|
| `#1061` 원장 | R11 fix `9f09fbde8` | **R12 재수렴(SOL) 결과 확인** → CI → 라이브QA → 머지 |
| `#1045` 가배차 | R12 fix `8ec6024ce` | **R13 재수렴(SOL) 결과 확인** → CI → 라이브QA |
| `#1057` riUsage | R14 사슬 이식 `4cd9c0505` · **CI green** | **R15 재수렴(SOL) 결과 확인** → 라이브QA |
| `#1059` 배차문자 | R12 fix(LUNA) 진행 중 | 산출물 커밋·게시 → 재수렴. **`CONFLICTING` — PM 이 충돌 해소(=fix 라운드)** |
| `#984` 이카운트 | 판정 닫힘 | 병합 규칙 반영 + **R7 BLOCK(정상 주문 3,489건 차단)** 해소. `CONFLICTING` |

### 3. 이번 세션 방법론 메모

- **간섭 축은 Docker 와 실 DB write 다.** 코드+단위테스트는 워크트리가 다르면 병렬 안전. 라이브QA·이미지 재빌드는 **한 번에 하나**.
- codex 동시 4개까지만 유지했다(그 이상은 처리량 붕괴 실측 있음).
- fix 뒤 재수렴을 **전부 SOL 로** 배정했다 — 메모리의 "SOL 굶음" 계열 재발 방지. 한 세션 SOL 3건.
- 라이브QA 전 **컨테이너 나이**를 먼저 쟀다. 현재 `slip-service`·`accounting-service` 는 **2026-07-27 자**로 stale — 재배포 없이 QA 하면 "없는 기능" 으로 오독한다.

---

## 2026-08-03 새벽 세션 — **머지 7건** · 개발책임자 결정 4건 반영

> **다음 세션(회사 PC)은 이 절만 읽으면 된다.**

### 0. 한 줄 결론

야간에 5트랙을 돌려 **약 45라운드 · 도달 가능한 결함 40여 건**을 잡았다. `#1060` 을 머지했고 나머지 4트랙은 각각 **한 걸음 남았다**.

```text
오늘 머지
  #1048 13:04  창고 UUID 기동 검증        #1035 close
  #1024 13:16  품목명 중복 차단            #1019 close
  #1046 14:39  순번코드 → 모델명 전환      #1000 close
  #1050 16:03  검색 모달 복수 선택         #1049 close
  #1047 16:17  입출고 내역·분석 완전계승   #1012 close
  #1058 16:30  레거시 GAS 일마감 완전계승  #1008 close
  #1060 22:36  주문서 앱 접근권한 설정     #1015 close   ← 새벽 추가
```

---

## 1. 🚨 개발책임자 결정 4건 (2026-08-03 · 전부 PR 기록 완료)

| # | 결정 | 상태 |
|---|---|---|
| **#984** | **D→C** 원본 재취득 후 25그룹 개별 판정 | ⏸ **파일 대기** (3절) |
| **#1059** | **A** 계승 충실 — 표시·복사만, 자동 SMS 제거 | ✅ 반영 → 새 BLOCK 발견 |
| **#1045** | **A** 원천 엄격 유지 — 추측 backfill 금지 | ✅ 반영·검증됨 |
| **#1057** | **B** 행 순서 계승 (PM 권고 A 와 반대) | ✅ 반영 |

> `#1057` 은 PM 이 A(이탈 승인)를 권고했으나 개발책임자께서 **B(순서 계승)** 를 결정하셨다. `line_no` 를 레거시 시트 행 순서와 동일시하는 것이 **결정 사항**이므로, 근거 부재를 이유로 다시 멈추지 않는다.

---

## 2. 트랙별 현황 — 각각 "다음 한 수"

### `#1061` 거래처별 원장 — **가장 가깝다**

```text
① 결함    R9 에서 무필터 집계 경로 fix (292,000,000 → 299,498,000 · 19건 중 3건만 변경)
② CI      직전 42/42 green · 현 커밋(f944aeb7a) 재실행 중
③ QA      재실시 필요
```

**다음 한 수**: CI green 확인 → **라이브QA 재실시** → 머지.

⚠️ QA 브리핑 주의 — 화면은 **사업자번호를 표시**하되 API 엔 `P-2026-0017` 형태를 넘긴다. 지난 QA 가 화면 값을 API 에 써서 400 을 냈고 그건 **PM 브리핑 오류**였다.

### `#1045` 가배차 8모드 — fix 연쇄 4번째

```text
R5 조회가 죽는다 → R6 이 고치며 R7 "발행이 죽는다" 를 만듦
R7 발행이 죽는다 → R8 이 고치며 R9 "정상에서도 저장 안 됨" 을 만듦
R9 저장 안 됨    → R10 이 고치며 R11 "큐 기아·경합" 을 만듦
```

**남은 결함 2건** — ① 재시도 상한·백오프·포기 조건 없음 + oldest Top 100 고정으로 후속 큐 기아 ② executor↔scheduler claim 부재로 중복 호출·optimistic-lock 경합.

**다음 한 수**: 위 2건 fix → 재수렴 → 라이브QA.

🔴 **CI 실패 2건 — 먼저 확인할 것.** 마감 시점 `slip-units` 잡이 `fail` 로 떴는데 run 이 아직 진행 중이라 **로그를 확보하지 못했다**. `bcb946666`(R10 · `V102` pending 큐 + scheduler)이 건드린 표면일 가능성이 높다.
`gh run view 30770662283 --log-failed` 로 시작할 것.

✅ 개발책임자 A안이 실제로 지켜짐을 확인 — V102 기본값 적용해도 **기존 scheduler 대상 0건**(2,455행 기준).

### `#1057` 일마감 riUsage — **접근 전환 필요**

여섯 라운드째 같은 계열이고, 매번 리뷰어가 **빠진 축**을 하나씩 찾아 왔다.

```text
R7   scope 축 누락
R9   행 순서 축 누락
R13  zone 전환 축 누락 + 선행 판정 라우팅 + target-model predicate
```

🔑 **원인은 명확하다.** 레거시 `Code.js:668-734` 는 **라우팅 사슬**(운임·절삭 → `_isOld` → zone 별 분기)인데, 우리는 사슬의 **SINGLE 한 칸**만 구현하고 바깥을 비워 뒀다. 그래서 뒤의 `riUsage` 가 앞 판정을 덮는다.

**다음 한 수**: 패치 말고 **`Code.js:668-734` 판정 사슬 전체를 한 번에 이식**. 지금까지 세운 것(R2·R4·R6·R8·R12)은 사슬 안 SINGLE 분기로 들어간다. 이렇게 안 하면 네 번째 축이 나온다.

라이브QA 는 standalone 이 Docker 내부 `partner-service` 에 못 닿아 미실시(하네스 문제).

### `#1059` 배차안내문자 — **BLOCK · 계승 본체 부재**

A안대로 자동 SMS 는 제거했는데, **남기기로 한 쪽이 비어 있었다.**

> 하차일별 그룹 문구 생성기가 **입력 DTO 부터 존재하지 않는다.** 화면·복사에는 기존 `[배차안내]` **전표별** 문구가 전달된다.

열 라운드 동안 다룬 게 전부 **발송 쪽**(2,000자 분할·`blocked`·포트)이라 **표시 쪽 본체를 아무도 안 봤다.**

그 밖 — `V91` 이 deprecated 테이블만 지워 **실제 권한이 남는다**(VIEW 3명·mutation 1명) · 미매핑 전표 표시·편집 UI 없음 · `SEND_AUDIT` 잔재(mock·QA 계약·생성 API).

**다음 한 수**: 하차일별 그룹 문구 생성기 구현부터.

### `#984` 이카운트 임포트 — **파일 대기**

D→C 결정. **`품목관계리스트` 최신본을 `docs/migration/ecount-data/raw/` 에 넣으면** 25그룹이 여전한지부터 세고 개별 판정에 들어간다.

---

## 3. 🚨 회사 PC 에서 먼저 할 것

1. `git pull` → `.\scripts\sync-claude-memory.ps1`
2. **`품목관계리스트` 최신본 다운로드** → `docs/migration/ecount-data/raw/` (#984 착수 조건)
3. `#1061` CI 확인 → green 이면 라이브QA → 머지

---

## 4. 🔑 이번 세션의 방법론 — 다음 세션도 이대로

### 브리핑 각도 수가 산출물을 결정한다 (실측)

```text
각도 1~4   전부 완주
각도 6~8   7200초 타임아웃 4건 (그중 2건은 산출물 0)
```

메모리에 이미 있던 규칙인데 조항 둘 중 **하나만** 넣었다. 빠뜨린 것은 **"조사 전에 보고서 파일을 만들고 하나 확인할 때마다 append 하라"** — 이게 없으면 타임아웃이 부분 산출물이 아니라 **0** 이 된다.
→ `feedback_narrow_briefing_completes_wide_times_out` 에 세 번째 실측 추가

### 조인 키로 쓰는 코드 컬럼이 비어 있다 (하루 3회)

```text
#1060  주문 2,024건 중 조회되는 것 1건
#1060  주문 전환 출고 20건 전부 NULL
#1061  원장 후보 31건 전부 비어 있음 → 12,276,000원 소실
```

셋 다 CI green 이었고 결과가 오류가 아니라 **200 OK 로 빈 값**이라 `#1060` 이 애써 만든 보류 장치도 못 잡았다.
→ 새 메모리 `feedback_join_key_column_empty_uuid_populated`

### "미판정" 문단이 결함을 낸다

`#1060` R15 가 *"재현하지 못했으므로 미판정"* 이라 적은 문단을 추적했더니, **두 키 공간이 안 겹쳐 8월 8일부터 활동 중인 거래처가 차단될 것**이었다. 그 문단을 0으로 집계했으면 사용자가 먼저 발견했다.

**리뷰 판정을 집계하기 전에 "이 라운드가 안 본 것"을 항상 읽을 것.**

### 저장 규약을 바꾸는 fix 는 생성 경로를 전수로 센다 (2회)

`#1045` 4경로 · `#1060` 7경로 중 **각각 1개만** 고쳐져 있었다.

### fix 는 다음 라운드의 결함을 만든다 (`#1045` 3연속)

조회 → 발행 → 저장 → 큐 운영으로 **층이 내려가며** 크기가 줄었다. 발산이 아니라 수렴으로 본다.

---

## 5. ⚠️ 커밋하면 안 되는 잔여 파일

```text
t874     clients/desktop/playwright/874-riusage-real-qa.spec.ts
t1001b   clients/desktop/playwright/1001-live-qa/
main     .gradle-user-t1008b/  ·  docs/dev-reports/2026-08-03-874-live-qa.md (t874 에 이미 커밋됨)
```

앞의 둘은 **디렉토리에 `-real-qa` 접미사가 없어** mock 스위트가 집어가면 `ECONNREFUSED` 로 CI 가 깨진다. 라이브QA 하네스를 다시 만들 때 **`-real-qa` 접미사 규약**을 지킬 것.

---

## 6. 환경 메모

- **standalone 라이브QA 는 다운스트림 URL 주입이 관건** — host 에서 Docker 내부 서비스에 직접 못 닿는다. **publish 된 포트**를 가리킬 것. 이걸 놓쳐 QA 2건이 `불러오지 못했습니다` 만 띄웠다.
- standalone 포트 사용 이력: `18386 18387 18390 18391 18392 18419 18420 18486`
- `VITE_APP_VERSION` (`YYYY/MM/DD-N`) 미주입 시 renderer 런타임 사망
- `design-system` junction 깨지면 `dist` 빌드 + `npm install`
- **Flyway 번호는 적용된 최고 버전보다 높게** — `slip_db` 는 `V100` 이 적용돼 있어 `V62` 가 배포본 기동을 막을 뻔했다(`#984` V27 과 같은 모양). `out-of-order` 는 기본값 `false`. 현재 `slip-service` 는 `V102`, `auth-service` 는 `V91` 까지.
- `accounting-service` 전체 테스트는 **일곱 라운드 연속 300초대 timeout** — 권위는 CI
- 공유 `partner_auth_db` 에 `V3` 가 적용돼 있다(QA 하네스 부작용). **`V3` 파일 수정 시 checksum 으로 죽는다.**

---

## 7. 별건 보고 — 확인 필요

`#1061` 을 세다가 나온 것. **이 PR 의 결함은 아니다.**

```text
slip 없는 회계분개   30건 · 401 순매출 412,300,000원
```

`journals` 에 `POSTED` 인데 대응 slip 이 없다. ① slip 이 나중에 삭제됐고 분개가 남았다 ② 분개가 slip 없이 만들어졌다(수기 등) — 어느 쪽인지 미판정. **회계 원장에 닿는 데이터라 별도 확인이 필요해 보인다.**

---
