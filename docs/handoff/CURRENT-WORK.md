# 현재 작업 — 2026-08-14 **집PC** 세션 종료 (회사PC 에서 이어받음)

> 이 파일만 읽으면 이어받을 수 있습니다.

---

## 0. 🚨 이어받는 사람이 가장 먼저 할 것

```text
1  git pull && .\scripts\sync-claude-memory.ps1
2  docker ps -a --filter "name=samhan-" --format "{{.Names}}\t{{.Status}}"
   🚨 있는 것만 읽지 말고 **없는 것을 세십시오**
3  수치는 그 PC 에서 다시 세십시오 (양 PC 시드 상이 — 이번 세션에 크게 데였습니다)
4  열린 PR 7건의 CI 를 exact SHA 로 다시 확인하십시오
```

### 🔴🔴 이번 세션 최대 발견 — `docker compose --build` 는 Gradle 을 안 돌립니다

**하루에 세 번 헤맸고 전부 "코드 결함" 처럼 보였습니다.**

```
Dockerfile 이 services/<svc>/build/libs/<svc>.jar 를 **복사만** 합니다.
컨테이너를 재생성해도 jar 가 낡았으면 코드가 그대로입니다.

실측   groupware  image created 2026-08-13T13:10Z  /  jar 2026-07-23  (3주 전)
```

⟹ **반드시 `.\scripts\redeploy-service.ps1 <svc>` 를 쓰십시오**(Gradle → compose 순서 고정).
🔑 판별: `docker inspect` 의 `.Created` 를 믿지 말고 **jar 나이**를 재십시오.
→ [`.claude/memory/feedback_docker_build_does_not_run_gradle.md`](../../.claude/memory/feedback_docker_build_does_not_run_gradle.md)

### 🚨 라이브QA 브리핑은 파일을 **통째로 붙여넣으십시오**

```
.claude/briefing/liveqa-preamble.md
```

같은 항목을 메모리에 세 번 박제했는데 세 번 다 재발했습니다. 이번 세션 실측 — **같은 날 6라운드가 브리핑 유무로 갈렸습니다.**

```
완주  #1189(15장) · #1197 · #1181(16장) · #1199(27장)   ← 블록 있었음
포기  #1198 · #1200 · #1202                             ← 블록 없었음
```

🔑 `"No browser is available"` 은 **인앱 Browser 런타임**의 출력입니다. 로컬 Playwright(`chromium-1217`)는 별개로 정상 동작합니다.

### 🚨 PM 은 브리핑에 **환경 값을 단정하지 마십시오**

*"스택은 08-12T17:53 빌드"* 처럼 수치를 적으면, 실측과 다를 때 검증자가 정당하게 멈춥니다. **하루에 세 번** 그렇게 멈췄습니다(`#1189` 스택 시각 · `#1161` 이슈 제목 수치 · `#1201` 이미지 시각).

⟹ **"무엇을 확인하라" 만 쓰고 "그 값이 얼마다" 는 쓰지 마십시오.**

### 🚩 개발자 모드가 켜져 있습니다 (집PC)

`electron-builder` 의 `winCodeSign` 이 심볼릭 링크를 만들어야 해서 개발책임자가 켰습니다.
⚠️ **PowerShell `New-Item -SymbolicLink` 는 개발자 모드를 켜도 여전히 실패합니다**(cmdlet 한계). `electron-builder` 가 쓰는 **Node `fs.symlink`** 로 판단하십시오.

---

## 1. 오늘 머지된 것 — **8건**

```
#1189  #1142 검수완료 전표 되돌림 S1 + 창고 UUID       → 이슈 #1142 종료
#1197  #1092 견적서 관리 2페이지 분리                   → 이슈 #1092 종료
#1181  #910+#935 클라이언트 자동 업데이트 9앱
#1199  시리얼 재고 입출고가 수불부에 안 남던 것
#1200  #1161 중앙 감사 파이프 S2a 계약 마감
#1202  #1140 구형 baseline + 단가변동 토글             → 이슈 #1140 종료
#1198  #1072 계정과목 이카운트 4자리 통일               → 이슈 #1072 종료
#1205  날짜 의존 테스트가 main 을 빨갛게 만들던 것
```

**이슈 종료 3건 · 열린 이슈 13 → 11.**

🔑 **머지 개수 ≠ 이슈 소진.** 개발책임자 지적으로 규칙이 바뀌었습니다 → §5.

---

## 2. 열린 PR 8건 — 상태와 다음 한 걸음

### 🟢 `#1207` 재배포 스크립트 — **가장 먼저 머지하십시오**

`scripts/redeploy-service.ps1` — Gradle → compose 순서를 고정합니다. §0 의 배포 함정을 막는 장치라 **다른 트랙보다 먼저 들어가는 게 낫습니다.**


### 🟡 `#1206` `#1161` S1.5 + S2b — **결함 fix 완료 · 재수렴 QA 필요**

```
S1.5  A/B/C 등급별 ES 인덱스 + ILM · Rabbit 큐 분리 · 재시도 3회 후 DLQ
      DLQ inspect/retry/discard 운영 API · 재처리 한도로 무한 순환 방지
S2b   user-service 역할변경 · dashboard-service 릴리스 게시 발행
      🔑 "붙였다" 가 아니라 audit.# 큐 수신을 실제로 확인
```

🔴 **라이브QA 가 스택을 깨뜨리는 결함을 찾았습니다.**

```
기존 samhan.audit.queue 에는 TTL 이 없는데 PR 이 TTL 86400000 으로 재선언
⟹ RabbitMQ 406 PRECONDITION_FAILED → logging-service 13회 재시작 후 unhealthy
```

🔑 **빈 브로커에서 테스트하면 안 나옵니다.** 구현 라운드 통합테스트가 깨끗한 RabbitMQ 에서 돌아 놓쳤습니다.

✅ **fix 완료** (`fc0dcabdb`) — 큐 인자 대신 **RabbitMQ policy** (`samhan-audit-retention`) 로 적용했습니다. policy 는 기존 큐에도 적용되고 재선언 충돌이 없습니다. **기존 메시지 삭제·purge·이관 0**. TTL 없는 큐를 먼저 만들어 두고 배포하는 조건을 Testcontainers 에 넣어 GREEN 확인했습니다(22 tests · 0 failed).

**다음 한 걸음** — **재수렴 QA**(이번엔 consumer 가 살아 있어야 질문 1~6 과 DLQ 한도 초과를 볼 수 있습니다) → 머지하면 **이슈 `#1161` 종료**.
⏳ 보존기간(`A/B 1년 · C 30일`)은 **설정 기본값**이며 법적·업무 정본 미확정.

### 🟡 `#1203` 재고이동 확정 + 재고수불부 — **구현 완료 · 재수렴 QA 필요**

```
✅ 재고이동 confirm 이 실제 재고·수불을 바꾸게 (-1/+1 · 총량 불변 · 양쪽 수불부 표시)
✅ 확정 직후 캐시 무효화를 **계열로** — 이동·판매ship/confirm·입고confirm·실사complete
✅ 수불부 모달 — 엑셀본 9열 + 전표번호 열 · xxl(1320px) · 전표 클릭 이동
✅ 결정 5·6·7 — 전일재고 유지 + 합계 행 신설(시각 구분) · 적요 한 열 · 기간 3개월
   🔑 합계에서 전일재고를 뺐습니다 — 이월이지 기간 내 거래가 아닙니다
   실측 1600px·1440px 둘 다 dialog 1320 / table 1280 / overflow=false
```

⟹ 정본 결정 1~7 이 **전부 반영**됐고 미결정 없습니다.

정본: [`docs/decisions/2026-08-14-stock-ledger-modal-spec.md`](../decisions/2026-08-14-stock-ledger-modal-spec.md) (결정 1~7 확정, 미결정 없음)
참조자료: `.claude/uploads/재고수불부_AP145BNPPHH1.xlsx` (개발책임자 제공, 직접 파싱함)

**다음 한 걸음** — 반영 확인 → 재수렴 QA → 머지. `#999` 축 ②가 닫힙니다(축 ③ QR 스캔은 미착수).

### 🟡 `#1201` `#894` 사내 메신저 — **7건 fix 완료 · 재수렴 QA 필요**

```
1단계  본체 S1 채팅을 독립 앱으로 이식
2단계  개별 화면 — 직원 directory · 직급 순서표 · employeeCode 기반 1:1 대화
3단계  상태 4종 · [개별]/[그룹별] · 그룹방 목록 · 검색/생성 모달
fix    라이브QA 도달 결함 7건 — 🚨 본체 채팅 회귀 포함
```

화면 정본: [`docs/decisions/2026-08-13-messenger-ui-spec.md`](../decisions/2026-08-13-messenger-ui-spec.md)

**다음 한 걸음** — **브랜치 빌드를 배포한 뒤** 7건 재수렴 QA.
⏳ 미결정 3가지 — 동일 직급 내 정렬 · 개별 목록 안읽음 표시 · 자리비움 자동 전환 기준 시간.
⏳ S3~S7 (자동로그인·부재중 외·알림·파일전송·이모티콘) 남음 → **같은 트랙에서** 이어가십시오.

### 🟡 `#1204` `#910` 릴리스 feed + 자체서명 — **적대검증 필요**

```
✅ 라운드 5 에서 fresh 셸 E2E 완전 재현
   SignerRootMatch=True · 9101→9102 · quitAndInstall · 재기동 · DisplayVersion 확인
   scripts/run-internal-chat-release-feed.ps1
```

🚩 **라운드 2·3 이 두 번 재현에 실패**했습니다(대화형 세션 변수 의존). 판정 기준을 *"fresh 셸에서 그 스크립트만 돌리면 되는가"* 로 좁혀 해결했습니다.

🔴 **운영 선행조건** — 인증서를 사내 PC **신뢰 루트에 배포**해야 합니다. 미배포 시 `UnknownError → ERR_UPDATER_INVALID_SIGNATURE` 로 자동 설치가 전부 차단됩니다.

**다음 한 걸음** — 적대검증 → 머지. ⏳ `#910` 채널 분리(stable/beta)는 미결정.

### 🔴 `#1162` IT 임시 자격 — **rotation 이 선행**

```
GitGuardian 실패 원인 규명 완료 — env key 이름 오탐 (근거 확인됨)
fix 완료 — key 목록에서 쉼표 제거 (8줄) · allowlist 확대 없음
```

🔴 **그러나 머지 전에 rotation 이 필요합니다** (개발책임자 결정).

```
PR 이 삭제한 13자 개발스택 비밀번호 · 28자 내부 토큰이 실행 컨테이너와 exact match
ensure-local-env.ps1:121~124 가 기존 값을 재사용 ⟹ 머지는 폐기가 아니라 승계
한 값이 PostgreSQL · RabbitMQ · MinIO · Grafana 넷에 공유
9자 시드 비번이 auth_db 활성 계정 5개 hash 와 일치 (+ QA기본·QA MASTER·아로로지스 관리자)
🚨 V48/V49/V9 시드 hash 가 fresh DB 에서 폐기 비번을 되살림
   ⟹ forward migration 차단이 머지 전 필수 게이트
```

런북: [`docs/dev-reports/2026-08-13-credential-rotation-runbook.md`](../dev-reports/2026-08-13-credential-rotation-runbook.md)
**다음 한 걸음** — **유지보수 창을 잡아** 런북대로 실행(공유 스택 전체가 잠시 내려갑니다) → 그 후 머지.

### 🟡 `#1180` `#901` 클로드 대화 — **S1 완료 · 다음 슬라이스**

```
✅ system.claude 를 기존 개인별·그룹별 7비트 권한 체계에 추가
   진입 endpoint — 권한 없음 403 · 권한 있음 501
✅ 라이브QA 전수 통과 — 4조합 4/4 · OFF 직후 100회 모두 403 · 역할 11종 11/11
```

🔴🔴 **이 트랙의 게이트는 "기능이 되는 것" 이 아니라 "권한 우회 경로 0"** 입니다. 축이 셋입니다.

```
축 0  클로드를 쓸 수 있는가      마스터 토글 · 개인별/그룹별   ← S1 완료
축 1  그 계정의 업무 권한         클로드는 절대 넘지 않는다
축 2  되돌릴 수 있는 작업인가      되돌릴 수 없으면 추가 확인
⟹ 할 수 있는 것 = 축 0 켜짐 · (축 1 ∩ 축 2)
```

**다음 한 걸음** — 대화 UI · 모델 호출 · 도구 호출 · 감사 로그. **같은 트랙에서** 이어가십시오.
결정: [`docs/decisions/2026-08-13-blocked-tracks-unblocked.md`](../decisions/2026-08-13-blocked-tracks-unblocked.md)

### ⏸️ `#1188` `#922`+`#1098` 바로빌·알리고 — **자격 확보 대기**

개발책임자: *"자격 확보 중 — 계속 대기."* **이 트랙에 시간을 쓰지 마십시오.**

---

## 3. 🔴 개발책임자 결정 — 이번 세션 확정분

| 건 | 결정 | 문서 |
|---|---|---|
| **계정과목 `255`** | `2559` 로 이관 (집PC 7행 발견) | `2026-08-13-account-code-unification.md` |
| **UUID** | 무엇이든 비공개 — 화면·URL·요청·응답 전부 | `2026-08-13-uuid-never-exposed.md` |
| **자격 rotation** | rotation 먼저, `#1162` 머지는 그 후 | `2026-08-13-credential-rotation-and-1181-gate.md` |
| **`#1181` 게이트 ③** | 관측 상한까지 인정하고 머지 | 〃 |
| **`#910` 배포** | 사내 서버 + 자체서명 (SmartScreen 경고 감수) | `2026-08-13-blocked-tracks-unblocked.md` |
| **`#901` 범위** | 되돌릴 수 없는 작업까지 · **별도 확인 절차** | 〃 |
| **`#901` 외부 전송** | 질문 + 호출한 API 응답만 (DB 덤프 금지) | 〃 |
| **`#901` 축 0** | 클로드 사용 자체가 권한 — 마스터 토글 개인별/그룹별 | 〃 |
| **메신저 화면** | 개별/그룹별 2페이지 · 상태 4종 · 직급 순서표 신설 등 | `2026-08-13-messenger-ui-spec.md` |
| **재고수불부** | 엑셀본 9열 + 전표번호 · 전일재고+합계 행 · 기간 3개월 | `2026-08-14-stock-ledger-modal-spec.md` |
| **시리얼 수불** | 수불부에 남아야 한다 — 결함 | (`#1199` 머지) |
| **트랙 운영** | 슬라이스마다 PR 열지 말 것 — 한 트랙에서 단계별 | `feedback_one_track_many_slices.md` |

### ⏳ 남은 결정

```
감사 로그 보존기간의 법적·업무 정본        (#1206 — 잠정 A/B 1년 · C 30일)
#910 채널 분리 (stable/beta)
#894 미결정 3가지 — 동일 직급 정렬 · 개별 목록 안읽음 표시 · 자리비움 기준 시간
#1198 결정 7 (leaf 전용 기표) 판단 질문 7개
#1142 되돌림 권한·범위·연결 처리·이력 (S1 에서 더 못 나감)
```

---

## 4. 🚩 이번 세션에서 PM 이 반복한 실수

**검증자가 여덟 번 멈췄고 전부 옳았습니다.**

```
1  브리핑 전제를 실측 없이 적었다 — 7회
     정책 문서 경로(main 인데 브랜치로 지목) · 정찰 보고서 미커밋 ·
     스택 빌드 시각 단정 · 이슈 제목 수치 · 이미지 시각 단정 ·
     "백엔드 계약 불변" 오적용 · "클로드 전용 엔드포인트 금지" 모순
   ⟹ **"머지해 두겠다" 고 말했으면 실제로 머지하고 나서 발주하십시오**

2  라이브QA preamble 을 브리핑에서 뺐다 — 3라운드 손실
   ⟹ .claude/briefing/liveqa-preamble.md 를 통째로 붙여넣으십시오

3  git add -A 가 무관한 삭제를 삼켰다
     tools/.s24-build-only/build/deep/tracked-writer.mjs 를 지웠다가 복원
   ⟹ 경로를 명시해서 add 하십시오

4  git checkout <브랜치> -- . 로 squash 를 만들다 main 을 되돌렸다
     그 명령은 브랜치가 **삭제한 파일을 지우지 않고**, 공통 파일은 브랜치 버전으로 덮습니다
   ⟹ squash 는 **git merge --squash** 를 쓰십시오

5  gh pr comment 에 heredoc(<<EOF)을 써서 백틱·괄호가 셸 치환됐다 — 2회
   ⟹ 본문을 **파일로 작성**하고 --body-file 로 올리십시오
```

---

## 5. 🔑 새로 생긴 규칙

### 슬라이스마다 트랙을 열지 않는다

> 개발책임자: *"슬라이스별로 트랙을 개설하지 말고 **한 트랙 안에서 관련 슬라이스들을 단계별로 처리**"*

실측 근거 — **머지 5건인데 이슈 종료 1건**이었습니다. `#1200` 은 `#1161` 의 S2a 만, `#1199` 는 `#999` 의 축 ① 만 닫았습니다.

```
✅ 한 이슈 = 한 트랙(브랜치·PR) = 여러 슬라이스를 단계별로 → 마지막에 한 번 머지
🚫 슬라이스마다 새 브랜치·새 PR
보고도 "머지 N건" 이 아니라 **"이슈 종료 N건"** 으로
```

### 라이브QA 브리핑 파일

`.claude/briefing/liveqa-preamble.md` — 읽고 요약하는 파일이 아니라 **통째로 붙여넣는** 파일입니다.

### 재배포 스크립트

`.\scripts\redeploy-service.ps1 <svc>[,<svc>]` — Gradle → compose 순서를 고정합니다.

---

## 6. 미해결 정리 항목

```
공유 DB QA 잔재
  판매전표 2026/08/13-1~5 · 입고전표 -1~3 · 이동전표 2026/08/13-1
  회계전표 2026/08/13-5591 · -6831 · QA 창고 4건 · 거래처 P-2026-0017
  채팅방 CHAT-20260813-000017 · 웹 종합견적서 PR1197-R3-BRANCH-1786618391920
  🚩 다음 라운드가 실 업무 데이터로 오인하지 않게 하십시오

잔여 QA DB      inventory_qa_1199_20260813 (그 외 3개는 이번 세션에 정리)
고아 워크트리    ✅ 8개 전부 정리 완료 (세션 종료 시)
                남은 7개는 전부 열린 PR 소속 —
                w1161b · w1162 · w894 · w901 · w910f · wdeploy · wtransfer

#999 축 ③       QR 스캔 입출고 — mutation 경로 자체가 없음
#1144           #1072 종료로 선행 해소 — 착수 가능
                🚩 정찰: 삭제 원천 아래 활성 allocation 1건 · UUID-only slips 9건·tax_invoices 13건
#894 S3~S7      자동로그인 · 부재중(일부 3단계에 포함) · 알림 · 파일전송 · 이모티콘
날짜 의존 테스트  #1205 가 하나 고쳤고, 같은 형태 후보 목록이 보고서에 있습니다
```

---

## 7. 🚩 이번 세션에서 확인한 환경 사실

```
집PC RAM        61.6GB — 회사PC(16GB)와 달라 .wslconfig 불필요
개발자 모드      켜짐 (electron-builder winCodeSign 용)
winCodeSign 캐시  실패 잔재 7,815파일 2.7GB 를 비웠습니다
계정과목 데이터   V101 seed 276행(4자리 275 + 00010)을 집PC 에 반영 — 77 → 353
                 🚩 journal_lines 는 집PC 309행 / 회사PC 7,275행 — 시드가 크게 다릅니다
prometheus·nginx  삭제된 워크트리 경로를 마운트하던 고장 컨테이너 — 제거했습니다
```

---

# 🔄 2026-08-14 오후 세션 진행분 (집PC · 원격) — 위 내용에 이어서

> 이 절이 **현행**입니다. 위쪽은 오전 세션 기록입니다.

## 열린 트랙 5건 — 전부 라운드 진행 중

| PR | 이슈 | 상태 | 다음 한 걸음 |
|---|---|---|---|
| **#1212** | #1209 | 도달 결함 **0** · CI fix 올림 | CI green 확인 → 머지 |
| **#1211** | #910 #935 | 도달 결함 **0** · mock 스펙 제거 | `825-s5` 실패가 선재인지 판정 → 머지 |
| **#1180** | #901 | fix 4라운드 · 재수렴 중 | 채팅방 목록이 빈 것부터 판정 |
| **#1210** | #999 | 마이그레이션 재적용 + 라이브QA | 명칭 변경 + 태그 재구성 발주 |
| **#1213** | #1144 | read model 구현 · 검증 중 | 실 데이터 판정 확인 |

## 🚨 오늘 실제로 터진 것 — 반복하지 마십시오

### ① 머지 안 된 마이그레이션을 공유 DB 에 적용했다

```
codex 는 git 금지라 origin/main 을 볼 수 없다
⟹ 워크트리 파일시스템만 보고 V121 을 골랐다 (main 에는 이미 V120 이 있었다)
⟹ 공유 slip_db 가 … 119 · [120 없음] · 121 이 됐다

두 곳이 동시에 깨진다
  다른 트랙이 main 기준 재배포 시 "적용된 V121 이 로컬에 없다" 로 기동 실패
  out-of-order 미설정(기본 false) → 머지해도 validate 실패
```

🔑 **진짜 원인은 번호가 아니라 순서였다.** `origin/main` 을 머지하니 V121 이 그대로 맞았다.
🚩 PM 의 첫 처방(*"V122 로 바꿔라"*)도 틀렸다 — 머지 전 상태를 보고 판단했다.

⟹ **codex 에게 마이그레이션을 맡길 때는 "붙이기 전에 PM 에게 세 곳 대조를 요청하라"** 로 브리핑한다.
⟹ 되돌릴 수 있었던 이유 = 브리핑에 *"되돌릴 수 있게 하라"* 를 넣어 audit 61건이 남았기 때문.

### ② 폐기된 설계로 도는 codex 스레드를 안 죽였다 (3건)

설계가 뒤집혔는데 이전 브리핑으로 돌던 스레드가 **같은 워크트리에서 폐기된 설계를 구현**하고 있었다.
⟹ 결정 문서를 쓰기 **전에** 먼저 `TaskStop`. → [`feedback_stop_superseded_codex_threads.md`](../../.claude/memory/feedback_stop_superseded_codex_threads.md)

### ③ 내가 커밋한 QA 하네스가 CI 를 깼다 (2건)

```
#1211  캡처용 Playwright 스펙이 mock 게이트를 깼다 (5201 포트 가정 + 범위 오류)
#1212  검증 스크립트의 8080·8097 리터럴이 Port Resolver Guard 에 걸렸다
```

## 📌 개발책임자 결정 — `#1210` (전부 확정, 구현 대기)

```
명칭     구매전표 → 입고전표 · 판매전표 → 출고전표
         🔒 page-code 는 유지 (권한 테이블 3곳의 키)
입고 7종  구매(기본)✅ 차용✅ 대여반납✅ · 반품 착하반품 회차 재입고
출고 11종 판매(기본) 대여 차용반납 불량반납 직배 착하선조치
          로젠택배 경동택배 경동화물 야적 지방
당일     → 판매 로 개명 (폐기 아님)
마감시각  지방 12:00 · 야적 14:00 · 경동 둘 15:00 만 남긴다
          🚨 DAY 00:01 · LOGEN 00:01 은 제거 — 00:01 은 마감이 아니라 "당일 등록 금지" 스위치였다
QR       발행 = 입고 자동일괄 / 출력 = 입고전표 화면 / 스캔 = 출고전표 내부
         🚫 독립 메뉴 없음
```

🚩 **커밋을 분리할 것** — 명칭 일괄 치환(131 파일)과 기능 변경이 한 diff 에 섞이면 결함이 치환 속에 묻힌다.

## ⏳ 미결 (개발책임자 판단 대기)

```
① 클로드 세션 목록 상단 헤더가 크고 짙은 단색 배너 — 카톡 기준에 못 미침
② 본체 데스크톱 채팅 화면이 원시 목록 (방 이름 없이 "채팅방" 반복) — 범위에 넣을지
③ 업데이트 배너 제목과 본문 첫 문장이 겹침 (두 줄인데 정보는 한 줄치)
④ 사내 메신저만 옛 배너 모양 — 세 앱을 맞출지
⑤ #1162 자격 로테이션 — 집PC 물리 착석 시 진행 (공유 스택 내려감)
```

## 🔒 여전히 유효한 규율

```
공유 스택   트랙이 소유한 서비스만 재배포
            🚫 postgres · eureka · rabbitmq · elasticsearch recreate 금지
캡처        제출 전 SHA-256 중복 0 을 PM 이 직접 재측정
관측 불가   못 한 것을 통과로 세지 않는다 (오늘 이것이 여러 번 결함을 살렸다)
```
