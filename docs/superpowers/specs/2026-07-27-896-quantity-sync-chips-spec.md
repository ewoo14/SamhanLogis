# #896 품목 수량 동기화 → 칩 기반 설정 전환 (레거시 GAS 웹 견적서·주문서)

> 개발책임자 지시(2026-07-22): 종합견적서·주문서 품목 메뉴의 하드코딩 수량 동기화 수식을,
> 해당 메뉴에서 칩으로 종속 품목을 지정하면 자동 동기화되도록 **데이터(설정)** 로 전환.
> 예: 홈멀티 실내기 1대 ↔ 판넬·리모컨·유연호스 수량 동기화.

## 범위 (PM 정찰 확정)
- **레거시 GAS 웹앱 전용**: `tools/legacy-gas/estimate-app/views/index.ejs`(`recomputeHomeDerived` :8289) + `tools/legacy-gas/order-app/index.html`(:5033+ 복제). 데스크톱/모바일 범위 밖.
- 🟡 **금액 민감** — 수량→금액. 이관 전후 견적/주문 금액 회귀 0 필수.

## 기획 진행 (Codex 전수 정찰 → 규칙모델 제안 → 개발책임자 설계 승인 → 구현)
착수 확정 사항: ①하드코딩 규칙 전수(어느 품목/화면/몇 건) ②규칙 모델(기준→종속[]+배수 1:1/1:N) ③세트구성품(#494/#495)·수식빌더(#499~506)와 경계 ④마이그레이션(하드코딩→설정, 회귀 0) ⑤칩 UI(#825 MultiSelectAutocomplete 재사용).

_본 spec 은 Codex 전수 정찰 결과로 갱신된다._
