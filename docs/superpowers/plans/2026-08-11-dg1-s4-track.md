# D-G1 S4a — 영업수수료 정산 API + 회계 탭 화면

> S1·S2 는 `#1165` 로 머지됨(`9b804493c`). S3(그룹웨어 연결)은 `#1168` 진행 중.
> 결정 정본: `docs/dev-reports/2026-08-11-gas-sweep-devlead-decisions.md` (D-G1 · D-G6)

## PM 정찰 결과

```text
✅ 있는 것   SalesCommissionSettlement · RateContract · Calculator ·
             NumberSequence · Repository 3종 · Status enum  (S1·S2)
🚫 없는 것   **REST 컨트롤러가 하나도 없다** — 화면이 붙을 API 자체가 없다
좌표         회계 사이드바  AppLayout.tsx:909  <SidebarCategory label="회계">
             경로 접두·권한  AppLayout.tsx:192  { prefix:'/accounting', pageCode:'accounting.reports' }
             권한 매트릭스   PermissionMatrixPage.tsx:108
```

## 범위 — S4a 만

```text
① 정산 REST API (목록·상세·생성·확정)
② 회계 탭에 메뉴 등록 + 라우트
③ 권한 — 전용 pageCode · 기본 회계담당자 이상 · 권한관리에서 개별 관리 (D-G6)
④ 목록·상세 화면 (문서번호 클릭 상세 — #1094 규약)

🚫 S4b 로 미룸 — **그룹웨어 연결 버튼** (S3 의 참조 첨부 API 가 머지돼야 함)
```

## 개발책임자 결정 (해당분)

```text
D-G1  정산 화면은 **회계 탭 신설 메뉴**
D-G6  권한 — "회계담당자 이상이 기본이며 해당 메뉴도 권한관리에서 관리"
      전용 pageCode 신설 · PermissionMatrix 에 노출 · 기본 부여는 회계담당자 이상
#1094 상세 진입은 전표번호·문서번호 하이퍼링크 · 상세에 뒤로 가기 (전 화면 공통)
```
