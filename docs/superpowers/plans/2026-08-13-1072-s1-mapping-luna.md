# 1072 S1 Account Mapping Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expose only evidence-backed Ecount mappings while preserving every existing journal row and amount, and render the ten explicitly undecided legacy codes as `미정`.

**Architecture:** Keep `journal_lines.account_code` immutable. Add a deterministic, code-only mapping policy to the accounting account read model; the policy contains only mappings explicitly established by the reconnaissance/decision record (`110→1089`, `401→4019`). The desktop account tree renders mapping status and never invents an Ecount code for undecided or evidence-missing accounts.

**Tech Stack:** Spring Boot/JUnit/AssertJ; React/TypeScript/Vitest; existing accounting account API and desktop account tree.

## Global Constraints

- The ten codes `103,104,105,201,919,142,210,220,255,900` must have no mapped Ecount value and must display `미정`.
- Existing journals and journal lines are read-only for this slice; no migration is required.
- RED output for invariants 2, 3, and 4 must be recorded before GREEN output.
- Do not claim a test passed unless the command output is fresh and complete.

### Task 1: Lock RED contracts

**Files:**
- Create: `services/accounting-service/src/test/java/com/samhanair/logis/accounting/service/AccountEcountMappingContractTest.java`
- Create: `clients/desktop/src/renderer/routes/AccountTreePage.mapping.test.ts`

- [ ] Write failing assertions for undecided status, journal-count/debit/credit snapshot preservation, and representative line-side/amount preservation.
- [ ] Run the focused backend and desktop tests and append the exact RED output to the implementation report.

### Task 2: Add the evidence-backed mapping read model

**Files:**
- Modify: `services/accounting-service/src/main/java/com/samhanair/logis/accounting/web/dto/AccountTreeNodeResponse.java`
- Create: `services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/AccountEcountMapping.java`
- Modify: `clients/web/design-system/src/components/AccountCodeSelect/AccountCodeSelect.tsx`
- Modify: `clients/desktop/src/renderer/routes/AccountTreePage.tsx`
- Modify: desktop mock/API account typing and fixtures as needed.

- [ ] Implement a closed mapping table with only `110→1089` and `401→4019`.
- [ ] Mark all ten undecided codes with `UNDETERMINED`, null Ecount code, and display label `미정`.
- [ ] Preserve existing account code/name/category fields and keep journal write forms using the legacy code.
- [ ] Run focused tests and append GREEN output.

### Task 3: Reconcile and document evidence

**Files:**
- Create: `docs/dev-reports/2026-08-13-1072-s1-mapping-luna.md`

- [ ] Record classification evidence, RED→GREEN output, unchanged journal totals, representative journal line comparisons, UI behavior, and blocked checks.
- [ ] Run accounting-service tests and `clients/desktop` typecheck; record exact results or timeout/incomplete status.
- [ ] Check deleted tracked files, especially `tools/.s24-build-only/build/deep/tracked-writer.mjs`, and record cleanup status.
