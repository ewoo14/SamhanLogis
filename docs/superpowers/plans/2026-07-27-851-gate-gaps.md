# #851 게이트 0 표면 보강 구현 계획

> **작업자 안내:** 이 계획은 현재 작업 트리에서 단계별로 실행한다. 사용자 지시에 따라 git 명령(add/commit/push/checkout/branch/stash)은 실행하지 않는다.

**목표:** real-QA 공유 하네스의 상대경로 네비게이션을 복구하고, arologis-only PR에서도 Notion zero 및 config audit 가드를 실행한다.

**구조:** 공유 Playwright 설정의 `use.baseURL`만 보강해 기존 스펙의 `/…`와 `/#/…` 이동 문자열을 유지한다. arologis 전용 워크플로에는 기존 가드 명령을 별도 read-only 잡으로 추가하고, 공용 CI와 중복되는 경우에도 서로 독립된 check로 동작하도록 잡 이름을 구분한다.

**기술 스택:** Playwright Test, GitHub Actions YAML, Bash 정적 가드, PowerShell 설정 감사 가드.

## 전역 제약

- `clients/desktop/playwright.real-qa.config.ts` 로 발견되는 `*-real-qa.spec.ts` 중 네비게이션 설정 때문에 실행되지 못하는 스펙은 0개여야 한다.
- 기존 real-QA 스펙의 BrowserRouter 경로와 HashRouter `#/` 경로를 수정하지 않는다.
- 로컬에서는 이 슬라이스와 직접 관련된 대상 스펙/가드만 실행하고 저장소 전체 테스트 스위트는 실행하지 않는다.
- Playwright는 `clients/desktop/node_modules/.bin/playwright`를 직접 사용한다.
- 두 가드의 실제 위반 주입 RED 출력 원문을 기록하고 주입한 변경은 원복한다.
- 모든 보고서와 주석은 한국어로 작성한다.

---

### 작업 1: 변경 전 기준 측정

**파일:**
- 읽기: `clients/desktop/playwright.real-qa.config.ts`
- 읽기: 대상 4개 `*-real-qa.spec.ts`
- 읽기: `.github/workflows/ci.yml`, `.github/workflows/arologis-ci.yml`
- 읽기: `scripts/check-notion-zero.sh`, `infrastructure/scripts/validate-config-audit.ps1`

- [ ] Playwright 목록 명령으로 대상 4개 파일의 스펙 수를 확인한다.
- [ ] 서버를 기동하지 않은 상태에서 대상 스펙 실행 결과를 기록해 현재의 `Cannot navigate to invalid URL`과 서버 연결 실패를 구분한다.
- [ ] 두 워크플로의 트리거 `paths`/`paths-ignore` 원문과 현재 가드 잡 부재를 기록한다.

### 작업 2: B-1 공유 real-QA 하네스 보강

**파일:**
- 수정: `clients/desktop/playwright.real-qa.config.ts`

- [ ] `use` 블록에 `baseURL: process.env['AUDIT_BASE_URL'] ?? 'http://127.0.0.1:5175'`를 추가한다.
- [ ] 대상 4개 파일의 `page.goto` 인자를 변경하지 않았는지 확인한다.
- [ ] 같은 Playwright 직접 실행 명령으로 목록 및 실행 결과를 재측정한다. 서버가 없으면 실행이 네비게이션 설정 오류가 아니라 연결 실패로 진행되는지 기록한다.

### 작업 3: B-2 arologis 전용 정적 가드 잡 추가

**파일:**
- 수정: `.github/workflows/arologis-ci.yml`

- [ ] `notion-zero-guard`와 `config-audit-guard`를 기존 `ci.yml` 명령과 동일한 방식으로 추가한다.
- [ ] 잡 이름은 `arologis` 식별자를 포함해 공용 CI의 동일 가드와 check 이름을 구분한다.
- [ ] 기존 arologis PR `paths` 필터 원문을 유지하고, arologis-only 경로가 필터에 포함되는 근거를 보고서에 적는다.
- [ ] `shared/**`처럼 두 워크플로가 함께 트리거되는 경우는 각 워크플로가 체크아웃한 동일 커밋을 독립적으로 검사하며 파일/아티팩트 공유나 상호 배제가 없음을 명시한다.

### 작업 4: 가드 RED 실증

**파일:**
- 임시 수정 후 원복: Notion runtime 검사 대상의 안전한 임시 소스 파일
- 임시 수정 후 원복: config audit가 읽는 compose/application 설정 파일

- [ ] Notion 패턴 하나를 실제 검사 대상에 임시 주입하고 `bash scripts/check-notion-zero.sh`의 실패 출력 원문과 종료 코드를 저장한다.
- [ ] config audit 대상 서비스의 포트를 실제 compose 포트와 다르게 임시 주입하고 `pwsh -NoProfile -File infrastructure/scripts/validate-config-audit.ps1 -Detailed`의 실패 출력 원문과 종료 코드를 저장한다.
- [ ] 두 임시 주입을 원복하고 각 가드를 다시 실행해 PASS 출력과 종료 코드 0을 확인한다.

### 작업 5: 직접 관련 검증 및 보고서

**파일:**
- 생성: `docs/dev-reports/2026-07-27-851-gate-gaps.md`

- [ ] B-1 전후 대상 스펙 수와 실제 실행 수를 숫자로 기록한다.
- [ ] B-2 트리거 조건 및 `paths`/`paths-ignore` 원문을 기록한다.
- [ ] 두 가드의 위반 주입 RED 원문과 원복 후 PASS 원문을 기록한다.
- [ ] 중복 실행 동작과 기존 스펙 이동 경로 무훼손 근거를 기록한다.
- [ ] 변경 파일 목록과 실행 명령/결과 원문을 마지막 응답에 제공한다.
