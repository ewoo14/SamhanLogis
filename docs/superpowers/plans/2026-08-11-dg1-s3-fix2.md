# D-G1 S3 fix2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Status:** 완료. 구현·RED 재현·Chromium live QA·desktop/backend 회귀 검증 및 개발 리포트 작성 완료.

**Goal:** DocumentReferencePicker의 검색 dropdown이 picker와 상위 overflow에 잘리지 않고 실제 viewport에서 보이도록 portal로 배치하며, 7개 문서 유형 모두에 hit-test 기반 가시성 회귀 gate를 둔다.

**Architecture:** picker 본체는 기존 inline overflow 보호를 유지한다. dropdown만 `document.body`에 portal로 렌더하고 picker의 viewport 좌표를 기준으로 fixed 배치한다. 아래 공간이 부족하면 위로 flip하고, resize/scroll capture 이벤트에서 좌표를 재계산해 입력창과 목록의 분리를 막는다. modal consumer가 현재 없으므로 z-index는 기존 app overlay 계층을 확인한 뒤 dropdown 전용 값으로 제한한다.

**Tech Stack:** React 18, TypeScript, CSS Modules, React Testing Library/Vitest, Playwright Chromium-1217, Vite mock renderer.

## Global Constraints

- 부모의 overflow를 통째로 visible로 변경하지 않는다.
- 7개 문서 유형을 공통 picker 경로로 검증한다.
- 기존 6종 라벨·상세 라벨·href를 exact 불변으로 유지한다.
- 기존 7유형/6 URL transport 계약을 변경하지 않는다.
- dropdown 결과 DOM 존재나 Playwright 자동 스크롤만으로 통과시키지 않는다.
- 공유 DB write, 배포, git 조작, S4 화면 추가, 기존 라벨 변경을 하지 않는다.

---

### Task 1: 현행 실패를 정본 RED gate로 고정

**Files:**
- Modify: `clients/desktop/playwright/2026-08-11-dg1-s3-fix/s3-fix-live.spec.ts`
- Create: `clients/desktop/playwright/2026-08-11-dg1-s3-fix/visibility.ts` (필요할 때만 helper를 분리)
- Test output: `docs/qa/2026-08-11-dg1-s3-fix2/00-red-settlement-search-result.png`

**Interfaces:**
- Produces: option의 `toBeVisible`, viewport bounds, 중앙점 `elementFromPoint`, option 자신/자손 hit-test를 모두 확인하는 Playwright assertion.

- [x] **Step 1: 기존 두 흐름에 DOM-only assertion 대신 visibility helper를 호출하도록 테스트를 먼저 작성한다.**

```ts
async function expectVisibleAndHitTestable(option: Locator): Promise<void> {
  await expect(option).toBeVisible()
  const evidence = await option.evaluate((node) => {
    const rect = node.getBoundingClientRect()
    const hit = document.elementFromPoint(rect.left + rect.width / 2, rect.top + rect.height / 2)
    return {
      inViewport: rect.top >= 0 && rect.left >= 0
        && rect.bottom <= window.innerHeight && rect.right <= window.innerWidth,
      optionContainsHit: !!hit && node.contains(hit),
    }
  })
  expect(evidence.inViewport).toBe(true)
  expect(evidence.optionContainsHit).toBe(true)
}
```

- [x] **Step 2: 수정 전 현행에서 helper가 F-1을 실패하는지 실행한다.**

Run from `clients/desktop`:

```powershell
$env:AUDIT_BASE_URL='http://127.0.0.1:5193'
node_modules\.bin\playwright.cmd test --config=playwright.config.ts playwright/2026-08-11-dg1-s3-fix/s3-fix-live.spec.ts --reporter=line
```

Expected: the new gate fails before any click because the option is below the picker clipping boundary and its center hit-test is not the option. Preserve the measured RED object and screenshot in the dev report.

### Task 2: portal positioning unit RED/GREEN contract

**Files:**
- Modify: `clients/desktop/src/renderer/components/groupware/DocumentReferencePicker.test.tsx`
- Modify: `clients/desktop/src/renderer/components/groupware/DocumentReferencePicker.tsx`
- Modify: `clients/desktop/src/renderer/components/groupware/DocumentReferencePicker.module.css`

**Interfaces:**
- Consumes: picker `ref` rect, `window.innerWidth/innerHeight`, open/options state.
- Produces: body-level listbox with fixed `left`, `width`, and either `top` or `bottom` placement; cleanup-safe `resize` and capture-phase `scroll` listeners.

- [x] **Step 1: Add tests that assert an open option is portaled outside the picker and that a narrow below-space flips above.**
- [x] **Step 2: Run the focused picker test and confirm the new geometry tests fail for the current inline dropdown.**
- [x] **Step 3: Add `pickerRef`, `dropdownPosition` state, and a layout-safe `updateDropdownPosition` callback.**

```ts
type DropdownPosition = {
  left: number
  width: number
  top?: number
  bottom?: number
  maxHeight: number
}
```

The callback must clamp horizontal placement to an 8px viewport inset, use the picker rect width without creating horizontal page overflow, use below placement when there is enough space, and use above placement when the available below space is smaller than the available above space. Its max-height must be bounded by the selected side's available viewport space.

- [x] **Step 4: Render only the `<ul>` through `createPortal(..., document.body)` with `position: fixed`, the calculated coordinates, and a z-index below global modal overlays but above page content.**
- [x] **Step 5: Register `resize` and capture-phase `scroll` listeners while the dropdown is open; recalculate coordinates on both and close if the anchor leaves the viewport. Remove listeners on close/unmount.**
- [x] **Step 6: Run the focused picker tests and confirm the geometry contract passes without changing selection, keyboard, blur, or stale-response behavior.**

### Task 3: seven-type Playwright visibility and interaction matrix

**Files:**
- Modify: `clients/desktop/playwright/2026-08-11-dg1-s3-fix/s3-fix-live.spec.ts`
- Modify: `clients/desktop/src/renderer/api/mock.test.ts` only if a missing existing fixture contract is discovered (no fixture broadening without evidence)

**Interfaces:**
- Consumes: shared visibility helper and existing mock search endpoints.
- Produces: one Chromium-1217 gate for each of `OUTBOUND_SLIP`, `INBOUND_SLIP`, `JOURNAL`, `TAX_INVOICE`, `STATEMENT`, `PARTNER_LEDGER`, `SALES_COMMISSION_SETTLEMENT`.

- [x] **Step 1: Add a table of the seven existing type values, labels, query strings, and expected visible business identifier.**
- [x] **Step 2: For each type, select the type, enter the fixture query, assert the option is visible and hit-testable before click, capture before click, and select it without `force` or manual scrolling.**
- [x] **Step 3: Add explicit zero-result coverage with a query that is absent from the existing mock fixture and assert no listbox is rendered.**
- [x] **Step 4: Add multiple-result/scroll-needed coverage using an existing broad query, assert the listbox itself scrolls while the first visible option remains hit-testable, and preserve keyboard ArrowDown/Enter selection.**
- [x] **Step 5: Add long-label coverage from existing fixture text and assert no document horizontal overflow or picker width collapse.**
- [x] **Step 6: Add bottom-of-viewport, narrow viewport, resize, and open-while-scroll scenarios. For scroll, verify the list remains aligned after scroll or closes when its anchor exits the viewport.**
- [x] **Step 7: Inspect all `DocumentReferencePicker` consumers; record that only groupware create/detail currently consume it and execute both routes. If no modal consumer exists, record the absence rather than inventing a production modal.**

### Task 4: regression and contract verification

**Files:**
- Modify: `docs/dev-reports/2026-08-11-dg1-s3-fix2.md`
- Reuse: existing label/href tests, picker tests, backend test suites, V19 and mutation IT commands from SOL review

- [x] **Step 1: Run the focused desktop picker and search contract tests.**
- [x] **Step 2: Run all desktop Vitest and `npm run typecheck`.**
- [x] **Step 3: Run the two existing Chromium live flows plus the seven-type visibility matrix with API base `http://127.0.0.1:1`, recording zero leaked requests.**
- [x] **Step 4: Run accounting and groupware suites and report fresh XML totals of 1,867 and 254 tests respectively.**
- [x] **Step 5: Re-run V19 existing-row survival and `refDocNo` POST→GET mutation IT without shared DB writes.**
- [x] **Step 6: Verify exact labels/hrefs for the existing six types and confirm no S4 route or label changes.**
- [x] **Step 7: Write the Korean dev report with RED coordinates/capture, portal rationale, scroll/modal/resize evidence, seven-type matrix, combination table, and every command's raw result.**

No commit or push is part of this plan.
