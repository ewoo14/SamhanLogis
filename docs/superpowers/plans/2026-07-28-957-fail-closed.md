# #957 QA 출력 경로 fail-closed 수정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 물리 경로 판정에 필요한 조회가 실패하면 QA 출력 경로 resolver와 Bash 호출부가 안전하게 중단하도록 10개 사본의 계약을 일치시킨다.

**Architecture:** 존재하지 않는 출력 하위 경로만 기존 물리 부모까지 올라가 허용하고, `lstat`/`realpath`/Win32 handle/DNS/매핑 조회의 그 밖의 실패는 예외 또는 명시적 실패 상태로 전달한다. 각 구현 언어의 기존 패키지 경계를 유지하며 공통 모듈로 대규모 이동하지 않는다.

**Tech Stack:** Node.js CommonJS/ESM, TypeScript transpile probe, Python, Bash/Git Bash, PowerShell, Node `node:test`.

## Global Constraints

- 조사 보고서의 6건을 RED-first로 재현하고 GREEN 원문을 기록한다.
- 다음 10개 resolver 사본을 모두 반영한다: `clients/desktop/playwright/support/qa-screenshot-dir.{mjs,ts}`, `clients/desktop/src/main/capture.ts`, `infrastructure/scripts/operational-validation.ps1`, `qa/playwright/utils/screenshot.ts`, `scripts/lib/qa_shots_dir.py`, `scripts/lib/qa-shots-dir.{cjs,mjs,ps1,sh}`.
- 정상 `<committedDir>/_local` 및 OS temp 출력은 계속 생성·쓰기 가능해야 한다.
- 조사 보고서가 제외한 경로 표기별 판정표, 다른 OS 컨테이너, `operational-validation.ps1` 항목 4 `Join-Path` 배열은 수정하지 않는다.
- git 쓰기 명령(커밋·push·checkout·reset·stash)은 실행하지 않는다.
- 저장소 안 임시 파일은 만들지 않으며, 프로브 산출물은 OS temp만 사용한다.

### Task 1: RED regression probes

**Files:**
- Modify: `clients/desktop/scripts/qa-output-path-guard.test.cjs`
- Test: OS temp probe scripts under `%TEMP%` only

- [ ] **Step 1: Add one failing regression test per investigation finding.**
- [ ] **Step 2: Run the six focused probes and capture their failing output before changing resolver production code.**

### Task 2: Fail-closed Node/TypeScript resolvers

**Files:**
- Modify: `scripts/lib/qa-shots-dir.cjs`
- Modify: `scripts/lib/qa-shots-dir.mjs`
- Modify: `clients/desktop/playwright/support/qa-screenshot-dir.mjs`
- Modify: `clients/desktop/playwright/support/qa-screenshot-dir.ts`
- Modify: `clients/desktop/src/main/capture.ts`
- Modify: `qa/playwright/utils/screenshot.ts`

- [ ] **Step 1: Replace `existsSync` ancestor discovery with `lstat` plus an explicit ENOENT/ENOTDIR-only missing-path branch.**
- [ ] **Step 2: Propagate physical-path and LAN-address lookup failures instead of converting them into “outside docs/qa”.**
- [ ] **Step 3: Run the focused Node/TypeScript RED tests and confirm GREEN.**

### Task 3: Fail-closed Python, Bash, and PowerShell resolvers

**Files:**
- Modify: `scripts/lib/qa_shots_dir.py`
- Modify: `scripts/lib/qa-shots-dir.sh`
- Modify: `scripts/lib/qa-shots-dir.ps1`
- Modify: `infrastructure/scripts/operational-validation.ps1`
- Modify: `docs/qa/dev-menu-dev2/backend-qa.sh`

- [ ] **Step 1: Make Python physical/DNS/commonpath failures raise and keep successful external/temp paths allowed.**
- [ ] **Step 2: Make Bash command, mapping, physical-path, mkdir, and containment failures distinguishable from an ordinary outside result.**
- [ ] **Step 3: Make the Bash consumer stop when resolver status is nonzero or output is empty.**
- [ ] **Step 4: Make PowerShell path/handle/link/DNS failures terminating and remove lexical fail-open fallback.**
- [ ] **Step 5: Run focused Python/Bash/PowerShell probes and confirm GREEN.**

### Task 4: Regression, overblocking, and documentation verification

**Files:**
- Modify: `clients/desktop/scripts/qa-output-path-guard.test.cjs`
- Create or modify: `docs/dev-reports/2026-07-28-957-qa-output-path-fail-closed.md`

- [ ] **Step 1: Run the complete QA output-path guard suite.**
- [ ] **Step 2: Run `harness-false-green-guard.test.ts` and record all 49 tests.**
- [ ] **Step 3: Run existing guard suites available in the workspace and record skips with reasons.**
- [ ] **Step 4: Verify `_local` creation/write and OS temp creation/write.**
- [ ] **Step 5: Record `git diff --numstat` per modified file and save the Korean report to the requested OS temp path.**
