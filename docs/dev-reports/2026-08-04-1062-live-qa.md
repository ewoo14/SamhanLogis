# PR #1063 / 이슈 #1062 실 라이브 QA 보고서

## 환경 확인

- 상태: 측정 시작 전
- 워크트리: `C:\dev\Samhan-Public\.claude\worktrees\t1062`
- HEAD: `0347d3fc0bb1ce0456a8e8eea2bddeb14ca629a4`
- 프런트 포트/config: `18502` / `clients/desktop/vite.renderer.dev.config.ts`, `VITE_API_BASE_URL=http://localhost:8080`, `VITE_MOCK_MODE=0`, `VITE_APP_VERSION=2026/08/04-1`
- API 경로/컨테이너: 게이트웨이 `:8080` 경유. 전표 `GET /slips?page=0&size=20&slipType=OUTBOUND&includeDeleted=true` → `samhan-slip-service`; 자동완성 `GET /api/products?q=...` → `samhan-product-service`
- Docker 생성 시각: `samhan-slip-service=2026-08-03T17:02:05.035143939Z`, `samhan-product-service=2026-08-03T11:27:49.275680787Z`, `samhan-api-gateway=2026-07-31T15:15:50.070347996Z`
- 실 백엔드 증거: 브라우저 실응답 `200 http://localhost:8080/slips?page=0&size=20&slipType=OUTBOUND&includeDeleted=true`; 응답에 실 전표 `2026/08/03-6`, 거래처 `(주)삼한공조시스템`, `memo=QA-874-R4-OUTBOUND`, 금액 `displayTotalAmount=1739100`가 포함됨. 로그인은 실 `dev_master` 계정으로 GUI 수행.
- 캡처 경로: `QA_SHOTS_DIR`로 지정한 `docs/qa/1062-line-input-real-qa/` (디렉터리명 `-real-qa` 접미사)

## 시나리오 1 — 수정 모드 빈행 및 빈행 증식

- 클릭 경로: 실 로그인 → 판매 → 판매전표 목록 → `2026/07/27 - 245`(작성중) → `수정`.
- 기대: 수정 모드 진입 즉시 맨 아래 빈행이 있고, 빈행에 값을 넣으면 그 아래 빈행이 증식.
- 실제: 기존 1행(`AR09TXEAAWKNEU-04`)만 표시되고 맨 아래 빈행이 없음. 입력 가능한 모델명 input은 `모델명 1` 하나뿐이며 빈행 input은 0개. 마지막 기존 행의 `1번 행 삭제`를 GUI 클릭했더니 행이 사라지고 빈행도 생성되지 않음. 취소 후 재진입해도 라인 0개 상태가 유지되어, 해당 삭제 조작은 실 API로 즉시 반영된 것으로 보임(실전표 라인 변경 부작용은 아래 미확인 사항에 기록).
- 캡처: `01-sales-list-real-qa.png`, `02-slip-detail-real-qa.png`, `03-edit-mode-before-input-real-qa.png`, `04-after-last-line-delete-real-qa.png`
- 판정: **FAIL** — ① 빈행 유지, ② 빈행 증식 모두 조작으로 확인 불가/미충족.

## 시나리오 2 — 빈행 저장 제외 및 재조회/DB 확인

- 클릭 경로: 실 작성중 전표 `2026/07/27 - 244` → `수정` → 품목명/모델명을 실제 GUI에서 빈 문자열로 지움 → `저장`.
- 기대: 미확정 빈행은 저장 시 자동 삭제되고, 저장 후 재조회에도 빈행이 없어야 함.
- 실제: 빈행을 새로 만들 수 없어 기존 1행을 GUI 조작으로 미확정 상태처럼 비운 뒤 저장을 시도함. 실 `PUT /slips/8b29c16b-db87-45de-8d10-5f4b74c69ead/sales`가 `400 INVALID_INPUT`으로 거부되었고 응답은 `lines[0].productName: 품목명은 필수입니다.`였음. 자동 삭제 후 저장 성공은 발생하지 않음. 원상 값으로 GUI 입력 후 저장하여 `PUT 200`으로 복구했고, 실 API 재조회 `GET /slips/{id}`에서 `lines=[{modelName: AR09TXEAAWKNEU-04, productName: 삼성 윈드프리 9평형, quantity: 2}]`를 확인함. DB 직접 조회는 지시상 수행하지 않음.
- 캡처: `05-second-slip-edit-real-qa.png`, `06-unconfirmed-line-before-save-real-qa.png`, `07-after-unconfirmed-save-real-qa.png`
- 판정: **FAIL** — 빈행 자체가 생성되지 않고, 미확정 라인 저장은 자동 삭제가 아니라 실 API 400 검증 오류.

## 시나리오 3 — 모델명 자동완성 1건/2건 이상 및 판독성

- 클릭 경로: 실 작성중 전표 `2026/07/27 - 244` → `수정` → `모델명 1` input에 `AR09TXEAAWKNEU-04` 입력/Tab → 다시 `품목 1` input에 `삼성` 입력.
- 기대: 모델명 후보 1건은 단일 후보 방식, 후보 2건 이상은 넓은 목록/모달로 표시되고 모델명이 잘리지 않아야 함.
- 실제: 두 입력 모두 GUI 타이핑은 가능했으나 후보 목록/모달이 나타나지 않았고 `role=listbox=0`, `role=dialog=0`, `role=menu=0`이었다. 자동완성 관련 실 API 응답도 발생하지 않았다. 모델명 폭/후보 판독성은 후보 UI가 없어 미측정.
- 캡처: `08-autocomplete-one-candidate-real-qa.png`, `09-autocomplete-multi-candidate-real-qa.png`
- 판정: **FAIL** — 자동완성 UI와 1건/2건 이상 분기 모두 관찰되지 않음.

## 이 라운드가 보지 않은 것

- 실제 DB SQL 직접 조회(`SELECT slip_type, status, COUNT(*) ...`)는 수행하지 않음. 지시상 DB 직접 INSERT/DELETE를 하지 않았고, 실 API 응답/재조회로만 확인함.
- 후보가 실제 1건/2건 이상인 모델명을 백엔드에서 별도로 탐색하여 자동완성 후보 수를 확정하는 테스트는 수행하지 못함. 해당 화면에는 후보 UI/관련 API 호출 자체가 없었음.
- `2026/07/27 - 245`의 마지막 라인 삭제를 GUI로 조작했고 라인이 즉시 사라졌으며, 이후 해당 전표의 원래 라인 복원 여부는 별도 재조회하지 못함. 이 부작용은 결함 판정과 함께 PM 확인이 필요함.
- 비수정 모드 전표 라인 편집, 확정/저장 전환 이후 상태, 모바일 화면, 인쇄/금액 회귀는 보지 않음.
