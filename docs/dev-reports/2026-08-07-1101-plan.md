# #1101 docs 평문 dev 비밀번호 132건 정리 — 기획

## 📌 개발책임자 결정 (2026-08-07)
> **git 에서 빼고 `.env` 계열로**

## PM 실측 (2026-08-07)
```bash
git grep -c "${QA_DEV_DEFAULT_PASSWORD}\|${QA_MASTER_PASSWORD}\|${QA_AROLOGIS_ADMIN_PASSWORD}" origin/main -- docs/
#  73 파일 · 132 건
```
상위: `docs/qa/local-test-seed-data/scenarios/01-master-login.md` 12 ·
`docs/dev-reports/2026-08-04-1013-live-qa-r25.md` 8 · `docs/handoff/CURRENT-WORK.md` 7 ·
`docs/dev-reports/sp-08-8-credential-plaintext-guard.md` 3 (가드 문서 자신)

저장소 자체 가드 `scripts/check-credential-plaintext.sh` 는 **통과**하는데 GitGuardian 은 잡는다.
오늘 `#1066` 이 여기서 CI red 가 나 재실행이 필요했다.

## 🚨 착수 전 반드시 푸는 것 — QA 에이전트 자격 주입 경로

git 에서 빼면 **codex QA 에이전트가 비밀번호를 못 읽는다.** 대안 없이 지우면 라이브QA 가 멈춘다.

```text
(가) PM 이 브리핑에 직접 주입   브리핑 원문이 세션 로그에 남는다 (저장소 밖으로만 이동)
(나) 에이전트가 .env 를 읽게    브리핑에 경로만 주면 되고 저장소·브리핑 양쪽에 평문이 없다  ← PM 권고
(다) dev 비밀번호를 무해한 값으로 통일   노출돼도 무의미. 시더·문서 전면 수정 + 양 PC 재세팅
```
⚠️ 이 선택은 보안 정책이라 **착수 전 개발책임자 확인**을 받는다.

## 불변식 (수단은 구현자가 정한다)
- RED-A `git grep` 으로 docs 의 평문 비밀번호가 **0건**
- RED-B 🚨 **계정명은 남는다** — QA 표본 지목에 필요하다. 지우면 QA 브리핑이 무용해진다
- RED-C 🚨 **라이브QA 가 계속 가능하다** — 위 (가)/(나)/(다) 중 택한 경로가 실제로 동작함을 보여라
- RED-D `scripts/check-credential-plaintext.sh` 가 재발을 **막도록 조여진다**

## 함께 할 것
QA·정찰 브리핑 템플릿에 "비밀번호를 산출물에 평문으로 남기지 마라(`<redacted>`)" 상시 문구.
(2026-08-07 `#1078` R13 브리핑부터 이미 적용 중)

## 범위 밖
- 운영 시크릿(AWS·DB·JWT·외부 API 키) — 이 목록에 없다
- git 히스토리 재작성
