# #1085 QA797 잔재 정리 라이브QA 2 — SOL

- 실행일: 2026-08-13 (Asia/Seoul)
- 질문: **잔재 정리 때문에 망가진 화면이 있는가?**
- 답: **없다. 도달 가능한 결함 0건.**
- 머지 가능 여부: **가능.** 지정된 세 실제 사용자 URL과 목록 상세를 정리 전·후 같은 renderer·gateway·MASTER 계정으로 비교했다.
- 공유 DB 쓰기: **0건.** 공유 DB에는 `pg_dump`와 `SELECT`만 실행했다. 정리/되돌림 SQL과 로그인은 `recon845-*` 격리 복제 스택에서만 실행했다.

## 1. 정리 전·후 대조표

| 라우트 | 정리 전 | 정리 후 | 판정 |
|---|---:|---:|---|
| `/#/sales/slips` | API/UI `147건`, 활성 판매전표 라인 합계 `116,336,094원`, 첫 페이지 `20행`, `1 / 8` | `147건`, `116,336,094원`, 첫 페이지 `20행`, `1 / 8` | **동일.** 목록 PNG SHA-256도 동일하다. 첫 행과 페이지 수가 같고 같은 행에서 상세 진입 성공. |
| `/#/sales/estimates` | API/UI `70건`, 합계 `656,909,053원`, 첫 페이지 `50행`, `1 / 2` | API/UI `45건`, 합계 `651,673,053원`, 한 페이지 `45행` | **의도한 QA797 25건·5,236,000원만 제거.** 남은 45건 전체 행 해시 동일. 첫 생존 행과 상세 내용 동일. |
| `/#/sales/estimates/new` | 거래처명·작성일·라인·합계·임시저장 UI 정상 표시 | 동일 UI 정상 표시 | **동일.** 정리 전·후 PNG가 바이트 단위로 동일하다. |
| 최근단가 | 활성 `78행` | 활성 `70행` | QA797 전용 8행만 제거. 새 견적 작성 화면 렌더에 단절 없음. |

상세 진입:

- 판매전표: 목록 첫 행에서 `/#/sales/{내부키}` 상세로 정리 전·후 모두 진입, 상세 본문 길이 `4,498자`로 동일했다. 내부키는 사용자 화면에 노출하지 않는다.
- 견적: 생존 표본 `2026/08/10-9`를 목록에서 눌러 `/#/sales/estimates/2026-08-10-9` 상세로 정리 전·후 모두 진입, 상세 본문 길이 `654자`로 동일했다.

정리된 데이터를 필요로 해 망가진 화면은 없었다. 견적 목록은 정리 대상 25건이 빠져 `70→45건`이 되는 것이 의도한 변화이고, 판매전표 목록·두 종류 상세·새 견적 작성 화면은 유지됐다.

## 2. 증거 무결성

### #1191 UTF-8 복제 스크립트

현재 워크트리에는 파일이 아직 없어서, `main`의 `scripts/qa/clone-db-utf8.sh` 원문을 GitHub에서 직접 읽어 Git for Windows Bash로 실행했다. 저장소 파일은 추가하거나 수정하지 않았다.

```text
[clone] isolated container: qa-clone-utf8-20260813020344-1640
[clone] PASS auth_db
[clone] PASS user_db
[clone] PASS slip_db
[clone] PASS product_db
[clone] PASS partner_db
[clone] PASS dc_config_db
[clone] PASS partner_order_db
[clone] PASS dashboard_db
[clone] PASS inventory_db
[clone] PASS all databases; isolated container and dump files will be removed
Exit code: 0
```

### 정리 전 기준치 재현

```text
 active_outbound_slips | active_outbound_total | active_estimates | active_estimate_total | active_price_memory
-----------------------+-----------------------+------------------+-----------------------+--------------------
                   147 |          116336094.00 |               70 |          656909053.00 |                  78
(1 row)
```

직전 정리 전 수치 **`70건 · 656,909,053원 · 최근단가 78행`을 전부 재현**했다.

### 정리 실행과 정리 후 수치

```text
BEGIN
 pg_advisory_xact_lock
-----------------------

(1 row)

UPDATE 10
UPDATE 53
UPDATE 25
UPDATE 8
COMMIT
exit_code=0
```

```text
 active_outbound_slips | active_outbound_total | active_estimates | active_estimate_total | active_price_memory | marked_slip_lines | marked_estimate_lines | marked_estimates | marked_price_memory
-----------------------+-----------------------+------------------+-----------------------+---------------------+-------------------+-----------------------+------------------+--------------------
                   147 |          116336094.00 |               45 |          651673053.00 |                  70 |                10 |                    53 |               25 |                   8
(1 row)
```

### 생존 데이터 동일성

정리 전 통제군은 격리 DB에서 표지 rollback으로 만들고, 같은 조건으로 정리 SQL을 재적용했다. 생존 견적 45건과 활성 판매전표 147건의 전체 JSON 행 해시는 세 시점 모두 동일했다.

```text
시점                     surviving_estimates  surviving_estimate_hash         active_slips  active_slip_hash
정리 후 1차              45                   e2a6cdad734e35ad03e6a00a7f0c8a60 147           1bff8c18b066e7d60d8d645de4e5f81f
rollback 정리 전          45                   e2a6cdad734e35ad03e6a00a7f0c8a60 147           1bff8c18b066e7d60d8d645de4e5f81f
정리 재적용 후            45                   e2a6cdad734e35ad03e6a00a7f0c8a60 147           1bff8c18b066e7d60d8d645de4e5f81f
```

### 원본의 기존 `?` 행

정정된 규칙에 따라 중단하지 않았다. 격리 복제본과 공유 원본의 같은 조회 결과가 정확히 일치했다.

```text
대상        estimate_no    partner_name  길이  물음표수
격리 복제본 2026/08/08-2   S26 ??? ???   11    6
공유 원본   2026/08/08-2   S26 ??? ???   11    6
```

이는 복제본에만 생긴 파손이 아니며, 지시대로 결함에 포함하지 않았다. #1191 스냅샷 검증도 9개 DB 전부 통과했다.

## 3. Playwright 실행 원문

인앱 Browser 가용 목록은 `[]`였다. 이를 실행 불가로 판정하지 않고 저장소 Playwright Chromium을 사용했다.

```text
정리 전
loginStatus=200 role=MASTER
/#/sales/slips       renderedRows=20 page=1/8 totalElements=147 detail=true
/#/sales/estimates   renderedRows=50 page=1/2 totalElements=70 questionMarks=6 sourceResidueMatched=true
estimate detail      estimateNo=2026/08/10-9 bodyChars=654
/#/sales/estimates/new partnerNameVisible=true dateVisible=true totalsVisible=true
Exit code: 0

정리 후
loginStatus=200 role=MASTER
/#/sales/slips       renderedRows=20 page=1/8 totalElements=147 detail=true
/#/sales/estimates   renderedRows=45 page=1/1 totalElements=45 questionMarks=6 sourceResidueMatched=true
estimate detail      estimateNo=2026/08/10-9 bodyChars=654
/#/sales/estimates/new partnerNameVisible=true dateVisible=true totalsVisible=true
Exit code: 0
```

견적은 45건이라 한 페이지만 남아 pagination 컨트롤이 조건부로 숨겨졌다. API `totalPages=1`, 화면 헤더 `전체 45건`, 테이블 45행이 일치한다.

## 4. 스크린샷

| 정리 전 | 정리 후 | 결과 |
|---|---|---|
| `before-sales-slips-list.png` | `after-sales-slips-list.png` | 판매전표 목록 쌍, SHA-256 동일 |
| `before-sales-slips-detail.png` | `after-sales-slips-detail.png` | 같은 판매전표 상세 진입 쌍 |
| `before-sales-estimates-list.png` | `after-sales-estimates-list.png` | 견적 `70→45건`, 대상 25건 제거 쌍 |
| `before-sales-estimates-detail.png` | `after-sales-estimates-detail.png` | 같은 생존 견적 상세 진입 쌍 |
| `before-sales-estimates-new.png` | `after-sales-estimates-new.png` | 새 견적 작성 화면 쌍, SHA-256 동일 |

경로: `docs/qa/2026-08-13-1085-liveqa2/`

동일 파일 해시:

```text
sales slips list before/after
63BE1BEC6206D0E50280131BFBD962422345635D7FEB073D78630225C4D9F126

sales estimates new before/after
2F4B69BE0256BF90EFFB70143DF154AB07E4F8AF292AA5F6C07D93A4A0147672
```

## 5. 도달 가능한 결함과 못 한 것

- 잔재 정리 때문에 도달 가능한 결함: **0건**.
- 지정된 세 라우트, 판매전표·견적 상세 진입, 정리 데이터 의존 여부, 직전 정리 전 수치 재현을 모두 수행했다.
- 견적 저장·전송처럼 데이터를 새로 쓰는 조작은 화면 정상 렌더 확인 범위가 아니어서 실행하지 않았다.

## 6. 라운드 종료

격리 DB를 사용 전 정리 전 상태로 rollback했고, 이번 라운드가 만든 Playwright/Vite 임시 산출과 프로세스를 정리했다. 기존 `recon845-*` 스택은 이 라운드가 만든 것이 아니므로 제거하지 않았으며 DB 내용만 원상복구했다.

```text
RESTORE_ISOLATED_DB
UPDATE 10
UPDATE 53
UPDATE 25
UPDATE 8
COMMIT
exit_code=0

RESTORED_NUMBERS
70|656909053.00
78

stopped_vite_pid=52752
vite_listener=False
temp_exists=False
clone_containers=0
temp_harness_dir_exists=False
qa_temp_dirs=0
png_count=10
```

삭제된 추적 파일 확인 한 줄: **금지된 git 명령 없이 현재 HEAD `d48dfc1077b5ff4db749836b3ce8ba2170b36c1c`의 GitHub tree 22,812개를 로컬과 대조해 누락 0건(`truncated=False`), `tools/.s24-build-only/build/deep/tracked-writer.mjs` `Test-Path=True`.**
