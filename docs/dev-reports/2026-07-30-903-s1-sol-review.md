# PR #998 / #903 슬라이스 1 — SOL 적대검증 1차

- 검증일: 2026-07-30
- 대상 브랜치: `feat/903-template-authoring-mode`
- 대상 HEAD: `28a6ac83a3117260b76c1fe0099606966b8595be`
- 기준 브랜치: `origin/main` (`1dc22645c30dc57577254169eaac94da4c0ada01`)
- 검증 질문: **이 PR이 바꾼 표면 전체에서, 실 사용자 경로로 재현 가능한 결함이 있는가?**
- 조사 구성: 통합·독립 재현 1명, 도달성/normalize 1명, 저장 타입/ACTIVE gate 1명, 정찰 증거 1명, 원격 PR 충돌 1명

## 최종 판정

**PASS — 실 사용자 경로로 재현 가능한 결함 0건, 증거 무결성 불일치 0건.**

새 계약 모듈은 테스트 외 production import가 0건이다. UI, API, JSONB 저장, 기존 parser,
renderer와 연결되지 않았으므로 기존 출력 또는 사용 동작을 바꿀 도달 경로가 없다. 지정된
정찰 사실과 라이선스 원문도 모두 재현됐다. 이 라운드가 첫 적대검증이며, 제시된 게이트에
따르면 이 결과는 머지 대상 판정이다.

## 대상 표면 확인

`git diff --name-status origin/main...HEAD`의 실제 결과는 기존 파일 수정 없이 신규 5개다.

```text
A clients/desktop/src/renderer/print/templateAuthoringMode.test.ts
A clients/desktop/src/renderer/print/templateAuthoringMode.ts
A docs/dev-reports/2026-07-30-903-s1-authoring-mode-contract.md
A docs/dev-reports/2026-07-30-903-template-authoring-mode-plan.md
A docs/dev-reports/2026-07-30-903-template-authoring-recon.md
```

따라서 사용자 브리프에 열거된 4개 파일뿐 아니라 조기 기획서
`2026-07-30-903-template-authoring-mode-plan.md`도 읽었으며, 이 파일 역시 실행 표면은
바꾸지 않는 문서다.

## 각도 1 — 기존 출력물 무변경

**판정: PASS.**

다음 검색에서 계약 파일·테스트·문서를 제외한 production 소비처가 0건이었다.

```text
rg ... "templateAuthoringMode|TemplateAuthoringMode|TEMPLATE_AUTHORING_MODES|
        DEFAULT_TEMPLATE_AUTHORING_MODE|normalizeTemplateAuthoringMode" .
→ NO_NON_DOC_CONSUMERS
```

`clients/desktop/src`의 `import.meta.glob`, `require.context`, `globSync`, `fast-glob` 기반
동적 loader도 0건이다. 현재 실제 결재 문서 출력은
`ApprovalDocView.tsx:302-306`에서 기존 layout을 `DocumentRenderer`에 넘기고,
`DocumentRenderer.tsx:602-720`이 band/element를 compile하는 경로다. 이 두 경로는 새
계약을 읽지 않는다.

실 사용자 경로 관점에서 신규 모듈에 도달할 수 없으므로 기존 출력이 달라지는 재현 경로는
없다. 문서에 제시된 좁은 회귀 실행도 그대로 재현됐다.

```text
npx vitest run src/renderer/print/templateAuthoringMode.test.ts
→ Test Files 1 passed, Tests 9 passed

npx vitest run src/renderer/print/document-template-fixtures.test.ts \
  src/renderer/print/ApprovalDocView.test.tsx
→ Test Files 2 passed, Tests 37 passed
```

근거: `templateAuthoringMode.ts:1-18`,
`2026-07-30-903-s1-authoring-mode-contract.md:17-29`.

## 각도 2 — normalize가 잘못된 renderer를 고르는가

**판정: PASS.**

`templateAuthoringMode.ts:12-17`의 실제 규칙은 다음과 같다.

| 입력 | 결과 |
|---|---|
| `WORD` | `WORD` |
| `EXCEL` | `EXCEL` |
| `undefined`, `null`, `""` | `WORD` |
| `word`, `excel` 등 대소문자 차이 | `WORD` |
| `PDF` 등 알 수 없는 문자열 | `WORD` |
| 숫자·객체 | `WORD` |

legacy 양식은 mode 필드가 없고 현재 band/element renderer를 계속 사용하므로 기본값
`WORD`가 맞다. 또한 현재 함수 호출자가 0건이어서 이 반환값으로 renderer를 잘못 고르는
실 사용자 경로 자체가 없다.

근거: `templateAuthoringMode.ts:12-17`,
`templateAuthoringMode.test.ts:13-22`,
`approvalDefaultTemplate.ts:32-65,73-92`,
`DocumentRenderer.tsx:602-720`.

## 각도 3 — 실제 저장 타입과 계약의 접점

**판정: 현재 PR의 도달 결함 없음.**

실제 저장 표면은 아직 mode를 갖지 않는다.

- FE `DocumentPayload`는 `paper`, `bands`만 갖는다:
  `clients/desktop/src/renderer/print/templateSchema.ts:167-170`.
- FE `TemplateEnvelope`에도 mode가 없다:
  `templateSchema.ts:172-180`.
- API 입력은 `document: DocumentPayload`다:
  `clients/desktop/src/renderer/api/documentTemplate.ts:28-33`.
- FE parser는 검증 후 새 객체를 만들며 알려진 필드만 남긴다:
  `templateSchema.ts:659-669`.
- BE 요청의 `document` 원문 타입은 `JsonNode`지만 service가 typed `DocumentPayload`로
  바꾼다:
  `DocumentTemplateCreateRequest.java:10-16`,
  `DocumentTemplateService.java:69-75`.
- BE typed payload도 `paper`, `bands`만 갖는다:
  `services/groupware-service/src/main/java/com/samhanair/logis/groupware/domain/DocumentPayload.java:7-10`.
- 현재 양식과 revision JSONB는 모두 그 typed payload를 저장한다:
  `DocumentTemplate.java:71-73`,
  `DocumentTemplateRevision.java:46-48`.

따라서 다음 슬라이스가 JSON에 문자열 하나만 추가하면 현 parser/typed record가 그 값을
보존하지 않는다. 다음 슬라이스는 FE parser, API input, BE payload, revision 저장을 함께
확장해야 한다. 그러나 이번 HEAD에는 값을 생성·전송·저장·조회하는 소비자가 0건이므로
현재 사용자가 mode를 잃는 재현 경로는 없으며 결함으로 분류하지 않는다.

## 각도 4 — 정찰 보고서 증거 무결성

**판정: 지정 항목 전부 재현. 불일치 0건.**

| 정찰 주장 | 재현 결과 | 근거 |
|---|---|---|
| `OutboundView.tsx` tracked 0건 | exact path와 `*OutboundView.tsx` 모두 0건 | 정찰 `:74,294-302`; `git ls-files` |
| `/sales/:id/print/dispatch` → `DispatchView` | import와 route 모두 재현 | `routes/index.tsx:83,556` |
| `poi-ooxml:5.4.0` 4곳 | 정확히 4파일 | 아래 원문 |
| desktop의 `exceljs`/`xlsx`/`@fortune-sheet/react` | dependency entry 각각 0건 | `clients/desktop/package.json:31-77` |
| ExcelJS 라이선스 | MIT 재현 | 공식 저장소 `LICENSE` 첫 줄 `The MIT License (MIT)` |
| SheetJS CE 라이선스 | Apache-2.0 재현 | 공식 저장소 `LICENSE`의 `Apache License, Version 2.0` |
| FortuneSheet 라이선스 | MIT 재현 | 공식 저장소 `LICENSE` 첫 줄 `MIT License` |
| Apache POI 라이선스 | Apache-2.0 재현 | 공식 저장소 `legal/LICENSE`, 공식 legal page |

POI 의존성 검색 원문:

```text
shared/ecount-io/build.gradle:23:
  api 'org.apache.poi:poi-ooxml:5.4.0'
services/slip-service/build.gradle:46:
  implementation 'org.apache.poi:poi-ooxml:5.4.0'
services/inventory-service/build.gradle:50:
  implementation 'org.apache.poi:poi-ooxml:5.4.0'
services/arologis-service/build.gradle:68:
  implementation 'org.apache.poi:poi-ooxml:5.4.0'
```

npm metadata도 정찰의 버전·라이선스와 일치했다.

```text
exceljs 4.4.0                 license=MIT
xlsx 0.18.5                   license=Apache-2.0
@fortune-sheet/react 1.0.4    license=MIT
```

공식 원문:

- ExcelJS: <https://github.com/exceljs/exceljs/blob/master/LICENSE>
- SheetJS CE: <https://github.com/SheetJS/sheetjs/blob/master/LICENSE>
- FortuneSheet: <https://github.com/ruilisi/fortune-sheet/blob/main/LICENSE>
- Apache POI: <https://github.com/apache/poi/blob/trunk/legal/LICENSE>,
  <https://poi.apache.org/legal.html>

추가로 PR HEAD와 GitHub check를 다시 조회했다.

```text
HEAD=28a6ac83a3117260b76c1fe0099606966b8595be
CHECKS=42
SUCCESS=42
```

## 각도 5 — `DocumentTemplateService` ACTIVE gate

**판정: PASS.**

현행 정책은 재현된다.

1. 편집기에서 DETAIL/IMAGE를 추가하고 DRAFT로 저장할 수 있다:
   `ElementPalette.tsx:6-12,29-53`,
   `DocumentTemplateEditorPage.tsx:100-105,138-153`.
2. create/update는 구조 validator를 거쳐 저장한다:
   `DocumentTemplateService.java:69-75,93-102`.
3. activation에서만 DETAIL/IMAGE를 검사해 422로 차단한다:
   `DocumentTemplateService.java:112-120`,
   `DocumentPayloadValidator.java:115-120`.

새 계약은 FE 저장 입력, BE payload, activation service 어디에도 import되지 않았다.
따라서 `EXCEL` 또는 normalize된 `WORD`가 현 ACTIVE gate를 우회하거나 잘못 차단시키는
실 사용자 경로는 없다. 정찰의 “편집·저장은 되지만 activation은 막힌다”는 사실 주장도
현 코드와 일치한다.

## 각도 6 — 다른 열린 PR과 표면 충돌

**판정: PASS. 동일 파일 충돌 0건.**

`git ls-remote --heads origin`으로 원격 head를 고정하고, 열린 PR 6개의 head에서 PR #998의
정확한 5개 경로를 `git ls-tree -r --full-tree <head> -- <path>`로 조회했다.

| 열린 PR | #998의 5개 정확한 경로 존재 수 |
|---|---:|
| #998 | 5 |
| #996 | 0 |
| #994 | 0 |
| #993 | 0 |
| #991 | 0 |
| #984 | 0 |

PR #993(`feat/910-client-version-policy`, 최신 원격 head `fa9914540d`)의
`clients/desktop` 변경은
`electron-builder.yml`, release validator, `src/main/auto-update*`,
`renderer/components/common/AppVersionGate.test.tsx`, `renderer/version/*`,
`vite.renderer.dev.config.ts`다. PR #998의
`renderer/print/templateAuthoringMode.ts`와 `.test.ts`는 교집합이 없다.

최신 head로 다시 계산한 `git merge-tree`의 #998↔#993 conflict signal도 0이다. GitHub는
#998을 `MERGEABLE/CLEAN`, #993을 `MERGEABLE/UNSTABLE`로 판정한다. #993의 `UNSTABLE`은
merge conflict 판정이 아니며, 같은 `clients/desktop` 디렉터리를 만진다는 사실만으로는
실 사용자 충돌 경로가 되지 않는다.

## 결함

없음.

## 이 라운드가 보지 않은 것

- 후속 슬라이스가 mode를 UI/API/JSONB/revision/renderer에 실제 연결한 뒤의 동작.
- Excel editor, XLSX import/export, Excel renderer 자체의 사용자 경로와 출력 정합성.
- 테스트의 강도, mock의 충분성, 문서 표현의 품질, 가드의 촘촘함. 지시대로 검증·보고
  대상에서 제외했다.
- Docker·Gradle 및 실 서비스 기동. 지시대로 실행하지 않았다.
- 전체 desktop `npm run typecheck`의 로컬 끝까지 완료. TypeScript 단계와
  `real-qa-cleanup-scope` 2건 통과 뒤 `real-qa-scope`가 이 리뷰의 5분 실행 상한을 넘겨
  중단했다. 이 항목은 성공/실패로 판정하지 않았고, 동일 HEAD의 GitHub 42/42 성공 및
  요청 범위의 Vitest 9/9·37/37만 재현 근거로 사용했다.
