# PR #1145 R13 SOL 5.6 적대검증 — Flyway 채번 + MASTER 정본 머지 판정

검증 워크트리: `C:\dev\Samhan-Public\.claude\worktrees\t1144`  
검증 HEAD: `1fba06036eebda26401e78e5cc5ed3178c37ae38`  
검증일: 2026-08-09 (KST)  
금지 준수: commit/push 0회 · 다른 워크트리/main checkout 0회 · 공유 DB write 0회 · V99 변경 0회

## 최종 판정

```text
도달 가능한 결함: 2건
머지 불가
```

### 결함 1 — R13 MASTER 파생을 refresh 스크립트가 재생성하지 못한다

- 실 사용자 경로: 후속 권한 migration 작업자가 정식 갱신 명령
  `scripts/refresh-accounting-permission-db-snapshot.ps1`을 실행한다. 스크립트는 exit 0과
  `Wrote DB-derived projection`을 출력하지만, R13의 MASTER 런타임 전권 wrapper를 없애고
  템플릿 projection을 다시 정본으로 내보낸다. 그 산출물을 받아들이면 R13이 제거한 MASTER
  110 divergence가 되살아나거나 계약 검사가 RED가 된다.
- 재현 절차: HEAD에서 script·catalog·projection·auth migration만 임시 디렉터리에
  `git archive`로 추출 → 그 임시 복제에서 refresh 실행 → 체크인 파일과 SHA-256/diff 비교.
  워크트리 파일과 공유 DB는 건드리지 않았다.
- 측정값: 97 migration 적용 및 exit 0. 체크인 hash
  `A44F7A67DD11D7DBCD2D836D4E2AA3D1D18C36D9528909F92FB52933F00B729D` → refresh hash
  `1FDE26D605E919C886E05FC1C2F0E011A9367A51DB74BD5B074CA212DD61964F`, `HASH_EQUAL=False`.
  diff stat은 `1 insertion, 10 deletions`이다. 측정 시각은 `2026-08-09 14:05:47 +09:00`.
- 원문 근거:
  - `scripts/refresh-accounting-permission-db-snapshot.ps1:79-81`은 곧바로
    `export const PERMISSION_DB_BITS_BY_ROLE`와 각 role row를 쓴다.
  - 체크인 projection은
    `accounting-slip-permission-db-snapshot.ts:1,5,410-412`에서 page catalog를 import하고,
    template을 별도 보관한 뒤 MASTER를 모든 page × `1111111`로 override한다.
  - 즉 R13이 바꾼 산출물 형식과 R12 refresh 생성기가 재수렴하지 않았다.

### 결함 2 — V99가 새로 연 SALES view-only 화면에 쓰기 CTA가 그대로 노출된다

- 실 사용자 경로: PR 배포로 V99 적용 → SALES 로그인 → `/#/accounting/sales-slips` 또는
  `/#/accounting/purchase-slips` 진입 → 목록은 VIEW 권한으로 열린다 → 상단 `작성` 버튼 클릭 →
  `/new`의 CREATE PermissionGuard가 사용자를 설명 없이 홈으로 되돌린다. DRAFT 행이 있으면
  `전기` 버튼도 권한 확인 없이 보인다.
- 재현 절차: 일회용 PostgreSQL에서 `V96 → #1130 V98 → PR V99`를 적용하고 SALES 두 셀을
  조회 → 두 목록 컴포넌트와 `/new` route guard 대조.
- 측정값: `SALES|accounting.sales-slip.accounting|1000000`,
  `SALES|accounting.purchase-slip.accounting|1000000` (VIEW만 true). 측정 시각은
  `2026-08-09 14:12:15 +09:00`.
- 원문 근거:
  - V99 `24-31,46-54`는 기존 `.list` 비트를 `.accounting`으로 복제하며 SALES를 포함한다.
  - `SalesAccountingSlipPage.tsx:100,109,113`과
    `PurchaseAccountingSlipPage.tsx:100,109,113`은 `전기`/`작성`을 action 권한 없이 렌더한다.
  - `routes/index.tsx:1188,1204`는 `/new`에서만 CREATE를 검사한다.
- 영향: 이 화면은 V99 전 공유 DB에서는 SALES가 홈으로 redirect되어 닫혀 있었고, V99가
  `1000000`으로 넓힌 뒤 처음 도달한다. 따라서 단순 기존 결함이 아니라 이 PR 배포로 열리는 경로다.

## ① Flyway 채번 재검증

### V96 → V98 → V99 + validate

#1130 HEAD의 V98을 Git 객체에서 임시 추출하고, 현재 V1~V96·V99와 함께 일회용
`postgres:16-alpine`에 적용했다. 공유 DB에는 SELECT 외 작업을 하지 않았다.

```text
Successfully applied 98 migrations ... now at version v99
MIGRATE_EXIT=0
Successfully validated 98 migrations
VALIDATE_EXIT=0
96|seed slip closed date admin permission|t
98|grant manager inbound inspection update|t
99|align accounting slip permissions|t
QUERY_EXIT=0
```

측정 시각 `2026-08-09 13:55:23 +09:00`, 임시 container/network 잔존 0.

### 번호 충돌

- 원격 `main`: SHA `48c5c250d8c76cd52894dd11f95158c50c3c17ab`, auth migration 96개,
  최고 V96.
- 열린 PR: 13개.
- 열린 PR 중 auth migration 파일을 추가/변경하는 PR: 2개만 존재.
  - #1130: `V98__grant_manager_inbound_inspection_update.sql`
  - #1145: `V99__align_accounting_slip_permissions.sql`
- 열린 PR 및 최신 main에 다른 V99는 0개다.

### SQL 바이트

R13 직전 V97 blob, 현재 HEAD V99 blob, 워크트리 V99 `git hash-object`가 모두
`bab045b30d26ae77de7652c677dec61ebfe87232`다. rename은 100%, 삽입/삭제 0으로 SQL 바이트는
동일하다.

## ② MASTER 전권 파생 및 비-MASTER 보존

실 API `POST /auth/login` 후 `GET /auth/admin/permissions/my`를 mock 없이 호출했다.
비밀번호·token·UUID는 출력하지 않았다.

| 역할 | login | permissions | page 수 | 전 7-action 셀 | 회계 3 page |
|---|---:|---:|---:|---:|---|
| MASTER | 200 | 200 | 200 | 200 | 모두 7-action |
| MANAGER | 200 | 200 | 182 | 0 | 공유 DB V99 미적용으로 3셀 `[]` |
| ACCOUNTANT | 200 | 200 | 137 | 0 | 3셀 `VIEW/CREATE/UPDATE/DELETE` |
| SALES | 200 | 200 | 130 | 0 | 공유 DB V99 미적용으로 3셀 `[]` |

MASTER는 `pageCount=200`, `full7Count=200`, `invalidCount=0`이다.
`PermissionAdminController.java:260,322`의 `allPageActions()` 실제 경로와 일치한다.

R13의 비-MASTER 동결 파일은 총 239셀이고 MASTER 항목은 0이다. 최종 8계약 검사와
post-V99 일회용 DB 조회에서 ACCOUNTANT/MANAGER 두 회계 page는 `1111000`, SALES는
`1000000`으로 확인했다. MASTER runtime bypass 정리는 비-MASTER 239 목록을 바꾸지 않았다.

## ③ “MASTER가 못 한다” 단정 전수 조사

`services/auth-service/src/test`와 `clients/desktop/src`의 Git 추적
Java/TS/TSX/JS/JSX 764개 파일을 전수 스캔했다.

- MASTER와 `false/403/forbidden/denied/못 한다/불가/금지/차단`이 같은 줄인 원시 후보: 105줄.
- MASTER 사용자의 특정 page code/action을 false 또는 403으로 단정한 테스트·화면 코드: 0건.
- 105 후보의 실제 분류: 비-MASTER deny, `X-Is-System-Master=false` 입력, MASTER 전용 설명,
  MASTER 시스템그룹 자체의 불변성(배속/편집 금지), 누락 시 MASTER도 false가 된다는 회귀 설명.
  이들은 MASTER 사용자 runtime page/action 전권을 부정하지 않는다.

## ④ 동결 239 exact 및 증감 RED

파일 직접 파싱 결과:

```text
total=239, MASTER=0, non-MASTER=239, unique=239, duplicates=0
MANAGER=97 SALES=26 ACCOUNTANT=45 WAREHOUSE=21 INVENTORY=21
DISPATCH=10 DRIVER=3 STAFF=3 DEVELOPER=8 PARTNER=5
```

- 실제 divergence 1 증가: MASTER sales accounting view 제거 → `0111111` 대 `1111111`, RED.
- 실제 divergence 1 감소: MANAGER tax-invoice.list download를 DB와 일치 → frozen
  `1000011` 대 실제 `1000001`, RED.
- 동결 항목 삭제 → 실제 `MANAGER products.price-schedule=1010000` 대 DB `0000000`, RED.
- 실제 차이가 아닌 동일값 항목 추가 → 역할별 actual 97 대 frozen 98, RED.

따라서 239는 늘어도 줄어도 RED다.

## ⑤ R7~R11 회귀와 9종 뮤테이션

최종 원복 상태:

- `npx vitest run ...accounting-slip-permission-contract.test.ts --reporter=verbose`:
  1 file, 8/8 pass, exit 0.
- `gradlew ... --tests '*AccountingPermissionProjectionFreshnessIT' --tests
  '*DynamicPermissionServiceTest' --tests '*AccountPermissionServiceTest' --rerun-tasks`:
  exit 0.

9종 뮤테이션은 모두 의도대로 RED였다.

| # | 뮤테이션 | 직접 관측 |
|---:|---|---|
| 1 | PARTNER × `messenger.send` 과부여 | `1111000` 대 `0000000` |
| 2 | snapshot page catalog +1 | mock 122 대 snapshot 123 |
| 3 | mock role catalog에 AUDITOR +1 | mock 12 대 snapshot 11 |
| 4 | MASTER sales accounting view 제거 | `0111111` 대 `1111111` |
| 5 | 실제 divergence 1 감소 | frozen `1000011` 대 actual `1000001` |
| 6 | 동결 목록에서 실제 항목 삭제 | MANAGER price-schedule 불일치 포착 |
| 7 | 동일값을 divergence로 허위 추가 | 역할별 exact set 불일치 |
| 8 | 임시 V98에서 MANAGER sales list print 반전 | freshness가 list/accounting 두 셀 `1111001` 포착 |
| 9 | projection MASTER partners.delete 한 비트 반전 | `db=1111100 projection=1111101` |

모든 임시 mutation과 migration은 즉시 원복했고 최종 baseline을 다시 GREEN으로 확인했다.

## ⑥ R12 자격증명 및 refresh

현재 `origin/main...HEAD` diff는 R12 당시 41개가 아니라 44개 파일이다. 44개를 다시 스캔했다.

- `samhan_dev_pw`: 0건.
- `QA_DEV_DEFAULT_PASSWORD = '<literal>'`: 0건.
- 코드의 `password/PASSWORD = '<literal>'`: 0건.
- 문서의 검색 패턴 예시 ``password = '...'`` / ``PASSWORD = '...'`` 두 문자열만 검출됐으며
  자격증명이 아니다.
- refresh/Testcontainers는 실행별 CSPRNG/UUID 비밀번호를 사용한다.

자격증명 제거 자체는 유효하다. 그러나 refresh hash 불변 주장은 결함 1 때문에 R13 이후 더는
유효하지 않다.

## ⑦ 역할별 라이브 QA

실행 조건:

- `clients/desktop` 패키지 안에서 Playwright Chromium, `headless: true`.
- 임시 실행 스펙의 디렉터리 `1145-r13-real-qa`, 파일
  `accounting-slip-roles-real-qa.spec.ts`로 둘 다 `-real-qa` 준수.
- `VITE_MOCK_MODE=0`, 실 API `127.0.0.1:8080`, HashRouter
  `${BASE_URL}/#/accounting/...`.
- 캡처 경로는 `resolveQaShotsDir`를 통해
  `docs/qa/1145-r13-real-qa/_local`로 결정. 절대 screenshot path literal 없음.
- 각 캡처 전에 판매/매입 화면 고유 test id를 단정.
- renderer 종료 후 `PORT_5175_LEFT=0`.

| 역할 | 판매전표 | 매입전표 | API/관측 |
|---|---|---|---|
| MASTER | 전용 화면 visible, 캡처 | 전용 화면 visible, 캡처 | 작성 visible |
| MANAGER | 홈 redirect, RED | 홈 redirect, RED | 공유 DB V99 미적용 |
| ACCOUNTANT | 전용 화면 visible, 캡처 | 전용 화면 visible, 캡처 | 두 목록 API 200, 작성 visible |
| SALES | 홈 redirect, RED | 홈 redirect, RED | 공유 DB V99 미적용 |

공유 DB를 read-only SELECT한 결과는 `2026-08-09 14:09:55 +09:00` 기준
`V97=0, V98=1, V99=0`이다. 따라서 MANAGER/SALES live RED는 배포 전 환경 상태이며 그 자체를
머지 후 결함으로 세지 않았다. 대신 V99 적용 후 실제로 열리는 SALES 화면의 action 노출을
결함 2로 셌다.

로컬 캡처 4장:

- `docs/qa/1145-r13-real-qa/_local/master-sales-slips.png`
- `docs/qa/1145-r13-real-qa/_local/master-purchase-slips.png`
- `docs/qa/1145-r13-real-qa/_local/accountant-sales-slips.png`
- `docs/qa/1145-r13-real-qa/_local/accountant-purchase-slips.png`

이 `_local` 산출물은 커밋 증거를 덮지 않는 로컬 실행 산출물이다. 임시 untracked spec은 실행 후
삭제했다.

## 최종 신규 파일

- `docs/dev-reports/2026-08-09-1145-r13-sol-reconv.md`

