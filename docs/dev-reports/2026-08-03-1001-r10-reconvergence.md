# PR #1061 R10 재수렴 적대검증 보고서

- 대상: `feat/1001-ledger-spec-rest`, HEAD `f944aeb7a` (사용자 제공 기준)
- 작성일: 2026-08-03
- 유일한 질문: **실 사용자 경로로 재현 가능한 결함이 있는가.**
- 조사 범위: R9 fix가 건드린 표면 전체, 하위 각도 ①→④ 순서
- 규율: 소스 수정·`git` 명령·Docker 빌드/배포 없이 읽기·조사·테스트와 실 DB 읽기 조회만 수행

## 진행 기록

보고서 파일을 조사 시작 전에 생성했다. 이후 확인 결과는 확인 즉시 이 파일에 순차적으로 추가한다.

## ① 차단되면 안 되는 것이 차단되는가

### ①-0 근거 위치 1차 검색

실행 명령 원문:

```powershell
rg -n --hidden --glob '!docs/dev-reports/2026-08-03-1001-r10-reconvergence.md' "299,?498,?000|292,?000,?000|12,?276,?000|20,?000,?000|f944aeb7|R9" .
```

출력 원문:

```text
command timed out after 24021 milliseconds
```

판정: 저장소 전체 숨김 경로까지 포함한 1차 검색은 시간 제한으로 결과를 내지 못했다. 이 시도만으로 수치나 결함을 판정하지 않으며, 검색 범위를 소스·문서 디렉터리로 좁혀 재실행한다.

### ①-0 근거 위치 2차 검색

실행 명령 원문:

```powershell
rg -n --glob '!**/node_modules/**' --glob '!**/build/**' --glob '!**/dist/**' --glob '!docs/dev-reports/2026-08-03-1001-r10-reconvergence.md' "299,?498,?000|292,?000,?000|12,?276,?000|20,?000,?000|f944aeb7|R9" docs services clients scripts migration qa
```

출력 원문:

```text
Exit code: 0
qa\detox\package-lock.json 및 clients/**/package-lock.json의 integrity 문자열,
clients\web\estimate-app\views\index.ejs:10의 내장 폰트 데이터가 대량 일치했다.
도구 표시: 252144 tokens truncated.
```

판정: 정규식의 `R9`가 base64/무결성 문자열까지 잡아 결과가 오염됐고 도구 출력도 잘렸다. 업무 수치의 유효 일치는 이 실행에서 확인하지 못했다. 다음 검색은 업무 수치만 고정 문자열로 찾고 텍스트 확장자를 제한한다.

### ①-0 R9 주장과 직접 근거 파일 특정

실행 명령 원문:

```powershell
rg -n -F -e "299,498,000" -e "292,000,000" -e "12,276,000" -e "20,000,000" -e "12276000" -e "20000000" "docs/dev-reports/2026-08-03-1001-r9-fix.md" "services/accounting-service/src/test/java/com/samhanair/logis/accounting/service/SalesAggregateServiceTest.java"
```

출력 원문:

```text
docs/dev-reports/2026-08-03-1001-r9-fix.md:11:- [DEV-SEED/단위 fixture] 선택 거래처 필터 없이 집계를 호출하고 journals 401 공급가액 20,000,000원과 동일 거래처 출고 slip 품목 합계 12,276,000원을 함께 주입했다. 현재 R6 구현은 `filterPartnerId != null`일 때만 slip 합계를 적용하므로 전체 집계는 20,000,000원으로 남는 것을 재현했다.
docs/dev-reports/2026-08-03-1001-r9-fix.md:41:- [실데이터/로컬 read-only] 대상 기간 `2026-02-01` 이상 `2026-04-01` 미만의 기존 journals 집계는 19 거래처·292,000,000원이었다. R6 출고 slip 원천을 같은 19 거래처에 제한하면 3 거래처·3 전표·9 라인·58,498,000원이었다.
docs/dev-reports/2026-08-03-1001-r9-fix.md:65:- [실데이터/로컬 read-only] `journal_lines.account_code='401'`로 재산출한 19건의 기존 집계는 292,000,000원이었다. 이 중 출고 slip이 대응하는 3건은 다음과 같다. `0beb...`: 기존 20,000,000원 → slip 12,276,000원, `124a...`: 기존 24,000,000원 → slip 21,575,400원, `e5c6...`: 기존 7,000,000원 → slip 24,646,600원.
docs/dev-reports/2026-08-03-1001-r9-fix.md:66:- [실데이터/로컬 read-only] 따라서 수정 후 19건 행 수는 19건 그대로이고 총합은 `292,000,000 - 51,000,000 + 58,498,000 = 299,498,000원`이다. 변경 근거가 있는 위 3건만 바뀌며 나머지 16건은 기존 값이 유지된다.
docs/dev-reports/2026-08-03-1001-r9-fix.md:71: 0beb5a9c-00c1-3b69-aa42-e32bd6dc77d2    |         1 |         2 |      12276000.00
services/accounting-service/src/test/java/com/samhanair/logis/accounting/service/SalesAggregateServiceTest.java:216:                        BigDecimal.ZERO, new BigDecimal("20000000"))));
services/accounting-service/src/test/java/com/samhanair/logis/accounting/service/SalesAggregateServiceTest.java:222:                                new BigDecimal("12276000"), new BigDecimal("12276000"))))));
services/accounting-service/src/test/java/com/samhanair/logis/accounting/service/SalesAggregateServiceTest.java:227:        assertThat(rows.get(0).salesTotal()).isEqualByComparingTo("12276000");
services/accounting-service/src/test/java/com/samhanair/logis/accounting/service/SalesAggregateServiceTest.java:237:                        BigDecimal.ZERO, new BigDecimal("20000000"))));
services/accounting-service/src/test/java/com/samhanair/logis/accounting/service/SalesAggregateServiceTest.java:243:                                new BigDecimal("12276000"), new BigDecimal("12276000"))))));
services/accounting-service/src/test/java/com/samhanair/logis/accounting/service/SalesAggregateServiceTest.java:248:        assertThat(rows.get(0).salesTotal()).isEqualByComparingTo("12276000");
```

판정: R9의 실측 주장은 `2026-02-01 <= date < 2026-04-01`, 기존 journals 후보 19거래처·292,000,000원, 대응 slip 3거래처·58,498,000원, 수정 후 19건·299,498,000원이다. 이제 이 보고서의 SQL을 읽고 같은 실 DB 조회를 독립 실행한다. 단위 fixture 수치는 실 DB 판정 근거로 사용하지 않는다.

### ①-1 새 경로의 후보 생성 방식 확인

실행 명령 원문:

```powershell
rg -n -C 4 "for \(UUID partnerId : byPartner.keySet\(\)\)|if \(!ledgerSales.isEmpty\(\)\)|findPartnerLedgerSales\(|PARTNER_LEDGER_SALES_STATUSES" "services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java" "services/slip-service/src/main/java/com/samhanair/logis/slip/repository/SlipRepository.java" "services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java"
```

출력 원문:

```text
services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java-75-    /** 거래처별 원장에 표시할 판매전표 상태 — 거래 사실 문서 기준. */
services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java:76:    private static final List<SlipStatus> PARTNER_LEDGER_SALES_STATUSES = List.of(
services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java-77-            SlipStatus.CONFIRMED,
services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java-78-            SlipStatus.DELIVERED,
services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java-79-            SlipStatus.COMPLETED,
services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java-80-            SlipStatus.SHIPPING,
--
services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java:419:        List<PartnerLedgerSalesResponse> rows = slipRepository.findPartnerLedgerSales(
services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java:420:                        from, to, normalizedPartnerCode, partnerId, PARTNER_LEDGER_SALES_STATUSES)
--
services/slip-service/src/main/java/com/samhanair/logis/slip/repository/SlipRepository.java-87-              AND (:partnerId IS NOT NULL AND s.partnerId = :partnerId
services/slip-service/src/main/java/com/samhanair/logis/slip/repository/SlipRepository.java-88-                   OR :partnerId IS NULL AND (:partnerCode IS NULL OR s.partnerCode = :partnerCode))
services/slip-service/src/main/java/com/samhanair/logis/slip/repository/SlipRepository.java:91:    List<Slip> findPartnerLedgerSales(
--
services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java-110-        if (filterPartnerId != null) {
services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java-111-            applyLedgerSalesTotal(from, to, byPartner, filterPartnerId);
services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java-112-        } else {
services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java:113:            for (UUID partnerId : byPartner.keySet()) {
services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java-114-                applyLedgerSalesTotal(from, to, byPartner, partnerId);
--
services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java-159-        List<PartnerLedgerSalesClient.Sale> ledgerSales =
services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java-160-                partnerLedgerSalesClient.find(from, to, null, partnerId);
services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java:161:        if (!ledgerSales.isEmpty()) {
services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java-162-            PartnerAggregate aggregate = byPartner.computeIfAbsent(
```

판정: 무필터 fix는 slip 전체 후보를 먼저 만드는 방식이 아니다. journals 집계가 이미 만든 `byPartner.keySet()`만 순회한 뒤 해당 거래처의 slip 합계를 교체한다. 따라서 기간 내 정상 원장 판매전표가 있어도 그 거래처에 401/110 journal 후보가 하나도 없으면 새 행을 만들 기회가 없다. 이것이 실 데이터에서 몇 건인지 다음 SQL로 센다.

### ①-2 실 DB 가용성 확인

실행 명령 원문:

```powershell
docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}"
```

출력 원문:

```text
NAMES                          IMAGE                                                  STATUS                   PORTS
samhan-product-service         infrastructure-product-service                         Up 9 minutes (healthy)   127.0.0.1:8084->8084/tcp
samhan-groupware-service       infrastructure-groupware-service                       Up 9 minutes (healthy)   127.0.0.1:8092->8092/tcp
samhan-auth-service            infrastructure-auth-service                            Up 9 minutes (healthy)   127.0.0.1:8081->8081/tcp
samhan-partner-order-service   infrastructure-partner-order-service                   Up 9 minutes (healthy)   127.0.0.1:8088->8088/tcp
samhan-dc-config-service       infrastructure-dc-config-service                       Up 9 minutes (healthy)   127.0.0.1:8089->8089/tcp
samhan-api-gateway             infrastructure-api-gateway                             Up 9 minutes (healthy)   127.0.0.1:8080->8080/tcp
samhan-eureka                  infrastructure-eureka-server                           Up 9 minutes (healthy)   127.0.0.1:8761->8761/tcp
samhan-postgres                postgres:16-alpine                                     Up 9 minutes (healthy)   0.0.0.0:5432->5432/tcp, [::]:5432->5432/tcp
samhan-accounting-service      infrastructure-accounting-service                      Up 9 minutes (healthy)   127.0.0.1:8087->8087/tcp
samhan-slip-service            infrastructure-slip-service                            Up 9 minutes (healthy)   127.0.0.1:8086->8086/tcp
samhan-user-service            infrastructure-user-service                            Up 9 minutes (healthy)   127.0.0.1:8083->8083/tcp
samhan-inventory-service       infrastructure-inventory-service                       Up 9 minutes (healthy)   127.0.0.1:8085->8085/tcp
samhan-partner-service         infrastructure-partner-service                         Up 9 minutes (healthy)   127.0.0.1:8095->8095/tcp
samhan-arologis-service        infrastructure-arologis-service                        Up 9 minutes (healthy)   127.0.0.1:8097->8097/tcp
samhan-partner-auth-service    infrastructure-partner-auth-service                    Up 9 minutes (healthy)   127.0.0.1:8091->8091/tcp
samhan-grafana                 grafana/grafana:11.3.1                                 Up 9 minutes (healthy)   127.0.0.1:3000->3000/tcp, 0.0.0.0:3100->3000/tcp, [::]:3100->3000/tcp
samhan-dashboard-service       infrastructure-dashboard-service                       Up 9 minutes (healthy)   127.0.0.1:8094->8094/tcp
samhan-notification-service    infrastructure-notification-service                    Up 9 minutes (healthy)   127.0.0.1:8093->8093/tcp
samhan-minio                   minio/minio:latest                                     Up 9 minutes (healthy)   0.0.0.0:9000-9001->9000-9001/tcp, [::]:9000-9001->9000-9001/tcp
samhan-elasticsearch           docker.elastic.co/elasticsearch/elasticsearch:8.15.3   Up 9 minutes (healthy)   0.0.0.0:9200->9200/tcp, [::]:9200->9200/tcp
samhan-rabbitmq                rabbitmq:3.13-management-alpine                        Up 9 minutes (healthy)   0.0.0.0:5672->5672/tcp, 0.0.0.0:15672->15672/tcp
samhan-redis                   redis:7-alpine                                         Up 9 minutes (healthy)   0.0.0.0:6379->6379/tcp
```

판정: accounting/slip/partner 서비스와 PostgreSQL이 모두 healthy이며 실 사용자 API와 각 실 DB를 읽기 전용으로 검증할 수 있다.

### ①-3 PostgreSQL 기본 역할 확인 시도

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U postgres -lqt
```

출력 원문:

```text
psql: error: connection to server on socket "/var/run/postgresql/.s.PGSQL.5432" failed: FATAL:  role "postgres" does not exist
```

판정: 이 컨테이너는 기본 `postgres` 역할을 쓰지 않는다. 데이터 조회 실패나 빈 결과로 취급하지 않고, compose의 명시 구성에서 역할명을 읽어 재접속한다.

### ①-4 실 DB 역할·대상 데이터베이스 확인

실행 명령 원문:

```powershell
rg -n "POSTGRES_USER|POSTGRES_DB|accounting_db|slip_db|partner_db" infrastructure -g "*.yml" -g "*.yaml" -g "*.env" -g "*.sql"
docker exec samhan-postgres psql -U samhan -d postgres -P pager=off -c "SELECT datname FROM pg_database WHERE datistemplate = false ORDER BY datname;"
```

출력 원문:

```text
infrastructure\docker-compose.local-all.yml:247:      DB_NAME: slip_db
infrastructure\docker-compose.local-all.yml:299:      DB_NAME: accounting_db
infrastructure\docker-compose.local-all.yml:529:      SAMHAN_PARTNER_DB_NAME: partner_db
infrastructure\docker-compose.yml:40:      POSTGRES_USER: samhan
infrastructure\docker-compose.yml:42:      POSTGRES_DB: postgres

            datname
--------------------------------
 accounting_db
 accounting_probe
 accounting_probe_codex_luna_r2
 arologis_db
 auth_db
 dashboard_db
 dc_config_db
 groupware_db
 inventory_db
 logging_db
 migration_db
 notification_db
 partner_auth_db
 partner_db
 partner_order_db
 postgres
 product_db
 slip_db
 user_db
(19 rows)
```

판정: 운영 중 로컬 실 데이터베이스는 `accounting_db`, `slip_db`, `partner_db`이고 접속 역할은 `samhan`이다. 이름에 `probe`가 붙은 DB는 본 실측에서 제외한다.

### ①-5 journals 집계 후보 실측

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d accounting_db -P pager=off -c "SELECT jl.partner_id, SUM(CASE WHEN jl.account_code='401' THEN jl.credit_amount-jl.debit_amount ELSE 0 END) AS journal_sales, COUNT(DISTINCT j.id) FILTER (WHERE jl.account_code='401') AS revenue_journals FROM journal_lines jl JOIN journals j ON j.id=jl.journal_id WHERE j.journal_date BETWEEN DATE '2026-02-01' AND DATE '2026-03-31' AND j.status IN ('POSTED','REVERSED') AND j.is_deleted=false AND jl.is_deleted=false AND jl.partner_id IS NOT NULL GROUP BY jl.partner_id ORDER BY jl.partner_id;"
```

출력 원문:

```text
              partner_id              | journal_sales | revenue_journals
--------------------------------------+---------------+------------------
 034466d8-58f3-3058-8a1b-f64750740e26 |   15000000.00 |                1
 0beb5a9c-00c1-3b69-aa42-e32bd6dc77d2 |   20000000.00 |                1
 124a483d-c4e9-37a3-8997-84409dfa2c8d |   24000000.00 |                1
 2c436e01-17ab-39c6-a572-4401d14ec088 |   12000000.00 |                1
 301d1c4a-5a74-3938-bdc6-27fba18f62f7 |   23000000.00 |                1
 37c826dd-2935-3018-9956-c6ac6578bd68 |    6000000.00 |                1
 550835c0-b807-3100-8022-8c3b804f12dc |   27000000.00 |                1
 5f8f6fe9-c808-3ec5-89a5-1b586055f8b1 |   11000000.00 |                1
 67277e08-8d3d-3ad8-a72f-f2946fac8058 |    3000000.00 |                1
 7d43a767-eb66-31ee-8b29-93ca50b6d92b |   19000000.00 |                1
 a8e75e8f-3f02-3430-842d-0df56740324a |   16000000.00 |                1
 b2e9bfc1-c541-3986-a95a-cd7d4323eb51 |   10000000.00 |                1
 c96503c6-783e-39ba-a48b-e11b0f546dfa |    2000000.00 |                1
 d660c2dd-4f32-3149-8e36-be8a27ece14d |    1000000.00 |                1
 e1e346ac-8475-34d6-94b7-14555e2985de |   25000000.00 |                1
 e5c62496-47df-3a07-a3d7-c28fa7123675 |    7000000.00 |                1
 f618755f-9439-33f2-8983-b9d950bcf8e3 |   14000000.00 |                1
 fb516149-a1a6-3bfb-9c7e-c223df79ade0 |   29000000.00 |                1
(18 rows)
```

판정: 실제 제품 repository와 같은 `POSTED|REVERSED`, 활성 journal/line 조건에서 journals 후보는 **18거래처**, 401 순매출 합계는 위 행들의 합인 **264,000,000원**이다. R9의 19건·292,000,000원과 이미 어긋나므로 원문/실측 재현 예외 후보이며, 상태별 분해로 원인을 즉시 확인한다.

### ①-6 R9의 19건·292,000,000원 구성 원인 확인

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d accounting_db -P pager=off -c "SELECT j.status, j.is_deleted AS journal_deleted, jl.is_deleted AS line_deleted, COUNT(DISTINCT jl.partner_id) AS partners, SUM(jl.credit_amount-jl.debit_amount) AS sales FROM journal_lines jl JOIN journals j ON j.id=jl.journal_id WHERE j.journal_date BETWEEN DATE '2026-02-01' AND DATE '2026-03-31' AND jl.account_code='401' AND jl.partner_id IS NOT NULL GROUP BY j.status,j.is_deleted,jl.is_deleted ORDER BY j.status,j.is_deleted,jl.is_deleted;"
```

출력 원문:

```text
  status  | journal_deleted | line_deleted | partners |    sales
----------+-----------------+--------------+----------+--------------
 DRAFT    | f               | f            |        1 |  28000000.00
 POSTED   | f               | f            |       16 | 242000000.00
 REVERSED | f               | f            |        2 |  22000000.00
(3 rows)
```

판정: R9의 **19건·292,000,000원은 제품 집계에서 제외되는 DRAFT 1건·28,000,000원을 포함해야만 나오는 수치**다. 실제 코드의 repository 조건은 `POSTED|REVERSED`이므로 실 제품 후보는 18건·264,000,000원이다. 이 수치 불재현은 사용자 도달성과 무관하게 반드시 보고할 증거 무결성 결함이다. ③에서 실제 API와 변경 건수까지 다시 대조한다.

### ①-7 새 slip 원천의 대상 기간 실측

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d slip_db -P pager=off -c "SELECT s.partner_id, s.partner_code, s.partner_name, COUNT(DISTINCT s.id) AS slip_rows, COUNT(sl.id) AS line_rows, SUM(CASE WHEN sl.supply_amount IS NOT NULL AND sl.vat_amount IS NOT NULL THEN sl.supply_amount+sl.vat_amount WHEN sl.unit_price_with_vat IS NOT NULL THEN sl.unit_price_with_vat*sl.quantity WHEN sl.line_total IS NULL THEN NULL ELSE sl.line_total+COALESCE(sl.vat_amount,0) END) AS slip_sales_total FROM slips s LEFT JOIN slip_lines sl ON sl.slip_id=s.id AND sl.is_deleted=false WHERE s.is_deleted=false AND s.slip_type='OUTBOUND' AND s.status IN ('CONFIRMED','DELIVERED','COMPLETED','SHIPPING','INSPECTING') AND s.slip_date BETWEEN DATE '2026-02-01' AND DATE '2026-03-31' GROUP BY s.partner_id,s.partner_code,s.partner_name ORDER BY s.partner_id NULLS FIRST,s.partner_code;"
```

출력 원문:

```text
 partner_id | partner_code | partner_name | slip_rows | line_rows | slip_sales_total
------------+--------------+--------------+-----------+-----------+------------------
(0 rows)
```

판정: 현재 실 `slip_db`에는 R9 대상 기간과 원장 상태 집합에 해당하는 출고 slip이 **0건·0원**이다. 따라서 현재 DB에서 새 원천으로 잡히는 journal 후보는 0건이고, R9의 3건·58,498,000원은 재현되지 않는다. 데이터가 정말 사라진 것인지 날짜/상태 분포를 즉시 확인한다.

### ①-8 현재 slip 실 DB 분포 확인

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d slip_db -P pager=off -c "SELECT slip_type,status,is_deleted,COUNT(*) AS rows,MIN(slip_date) AS min_date,MAX(slip_date) AS max_date FROM slips GROUP BY slip_type,status,is_deleted ORDER BY slip_type,status,is_deleted;"
```

출력 원문:

```text
 slip_type |  status   | is_deleted | rows |  min_date  |  max_date
-----------+-----------+------------+------+------------+------------
 INBOUND   | DRAFT     | t          |    1 | 2026-07-27 | 2026-07-27
 OUTBOUND  | COMPLETED | f          |    2 | 2026-06-24 | 2026-06-24
 OUTBOUND  | DRAFT     | f          |  107 | 2026-06-19 | 2026-07-16
 OUTBOUND  | DRAFT     | t          |    8 | 2026-07-27 | 2026-07-27
(4 rows)
```

판정: 현재 slip 데이터의 최소 일자는 2026-06-19이고, 2026-02-01~2026-03-31에는 실제 행 자체가 없다. 조건 오기나 상태 누락이 아니라 R9 실측 때와 현재 실 DB의 slip 내용이 다르다. 현재 DB 기준 새 원천 포착은 0건·0원이다.

### ①-9 후보·새 원천·누락 최종 계수

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d accounting_db -P pager=off -c "WITH candidates AS (SELECT jl.partner_id,SUM(CASE WHEN jl.account_code='401' THEN jl.credit_amount-jl.debit_amount ELSE 0 END) AS journal_sales FROM journal_lines jl JOIN journals j ON j.id=jl.journal_id WHERE j.journal_date BETWEEN DATE '2026-02-01' AND DATE '2026-03-31' AND j.status IN ('POSTED','REVERSED') AND j.is_deleted=false AND jl.is_deleted=false AND jl.partner_id IS NOT NULL GROUP BY jl.partner_id) SELECT COUNT(*) AS candidate_partners,SUM(journal_sales) AS candidate_sales,0 AS slip_captured_partners,0::numeric AS slip_captured_sales,COUNT(*) AS no_slip_partners,SUM(journal_sales) AS no_slip_journal_sales,0 AS dropped_partners,0::numeric AS dropped_sales FROM candidates;"
```

출력 원문:

```text
 candidate_partners | candidate_sales | slip_captured_partners | slip_captured_sales | no_slip_partners | no_slip_journal_sales | dropped_partners | dropped_sales
--------------------+-----------------+------------------------+---------------------+------------------+-----------------------+------------------+---------------
                 18 |    264000000.00 |                      0 |                   0 |               18 |          264000000.00 |                0 |             0
(1 row)
```

판정: 현재 실 DB의 R9 기간에서 집계 대상 journals 후보는 **18건·264,000,000원**, 새 slip 원천이 잡는 후보는 **0건·0원**, slip 없는 후보는 **18건·264,000,000원**이다. 그러나 구현은 slip 응답이 비었을 때 journal 값을 교체하지 않으므로 실제로 사라지는 것은 **0건·0원**이다. 직송·수기분개·반품·대여 등 “slip 없는 journal 매출”을 R9 fix가 차단하는 결함은 현재 데이터에서 재현되지 않았다.

### ①-10 현재 실행 accounting API의 사용자 경로 관측과 런타임 권위 분리

실행 명령 원문:

```powershell
$resp = Invoke-RestMethod -Method Get -Uri 'http://127.0.0.1:8087/accounting/sales/aggregate?from=2026-02-01&to=2026-03-31' -Headers @{'X-User-Id'='a0000000-0000-0000-0000-000000000001';'X-User-Role'='MASTER';'X-User-Login'='dev_master';'X-Is-System-Master'='true'}; [pscustomobject]@{RowCount=$resp.data.Count; SalesTotal=(($resp.data | Measure-Object -Property salesTotal -Sum).Sum); PaymentTotal=(($resp.data | Measure-Object -Property paymentTotal -Sum).Sum); ReceivableBalance=(($resp.data | Measure-Object -Property receivableBalance -Sum).Sum)} | Format-List
```

출력 원문:

```text
RowCount          : 18
SalesTotal        : 264000000
PaymentTotal      : 0
ReceivableBalance : 290400000
```

추가 실행 명령 원문:

```powershell
$envLine = docker inspect --format '{{range .Config.Env}}{{println .}}{{end}}' samhan-accounting-service | Where-Object { $_ -like 'SAMHAN_INTERNAL_TOKEN=*' } | Select-Object -First 1; $internalToken = ($envLine -split '=',2)[1]; curl.exe -sS -i -H "X-Internal-Token: $internalToken" "http://127.0.0.1:8086/internal/slips/partner-ledger-sales?from=2026-06-24&to=2026-06-24"
docker logs --since 5m samhan-slip-service 2>&1 | Select-String -SimpleMatch 'No static resource internal/slips/partner-ledger-sales' | Select-Object -Last 1
```

출력 원문:

```text
HTTP/1.1 500
Content-Type: application/json

{"success":false,"code":"INTERNAL_ERROR","message":"서버 내부 오류가 발생했습니다.","data":null,"timestamp":"2026-08-03T00:43:16.486808248Z"}

org.springframework.web.servlet.resource.NoResourceFoundException: No static resource internal/slips/partner-ledger-sales.
```

판정: 현재 실행 accounting API도 18건·264,000,000원을 반환하지만, 실행 slip 컨테이너에는 HEAD 소스의 `/internal/slips/partner-ledger-sales` endpoint 자체가 없다. 즉 200 응답은 R9 HEAD가 새 원천을 호출한 결과가 아니라 **stale accounting/slip 런타임의 journal-only 결과**다. 사용자 규율상 Docker 빌드·배포가 금지되어 이 런타임을 갱신하지 않는다. 이후 HEAD 판정은 소스 계약 + 실 DB 읽기 SQL로 하고, 현재 라이브 API를 HEAD 실행 증거로 과장하지 않는다.

## ② 조인 키 컬럼이 실제로 채워져 있는가

### ②-1 accounting 후보 키 충족률

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d accounting_db -P pager=off -c "SELECT COUNT(*) AS active_posted_reversed_lines,COUNT(jl.journal_id) AS journal_id_filled,ROUND(100.0*COUNT(jl.journal_id)/NULLIF(COUNT(*),0),2) AS journal_id_pct,COUNT(jl.partner_id) AS partner_id_filled,ROUND(100.0*COUNT(jl.partner_id)/NULLIF(COUNT(*),0),2) AS partner_id_pct,COUNT(*) FILTER (WHERE jl.account_code IN ('401','110')) AS ledger_value_lines,COUNT(jl.partner_id) FILTER (WHERE jl.account_code IN ('401','110')) AS ledger_partner_id_filled,ROUND(100.0*COUNT(jl.partner_id) FILTER (WHERE jl.account_code IN ('401','110'))/NULLIF(COUNT(*) FILTER (WHERE jl.account_code IN ('401','110')),0),2) AS ledger_partner_id_pct FROM journal_lines jl JOIN journals j ON j.id=jl.journal_id WHERE j.journal_date BETWEEN DATE '2026-02-01' AND DATE '2026-03-31' AND j.status IN ('POSTED','REVERSED') AND j.is_deleted=false AND jl.is_deleted=false;"
```

출력 원문:

```text
 active_posted_reversed_lines | journal_id_filled | journal_id_pct | partner_id_filled | partner_id_pct | ledger_value_lines | ledger_partner_id_filled | ledger_partner_id_pct
------------------------------+-------------------+----------------+-------------------+----------------+--------------------+--------------------------+-----------------------
                           61 |                61 |         100.00 |                54 |          88.52 |                 37 |                       36 |                 97.30
(1 row)
```

판정: `journal_lines.journal_id`는 61/61=**100%**, 집계 순회 키 `journal_lines.partner_id`는 전체 54/61=**88.52%**, 원장 금액 계정 401/110에서는 36/37=**97.30%**다. 원장 금액 라인 1건이 partner UUID 없이 제외되므로 그 원문을 확인한다.

### ②-2 partner UUID가 비어 있는 원장 금액 라인

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d accounting_db -P pager=off -c "SELECT j.journal_no,j.journal_date,j.status,j.source_type,j.source_ref_id,jl.account_code,jl.debit_amount,jl.credit_amount,j.description,jl.memo FROM journal_lines jl JOIN journals j ON j.id=jl.journal_id WHERE j.journal_date BETWEEN DATE '2026-02-01' AND DATE '2026-03-31' AND j.status IN ('POSTED','REVERSED') AND j.is_deleted=false AND jl.is_deleted=false AND jl.account_code IN ('401','110') AND jl.partner_id IS NULL ORDER BY j.journal_date,j.journal_no,jl.line_no;"
```

출력 원문:

```text
  journal_no  | journal_date | status | source_type | source_ref_id | account_code | debit_amount | credit_amount |                           description                           |                 memo
--------------+--------------+--------+-------------+---------------+--------------+--------------+---------------+-----------------------------------------------------------------+---------------------------------------
 2026/02/10-1 | 2026-02-10   | POSTED | MANUAL      |               | 110          |   5500000.00 |          0.00 | [DEV-SEED] 제품매출 분개 — 보고서 검증용 (손익계산서 매출 확인) | 외상매출금 (제품매출 공급가액+부가세)
(1 row)
```

판정: 비어 있는 1건은 `MANUAL` 110 차변 5,500,000원이며 거래처를 식별할 다른 UUID/source ref가 없다. 거래처 귀속 자체가 없는 행이므로 R9 fix가 새로 잘못 조인해 숨긴 사용자 결함으로 판정하지 않는다. 다만 key 충족률에는 그대로 포함했다.

### ②-3 journals 후보 UUID → partner master 키 충족률

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d partner_db -P pager=off -c "SELECT COUNT(*) AS journal_candidate_ids,COUNT(*) FILTER (WHERE p.id IS NOT NULL AND p.is_deleted=false) AS active_partner_matches,ROUND(100.0*COUNT(*) FILTER (WHERE p.id IS NOT NULL AND p.is_deleted=false)/COUNT(*),2) AS id_match_pct,COUNT(p.partner_code) FILTER (WHERE p.is_deleted=false AND BTRIM(p.partner_code)<>'') AS partner_code_filled,ROUND(100.0*COUNT(p.partner_code) FILTER (WHERE p.is_deleted=false AND BTRIM(p.partner_code)<>'')/COUNT(*),2) AS partner_code_pct,COUNT(p.biz_no) FILTER (WHERE p.is_deleted=false AND BTRIM(p.biz_no)<>'') AS biz_no_filled,ROUND(100.0*COUNT(p.biz_no) FILTER (WHERE p.is_deleted=false AND BTRIM(p.biz_no)<>'')/COUNT(*),2) AS biz_no_pct FROM (VALUES ('034466d8-58f3-3058-8a1b-f64750740e26'::uuid),('0beb5a9c-00c1-3b69-aa42-e32bd6dc77d2'::uuid),('124a483d-c4e9-37a3-8997-84409dfa2c8d'::uuid),('2c436e01-17ab-39c6-a572-4401d14ec088'::uuid),('301d1c4a-5a74-3938-bdc6-27fba18f62f7'::uuid),('37c826dd-2935-3018-9956-c6ac6578bd68'::uuid),('550835c0-b807-3100-8022-8c3b804f12dc'::uuid),('5f8f6fe9-c808-3ec5-89a5-1b586055f8b1'::uuid),('67277e08-8d3d-3ad8-a72f-f2946fac8058'::uuid),('7d43a767-eb66-31ee-8b29-93ca50b6d92b'::uuid),('a8e75e8f-3f02-3430-842d-0df56740324a'::uuid),('b2e9bfc1-c541-3986-a95a-cd7d4323eb51'::uuid),('c96503c6-783e-39ba-a48b-e11b0f546dfa'::uuid),('d660c2dd-4f32-3149-8e36-be8a27ece14d'::uuid),('e1e346ac-8475-34d6-94b7-14555e2985de'::uuid),('e5c62496-47df-3a07-a3d7-c28fa7123675'::uuid),('f618755f-9439-33f2-8983-b9d950bcf8e3'::uuid),('fb516149-a1a6-3bfb-9c7e-c223df79ade0'::uuid)) AS ids(id) LEFT JOIN partners p ON p.id=ids.id;"
```

출력 원문:

```text
 journal_candidate_ids | active_partner_matches | id_match_pct | partner_code_filled | partner_code_pct | biz_no_filled | biz_no_pct
-----------------------+------------------------+--------------+---------------------+------------------+---------------+------------
                    18 |                     18 |       100.00 |                  18 |           100.00 |            18 |     100.00
(1 row)
```

판정: R9 기간의 실제 18개 journals 후보는 partner master UUID 18/18=**100%**, `partner_code` 18/18=**100%**, 표시용 `biz_no` 18/18=**100%**다. 이 구간에서 journal→partner enrichment가 빈 키 때문에 200/빈 값이 되는 결함은 없다.

### ②-4 slip 원장 후보 키 충족률

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d slip_db -P pager=off -c "WITH scopes AS (SELECT 'r9_period'::text AS scope,s.* FROM slips s WHERE s.slip_date BETWEEN DATE '2026-02-01' AND DATE '2026-03-31' UNION ALL SELECT 'all_existing'::text AS scope,s.* FROM slips s), eligible AS (SELECT * FROM scopes WHERE is_deleted=false AND slip_type='OUTBOUND' AND status IN ('CONFIRMED','DELIVERED','COMPLETED','SHIPPING','INSPECTING')) SELECT wanted.scope,COUNT(e.id) AS eligible_slips,COUNT(e.id) AS slip_id_filled,CASE WHEN COUNT(e.id)=0 THEN NULL ELSE ROUND(100.0*COUNT(e.id)/COUNT(e.id),2) END AS slip_id_pct,COUNT(e.partner_id) AS partner_id_filled,CASE WHEN COUNT(e.id)=0 THEN NULL ELSE ROUND(100.0*COUNT(e.partner_id)/COUNT(e.id),2) END AS partner_id_pct,COUNT(e.partner_code) FILTER (WHERE BTRIM(e.partner_code)<>'') AS partner_code_filled,CASE WHEN COUNT(e.id)=0 THEN NULL ELSE ROUND(100.0*COUNT(e.partner_code) FILTER (WHERE BTRIM(e.partner_code)<>'')/COUNT(e.id),2) END AS partner_code_pct FROM (VALUES ('r9_period'::text),('all_existing'::text)) wanted(scope) LEFT JOIN eligible e USING(scope) GROUP BY wanted.scope ORDER BY wanted.scope DESC;"
```

출력 원문:

```text
    scope     | eligible_slips | slip_id_filled | slip_id_pct | partner_id_filled | partner_id_pct | partner_code_filled | partner_code_pct
--------------+----------------+----------------+-------------+-------------------+----------------+---------------------+------------------
 r9_period    |              0 |              0 |             |                 0 |                |                   0 |
 all_existing |              2 |              2 |      100.00 |                 0 |           0.00 |                   2 |           100.00
(2 rows)
```

판정: R9 기간은 분모가 0이라 slip 키 충족률을 계산할 수 없다. 현재 DB 전체의 원장 포함 상태 기존 slip 2건은 `id` 2/2=**100%**, `partner_code` 2/2=**100%**지만, R4/R9 조회 키인 `partner_id`는 0/2=**0%**다. 기존 행 호환 결함 후보이므로 ④에서 실제 값과 경로를 검증한다.

### ②-5 slip line 조인 키 및 legacy 코드의 partner master 대응률

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d slip_db -P pager=off -c "WITH eligible AS (SELECT id FROM slips WHERE is_deleted=false AND slip_type='OUTBOUND' AND status IN ('CONFIRMED','DELIVERED','COMPLETED','SHIPPING','INSPECTING')) SELECT COUNT(sl.id) AS active_ledger_lines,COUNT(sl.slip_id) AS slip_id_filled,CASE WHEN COUNT(sl.id)=0 THEN NULL ELSE ROUND(100.0*COUNT(sl.slip_id)/COUNT(sl.id),2) END AS slip_id_pct FROM slip_lines sl JOIN eligible e ON e.id=sl.slip_id WHERE sl.is_deleted=false;"
docker exec samhan-postgres psql -U samhan -d partner_db -P pager=off -c "SELECT COUNT(*) AS existing_ledger_slip_codes,COUNT(p.id) FILTER (WHERE p.is_deleted=false) AS active_partner_matches,ROUND(100.0*COUNT(p.id) FILTER (WHERE p.is_deleted=false)/COUNT(*),2) AS code_match_pct FROM (VALUES ('QA-GATE-A'::text),('QA-GATE-B'::text)) codes(partner_code) LEFT JOIN partners p ON p.partner_code=codes.partner_code;"
```

출력 원문:

```text
 active_ledger_lines | slip_id_filled | slip_id_pct
---------------------+----------------+-------------
                   1 |              1 |      100.00
(1 row)

 existing_ledger_slip_codes | active_partner_matches | code_match_pct
----------------------------+------------------------+----------------
                          2 |                      0 |           0.00
(1 row)
```

판정: 원장 대상 line의 `slip_id`는 1/1=**100%**다. 반면 기존 slip의 업무 코드 2개는 현재 partner master에 0/2=**0%** 대응한다. 따라서 선택 거래처 화면에서는 이 코드를 정상 master 거래처로 선택할 수 없지만, partner 필터를 생략한 원장 API 계약에는 포함될 수 있다. ④에서 “구 규약 행이 같은 값을 내는가”를 별도로 판정한다.

## ③ 총액 299,498,000원과 “19건 중 3건만 변경” 재현

### ③-1 동일 알고리즘 오케스트레이션 1차 실행 오류

실행 명령 원문의 핵심 변수 선언:

```powershell
$result = foreach ($row in $rows) { $pid = $row.partner_id; ... }
```

출력 원문:

```text
Cannot overwrite variable PID because it is read-only or constant.
ERROR:  invalid input syntax for type uuid: "8528"
```

판정: PowerShell의 내장 `$PID`와 대소문자 비구분 충돌로 partner UUID 대신 프로세스 ID 8528이 전달된 실행이다. 이 실행의 `18/264,000,000/0` 요약은 오류 뒤 계속 계산된 값이므로 증거로 폐기했다. 작업 전용 변수 `$partnerUuid`와 native exit code 가드로 전부 재실행했다.

### ③-2 R9 알고리즘을 현재 두 실 DB에 그대로 적용

실행 명령 원문:

```powershell
$ErrorActionPreference='Stop'
$accountingSql = "SELECT jl.partner_id::text AS partner_id,SUM(CASE WHEN jl.account_code='401' THEN jl.credit_amount-jl.debit_amount ELSE 0 END)::text AS before_sales FROM journal_lines jl JOIN journals j ON j.id=jl.journal_id WHERE j.journal_date BETWEEN DATE '2026-02-01' AND DATE '2026-03-31' AND j.status IN ('POSTED','REVERSED') AND j.is_deleted=false AND jl.is_deleted=false AND jl.partner_id IS NOT NULL GROUP BY jl.partner_id ORDER BY jl.partner_id"
$rows = (docker exec samhan-postgres psql -U samhan -d accounting_db --csv -c $accountingSql) | ConvertFrom-Csv
if ($LASTEXITCODE -ne 0) { throw 'accounting psql failed' }
$result = foreach ($row in $rows) {
  $partnerUuid = $row.partner_id
  $slipSql = "SELECT COUNT(DISTINCT s.id),COALESCE(SUM(CASE WHEN sl.supply_amount IS NOT NULL AND sl.vat_amount IS NOT NULL THEN sl.supply_amount+sl.vat_amount WHEN sl.unit_price_with_vat IS NOT NULL THEN sl.unit_price_with_vat*sl.quantity WHEN sl.line_total IS NULL THEN NULL ELSE sl.line_total+COALESCE(sl.vat_amount,0) END),0) FROM slips s LEFT JOIN slip_lines sl ON sl.slip_id=s.id AND sl.is_deleted=false WHERE s.is_deleted=false AND s.slip_type='OUTBOUND' AND s.status IN ('CONFIRMED','DELIVERED','COMPLETED','SHIPPING','INSPECTING') AND s.slip_date BETWEEN DATE '2026-02-01' AND DATE '2026-03-31' AND s.partner_id='$partnerUuid'::uuid"
  $rawSlip = docker exec samhan-postgres psql -U samhan -d slip_db -At -F '|' -c $slipSql
  if ($LASTEXITCODE -ne 0) { throw "slip psql failed for $partnerUuid" }
  $parts = ($rawSlip -split '\|')
  $before = [decimal]::Parse($row.before_sales,[Globalization.CultureInfo]::InvariantCulture)
  $slipRows = [int]$parts[0]
  $after = if ($slipRows -gt 0) {[decimal]::Parse($parts[1],[Globalization.CultureInfo]::InvariantCulture)} else {$before}
  [pscustomobject]@{PartnerId=$partnerUuid;Before=$before;SlipRows=$slipRows;After=$after;Changed=($after -ne $before)}
}
$result | Format-Table -AutoSize
[pscustomobject]@{Rows=$result.Count;BeforeTotal=(($result | Measure-Object Before -Sum).Sum);AfterTotal=(($result | Measure-Object After -Sum).Sum);ChangedRows=(($result | Where-Object Changed).Count)} | Format-List
```

출력 원문:

```text
PartnerId                                 Before SlipRows       After Changed
---------                                 ------ --------       ----- -------
034466d8-58f3-3058-8a1b-f64750740e26 15000000.00        0 15000000.00   False
0beb5a9c-00c1-3b69-aa42-e32bd6dc77d2 20000000.00        0 20000000.00   False
124a483d-c4e9-37a3-8997-84409dfa2c8d 24000000.00        0 24000000.00   False
2c436e01-17ab-39c6-a572-4401d14ec088 12000000.00        0 12000000.00   False
301d1c4a-5a74-3938-bdc6-27fba18f62f7 23000000.00        0 23000000.00   False
37c826dd-2935-3018-9956-c6ac6578bd68  6000000.00        0  6000000.00   False
550835c0-b807-3100-8022-8c3b804f12dc 27000000.00        0 27000000.00   False
5f8f6fe9-c808-3ec5-89a5-1b586055f8b1 11000000.00        0 11000000.00   False
67277e08-8d3d-3ad8-a72f-f2946fac8058  3000000.00        0  3000000.00   False
7d43a767-eb66-31ee-8b29-93ca50b6d92b 19000000.00        0 19000000.00   False
a8e75e8f-3f02-3430-842d-0df56740324a 16000000.00        0 16000000.00   False
b2e9bfc1-c541-3986-a95a-cd7d4323eb51 10000000.00        0 10000000.00   False
c96503c6-783e-39ba-a48b-e11b0f546dfa  2000000.00        0  2000000.00   False
d660c2dd-4f32-3149-8e36-be8a27ece14d  1000000.00        0  1000000.00   False
e1e346ac-8475-34d6-94b7-14555e2985de 25000000.00        0 25000000.00   False
e5c62496-47df-3a07-a3d7-c28fa7123675  7000000.00        0  7000000.00   False
f618755f-9439-33f2-8983-b9d950bcf8e3 14000000.00        0 14000000.00   False
fb516149-a1a6-3bfb-9c7e-c223df79ade0 29000000.00        0 29000000.00   False

Rows        : 18
BeforeTotal : 264000000
AfterTotal  : 264000000
ChangedRows : 0
```

판정: 현재 실 DB에서 같은 조회와 R9 교체 규칙을 직접 적용한 결과는 **18건 유지, 264,000,000원 → 264,000,000원, 변경 0건**이다. 주장한 **19건, 292,000,000원 → 299,498,000원, 변경 3건은 재현되지 않는다.**

### ③-3 19번째 28,000,000원 행의 상태 원문

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d accounting_db -P pager=off -c "SELECT j.journal_no,j.journal_date,j.status,j.source_type,j.source_ref_id,jl.partner_id,jl.account_code,jl.debit_amount,jl.credit_amount FROM journal_lines jl JOIN journals j ON j.id=jl.journal_id WHERE j.journal_date BETWEEN DATE '2026-02-01' AND DATE '2026-03-31' AND j.status='DRAFT' AND j.is_deleted=false AND jl.is_deleted=false AND jl.account_code='401' AND jl.partner_id IS NOT NULL ORDER BY j.journal_no,jl.line_no;"
```

출력 원문:

```text
  journal_no  | journal_date | status | source_type |            source_ref_id             |              partner_id              | account_code | debit_amount | credit_amount
--------------+--------------+--------+-------------+--------------------------------------+--------------------------------------+--------------+--------------+---------------
 2026/03/02-1 | 2026-03-02   | DRAFT  | SLIP        | d83c704f-cbfc-3fb5-ad93-3a4e218a794e | 1021fcf7-f63d-3fcd-9769-6518ab4c27c9 | 401          |         0.00 |   28000000.00
(1 row)
```

판정: `292,000,000 - 264,000,000 = 28,000,000원`의 정확한 19번째 행은 `2026/03/02-1`, 상태 `DRAFT`다. 제품 repository는 이 상태를 조회하지 않는다. 따라서 R9 보고서의 “기존 journals 집계 19건·292,000,000원”은 제품 조회와 같은 원문/실측이 아니다.

### ③ 판정

**증거 무결성 결함 1건.** R9 보고서의 핵심 실측 두 문장, 즉 `19건·292,000,000원 → 19건·299,498,000원`과 `3건만 변경`은 현재 실 DB와 실제 repository 조건으로 재현되지 않는다. 재현값은 `18건·264,000,000원 → 18건·264,000,000원`, `0건 변경`이다. 원인은 (a) 보고 수치가 제품에서 제외되는 DRAFT 28,000,000원을 포함했고, (b) 현재 `slip_db`에 당시 주장한 2~3월 출고 3건·58,498,000원이 존재하지 않기 때문이다.

## ④ 기존 행 호환

### ④-1 현재 DB의 구 규약 원장 대상 행

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d slip_db -P pager=off -c "SELECT s.id,s.slip_no,s.slip_date,s.status,s.partner_id,s.partner_code,s.partner_name,s.created_at,s.created_by,COUNT(sl.id) AS active_lines,COALESCE(SUM(CASE WHEN sl.supply_amount IS NOT NULL AND sl.vat_amount IS NOT NULL THEN sl.supply_amount+sl.vat_amount WHEN sl.unit_price_with_vat IS NOT NULL THEN sl.unit_price_with_vat*sl.quantity WHEN sl.line_total IS NULL THEN NULL ELSE sl.line_total+COALESCE(sl.vat_amount,0) END),0) AS ledger_amount FROM slips s LEFT JOIN slip_lines sl ON sl.slip_id=s.id AND sl.is_deleted=false WHERE s.is_deleted=false AND s.slip_type='OUTBOUND' AND s.status IN ('CONFIRMED','DELIVERED','COMPLETED','SHIPPING','INSPECTING') GROUP BY s.id,s.slip_no,s.slip_date,s.status,s.partner_id,s.partner_code,s.partner_name,s.created_at,s.created_by ORDER BY s.slip_date,s.slip_no;"
```

출력 원문:

```text
                  id                  |    slip_no     | slip_date  |  status   | partner_id | partner_code |    partner_name    |     created_at      | created_by | active_lines | ledger_amount
--------------------------------------+----------------+------------+-----------+------------+--------------+--------------------+---------------------+------------+--------------+---------------
 dddddddd-0000-0000-0000-000000000a01 | 2026/06/24-901 | 2026-06-24 | COMPLETED |            | QA-GATE-A    | 대구공조(검수완료) | 2026-06-24 09:00:00 | dev_master |            1 |    2400000.00
 dddddddd-0000-0000-0000-000000000b02 | 2026/06/24-902 | 2026-06-24 | COMPLETED |            | QA-GATE-B    | 부산냉동(미검수)   | 2026-06-24 09:05:00 | dev_master |            0 |             0
(2 rows)
```

판정: 새로 삽입하지 않은 기존 `COMPLETED` OUTBOUND 2건이 있다. 두 행 모두 구 규약인 `partner_code` snapshot은 있지만 새 조회 키 `partner_id`는 NULL이다. 금액 영향은 활성 line 1개·**2,400,000원**이다.

### ④-2 구 코드 조회와 새 UUID 조회의 값 비교

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d slip_db -P pager=off -c "WITH eligible AS (SELECT s.id,s.partner_id,s.partner_code,COUNT(sl.id) AS line_rows,COALESCE(SUM(CASE WHEN sl.supply_amount IS NOT NULL AND sl.vat_amount IS NOT NULL THEN sl.supply_amount+sl.vat_amount WHEN sl.unit_price_with_vat IS NOT NULL THEN sl.unit_price_with_vat*sl.quantity WHEN sl.line_total IS NULL THEN NULL ELSE sl.line_total+COALESCE(sl.vat_amount,0) END),0) AS amount FROM slips s LEFT JOIN slip_lines sl ON sl.slip_id=s.id AND sl.is_deleted=false WHERE s.is_deleted=false AND s.slip_type='OUTBOUND' AND s.status IN ('CONFIRMED','DELIVERED','COMPLETED','SHIPPING','INSPECTING') AND s.slip_date=DATE '2026-06-24' GROUP BY s.id,s.partner_id,s.partner_code) SELECT 'unfiltered_contract' AS mode,COUNT(*) AS slips,COALESCE(SUM(line_rows),0) AS lines,COALESCE(SUM(amount),0) AS amount FROM eligible UNION ALL SELECT 'legacy_partner_code_A',COUNT(*),COALESCE(SUM(line_rows),0),COALESCE(SUM(amount),0) FROM eligible WHERE partner_code='QA-GATE-A' UNION ALL SELECT 'new_partner_uuid_key',COUNT(*),COALESCE(SUM(line_rows),0),COALESCE(SUM(amount),0) FROM eligible WHERE partner_id IS NOT NULL;"
```

출력 원문:

```text
         mode          | slips | lines |   amount
-----------------------+-------+-------+------------
 unfiltered_contract   |     2 |     1 | 2400000.00
 legacy_partner_code_A |     1 |     1 | 2400000.00
 new_partner_uuid_key  |     0 |     0 |          0
(3 rows)
```

판정: 구 `partner_code`로는 `QA-GATE-A` 1건·1라인·2,400,000원이 조회되고, 무필터 slip 계약은 2건·1라인·2,400,000원을 조회한다. 하지만 R9 집계가 넘기는 새 UUID 키로 잡히는 기존 행은 **0건·0원**이다. 기존 행에서 같은 값이 나오지 않는다.

### ④-3 같은 날짜의 R9 무필터 집계 후보

실행 명령 원문:

```powershell
docker exec samhan-postgres psql -U samhan -d accounting_db -P pager=off -c "WITH candidates AS (SELECT jl.partner_id,SUM(CASE WHEN jl.account_code='401' THEN jl.credit_amount-jl.debit_amount ELSE 0 END) AS sales FROM journal_lines jl JOIN journals j ON j.id=jl.journal_id WHERE j.journal_date=DATE '2026-06-24' AND j.status IN ('POSTED','REVERSED') AND j.is_deleted=false AND jl.is_deleted=false AND jl.partner_id IS NOT NULL GROUP BY jl.partner_id) SELECT COUNT(*) AS aggregate_rows,COALESCE(SUM(sales),0) AS aggregate_sales FROM candidates;"
```

출력 원문:

```text
 aggregate_rows | aggregate_sales
----------------+-----------------
              0 |               0
(1 row)
```

판정: `SalesAggregateService`는 이 journals 결과로 `byPartner`를 먼저 만들고 그 key만 slip 조회하므로, 2026-06-24에는 slip client를 호출할 partner UUID 자체가 없다. 기존 slip 2건은 무필터 집계에서 전부 빠진다.

### ④-4 실 사용자 API 양쪽 관측

실행 명령 원문:

```powershell
$rawJson = curl.exe -sS -H "X-User-Id: a0000000-0000-0000-0000-000000000001" -H "X-User-Role: MASTER" -H "X-User-Login: dev_master" -H "X-Is-System-Master: true" "http://127.0.0.1:8086/slips?slipType=OUTBOUND&status=COMPLETED&from=2026-06-24&to=2026-06-24&size=50"; $resp = $rawJson | ConvertFrom-Json; [pscustomobject]@{TotalElements=$resp.data.totalElements;Returned=$resp.data.content.Count} | Format-List; $resp.data.content | Select-Object slipNo,slipDate,status,partnerCode,partnerName,totalAmount,totalQuantity | Format-Table -AutoSize

$resp = Invoke-RestMethod -Method Get -Uri 'http://127.0.0.1:8087/accounting/sales/aggregate?from=2026-06-24&to=2026-06-24' -Headers @{'X-User-Id'='a0000000-0000-0000-0000-000000000001';'X-User-Role'='MASTER';'X-User-Login'='dev_master';'X-Is-System-Master'='true'}; [pscustomobject]@{Success=$resp.success;RowCount=$resp.data.Count;SalesTotal=(($resp.data | Measure-Object -Property salesTotal -Sum).Sum)} | Format-List
```

출력 원문:

```text
TotalElements : 2
Returned      : 2

slipNo         slipDate   status    partnerCode partnerName       totalAmount totalQuantity
------         --------   ------    ----------- -----------       ----------- -------------
2026/06/24-902 2026-06-24 COMPLETED QA-GATE-B   부산냉동(미검수)            0             0
2026/06/24-901 2026-06-24 COMPLETED QA-GATE-A   대구공조(검수완료)  2400000.00             2

Success    : True
RowCount   : 0
SalesTotal :
```

판정: 실제 사용자 GET 경로에서 판매전표 목록은 2건을 반환하지만 같은 날짜의 거래처별 집계는 200 OK 빈 배열이다. 실행 accounting 컨테이너는 HEAD보다 stale이지만, 이 날짜는 journals 후보가 0이라 HEAD R9 코드도 `byPartner`가 비어 동일하게 빈 배열로 끝난다. 반면 HEAD slip repository의 무필터 원장 조건은 SQL로 2건·2,400,000원을 반환한다.

### ④ 결함 및 실 사용자 재현

**도달 가능한 결함 1건: R9 무필터 fix가 journal에 이미 등장한 partner UUID만 새 slip 원천으로 조회하므로, slip-only 및 `partner_id=NULL` 구 규약 판매전표가 거래처별 원장 집계에서 사라진다.**

- 영향: 기존 판매전표 **2건**, 활성 품목 **1라인**, 금액 **2,400,000원**. 같은 날짜 journals 후보 0건·0원이므로 집계 화면에서는 두 행 모두 보이지 않는다.
- 화면 재현:
  1. `판매 > 판매전표`(`/sales/slips`)에서 날짜 `2026-06-24`, 상태 `COMPLETED`를 조회한다.
  2. `2026/06/24-901`(QA-GATE-A)와 `2026/06/24-902`(QA-GATE-B) 2건이 보이는지 확인한다.
  3. `회계 > 거래처별 원장`(`/accounting/partner-ledger`)에서 시작·종료일을 모두 `2026-06-24`로 두고 거래처 필터를 비운 채 조회한다.
  4. 거래처별 집계가 0건이어서 두 판매전표를 원장 행으로 선택할 수 없다.
- API 재현:
  1. `GET /slips?slipType=OUTBOUND&status=COMPLETED&from=2026-06-24&to=2026-06-24&size=50` → 200, 2건.
  2. `GET /accounting/sales/aggregate?from=2026-06-24&to=2026-06-24` → 200, `data=[]`.
- 원인 위치: `SalesAggregateService.java:113-115`가 journals로 만든 `byPartner.keySet()`만 순회하고, `PartnerLedgerSalesClient.find(..., null, partnerId)`가 code snapshot fallback 없이 UUID만 보낸다. 기존 행의 `partner_id` 충족률은 0%다.

## 최종 판정

유일한 질문 **“실 사용자 경로로 재현 가능한 결함이 있는가.”**에 대한 답은 **예**다.

1. **실 사용자 도달 결함:** 무필터 집계가 journals 후보 UUID만 순회하여 slip-only/구 규약 `partner_id=NULL` 판매전표를 집계 목록에서 누락한다. 실 판매전표 GET 2건과 동일 날짜 집계 GET 0건으로 재현했다. 영향은 기존 전표 2건·활성 1라인·2,400,000원이다.
2. **유일한 예외인 증거 무결성 결함:** R9의 `19건·292,000,000원 → 19건·299,498,000원, 3건 변경`은 재현되지 않았다. 제품 조건으로 직접 재산출한 값은 `18건·264,000,000원 → 18건·264,000,000원, 0건 변경`이다. 19번째 28,000,000원은 제품에서 제외되는 DRAFT이고, 현재 slip DB에는 주장한 2~3월 slip 3건·58,498,000원이 없다.

하위 각도별 결론:

- ① R9 기간의 “slip 없는 journal 매출”은 18건·264,000,000원이지만 빈 slip 응답이면 journal 값이 유지되어 실제 차단은 0건·0원이다. 별도로 전체 기존 DB의 slip-only 구 규약 2건·2,400,000원은 journal 후보가 없어 누락되며 ④ 결함으로 확정했다.
- ② R9 기간 journals 후보 18개의 partner master UUID/code/bizNo는 모두 100%다. 반면 현재 원장 상태 기존 slip 2건의 `partner_id`는 0%, `partner_code`는 100%, 그 코드의 partner master 대응은 0%다.
- ③ 주장 수치 불재현. 실제 18건·264,000,000원, 변경 0건.
- ④ 기존 `COMPLETED` 구 규약 행으로 조회 시 code/무필터 slip 원천은 2건·2,400,000원이지만 새 UUID 키는 0건·0원이고 집계 API도 0건이다. 기존 행 호환 실패다.

## 이번 라운드가 보지 않은 표면

- Docker 빌드·배포 금지 때문에 HEAD `f944aeb7a`의 accounting↔slip 서비스 간 실제 HTTP 통합 호출은 실행하지 않았다. 현재 slip 컨테이너에는 HEAD의 내부 원장 endpoint가 없어 stale 런타임임을 확인했고, 이를 HEAD 성공/실패 증거로 사용하지 않았다.
- 브라우저 GUI 클릭과 화면 캡처는 실행하지 않았다. 대신 동일 화면이 소비하는 실제 GET 두 경로와 실 DB 원문으로 도달성을 확인했다.
- R9 fix가 건드리지 않은 수금·채권 계산, 선택 거래처 상세 원장 로직, 다른 회계 화면과 API는 판정하지 않았다.
- 현재 `slip_db`에 없는 R9 당시 주장 2~3월 slip 3건의 이후 삭제·초기화 경위는 조사하지 않았다. 현재 실 DB에서 재현되지 않는다는 사실까지만 판정했다.
- 테스트 강도, 문서 과장 일반론, 가드/커버리지 등 사용자가 제외한 검증 품질 표면은 찾거나 보고하지 않았다.

소스 수정, 데이터 mutation, `git` 명령, Docker 빌드·배포는 수행하지 않았다. 지정 보고서 파일 작성만 수행했다.
