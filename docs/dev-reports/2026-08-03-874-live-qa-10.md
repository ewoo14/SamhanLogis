# PR #1057 라이브 QA 10차 — 실행 보고서

- 표식: `QA-874-R10-`
- 실행일: 2026-08-03 (KST)
- 기준 커밋: `67efda908` 확인 예정 (`git pull` 완료, already up to date)
- 원칙: 기존 실 전표·마스터·설정 보존, DB 직접 INSERT/UPDATE 금지, 화면 조작만 수행
- 캡처 경로: `docs/qa/874-riusage-global-dc-real-qa/r10-real-qa/`

## 실행 로그

### 시작

- `git pull`: 완료 — Already up to date.
- 작업 트리의 기존 미추적 `clients/desktop/playwright/874-riusage-real-qa.spec.ts`는 수정하지 않음.

## ① 결재선 조회

조회 시각: 2026-08-03 14:51~14:52 KST, gateway `http://127.0.0.1:8080`, 인증 `dev_master`의 read-only bearer 요청.

조회 API:

- `GET /auth/approval-line-configs/SLIP_OUTBOUND/structure`
- `GET /auth/approval-line-configs/SLIP_OUTBOUND/default-approvers`
- `GET /auth/admin/approval-line-configs?documentType=SLIP_OUTBOUND` (MASTER VIEW read-only)
- 계정명 확인: `GET /api/v1/admin/users?q=김기철`, `GET /api/v1/admin/users?q=김은지` (MASTER VIEW read-only)

실제 결재선:

| sequence | 단계 | actionKey | stepType | 결재자 유형 | 결재자 |
|---:|---|---|---|---|---|
| 0 | 작성자 | — | CREATOR | — | 작성자 |
| 1 | 검수자 | `OUTBOUND_INSPECT` | GROUP | USER | 김기철 (`kimgicheol`) |
| 1 | 검수자 | `OUTBOUND_INSPECT` | GROUP | USER | 김은지 (`kimeunji`) |
| 2 | 출고담당 | `OUTBOUND_DISPATCH` | GROUP | — | 지정 결재자 없음 |

결론: `SLIP_OUTBOUND × OUTBOUND_INSPECT`에는 그룹 결재자가 없고, 개인 결재자 2명(김기철·김은지)만 실제 설정되어 있다. 이는 추측이 아닌 위 API 응답 및 계정 검색 결과에 근거한다.

## ② 출고 lifecycle

대상: `2026/08/03 - 6` (`INSPECTING`, 화면 라벨 `검수 중`, 검수자/서명 미기록 상태 확인).

| 단계 | 계정 | 화면 조작 | 결과 |
|---|---|---|---|
| `INSPECTING → COMPLETED` | `kimgicheol` (김기철, SALES) | 판매전표 목록 → 대상 전표 → `완료 (처리 완료)` 버튼 확인 | **차단**. 버튼 disabled, title=`다음 단계: 처리 완료`. 클릭 불가 |
| `INSPECTING → COMPLETED` | `kimeunji` (김은지, ACCOUNTANT) | 실 로그인 후 판매전표 목록/대상 상세 진입 | **차단**. 목록에서 대상 전표 행 접근 불가. 대상 상세를 화면 URL로 열면 `전표를 불러오지 못했습니다.` |
| `COMPLETED → SHIPPING` | — | — | 미수행: 검수 단계 차단 |
| `SHIPPING → DELIVERED` | — | — | 미수행: 검수 단계 차단 |
| `DELIVERED → CONFIRMED` | — | — | 미수행: 검수 단계 차단 |

결함 판정: 결재선에 실제 등록된 두 계정 어느 쪽도 화면에서 출고 검수를 수행할 수 없다. `dev_manager`의 9차 원문은 `출고 검수 권한이 없습니다 — 검수자 결재자(그룹/개인)만 처리할 수 있습니다`였고, 이번에는 김기철의 UI disabled 및 김은지의 상세 조회 실패로 재현되었다. DB/API 직접 우회, 결재선 변경, 전표 변경은 수행하지 않았다.

캡처:

- `docs/qa/874-riusage-global-dc-real-qa/r10-real-qa/QA-874-R10-01-kimgicheol-sales-list.png`
- `docs/qa/874-riusage-global-dc-real-qa/r10-real-qa/QA-874-R10-02-kimgicheol-detail-before.png`
- `docs/qa/874-riusage-global-dc-real-qa/r10-real-qa/QA-874-R10-04-kimeunji-direct-detail.png`

## ③ 회계 매출전표 배분·전기 / POSTED 모델축 행 수

미수행. 출고 lifecycle이 `INSPECTING → COMPLETED`에서 차단되어 회계 매출전표 배분·전기를 하지 않았다.

## ④ 시나리오 2~5

미수행. 전표가 `CONFIRMED`에 도달하지 않아 시나리오 2~5의 대상 집계행을 준비할 수 없었다.

## 결함

1. **출고 검수 실행 불가** — `SLIP_OUTBOUND × OUTBOUND_INSPECT`의 실제 개인 결재자 김기철·김은지 모두 화면에서 진행 불가.
   - 김기철: `완료 (처리 완료)` 버튼 disabled.
   - 김은지: 판매전표 목록/상세 접근 실패.
   - 기존 9차 원문: `출고 검수 권한이 없습니다 — 검수자 결재자(그룹/개인)만 처리할 수 있습니다`.
2. 이 결함 때문에 이후 회계 및 시나리오 2~5를 실행하지 않음.

## 새 파일

- `docs/dev-reports/2026-08-03-874-live-qa-10.md`
- `docs/qa/874-riusage-global-dc-real-qa/r10-real-qa/QA-874-R10-01-kimgicheol-sales-list.png`
- `docs/qa/874-riusage-global-dc-real-qa/r10-real-qa/QA-874-R10-02-kimgicheol-detail-before.png`
- `docs/qa/874-riusage-global-dc-real-qa/r10-real-qa/QA-874-R10-04-kimeunji-direct-detail.png`
