# PR #1078 · 이슈 #1075 — S14 fix 보고

## 판정

S13 지시서의 자동 규격 잔존 결함을 수정했다. 품목 해제는 자동 반영 규격만 대상 라인에서 회수하고, 사용자가 직접 입력한 규격·단가와 다른 라인은 보존한다.

## RED 실패 원문

수정 전 RED 실행:

```text
Test Files  1 failed (1)
Tests       1 failed | 39 passed (40)
S14 RED-A: 자동 반영 규격은 품목 해제 시 로컬·coedit에서 함께 회수한다
AssertionError: expected '4HP' to be '' // Object.is equality
```

실제 Chromium RED에서도 편집 hydrate 라인의 규격이 재현됐다.

```text
Locator: getByRole('textbox', { name: '라인 1 규격' })
Expected: ''
Received: '4HP'
```

## 변경 파일: 줄

- `clients/desktop/src/renderer/routes/EstimateFormPage.tsx:98` — `specificationSource` provenance 추가.
- `clients/desktop/src/renderer/routes/EstimateFormPage.tsx:272` — 상세 hydrate 규격을 catalog snapshot으로 초기화.
- `clients/desktop/src/renderer/routes/EstimateFormPage.tsx:365-381` — coedit 규격 변경을 원격 직접 편집으로 분류.
- `clients/desktop/src/renderer/routes/EstimateFormPage.tsx:1114-1118` — 사용자 규격 입력을 `USER`로 승격.
- `clients/desktop/src/renderer/routes/EstimateFormPage.tsx:1435-1450` — lookup 규격의 `CATALOG`/`USER` 보존 정책 반영.
- `clients/desktop/src/renderer/routes/EstimateFormPage.tsx:1514-1538` — 품목 해제 시 자동 규격과 coedit 규격만 회수.
- `clients/desktop/src/renderer/routes/EstimateFormPage.coedit.test.tsx:443-522` — S14 RED-A/RED-B 양방향 회귀 추가.
- `clients/desktop/playwright/1062-line-input-ux/1062-line-input-ux.spec.ts:110-148` — 규격·자동단가 삭제 및 2행 로컬/coedit 격리 회귀 보강.

## 게이트 결과 원문

```text
cd clients/web/design-system && npm run build
✓ built in 4.58s

npx vitest run src/components/AsyncAutocomplete/AsyncAutocomplete.test.tsx src/components/ProductAutocomplete/ProductAutocomplete.test.tsx
Test Files  2 passed (2)
Tests       40 passed (40)

cd clients/desktop
npx vitest run src/renderer/routes/EstimateFormPage.coedit.test.tsx src/renderer/routes/SlipFormPage.test.tsx
Test Files  2 passed (2)
Tests       100 passed (100)

$env:CI='1'; npx playwright test playwright/1062-line-input-ux/1062-line-input-ux.spec.ts playwright/bundle-set-options/bundle-set-options.spec.ts --reporter=line
Running 17 tests using 2 workers
17 passed (14.9s)

cd ../.. && git diff --check
Exit code: 0
```

Playwright는 5173 포트에 기존 listener가 없어 webServer 정상 기동 경로로 실행했다. `PLAYWRIGHT_SKIP_WEB_SERVER=1` 재사용은 사용하지 않았다.

## 신규 파일 목록

- `docs/dev-reports/2026-08-06-1075-s14-fix.md` (본 보고서)

작업 시작 전부터 존재한 `docs/dev-reports/2026-08-06-1075-s13-fix-directive.md`는 SOL 지시서로 읽기만 했고 생성·수정하지 않았다.

커밋은 가드레일에 따라 수행하지 않았다.
