# 1017 VAT 재계산 fix2 검증 보고서

## 1. RED — 계약 위반 재현

`VatOverchargeCorrectionMigrationSqlTest.v61_stores_line_total_as_supply_amount_after_correction`를 먼저 추가하고 구현 변경 전 실행했다.

실행:

```text
./gradlew :services:slip-service:test --tests com.samhanair.logis.slip.domain.vat.VatOverchargeCorrectionMigrationSqlTest
```

실패 원문:

```text
VatOverchargeCorrectionMigrationSqlTest > v61_stores_line_total_as_supply_amount_after_correction() FAILED
    java.lang.AssertionError at VatOverchargeCorrectionMigrationSqlTest.java:48
18 actionable tasks: 2 executed, 16 up-to-date

2 tests completed, 1 failed

> Task :services:slip-service:test FAILED
```

실패는 정정 후 `line_total = supply_amount` 계약을 V61 SQL이 보장하지 않는다는 뜻이며, 이제 SQL만 최소 수정한다.

## 2. GREEN — 최소 수정 및 집중 테스트

V61의 OLD 매칭(`old_line_total`)은 변경하지 않았다. 정정 target의 `new_line_total`을 `new_supply`로 정규화하고, NEW 판정·감사 after JSON·실제 UPDATE에서 모두 `line_total = new_supply`를 사용하도록 수정했다.

실행 결과 원문:

```text
> Task :services:slip-service:test

BUILD SUCCESSFUL in 4s
18 actionable tasks: 2 executed, 16 up-to-date

## 3. 일회성 PostgreSQL 적용 — B/C 및 두 번째 실행

공유 스택과 분리한 `codex-1017-fix2` 일회성 PostgreSQL 16 컨테이너를 사용했다. fixture는 19행이며, Flyway와 같이 V61을 단일 트랜잭션(`psql -1`)으로 적용했다.

적용 및 B/C 확인 원문:

```text
INSERT 0 19
UPDATE 19
UPDATE 19
UPDATE 19
SELECT 19
DO
INSERT 0 19
UPDATE 19
 corrected_rows | b_line_total_is_supply | c_supply_plus_vat_preserved
----------------+------------------------+-----------------------------
             19 |                     19 |                          19
(1 row)
```

B는 after 감사값의 `line_total = supply_amount`, C는 after 감사값의 `supply_amount + vat_amount = before line_total`로 각각 19/19다.

두 번째 실행 판정 원문:

```text
 second_run_old_state_rows
---------------------------
                         0
(1 row)

 second_run_new_state_rows
---------------------------
                        19
(1 row)
```

두 번째 실행에서 OLD 매칭은 0행이고, 19행 모두 새 계약의 NEW 상태다.

## 4. 공유 실DB 읽기 전용 매칭 재확인

공유 `samhan-postgres`는 재빌드·재기동하지 않았고, `slip_db`에 읽기 전용 트랜잭션으로 정확한 업무키·금액·품목명을 대조했다. UUID와 전표 식별자 외 값은 보고서에 쓰지 않았다.

실행 원문:

```text
BEGIN
SET
 matched_rows | product_name_matches | old_line_total_is_supply
--------------+----------------------+--------------------------
           19 |                   19 |                       19
(1 row)

ROLLBACK
```

실DB 매칭은 여전히 정확히 19행이고 품목명 불일치는 0행이다. `old_line_total`은 현재 실DB 값과 동일하다.

6개 시나리오 fixture 준비 1차 실행은 PostgreSQL `DROP DATABASE` 여러 문장을 한 `-c` 트랜잭션으로 보내는 실행 오류로 중단됐다. 공유 DB에는 영향이 없고, 일회성 컨테이너의 `slip_db` OLD fixture 복원만 완료된 상태다. 각 database 생성과 적용은 개별 명령으로 재실행한다.

6개 시나리오 재실행 원문:

```text
DATABASE exactdb
INSERT 0 19
UPDATE 19
SELECT 19

DATABASE partialdb
SELECT 19
INSERT 0 9
UPDATE 9

DATABASE deleteddb
SELECT 18
INSERT 0 18
UPDATE 18

DATABASE changeddb
SELECT 18
INSERT 0 18
UPDATE 18

DATABASE duplicatedb
SELECT 20
ERROR:  VAT correction target is ambiguous

DATABASE substitutiondb
SELECT 18
INSERT 0 18
UPDATE 18
```

정리하면 exact는 19행, partial은 NEW 10행을 건너뛰고 OLD 9행, deleted/changed는 18행, substitution은 18행을 정정했다. duplicated는 중복 target을 안전하게 감지해 `VAT correction target is ambiguous`로 롤백했다. 네 정상 적용 DB의 최종 `line_total = supply_amount` 행 수는 각각 19/19다.

## 6. slip-service 전체 테스트

실행을 시작했으나 120초 실행 제한에 도달해 종료됐고, 이 실행에서는 Gradle 출력이 flush되지 않아 pass/fail 집계 원문을 얻지 못했다.

```text
command timed out after 124031 milliseconds
```

재시도는 `--console=plain` 및 600초 제한으로 완료했다. 최종 원문:

```text
> Task :services:slip-service:test

BUILD SUCCESSFUL in 4m 12s
18 actionable tasks: 1 executed, 17 up-to-date
```

테스트 결과 XML 집계 원문:

```text
test_xml_total=1532 failures=0 errors=0 skipped=0
```

일회성 컨테이너 `codex-1017-fix2`는 검증 후 제거했다.
