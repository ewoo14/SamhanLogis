연관 Issue: #1095

## 트랙 개설 — 착수 전 확인

2026-08-06 등록. 개발책임자 지시.

### 요구
- **단종·미판매** → 미표시
- **품절** → 표시하되 **수량 입력 차단**

### 정찰이 실측할 것
1. 시트의 해당 표기가 **실제로 어떤 값**인지 — 레거시 원문에서 확인 (🚨 `#1114`·`#896` 처럼 표기 축은 코드 주석이 아니라 **레거시 원문**이 정본입니다)
2. `products` 에 상태 축이 있는가 · 없으면 무엇을 새로 두어야 하는가
3. 🚨 **실 데이터로 세십시오** — 지금 시트에 단종·미판매·품절이 각각 몇 건인가. **0건이면 "결함 없음" 이 아니라 "판정 불가"** 입니다
4. "미표시" 의 범위 — 견적·주문·전표 어디까지인가. 이미 저장된 문서의 그 품목은 어떻게 되는가

### 🚨 양방향 RED 를 미리 겁니다
- A 단종·미판매가 **안 보인다** · 품절은 **보이되 수량 입력이 막힌다**
- B 🚨 **정상 품목이 사라지지 않는다** — 실 데이터로 몇 건이 영향받는지 세십시오. 이 저장소에서 필터가 정상 데이터를 지운 사고가 반복됐습니다
- C 이미 저장된 문서의 값이 **소급으로 바뀌지 않는다**