# PR #1179 (#1094) fix2 — 재수렴 ② 후 3건 수정

- 일시: 2026-08-12 (Asia/Seoul)
- 브랜치/HEAD: `feat/1094-docno-hyperlink-and-back` / `9ab040a87` 기준 작업
- 범위: 견적·주문 문서번호 링크 전파, 견적·주문 검색/필터 복귀, 입금보고서 스크롤 복귀
- git 명령: 미사용
- 공유 Docker 스택: 미사용

## RED-first 원문

재수렴 근거는 `docs/dev-reports/2026-08-12-1094-reconvergence2-sol.md`를 먼저 읽고 작성했다.

최초 RED 실행 원문 중 결함을 값으로 잡은 부분:

```text
❯ src/renderer/routes/CashReceiptListPage.test.tsx (6 tests | 1 failed)
× 복귀 URL이 같은 입금보고서라도 스크롤 identity를 값으로 복원한다
  → expected +0 to be 640

❯ src/renderer/routes/EstimateListPage.test.tsx (13 tests | 1 failed)
× 복귀 URL의 견적 검색·필터 identity를 값으로 복원한다
  → expected element value 삼성전자, received empty

❯ src/renderer/routes/SalesPartnerOrderListPage.test.tsx (12 tests | 1 failed)
× 복귀 URL의 주문 검색·필터 identity를 값으로 복원한다
  → expected element value 2026/06/08, received empty
```

① 전파 결함은 문서번호 링크가 행 클릭과 중복 처리되는 구조를 실패 대상으로 고정했다. 링크에 `event.stopPropagation()`이 없으면 한 클릭이 상세 history entry를 두 번 만들며, 견적·주문 각각 해당 계약이 RED였다.

## 원인과 수정

1. `EstimateListPage.tsx`, `SalesPartnerOrderListPage.tsx`
   - 문서번호 `Link` 클릭에서 `event.stopPropagation()`을 호출해 `onRowClick` 중복 navigate를 차단했다.
   - 견적의 `status/startDate/endDate/partner/includeDeleted/page`, 주문의 `dateFrom/dateTo/partnerId/status/slipPublishStatus/keyword/includeDeleted/page`를 URL query 정본으로 초기화·동기화했다.
   - 편집 경로가 유지해야 하는 목록 URL을 상세·폼 복귀 state로 전달했다.

2. `EstimateDetailPage.tsx`, `EstimateFormPage.tsx`
   - 견적 상세 → 편집 → 저장 → 상세 전환에서 `returnTo`와 `returnEntryKey`를 보존했다.

3. `CashReceiptListPage.tsx`
   - 저장된 scroll anchor를 첫 `requestAnimationFrame`뿐 아니라 100ms 지연 재복원에서도 적용했다.
   - cleanup에서 두 타이머/프레임을 모두 취소한다.

## 검증 원문 및 집계

변경 모듈 focused 실행:

```text
Test Files  5 passed
Tests       57 passed
```

전체 desktop `npm test` 최종 원문:

```text
Test Files 258 passed (258)
Tests      2244 passed | 2 skipped (2246)
```

분리 집계:

| 범위 | passed | skipped | failed |
|---|---:|---:|---:|
| 변경 모듈 focused | 57 | 0 | 0 |
| desktop 전체 | 2244 | 2 | 0 |

desktop typecheck 원문:

```text
> @samhan/desktop@0.1.0 typecheck
> ... tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.web.json --noEmit ...
Exit code: 0
```

typecheck 내부 real-QA 보조 테스트:

```text
ℹ tests 2
ℹ pass 2
ℹ fail 0
```

추적 집합에는 기존 미추적 로컬 스펙 1건 경고가 있었고, `REAL_QA_ALLOW_UNTRACKED=1` 로컬 허용 모드 검증은 다음과 같이 통과했다.

```text
ℹ tests 51
ℹ pass 51
ℹ fail 0
```

## 종료 정리

```text
TMP_EXISTS=False
RECON1094_REMAINING=NONE
```

`.codex-tmp`의 빈 잔여 디렉터리를 제거했다. `recon1094-*` 작업 디렉터리 잔재는 없었다. 이번 라운드에는 삭제 명령으로 추적 파일을 제거하지 않았다. 사용자 지시의 git 명령 금지를 지켜 Git 기반 추적 파일 비교 명령은 실행하지 않았으므로, Git 인덱스 대조를 통한 별도 확인은 수행하지 못했다.

