# PR #1106 / 이슈 #1074 S3 최종 라이브QA

## 환경 확인

- 검증 시각: 2026-08-08 05:40~05:55 KST
- 작업 트리: `C:\dev\Samhan-Public\.claude\worktrees\t1074`
- 브랜치/HEAD: `fix/1074-outbound-cutoff-next-day` / `b7ac5d6f84a9df3af747bb65282f08f9dc337feb`
- 배포본: `samhan-slip-service`가 `127.0.0.1:18086 -> 8086/tcp`에서 healthy. 검증 시작과 본 QA 프로세스 회수 직후 전체 컨테이너는 24개 running이었다. 최종 파일 검증 중 본 라운드와 무관한 동시 Gradle IT가 Testcontainers PostgreSQL·Ryuk 2개를 추가해 현재는 26개다. `samhan-nginx` unhealthy는 선재 상태이며 스택을 재기동하지 않았다.
- 네트워크 확인:
  - `GET http://127.0.0.1:8080/actuator/health` -> 200 `UP`
  - `GET http://127.0.0.1:18086/actuator/health` -> 200 `UP`
  - 인증 없이 gateway의 slip 경로 호출 -> 401. 즉 gateway 인증 경계와 실제 slip 라우팅을 확인했다.
  - 서버 우회 검증은 `POST http://127.0.0.1:8080/api/v1/slips`, 화면 저장은 renderer가 실제 호출하는 `POST http://localhost:8080/slips`를 관측했다.
- 실 GUI: `clients/desktop`을 cwd로 두고 branch renderer를 `http://127.0.0.1:5175`에 기동했다. Playwright Chromium headless, viewport 1440x900, 브라우저 타임존은 의도적으로 `UTC`로 두었고 mock은 사용하지 않았다.
- 계정/권한: 실 로그인 역할 `MANAGER` 풀네임. 자격증명과 JWT는 런타임에서만 사용했고 산출물에 기록하지 않았다.
- DB: `slip_db`에 `SELECT`만 실행했다. DB 직접 INSERT/UPDATE/DELETE, cutoff 행 변경, QA 전표 삭제는 하지 않았다.

## 결론

**결함 0건이다. PR #1106은 최종 라이브QA PASS다.**

- P1 PASS: 화면을 우회한 과거 출고일 신규 생성이 서버에서 HTTP 409로 차단됐고 신규 과거 행은 0건이다.
- 기존 과거 전표 수정 PASS: `2026/08/07-42`의 실 화면 no-op 저장 PUT은 HTTP 200이다.
- 결함 2 PASS: 사용자가 고친 N은 M 변경 후 보존된다. 새 M과 맞지 않으면 오류와 저장 차단이 나타나며 조용히 재계산되지 않는다. 맞는 N으로 고치면 저장 가능하고 실제 전표가 생성된다.
- 결함 3 PASS: UTC 브라우저에서도 KST 기본일과 익일이 유지됐다. 화면 M, POST payload, DB `slip_date`가 모두 `2026-08-09`로 일치해 하루 밀림이 없다.
- A/B/C/D PASS: A 201, B 4/4 201·오차단 0건, C 409, D 활성 6개 전수 확인.
- 화면 UUID 0건, 금지 용어 `슬립` 0건, 역할 `MANAGER` 풀네임을 확인했다.

## 발화 조건 선계수

시나리오를 실행하기 전에 KST 시각과 실 DB 기준으로 먼저 셌다.

| 조건 | 선계수 | 판정 |
|---|---:|---|
| 활성 cutoff 행 | 6 | D 전수 판정 가능 |
| 현재 시각보다 cutoff가 지난 태그 | 2 (`DAY`, `LOGEN`) | A/C 라이브 판정 가능 |
| 현재 시각보다 cutoff가 남은 태그 | 4 (`REGION`, `STACK`, `GYEONGDONG_PARCEL`, `GYEONGDONG_FREIGHT`) | B 라이브 판정 가능 |
| 실행 전 당일 OUTBOUND 저장 행 | 8 | 이번 라운드 생성분을 이후 시각으로 분리 가능 |
| 과거 OUTBOUND DRAFT/SAVED 수정 후보 | 212 | 기존 과거 전표 수정 판정 가능 |
| 실행 전 이번 라운드 신규 행 | 0 | 라운드 부작용 분리 가능 |

당시 KST는 05:41이었고 phase는 DAY·LOGEN `AFTER`, 나머지 4태그 `BEFORE`였다. 발화 조건 0인 반대 phase는 라이브 PASS로 세지 않고 fixed-clock 테스트로 보완했다.

## P1 — 과거 신규 서버 차단과 기존 과거 수정

### 화면 우회 과거 신규 생성

- 요청: 실 JWT로 `POST /api/v1/slips`, OUTBOUND, M=`2026-08-07`, 태그 `LOGEN`.
- 화면의 `min` 속성이나 저장 버튼을 전혀 사용하지 않은 직접 API 호출이다.
- 응답: HTTP **409**, code `CONFLICT`, `과거 출고일 신규 전표는 생성할 수 없습니다`.
- 사후 SELECT: 기준시각 이후 `slip_date < 2026-08-08` 신규 행 **0건**.

서버 가드가 실제 배포본에서 동작하며, UI 제약만 확인한 false-green이 아니다.

### 기존 과거 전표 수정

- 발화 후보: 과거 OUTBOUND DRAFT/SAVED 212건.
- 직전 라운드 QA 전표 `2026/08/07-42` 상세 화면에서 직접 수정 UI를 열고 값 변경 없이 저장했다.
- 실제 `PUT /slips/{id}/sales` 응답: HTTP **200**.
- 신규 생성 전용 과거일 가드가 기존 과거 전표 수정까지 오차단하지 않았다.
- 증거: `07-past-existing-edit-open.png`, `08-past-existing-edit-200.png`.

## 결함 2 — 수동 N 보존·불일치 차단·복구 가능성

실 GUI에서 REGION으로 검증했다. 저장 전제인 출고 창고와 실 품목을 먼저 선택했다.

| 단계 | M | N | 오류 | 저장 |
|---|---|---|---|---|
| 자동 계산 | `2026-08-08` | `2026-08-10` | 없음 | 가능 |
| 사용자가 N 수정 후 M 변경 | `2026-08-09` | `2026-08-14` 그대로 | `출고일(M)과 하차일(N)을 확인하세요...` | 비활성, 클릭 차단, POST 0건 |
| 사용자가 새 M에 맞게 N 수정 | `2026-08-09` | `2026-08-10` | 0건 | 활성 |

- 수동 N은 M 변경 뒤에도 `2026-08-14`로 보존됐다. 조용한 덮어쓰기는 없었다.
- 불일치 상태에서 저장 버튼은 disabled였고 실제 클릭 시도도 차단됐다.
- N을 `2026-08-10`으로 고치면 오류가 사라지고 저장 가능해졌다.
- 실제 생성 결과: `2026/08/09-3`, REGION, M=`2026-08-09`, N=`2026-08-10`, DRAFT.
- 따라서 어떤 N을 넣어도 저장할 수 없는 막다른 조합은 아니다.
- 증거: `02-n-manual-preserved-mismatch-blocked.png`, `03-n-corrected-save-enabled.png`.

## 결함 3 — KST 날짜 왕복

- 브라우저 실행 타임존을 `UTC`로 강제했다.
- 신규 화면 기본 M과 `min`: 둘 다 `2026-08-08` KST.
- DAY 익일 생성 화면 M: `2026-08-09`.
- 실제 POST payload `slipDate`: `2026-08-09`.
- 응답: HTTP **201**.
- DB: `2026/08/09-4`, DAY, `slip_date=2026-08-09`.

화면값 -> 네트워크 payload -> DB 저장일까지 동일해 날짜가 하루 밀리지 않았다. 증거는 `01-kst-default-date-utc-browser.png`, `05-a-day-next-day-before-save.png`, `06-a-day-next-day-created.png`다.

## A/B/C/D 회귀

### A — 마감 후 익일 출고

- 발화 조건: 마감 후 태그 2개.
- 실 GUI: DAY, M=`2026-08-09`.
- 결과: HTTP **201**, `2026/08/09-4` 생성.
- 날짜 왕복도 위 결함 3 절과 같이 확인했다.

### B — 마감 전 정상 생성 오차단

- 발화 조건: 마감 전 태그 4개.
- 실 API 정상 경로: REGION, STACK, GYEONGDONG_PARCEL, GYEONGDONG_FREIGHT 각 1회.
- 결과: HTTP 201 **4/4**, 오차단 **0건**.
- DB 저장: `2026/08/08-9`~`2026/08/08-12`, 태그별 1건.
- 기준시각 이후 당일 B 대상 저장 행: 정확히 **4건**. DAY 저장 행은 0건.
- 증거: `09-b-four-before-cutoff-created.png`.

이번 서버 가드의 최대 위험이었던 정상 당일 생성 오차단은 실데이터에서 발생하지 않았다.

### C — 마감 후 당일 출고 차단 유지

- 발화 조건: 마감 후 태그 2개.
- DAY, M=`2026-08-08`을 직접 API와 실 GUI에서 각각 시도했다.
- 두 경로 모두 HTTP **409**, `익일 출고로 생성하세요` 안내.
- 이번 라운드 당일 DAY 저장 행: **0건**.
- 증거: `10-c-day-same-day-409.png`.

`당일 당일 마감` 중복 문구는 직전 라운드에도 존재한 선재 표현이며 이번 fix의 신규 회귀로 세지 않았다.

### D — 활성 태그 6개 전수

| 태그 | cutoff | 현재 phase | 라이브 결과 | 반대 phase 보완 |
|---|---:|---|---|---|
| DAY | 00:01 | AFTER | 당일 409, 익일 201 | fixed-clock PASS |
| LOGEN | 00:01 | AFTER | 활성/API·DB 확인 | fixed-clock PASS |
| REGION | 12:00 | BEFORE | 당일 201 | fixed-clock PASS |
| STACK | 14:00 | BEFORE | 당일 201 | fixed-clock PASS |
| GYEONGDONG_PARCEL | 15:00 | BEFORE | 당일 201 | fixed-clock PASS |
| GYEONGDONG_FREIGHT | 15:00 | BEFORE | 당일 201 | fixed-clock PASS |

- DB `active=true`, `is_deleted=false`: 6건.
- `GET /admin/slip-cutoffs`: 6건.
- 실 설정 화면: 6행 모두 활성.
- `OutboundCutoffGuardTest --rerun-tasks`: BUILD SUCCESSFUL, 18 tasks executed.
- 증거: `00-environment-6-cutoffs-manager.png`.

## 화면 UUID·용어·권한

- 10개 캡처 시점 body text 검사: UUID **0건**.
- 사용자 화면 금지 용어 `슬립`: **0건**.
- 헤더 역할: `MANAGER` 풀네임.
- 내부 path parameter와 JWT는 화면·스크린샷·보고서에 실제 값으로 노출하지 않았다.

상단의 기존 `업데이트 실패` 배너는 모든 화면에서 재현된 선재 환경 상태이며 본 PR 결함으로 판정하지 않았다.

## 자동 보완 검증

| 검증 | 결과 |
|---|---|
| `:services:slip-service:test --tests "*OutboundCutoffGuardTest" --rerun-tasks` | BUILD SUCCESSFUL, 18 tasks executed |
| `npm run test -- SlipFormPage.test.tsx deliverySchedule.test.ts` | 2 files, **135/135 PASS** |

## 라이브QA 데이터 부작용

DB 직접 DML 없이 실 API 경로로 DRAFT 6건을 생성했다.

- N 복구 가능성: `2026/08/09-3` REGION.
- A/KST 익일: `2026/08/09-4` DAY.
- B 정상 당일: `2026/08/08-9`~`2026/08/08-12` 4건.
- C와 과거 신규 차단 시도는 저장 행 0건.
- 직전 라운드 QA 전표 12건은 삭제하지 않았다. `2026/08/07-42`는 과거 수정 200 확인을 위해 화면에서 no-op 저장만 수행했다.

## 프로세스 회수와 신규 파일

- 이 라운드가 띄운 Vite `node.exe`/부모 `cmd.exe`를 실행파일 경로와 t1074 커맨드라인으로 식별해 종료했다.
- 본 QA 회수 직후 포트 5175 listener 0, 이 라운드의 Node/Chromium/cmd 잔존 0을 확인했다.
- 그 뒤 최종 파일 검증 시 다른 동시 작업이 같은 t1074에서 `PublicSlipAttachmentControllerIT`를 실행해 별도 Gradle `cmd.exe`와 Testcontainers 2개가 새로 나타났다. 본 라운드가 띄운 프로세스가 아니므로 종료하지 않았다.
- Docker는 본 QA 종료 시점까지 24개 running을 유지했다. 이후 위 동시 IT 때문에 현재 26개이며, `samhan-slip-service`는 계속 healthy다. 스택은 재기동하지 않았다.
- 커밋·push·코드 수정은 하지 않았다.

신규 파일:

- `docs/dev-reports/2026-08-08-1074-s3-final-live-qa.md`
- `docs/qa-shots/1074-s3-live-qa/00-environment-6-cutoffs-manager.png`
- `docs/qa-shots/1074-s3-live-qa/01-kst-default-date-utc-browser.png`
- `docs/qa-shots/1074-s3-live-qa/02-n-manual-preserved-mismatch-blocked.png`
- `docs/qa-shots/1074-s3-live-qa/03-n-corrected-save-enabled.png`
- `docs/qa-shots/1074-s3-live-qa/05-a-day-next-day-before-save.png`
- `docs/qa-shots/1074-s3-live-qa/06-a-day-next-day-created.png`
- `docs/qa-shots/1074-s3-live-qa/07-past-existing-edit-open.png`
- `docs/qa-shots/1074-s3-live-qa/08-past-existing-edit-200.png`
- `docs/qa-shots/1074-s3-live-qa/09-b-four-before-cutoff-created.png`
- `docs/qa-shots/1074-s3-live-qa/10-c-day-same-day-409.png`

## 이 라운드가 보지 않은 것

- 실제 KST 00:01 이전까지 기다려 DAY·LOGEN의 마감 전 당일 201을 라이브로 재현하지 않았다. 현재 발화 조건이 0이므로 판정 불가이며 fixed-clock 전수 테스트로만 보완했다.
- 실제 KST 12:00/14:00/15:00 이후까지 기다려 REGION·STACK·경동 2종의 마감 후 당일 409를 라이브로 재현하지 않았다. 현재 발화 조건이 0이므로 판정 불가이며 fixed-clock 전수 테스트로만 보완했다.
- LOGEN의 마감 후 당일/익일 GUI를 별도로 클릭하지 않았다. 같은 after-phase인 DAY 실 GUI와 태그 전수 fixed-clock 테스트로 보완했다.
- 견적 변환·모바일 주문·주문 발행·복제 등 수동 작성 외 신규 생성 경로는 이번 라이브 라운드에서 호출하지 않았다.
- Electron packaged binary, OS별 date picker 팝업, 키보드·스크린리더 접근성은 보지 않았다.
- 선재 `samhan-nginx` unhealthy와 상단 `업데이트 실패` 배너의 원인은 본 PR 범위로 진단하지 않았다.
