# R30 교차 날짜 연락처 섞임 수정 기록

## 진행 기록

- 2026-08-04: `git pull` 완료 (`Already up to date`). R29 라이브 QA 보고서와 구현을 확인한 뒤 RED-A/RED-B를 고정한다.
- 원인 확인: R22의 `handleDateChange`가 `syncDriverContactDates(rows, nextDate)`로 공유 `driverContacts` 전체 행의 숨은 `date`를 현재 선택 날짜로 덮어쓴다. 따라서 A에서 만든 X 행이 B로 이동한 뒤 B의 Y 입력 대상이 되고, A 복귀 시 Y가 표시된다. 서버 `DispatchBatchPreviewService.resolveDriverPhone`의 `input.date` 대 `requestedDate` 필터는 유지되지만 FE에서 행 날짜가 이미 변조된 뒤라 교차 날짜를 막지 못한다.
- 레거시 대조: `tools/legacy-gas/배차안내문자/Index.html`은 날짜별 `pid` 패널과 `tbody_driver_{pid}`를 별도로 유지하고 제출 시 각 패널의 `dv`를 `날짜`로 붙인다. 현 구현도 날짜별 연락처 행 컬렉션으로 대응시킨다.

## RED 테스트

- RED-A 원문: `RED-A: 날짜를 바꾼 뒤 새로 입력한 연락처는 그 날짜 요청에 반영된다` — 날짜별 상태 API가 없어 `setDriverContactsForDate is not a function`으로 실패.
- RED-B 원문: `RED-B: A의 연락처는 B를 거쳐 A로 돌아와도 A에만 남는다` — 동일하게 `setDriverContactsForDate is not a function`으로 실패.
- 실행: `clients/desktop`에서 `npx vitest run src/renderer/routes/DispatchSmsPage.test.ts`
- 결과: 3 tests 중 기존 R28 성격 테스트 1 PASS, RED-A/RED-B 2 FAIL. 실패가 새 날짜별 상태 동작 부재를 직접 가리킴.

## GREEN 1차

- 날짜별 `DriverContactsByDate` 상태를 도입하고 `handleDateChange`에서는 날짜만 바꾸도록 수정했다. 현재 날짜의 행만 `PreviewSection`에 전달하며, 새 행 생성 시 현재 날짜를 부착한다.
- RED-A 원문: `날짜를 바꾼 뒤 새로 입력한 연락처는 그 날짜 요청에 반영된다` — GREEN.
- RED-B 원문: `A의 연락처는 B를 거쳐 A로 돌아와도 A에만 남는다` — GREEN.
- 실행 결과: `npx vitest run src/renderer/routes/DispatchSmsPage.test.ts` → 3/3 PASS.

## 원인·구조 대응

- 서버 필터는 `DispatchBatchPreviewService.java:80, 94, 223`의 날짜별 전표 조회와 `input.date()` 불일치 제외 로직으로 그대로 유지했다. 이번 수정은 서버 계약이나 필터를 우회하지 않는다.
- 레거시 `Index.html:407, 415, 424, 484, 1354`의 날짜 패널·패널별 기사 tbody·패널 날짜 제출 구조에 맞춰, FE도 `DriverContactsByDate`로 날짜별 연락처 행을 분리했다. 날짜 변경은 공유 행의 날짜를 변경하지 않고 현재 날짜의 컬렉션만 선택한다.

## 최종 검증

- 두 RED 동시 GREEN 원문:
  - `RED-A: 날짜를 바꾼 뒤 새로 입력한 연락처는 그 날짜 요청에 반영된다` — PASS.
  - `RED-B: A의 연락처는 B를 거쳐 A로 돌아와도 A에만 남는다` — PASS.
- `clients/desktop`: `npx vitest run src/renderer/routes/DispatchSmsPage.test.ts src/renderer/routes/dispatchSmsClipboard.test.ts src/renderer/routes/scopeADisplayOnly.contract.test.ts` → 3 files, 11/11 PASS.
- `services/notification-service`: `DispatchBatchPreviewServiceTest` 집중 Gradle 실행 → BUILD SUCCESSFUL. R21/R22 날짜 필터·R27/R28 레거시 매칭 및 fallback 테스트 포함.
- `clients/desktop`: `npm run typecheck` → TypeScript 및 real-QA scope 검사 PASS. 로컬 미추적 스펙 경고는 기존 환경 상태였고, scope 검사는 허용 모드로 50/50 PASS.
- R28 유지 확인: ① 순수 순번·전체 전표번호·끝 1~3자리·`/` 다중 레거시 매칭, ⑥ 연락처 없는 전표 fallback, ⑦ 자동 SMS 미발송 계약, ⑧ 편집→선택→복사 계약을 집중 테스트에서 유지했다. 서버 `date` 필터도 유지했다.

## 새 파일 목록

- `docs/dev-reports/2026-08-04-1013-r30-cross-date-fix.md` — R30 단계·RED/GREEN·검증 기록

수정 파일: `clients/desktop/src/renderer/routes/DispatchSmsPage.tsx`, `clients/desktop/src/renderer/routes/DispatchSmsPage.test.ts`. 커밋·push·Docker 작업은 수행하지 않았다.
