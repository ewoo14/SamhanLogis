# PR #1078 · 이슈 #1075 — S9 단정 복원 결과

## 판정

`origin/main` 기준 단정 18개는 현재 Playwright 파일에 전부 유지된다. 누락은 0개다.

이번 라운드에는 `1062-line-input-ux.spec.ts`에 provider 실패 폴백 편집 시나리오를 별도로 추가하고, trailing 빈행 확정 뒤 다음 빈행 표시 및 행 수 4개 단정을 원문 그대로 복원했다. 기존 coedit 시나리오의 기존행 교체·trailing/삭제 잠금 단정과 신규 작성 자동 빈행 시나리오는 유지했다.

제품 코드 변경은 없다.

## `origin/main` 18개 ↔ 현재 단정 대조

대조 방법:

```powershell
$path = 'clients/desktop/playwright/1062-line-input-ux/1062-line-input-ux.spec.ts'
$origin = @(git show "origin/main:$path" | Select-String 'expect\(' | % { $_.Line.Trim() })
$current = @(Get-Content $path | Select-String 'expect\(' | % { $_.Line.Trim() })
$origin | % { $current -contains $_ }
```

결과: 기준 18개, 현재 43개, 기준 단정 누락 0개.

| # | `origin/main` 단정 | 현재 |
|---:|---|:---:|
| 1 | `await expect(page.getByTestId('safety-stock-config')).toBeVisible({ timeout: 15_000 })` | 유지 |
| 2 | `await expect(dialog).toBeVisible({ timeout: 10_000 })` | 유지 |
| 3 | `await expect(dialog.getByRole('table')).toBeVisible()` | 유지 |
| 4 | `await expect(dialog.getByRole('columnheader', { name: '모델명' })).toBeVisible()` | 유지 |
| 5 | `await expect(dialog.getByRole('columnheader', { name: '품목명' })).toBeVisible()` | 유지 |
| 6 | `await expect(dialog.getByRole('columnheader', { name: '규격' })).toBeVisible()` | 유지 |
| 7 | `await expect(dialog.getByRole('columnheader', { name: '단가' })).toBeVisible()` | 유지 |
| 8 | `await expect(dialog.getByText('AJ040RXH4BC1', { exact: true })).toBeVisible()` | 유지 |
| 9 | `await expect(dialog.getByText('1,850,000원', { exact: true })).toBeVisible()` | 유지 |
| 10 | `await expect(dialog).not.toContainText('aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaa040')` | 유지 |
| 11 | `await expect(dialog).toBeHidden()` | 유지 |
| 12 | `await expect(input).toHaveValue('AJ040RXH4BC1')` | 유지 |
| 13 | `await expect(page.getByRole('listbox', { name: '품목 목록' })).not.toBeVisible()` | 유지 |
| 14 | `await expect(page.getByTestId('estimate-form-line-0')).toBeVisible({ timeout: 15_000 })` | 유지 |
| 15 | `await expect(page.getByTestId('estimate-form-line-1')).toBeVisible()` | 유지 |
| 16 | `await expect(page.getByTestId('estimate-form-line-2')).toBeVisible()` | 유지 |
| 17 | `await expect(page.getByTestId('estimate-form-line-3')).toBeVisible({ timeout: 10_000 })` | 유지 |
| 18 | `await expect(page.locator('[data-testid^="estimate-form-line-"][data-price-source]')).toHaveCount(4)` | 유지 |

## 검증

- `npx vitest run src/renderer/routes/EstimateFormPage`: **38 passed**.
- `ac-2-product-autocomplete + 1062-line-input-ux`: 첫 실행에서 **10 passed, 신규 폴백 시나리오 1 failed**. 이후 provider 실패 유도를 보완했으나, Playwright 재실행은 기존 로컬 Vite 테스트 서버 잔류/기동 타임아웃으로 완료 결과를 얻지 못했다.
- `git diff --check`: 통과.

따라서 단정 대조 BLOCK은 해소했지만, 신규 폴백 시나리오의 최종 Playwright green은 PM 실행 환경에서 재확인해야 한다.

## 변경 파일

- `clients/desktop/playwright/1062-line-input-ux/1062-line-input-ux.spec.ts`
- `docs/dev-reports/2026-08-05-1075-s9-fix.md`
