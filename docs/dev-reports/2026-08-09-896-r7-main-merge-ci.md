# PR #1126 R7 — main 병합 + CI 회복

검증 대상: `feat/896-qty-sync-chip-track`, 병합 전 `0be8ecd8d`, `origin/main` `fe9709df1c63f5704bbc500d0281e67ac2111955`

## 결과 요약

- `origin/main` 병합 완료(텍스트 충돌 1건 해결). 병합 커밋은 이 보고서 작성 후 생성한다.
- 타입 오류 원인은 테스트 로컬 `Option` 타입이 `{ id, name }`인데 R2 테스트만 `{ id, label }`을 사용한 것이었다. 테스트를 삭제하지 않고 전체 `Option`을 `{ id, label }`로 통일했다.
- `MultiSelectAutocomplete`의 인스턴스별 chip-count 계약은 유지됐다: `${inputTestId}-chip-count`.
- `tools/legacy-gas/**` 변경 0건.

## 충돌·의미 통합 표

| 파일/표면 | 텍스트 충돌 | 수량동기화 축 | `goodsType` 축 | 기타 통합/판정 |
|---|---:|---|---|---|
| `clients/web/design-system/src/components/MultiSelectAutocomplete/MultiSelectAutocomplete.test.tsx` | 1 | R2 chip-count 테스트 보존 | 해당 없음 | main의 단수 결과 opt-in 테스트도 보존하고 `Option.label`로 통일 |
| `clients/desktop/src/renderer/routes/EstimateItemsCatalogPage.tsx` | 0 | 수량동기화 규칙 조회·칩 저장·source/target 계약 보존 | 상품/비상품 셀렉트와 PATCH, 검색 결과 `goodsType` 보존 | 두 축이 같은 견적품목 카탈로그 화면에서 공존하는지 테스트 통과 |
| `clients/web/estimate-app/lib/db-catalog.js` | 0 | `quantitySyncRules()` 내부 카탈로그 endpoint 보존 | 이 레거시 adapter의 기존 출력 shape에는 `goodsType`을 임의 추가하지 않음 | 납품가/출고가·분류·할인 필드가 유지됨. main 변경과 의미상 덮어쓰기 없음 |
| `clients/web/order-app/src/samhanApi.ts` | 0 | `/quantity-sync-rules` 페이지 수집 계약 보존 | 비상품 축과 직접 겹치는 변경 없음 | RPC mapping·날짜 파라미터·기존 endpoint 유지 |
| `clients/web/order-app/src/__tests__/samhanApi.test.ts` | 0 | 수량 규칙 endpoint assertion 보존 | 해당 없음 | 23/23 통과 |

`recomputeHomeDerived`는 조기 return 없이 실내기 집계 → 호스 → 분기관 → 리모컨 → 발통 → 판넬 레거시 계산을 모두 수행한 뒤 `applyServerHomeQuantitySync_()`를 호출한다. 규칙이 소유한 target만 서버 값으로 덮고, 규칙 0건/장애 시 빈 집합으로 순수 레거시 결과를 유지한다.

## RED-A~E 결과

| 게이트 | 결과 | 원문/근거 |
|---|---|---|
| RED-A 타입·칩 | GREEN | design-system build 성공, `MultiSelectAutocomplete.test.tsx`: 8 tests passed; desktop typecheck exit 0 |
| RED-B 계열 단위 동기화 | GREEN | estimate 전체 14 suites / 205 tests passed; `legacy-quantity-golden.test.js` 포함 |
| RED-C 규칙 0건 exact legacy diff | GREEN | `legacy-quantity-golden.test.js` 통과(전체 estimate suite에 포함) |
| RED-D 비상품 34건·납품가 수량 1 | GREEN | product-service `*Product*` Gradle test BUILD SUCCESSFUL; desktop `estimateLineModel`·catalog 테스트 8/8; estimate 수량 테스트 포함 전체 205/205 |
| RED-E hard gate | GREEN | `[guard] expected=1 unexpected=0 skipped=0 flaky=0` |

실행한 주요 명령:

- `clients/web/design-system`: `npm run build`, 대상 Vitest 8/8
- `clients/desktop`: `npm run build`, 관련 Vitest 8/8, `npm run typecheck`, real-QA guard 50/50
- `clients/web/estimate-app`: `npm test` 14 suites/205 tests, `npm run typecheck` → `typecheck OK: 17 JavaScript files`
- `clients/web/order-app`: `samhanApi.test.ts` 23/23
- `./gradlew :services:slip-service:test --tests '*OutboundCutoffGuardTest*' --no-daemon`
- `./gradlew :services:product-service:test --tests '*Product*' --no-daemon`

## OutboundCutoffGuardTest 판정

병합 후 대상 suite는 `tests="20" skipped="0" failures="0" errors="0"`이다. 기존에 보고된 cutoff 테스트 4건은 병합 후 실패 없이 해소된 것으로 판정한다.

처음 실행한 `:slip-service:test`는 Gradle 경로가 잘못되어 `project 'slip-service' not found`가 났고, 멀티프로젝트 경로인 `:services:slip-service:test`로 재실행해 성공했다. 이는 테스트 실패가 아니다.

## 새 파일 목록

- 이번 R7에서 새로 작성한 파일: 이 보고서 1개.
- main 병합으로 upstream에서 들어온 신규 파일: `git diff --cached --name-status HEAD` 기준 751개. 주요 관련 신규 표면은 `clients/desktop/playwright/1152-*-real-qa/*`, `docs/dev-reports/2026-08-09-1152-*`, `clients/desktop/playwright/1127-*`, `services/product-service`·`services/inventory-service`의 #1152 산출물이다. 이 파일들은 R7에서 생성한 것이 아니라 `origin/main` 병합 산출물이다.
- `tools/legacy-gas/**` 신규/변경: 0개.

## 못 한 것·환경 판정

- 실제 공유 DB write 및 실 API live QA는 수행하지 않았다(요청의 DB write 금지 준수). 기존 커밋된 QA 증거와 단위/통합 테스트로 판정했다.
- `qa-output-path-guard.test.cjs`는 Windows checkout에서 `infrastructure/scripts/operational-validation.ps1`의 UTF-16/EOL 바이트 불일치 1건으로 54/55가 됐다. 원문은 `blob 86028바이트, checkout 86630바이트`, `.gitattributes`의 `-text` 누락을 지목한다. 이 파일은 R7 변경 대상이 아니며, Linux CI와 다른 Windows checkout 변환 문제라 이번 라운드에서 수정하지 않았다.
- Playwright 전체 mock 실행 자체는 하지 않았고, 기존 결과 파일의 hard gate만 확인했다(`unexpected=0`). 새 R7 live QA spec/캡처는 만들지 않았다.
- 보고서와 증거에는 로그인 자격/토큰을 기록하지 않았다.
