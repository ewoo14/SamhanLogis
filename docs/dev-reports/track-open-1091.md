연관 Issue: #1091

## 트랙 개설 — 착수 전 확인

2026-08-06 등록. PR #1104 로 **첫 표면이 머지**됐고, 이슈는 **나머지 18개 표면** 때문에 열려 있습니다.

### 요구 (개발책임자 결정)
- 버전이력을 **'버전이력' 버튼 → 모달**로
- 카드 안 **변경항목 접기**

### 정찰이 실측할 것
1. PR #1104 가 **어느 표면**에 적용했는지, 남은 **18개가 정확히 어디**인지 (`파일:줄` 전수)
2. 🚨 표면마다 **버전이력 데이터 구조가 같은지** — 다르면 한 모달로 못 받습니다. 다른 것이 있으면 그 사실을 적으십시오
3. 이미 적용한 표면의 **계약**(모달 열기·닫기·접기 상태)을 원문으로 — 나머지가 그것과 같아야 합니다
4. 🚨 **재사용** — 이미 만든 모달 컴포넌트를 쓰는지 확인. 자체 재구현 금지

### 🚨 이 트랙의 위험
18개 표면을 한 번에 바꾸면 **회귀 범위가 넓습니다.** 정찰이 표면을 성격별로 묶어 주면 제가 슬라이스를 자릅니다.
그리고 이 저장소에서 **"목록으로 걸면 목록만 닫힌다"** 가 오늘만 네 번 났습니다 — 18개를 세는 방법 자체를 축으로 만드십시오(예: 버전이력을 표시하는 모든 지점을 grep 으로 발견).