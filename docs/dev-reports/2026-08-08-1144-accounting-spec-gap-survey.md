# #1144 회계전표 명세 vs 현행 구현 — 전수 조사 종합

> 2026-08-08 · Workflow wf_c839de0a-eb0 · 에이전트 11개 · 실패 0 · tool call 700
> 적대검증에서 뒤집힌 2건(규칙 1·7)은 뒤집힌 쪽을 정본으로 반영

## 집계

- summary: #1144 회계전표 명세 11개 규칙 vs 현행 구현 전수 조사 — 영역별 병렬 + 차집합 종합 + 적대 검증
- agentCount: 11
- logs: ['영역 조사 5/5 완료 · 규칙 판정 16건', '적대 검증 대상 5건 (구현됨·없음 단정만)', '반증 2/5 건 — 단정이 뒤집힌 것']
- result: {'영역': 5, '조사성공': 5, '규칙판정': 16, '적대검증': 5, '반증': 2, '종합': '# 명세 대비 현행 차집합 종합 — 판매/구매전표 ↔ 회계전표 ↔ 세금계산서 ↔ 원장\n\n조사 범위: `C:/dev/Samhan-Public` main 트리 (읽기 전용). DB 는 **집PC 시드** 기준(2026-08-08 실측).\n적대검증에서 뒤집힌 2건(규칙 1, 규칙 7)은 **뒤집힌 쪽을 정본**으로 반영했습니다.\n아래 ✔PM표시는 이번 종합 단계에서 제가 직접 파일·DB 로 재확인한 항목입니다(릴레이 아님).\n\n---\n\n## 1. 규칙 11개 × 현행 상태\n\n| 규칙 | 상태 | 근거(파일:줄 · 테이블) | 명세와의 차이 |\n|---|---|---|---|\n| **1** 판매전표→매출전표 · 구매전표→매입전표 를 회계담당자가 생성·연결 | **부분구현** | ✔PM `web/SalesAccountingSlipController.java:24-59` = GET / POST / POST `{slipNo}/post` **3개뿐**, 전부 `@RequirePermission(page="accounting.sales-slip.accounting")`. `service/SalesAccountingSlipCreateAttemptService.java:38-116` 생성, `:124-144` 4중 검증(OUTBOUND·CONFIRMED·거래처 필수·헤더 일치). 매입은 `PurchaseAccountingSlipController.java:36-42` 동형(`:119` INBOUND). auth_db: 회계원만 t/t/t·8그룹 f | ① 매출 경로는 성립하나 **매입은 실행 표본 0** — ✔PM `purchase_accounting_slips` **0행**, 유일한 CONFIRMED INBOUND 1건은 `partner_code` NULL 이라 `:134-139` 에서 하드 거절 ⇒ **판정 불가**. ② FE 가드가 BE 와 다른 페이지코드 — `routes/index.tsx:1180/1188/1196/1204` 는 `accounting.sales-slip.list`, BE 는 `.accounting`. `.list` view 만 있는 계정 13개는 화면 진입 후 GET 403, `.list` update 만 있는 3개(dev_manager·장영구·manager@samhan.test)는 저장 403. ③ 액션 축 불일치 — FE `action=\'edit\'`→`permissionsApi.ts:409-411` 이 UPDATE 로 매핑, BE 생성은 CREATE |\n| **2** 생성 경로 ① 해당 전표 상세(그 전표 한 건) | **없음** | 판매전표 상세 `SlipDetailPage.tsx` 5,349줄에 회계/accounting/POSTED/전기 grep **0건**. 회계전표 API import 파일 전수 = 작성 2 · 목록 2 · 세금계산서 3 · 테스트 — 전표 상세 **없음**. API 도 `slipId` 단건 endpoint 없음(`CreateSalesAccountingSlipRequest.java:13-37` 은 `sourceLineId` 배열만) | 경로 자체가 없다. 역방향 표시도 없음 — slip_db `slips`/`slip_lines` 에 account·tax·journal 계열 컬럼 information_schema 실측 **0개** ⇒ 판매전표 쪽에서 연결 여부를 알 수단이 없다 |\n| **3** 생성 경로 ② 외부에서 일괄(선택 후 복수 생성) | **부분구현** | 외부 화면·다건 선택 있음: `routes/index.tsx:1186` → `SalesAccountingSlipFormPage.tsx:133-141` → `slipAllocationSourceApi.ts:137-158` → `SlipAllocationSourceController.java:38-45` → `SlipRepository.java:228-241`. 배분 편집기 `SlipLineAllocationEditor.tsx:94-128,:190-204` | ① **복수 생성 없음** — `SalesAccountingSlipFormPage.tsx:66-81` 이 lines 를 원소 1개 배열로 고정. 1회 제출 = 회계전표 1건. ② 선택한 여러 품목이 **첫 행 품목코드 한 줄로 합쳐짐**(`:68-69`) — 품목 구분 소실. ③ 원천 검색 기간이 전표일자에 묶임(`:136-137` from=to=slipDate) ⇒ 다른 날짜 출고전표를 오늘 일자 회계전표로 만들 수 없다. ④ 후보에 **상태 필터 없음** — `SlipRepository:228-241` 에 status 술어 없고 Editor 가 status 를 버림. ✔PM 오늘 OUTBOUND 45행 표시 / CONFIRMED **0건**. ⑤ 기배분 잔여 미차감(`:104`) — 초과는 저장 시 `SAS_OVER_ALLOCATION` |\n| **4** 생성 경로 ③ 일마감에서 바로 생성·연결 | **없음** | ✔PM `DailyClosingController.java` @Mapping **3개뿐** — `:77` POST(마감) · `:101` GET · `:145` PATCH `/{closingDate}/lock`. `DailyClosingService.java:106-168` 은 집계+lock 만, `:335-357` 은 회계전표 **조회만**(저장 호출 0). `DailyClosingPage.tsx`(1,217줄) `navigate|Link|href` grep **0건**, `sales-slips|createSalesAccountingSlip` grep **0건**. accounting_db `daily_closings` 20컬럼에 회계전표 FK·번호 컬럼 없음 | 경로가 코드·화면·스키마 어디에도 없다. 일마감은 **이미 있는** 세금계산서/회계전표를 집계·잠그기만 한다 — 명세의 방향(일마감에서 판단하고 그 자리에서 생성)과 반대 |\n| **5** 일마감에서 적절한 금액(할인율/할인액) 판단 | **부분구현** | 판정엔진 4개 실재: `DiscountRevalidator.java:71-229`(434줄) · `LegacyVerificationChain.java`(248줄) · `LegacySetMatcher.java:96-119`(171줄) · `RiUsageDecision.java:25-26,:70-72`. DTO `DailyProductLine` = releasePrice·deliveryPrice·actualUnitPrice·discountAmount·expectedRate·actualRate·verified. FE 컬럼 `DailyClosingPage.tsx:663,670,684,694,702,713`. 마감 전 조회 가능(`MonthEndCloseService.java:474-483`) | ① 판단이 **아무것도 게이트하지 않음** — `close()` 집계(`:321-357`)가 verified 를 안 본다(false 여도 마감). ② 확인 표시가 read-only Badge, **수동 정정 불가**. ③ 판단 대상이 판매/구매전표가 아니라 세금계산서 또는 **이미 만들어진 회계전표**(`DailyClosingSourceKind` = TAX_INVOICE/SALES_SLIP/PURCHASE_SLIP, SALES_SLIP=`SalesAccountingSlip`) — slip_db `slips` 를 읽는 코드 0건 ⇒ "회계전표 생성 **전의** 판단"이 세금계산서 경로 외에는 성립하지 않는다. ④ 세트 합계 검증이 **데이터상 성립 불가**(아래) |\n| **5-보조** 레거시 \'싱글 구성품\' 두 납품가 검증 2경로 | **부분구현(값 부재)** | 경로는 포팅됨 — `LegacySetMatcher.java:96-119` ← `MonthEndCloseService.java:592-599`(`:594` 주석이 `Code.js pCols[1]` 명시) / `DiscountRevalidator.java:127-129,:191-198`. 그러나 값은 하나 — 원천이 `products.delivery_price` 와 `price_history.delivery_price` 이고 실측 1,119건 **전부 동일(차이 0건)**. `bundle_component` 17컬럼에 가격 컬럼 없음. `price_history.set_material_key` 2,201행 전부 NULL | 두 \'경로\'는 있으나 두 \'값\'을 담을 **자리가 스키마에 없다**. SINGLE_SET 구성품 1,448행 중 INDOOR 271/271 · OUTDOOR 271/271 이 `delivery_price=0` ⇒ `:96` expected=0 → `:117` 항상 불일치 → 확인=false. 레거시 세트 합계 판정이 재현되지 않는다 |\n| **6** 매출전표=채권 · 매입전표=채무 | **다르게구현** | 분류 근거는 **계정과목 코드**이지 전표가 아니다 — `report/ReceivablesPayablesService.java:45-47` RECEIVABLE=(110,120)/PAYABLE=(201,210) 하드코딩, `:163,:340-342`. `report/PartnerAgingService.java:50 "110" · :52 "201"`. `service/PartnerLedgerReadModelService.java:570,:575-583`. 회계전표 repository 의 main 사용처 = `DailyClosingService:80-81,336,348` + `MonthEndCloseService:96-97,262,297`(마감 집계)뿐 | `SalesAccountingSlipService.java:77-82` `post()` 는 **상태만 전이하고 분개를 만들지 않는다**(`PurchaseAccountingSlipService` 에 \'journal\' grep 0회) ⇒ 매출전표를 POSTED 해도 채권 잔액이 1원도 안 변한다. 실측 201/210 journal_lines 4행 전부 `[DEV-SEED]`/SYSTEM_SEED ⇒ **매입전표=채무는 실행 표본 0, 판정 불가** |\n| **7** 회계전표 = 총금액·공급가액·부가세액 표시 | **부분구현** (스키마·BE=구현됨 / 표시=없음) | 저장·계산·API 는 완비 — `V18__add_sales_accounting_slips.sql:13-15` total_supply/vat/amount NUMERIC(15,2) NOT NULL(매입 V19:13-15), 라인 `:47-49`. `SalesAccountingSlipCreateAttemptService.java:90` → `VatCalculator.java:24-33`(VAT 포함액 1.1 나눗셈 DOWN). 헤더=라인합 `SalesAccountingSlip.java:95-100`, 배분합 일치 강제 `:101-112`. 응답 3필드 `SalesAccountingSlipResponse.java:18-20,:30-32`. IT:76-89 1,500,000→1,363,636/136,364/1,500,000 | ① **저장된 부가세액을 볼 화면이 0** — 목록은 공급가·합계 2열뿐(`SalesAccountingSlipPage.tsx:71-86`, Purchase 동일), `totalVatAmount` 렌더 0곳, **상세 화면·getById API 없음**(`routes/index.tsx:1177-1207` = 목록·신규 2개). ② 세 값을 다 보이는 유일 화면인 작성 폼이 **서버와 반대 VAT 의미** — 폼 `:46-48` totalSupply=Σ allocatedAmount·vat=×0.1·합계=×1.1, 서버는 allocatedAmount 가 VAT 포함액(`SlipLineSnapshot.java:14-17`). 실데이터 2026/07/26-1027 배분 330,000 → 저장 300,000/30,000/330,000, 폼 표시 330,000/33,000/363,000(**합계 33,000원 차이**). ③ 값이 원 전표 파생이 아니라 **클라이언트가 보낸 qty×unitPrice** |\n| **8** 거래처별 채권/채무 원장에 매출·매입액 반영(회계전표 전에도) | **부분구현** (채권=명세대로 / 채무=없음) | 채권: `PartnerLedgerReadModelService.java:131,:465` → `PartnerLedgerSalesClient.java:32` GET `/internal/slips/partner-ledger-sales` → `SlipInternalController.java:405-429` → ✔PM `SlipRepository.findPartnerLedgerSales` (OUTBOUND 하드코딩, status IN :statuses). 의도 명시 `SlipInternalController.java:384-386` *"원장은 회계 반영 완료 목록이 아니라 거래 사실 문서"*. `:165-171` 추가 → `:350-369` fold → salesTotal·receivableBalance. 테스트 `PartnerLedgerReadModelServiceTest.java:46-66`(journal 집계 빈 리스트 → salesTotal 12,276,000). 실측 원장산식 합 **359,003,920원**, 같은 시점 살아있는 회계전표 **0** | ① **채무 원장이 아예 없다** — ✔PM 쿼리가 `SlipType.OUTBOUND` 하드코딩이라 INBOUND 20건/12거래처가 원장 밖이고, `PartnerLedgerPage.tsx:130-133,532-535` 컬럼에 채무가 없다. ② 채권도 **표면마다 시점이 다르다** — 거래처별 원장은 전표 즉시(명세 충족), 채권채무 현황(`ReceivablesPayablesService.java:160`)·총계정원장(`LedgerService.java:37`·`LedgerController.java:32`)은 journal_lines 전용이라 미반영이고 **회계전표를 연결해도 여전히 미반영**(매출전표가 분개를 안 만듦). 실제 110 채권 분개는 `TaxInvoiceService.java:228-265` issue() 만이 만든다(실측 5건, 110 차변 합 4,589,999원) ⇒ 명세와 **반대로 \'세금계산서 발행→채권 분개\'** 로 돈다 |\n| **9** 회계전표 생성·연결의 이유 = 세금계산서 발행 + 다른 회계메뉴 확인 | **부분구현** | 발행에 쓰임=실재: `SalesAccountingSlipRepository.java:65-77` `findPostedUnlinkedForBatchCandidates`(POSTED AND taxInvoiceId IS NULL) → `TaxInvoiceBatchFromSalesSlipsService.java:97-111` 이 TaxInvoice 생성 후 `slip.linkTaxInvoice()`. 매입 `TaxInvoiceInboundService.java:87-110` 동형 | ① 다른 회계메뉴 소비처가 **4곳뿐** — 일마감(`DailyClosingService:336`)·월마감(`MonthEndCloseService:262`)·목록(`SalesAccountingSlipService:25`)·묶음(`TaxInvoiceBatchFromSalesSlipsService:50`). ② **역방향 조회가 죽어 있다** — `SalesAccountingSlipRepository.findByTaxInvoiceId(:37)`·Purchase 동일이 main 호출처 **0**(IT 2건만). `TaxInvoiceDetailResponse.java:17-41` 에 연결 전표 필드 없음. 일마감 세금계산서 detail 은 `MonthEndCloseService.java:230-238` 이 salesSlipNo·sourceSlipNo 를 **리터럴 null** 로 채운다 ⇒ 세금계산서 어느 화면에서도 출처 전표를 볼 수 없다 |\n| **10** 회계전표 미연결이면 세금계산서 메뉴에 안 나옴 → 발행 불가 | **다르게구현** | 게이트 있는 곳 **2화면**: 발행묶음 `TaxInvoiceBatchIssuePage.tsx:32-39` → `TaxInvoiceBatchController.java:91-100` · 수신 `TaxInvoiceInboundPage.tsx:32-40`. 중복연결 차단 각 Service`:150-153` | 게이트 **없는 곳 5경로**: ① ✔PM `TaxInvoiceController.java:92` POST 신규작성(`CreateTaxInvoiceRequest` 필드 = partnerId·partnerCode·partnerBusinessNo·partnerName·partnerAddress·supplyDate·description·lines — **전표 필드 0개**) ② `:172` POST `/issue-request` ③ `:127` POST `/{id}/issue`(가드=DRAFT/라인≥1/공급가액>0) · `TaxInvoiceEmitService.java:122-131` emit-nts ④ 홈택스 일괄양식 `HometaxExportService.java:276-299`(판매전표 직접 fetch) ⑤ `SlipDetailPage.tsx:3650-3658`→`SalesInvoicePrintPage.tsx:39-41` 계산서 출력. ✔PM 실측: tax_invoices **19**(ISSUED 12), **미연결 18 · 그중 ISSUED 12** ⇒ 발행된 12건이 전부 연결 0. 목록 쿼리(`TaxInvoiceRepository.java:34-55,69-83`)에도 연결 조건 없음. `tax_invoices`/`tax_invoice_lines` 에 원천 전표 컬럼 없음(전달이 복사) |\n| **11** 연결 후 금액 변경 시 삭제 후 재생성하여 재연결 | **없음** | ✔PM `SalesAccountingSlipController` = GET/POST/POST-post **3개**(PUT·PATCH·DELETE 없음), Purchase 동일. accounting-service main @DeleteMapping 8건 중 회계전표 대상 **0**. `voidSlip()` 는 `SalesAccountingSlip.java:126-129`/`PurchaseAccountingSlip.java:125-128` 에 있으나 **main 호출자 0**(테스트 3곳만), 스펙 `:365` 의 `SAS_SALES_SLIP_VOID` 는 ErrorCode 에 없음. FE `salesAccountingSlipApi.ts` = list/createDraft/post 3함수, 행 액션은 \'전기\' 하나 | 삭제·수정·무효·연결해제 **어느 것도 사용자 경로가 없다**. 금액이 틀린 회계전표가 POSTED 되면 정정 수단 0. 재연결도 불가 — `linkTaxInvoice` 는 기연결 시 `SAS_TAX_INVOICE_ALREADY_LINKED`(`:131-137`), unlink 없음, 후보 쿼리가 `taxInvoiceId IS NULL` 요구 ⇒ 세금계산서를 CANCELLED 해도 `tax_invoice_id` 가 남아 **영구히 후보에서 빠진다** |\n| **11-부속** 원 전표 금액이 바뀌면 연결된 회계전표는 | **다르게구현** | accounting-service main 에 `@Scheduled`/`@RabbitListener`/`@EventListener`/`@TransactionalEventListener` **0건**. allocation 은 생성 시점 snapshot(`V18:65-83` source_slip_no·allocated_qty·allocated_amount), accounting_db↔slip_db FK 없음(pg_constraint 실측 FK 5건 전부 동일 DB 내부) | \'자동 갱신 아님\'은 결과적으로 맞으나, **\'삭제 후 재생성\'을 구현한 결과가 아니라 정정 수단이 없어 생긴 상태**다. 금액 불일치 경고·배지·재검증도 없다 |\n| **11-부속** 삭제가 soft 인가 hard 인가 | **판정불가** | 엔티티는 soft 전제(`SalesAccountingSlip.java:28` `@SQLRestriction("is_deleted = false")`, `V18:25-27` is_deleted/deleted_at/deleted_by) 이나 이를 세팅하는 main 경로 없음. 실 DB 소프트삭제 1건 = slip_no `2026/07/26-1027`, memo `#937-RC4A-JRNL`, deleted_at 2026-07-27 03:44:01, **deleted_by NULL** ⇒ 직접 SQL(QA 잔재)로 보임 | `is_deleted` 축과 `status=VOIDED` 축이 **둘 다 준비되어 있고 둘 다 미사용** — 코드로 갈리지 않는다 |\n| **11-부속** 세금계산서 발행 후 회계전표 삭제 시 | **판정불가** | 삭제 경로 자체가 없어 실행 확정 불가. 구조만: accounting_db FK 5건에 `sales_accounting_slips ↔ tax_invoices` FK 없음. `tax_invoice_lines` 는 `TaxInvoice.java:299` `createDraftFromSalesSlips` 가 복사한 snapshot ⇒ 회계전표를 지워도 세금계산서 본문은 남는다 | 순서 규칙(세금계산서를 먼저 취소해야 하나, 취소 시 연결이 풀리나)이 **코드에도 2026-05-19 설계 스펙에도 없다** |\n\n---\n\n## 2. 차집합 — 명세대로 하려면 무엇을 만들어야 하는가\n\n### 2-0. 🚫 이미 되어 있는 것 — **다시 만들지 말 것**\n\n| 이미 있는 것 | 위치 | 재사용 방법 |\n|---|---|---|\n| 회계전표 **생성 도메인·검증·번호채번** | `SalesAccountingSlipCreateAttemptService.java:38-144`, Purchase 동형 | 새 경로(전표 상세·일마감)는 이 서비스를 호출만 하면 된다. 검증 4중(OUTBOUND/CONFIRMED/거래처 필수/헤더 일치)은 재작성 금지 |\n| **VAT 분리 계산** | `VatCalculator.java:24-33`(VAT 포함액 1.1 DOWN 절사) + `VatAmountCalculator.splitVatInclusive` | FE 미리보기는 **이 계약을 따르도록 고치는 것**이지 새 계산기를 만드는 게 아니다 |\n| 회계전표 **3금액 스키마** | `V18:13-15,47-49` / `V19:13-15,47-49` | 컬럼 추가 불필요. 응답 DTO 에도 이미 있다(`SalesAccountingSlipResponse.java:18-20,30-32`) |\n| **배분(allocation) 구조** | `V18:65-83`, `SalesAccountingSlip.java:101-112` 라인합==배분합 강제 | 복수 생성/단건 생성 모두 이 위에 얹는다 |\n| **전기(POST) 상태 전이** | `SalesAccountingSlipService.java:77-82` + `/{slipNo}/post` | 그대로. 다만 분개 생성은 여기에 없다(2-1 D 참조) |\n| **거래처별 채권 원장의 "전표 즉시 반영"** | `SlipInternalController.java:384-386,405-429` + `SlipRepository.findPartnerLedgerSales` + `PartnerLedgerContract.fold` | **규칙 8 의 채권 절반은 이미 명세대로다.** 채무는 이 구조를 **복제**하는 것이지 원장을 새로 설계하는 게 아니다 |\n| **세금계산서 발행묶음 게이트** | `SalesAccountingSlipRepository.java:65-77` + `TaxInvoiceBatchFromSalesSlipsService.java:97-111` + 수신 `TaxInvoiceInboundService.java:87-110` | 규칙 10 의 "게이트" 구현체가 이미 있다. 할 일은 **다른 5경로를 이 게이트로 모으는 것** |\n| **일마감 할인 판정엔진** | `DiscountRevalidator`(434줄)·`LegacyVerificationChain`·`LegacySetMatcher`·`RiUsageDecision` + FE 6컬럼 | 판정 로직 재구현 금지. 부족한 건 **판정 대상**(판매/구매전표)과 **판정 결과의 사용처** |\n| **소프트삭제 인프라** | BaseEntity 7 audit + `@SQLRestriction` + `V18:25-27` | 컬럼·기반 다 있다. **호출 경로만 없다** |\n\n### 2-1. 없는 것 (신규 구현)\n\n| # | 없는 것 | 무엇을 해야 하는가 | 관련 규칙 |\n|---|---|---|---|\n| A | **채무 원장** | `SlipRepository.findPartnerLedgerSales` 의 `SlipType.OUTBOUND` 하드코딩을 파라미터화하거나 INBOUND 전용 메서드 추가 → `/internal/slips/partner-ledger-purchases` → `PartnerLedgerReadModelService` 채무 fold → `PartnerLedgerPage.tsx` 채무 컬럼/탭. 🚨원장 | 6·8 |\n| B | **회계전표 정정 수단** (삭제 / 무효 / 연결해제) | `voidSlip()`(이미 존재, 호출자 0) 또는 soft-delete 를 endpoint 로 배선 + `ErrorCode` 에 `SAS_SALES_SLIP_VOID` 추가 + FE 행 액션. 세금계산서 연결 시 `tax_invoice_id` 를 어떻게 할지 규칙 필요(5절 Q6) 🚨금액·세금계산서 | 11 |\n| C | **생성 경로 ① 전표 상세 단건** | `SlipDetailPage.tsx` 에 회계전표 생성 액션 + 그 전표 한 건의 라인을 받는 BE 경로(기존 CreateAttemptService 재사용). 함께 **역방향 표시** — slip 쪽에서 연결 여부를 알 수단이 현재 0 | 2 |\n| D | **회계전표 POST 시 분개 생성** | 현행 `post()` 는 상태만 바꾼다. 채권/채무 분개를 만들지, 아니면 채권채무 현황·총계정원장이 전표를 직접 읽게 할지 선택이 필요(5절 Q4) 🚨금액·원장 | 6·8 |\n| E | **복수 회계전표 일괄 생성** | `SalesAccountingSlipFormPage.tsx:66-81` 의 "lines 1개 고정"을 풀고 BE 일괄 endpoint 추가. 단 \'복수\'의 정의 확인 선행(5절 Q2) | 3 |\n| F | **일마감에서의 생성·연결** | `DailyClosingController` 에 생성·연결 endpoint + `DailyClosingPage` 액션. C/E 의 서비스 재사용. 🚨금액 | 4 |\n| G | **회계전표 상세 화면 + 부가세액 표시** | `getById` API 와 상세 라우트가 둘 다 없다. 목록에 부가세·과세구분·세금계산서번호 컬럼 추가(설계 스펙 `2026-05-19-…design.md:506` 요구분). 🚨금액 | 7 |\n| H | **세금계산서 → 출처 전표 역방향 표시** | `findByTaxInvoiceId` 는 **이미 있고 main 호출처가 0**. `TaxInvoiceDetailResponse` 필드 추가 + `MonthEndCloseService.java:230-238` 의 null 하드코딩 제거 | 9 |\n| I | **세트 검증용 두 번째 납품가 저장 자리** | `bundle_component` 에 (세트,구성품) 문맥 가격 컬럼이 없다. 스키마 결정 필요. 🚨금액 | 5 |\n\n### 2-2. 다르게 된 것 (수정·정렬)\n\n| # | 어긋난 것 | 무엇을 해야 하는가 | 관련 규칙 |\n|---|---|---|---|\n| J | **세금계산서 게이트가 2화면 한정** — 신규작성·issue-request·issue·emit-nts·홈택스양식·계산서출력 6경로에 연결 검사 없음. 실측 ISSUED 12건 전부 미연결 | \'발행\'의 정의 확정(5절 Q3) 후 그 지점에 연결 검사 삽입. 목록 쿼리(`TaxInvoiceRepository.java:34-55,69-83`)에도 조건 반영. 🚨세금계산서 | 10 |\n| K | **FE/BE 권한 페이지코드 불일치** — FE `.list` / BE `.accounting`, FE edit→UPDATE / BE 생성 CREATE | 한쪽으로 통일. 안 고치면 13계정 목록 403 · 3계정 저장 403 이 계속되고, **다른 항목의 라이브 검증도 막힌다** | 1 |\n| L | **작성 폼 VAT 의미가 서버와 반대** — 330,000 배분 시 폼 363,000 / 저장 330,000 | `SalesAccountingSlipFormPage.tsx:46-48,146-148` 및 `mock buildMockDraft:143-147` 을 `VatCalculator` 계약에 맞춤. 🚨금액 | 7 |\n| M | **원천 선택기 4결함** — 상태 필터 없음(오늘 45행 중 CONFIRMED 0) · 기간이 전표일자에 고정 · 기배분 잔여 미차감 · 여러 품목이 한 줄로 합쳐짐 | `SlipRepository:228-241` 에 status 술어, `SlipLineAllocationEditor` 에 상태 표시·잔여 차감, 기간 입력 분리, 품목별 라인 유지. 🚨금액 | 3 |\n| N | **채권 표면마다 반영 시점이 다름** — 거래처별 원장=전표 즉시 / 채권채무 현황·총계정원장=journal_lines 전용. 실제 110 분개는 세금계산서 발행이 만든다 | 어느 표면이 명세의 \'원장\'인지 확정(5절 Q4) 후 정렬. 🚨원장 | 8 |\n| O | **일마감 판단이 무엇도 게이트하지 않음** — verified=false 여도 마감되고, 판단 대상이 판매/구매전표가 아니라 세금계산서/회계전표 | 판정 대상에 판매·구매전표 추가 + 판단 결과의 사용처 정의(5절 Q5). 🚨금액 | 5 |\n| P | **회계전표 금액이 원 전표 파생이 아니라 클라이언트 입력** — BE 는 배분합 일치만 검사 | 명세("총금액과 그 공급가액·부가세액")가 원 전표 파생을 요구하는지 확인 후 결정(5절 Q7). 🚨금액 | 7 |\n| Q | **화면 라벨 충돌** — `DailyClosingPage.tsx:48` `SALES_SLIP=\'매출전표\'` 는 회계전표를 가리키고, 명세의 \'판매전표\'는 slip_db OUTBOUND | 용어 확정 후 라벨 정리(5절 Q1) | 전반 |\n\n---\n\n## 3. 두 축이 분리돼 있는가\n\n```\n축 ①  원장 반영   = 전표 자체로 (회계전표 없어도)\n축 ②  세금계산서  = 회계전표 연결이 게이트\n```\n\n**결론: 두 축 모두 부분적으로만 지켜지고, 실제 시스템은 명세와 반대 방향으로 돌고 있는 구간이 있다.**\n\n### 축 ① 원장 반영 — 절반만 지킴\n\n| 표면 | 반영 시점 | 명세 준수 |\n|---|---|---|\n| 거래처별 채권 원장 (`PartnerLedgerReadModelService` ← slip-service) | **전표 즉시** — 회계전표 무관 | ✅ **명세대로** (실측: 살아있는 회계전표 0 상태에서 원장 합 359,003,920원 산출) |\n| 거래처별 **채무** 원장 | 존재하지 않음 | ❌ INBOUND 20건/12거래처가 원장 밖 |\n| 채권채무 현황 (`ReceivablesPayablesService`) | journal_lines 전용 | ❌ 전표로도 회계전표로도 안 들어옴 |\n| 총계정원장 (`LedgerService`/`LedgerController`) | journal_lines 전용 | ❌ 동일 |\n\n### 축 ② 세금계산서 게이트 — 2화면만 지킴\n\n| 경로 | 연결 게이트 | 명세 준수 |\n|---|---|---|\n| 발행 묶음 `TaxInvoiceBatchController.java:91-100` | POSTED + `taxInvoiceId IS NULL` | ✅ |\n| 수신 세금계산서 `TaxInvoiceInboundPage.tsx:32-40` | 동일 | ✅ |\n| 신규 작성 `TaxInvoiceController.java:92` | **없음** (요청 DTO 에 전표 필드 0개 — ✔PM 확인) | ❌ |\n| `POST /issue-request` `:172` | **없음** | ❌ |\n| `POST /{id}/issue` `:127` · `emit-nts` `:282` | **없음** | ❌ |\n| 홈택스 일괄 양식 `HometaxExportService.java:276-299` | **없음** (판매전표를 직접 읽음 — 회계전표를 건너뜀) | ❌ |\n| 판매전표 상세 \'계산서 출력\' `SlipDetailPage.tsx:3650-3658` | **없음** | ❌ |\n\n**실측 반증: ✔PM tax_invoices 19건 중 미연결 18건, 그중 ISSUED 12건.** 게이트가 발행을 막은 적이 없다.\n\n### 🚨 두 축이 **뒤바뀐** 구간 — 이것이 가장 큰 어긋남\n\n명세: `전표 → 원장 반영` / `전표 → 회계전표 → 세금계산서`\n현행: `전표 → 거래처별 원장(OK)` / **`세금계산서 발행 → 채권 분개 → 채권채무 현황·총계정원장`**\n\n근거: `TaxInvoiceService.java:228-265` `issue()` 만이 110 채권 분개를 만든다(실측 5건, 110 차변 합 4,589,999원). `SalesAccountingSlipService.java:77-82` `post()` 는 분개를 만들지 않는다.\n\n⇒ 회계 원장 계열 표면에서는 **회계전표가 원장에 아무 영향이 없고, 세금계산서가 원장을 움직인다**. 명세의 인과(원장은 전표가, 세금계산서는 회계전표가)가 이 구간에서 정확히 역전되어 있다.\n\n---\n\n## 4. 착수 순서\n\n### 관문 0 — 착수 전 반드시 (검증 자체가 불가능한 상태)\n\n| 항목 | 왜 | 검증 |\n|---|---|---|\n| **G0-1 권한 코드 정합 (K)** | FE `.list` / BE `.accounting` 불일치 때문에 회계원 아닌 계정으로는 화면에서 아무것도 못 밟는다. 이걸 안 고치면 이후 모든 라이브QA 가 403 에 막힌다 | 13계정·3계정 목록으로 실 로그인 후 목록 진입·저장 |\n| **G0-2 표본 만들기** | ✔PM `purchase_accounting_slips` **0행**, 활성 `sales_accounting_slips` **0행**, CONFIRMED INBOUND 1건(partner_code NULL). **표본 0 = 판정 불가**이지 "구현됨"이 아니다 | **실 관리자·실 화면 경로**로 INBOUND/OUTBOUND CONFIRMED 전표 각 1건 이상 + 매출·매입 회계전표 각 1건. 🚫 DB 직접 INSERT 금지 |\n| **G0-3 QA 잔재 식별** | `2026/07/26-1027`(deleted_by NULL, memo `#937-RC4A-JRNL`)·`2026/07/27-64`(is_deleted=t·REJECTED, 시드 6초 차) 는 결함이 아니라 잔재일 수 있다 | `created_at`/`created_by` 대조 후 PR 에 근거 기록 |\n\n### 묶음 A — 독립 · 즉시 착수 가능 (의존 없음)\n\n| 묶음 | 내용 | 🚨 | 검증 |\n|---|---|---|---|\n| **A1 채무 원장** (2-1 A) | `findPartnerLedgerSales` OUTBOUND 하드코딩 → INBOUND 경로 복제 + 화면 채무 컬럼 | 🚨원장 | 양방향 RED — RED-A: 채무 12거래처/20건이 원장에 뜬다 / RED-B: **기존 채권 34거래처·359,003,920원이 변하지 않는다**(회귀). 실 DB 수치 고정 |\n| **A2 작성 폼 VAT 의미** (2-2 L) | 폼·mock 을 `VatCalculator` 계약에 맞춤 | 🚨금액 | 배분 330,000 → 폼 표시가 저장값 300,000/30,000/330,000 과 **동일**함을 실 화면으로 |\n| **A3 회계전표 표시** (2-1 G) | `getById` + 상세 라우트 + 목록 부가세/과세구분/세금계산서번호 컬럼 | 🚨금액 | 저장된 `total_vat_amount` 가 화면에 나타남 (현재 렌더 0곳) |\n| **A4 세금계산서 역방향 표시** (2-1 H) | `findByTaxInvoiceId`(이미 존재) 배선 + DTO 필드 + `MonthEndCloseService:230-238` null 제거 | 🚨세금계산서 | 연결된 세금계산서 상세에 출처 전표번호 노출 |\n\n### 묶음 B — A2/G0-2 이후\n\n| 묶음 | 내용 | 의존 | 🚨 | 검증 |\n|---|---|---|---|---|\n| **B1 원천 선택기 4결함** (2-2 M) | 상태 필터·기간 분리·잔여 차감·품목별 라인 유지 | A2(금액 의미 확정 후) | 🚨금액 | 오늘 기준 45행 표시 → CONFIRMED 만 표시되고 그 수가 실 DB `slips` 카운트와 일치. 기배분 잔여가 표에서 차감 |\n| **B2 회계전표 정정 수단** (2-1 B) | void/삭제/연결해제 배선 | G0-2(표본) · **Q6 확정 필요** | 🚨금액·세금계산서 | 삭제 → 재생성 → 재연결 3단계를 실 화면으로 완주. 세금계산서 CANCELLED 후 후보 복귀 여부가 결정대로 동작 |\n\n### 묶음 C — B2 이후 (게이트를 먼저 걸면 정정 불가로 막힘)\n\n| 묶음 | 내용 | 의존 | 🚨 | 검증 |\n|---|---|---|---|---|\n| **C1 세금계산서 게이트 일원화** (2-2 J) | 6경로에 연결 검사 삽입 + 목록 쿼리 조건 | B2 · **Q3 확정 필요** | 🚨세금계산서 | 양방향 RED — RED-A: 연결된 회계전표로 정상 발행 / RED-B: 미연결 상태에서 6경로 전부 차단. **기존 미연결 18건이 어떻게 되는지 먼저 세고 결정**(무단 차단 시 실데이터 18건이 잠긴다) |\n\n### 묶음 D — Q4 확정 이후 (설계 결정 대기)\n\n| 묶음 | 내용 | 의존 | 🚨 | 검증 |\n|---|---|---|---|---|\n| **D1 회계전표 ↔ 분개/원장 인과 정렬** (2-1 D, 2-2 N) | 채권채무 현황·총계정원장이 무엇을 근거로 삼는지 확정 후 배선 | **Q4** | 🚨🚨원장·금액 | 매출전표 POSTED 전/후 채권 잔액 델타를 SQL 로 대조. 현재는 델타 0 |\n\n### 묶음 E — 생성 경로 확장 (G0-1·B1 이후)\n\n| 묶음 | 내용 | 의존 | 🚨 | 검증 |\n|---|---|---|---|---|\n| **E1 전표 상세 단건 생성 + 역방향 표시** (2-1 C) | `SlipDetailPage` 액션 + 단건 경로 | G0-1, B1 | 🚨금액 | 전표 상세에서 생성 → 그 전표에 연결 표시가 남음 |\n| **E2 일괄 복수 생성** (2-1 E) | lines 1개 고정 해제 + BE 일괄 endpoint | **Q2 확정** · B1 | 🚨금액 | 3전표 선택 → 회계전표 3건(또는 결정된 형태) |\n| **E3 일마감에서 생성·연결** (2-1 F) | `DailyClosingController` 확장 | E1/E2 | 🚨금액 | 일마감 화면에서 생성 → 목록·마감 집계에 즉시 반영 |\n\n### 묶음 F — 일마감 판단 (독립하되 데이터 결정 필요)\n\n| 묶음 | 내용 | 의존 | 🚨 | 검증 |\n|---|---|---|---|---|\n| **F1 세트 검증 값 자리** (2-1 I) | `bundle_component` 가격 컬럼 또는 대체 저장 | **Q8 확정** | 🚨금액 | INDOOR/OUTDOOR 542행 delivery_price=0 이 해소되고 `LegacySetMatcher:117` 불일치가 레거시와 같은 결과 |\n| **F2 판단의 대상·사용처** (2-2 O) | 판정 대상에 판매·구매전표 추가, verified 의 게이트 여부 | **Q5 확정** · F1 | 🚨금액 | verified=false 인 건에 대해 결정된 동작(마감 차단 / 경고 / 정정)이 실제로 일어남 |\n\n### 의존 그래프 요약\n\n```\nG0-1 권한 ─┬─→ A1 채무원장 ────────────────────────→ (D1)\n           ├─→ A3 상세표시   A4 역방향표시\nG0-2 표본 ─┤\n           └─→ A2 VAT의미 ──→ B1 선택기 ──┬─→ E1 단건생성 ──┐\n                             B2 정정수단 ─┴─→ E2 일괄생성 ──┴─→ E3 일마감생성\n                                    └────────→ C1 게이트일원화\nQ4 ────────────────────────────────────────→ D1 분개·원장 인과\nQ8 ──→ F1 세트값 ──→ F2 판단 사용처\n```\n\n---\n\n## 5. 개발책임자 확인이 필요한 것 (추측으로 채우지 않음)\n\n| # | 질문 | 왜 필요한가 (코드로 갈리지 않는 근거) |\n|---|---|---|\n| **Q1** | 명세의 **\'판매전표\'가 코드의 OUTBOUND(출고전표)와 같은 것**입니까? \'구매전표\'는 INBOUND(입고전표)입니까? | 코드 근거가 둘뿐이고 서로 다릅니다 — `SlipFormPage.tsx:670`(OUTBOUND 일 때 제목 "새 판매전표") / `DispatchGroupPage.tsx:49`(구매전표 편입 시 INBOUND) 인데 `SlipType` enum 의 displayName 은 "출고전표"/"입고전표"입니다. 더해 `DailyClosingPage.tsx:48` 의 `SALES_SLIP=\'매출전표\'` 는 **회계전표**를 가리켜 라벨이 이중으로 쓰입니다 |\n| **Q2** | *"외부에서 일괄(선택 후 복수 생성)"* 의 **\'복수\'** 는 ⓐ 회계전표 **여러 건** 입니까, ⓑ 한 회계전표에 **여러 원천 라인** 입니까? | ⓑ면 현행이 절반 충족(단 여러 품목이 한 줄로 합쳐지는 결함 잔존), ⓐ면 미구현입니다. 설계 스펙 `docs/superpowers/specs/2026-05-19-sales-purchase-accounting-slip-design.md` 에서 \'일괄\'은 전부 세금계산서·거래명세서 쪽이고 **회계전표 일괄 생성 항목은 0건** — ⓐ는 설계된 적이 없습니다 |\n| **Q3** | 규칙 10 의 **\'세금계산서 발행\'** 은 어디까지입니까? ⓐ TaxInvoice 를 ISSUED 로 만드는 것 ⓑ emit-nts(NTS 전송) ⓒ 홈택스 일괄 xlsx 업로드 ⓓ 판매전표 상세의 \'계산서 출력\' 인쇄 | 어디까지가 \'발행\'인지에 따라 게이트를 걸 지점과 위반 범위가 달라집니다. 현재 ⓐ~ⓓ 어디에도 연결 검사가 없습니다 |\n| **Q3-b** | 규칙 10 의 **\'세금계산서 메뉴\'** 는 사이드바 4개 중 어느 것입니까? (세금계산서 / 발행 묶음 / 수신 세금계산서 — `AppLayout.tsx:991-1009` / 홈택스 일괄 양식 — `:1200-1206`) | \'발행 묶음\' 하나만 가리킨다면 규칙 10 의 판정은 \'구현됨\' 쪽으로 이동합니다. 네 메뉴의 후보 원천이 서로 다릅니다 |\n| **Q4** | 규칙 8 의 **\'거래처별 채권 또는 채무원장\'** 은 어느 화면입니까? 채무원장은 거래처별 원장의 탭입니까, 별도 화면입니까? 그리고 **채권채무 현황·총계정원장도 전표 즉시 반영이어야 합니까**, journal 기준이 맞습니까? | 현행에 채권 성격 표면이 4개이고 **반영 시점이 서로 다릅니다**. 현재 110 채권 분개를 만드는 것은 세금계산서 발행뿐이라, 이 답에 따라 D1(가장 큰 구조 변경)의 방향이 정반대가 됩니다 |\n| **Q4-b** | 규칙 8 의 **\'반영\'** 은 금액 표시만입니까, 잔액 산식까지입니까? | 거래처별 원장은 salesTotal 과 receivableBalance 둘 다에 넣습니다(`PartnerLedgerContract.fold:107-127`). 채권채무 현황은 둘 다 미반영입니다 |\n| **Q5** | 규칙 5 에서 회계담당자가 **확인=false 를 본 뒤 해야 하는 조치**는 무엇입니까? *"판단하고 연결할 수 있어"* 의 \'판단\'이 어떤 조작입니까? verified 가 마감이나 회계전표 생성을 **막아야 합니까**? | 레거시 GAS·현행 코드 **어느 쪽에도 후속 조치가 없습니다**. 현재 확인은 read-only Badge 이고 아무것도 게이트하지 않습니다 |\n| **Q6** | 규칙 11 의 **\'삭제\'** 는 ① `is_deleted` soft-delete ② `status=VOIDED` 무효 ③ 둘 다 중 무엇입니까? 그리고 **이미 세금계산서가 붙은 회계전표를 삭제할 때** — 세금계산서를 먼저 취소해야 합니까? 취소하면 `tax_invoice_id` 를 NULL 로 되돌려 재발행 후보로 되살립니까? | 스키마가 **두 축을 다 갖고 있고 둘 다 미사용**입니다. 순서 규칙은 코드에도 2026-05-19 설계 스펙에도 정의가 없습니다. 현재는 세금계산서를 CANCELLED 해도 `tax_invoice_id` 가 남아 영구히 후보에서 빠집니다 |\n| **Q7** | 규칙 7 의 **"총금액과 총금액의 공급가액, 부가세액"** 은 **원 전표에서 파생**되어야 합니까, 회계담당자 입력값이어도 됩니까? | 현행은 클라이언트가 보낸 qty×unitPrice 로 계산하고 BE 는 배분합 일치만 봅니다. 원 전표 금액을 그대로 복사하지 않습니다 |\n| **Q8** | 시트 **\'싱글 구성품\'의 어느 물리 컬럼**이 `products.delivery_price` 로 적재됩니까(첫 번째 납품가인가 두 번째인가)? 그리고 (세트, 구성품) 문맥의 두 번째 납품가를 **어디에 저장**해야 합니까? | `ProductSheetSyncService.java:115-117` 은 고정 인덱스(delivery=7)로 읽고 레거시 fallback 인덱스는 8(`Code.js:231`)입니다. 원격 시트(`1RJqO3jT-yJTi3NDBhL60o_cZWlVETGTU7UlvIKXuVNQ`) \'싱글 구성품\' 2행 헤더 실물이 있어야 확정됩니다. 저장 자리가 없으면 세트 검증이 영구히 재현 불가입니다 |\n| **Q9** | 현재 **미연결 상태로 발행된 세금계산서 18건(ISSUED 12건)** 을 어떻게 처리합니까? 게이트를 거는 순간 이 18건은 규칙 위반 상태가 됩니다 | 소급 연결 / 예외 허용 / 정리 중 무엇인지가 C1 착수 조건입니다 |\n\n---\n\n## 6. 이 조사가 보지 않은 것\n\n| # | 보지 않은 것 | 확정하려면 무엇이 필요한가 |\n|---|---|---|\n| 1 | **라이브 실행 재현 전부** — 회계전표 생성·전기·연결·삭제 어느 것도 실제 화면으로 밟지 않았습니다. 판정은 코드·스키마·DB·단위테스트로만 했습니다 | 관문 0(권한 정합 + 실 경로 표본 생성) 이후 실서버 GUI 로 완주 |\n| 2 | **매입 계열 전 항목 — 표본 0 이라 판정 불가** (`purchase_accounting_slips` 0행 · 채무 journal_lines 4행 전부 dev seed · CONFIRMED INBOUND 1건도 partner_code NULL) | 실 화면 경로로 매입전표 1건 생성 후 채권채무 현황·미지급금 보고서 확인. 🚫DB 직접 INSERT 금지 |\n| 3 | **원장 API 실제 응답** — 내부 토큰 curl 이 권한 분류기에 차단됐습니다 | 회계담당자 JWT 로 `GET /accounting/sales/aggregate?from=2026-01-01&to=2026-12-31` 1회 + `GET /accounting/journals/partner-ledger?partnerCode=<40건 중 하나>&from&to` 1회 — salesTotal 이 359,003,920 근방인지, 회계전표 0 상태에서 receivableBalance 가 산출되는지 |\n| 4 | **`SAS_LINE_AMOUNT_MISMATCH` 7/15 조합 실패** — 코드와 실 DB 금액으로 계산한 결과이고 실제 POST 를 실행하지 않았습니다 | 회계원 계정으로 `/accounting/sales-slips/new` 에서 전표일자를 2026-08-03 으로 두고 `2026/08/03-6` 의 1·2번 라인만 100% 배분해 저장 → 응답 코드 확인 |\n| 5 | **권한 최종 결정 층** — account / group / role 세 테이블 중 어디로 결정되는지 코드로 끝까지 추적하지 않았습니다(`PermissionAspect.java:297-309` 는 role 폴백, 계정 경로는 auth-service `EffectivePermissionMaterializer`) | 세 층이 모두 `.accounting = 회계원/ACCOUNTANT/MASTER 만 t` 로 일치해 13계정·3계정 결론은 층과 무관하게 성립하나, 실제 403 화면은 미확인 |\n| 6 | **MASTER bypass 로 batch-issue CREATE 를 수행 가능한 계정 수** — auth_db `accounts` 에 role 컬럼이 없습니다 | `X-Is-System-Master` 헤더를 주입하는 주체(api-gateway 또는 user-service)의 판정 근거 확인 |\n| 7 | **CONFIRMED 전표 중 `partner_code` NULL 인 것의 원인** (매출 8건 중 6건, 매입 1건 중 1건) — 레거시 이관 산물인지 저장 경로 결함인지 | `SlipPublishService` 가 `partner_code` 를 채우는지 확인. **이것이 규칙 1·3 의 실행 표본을 0 으로 만드는 직접 원인**입니다 |\n| 8 | **회계기간/일마감 잠금(`lock_flag`)이 회계전표 생성을 막는지** — `CreateAttemptService` 에 기간 마감 검사 코드는 보이지 않았습니다 | 잠금 상태에서 생성 시도 |\n| 9 | **INBOUND→채무 경로가 accounting-service·slip-service 밖(inventory-service 등)에 있는지** — 두 서비스 범위에서는 0입니다 | 전 서비스 대상, INBOUND 전표를 거래처별 금액으로 집계하는 read 표면 grep |\n| 10 | **판매전표 상세 \'계산서 출력\'(`/sales/:id/print/invoice`)의 권한 가드** — `routes/index.tsx:576` 에 PermissionGuard 래핑이 없으나 상위 레이아웃 라우트 가드 상속 여부 미확인 | 라우트 트리 상속 확인 또는 비권한 계정 실접속 |\n| 11 | **INDOOR/OUTDOOR 구성품 542행 `delivery_price=0` 의 원인** — 시트가 비어서인지 적재가 다른 컬럼을 읽어서인지 | 원격 시트 값 표본 대조 또는 `ProductSheetSyncService` 실행 로그 |\n| 12 | **세트 검증 분기 라이브 재현** — 집PC 는 `tax_invoice_lines` 13행 전부 `model_name` 이 비어 있고(`MonthEndCloseService.java:373-377` 의 setPool 조건) 회계전표 0건 | `model_name` 이 채워진 세금계산서 라인 또는 POSTED 회계전표를 실 경로로 만든 뒤 상세 호출 |\n| 13 | **`daily_closings` 2행(둘 다 2020-01-02·금액 0·2026-07-22 생성)이 QA 잔재인지 운영 흔적인지** — `created_by` 미확인 | `created_by` 확인 + 다른 PC 데이터/감사 로그 |\n| 14 | **실 DB 유일 회계전표를 누가 삭제했는지** — `deleted_by` NULL, 애플리케이션 soft-delete 경로 0. 2026-07-27 03:44 QA 라운드 직접 SQL 로 **추정하나 확정 못 함** | 그 시각의 QA 스크립트·라운드 기록 |\n| 15 | **작성 폼 \'공급가\' 라벨이 단순 오표기인지 다른 계약인지** — Σ allocatedAmount 가 VAT 포함액임은 확정했으나 FE 가 그것을 공급가로 부르는 이유는 코드로 갈리지 않음 | 스펙 `:503-509`("VAT-inclusive 단가 입력 → 공급가액/부가세 자동 분리 표시")가 오표기 쪽을 시사하나 단정하지 않음 |\n| 16 | **본 조사의 모든 DB 수치는 집PC 시드 기준** (2026-08-08 측정) | 회사PC·운영은 데이터가 다릅니다. 수치를 근거로 판단하기 전 그 PC 에서 다시 세야 합니다 |\n| 17 | **CI·빌드 상태** — Gradle/npm 빌드 금지 제약으로 컴파일·테스트를 돌리지 않았습니다 | 해당 없음(읽기 전용 조사) |'}
- workflowProgress: [{'type': 'workflow_phase', 'index': 1, 'title': 'Survey'}, {'type': 'workflow_phase', 'index': 2, 'title': 'Verify'}, {'type': 'workflow_phase', 'index': 3, 'title': 'Synthesis'}, {'type': 'workflow_agent', 'index': 1, 'label': 'survey:creation-paths', 'phaseIndex': 1, 'phaseTitle': 'Survey', 'agentId': 'a7b7c89294942934f', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786196856756, 'queuedAt': 1786196849990, 'attempt': 1, 'lastToolName': 'StructuredOutput', 'lastToolSummary': '회계전표 생성·연결 경로 (규칙 1·2·3) — 판매전표(출고)→매출…', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786197946654, 'tokens': 210093, 'toolCalls': 98, 'durationMs': 1089896, 'resultPreview': '{"area":"회계전표 생성·연결 경로 (규칙 1·2·3) — 판매전표(출고)→매출전표 / 구매전표(입고)→매입전표","rules":[{"rule":"1 — 판매전표 → 매출전표 · 구매전표 → 매입전표 를 회계담당자가 생성·연결","status":"구현됨","evidence":"BE: `web/SalesAccountingSlipController.java:36-42` POST /admin/sales-slips → `service/SalesAccountingSlipService.java:49-63` → `service/SalesAccountingSlipCreateAttemptService.java:38-116`. 매입은 `PurchaseAccountingSlipController.java:36-42` 동형…'}, {'type': 'workflow_agent', 'index': 2, 'label': 'survey:tax-invoice-gate', 'phaseIndex': 1, 'phaseTitle': 'Survey', 'agentId': 'a533f7a713fcd5863', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786196859322, 'queuedAt': 1786196849990, 'attempt': 1, 'lastToolName': 'StructuredOutput', 'lastToolSummary': '세금계산서 게이트 (규칙 9·10) — accounting-service 세금계산…', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786197936163, 'tokens': 234703, 'toolCalls': 101, 'durationMs': 1076841, 'resultPreview': '{"area":"세금계산서 게이트 (규칙 9·10) — accounting-service 세금계산서 도메인 + 홈택스 일괄 양식 + 데스크톱 세금계산서/발행묶음/수신 메뉴","rules":[{"rule":"9 — 회계전표 생성·연결의 이유 = 세금계산서 발행 + 다른 회계메뉴 확인","status":"부분구현","evidence":"①발행에 쓰임=존재: SalesAccountingSlipRepository.java:65-77 findPostedUnlinkedForBatchCandidates 가 status=POSTED AND taxInvoiceId IS NULL 매출전표만 후보로 내고, TaxInvoiceBatchFromSalesSlipsService.java:97-111 이 그 라인으로 TaxInvoice…'}, {'type': 'workflow_agent', 'index': 3, 'label': 'survey:ledger', 'phaseIndex': 1, 'phaseTitle': 'Survey', 'agentId': 'a99c7d8aa723b865f', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786196863150, 'queuedAt': 1786196849990, 'attempt': 1, 'lastToolName': 'StructuredOutput', 'lastToolSummary': '채권·채무 원장 반영 (규칙 6·8) — accounting-service 원장/채…', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786198321641, 'tokens': 224820, 'toolCalls': 93, 'durationMs': 1458491, 'resultPreview': '{"area":"채권·채무 원장 반영 (규칙 6·8) — accounting-service 원장/채권채무 표면 + slip-service 원장 projection","rules":[{"rule":"6  매출전표 = 채권 · 매입전표 = 채무","status":"다르게구현","evidence":"분류 근거는 있으나 \'계정과목 코드\'이지 전표가 아니다. report/ReceivablesPayablesService.java:45-47 RECEIVABLE_ACCOUNTS=(110,120)·PAYABLE_ACCOUNTS=(201,210) 하드코딩, 분류는 :163,:340-342. report/PartnerAgingService.java:50 \\"110\\" · :52 \\"201\\". service/PartnerLed…'}, {'type': 'workflow_agent', 'index': 4, 'label': 'survey:daily-closing', 'phaseIndex': 1, 'phaseTitle': 'Survey', 'agentId': 'a468d668383d48bf9', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786196861128, 'queuedAt': 1786196849990, 'attempt': 1, 'lastToolName': 'StructuredOutput', 'lastToolSummary': '일마감 연동 (규칙 4·5) — 일마감 화면/기능, 금액(할인율·할인…', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786198328709, 'tokens': 259867, 'toolCalls': 102, 'durationMs': 1467581, 'resultPreview': '{"area":"일마감 연동 (규칙 4·5) — 일마감 화면/기능, 금액(할인율·할인액) 판단, 일마감에서의 회계전표 생성·연결, 레거시 GAS 일마감 프로그램 대조","rules":[{"rule":"규칙 4 — 일마감에서도 바로 회계전표를 생성하여 연결 (생성 경로 ③)","status":"없음","evidence":"DailyClosingController.java endpoint 3개뿐: :77 POST(마감실행) · :101 GET(기간조회) · :145 PATCH /{closingDate}/lock(잠금해제). DailyClosingService.java:106-168 close() 는 집계+lock 만 하며 Sales/PurchaseAccountingSlipRepository 는 :335-357 …'}, {'type': 'workflow_agent', 'index': 5, 'label': 'survey:amount-and-lifecycle', 'phaseIndex': 1, 'phaseTitle': 'Survey', 'agentId': 'af80d72c6247691c9', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786196854716, 'queuedAt': 1786196849990, 'attempt': 1, 'lastToolName': 'StructuredOutput', 'lastToolSummary': '회계전표(매출전표·매입전표) 금액 구성과 삭제·재생성 — 규칙 …', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786198135100, 'tokens': 201216, 'toolCalls': 91, 'durationMs': 1280382, 'resultPreview': '{"area":"회계전표(매출전표·매입전표) 금액 구성과 삭제·재생성 — 규칙 7·11","rules":[{"rule":"7 — 회계전표는 총금액·공급가액·부가세액 등이 표시되는 것 (스키마·계산)","status":"구현됨","evidence":"V18__add_sales_accounting_slips.sql:13-15 total_supply_amount / total_vat_amount / total_amount NUMERIC(15,2) NOT NULL. 매입 V19:13-15 동일. 라인 V18:47-49, V19:47-49 supply_amount/vat_amount/line_total.\\n계산 출처=원 전표 파생이 아니라 요청 body 의 qty×unitPrice — SalesAccountingSl…'}, {'type': 'workflow_agent', 'index': 6, 'label': 'verify:1 — 판매전표 → 매출전표 · 구매전표 →', 'phaseIndex': 2, 'phaseTitle': 'Verify', 'agentId': 'aa85a16c904856f67', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786198332398, 'queuedAt': 1786198330180, 'attempt': 1, 'lastToolName': 'StructuredOutput', 'lastToolSummary': '부분구현', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786198851770, 'tokens': 111310, 'toolCalls': 53, 'durationMs': 519372, 'resultPreview': '{"refuted":true,"correctedStatus":"부분구현","reason":"인용 근거는 대부분 재현됨: Controller:25/37/53 `.accounting` 권한, CreateAttemptService:124-128/129-133/134-139/140-144 4중 검증, 런타임 openapi 4경로(8087), 회계원만 t/t/t·8그룹 f 모두 일치. 단정이 누락한 화면도 실재(routes/index.tsx:1179-1206, /accounting/{sales,purchase}-slips/new + 사이드바).\\n\\n반증 4건:\\n① 구매→매입 표본 0 — purchase_accounting_slips 0행. CONFIRMED INBOUND 1건뿐이고 partner_code IS N…'}, {'type': 'workflow_agent', 'index': 7, 'label': 'verify:2 — 생성 경로 ① 해당 전표 상세 (그', 'phaseIndex': 2, 'phaseTitle': 'Verify', 'agentId': 'aac0ea70c98ea4b9d', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786198334880, 'queuedAt': 1786198330181, 'attempt': 1, 'lastToolName': 'StructuredOutput', 'lastToolSummary': '반증 실패 — 단정이 맞다. ①SlipDetailPage.tsx(5,349줄. index…', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786198684590, 'tokens': 96643, 'toolCalls': 32, 'durationMs': 349710, 'resultPreview': '{"refuted":false,"reason":"반증 실패 — 단정이 맞다. ①SlipDetailPage.tsx(5,349줄. index.tsx:566 /sales/:id, :611 /purchases/:id 로 걸린 실제 상세)에 회계|accounting|전기|POSTED 0건 재현 + journal|분개|voucher 도 0건(파일 내 \'매출/매입 전표\'는 출고·입고 전표 자신의 수정·삭제·인쇄). ②생성 API 호출부 13파일 중 비테스트 화면은 작성폼2·목록2·세금계산서3 뿐. ③작성폼 진입점 전수 = SalesAccountingSlipPage.tsx:113·PurchaseAccountingSlipPage.tsx:113 「작성」이 유일. ④POST /admin/sales-slips(Controller…'}, {'type': 'workflow_agent', 'index': 8, 'label': 'verify:규칙 4 — 일마감에서도 바로 회계전표를 생', 'phaseIndex': 2, 'phaseTitle': 'Verify', 'agentId': 'ab85173d7bc192466', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786198332269, 'queuedAt': 1786198330181, 'attempt': 1, 'lastToolName': 'StructuredOutput', 'lastToolSummary': '반증 실패. 전 근거 재현됨: Controller endpoint 3개(:77·:101…', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786198733673, 'tokens': 94142, 'toolCalls': 33, 'durationMs': 401403, 'resultPreview': '{"refuted":false,"reason":"반증 실패. 전 근거 재현됨: Controller endpoint 3개(:77·:101·:145) 일치. close() :106-168 은 집계+lock 만이고 slip repo 는 :80-81 주입·:336·:348 findBySlipDateAndStatus 조회뿐, save 0건. DailyClosingPage 1,217줄·navigate류 0·slip류 0. daily_closings 정확히 20컬럼, 회계전표 FK 없음. 일괄생성 없음(CreateAttemptService=단건 REQUIRES_NEW 재시도).\\n\\n단정이 놓쳤으나 결론 불변 3건: ①미검토 4번째 endpoint AccountingReportController.java:261 GET …'}, {'type': 'workflow_agent', 'index': 9, 'label': 'verify:7 — 회계전표는 총금액·공급가액·부가세액', 'phaseIndex': 2, 'phaseTitle': 'Verify', 'agentId': 'afbf53c0452d25d33', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786198333735, 'queuedAt': 1786198330181, 'attempt': 1, 'lastToolName': 'StructuredOutput', 'lastToolSummary': '인용 근거는 전부 재현됨(V18/V19:13-15·47-49, VatCalculator:…', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786198696763, 'tokens': 97504, 'toolCalls': 30, 'durationMs': 363028, 'resultPreview': '{"refuted":true,"reason":"인용 근거는 전부 재현됨(V18/V19:13-15·47-49, VatCalculator:24-33 taxType 분기 포함, SalesAccountingSlip:95-112, Response:18-20·30-32, IT:87-89). 스키마·BE 계산·API 는 구현됨. 반증은 \\"표시\\" 축.\\n① 저장된 부가세액이 어느 화면에도 없다 — 목록은 공급가·합계 2열뿐(SalesAccountingSlipPage.tsx:71-86, Purchase 동일), 상세 화면·getById API 없음(salesAccountingSlipApi.ts=list/create/post, routes/index.tsx:1178-1208=목록·신규뿐). totalVatAmount 렌더…'}, {'type': 'workflow_agent', 'index': 10, 'label': 'verify:11 — 연결 후 금액 변경 시 삭제 후 재', 'phaseIndex': 2, 'phaseTitle': 'Verify', 'agentId': 'afdbe893dfd5699b3', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786198332541, 'queuedAt': 1786198330181, 'attempt': 1, 'lastToolName': 'StructuredOutput', 'lastToolSummary': '단정 확인. SalesAccountingSlipController 매핑 3개(GET:24/PO…', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786198994799, 'tokens': 119059, 'toolCalls': 54, 'durationMs': 662258, 'resultPreview': '{"refuted":false,"reason":"단정 확인. SalesAccountingSlipController 매핑 3개(GET:24/POST:36/POST /{slipNo}/post:52), Purchase 동일 — PUT/PATCH/DELETE 0. @DeleteMapping 8건 인용 위치 그대로, 매출/매입전표 대상 0. voidSlip(126-129/125-128) src/main 호출자 0. Service 는 list:29/createDraft:49/post:78 뿐. FE 함수 3개(185/213/223), 행 액션은 DRAFT \'전기\' 하나(Page:92-102), FE 타입(:7)에 VOIDED 없음. DailyClosingService:336,348·MonthEndClose:262,29…'}, {'type': 'workflow_agent', 'index': 11, 'label': 'synthesis', 'phaseIndex': 3, 'phaseTitle': 'Synthesis', 'agentId': 'aa475ff416b59d60d', 'model': 'claude-opus-5[1m]', 'state': 'done', 'startedAt': 1786199000771, 'queuedAt': 1786198996376, 'attempt': 1, 'lastToolName': 'Bash', 'lastToolSummary': 'docker exec samhan-postgres psql -U samhan -d slip_db -tA -…', 'promptPreview': '당신은 Samhan Public 모노레포의 **읽기 전용 분석자**입니다.\n\n작업 트리: C:/dev/Samhan-Public\n\n## 절대 규칙\n- 코드 수정 금지 · 커밋·push 금지 · 저장소에 파일 생성 금지(보고는 응답으로)\n- DB 쓰기 금지. SELECT 만\n- Docker 재기동·재배포 금지 — 다른 트랙이 같은 스택을 씁니다\n- Gradle/npm 전체 빌드 금지\n- 평문 비밀번호·JWT 출력 금지\n\nDB 조회 예: docker exec samhan-postgres psql -U samhan -d slip_db -tA -c "<SELECT ...>"\n  (회계는 accounting_db · 품목은 product_db · 거래처는 partner_db 일 수 있습니다. 먼저 확인하십시오)\n\n##…', 'lastProgressAt': 1786199472945, 'tokens': 75176, 'toolCalls': 13, 'durationMs': 472174, 'resultPreview': '# 명세 대비 현행 차집합 종합 — 판매/구매전표 ↔ 회계전표 ↔ 세금계산서 ↔ 원장\n\n조사 범위: `C:/dev/Samhan-Public` main 트리 (읽기 전용). DB 는 **집PC 시드** 기준(2026-08-08 실측).\n적대검증에서 뒤집힌 2건(규칙 1, 규칙 7)은 **뒤집힌 쪽을 정본**으로 반영했습니다.\n아래 ✔PM표시는 이번 종합 단계에서 제가 직접 파일·DB 로 재확인한 항목입니다(릴레이 아님).\n\n---\n\n## 1. 규칙 11개 × 현행 상태\n\n| 규칙 | 상태 | 근거(파일:줄 · 테이블) | 명세와의 차이 |\n|---|---|---|---|\n| **1** 판매전표→매출전표 · 구매전표→매입전표 를 회계담당자가 생성·연결 | **부분구현** | ✔PM `web/SalesAccoun…'}]
- totalTokens: 1724533
- totalToolCalls: 700

---


