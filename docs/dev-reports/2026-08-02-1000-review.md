# #1000 슬2 1차 적대검증 — 도달성 (PR #1046)

## 검증 경계

- 확인 브랜치/HEAD: `feat/1000-model-code-rest` / `aa87d60e8`.
- PR 변경 파일 실측: product-service 구현 2개, V30 1개, 단위 테스트 1개, 보고서 3개. 화면 6개와 엑셀 3개 자체는 이 커밋에서 수정되지 않았다.
- 금지사항 준수: 코드·git·Docker·DB 쓰기 없음. 이 보고서만 생성·append한다.

## 각도 1 — 닿지 못하는 것이 생기는가

### A1-1 순번코드만 있는 품목의 응답 도달성 — 코드상 PASS, 실 DB 적용 전

① 실 사용자 경로 재현 여부: 화면 6개가 소비하는 `ProductSummaryResponse.productCode` 매핑을 코드에서 역추적했다. `model_code`가 NULL/공백이면 기존 `product_code`를 반환하므로 순번코드만 있는 활성 100건은 빈칸이 되지 않는다. 다만 실 API 호출은 아직 하지 않았다.

② 재현 명령·출력 원문:

```text
> $p='services/product-service/src/main/java/com/samhanair/logis/product/web/dto/ProductSummaryResponse.java'; ...
143:     /**
144:      * 사용자 노출 품목코드. 모델코드가 있으면 모델코드를 사용하고, 모델코드가 없는 기존 품목은
145:      * 순번코드를 fallback으로 유지해 화면·전표 조회에서 고아가 되지 않도록 한다.
150:     private static String exposedProductCode(Product p) {
151:         if (p.getModelCode() != null && !p.getModelCode().isBlank()) {
152:             return p.getModelCode();
153:         }
154:         return p.getProductCode();
155:     }
```

③ 실 데이터 영향 건수: 제공된 활성 실측 기준 100건(`model_code` 없음, `product_code` 있음). 예상 빈칸 0건.

### A1-2 V30 실 DB 적용 전 충돌 계수 — PASS

① 실 사용자 경로 재현 여부: 운영 로컬 실측 DB에 쓰지 않고 V30의 guard와 INSERT 대상 SELECT를 그대로 계수했다. 다른 품목을 가리키는 활성 alias 충돌 0건, 활성 `product_code` 자체 중복 0건이며 V30 버전도 미적용 상태다.

② 재현 명령·출력 원문:

```text
> docker exec samhan-postgres psql -U samhan -d product_db ...
 active_products | model_name_present | model_name_distinct | model_code_present | product_code_present | serial_only | both_missing
-----------------+--------------------+---------------------+--------------------+----------------------+-------------+-------------
            1220 |               1220 |                1220 |               1120 |                  100 |         100 |            0

 v30_cross_product_alias_conflicts
-----------------------------------
                                 0

 active_product_code_duplicate_groups | affected_rows
--------------------------------------+--------------
                                    0 |             0

 v30_rows_to_insert
-------------------
                100

 installed_rank | version | description | success
----------------+---------+-------------+--------
(0 rows)  -- version IN ('27','28','29','30')
```

③ 실 데이터 영향 건수: V30 신규 alias 예상 100건, 충돌 0건, 중복 영향 0건. 실제 migration 실행은 금지사항에 따라 하지 않았다.

### [BLOCKER] A1-3 노출 키가 `model_name`이 아니라 `model_code → product_code` — 순번코드 100건에 옛 코드 잔존

① 실 사용자 경로 재현 여부: **재현됨(응답 생성 경로 + 실 DB 조인 없는 전건 계수).** 화면이 표시하는 `ProductSummaryResponse.productCode`는 요구 계약인 `model_name`을 전혀 읽지 않고 `model_code`, 없으면 `product_code`를 반환한다. 따라서 시트 계보 1,120건은 우연히 `model_code = model_name`이라 새 코드처럼 보이지만, 이카운트 계보 100건은 모델명이 아니라 옛 순번코드가 그대로 보인다. 빈칸은 아니지만 “노출용 품목코드를 모델명과 같게” 및 화면별 동일 값 요구를 위반한다.

② 재현 명령·출력 원문:

```text
> ProductSummaryResponse.java lines 150-155
150: private static String exposedProductCode(Product p) {
151:     if (p.getModelCode() != null && !p.getModelCode().isBlank()) {
152:         return p.getModelCode();
153:     }
154:     return p.getProductCode();
155: }

> docker exec samhan-postgres psql -U samhan -d product_db ...
 active_products | model_name_present | model_name_distinct | model_code_present | product_code_present | serial_only | both_missing
            1220 |               1220 |                1220 |               1120 |                  100 |         100 |            0
```

③ 실 데이터 영향 건수: 활성 1,220건 중 100건(8.20%)이 새 모델명 대신 옛 순번코드를 노출한다. 빈칸 0건, 요구값 불일치 100건.
