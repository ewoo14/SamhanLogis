연관 Issue: #896, #1114

## 트랙 개설 — 착수 전 확인

2026-07-22 등록. PR #996(슬4)이 머지됐고 **남은 범위**가 있어 이슈가 열려 있습니다.

### 개발책임자 지시 누적
- 2026-08-07: *"옵션은 견적품목 등록 시 거기서 설정한 **각 품목 별 라벨(특징)**로 설정하도록. **모든 옵션 및 수량 동기화 동일**"*
- *"여기서 말하는 옵션은 **웹 종합견적서 및 주문서**를 말함. **그 외는 없애는게 맞음**"*
- `#1114`(리모컨/실외기 표기) 흡수 — 같은 표기 축

### 정찰이 실측할 것
1. PR #996 이 **무엇을 전달했고 무엇이 남았는지**
2. 지금 **하드코딩된 옵션 전수** — 이슈 본문에 목록이 있으나 현행 코드에서 다시 세십시오
3. `BundleExpander.java:398` 의 `isRemote` 정규식이 프런트 라벨과 어긋나는 지점(#1114)
4. 🚨 **금액에 닿는지** 판정 — 닿으면 정밀 경로입니다