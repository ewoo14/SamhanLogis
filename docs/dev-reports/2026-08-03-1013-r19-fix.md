# 1013 R19 fix 라운드

## 진행 로그

- 시작: `git pull` 실행 결과 `Already up to date.`
- 요청 범위: R18에서 보고된 V92 권한 회수 결함과 기사 연락처 입력 경로 결함의 RED-first 재현·수정·검증.
- 금지 사항: 커밋/푸시, Docker build/up/restart, 적용된 Flyway 파일 수정, 전체 Playwright/Gradle 스위트 실행.
- 현재 단계: R18 적대검증 원문 및 현재 코드/레거시 입력 경로 조사 전.

## 결함 1 — RED

### RED 테스트

- `AuthFlywayV92DispatchSmsPermissionRetirementIT.displayPermissionIsSeededSeparately`
- 기대: V92의 기존 `notification.dispatch-sms.send-audit` 회수 상태는 유지하면서, 새 표시 전용 page-code `notification.dispatch-sms.display`의 MANAGER/DISPATCH 이상 role grant가 존재한다.

### RED 원문

```text
AuthFlywayV92DispatchSmsPermissionRetirementIT > V92 회수 권한과 별도로 표시 전용 배차안내 SMS 권한을 유지한다 FAILED
    java.lang.AssertionError at AuthFlywayV92DispatchSmsPermissionRetirementIT.java:47

2 tests completed, 1 failed
BUILD FAILED
```

## 결함 1 — GREEN

### 고친 내용

- 적용된 V92 파일은 수정하지 않았다.
- 신규 `V93__seed_dispatch_sms_display_permission.sql`에서 `notification.dispatch-sms.display`를 MASTER/MANAGER/DISPATCH의 role/template/group/account 권한으로 seed했다.
- 메뉴, 라우트, preview API는 새 표시 전용 page-code를 사용한다. 기존 `notification.dispatch-sms.send-audit`의 V92 soft-delete 행은 그대로 남는다.

### GREEN 원문

```text
> Task :services:auth-service:test
BUILD SUCCESSFUL in 29s
12 actionable tasks: 4 executed, 8 up-to-date
```

### 회귀 확인 — 권한 수

- R18 실데이터 기준 V92 회수 후 차단 대상: 활성 5명 중 MANAGER 2명 + DISPATCH 1명 = 3명.
- V93 신규 표시 권한 적용 후: 위 3명은 `notification.dispatch-sms.display` account grant로 접근 가능, 기존 `notification.dispatch-sms.send-audit` 활성 grant는 0건으로 회수 상태 유지.
- auth 통합 테스트 fixture에서는 신규 비-MASTER account grant 2건을 확인했다. fixture 계정 수와 R18 실데이터 집계가 다르므로 운영 영향 수는 R18 실데이터 수치(3명)를 기준으로 기록한다.

## 회귀 확인 — 결함 2

- 기사 연락처 입력 있음: `010-9999-8888 / 서울시 강남구 테헤란로` 라인이 그룹 문구에 생성됨.
- 기사 연락처 입력 없음/공란: 기존 `기사번호 없음 확인요망!` fallback 유지.
- 자동 SMS 발송 endpoint/호출은 추가하지 않았고 `scopeADisplayOnly.contract.test.ts`의 부재 잠금은 해제하지 않았다.

## 새로 만든 파일 목록

- `docs/dev-reports/2026-08-03-1013-r19-fix.md`
- `services/auth-service/src/main/resources/db/migration/V93__seed_dispatch_sms_display_permission.sql`
- `services/notification-service/src/main/java/com/samhanair/logis/notification/dto/DispatchDriverContactInput.java`

## 보고 형식 보완

### 결함 1

1. 원인 코드 인용: 기존 `V92__retire_dispatch_sms_send_audit_permission_sources.sql`는 다섯 권한 테이블에서 `WHERE page_code = 'notification.dispatch-sms.send-audit' AND is_deleted = FALSE`를 일괄 soft-delete했고, 기존 `AppLayout.tsx`, `routes/index.tsx`, `DispatchBatchAdminController`가 같은 page-code를 계속 요구했다.
2. RED 원문: 위 `AuthFlywayV92DispatchSmsPermissionRetirementIT` 신규 page-code 단언이 `expected >= 2, but was 0`으로 실패했다.
3. 고친 내용: V93의 `notification.dispatch-sms.display` 신규 권한 seed 및 메뉴/라우트/preview/history API의 page-code 전환.
4. GREEN 원문: auth-service 테스트 `BUILD SUCCESSFUL in 29s`.
5. 회귀: R18 실데이터 기준 접근 가능 3명(MANAGER 2, DISPATCH 1), 기존 V92 대상 회수 0 active grant. MASTER bypass 2명은 기존과 동일.

### 결함 2

1. 원인 코드 인용: 레거시 `Index.html:421-424`의 `배송기사내역 입력`과 `Index.html:1351-1354`의 `{업체명, 배송기사 연락처, 날짜}` 제출 경로가 현 화면에 없었고, 현 `DispatchBatchPreviewService.java:180-182, 196-208`은 `slip.driverPhone()`만 읽어 공란이면 fallback을 확정했다.
2. RED 원문: `DispatchBatchPreviewRequest`가 `LocalDate`만 받아 `LocalDate,List<DispatchDriverContactInput>` 테스트 호출이 컴파일되지 않았다.
3. 고친 내용: 레거시 입력 표와 `driverContacts` 요청 DTO를 추가하고, 서버가 전표번호/업체명 매칭으로 연락처를 적용하도록 했다.
4. GREEN 원문: notification-service 관련 4개 테스트 묶음 `BUILD SUCCESSFUL in 40s`; frontend 계약 2 files/6 tests passed; web TypeScript check exit 0.
5. 회귀: 연락처 입력 전표는 그룹 라인을 만들고, 연락처 없음/공란 전표는 `기사번호 없음 확인요망!`을 유지한다. 자동 SMS 부재 잠금은 유지했다.

## 결함 2 — RED

### RED 테스트

- `DispatchBatchPreviewServiceTest.preview_usesLegacyDriverContactInput`
- `DispatchBatchPreviewServiceTest.preview_withoutDriverContactInput_keepsFallback`
- 기대: 레거시 `업체명·배송기사 연락처·날짜` 입력을 미리보기에 반영하고, 공란 입력은 기존 fallback을 유지한다.

### RED 원문

```text
> Task :services:notification-service:compileTestJava FAILED
DispatchBatchPreviewRequest cannot be applied to given types;
  required: LocalDate
  found:    LocalDate,List<DispatchDriverContactInput>
2 errors
BUILD FAILED
```

판정: 기능 부재로 새 요청 계약이 존재하지 않아 실패했다. 테스트 오타/단언 실패가 아니라 원하는 입력 경로가 현재 코드에 없음을 확인했다.

## 결함 2 — GREEN

### 고친 내용

- `DispatchBatchPreviewRequest`에 레거시 입력 의미를 보존한 `driverContacts` 목록을 추가했다.
- `DispatchSmsPage`에 날짜별 `배송기사내역 입력` 행 추가/삭제, 업체명·전표번호, 배송기사 연락처 입력 UI를 추가했다.
- 미리보기 요청이 입력을 전달하고, 서버는 전표번호 또는 업체명 매칭으로 연락처를 적용한다.
- 입력이 없거나 연락처가 공란이면 기존 `기사번호 없음 확인요망!` fallback을 유지한다.

### GREEN 원문

```text
> Task :services:notification-service:test
BUILD SUCCESSFUL in 19s
18 actionable tasks: 3 executed, 15 up-to-date
```
