# PR #1057 라이브QA 5차

- 일시: 2026-08-03
- 표식: `QA-874-R5-`
- 실행 계정: `dev_manager` (MANAGER)
- 기준 커밋: `bdf563b74`
- 범위: 수락된 출고 전표 `2026/08/03-6` 회계 매출전표 배분·전기 및 시나리오 2~5

## 진행 로그

- [시작] `git pull` 완료. 기존 실 전표·마스터·설정은 수정·삭제하지 않음.

## 배분·전기 시도

- 실 GUI에서 `dev_manager`로 `/accounting/sales-slips/new` 진입.
- 수락된 `2026/08/03-6`의 기본 구성품 4행을 배분율 100%로 입력하고 메모 `QA-874-R5-POST-AC060`을 입력함.
- 화면 실측 합계: 공급가 `1,739,100`, 부가세 `173,910`, 합계 `1,913,010`.
- 저장 POST는 서버에서 차단됨. 원문:

  ```text
  HTTP 403 POST /admin/sales-slips
  {"success":false,"code":"FORBIDDEN","message":"[SP-PO-1] 동적 권한 deny — page=accounting.sales-slip.accounting action=CREATE role=UNKNOWN reason=account permission missing","data":null,"timestamp":"2026-08-03T12:57:25.555772325Z"}
  ```

- 같은 세션의 `/auth/admin/permissions/my` 실응답에서 `accounting.sales-slip.accounting: []` 확인.
- 따라서 회계 매출전표 생성·배분·전기 미실행, `POSTED` 모델축 행 수 실측 불가(0이라고 단정하지 않음).
- `dev_master` 전환, DB 직접 INSERT, 권한/코드 수정은 실행하지 않음.

## 시나리오 2~5

| 시나리오 | 판정 | 캡처 | 사유 |
|---|---|---|---|
| 2. R15 숨겨진 불일치 | 미실시 | 없음 | 선행 배분·전기 차단 |
| 3. R17 과잉 불일치 | 미실시 | 없음 | 선행 배분·전기 차단 |
| 4. 순서 무관성 | 미실시 | 없음 | 선행 배분·전기 차단 |
| 5. 목록 항목 클릭 | 미실시 | 없음 | 검증 표 POSTED 행 미생성 |

## 결함

- 재현: `dev_manager` 로그인 → `/accounting/sales-slips/new` → `2026/08/03-6` 기본 4행 100% 배분 → 임시저장.
- 결과: 위 원문 403. `dev_manager` 계정에 `accounting.sales-slip.accounting` CREATE 권한이 없어 이번 라운드 임무를 시작할 수 없음.
- 이 현상은 이번 QA에서 새로 수정하지 않았으며, 사용자 지시대로 권한/DB/코드 우회는 하지 않음.

## 캡처

- `docs/qa/874-riusage-global-dc-real-qa/r5-01-allocation-form.png`
- `docs/qa/874-riusage-global-dc-real-qa/r5-02-saved.png` (403 후 화면; 저장 실패)
