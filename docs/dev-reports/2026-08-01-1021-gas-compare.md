# #1021 거래처 원천 GAS 대조

- 조사 위치: `C:/dev/Samhan-Public/.claude/worktrees/t1021`
- 조사 브랜치: `feat/1021-partner-source-gas`
- 안전 조건: 코드·Git·Docker·실 DB 쓰기 없이 조사하며, 본 보고서만 누적 기록한다.

## 원천 특정

두 현행 원천은 다음과 같다.

1. `estimate-app`가 읽는 **`partner-service`** 원천 — 실 DB는 `partner_db.partners`의 기본 할인율 계열이다.
2. `order-app`가 읽는 **`dc-config-service`** 원천 — 실 DB는 `dc_config_db.dc_configs`의 홈·상업 전역DC와 옵션 정액·단위처리 계열이다.

대조 기준인 레거시 GAS 원천은 `tools/legacy-gas/종합견적서/Code.js`가 거래처코드로 조회하는 Notion **`거래처별 DC리스트`**이다.

근거: Issue #1021 본문은 `estimate-app → partner-service`, `order-app → dc-config-service`를 명시한다. PR #1029의 GitHub issue/review comment API는 현재 모두 0건이며, PR 본문과 `docs/dev-reports/2026-08-01-1021-partner-source-gas-compare.md`가 레거시 원천과 기존 집계를 기록한다.
