# PR #1174 CODEX LUNA 5.6 fix4 보고

작성일: 2026-08-12  
대상: PR #1174 / 현재 HEAD `8765a82b6`  
범위: SOL 재검토4의 유일한 BLOCK 결함 수정

## 결론

`WEB_DEPLOY_REAL_QA`에 신규 BrowserRouter real-QA 스펙을 등록했다. 지정 focused guard와 desktop 전체 테스트를 재실행했고, 모두 통과했다.

## 원인 확인 및 전수 판정

`WEB_DEPLOY_REAL_QA`는 `vite.web.config.ts`의 BrowserRouter 배포 하네스를 사용하는 real-QA 스펙 목록이다. 목록에 등록된 스펙은 hash 경로가 아니라 BrowserRouter 경로를 사용해야 하며, G6가 스펙의 `BrowserRouter` 자기 진술과 목록을 대조한다.

PR #1174에서 변경된 real-QA 표면을 전수 확인했다.

| 변경 파일 | 판정 |
|---|---|
| `1163-fix3-real-qa/1163-fix3-live-real-qa.spec.ts` | `build:web 는 BrowserRouter` 자기 진술과 `history.pushState()`를 사용하므로 등록 대상 |
| `1163-uuid-history-leak-real-qa/1163-r4-readonly-live-ui-real-qa.spec.ts` | 기존 fix1 스펙의 디렉터리 rename. BrowserRouter 자기 진술이 없고 `url.hash` 기반 hash 경로이므로 등록 대상 아님 |
| `1163-fix3-real-qa/1163-fix3-real-qa.config.ts` | Playwright config이며 real-QA 스펙 목록 대상 아님 |

따라서 목록에는 아래 한 경로만 추가했다.

```text
1163-fix3-real-qa/1163-fix3-live-real-qa.spec.ts
```

변경 파일: `clients/desktop/src/renderer/test-utils/harness-false-green-guard.test.ts`

## 검증 원문

### 수정 전 focused 재현

```text
npx vitest run src/renderer/test-utils/harness-false-green-guard.test.ts --reporter=verbose

FAIL G6: 파일이 스스로 BrowserRouter 하네스라고 밝히면 WEB_DEPLOY_REAL_QA 에도 등재돼 있다
1163-fix3-real-qa/1163-fix3-live-real-qa.spec.ts
Test Files  1 failed (1)
Tests       1 failed | 61 passed (62)
```

### focused 재실행

```text
npx vitest run src/renderer/test-utils/harness-false-green-guard.test.ts --reporter=verbose

Test Files  1 passed (1)
Tests       62 passed (62)
```

### desktop 전체 재실행

```text
npm test

MUTATION_RED_EIGHT
01-direct.tsx=RED
02-alias-template.tsx=RED
03-concat.tsx=RED
04-print-csv.tsx=RED
NewExit.tsx=RED
NewActorRecordResponse.java=RED
NewActorResponse.java=RED
NotificationBodyFactory.java=RED
✔ all actor display reads are resolver-bound
ℹ tests 5
ℹ pass 5
ℹ fail 0
ℹ skipped 0

Test Files  255 passed (255)
Tests       2221 passed | 2 skipped (2223)
```

`npm test` exit code: `0`  
desktop 전체 failed: `0`

## RED-B 보존

- 공격 8형 전부 RED, 정상 resolver 구성 GREEN.
- `actorId` 역추적·화면/API UUID 비노출 축을 건드리지 않았다.
- `변경자 미상`, `SYSTEM → 시스템`, V102, 격리 real-QA 6화면 UUID 0을 변경하지 않았다.
- Git 조작(브랜치 변경, stage, commit, push)은 수행하지 않았다.
