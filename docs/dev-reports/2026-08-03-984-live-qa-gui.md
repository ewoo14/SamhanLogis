# PR #984 이카운트 임포트 라이브 GUI QA

- 일시: 2026-08-03 KST
- 범위: 실 데스크톱 GUI에서 이카운트 품목 임포트와 기존 품목 조회 경로 확인
- 시작 상태: `git pull` 실행, `Already up to date.`

## 진행 로그

### 시작

- PM 배포본을 재배포하지 않음.
- 실 GUI 화면 캡처만 증거로 수집 예정.

### 브라우저 연결

- 인앱 브라우저 연결 시도 결과: `No browser is available`.
- 대체로 합성 이미지/API 응답을 증거로 사용하지 않고, 저장소의 실 데스크톱 renderer QA 실행 경로를 확인 중.

### renderer 기동 확인

- 현재 worktree에는 `clients/desktop/node_modules`가 없어 renderer 기동이 불가했다.
- 기존 5175 listener는 다른 worktree(`t874`, HEAD `907577317`)의 renderer였으므로 PR #984 증거로 사용하지 않음.
- 현재 worktree의 소스 기준 실 GUI를 띄우기 위해 의존성 설치를 시도할 예정.

### renderer 첫 기동

- Vite `:5176`는 포트를 열었으나 `@samhan/design-system`의 `dist/index.js` 부재로 의존성 스캔 오류가 발생함.
- 배포 스택에는 접근하지 않고 renderer 로컬 실행에 필요한 디자인 시스템 산출물만 준비함.

### 실 GUI 진입 경로 확인

- renderer 실행: `VITE_MOCK_MODE=0`, `VITE_API_BASE_URL=http://127.0.0.1:8080`, `VITE_APP_VERSION=2026/08/03-98400`, 현재 worktree `:5176`.
- 로그인: `dev_master` HTTP 200 후 실 renderer 화면 진입.
- 발견 경로: 좌측 메뉴 `기초품목 관리` → `#/products/catalog`.
- 캡처: `docs/qa/984-ecount-import-real-qa/_local/01-entry-product-catalog.png`
- 화면에는 `품목 등록`, 모델명 검색, 품목 목록이 보였으나 이카운트 파일 업로드/임포트 버튼은 보이지 않음.

### 이카운트 임포트 경로 확인

- 직접 `#/admin/ecount/reimport` 진입 결과: 화면에 원문 `404 페이지를 찾을 수 없습니다`와 `요청하신 페이지가 없거나 주소가 변경되었습니다.`가 표시됨.
- 캡처: `docs/qa/984-ecount-import-real-qa/_local/02-direct-ecount-route.png`
- 따라서 실 사용자가 화면에서 `itemFile`/`relationFile`을 선택할 수 있는 GUI 진입점이 현재 renderer에 없어 XLSX 거부/CSV 정상 임포트/결과 요약/임포트 후 상세 반영은 실시하지 못함.

### 기존 품목 조회 회귀 확인

- `기초품목 관리` 화면에서 검색 입력에 `00003`을 넣고 `조회`를 누르는 시나리오를 시도했으나 Playwright 조작이 제한시간 내 완료되지 않아 캡처를 만들지 못함.
- 미실시 원문: `검색 조작 실행이 120초 제한시간 내 완료되지 않아 중단됨.`

## 항목별 판정

| 항목 | 판정 | 근거 |
|---|---|---|
| 1. 임포트 화면 진입 | **FAIL** | `01-entry-product-catalog.png`, `02-direct-ecount-route.png`; 품목 관리 화면은 진입되지만 임포트 UI 없음, 직접 경로는 404 |
| 2. 관계 XLSX 422 메시지 화면 표시 | **미실시** | 임포트 GUI 진입점 없음. API/JSON/터미널은 증거로 사용하지 않음 |
| 3. 관계 CSV 정상 임포트·결과 요약 | **미실시** | 임포트 GUI 진입점 없음 |
| 4. 목록·상세 코드/모델명 반영 | **미실시** | 임포트 성공 자체를 수행하지 못함 |
| 5. 기존 조회·검색·자동완성 차단 여부 | **미실시** | 검색 조작이 120초 내 완료되지 않아 캡처 없음; 임포트 후 회귀는 수행하지 못함 |

## 발견 결함

### GUI 임포트 진입점 부재 (BLOCKER)

재현:

1. `dev_master`로 데스크톱 renderer에 로그인한다.
2. 좌측 메뉴에서 `기초품목 관리`를 선택한다.
3. 화면에 품목 목록·검색·등록만 있고 이카운트 파일 임포트 컨트롤이 없다.
4. `#/admin/ecount/reimport`로 직접 접근하면 `404 페이지를 찾을 수 없습니다`가 표시된다.

영향: 사용자는 API를 직접 호출하지 않는 한 PR #984의 관계 XLSX 거부 안내, CSV 임포트, 결과 건수, 임포트 후 품목 반영을 수행할 수 없다. 이번 라운드의 2~5번은 결함 없음으로 처리하지 않고 미실시로 남긴다.
