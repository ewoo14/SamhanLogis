# #1000 순번코드 사용자 노출 사용처 조사

- DB 실측: `product_db.public.products`의 순번코드 컬럼은 `product_code`이며, 공백이 아닌 값은 100 / 전체 1,226건이다(값 범위 `010001`~`010100`, 모두 6자리).
- DB 실측: 같은 테이블의 `model_code`는 공백이 아닌 값이 1,126 / 전체 1,226건이다.
- 화면 실측: 품목코드(`productCode`)를 사용자에게 렌더링하거나 품목코드 입력 자리로 노출하는 라우트 파일은 6개 — `CompensationFailuresPage.tsx`, `InventoryAuditDetailPage.tsx`, `InventoryDpsComparePage.tsx`, `SafetyStockAlertsPage.tsx`, `warehouse/DpsByProductPage.tsx`, `warehouse/InventoryStockBalancePage.tsx`.
- 인쇄·엑셀 실측: 품목코드를 직접 출력하는 인쇄 파일은 0개이고, 엑셀/CSV 내보내기 파일은 3개 — `services/inventory-service/src/main/java/com/samhanair/logis/inventory/service/StockExcelExportService.java`, `clients/desktop/src/renderer/api/excelExportMock.ts`, `clients/desktop/src/renderer/routes/InventoryDpsComparePage.tsx`.
