# #1092 S8 최종 라이브QA — PR #1121 슬라이스 1

## 1. 종합 판정

**PASS — 범위 내 결함 0건.**

- S7 수정 후 주문서 4건 중 4건에 작성일이 표시됐다.
- 통합 목록 56건(종합견적서 52 + 주문서 4)은 표시 작성일 내림차순 위반 0건이며, 계열 전환이 4회 발생해 계열별 뭉침이 아니라 실제 교차 병합됐다.
- 화면 표시 작성일과 화면이 실제 호출한 두 API의 응답 값 불일치는 0건이었다.
- RED-B, 통합 ON 기간 필터, `작성자`/금지 용어/빈값/UUID 계약은 모두 PASS다.

## 2. 환경과 실제 네트워크 확인

1. 검증 HEAD는 `7d95a1917d340410c08129a32d4c902787fbd87c`다.
2. PM이 S7 HEAD 코드로 `partner-order-service`를 재빌드·재배포해 배포본과 HEAD가 일치한다고 명시한 뒤 검증했다. QA 시작·종료 시 `samhan-partner-order-service`는 `127.0.0.1:18088 -> 8088`, `samhan-slip-service`는 `127.0.0.1:18086 -> 8086`으로 각각 `running / healthy`였고 전체 24개 컨테이너가 running이었다.
3. 공유 Docker 스택은 중지·재기동하지 않았다. 특히 compose 파일을 다시 올리지 않았다.
4. 프런트는 `C:\dev\Samhan-Public\.claude\worktrees\t1092\clients\desktop`를 cwd로 삼아 `VITE_MOCK_MODE=0`, `VITE_API_BASE_URL=http://127.0.0.1:8080`, strict port `5196`으로 실행했다.
5. 저장소 Playwright Chromium을 `headless: true`, 1600×1000 viewport로 실행했다. 캡처는 실제 DOM과 실제 서버 응답이며 합성하지 않았다.
6. `dev_master`로 실제 로그인했다. 자격은 QA credential resolver로 읽었고 비밀번호는 `<redacted>`로만 취급했다.
7. 화면이 실제 호출한 네트워크 응답:
   - `GET /slips/estimates?page=0&size=10000` → **200**, 52건.
   - `GET /api/v1/partner-orders?page=0&size=10000` → **200**, 4건.
   - 주문 응답 4건의 `submittedAt`은 모두 non-null이었다: `2026/08/07-1 = 2026-08-07T19:35:02.280728`, `2026/07/30-1 = 2026-07-30T01:12:23.867626`, `2026/06/08-1982 = 2026-06-08T02:09:16.873267`, `2026/06/08-1980 = 2026-06-08T02:09:05.396744`.
   - 이 4개 값이 각 주문서 행의 화면 작성일과 모두 일치했다. S7의 `createdAt` fallback이 실제 배포 응답에 반영됐음을 네트워크와 화면 양쪽에서 확인했다.

원시 계측 요약은 `docs/qa-shots/1092-s8-live-qa/results.json`에 있다. 평문 비밀번호와 사용자 가시 UUID는 기록하지 않았다.

## 3. A — 주문서 작성일과 두 계열 교차 정렬

| 항목 | 발화 조건 건수 | 실제 관찰 | 판정 |
|---|---:|---|---|
| 주문서 작성일 표시 | 주문서 4건 | 4/4 행에 작성일 표시 | PASS |
| 두 계열 행 보존 | 견적 52 + 주문 4 | 통합 56 = 52 + 4 | PASS |
| API 응답과 표시값 대조 | 통합 56건 | 불일치 0건 | PASS |
| 표시 작성일 내림차순 | 통합 56건 | 인접 순서 위반 0건 | PASS |
| 교차 병합 | 양 계열 모두 0건 초과 | 계열 전환 4회 | PASS |

교차 경계의 대표 순서는 다음과 같다.

1. 주문 `2026/08/07-1`(2026-08-07 19:35) 뒤에 2026-08-07 종합견적서들이 이어졌다.
2. 2026-08-05 종합견적서 뒤에 주문 `2026/07/30-1`, 그 뒤에 2026-07-17 종합견적서들이 이어졌다.
3. 2026-07-16 종합견적서들 뒤에 2026-06-08 주문 2건이 이어졌다.

따라서 계열별로 한 덩어리씩 붙인 결과가 아니다. 목록 정렬에 사용된 표시값과 API 값도 56/56 동일했다. 증거는 `02-unified-written-dates-and-sort.png`다.

## 4. B — RED-B 무훼손 실제 클릭

| 항목 | 발화 조건 건수 | 실제 클릭·관찰 | 판정 |
|---|---:|---|---|
| 복원 버튼 | 전체 삭제 견적 1,993건 중 복원 가능 4건 | 삭제 포함 2페이지의 복원 가능 4행에 버튼 4개. 첫 버튼을 실제 클릭했고 URL은 유지됐다. DB 변경 방지를 위해 해당 POST만 409로 응답시켰으며 한국어 danger 배너(`rgb(153, 27, 27)`)가 표시됐다. | PASS |
| 복원 불가행 버튼 미노출 | 전체 1,989건, 확인 페이지 44건 | 확인 페이지의 복원 불가 44행에 복원 버튼 0개 | PASS |
| 삭제 배지·취소선·행 클릭 차단 | 확인 페이지 삭제행 48건 | 배지 48개, `line-through` 48행. 삭제행을 실제 클릭해도 URL 불변 | PASS |
| 삭제행 토글 | 활성 52, 삭제 1,993, 포함 합계 2,045 | ON에서 41페이지, OFF에서 활성 목록으로 복귀 | PASS |
| 2페이지에서 OFF 후 첫 활성 페이지 | 삭제 포함 2/41 | OFF 후 `1 / 2`, 첫 문서 `2026/08/07-21`로 기준 첫 페이지와 일치 | PASS |
| 통합 ON 기간 시작·종료 | 2026-08-07 견적 20 + 주문 1 | 두 날짜를 실제 입력. 양 API 200, 화면 21건 | PASS |
| 거래처명 필터 | `부산냉난방테크` 13건 | 실제 입력 후 13건, 불일치 0건 | PASS |
| 신규 견적 | 버튼 1개 | 실제 클릭 후 `#/sales/estimates/new` 이동 | PASS |
| 활성행 상세 | 활성 52건(첫 화면 50건) | 첫 행 실제 클릭 후 `#/sales/estimates/<내부 식별자>` 이동, 상세 가시 UUID 0건 | PASS |
| 한쪽 API 실패 | 실패 대상 주문 4, 유지 대상 견적 52 | 독립 새 페이지에서 주문 `size=10000` GET만 503 주입. 견적 GET은 실제 gateway 200. 화면에 견적 52건 유지, 주문 0건, 부분 실패 배너 표시 | PASS |

복원 클릭 배너·버튼 노출·삭제행 상태는 `06-restore-click-and-unavailable-hidden.png`, 토글 OFF 후 복귀는 `07-deleted-off-first-active-page.png`, 부분 실패는 `10-one-api-failure-other-list-remains.png`에 보존했다.

## 5. C — 앞 라운드 PASS 회귀

- 통합 표 헤더: `구분 · 문서번호 · 거래처 코드 · 거래처 · 작성자 · 작성일 · 합계 · 상태` — **PASS**.
- 통합 화면 가시 텍스트 `담당` — **0건**.
- 통합 표에서 정확히 `—`인 셀 — **0건**.
- 목록·클릭 상세의 가시 UUID 정규식 일치 — **0건**.
- 통합 ON 기간 필터 — 견적 20 + 주문 1 = 화면 21건, 양 API 200 — **PASS**.

상단 기존 견적 표의 `partnerBusinessNo`·`validUntil` `—`는 브리프대로 선재·범위 밖이므로 계수와 결함 판정에서 제외했다.

## 6. 신규 파일 목록

신규 보고서 1개:

- `docs/dev-reports/2026-08-08-1092-s8-final-live-qa.md`

신규 실제 Playwright headless 캡처 10장:

1. `docs/qa-shots/1092-s8-live-qa/01-baseline.png`
2. `docs/qa-shots/1092-s8-live-qa/02-unified-written-dates-and-sort.png`
3. `docs/qa-shots/1092-s8-live-qa/03-unified-period-filter.png`
4. `docs/qa-shots/1092-s8-live-qa/04-unified-partner-filter.png`
5. `docs/qa-shots/1092-s8-live-qa/05-deleted-toggle-badge-strikethrough.png`
6. `docs/qa-shots/1092-s8-live-qa/06-restore-click-and-unavailable-hidden.png`
7. `docs/qa-shots/1092-s8-live-qa/07-deleted-off-first-active-page.png`
8. `docs/qa-shots/1092-s8-live-qa/08-new-estimate-route.png`
9. `docs/qa-shots/1092-s8-live-qa/09-active-row-detail.png`
10. `docs/qa-shots/1092-s8-live-qa/10-one-api-failure-other-list-remains.png`

신규 계측 파일 1개:

- `docs/qa-shots/1092-s8-live-qa/results.json`

QA 실행용 임시 Playwright 파일과 Vite 로그·PID 파일은 종료 전에 삭제했으며 신규 파일 목록에 남기지 않았다.

## 7. 프로세스·데이터 안전

- DB 직접 INSERT/UPDATE/DELETE를 하지 않았다. DB 직접 SELECT도 필요하지 않아 실행하지 않았다.
- 복원 성공으로 공유 데이터를 바꾸지 않았다. 실제 버튼 클릭은 브라우저에서 해당 POST만 409로 차단했다.
- Docker 스택을 중지·재기동하지 않았다.
- 이 라운드가 띄운 Playwright 브라우저·컨텍스트를 닫고 Vite와 자식 프로세스를 종료했다. 종료 후 `5196` LISTENING 0건을 확인했다.
- 제품 코드 수정, 커밋, push를 하지 않았다.

## 8. 이 라운드가 보지 않은 것

- 실제 복원 성공으로 DB 상태가 바뀌는 경로는 실행하지 않았다. 버튼 노출·전파 차단·실패 배너만 검증했다.
- 반대 방향인 견적 API 실패 시 주문서만 남는 경우는 실행하지 않았다.
- 두 계열 합산 10,000건 경계, 성능·장시간 안정성, Electron 패키징 앱은 보지 않았다. t1092 renderer 전용 Vite 실화면만 검증했다.
- 화면 로드 중 기존 `SalesSubNav` 중복 React key 경고가 발생했지만, 본 슬라이스 사용자 계약·화면 동작 범위가 아니어서 원인과 영향은 조사하지 않았다.
