# PR #1059 R18 적대검증 — 실 사용자 도달 결함 검증

- 대상: `feat/1013-dispatch-inherit`
- 요청 HEAD: `a44dbcc71`
- 판정 범위: 실 사용자 경로로 재현 가능한 결함, 또는 제공된 증거의 무결성 불일치
- 제외: 검증 품질, 테스트 강도, 문서 표현, 가드·mock 품질
- 변경 금지: 소스 수정, commit, push, Docker 조작

## 시작 기록

- `git pull`: `Already up to date.`
- 검증 상태: 완료 — 실 사용자 도달 결함 2건

## 각도 1 — 정상 사용자 차단 여부

### 결함 1

① 한 줄 요약: V92가 새 표시·복사 화면과 API가 계속 사용하는 `notification.dispatch-sms.send-audit` 권한을 전 계층에서 삭제하여, 정상 대상인 MANAGER·DISPATCH 사용자를 화면에서 차단한다.

② 실 사용자 재현 절차:

1. 현재 HEAD `a44dbcc71`의 auth-service를 V92까지 마이그레이션한다.
2. 활성 MANAGER 또는 DISPATCH 계정으로 데스크톱 앱에 로그인한다.
3. 사이드바 `배차`에서 `배차안내 SMS`를 찾는다. 메뉴가 보이지 않는다.
4. `/#/arologis/dispatch-sms`로 직접 진입한다. `PermissionGuard`가 `/`로 돌려보낸다.
5. 같은 계정으로 `POST /admin/notifications/dispatch-batch/preview`를 호출하면 `@RequirePermission(page="notification.dispatch-sms.send-audit", action=CREATE)`에서 거부된다.

③ 관측 원문:

- `V92__retire_dispatch_sms_send_audit_permission_sources.sql`: `role_page_permissions`, `role_page_permission_templates`, `group_page_permissions`, `account_page_permissions`, `account_permission_overrides`의 동일 `page_code` 활성 행을 모두 soft-delete한다.
- `AppLayout.tsx`: `dynamicCanAccess('notification.dispatch-sms.send-audit', 'view')`가 메뉴 가시성을 결정한다.
- `routes/index.tsx`: `/arologis/dispatch-sms`가 동일 page-code의 VIEW 권한으로 보호된다.
- `DispatchBatchAdminController`: 미리보기 API도 동일 page-code CREATE 권한을 요구한다.
- 읽기 전용 실데이터 집계:
  - DISPATCH: 활성 1, 현재 VIEW 1, V92 적용 후 VIEW 0
  - MANAGER: 활성 2, 현재 VIEW 2, V92 적용 후 VIEW 0
  - MASTER: 활성 2, 현재 VIEW 2, V92 적용 후 MASTER bypass 2
  - 현재 동일 page-code 활성 행: role 11, template 11, group 9, account 25, override 0 — V92 적용 시 모두 0

④ 영향 건수: 표시 대상 활성 계정 5명 중 비-MASTER 3명(MANAGER 2, DISPATCH 1)이 차단된다.

## 각도 2 — 레거시 하차일별 그룹 문구 대조

### 직접 대조 결과

- `tools/legacy-gas/배차안내문자/Index.html:1154-1188` 원문을 직접 읽었다.
- 문구 생성 규칙은 현 `DispatchMessageGroupComposer`와 일치한다.
  - 1차 키: 단톡방 `R_` → 인수자번호 `P_` → 단건 `N_`
  - 하차일: 숫자 오름차순
  - section: `<일>일 하차 건 배송기사님 연락처를 안내드립니다.`
  - 라인: 배송기사 연락처 + ` / ` + 배송주소 앞 3토큰
  - 헤더: `AI 삼성무풍 시스템에어컨 배차실입니다.`
  - 단톡방 미매핑 문구: `※출하창고 상황에 따라 지연될 수 있음을 양해 부탁드립니다.`
  - 오류행: `기사번호 없음 확인요망!`, `이카운트 데이터 없음 최신화요망!`, `발송금지 업체입니다.`는 그룹 문구에서 분리
- 레거시는 `Code.js:321-327`에서 사용자가 `배송기사내역 입력` 화면에 넣은 연락처를 찾고, `Code.js:379`에서 실제 라인을 만든다.
- 현 화면 입력은 날짜뿐이며, 현 서비스는 `OutboundSlipDto.driverPhone`이 비면 즉시 `기사번호 없음 확인요망!` fallback을 확정한다.

### 결함 2

① 한 줄 요약: 현 표시 경로에는 레거시의 배송기사 연락처 입력 경로가 없고 실데이터 `driver_phone`도 대부분 비어 있어, 하차일별 그룹 문구 생성기가 존재해도 실제 사용자는 오류 문구만 받는다.

② 실 사용자 재현 절차:

1. MANAGER 또는 DISPATCH 권한 문제를 우회할 수 있는 MASTER 계정으로 `배차안내 SMS` 화면에 진입한다.
2. 날짜 `2026-08-03`을 선택하고 `미리보기`를 누른다.
3. 출고전표 4건의 textarea 값을 확인한다.
4. 네 건 모두 레거시 형식의 `<기사연락처> / <주소 앞 3토큰>` 및 하차일별 section 대신 `기사번호 없음 확인요망!`으로 표시된다.
5. 화면에는 레거시의 `배송기사내역 입력`에 해당하는 보정 입력 경로가 없어 사용자가 그룹 문구를 생성할 수 없다.

③ 관측 원문:

- 레거시 `Code.js:379`: `driver_phone ? (driver_phone + ' / ' + truncated_display) : '기사번호 없음 확인요망!'`
- 현 `DispatchBatchPreviewService:180-182`: `driverPhone`이 없으면 `effectiveFallback = "기사번호 없음 확인요망!"`
- 읽기 전용 실데이터 집계:
  - `2026-08-03`: 활성 OUTBOUND 4건, `driver_phone` 누락 4건, `unload_date` 누락 4건
  - 전체: 활성 OUTBOUND 2,307건, `driver_phone` 누락 2,288건, `unload_date` 누락 2,300건

④ 영향 건수: 지정 QA 날짜의 사용자 조회 결과 4/4건. 저장된 전체 활성 OUTBOUND 기준 동일 위험 2,288/2,307건.

## 각도 3 — 표시·편집·선택·복사 실제 동작

- 현재 HEAD 렌더러를 `VITE_MOCK_MODE=1`로 로컬 기동했다. Docker는 조작하지 않았다.
- 실제 Chromium 사용자 이벤트로 `배차` 메뉴 → `배차안내 SMS` → 미리보기 → 첫 textarea 편집 → 첫 체크박스 선택 → `선택 복사` 클릭 → 클립보드 재읽기를 수행했다.
- 관측값: SMS 메뉴 visible=true, 선택 1건, CopyButton enabled, 클립보드 길이 43, 편집 문자열 `R18-EDIT-COPY` 포함=true.
- 집중 실행: `dispatchSmsClipboard.test.ts`, `DispatchSmsPage.test.ts` — test file 2/2, test 4/4 통과.
- 실데이터의 동일 전표가 복수 단톡방 mapping으로 중복되어 `slipNo` 선택 key가 충돌하는지 읽기 전용 집계했다. 복수 mapping partner code 0, 복수 mapping name 0, 영향 OUTBOUND 0건이었다.
- 판정: 표시·편집·선택·복사 UI 이벤트 자체에서 추가 사용자 도달 결함은 재현되지 않았다. 실데이터 본문 값은 각도 2 결함의 영향을 받는다.

## 각도 4 — 자동 SMS 부활 여부

- 실행 코드 검색 결과 `/admin/notifications/dispatch-batch/send`, `sendDispatchBatch`, `DispatchBatchSendService`, `DispatchBatchSendRequest`, `DispatchBatchSendResponse`는 0건이다.
- `DispatchBatchAdminController`의 HTTP mapping은 `POST /preview` 1개뿐이다.
- `DispatchSmsPage`의 네트워크 경로는 preview와 저장내역 save/list/detail/latest뿐이며 SMS/Aligo 발송 호출이 없다.
- `scopeADisplayOnly.contract.test.ts`: test file 1/1, test 5/5 통과.
- 판정: 자동 SMS가 되살아난 사용자 경로는 재현되지 않았다.

## 증거 무결성

- 현재 HEAD: `a44dbcc7177bb7f303cfc1173a5a2586a17d2691` — 요청 인용과 일치.
- 현재 로그에서 직접 확인: R12 `d25ade812`, R14 `49c511b98`, R15 `bb4fa04a7`, R16 `6be4ee69f`, R17 `a44dbcc71` — 모두 인용과 일치.
- `git pull`: `Already up to date.`
- 인용된 “V92 인가 3층 통일”은 구현 존재 자체는 확인되지만, 동일 page-code를 새 화면이 계속 소비하므로 각도 1의 사용자 차단 결함이 있다.

## 최종 판정

- 실 사용자 경로로 재현 가능한 결함: 2건.
- 결함 1: V92 적용 후 정상 MANAGER·DISPATCH 3계정 화면/API 차단.
- 결함 2: 실데이터 기사 연락처 공급 단절로 QA 날짜 4/4건이 하차일별 그룹 문구 대신 오류 fallback.

## 이 라운드가 보지 않은 것

- Docker build/up/restart 및 현재 HEAD 서비스 컨테이너 재배포는 수행하지 않았다.
- 전체 652 Playwright와 전체 Gradle 스위트는 수행하지 않았다.
- 외부 Aligo 운영계정·실 SMS vendor 호출은 A안에 따라 수행하지 않았다.
- 운영 DB는 조회하지 않았다. 영향 건수는 현재 로컬 실데이터(auth_db/slip_db/notification_db)에 대한 읽기 전용 집계다.
- 레거시 파일 업로드부터의 전체 브라우저 실행은 수행하지 않았고, 지정 원문 `Index.html:1154-1188` 및 그 입력값 생성부 `Code.js`를 직접 대조했다.
