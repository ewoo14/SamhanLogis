# PR #1059 적대검증 R11 — 제거 후 참조 재수렴 검증

## 조사 고정

- 기준 작업 디렉토리: `C:/dev/Samhan-Public/.claude/worktrees/t1013b`
- 기준 HEAD: `f2029a336` (`[FEAT] #1013 A안 범위 축소 — 자동 SMS 발송 제거, 표시·편집·복사만 계승`)
- 시작 시 `git status --short` 출력: `?? docs/dev-reports/2026-08-03-1013-r11-reconvergence.md` — 본 보고서만 신규 파일.
- 이번 라운드 질문: **제거가 남긴 구멍이 있는가** 한 가지뿐이다. 레거시 문구 병합 규칙, 성능, 전체 스위트, 리팩터링은 보지 않는다.
- 변경 금지: 코드 수정, Git 변경 명령, 공유 DB write/DDL, V91 적용, Docker 이미지 재빌드, 실제 SMS 발송, 전체 스위트 실행.

## 1. 회수 권한 코드 참조 검색 — 1차 결과

### 확인 명령

```powershell
rg -n --hidden --glob '!**/node_modules/**' --glob '!**/build/**' --glob '!**/dist/**' "notification\.dispatch-sms\.send-audit|SEND_AUDIT|DispatchSmsSendAuditPage|dispatch-sms/send-audit|dispatchSmsSendAudit|send-audit" .
```

### 즉시 판정

- **런타임 화면·라우트·메뉴·`PageCode`의 정적 참조는 제거됐다.** 현재 `clients/desktop/src`의 정확한 권한 코드 정적 참조는 mock과 새 계약 테스트의 부재 assertion(`scopeADisplayOnly.contract.test.ts:23-24`)뿐이다. 단, 권한 매트릭스는 서버가 동적으로 반환하는 template row를 표시하므로 V91의 실제 DB 계층 판정은 §6에서 정정한다.
- 그러나 **실행 가능한 과거 mock/Playwright 테스트에는 회수 권한과 삭제 경로 참조가 남아 있다.** 최소 확인 지점:
  - `qa/playwright/tests/arologis/sp-10-2-insung-quick-vendor.spec.ts:179`
  - `clients/desktop/playwright/877-opus-r3-a/ct-route-sweep-real-qa.spec.ts:47`
  - `clients/desktop/playwright/sp-d3-slip-dispatch-permission-migration/sp-d3-slip-dispatch-permission-migration.spec.ts:290,292,315,337,348,371,397,555,574,576,625,638,642,1033`
  - `clients/desktop/playwright/sp-09-2-aligo-sms-real-send/sp-09-2-aligo-sms-real-send.spec.ts:100,103-107,157,259-260,303`
  - `clients/desktop/playwright/sp-08-3-4-dispatch-sms-history/sp-08-3-4-dispatch-sms-history.spec.ts:129-139,151`
  - `clients/desktop/playwright/full-qa/pagecodes.json:19`
  - `clients/desktop/playwright/menu-5category-real-qa/menu-5category-real-qa.spec.ts:108`
  - `clients/desktop/playwright/permission-groups-c5-followup/permission-groups-c5-followup.spec.ts:85`
- 이는 사용자 런타임 빈 화면 결함과는 별개지만, 해당 선택 QA를 실행하면 삭제 화면/권한/발송 버튼을 계속 기대하므로 **검증 계약이 현재 제품과 재수렴하지 않은 결함 후보**다. 실제 실패 출력은 아직 재현하지 않았으므로 이 시점 분류는 **미판정**이다.
- 과거 migration seed(`services/auth-service/src/main/resources/db/migration/V7__add_role_page_permissions.sql:85,99,113,127,141,155` 등)와 `SEND_AUDIT` enum/history schema는 V91 soft-delete 및 과거 데이터 판독 보존을 위해 남길 수 있는 참조다. 각각의 런타임 영향은 뒤에서 분리 판정한다.

### 결함 R11-01 — mock 권한 매트릭스와 저장내역 mock이 제거 전 상태를 계속 제공

- **파일:줄**
  - 권한 catalog: `clients/desktop/src/renderer/api/mock.ts:18437-18450`
  - MANAGER/DISPATCH view 기본값: `clients/desktop/src/renderer/api/mock.ts:18637-18644`, `18696-18701`
  - DISPATCH edit 기본값: `clients/desktop/src/renderer/api/mock.ts:18877-18882`
  - 위 catalog가 실제 mock 권한 셀/그룹 템플릿으로 확장되는 곳: `clients/desktop/src/renderer/api/mock.ts:18966-18975`, `19219-19230`
  - mock 권한 API가 그 셀을 그대로 반환하는 곳: `clients/desktop/src/renderer/api/mock.ts:13605-13625`
  - 삭제된 발송 감사 데이터/상세/필터 응답: `clients/desktop/src/renderer/api/mock.ts:11541-11669`
- **사용자 조작**: mock 모드에서 MASTER가 권한 매트릭스 또는 권한 그룹 상세를 연다. 또는 배차문자 저장내역 API를 `mode=SEND_AUDIT`로 조회하는 과거 QA를 실행한다.
- **잘못된 결과**: 회수한 `notification.dispatch-sms.send-audit` 행이 여전히 권한 관리 데이터에 나타나며 MANAGER/DISPATCH에 view, DISPATCH에 edit가 켜진다. 저장내역 mock도 제품에서 더 생성하지 않는 SEND_AUDIT 3건을 계속 반환한다. 실 서버(V91 적용 후)와 mock이 서로 다른 제품 계약을 제공한다.
- **재현 명령**:

```powershell
rg -n "notification\.dispatch-sms\.send-audit|SEND_AUDIT|DispatchSmsSendAuditPage|send-audit" clients/desktop/src/renderer/api/mock.ts
```

- **출력 원문**:

```text
11541:    // SP-09-2: SEND_AUDIT mock 데이터 3건 (날짜별, 결과 혼합) — DispatchSmsSendAuditPage 시연용.
11578:      id: 'dispatch-sms-history-send-audit',
11579:      saveMode: 'SEND_AUDIT',
11588:      id: 'dispatch-sms-history-send-audit-2',
11589:      saveMode: 'SEND_AUDIT',
11598:      id: 'dispatch-sms-history-send-audit-3',
11599:      saveMode: 'SEND_AUDIT',
11646:      // SP-09-2: ID 별 SEND_AUDIT 상세 조회 — 3건 mock 지원
11647:      if (id === 'dispatch-sms-history-send-audit-2') {
11650:      if (id === 'dispatch-sms-history-send-audit-3') {
11653:      if (id === 'dispatch-sms-history-send-audit' || url.includes('send-audit')) {
11663:      // SP-09-2: SEND_AUDIT 전용 baseRows 3건 포함
18450:  'notification.dispatch-sms.send-audit',
18644:    'accounting.general-ledger', 'notification.dispatch-sms.send-audit',
18697:    'notification.dispatch-sms.send-audit', 'dispatch.board', 'dispatch.external-carriers',
18878:    'notification.dispatch-sms.send-audit', 'dispatch.board',
```

- **판정**: 도달 가능한 mock/QA 결함. 운영 사용자가 보는 실 서버 권한 매트릭스는 soft-delete 필터 여부를 뒤에서 DB/API 코드로 별도 확인한다.

## 2. 삭제 화면 링크·네비게이션과 직접 진입

### 런타임 링크/라우트 판정

| 확인 지점 | 파일:줄 | 결과 |
|---|---|---|
| 화면 import/route | `clients/desktop/src/renderer/routes/index.tsx:176`, `1012-1021` | 삭제 화면 import는 없고 보존 화면 `/arologis/dispatch-sms`만 `dispatch.batch:view`로 등록됨 |
| 배차 메뉴 활성 경로 | `clients/desktop/src/renderer/components/AppLayout.tsx:1482-1497` | 삭제된 `/send-audit` 없음 |
| 배차안내 메뉴 링크 | `clients/desktop/src/renderer/components/AppLayout.tsx:1525-1533` | 보존 화면 `/arologis/dispatch-sms`만 링크 |
| 비관리자 미매칭 catch-all | `clients/desktop/src/renderer/routes/index.tsx:1700-1703` | `NotFoundPage`로 수렴 |
| 404 사용자 문구 | `clients/desktop/src/renderer/routes/NotFoundPage.tsx:28-80` | `404 / 페이지를 찾을 수 없습니다 / 요청하신 페이지가 없거나 주소가 변경되었습니다.`와 대시보드 버튼 표시 |

- `clients/desktop/src`에서 테스트를 제외하고 삭제 경로/컴포넌트를 검색한 결과, `mock.ts:11541`의 설명 주석 외 런타임 링크는 0건이었다.
- 따라서 사용자가 과거 북마크나 외부 링크로 `/#/arologis/dispatch-sms/send-audit`에 직접 진입하면 **빈 화면이나 권한 오류가 아니라 한국어 404 화면**이 뜬다. 제거된 화면으로 보내는 현행 메뉴 링크는 없다.

### 선택 계약 테스트 원문

명령:

```powershell
cd clients/desktop
npm test -- --run src/renderer/routes/scopeADisplayOnly.contract.test.ts
```

출력 원문(ANSI 장식 제외):

```text
RUN  v2.1.9 C:/dev/Samhan-Public/.claude/worktrees/t1013b/clients/desktop
✓ src/renderer/routes/scopeADisplayOnly.contract.test.ts (3 tests) 1ms
Test Files  1 passed (1)
Tests  3 passed (3)
```

### 남은 구멍

- 현행 런타임 링크는 없지만, 실행 가능한 과거 Playwright spec들이 삭제 경로로 직접 이동한다. 대표 위치는 `clients/desktop/playwright/sp-d3-slip-dispatch-permission-migration/sp-d3-slip-dispatch-permission-migration.spec.ts:625-642`, `clients/desktop/playwright/sp-09-2-aligo-sms-real-send/sp-09-2-aligo-sms-real-send.spec.ts:103-107,157-303`, `clients/desktop/playwright/877-opus-r3-a/ct-route-sweep-real-qa.spec.ts:47`이다.
- 이들은 실행 시 404를 삭제 화면의 접근 실패로 판정하거나, 이미 삭제된 DOM을 기다리게 된다. 아직 브라우저 실행으로 실패 원문을 얻지 않았으므로 **QA 결함 미판정**으로 유지한다. 운영 사용자 도달 링크 결함은 확인되지 않았다.

## 3. 제거 엔드포인트 호출 잔존

### 런타임 클라이언트 판정

- `clients/desktop/src/renderer/api/dispatchSmsApi.ts:9,98`에는 `previewDispatchBatch`와 `/admin/notifications/dispatch-batch/preview`만 남아 있다.
- `clients/desktop/src/renderer/api/mock.ts:11686-11687`에도 preview handler만 남아 있다.
- `services/notification-service/src/main/java/com/samhanair/logis/notification/controller/DispatchBatchAdminController.java:25` 이하에는 `/preview`만 남아 있다.
- 다음 검색에서 런타임 클라이언트 호출은 0건이고, 부재 assertion·과거 QA·README만 검출됐다.

```powershell
rg -n "sendDispatchBatch|dispatch-batch/send|DispatchBatchSend" clients services qa scripts
```

```text
clients\desktop\src\renderer\routes\scopeADisplayOnly.contract.test.ts:11:    expect(pageSource).not.toContain('sendDispatchBatch')
clients\desktop\src\renderer\routes\scopeADisplayOnly.contract.test.ts:13:    expect(apiSource).not.toContain('/admin/notifications/dispatch-batch/send')
clients\desktop\playwright\sp-08-3-dispatch-parity\sp-08-3-dispatch-parity.spec.ts:189:    expect(sources).toContain('/admin/notifications/dispatch-batch/send')
services\notification-service\README.md:57:| `POST /admin/notifications/dispatch-batch/send` | 동일 table append | `DISPATCH_SMS` | `SEND_AUDIT` |
```

- 따라서 현행 사용자 화면은 제거 endpoint를 호출하지 않으며, **404가 사용자 화면에 표시되는 경로도 없다.** 외부/구버전 클라이언트가 직접 POST하면 endpoint 미등록에 따른 404가 되지만 이번 HEAD의 화면 동작은 아니다.

### 결함 R11-02 — 과거 정적 Playwright 계약이 삭제된 `/send`를 필수 endpoint로 계속 요구

- **파일:줄**: `clients/desktop/playwright/sp-08-3-dispatch-parity/sp-08-3-dispatch-parity.spec.ts:152-198`, 특히 `:188-190`.
- **사용자 조작**: 개발자/CI가 이 선택 QA를 실행한다.
- **잘못된 결과**: 제품의 올바른 A안 제거 상태를 회귀로 오판해 QA가 red가 된다.
- **재현 명령**:

```powershell
cd clients/desktop
npx playwright test playwright/sp-08-3-dispatch-parity/sp-08-3-dispatch-parity.spec.ts --grep "현재 API/route 소스" --reporter=line
```

- **출력 원문(핵심 원문, ANSI 장식 제외)**:

```text
Running 1 test using 1 worker
[1/1] [chromium] › playwright\sp-08-3-dispatch-parity\sp-08-3-dispatch-parity.spec.ts:152:3 › SP-08-3-1 배차 legacy GAS DB/API parity 기반 잠금 › 현재 API/route 소스가 6개 기존 endpoint를 유지하고 SP-08-3-2 arologis history endpoint를 허용한다
1) [chromium] › ...:152:3 › ...
Error: expect(received).toContain(expected) // indexOf
Expected substring: "/admin/notifications/dispatch-batch/send"
```

- **판정**: 재현된 QA 계약 결함. 실제 endpoint 호출 잔존 결함은 0건이다.

## 4. 표시·편집·복사 경로 추적

### 실제 연결 경로

1. 화면은 `clients/desktop/src/renderer/routes/DispatchSmsPage.tsx:155-174`에서 `previewDispatchBatch(date)`를 호출한다.
2. 클라이언트는 `clients/desktop/src/renderer/api/dispatchSmsApi.ts:94-101`에서 `POST /admin/notifications/dispatch-batch/preview`를 호출한다.
3. 컨트롤러는 `services/notification-service/src/main/java/com/samhanair/logis/notification/controller/DispatchBatchAdminController.java:40-44`에서 `previewService.preview(req)`를 반환한다.
4. preview service는 `DispatchBatchPreviewService.java:73-107`에서 **전표 한 건마다** `MessageTemplateService.renderDispatchMessage(slip)`을 호출해 단톡방에 넣는다.
5. 화면은 매핑된 건을 `DispatchSmsPage.tsx:402-444`의 textarea에 표시·편집한다.
6. 화면은 `DispatchSmsPage.tsx:197-219`에서 현재 `edited` 문구를 clipboard row로 만들고, `dispatchSmsClipboard.ts:22-29`에서 선택 행을 `거래처명\t전표번호\t문구\t단톡방` TSV로 직렬화한다. `DispatchSmsPage.tsx:389-395`의 `CopyButton`이 그 문자열을 복사한다.

즉, **서버가 올바른 병합 문구를 만들어 준다는 전제에서는 매핑 행의 편집값이 복사까지 이어지는 배선은 살아 있다.** 그러나 그 전제가 현재 코드에서 성립하지 않는다.

### 결함 R11-03 — 보존하기로 한 하차일별 그룹 문구 생성기가 실제 경로에 없다

- **파일:줄**
  - 입력 DTO에 `unloadDate`·배송기사 연락처가 없음: `services/notification-service/src/main/java/com/samhanair/logis/notification/client/OutboundSlipDto.java:30-38`
  - slip-service 응답 parser도 `slipDate/scheduledAt/deliveryAddress/lines/recipientPhone`만 읽음: `RestClientSlipServiceClient.java:71-84`
  - 실제 생성 본문은 `[배차안내] / 거래처 / 시간 / 주소 / 품목`: `MessageTemplateService.java:46-56`
  - preview는 전표별 메시지를 생성해 방에 넣을 뿐 하차일/방/인수자번호 단위 병합 없음: `DispatchBatchPreviewService.java:73-110`
  - 기존 BE 테스트도 구 템플릿을 정답으로 고정: `services/notification-service/src/test/java/com/samhanair/logis/notification/service/MessageTemplateServiceTest.java:29-91`, `DispatchBatchPreviewServiceTest.java:82`
  - 새 FE 테스트는 생성기를 호출하지 않고 원하는 문구를 직접 fixture에 넣음: `clients/desktop/src/renderer/routes/DispatchSmsPage.test.ts:5-19`
- **사용자 조작**: `/arologis/dispatch-sms`에서 날짜를 고르고 `미리보기`를 누른다. 같은 단톡방/인수자번호에 여러 전표와 여러 하차일이 있는 정상 표본을 본다.
- **잘못된 결과**: `N일 하차 건 배송기사님 연락처를 안내드립니다.`로 묶인 문구가 만들어지지 않는다. 전표별 `[배차안내]` 구 템플릿이 각각 표시되고, 복사 버튼도 그 잘못된 전표별 문자열을 복사한다. 입력 DTO 단계부터 하차일·기사 연락처가 없어 현 경로에서는 올바른 문구를 만들 수 없다.
- **재현 명령**:

```powershell
rg -n "record OutboundSlipDto|LocalDate slipDate|LocalDateTime scheduledAt|String deliveryAddress|recipientPhone|renderDispatchMessage|sb\.append|for \(OutboundSlipDto|new PartnerEntry" services/notification-service/src/main/java/com/samhanair/logis/notification/client/OutboundSlipDto.java services/notification-service/src/main/java/com/samhanair/logis/notification/service/MessageTemplateService.java services/notification-service/src/main/java/com/samhanair/logis/notification/service/DispatchBatchPreviewService.java
```

- **출력 원문**:

```text
services/notification-service/src/main/java/com/samhanair/logis/notification/service/DispatchBatchPreviewService.java:73:        for (OutboundSlipDto slip : slips) {
services/notification-service/src/main/java/com/samhanair/logis/notification/service/DispatchBatchPreviewService.java:75:            String message = messageTemplateService.renderDispatchMessage(slip);
services/notification-service/src/main/java/com/samhanair/logis/notification/service/MessageTemplateService.java:46:    public String renderDispatchMessage(OutboundSlipDto slip) {
services/notification-service/src/main/java/com/samhanair/logis/notification/service/MessageTemplateService.java:51:        sb.append("[배차안내]\n");
services/notification-service/src/main/java/com/samhanair/logis/notification/service/MessageTemplateService.java:52:        sb.append("거래처: ").append(safe(slip.partnerName())).append('\n');
services/notification-service/src/main/java/com/samhanair/logis/notification/service/MessageTemplateService.java:53:        sb.append("시간: ").append(formatTime(slip)).append('\n');
services/notification-service/src/main/java/com/samhanair/logis/notification/service/MessageTemplateService.java:54:        sb.append("주소: ").append(truncateAddress(slip.deliveryAddress())).append('\n');
services/notification-service/src/main/java/com/samhanair/logis/notification/service/MessageTemplateService.java:55:        sb.append("품목:\n");
services/notification-service/src/main/java/com/samhanair/logis/notification/client/OutboundSlipDto.java:30:public record OutboundSlipDto(
services/notification-service/src/main/java/com/samhanair/logis/notification/client/OutboundSlipDto.java:34:        LocalDate slipDate,
services/notification-service/src/main/java/com/samhanair/logis/notification/client/OutboundSlipDto.java:35:        LocalDateTime scheduledAt,
services/notification-service/src/main/java/com/samhanair/logis/notification/client/OutboundSlipDto.java:36:        String deliveryAddress,
services/notification-service/src/main/java/com/samhanair/logis/notification/client/OutboundSlipDto.java:38:        String recipientPhone) {
```

- **선택 FE 테스트 결과**: `npm test -- --run src/renderer/routes/DispatchSmsPage.test.ts`는 `1 passed`지만, 이 테스트는 생성/화면을 통과하지 않고 문자열 helper에 이미 완성된 하차일 문구를 직접 공급하므로 결함을 검출하지 못한다.
- **판정**: 사용자 도달 기능 결함. 자동 SMS 제거 과정에서 “보존”으로 선언한 핵심 경로가 실 구현과 재수렴하지 않았다.

### 결함 R11-04 — 미매핑 행은 “확인·편집·복사” 안내와 달리 문구를 표시·편집하지 못함

- **파일:줄**: `clients/desktop/src/renderer/routes/DispatchSmsPage.tsx:448-466`. 반면 실제 textarea는 매핑 행에만 있다(`:402-444`).
- **사용자 조작**: 미리보기 후 `단톡방 미매핑 전표` 행을 확인한다.
- **잘못된 결과**: 화면은 `인수자 번호와 관계없이 안내 문구를 확인·편집·복사할 수 있습니다`라고 말하지만, 미매핑 목록에는 체크박스·거래처·코드·전표번호만 있고 메시지나 textarea가 없다. 체크 후 복사는 내부의 원본 메시지를 복사하지만 사용자는 그 내용을 확인하거나 수정할 수 없다.
- **재현 명령**:

```powershell
$f='clients/desktop/src/renderer/routes/DispatchSmsPage.tsx'; $a=Get-Content -Encoding UTF8 $f; for($i=448;$i -le 466;$i++){ '{0}:{1}' -f $i,$a[$i-1] }
```

- **출력 원문**:

```text
448:          {preview.unmapped.length > 0 ? (
449:            <div style={warningBoxStyle}>
450:               <strong>단톡방 미매핑 전표 {preview.unmapped.length}건</strong> — 인수자 번호와 관계없이 안내 문구를 확인·편집·복사할 수 있습니다.
452:                {preview.unmapped.map((u) => (
453:                  <li key={`${u.partnerCode}-${u.slipNo}`}>
455:                      <input
461:                    {u.partnerName} [{u.partnerCode}] · 전표 {u.slipNo}
463:                  </li>
```

- **판정**: 사용자 도달 기능 결함. `DispatchSmsPage.test.ts`는 helper만 시험해 이 DOM 구멍을 검출하지 않는다.

## 5. 공용 알림/SMS 경로의 다른 소비자

### 변경 경계

- `NotificationService.java`의 `492815f64..f2029a336` diff는 `DispatchBatchSendService`/`SEND_AUDIT`를 언급한 Javadoc을 공용 설명으로 바꾼 것뿐이며, 실행문 변경은 0이다.
- `services/notification-service/.../adapter/{sms,push,email}`과 공용 `NotificationAdminController`, `NotificationInternalController`, `NotificationCenterService`, notification-center web controller의 변경 numstat는 모두 빈 출력(변경 0)이다.
- 삭제된 `DispatchBatchSendService`만 `sendWithGatewayResult`의 배차 전용 소비자였고, 공용 `send`, 조회, 재시도, gateway/adapter는 남아 있다.

### 소비자별 판정

| 소비자 | 실제 호출 지점 | 채널/용도 | 영향 판정 |
|---|---|---|---|
| arologis-service | `services/arologis-service/src/main/java/com/samhanair/logis/arologis/client/NotificationClient.java:20-27`, `service/DispatchService.java:180` | 독립 배차 업무의 외부전화 SMS | `/internal/notifications/send`와 `NotificationService.send`가 그대로라 영향 없음 |
| slip-service | `services/slip-service/src/main/java/com/samhanair/logis/slip/client/NotificationClient.java:28-46`; `service/external/ExternalDispatchService.java:138`; `service/dispatch/*Service.java`의 호출(`Confirm:126`, `ModificationRequest:71`, `ModificationDecision:47,72`, `CancellationRequest:67`, `CancellationDecision:68,93`); 협업 push 호출 | 외부운송사/배차 상태 SMS, 협업/보상 push | 공용 endpoint·SMS/PUSH adapter 미변경, 영향 없음 |
| accounting-service | `services/accounting-service/src/main/java/com/samhanair/logis/accounting/client/NotificationClient.java:21-30`, `collab/JournalCollabEditService.java:110` | 회계 협업 push | 영향 없음 |
| inventory-service | `services/inventory-service/src/main/java/com/samhanair/logis/inventory/client/NotificationClient.java:19,76`, `service/SafetyStockService.java:297` | 안전재고 알림 | 영향 없음 |
| partner-order-service | `services/partner-order-service/src/main/java/com/samhanair/logis/partnerorder/client/NotificationClient.java:21-30`, `collab/PartnerOrderCollabEditService.java:111` | 주문 협업 push | 영향 없음 |
| notification publisher 소비자 | `groupware-service MessageService.java`, `GroupwareApprovalCollabEditService.java:107`; `accounting-service AccountingEditRequestService.java`; `inventory-service SafetyStockService.java` | `/internal/notifications` notification-center 게시 | 별도 `NotificationCenterService` 경로이며 변경 0, 영향 없음 |
| 관리자 공용 발송/조회/재시도 | `NotificationAdminController.java:47-87` | `/admin/notifications/send`, 목록, 상세, retry | 컨트롤러·서비스 실행문 미변경, 영향 없음 |
| 데스크톱 알림센터/푸시 토큰 | `clients/desktop/src/renderer/api/notificationApi.ts:40-47`, `pushTokens.ts:25-36` | 내 알림/이력 조회, FCM token 등록·삭제 | notification-center 및 token controller 미변경, 영향 없음 |
| 공용 adapter | `adapter/sms/AligoSmsAdapter.java`, `adapter/push/FcmPushAdapter.java`, email adapter | SMS/PUSH/EMAIL 실제 gateway | 파일 변경 0, 영향 없음 |

### 선택 회귀 테스트

명령:

```powershell
.\gradlew.bat :services:notification-service:test --tests com.samhanair.logis.notification.service.NotificationServiceTest --no-daemon
```

출력 원문:

```text
> Task :services:notification-service:compileJava
> Task :services:notification-service:compileTestJava
> Task :services:notification-service:test

BUILD SUCCESSFUL in 20s
18 actionable tasks: 3 executed, 15 up-to-date
```

- **판정**: 공용 경로의 다른 소비자에서 제거로 인한 도달 가능한 결함은 확인되지 않았다. 실제 외부 SMS는 호출하지 않았고, 단위 테스트의 mock gateway만 실행했다.

## 6. V91 대상 권한의 현재 보유자와 적용 후 결과

### 권한 정본 확인

- V39는 `account_page_permissions`를 만들고(`services/auth-service/src/main/resources/db/migration/V39__account_page_permissions_overhaul.sql:63-92`), 당시 role row를 template으로 복제한 뒤(`:94-116`) 활성 계정별로 materialize했다(`:186-213`).
- 같은 migration은 `role_page_permissions`를 **deprecated**로 선언하고 집행 정본이 `account_page_permissions`로 이동했다고 명시한다(`:215-216`).
- 실제 단일 권한 check도 `AccountPermissionService.java:53-60`에서 `account_page_permissions`를 직접 조회한다.
- `/auth/admin/permissions/my`도 일반 계정이면 `bulkLoad(accountId)`를 반환하며(`PermissionAdminController.java:253-269`), `bulkLoad`는 `PageCode` enum으로 필터하지 않고 계정 row를 전부 반환한다(`AccountPermissionService.java:69-85`).
- 반면 V91은 **deprecated `role_page_permissions` 한 테이블만** soft-delete한다(`V91__retire_dispatch_sms_send_audit_permission.sql:5-12`). template, account, group permission은 건드리지 않는다.

### 읽기 전용 공유 DB 집계

실행 명령은 모두 `docker exec samhan-postgres psql -U samhan -d auth_db -c "SELECT …"` 형식의 읽기 전용 SELECT였다. V91은 적용하지 않았다.

핵심 집계 출력 원문:

```text
                   source                   | rows_or_accounts | view_holders | mutation_holders
--------------------------------------------+------------------+--------------+------------------
 account_page_permissions_active_accounts   |               25 |            3 |                1
 active_group_member_accounts               |               25 |            3 |                1
 group_page_permissions_active_groups       |                9 |            2 |                1
 role_page_permission_templates_active_rows |               11 |            3 |                2
 role_page_permissions_active_rows          |               11 |            3 |                2
(5 rows)
```

활성 보유 그룹 출력 원문:

```text
 group_name | can_view | can_create | can_update | can_delete | active_member_accounts
------------+----------+------------+------------+------------+-----------------------
 매니저     | t        | f          | f          | f          |                     2
 배차담당자 | t        | t          | t          | t          |                     1
```

나머지 7개 그룹은 이 page에 대한 모든 action이 false였고, 전체 활성 계정 placeholder row 수가 25였다.

### 결함 R11-05 — V91은 회수 대상의 deprecated 복사본만 지워 실제 사용자 권한을 회수하지 못함

- **파일:줄**
  - 잘못된 회수 대상: `services/auth-service/src/main/resources/db/migration/V91__retire_dispatch_sms_send_audit_permission.sql:5-12`
  - 정본 이전 선언: `services/auth-service/src/main/resources/db/migration/V39__account_page_permissions_overhaul.sql:186-216`
  - 실제 집행: `services/auth-service/src/main/java/com/samhanair/logis/auth/service/AccountPermissionService.java:53-60,69-85`
  - 역할 template matrix가 orphan row를 계속 반환: `AccountPermissionService.java:283-289`
  - FE는 label이 없으면 page code 원문을 표시: `clients/desktop/src/renderer/routes/PermissionMatrixPage.tsx:756,938,1391,1408,1425`
  - 제거된 PageCode로 업데이트하면 validation 거부: `AccountPermissionService.java:472-474`
- **사용자 조작**:
  1. V91 적용 후 매니저/배차담당자가 로그인해 `/auth/admin/permissions/my`를 받는다.
  2. MASTER가 권한 매트릭스를 연어 orphan 행을 보거나 토글한다.
- **잘못된 결과**:
  - 현재 활성 사용자 **3명(매니저 2, 배차담당자 1)**은 V91 이후에도 VIEW를 보유하고, 배차담당자 1명은 CREATE/UPDATE/DELETE도 보유한다.
  - 25개 활성 계정에는 이 page의 materialized row가 그대로 남아 `/my`에 orphan key가 계속 들어간다(22명은 action 빈 집합).
  - template/group row도 남아 재-materialize 시 orphan 권한이 다시 계정 정본에 공급된다.
  - 실 권한 매트릭스는 제거한 label 대신 `notification.dispatch-sms.send-audit` 원문 행을 표시할 수 있고, 사용자가 토글하면 이미 `PageCode`에서 삭제되어 `등록되지 않은 페이지 코드입니다` 오류가 난다.
  - 삭제 화면 route는 없으므로 이 3명도 과거 URL에서는 404를 보지만, 권한 회수 자체는 이뤄지지 않는다.
- **재현 명령**:

```powershell
docker exec samhan-postgres psql -U samhan -d auth_db -c "SELECT COUNT(DISTINCT app.account_id) AS active_accounts_with_row, COUNT(DISTINCT app.account_id) FILTER (WHERE app.can_view) AS active_view_holders, COUNT(DISTINCT app.account_id) FILTER (WHERE app.can_create OR app.can_update OR app.can_delete OR app.can_restore OR app.can_download OR app.can_print) AS active_mutation_holders FROM account_page_permissions app JOIN accounts a ON a.id = app.account_id AND a.enabled = TRUE AND a.is_deleted = FALSE WHERE app.page_code = 'notification.dispatch-sms.send-audit' AND app.is_deleted = FALSE;"
```

- **출력 원문**:

```text
 active_accounts_with_row | active_view_holders | active_mutation_holders
--------------------------+---------------------+-------------------------
                       25 |                   3 |                       1
(1 row)
```
- **판정**: 사용자 보유 권한 **있음**. V91은 회수 migration으로서 기능적으로 불완전하며, 공유 DB에는 적용하지 않았다.

## 7. 추가 잔존 표면 — SEND_AUDIT 생성 계약

### 결함 R11-06 — 과거 감사 이력 판독 보존을 넘어 신규 SEND_AUDIT 생성 계약도 열려 있음

- **파일:줄**
  - FE 저장 타입과 공용 저장 함수가 `SEND_AUDIT`를 여전히 생성 가능하게 노출: `clients/desktop/src/renderer/api/dispatchSmsSaveHistoryApi.ts:1-13,16-24,30-42,96-107`
  - BE enum과 service가 SEND_AUDIT append를 계속 허용: `services/notification-service/src/main/java/com/samhanair/logis/notification/domain/DispatchSmsSaveMode.java:3-14`, `service/DispatchSmsSaveHistoryService.java:29-32,48-61,172-207`
  - 권한 있는 클라이언트가 POST로 생성 가능: `controller/DispatchSmsSaveHistoryController.java:57-76`
  - 요청 DTO도 `SEND_AUDIT`를 허용: `web/dto/DispatchSmsSaveHistoryRequest.java:9-23`
  - 생성 성공을 정답으로 고정한 테스트: `services/notification-service/src/test/java/com/samhanair/logis/notification/service/DispatchSmsSaveHistoryServiceTest.java:96-108`
- **사용자 조작**: `dispatch.sms-save-history:create` 권한을 가진 클라이언트가 `/admin/notifications/dispatch-sms/history`에 `saveMode=SEND_AUDIT` payload를 POST한다.
- **잘못된 결과**: 실제 자동 SMS 발송이 제거됐는데도 신규 “발송 감사” row를 임의 생성할 수 있다. 과거 row 조회 호환을 위해 enum/GET filter를 보존하는 것과 신규 POST 허용은 별개인데 현재 분리되지 않았다.
- **재현 명령**(mock repository를 쓰는 선택 단위 테스트이며 공유 DB write 없음):

```powershell
.\gradlew.bat :services:notification-service:test --tests com.samhanair.logis.notification.service.DispatchSmsSaveHistoryServiceTest --no-daemon
```

- **출력 원문**:

```text
> Task :services:notification-service:test

BUILD SUCCESSFUL in 15s
18 actionable tasks: 1 executed, 17 up-to-date
```

- **판정**: 도달 가능한 API 잔존 결함. 과거 데이터의 읽기 호환은 유지할 수 있으나 신규 SEND_AUDIT 쓰기 계약은 A안 제거와 재수렴하지 않았다. 공유 DB POST는 금지에 따라 실행하지 않았다.

## 8. 최종 결론

### 적대검증 verdict

**BLOCK — 이 각도에서 도달 가능한 결함 6건.**

| ID | 결함 | 도달 표면 | 판정 |
|---|---|---|---|
| R11-01 | mock 권한/저장내역이 제거 전 권한·SEND_AUDIT 데이터를 계속 제공 | mock 권한 매트릭스·그룹·저장내역 QA | 결함 |
| R11-02 | 과거 정적 QA가 삭제된 `/dispatch-batch/send`를 필수로 요구 | 선택 Playwright | 재현된 결함 |
| R11-03 | 보존 선언한 하차일별 그룹 문구 생성기가 입력 DTO부터 없음 | 실제 미리보기→화면→복사 | 사용자 도달 결함 |
| R11-04 | 미매핑 행은 안내와 달리 문구 확인·편집 UI가 없음 | 실제 미리보기 화면 | 사용자 도달 결함 |
| R11-05 | V91이 deprecated role table만 지워 실제 3명의 권한을 회수하지 못함 | `/my`, 실 권한 매트릭스, 동적 check | 사용자/운영 도달 결함 |
| R11-06 | 과거 audit 읽기 보존을 넘어 신규 SEND_AUDIT POST 생성도 허용 | 저장내역 API | API 도달 결함 |

### 여섯 질문에 대한 답

| 질문 | 답 | 확인 지점 |
|---|---|---|
| 회수 권한 코드 참조 | **남아 있다.** 정적 화면/menu/PageCode에서는 빠졌지만 mock, 과거 테스트, migration, DB template/group/account 정본에 남음 | §1, §6 |
| 삭제 화면 링크/직접 진입 | 현행 메뉴·route 링크 0건. 과거 URL은 한국어 404. 과거 Playwright 직접 링크는 잔존 | §2 |
| 제거 endpoint 호출 client | 현행 런타임 client 0건. 과거 QA/README만 잔존하고 QA 1건은 실제 red 재현 | §3 |
| 표시·복사 실제 동작 | 편집값→TSV helper→CopyButton 배선은 있으나, **하차일별 문구가 생성되지 않아 핵심 경로 실패**. 미매핑 편집도 실패 | §4 |
| 공용 알림/SMS 다른 소비자 | arologis/slip/accounting/inventory/partner-order, publisher, admin, 알림센터, push token, adapter 모두 실행 코드 미변경. 선택 unit test green | §5 |
| V91 현재 보유자 | **VIEW 3명, mutation 1명.** V91 적용 후에도 그대로 남음 | §6 |

### 확인한 참조 지점 목록

| 종류 | 현재 정본/잔존 위치 | 결론 |
|---|---|---|
| 삭제 permission 정적 runtime | `AppLayout.tsx`, `routes/index.tsx`, `PermissionMatrixPage.tsx` label, `permissionsApi.ts`, `PageCode.java` | 직접 문자열 제거 |
| 삭제 permission 동적 runtime | `role_page_permission_templates`, `group_page_permissions`, `account_page_permissions`; `AccountPermissionService` | 제거 안 됨 |
| 삭제 permission mock | `clients/desktop/src/renderer/api/mock.ts:18450,18644,18697,18878` 및 확장/응답 코드 | 제거 안 됨 |
| 삭제 permission 테스트 | `full-qa/pagecodes.json`, menu-5category, permission migration, notification dynamic permission, vendor QA | 제거 안 됨 |
| 삭제 화면 runtime route/link | `routes/index.tsx`, `AppLayout.tsx` | 0건, 404 수렴 |
| 삭제 화면 QA 링크 | sp-d3, sp-09-2, 877 route sweep | 잔존, 브라우저 실패는 미판정 |
| 삭제 `/send` runtime client | `dispatchSmsApi.ts`, `mock.ts`, `DispatchBatchAdminController.java` | 0건 |
| 삭제 `/send` QA/docs | sp-08-3 parity spec, notification-service README | 잔존; QA 실패 재현 |
| 표시·편집·복사 | `DispatchSmsPage.tsx`, `dispatchSmsClipboard.ts` | 복사 배선 생존, 생성/미매핑 UI 결함 |
| 공용 알림 | `NotificationService`, internal/admin/center controllers, channel adapters, 5개 service client | 영향 없음 |
| SEND_AUDIT | FE save-history client, BE enum/service/controller/tests, mock | 읽기 호환 외 신규 쓰기까지 잔존 |

### 이번 라운드에서 보지 않은 표면

- 레거시 문구 병합 규칙의 세부 대조와 정답 재설계
- 성능/대량 데이터 처리
- 전체 Gradle/Vitest/Playwright 스위트와 CI 권위 결과
- Docker 이미지 재빌드 및 서비스 재기동
- 실제 SMS/Aligo/FCM/EMAIL 외부 발송
- 공유 DB write/DDL, V91 실제 적용, SEND_AUDIT 실제 POST
- 삭제 화면을 대상으로 한 과거 Playwright의 브라우저 실패 재현(정적 참조만 확인해 미판정)
- 실제 브라우저에서의 전체 배차문자 라이브 QA(코드 경로와 선택 단위/정적 테스트만 수행)

## 9. 신규 파일

1. `docs/dev-reports/2026-08-03-1013-r11-reconvergence.md`

코드 파일 수정, migration 적용, Git 변경 명령, 공유 DB write는 수행하지 않았다.

## 10. 제출 직전 재검증

- FE 선택 테스트 재실행: `scopeADisplayOnly.contract.test.ts` + `DispatchSmsPage.test.ts` → **2 files / 4 tests passed**.
- notification 선택 단위 테스트 재실행: `NotificationServiceTest` + `DispatchSmsSaveHistoryServiceTest` → **BUILD SUCCESSFUL in 15s**, `18 actionable tasks: 1 executed, 17 up-to-date`.
- 과거 `/send` 정적 계약 재실행 → 예상대로 **1 failed**, 원문 `Expected substring: "/admin/notifications/dispatch-batch/send"`, `PLAYWRIGHT_EXIT=1`.
- 이 결과는 전체 suite 통과를 뜻하지 않는다. 선택된 생존 경로와 잔존 계약만 검증했다.
