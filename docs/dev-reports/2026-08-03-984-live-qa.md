# PR #984 이카운트 품목 임포트 라이브 QA 보고서

- 대상 브랜치: `fix/ecount-import-model-code-merge`
- 대상 HEAD: `091a44737849b5f60a3c8f51e16feb01cf67bb30`
- 대상 PR: #984
- QA 일자: 2026-08-03 (Asia/Seoul)
- 범위: 실 `product_db` 대상 이카운트 품목 임포트 및 임포트 결과·관리 화면 검증
- 데이터 규율: 임포트 API 외 기존 실 데이터 직접 변경·삭제 금지. Docker 빌드·배포·재기동 금지.

## 진행 로그

### 시작

- 2026-08-03: 보고서 파일 생성. 이후 각 QA 단계 종료 즉시 이 문서에 append한다.

### 1. 임포트 전 카운트 — 성공

- 조작: `samhan-postgres`의 `product_db`에 `BEGIN TRANSACTION READ ONLY`로 `public.products`와 `public.product_aliases`의 전체·활성·삭제 건수를 조회했다. 직접 INSERT/UPDATE/DELETE는 하지 않았다.
- 실행 명령:

```powershell
docker exec samhan-postgres psql -U samhan -d product_db -v ON_ERROR_STOP=1 -P pager=off -c "BEGIN TRANSACTION READ ONLY; SELECT 'products' AS table_name, COUNT(*) AS total, COUNT(*) FILTER (WHERE is_deleted=FALSE) AS active, COUNT(*) FILTER (WHERE is_deleted=TRUE) AS deleted, COUNT(*) FILTER (WHERE model_name IS NOT NULL) AS model_name_present FROM public.products; SELECT 'product_aliases' AS table_name, COUNT(*) AS total, COUNT(*) FILTER (WHERE is_deleted=FALSE) AS active, COUNT(*) FILTER (WHERE is_deleted=TRUE) AS deleted FROM public.product_aliases; COMMIT;"
```

- 실측 결과:

| 테이블 | 전체 | 활성 | 삭제 | 비고 |
|---|---:|---:|---:|---|
| `products` | 3,063 | 3,061 | 2 | `model_name` 있음 3,063 |
| `product_aliases` | 2,835 | 2,835 | 0 | 삭제 alias 0 |

- 판정: **성공**. PM 사전 카운트와 일치한다.
- 캡처: 없음. DB 조회 단계이며 화면 캡처 대상이 아니다.

### 임포트 입력 고정 — 성공

- `itemFile`: `docs/migration/ecount-data/raw/품목등록.xlsx` — 데이터 2,855행 — SHA-256 `3FD1A174D1EE9E3C8AA2F303AF932E331F8EB7BA646392DB2122DDF5B77DAE52`
- `relationFile`: `docs/migration/ecount-data/raw/품목관계리스트.xlsx` — 데이터 158행 — SHA-256 `017E5FD2D5099124C5A8DCF10D9B301783A78FD15E31E3AD6FF949D779EB3517`
- `groupFile`: **미전송**. 사용자 지정 엔드포인트의 multipart 계약에는 `groupFile`이 있으나, PM이 지정한 실 입력은 위 2개 재취득 XLSX이며 `raw` 디렉터리의 구 CSV 3개는 사용하지 않았다.
- 참고 해시(사용하지 않음): `품목-Excel다운로드.csv` `02785A731FCC502D8828ADA534DF103DC79BFDBB67D84A7142825AA323CE083C`, `품목관계-Excel다운로드.csv` `00A1964DF081FEDB1E1AF270ED0110345E1F856531EB630210F6E5BA7867DE85`, `품목계층그룹-Excel다운로드.csv` `4955F2999017F37511AF3ADE552113FA30C0628B081D6B992F7D171A7CC1EB7E`.
- 검증: XLSX 내부 XML row element는 관계 161행(메타/헤더 제외 158 데이터), 등록 2,857행(헤더 제외 2,855 데이터)이었다.
- 판정: **성공**. API 실행에 사용할 파일과 SHA-256을 고정했다.
- 캡처: 없음. 실행 전 파일 준비 단계다.

### 2. 실 임포트 — 재시도 전 관찰

- 1차 조작: `http://localhost:8080/admin/products/imports/ecount`로 동일 multipart를 전송했다.
- 1차 응답 원문:

```text
IMPORT_STARTED_AT=2026-08-03 17:37:22 +09:00
{"timestamp":"2026-08-03T08:37:22.452+00:00","path":"/admin/products/imports/ecount","status":404,"error":"Not Found","requestId":"ca3067e2-354"}
HTTP_STATUS=404
CURL_EXIT_CODE=0
IMPORT_FINISHED_AT=2026-08-03 17:37:22 +09:00
```

- 확인: 컨테이너 포트 조회 결과 `samhan-product-service`가 `127.0.0.1:8084->8084`로 노출되어 있고, 이전 런북도 이 관리 endpoint를 product-service `8084`에 직접 호출한다. 게이트웨이 `8080`은 이 관리 route를 노출하지 않는다.
- 판정: **미실행(라우트 404)**. 이 시도는 `product_db`를 변경하지 않았다. 올바른 PM 배포 대상인 product-service 직접 포트 `8084`로 재시도한다.

#### 2-1. product-service 직접 호출 — 미실행(CSV 계약 오류)

- 조작: `http://localhost:8084/admin/products/imports/ecount`에 원본 XLSX 2개를 같은 multipart part로 전송했다.
- 응답 원문:

```text
IMPORT_STARTED_AT=2026-08-03 17:37:57 +09:00
{"success":false,"code":"MIG2_CSV_HEADER_MISMATCH","message":"CSV 헤더 컬럼 수 불일치 — 예상=13 실제=1 차이컬럼=[1: expected='품목코드', actual='PK\\u0003\\u0004\\u0014\\u0000\\u0000\\u0000\\b\\u0000dN\\u0003]1I�\\u0011�\\u0000\\u0000\\u0000�\\u0002\\u0000\\u0000 \\u0000\\u0000\\u0000_rels/.rels��AN�0\\u0010\\u0005ЫX�7\\u0016�\\u0010��', 2: expected='품목명', actual='<missing>', 3: expected='출하가', actual='<missing>', 4: expected='입고단가', actual='<missing>', 5: expected='싱글', actual='<missing>', 6: expected='실외기(원형,스탠드)', actual='<missing>', 7: expected='멀티(50%)', actual='<missing>', 8: expected='멀티(48%)', actual='<missing>', 9: expected='멀티(45%)', actual='<missing>', 10: expected='단품(35%)', actual='<missing>', 11: expected='품목구분', actual='<missing>', 12: expected='규격명', actual='<missing>', 13: expected='사용구분', actual='<missing>'],"data":null,"timestamp":"2026-08-03T08:37:57.622690513Z"}
HTTP_STATUS=422
CURL_EXIT_CODE=0
IMPORT_FINISHED_AT=2026-08-03 17:37:57 +09:00
```

- 판정: **미실행**. 서비스의 실제 contract는 CSV이며, 이 422 응답으로는 DB 임포트가 실행되지 않았다.

#### 2-2. 원본 XLSX의 CSV 변환 준비 — 성공

- 원본 XLSX를 읽어 제목 행과 마지막 다운로드 시각 행을 제외하고, 서비스가 요구하는 UTF-8 BOM CSV로 변환했다. 원본 XLSX 자체는 변경하지 않았다.
- 변환 결과:
  - `itemFile` 파생 업로드: `C:\Users\ewoo2\AppData\Local\Temp\samhan-pr984-ecount-qa\item-from-xlsx.csv` — CSV data rows 2,854 — SHA-256 `4ADBBFB0FBE3E1481CD2FB29424D2B2996303918E19181C99ABDEFC3A5D68DD1`
  - `relationFile` 파생 업로드: `C:\Users\ewoo2\AppData\Local\Temp\samhan-pr984-ecount-qa\relation-from-xlsx.csv` — CSV data rows 158 — SHA-256 `557A13EFA3AD00B0E72C303B00FE4A8FA80A9BFEED085AB379CF88CB0D77077E`
- 변환 검증: item CSV 헤더 13열, relation CSV 헤더 9열, `Import-Csv` 데이터 행 수는 각각 2,854·158이다. XLSX 내부 row는 item 2,857개(설명 1 + 헤더 1 + 데이터 2,854 + 시각 1), relation 161개(설명 1 + 헤더 1 + 데이터 158 + 시각 1)였다.
- 판정: **성공**. API가 요구하는 포맷으로 원본 내용을 보존한 임시 업로드 파일을 준비했다.

#### 2-3. CSV 변환본 실 임포트 — 실패

- 조작: product-service `8084`에 변환된 `itemFile`·`relationFile`을 전송했다. `groupFile`은 전송하지 않았다.
- 응답 원문:

```text
IMPORT_STARTED_AT=2026-08-03 17:40:58 +09:00
{"success":false,"code":"MIG2_ALIAS_DUPLICATE","message":"동일 alias_code 가 다른 main 에 매핑됩니다: aliasCode=AAAA-00005, sourceRowNo=124","data":null,"timestamp":"2026-08-03T08:41:02.144896326Z"}
HTTP_STATUS=409
CURL_EXIT_CODE=0
IMPORT_FINISHED_AT=2026-08-03 17:41:02 +09:00
```

- 판정: **실 임포트 실패**. 서비스가 입력 중복 계약 위반으로 중단했다. 이 응답 이후 DB 반영 여부를 단계 3의 전/후 카운트로 확인한다. 결함 수정·DB 직접 수정·재기동은 하지 않는다.

### 3. 임포트 후 카운트 — 성공(변경 없음, 임포트 실패로 rollback)

- 조작: 409 응답 직후 `product_db`를 다시 `BEGIN TRANSACTION READ ONLY`로 조회했다. `public.products`, `public.product_aliases`, `staging.ecount_item_alias`를 확인했다.
- 실측 결과:

| 대상 | 임포트 전 | 임포트 후 | 증가 | 감소 | 변경된 행 |
|---|---:|---:|---:|---:|---:|
| `public.products` 전체 | 3,063 | 3,063 | 0 | 0 | 0건 확인 |
| `public.products` 활성 | 3,061 | 3,061 | 0 | 0 | 0건 확인 |
| `public.products` 삭제 | 2 | 2 | 0 | 0 | 0건 확인 |
| `public.product_aliases` 전체 | 2,835 | 2,835 | 0 | 0 | 0건 확인 |
| `public.product_aliases` 활성 | 2,835 | 2,835 | 0 | 0 | 0건 확인 |
| `public.product_aliases` 삭제 | 0 | 0 | 0 | 0 | 0건 확인 |
| `staging.ecount_item_alias` 전체 | 확인 전 별도 계수 없음 | 2,835 | - | - | 409 이후 새 staging 행 증가 없음으로 판단 |

- 후속 확인 원문:

```text
products   total=3063 active=3061 deleted=2 model_name_present=3063
product_aliases total=2835 active=2835 deleted=0
staging.ecount_item_alias total=2835
```

- 판정: **성공(보호 확인)**. 임포트가 409에서 중단되어 전/후 카운트상 늘거나 줄거나 변경된 실데이터는 없다. 따라서 이후 단계의 “임포트 후 결과” 판정은 **미실시**로 명확히 구분한다.
- 캡처: 없음. DB 조회 단계이며 화면 캡처 대상이 아니다.
