# #1032 전표 금액 VAT 포함 통일 — 1차 적대검증

## 검증 로그

### 1. 대상·작업 제한 확인

- 현재 브랜치는 `fix/1032-slip-amount-mismatch`, HEAD는 검증 대상 커밋 `f9a7460fa`다.
- 검증 시작 시 작업트리 변경은 이 보고서 신규 파일 1개뿐이었다. 코드·Git·Docker·실 DB에는 쓰지 않았다.
- 대상 커밋은 파일 8개를 바꿨다: 표시 금액 경로의 Java 운영 코드 6개(신규 계산기 1개 포함), 테스트 1개, 구현 보고서 1개다.
- 재현 명령과 출력 원문:

```text
> git branch --show-current
fix/1032-slip-amount-mismatch

> git status --short
?? docs/dev-reports/2026-08-01-1032-review.md

> git diff --name-status f9a7460fa^ f9a7460fa
A  docs/dev-reports/2026-08-01-1032-impl.md
M  services/slip-service/src/main/java/com/samhanair/logis/slip/service/SlipCleanupService.java
M  services/slip-service/src/main/java/com/samhanair/logis/slip/service/SlipSignatureService.java
A  services/slip-service/src/main/java/com/samhanair/logis/slip/web/dto/SlipDisplayAmount.java
M  services/slip-service/src/main/java/com/samhanair/logis/slip/web/dto/SlipResponse.java
M  services/slip-service/src/main/java/com/samhanair/logis/slip/web/dto/SlipSearchResult.java
M  services/slip-service/src/main/java/com/samhanair/logis/slip/web/dto/SlipSummary.java
M  services/slip-service/src/test/java/com/samhanair/logis/slip/web/dto/SlipResponseTest.java
```

### 2. 회계 내부 계약 `SlipSummary`의 우발 변경 도달성

- `GET /internal/slips/by-period`가 회계 전표 배분 source 검색용 `SlipSummary`를 반환한다. 이번 커밋은 이 내부 계약의 라인 합계 계산도 공통 계산기로 치환했다.
- 구·신 계산이 달라지는 조건은 `supply_amount IS NOT NULL AND vat_amount IS NULL`이다. 실 DB 활성 헤더·활성 라인에서 해당 조건은 **0건**이므로 현재 실 사용자/회계 경로 도달성은 **0건**이다. 결함으로 판정하지 않는다.
- 같은 모집단의 가격 규약은 legacy `NULL` **2,758건**, `VAT_INCLUSIVE` **17건**, `SUPPLY` **1건**, 합계 **2,776건**으로 구현 보고서 수치와 일치한다.
- 재현 명령과 출력 원문:

```text
> docker exec samhan-postgres psql -U samhan -d slip_db -At -c "SELECT count(*) FROM slips s JOIN slip_lines l ON l.slip_id=s.id WHERE s.is_deleted=false AND l.is_deleted=false AND l.supply_amount IS NOT NULL AND l.vat_amount IS NULL;"
0

> docker exec samhan-postgres psql -U samhan -d slip_db -At -c "SELECT count(*) FILTER (WHERE l.unit_price_domain IS NULL), count(*) FILTER (WHERE l.unit_price_domain='VAT_INCLUSIVE'), count(*) FILTER (WHERE l.unit_price_domain='SUPPLY'), count(*) FROM slips s JOIN slip_lines l ON l.slip_id=s.id WHERE s.is_deleted=false AND l.is_deleted=false;"
2758|17|1|2776
```

### 3. 세 규약 실 DB 표본과 실행 중 실 API

- 실 DB 활성 라인 규약 3종에서 각각 기존 전표를 골랐다.
  - legacy `NULL`: 전표 `2026/01/01-1`, 거래처코드 미저장, VAT 포함 저장 합계 `119,900`.
  - `VAT_INCLUSIVE`: 기준 전표 `2026/05/20-1`, 거래처코드 `P-2026-0001`, VAT 포함 저장 합계 `2,284,500`.
  - `SUPPLY`: 전표 `2026/08/01-6`, 거래처코드 `P-2026-0007`, VAT 포함 저장 합계 `8,999,999.19`.
- 실행 중 `samhan-slip-service`의 실 목록 GET은 세 전표 모두 아직 공급가액을 반환했다: 각각 `109,000`, `2,076,816`, `8,181,818.19`. 상세 GET의 저장 공급가액+부가세 합계와 각각 `10,900`, `207,684`, `818,181` 차이다.
- ① 실 사용자 경로: **현재 실행 중 서비스에서는 재현된다.** 판매/구매 조회 화면이 쓰는 `/slips/query`가 이 값을 그대로 표시한다.
- 다만 실행 중 컨테이너는 커밋 `f9a7460fa`를 로드하지 않은 배포 전 인스턴스다. Docker 재빌드·재기동 금지 때문에 이 결과는 **기존 결함의 live 도달성**이며, PR 소스의 수정 후 HTTP 결과로 판정하지 않는다. PR 소스의 실 DB HTTP 도달성은 이 라운드에서 확인 불가다.
- 인앱 브라우저 백엔드는 사용 가능 목록이 비어 있어 실제 화면 클릭·인쇄 미리보기는 수행하지 못했다.
- ② 재현 명령의 핵심 조건: `GET http://127.0.0.1:18086/slips/query?searchSlipNo=<전표번호>&dateFrom=2026-01-01&dateTo=2026-08-01` 후 목록 응답의 내부키로 상세 GET을 호출하되, 출력에는 전표번호·거래처코드·금액만 남겼다.
- 출력 원문:

```text
2026/01/01-1||list=109000.00|lines=1|lineTotal=109000|supply=109000|vat=10900|inclusive=119900
2026/05/20-1|P-2026-0001|list=2076816.00|lines=5|lineTotal=2076816|supply=2076816|vat=207684|inclusive=2284500
2026/08/01-6|P-2026-0007|list=8181818.19|lines=1|lineTotal=8181818.19|supply=8181818.19|vat=818181|inclusive=8999999.19
```
- ③ 실 데이터 영향 건수: 활성 전표 2,345건 중 VAT가 0이 아닌 **2,344건**이 현재 live 목록에서 상세 VAT 포함 합계와 다르다. 규약별 라인은 `NULL` 2,758건, `VAT_INCLUSIVE` 17건, `SUPPLY` 1건이다.

### 4. 면세·영세율 도달성

- slip 라인에는 세율/과세유형 snapshot 컬럼이 없다. 현재 활성 상품 마스터의 `tax_type`은 `TAXABLE` **1,220건**뿐이며 `EXEMPT`/`ZERO_RATED`는 각각 **0건**이다.
- 활성 전표 중 저장 부가세 합계가 0인 전표는 `2026/07/27-47`, 거래처코드 `P-2026-0018` **1건**이지만 공급가액과 총액도 모두 0이므로 면세·영세율 전표가 아니다.
- 따라서 실 데이터 기준 면세 전표 **0건**, 영세율 전표 **0건**이며, 10% 무조건 가산으로 잘못 표시되는 실 사용자 도달 건수도 **0건**이다. 공통 계산기는 저장된 `vat_amount`를 더하므로 세율을 10%로 재계산하지 않는다.
- 출력 원문:

```text
> SELECT tax_type, count(*) FROM products WHERE is_deleted=false GROUP BY tax_type;
TAXABLE|1220

> 활성 전표별 저장 vat 합계 0/비0 집계
1|2334

> 저장 vat 합계 0인 전표
2026/07/27-47|P-2026-0018|1|0.00|0.00|0.00
```

### 5. 각도 1 — 바뀐 곳 계수

#### 실사용 산출물: 7곳

1. 판매관리 `SalesQueryPage` 금액 열 — `SlipResponse.totalAmount`.
2. 구매관리 `PurchaseQueryPage` 금액 열 — `SlipResponse.totalAmount`.
3. 그룹웨어 결재 작성의 전표 참조 선택 금액 — `SlipSearchResult.totalAmount`.
4. 그룹웨어 결재 상세의 전표 참조 선택 금액 — `SlipSearchResult.totalAmount`.
5. 공개 인수증 합계 — `SlipSignatureService`의 `PublicSignatureViewResponse.totalAmount`.
6. 전표 정리 화면 합계금액 — `SlipCleanupService`.
7. 전표 정리 CSV 합계금액 — 화면과 같은 `SlipCleanupService` 응답을 클라이언트가 직렬화.

#### 추가로 응답 계약에 닿은 곳: 2곳

8. 일반 `/slips` 목록 및 복원 응답의 `SlipResponse.totalAmount`. `SlipListPage`는 이 필드를 표시하지 않아 현재 사용자 화면 영향은 없다.
9. 회계 전표 배분 source 검색 `/internal/slips/by-period`의 `SlipSummary.LineSummary.lineTotal`. 원래도 VAT 포함 계약이었고, 실 DB에서 구·신 계산 차이 조건은 0건이다.

- 따라서 **코드 도달 경로 9곳**, 그중 **실사용 표시/파일이 바뀐 곳 7곳**이다.
- 전표 정리의 `amountZero` 정합성 플래그도 같은 합계를 사용하지만, 실 DB에서 구 공급가액 기준 0 여부와 신 VAT 포함 기준 0 여부가 달라지는 전표는 **0건**이다.

```text
> 구 total=0/신 total!=0 | 구 total!=0/신 total=0 | zero 판정 변경 합계
0|0|0
```

### 6. 각도 1 — 안 바뀐 곳 계수

검증 단위를 사용자 산출물/업무 화면별로 세면 **15곳**이 값 변경 대상 밖에 남았다.

1. 매출 회계전표 장부 — accounting-service의 저장 `supplyAmount`, `vatAmount`, `lineTotal`.
2. 매입 회계전표 장부 — 같은 S/V/총액 분리 계약.
3. 세금계산서 발행·상세·인쇄·홈택스 출력 계열 — accounting-service `TaxInvoice` 저장 S/V/total.
4. 부가세 신고서 — `ISSUED TaxInvoice`의 `supply_amount`, `vat_amount` 집계.
5. 합계잔액시산표 — POSTED/REVERSED `journal_lines` 차변·대변 집계.
6. 손익계산서 — POSTED/REVERSED `journal_lines` 계정별 차변·대변 집계.
7. 재무상태표 — 기준일까지 POSTED/REVERSED `journal_lines` 누적 잔액.
8. 일반원장 — `journal_lines`의 차변, 대변, 누적잔액.
9. 거래처별 원장 — 전표 원천 projection은 저장 S/V를 분리하고 품목 총액은 기존부터 S+V.
10. 미수금·미지급금·채권채무 현황 — `journal_lines` 계정 110/120(채권), 201/210(채무)의 차변·대변 잔액과 받을어음/수금계획.
11. 전표 목록 XLSX — 금액 컬럼 자체가 없고 전표번호·일자·유형·상태·거래처명·배송태그·처리시각만 출력.
12. 거래명세표 인쇄 — 저장 공급가액과 부가세를 분리하고 합계는 기존부터 S+V.
13. 매출전표 인쇄 — 저장 공급가액과 부가세를 분리하고 합계는 기존부터 S+V.
14. 매입전표 인쇄 — 저장 공급가액과 부가세를 분리하고 합계는 기존부터 S+V.
15. 모바일 영업 대시보드 — 기존부터 저장 `supplyAmount + vatAmount` 합산.

- ① 실 사용자 경로 도달성: 위 15곳은 커밋 변경 파일을 직접 소비하지 않거나, `SlipSummary`처럼 기존부터 VAT 포함인 별도 계약을 사용한다. 이번 커밋으로 값이 바뀌는 실 데이터 도달 건수는 확인된 범위에서 **0건**이다.
- ② 재현 근거: `git diff --name-status f9a7460fa^ f9a7460fa`에 accounting-service와 세무/재무제표/원장/인쇄/Excel 구현 파일이 0개이며, 각 서비스 소스의 데이터 원천을 역추적했다.
- ③ 실 데이터 기준 영향 건수: `SlipSummary` 우선순위 차이 0건, 전표 정리 zero 플래그 차이 0건. 나머지는 변경된 계산기를 호출하지 않으므로 0건이다.

### 7. Excel·CSV·인쇄 정합성 판정

- 판매/구매관리 화면의 전표 목록 XLSX는 동일 필터의 행을 내보내지만 **금액 컬럼이 없다**. 따라서 화면의 VAT 포함 금액과 서로 다른 금액을 내보내는 경로는 없으며, 값 불일치 도달 건수는 0건이다.
- 전표 정리 CSV는 화면과 동일한 `CleanupEntry.totalAmount`를 직렬화하므로 화면/CSV 값이 갈라지는 경로가 없다.
- 인쇄 3종은 상세와 동일하게 저장 S/V를 합산한다. 기준 전표의 인쇄 계산값은 `2,076,816 + 207,684 = 2,284,500`이다.
- 실제 브라우저 인쇄 미리보기는 브라우저 백엔드 부재로 보지 못했다. 소스 계산 경로와 실 DB 저장값까지만 확인했다.

### 8. 각도 3 — 행·합계·페이지·반올림

- 판매/구매 목록에는 금액 총계가 없다. 화면 문구는 `총 N건`이고, 각 전표 행의 `금액`만 표시한다. 따라서 “현재 페이지 금액 합계/전체 금액 합계” 의미 충돌은 도달하지 않는다.
- 각 전표 행 금액은 그 전표 라인의 표시 금액을 합산한다. 활성 2,776라인은 모두 `supply_amount`가 있어 공통 계산기의 첫 분기인 저장 `S + V`만 사용한다.
- 활성 전표 전체에서 `sum(각 라인 S+V)`와 `sum(S)+sum(V)`가 다른 전표는 **0건**, 최대 차이는 `0.00`이다. 저장 컬럼이 `NUMERIC(...,2)`이고 표시 계산은 저장값 덧셈뿐이라 이 모집단에서 행 합계≠총계 반올림 차이는 없다.
- 페이지를 넘겨도 바뀌는 것은 행 50개와 `총 N건`/페이지 번호뿐이며 금액 총계는 표시하지 않는다.
- 출력 원문:

```text
> 활성 라인의 supply_amount NULL | 그중 VAT포함단가 존재 | 둘 다 NULL
0|0|0

> 전표별 sum(라인 S+V) <> sum(S)+sum(V) 건수 | 최대 차이
0|0.00
```

### 9. 각도 4 — 구현 보고서 수치 재현

- legacy `NULL` **2,758건**, `VAT_INCLUSIVE` 17건, `SUPPLY` 1건은 같은 활성 헤더·활성 라인 SQL로 재현됐다(§2 원문).
- 기준 전표 `2026/05/20-1`의 실 DB 저장값은 공급가액 `2,076,816`, 부가세 `207,684`, VAT 포함 `2,284,500`으로 재현됐다(§3 원문).
- 선택 slip-service 테스트 3개 클래스는 exit 0, `BUILD SUCCESSFUL in 7s`다.
- desktop `npm run typecheck`는 exit 0이며 내부 Node 테스트가 `2 pass / 0 fail`, `50 pass / 0 fail`로 재현됐다.
- 실행 후 작업트리는 이 검증 보고서 신규 파일 외 변경이 없다.
- 원문:

```text
> .\gradlew.bat :services:slip-service:test --tests "com.samhanair.logis.slip.web.dto.SlipResponseTest" --tests "com.samhanair.logis.slip.service.SlipCleanupServiceTest" --tests "com.samhanair.logis.slip.service.SlipSignatureServiceTest"
BUILD SUCCESSFUL in 7s
18 actionable tasks: 1 executed, 17 up-to-date

> clients/desktop에서 npm run typecheck
ℹ tests 2
ℹ pass 2
ℹ fail 0
...
ℹ tests 50
ℹ pass 50
ℹ fail 0

> git status --short
?? docs/dev-reports/2026-08-01-1032-review.md
```

## 결론

- **PR 소스 신규 결함: 0건.** 바뀌면 안 되는 회계·세무·재무제표·원장·채권채무 경로에 실 데이터로 도달하는 값 변경을 찾지 못했다.
- 바뀐 곳은 코드 도달 경로 9곳, 그중 실사용 표시/파일 7곳이다. 안 바뀐 곳은 검증 단위 15곳이다.
- legacy `NULL` 2,758라인, `VAT_INCLUSIVE` 17라인, `SUPPLY` 1라인 수치와 기준 전표 저장 합계, desktop typecheck 0 failed를 재현했다.
- **현재 실행 중 구 서비스의 기존 결함은 실 사용자 경로에서 계속 도달한다.** 활성 전표 2,345건 중 2,344건의 목록이 VAT 포함 상세 합계와 다르다. 이는 PR 코드가 로드되지 않은 배포 전 baseline이므로 PR 신규 결함 수에는 넣지 않았다.

## 이 라운드가 보지 않은 축

1. 커밋 `f9a7460fa`를 실제 로드한 서비스의 실 DB HTTP 응답 — Docker 재빌드·재기동 금지로 미확인.
2. 실제 데스크톱 화면 클릭, 페이지 전환, 인쇄 미리보기 — 사용 가능한 브라우저 백엔드가 없어 미확인.
3. 실제 내려받은 XLSX 바이너리와 실제 인쇄물의 육안 비교 — 소스 컬럼/계산 경로까지만 확인.
4. 면세·영세율 실전표 화면 — 실 데이터가 각각 0건이라 미확인.
5. 삭제 전표·삭제 라인 — 요청된 활성 헤더·활성 라인 모집단에서 제외.
6. slip-service 전체 테스트 suite — 이번 라운드는 구현 보고서와 동일한 대상/영향 테스트 3개 클래스만 신선 실행.
