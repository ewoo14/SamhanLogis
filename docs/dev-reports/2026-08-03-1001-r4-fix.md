# #1001 R4 수정 보고서

## D1 조사 및 RED

- [실데이터/로컬 read-only] `slip_db`의 출고 매출 후보 31건 중 `partner_id`는 31건(100%), 유효한 `partner_code`는 0건(0%), UUID만 있는 건은 31건(100%)이었다. 실행 SQL과 원문 출력은 다음과 같다.

```text
docker exec samhan-postgres psql -U samhan -d slip_db -P pager=off -c "SELECT COUNT(DISTINCT s.id) AS included_slips, COUNT(DISTINCT s.id) FILTER (WHERE s.partner_id IS NOT NULL) AS with_partner_id, COUNT(DISTINCT s.id) FILTER (WHERE NULLIF(BTRIM(s.partner_code),'') IS NOT NULL) AS with_partner_code, COUNT(DISTINCT s.id) FILTER (WHERE s.partner_id IS NOT NULL AND NULLIF(BTRIM(s.partner_code),'') IS NULL) AS uuid_only FROM slips s WHERE s.is_deleted = false AND s.slip_type = 'OUTBOUND' AND s.status IN ('CONFIRMED','DELIVERED','COMPLETED','SHIPPING','INSPECTING');"
 included_slips | with_partner_id | with_partner_code | uuid_only
----------------+-----------------+-------------------+----------
             31 |              31 |                 0 |        31
```

- [실데이터/로컬 read-only] 따라서 화면의 거래처 코드 `P-2026-0017`을 slip-service의 `partner_code`로 조회하면 정상 출고 매출을 0건으로 만드는 것이 재현된다. 거래처 코드로 UUID를 해석한 뒤 UUID를 내부 요청에만 전달하고, 응답 DTO에는 UUID를 추가하지 않는 방식으로 고친다.

- [DEV-SEED/단위 fixture] 출고 후 상태 전이 코드는 `Slip.java:1065-1071`의 `PROCESSING → INSPECTING` 및 “출고 완료” 주석, `Slip.java:1087`의 `INSPECTING → COMPLETED`, `Slip.java:1104`의 `COMPLETED → SHIPPING`, `Slip.java:1119`의 `SHIPPING → DELIVERED`, `Slip.java:1134`의 `DELIVERED → CONFIRMED`로 확인했다. 따라서 원장 상태는 이 전이 경로의 `INSPECTING`, `COMPLETED`, `SHIPPING`, `DELIVERED`, `CONFIRMED`이다.

- [DEV-SEED/단위 fixture] 상태 범위 회귀 테스트를 먼저 실행한 RED 원문:

```text
SlipPartnerLedgerInternalControllerIT > returnsEveryOutboundStatusAfterInventoryDispatchWithLinesAndNoUuid() FAILED
    java.lang.AssertionError at SlipPartnerLedgerInternalControllerIT.java:88
3 tests completed, 1 failed
> Task :services:slip-service:test FAILED

## D1 수정 및 GREEN

- [DEV-SEED/단위 fixture] slip-service 내부 조회에 선택 거래처의 `partnerId` 필터를 추가했다. accounting-service가 표시용 거래처 코드로 거래처 UUID를 해석하고, 그 UUID를 내부 요청 query parameter로만 전달한다. 응답 `PartnerLedgerSalesResponse`, accounting 응답 DTO, desktop 모델에는 UUID 필드를 추가하지 않았다. `partnerId`가 있으면 UUID만으로 필터하고, 없을 때만 기존 코드 필터를 사용하므로 다른 거래처가 섞이지 않는다.

- [DEV-SEED/단위 fixture] 상태 범위와 `partner_id`만 채워진 legacy 행의 거래처 분리 테스트 GREEN 원문:

```text
> Task :services:slip-service:test

BUILD SUCCESSFUL in 50s
18 actionable tasks: 3 executed, 15 up-to-date

## CI 백엔드 RED 및 수정

- [DEV-SEED/단위 fixture] CI `gh run view 30761139789 --log-failed`의 첫 context 실패는 `AccountingPermissionControllerIT`의 `WebMvcTest`에서 `AccountingReportController`의 `PartnerLedgerReadService` 생성에 필요한 `PartnerLedgerSalesClient` mock이 없어 발생했다. 캐시된 실패가 78건으로 전파되었다. 원문:

```text
AccountingPermissionControllerIT > … > accounts tree deny FAILED
    IllegalStateException at DefaultCacheAwareContextLoaderDelegate.java:180
        Caused by: UnsatisfiedDependencyException at ConstructorResolver.java:795
            Caused by: NoSuchBeanDefinitionException at DefaultListableBeanFactory.java:1880
```

- [DEV-SEED/단위 fixture] 해당 WebMvcTest에 `PartnerLedgerSalesClient`를 `@MockBean`으로 추가한다. 이 테스트는 ledger read 자체를 검증하지 않으므로 외부 RestClient를 호출하지 않는다.

- [DEV-SEED/단위 fixture] 첫 수정 후 targeted test를 실행한 결과, 이 slice에서 실제 누락된 bean은 `PartnerLedgerReadService`였다. 원문:

```text
AccountingPermissionControllerIT > … > accounts tree deny FAILED
    java.lang.IllegalStateException at DefaultCacheAwareContextLoaderDelegate.java:180
        Caused by: org.springframework.beans.factory.NoSuchBeanDefinitionException:
        No qualifying bean of type 'com.samhanair.logis.accounting.service.PartnerLedgerReadService' available
78 tests completed, 78 failed
> Task :services:accounting-service:test FAILED
```

- [DEV-SEED/단위 fixture] 따라서 `PartnerLedgerSalesClient`가 실제 HTTP 호출을 하지 않는 WebMvc slice에는 `PartnerLedgerReadService`를 `@MockBean`으로 등록했다. 외부 RestClient mock 규율은 full context IT에 적용하고, 이 테스트의 slice 의존성은 controller가 직접 요구하는 서비스로 격리했다.

- [DEV-SEED/단위 fixture] `AccountingPermissionControllerIT` targeted test GREEN 원문:

```text
> Task :services:accounting-service:test

BUILD SUCCESSFUL in 26s
21 actionable tasks: 2 executed, 19 up-to-date
```

## CI 프론트엔드 RED 및 수정

- [DEV-SEED/단위 fixture] CI와 동일한 좁은 테스트 `npm test -- --run src/renderer/api/mock.test.ts`를 실행한 결과, R2 테스트 fixture의 `S-1`, `R-1` 문서번호가 표준 형식 계약을 위반했다. 원문:

```text
src/renderer/api/mock.test.ts (128 tests | 1 failed)
expected [ { …(3) }, { …(3) }, { …(3) } ] to deeply equal []

Received:
documentNo src/renderer/api/partnerLedgerApi.test.ts "S-1"
documentNo src/renderer/api/partnerLedgerApi.test.ts "R-1"
journalNo src/renderer/print/PartnerLedgerView.test.tsx "S-1"
Tests 1 failed | 127 passed (128)
```

- [DEV-SEED/단위 fixture] production code와 D2/D3/D4 표면은 건드리지 않고, 세 테스트 fixture만 표준 전표번호인 `2026/08/01-1`, `2026/08/02-1`로 교체한다.

- [DEV-SEED/단위 fixture] fixture 수정 후 동일 targeted test GREEN 원문:

```text
✓ src/renderer/api/mock.test.ts (128 tests)
Test Files 1 passed (1)
Tests 128 passed (128)
```

## D1 실데이터 대조 및 범위 확인

- [실데이터/로컬 read-only] 선택 거래처 `P-2026-0017`의 partner UUID `0beb5a9c-00c1-3b69-aa42-e32bd6dc77d2`는 1전표·2라인·12,276,000원이었다. 다른 UUID들은 별도 그룹으로 남아 같은 원장에 합쳐지지 않는다. 실행 SQL 원문:

```text
partner_id                              | slips | lines |   amount
----------------------------------------+-------+-------+------------
0beb5a9c-00c1-3b69-aa42-e32bd6dc77d2   |     1 |     2 | 12276000.00
```

- [실데이터/로컬 read-only] 동일 후보의 partner별 분포도 서로 다른 UUID별로 분리되어 있었다(예: `09e98136-ff4f-3fc3-adbb-70d83a829327` 1전표·2라인·5,692,500원, `124a483d-c4e9-37a3-8997-84409dfa2c8d` 1전표·4라인·21,575,400원). UUID는 내부 필터에만 사용하고 사용자 응답 JSON에는 포함하지 않는 회귀 assertion을 GREEN으로 확인했다.

## 범위 및 신규 파일

- [DEV-SEED/단위 fixture] 이번 라운드에는 D1과 CI red 3건만 수정했다. D2 정렬, D3 인쇄 반복, D4 인쇄 음수색 production 표면은 변경하지 않았다.
- 신규 파일: `docs/dev-reports/2026-08-03-1001-r4-fix.md`.

## 최종 검증 및 파일별 변경량

- [DEV-SEED/단위 fixture] `git diff --check`는 출력 없이 종료 코드 0이었다. 전체 accounting-service suite는 실행하지 않았다.
- [DEV-SEED/단위 fixture] `git diff --numstat` 개별 파일 원문(보고서 신규 파일은 untracked이므로 파일 줄 수로 별도 표기):

```text
clients/desktop/src/renderer/api/partnerLedgerApi.test.ts             +3/-3
clients/desktop/src/renderer/print/PartnerLedgerView.test.tsx         +1/-1
services/accounting-service/src/main/java/.../PartnerLedgerSalesClient.java +2/-1
services/accounting-service/src/main/java/.../PartnerLedgerReadService.java +1/-1
services/accounting-service/src/test/java/.../AccountingPermissionControllerIT.java +2/-0
services/slip-service/src/main/java/.../SlipRepository.java           +3/-1
services/slip-service/src/main/java/.../SlipInternalController.java   +4/-3
services/slip-service/src/test/java/.../SlipPartnerLedgerInternalControllerIT.java +26/-0
docs/dev-reports/2026-08-03-1001-r4-fix.md                              +140/-0 (신규 untracked)
```
```

- [DEV-SEED/단위 fixture] partner_id만 채우고 `partner_code`를 비운 대상/타 거래처를 만든 뒤 `partnerId`로 내부 조회하는 회귀 테스트도 RED였다. 원문:

```text
SlipPartnerLedgerInternalControllerIT > filtersByInternalPartnerIdWhenLegacyPartnerCodeIsBlank() FAILED
    java.lang.AssertionError at SlipPartnerLedgerInternalControllerIT.java:153
4 tests completed, 1 failed
> Task :services:slip-service:test FAILED
```
```
