# PR #1097 / 이슈 #1096 — S5 fix 결과

- 작업일: 2026-08-07 KST
- 범위: 혼합 견적 4건 전체 soft-delete, 순수 QA797 견적 `2026/07/27-1` 삭제 상태 고정, 혼합 문서 일괄 복원 허용
- 제약 준수: git 명령 없음, DB 쓰기 없음, 컨테이너 재빌드·재시작 없음, 전체 테스트 스위트 미실행

## 1. 새로 가능해진 상태·문서 조합과 각각 밟은 결과

### RED-first

S5 계약을 먼저 추가하고 실행했다.

```text
Issue1096S2FixContractTest > s5_cleanupDeletesMixedDocumentsAndTheActiveQa797ResidueAsOneBatch() FAILED
3 tests completed, 1 failed
BUILD FAILED
```

실패 원인은 V117에 문서 번호 집합과 전체 라인 삭제 조건이 없었기 때문이다.

### GREEN 결과

| 상태·문서 조합 | 결과 |
|---|---|
| `2026/07/17-1`, `-2`, `-5`, `-20` 혼합 견적 4건 | 헤더와 각 3개 라인(정본 1 + QA797 2), 총 12라인을 `issue-1096-test-seed-cleanup`으로 함께 soft-delete. 동일 actor의 삭제 그래프라 일반 복원 성공 경로가 열린다. |
| 순수 QA797 `2026/07/27-1` | 헤더와 QA797 2라인을 같은 actor로 soft-delete. 전체 라인 그래프가 순수 QA797이므로 일반 복원은 409로 차단된다. 행은 보존된다. |
| 혼합 문서의 삭제 후 일반 복원 | `hasQa797Line && hasCanonicalLine`인 혼합 문서는 QA 잔재 차단에서 제외되고, 헤더 actor와 모든 삭제 라인 actor가 일치하므로 409 없이 헤더·전체 라인이 함께 복원된다. |
| 혼합이 아닌 정본 문서 | SQL 문서번호 대상 5건 밖의 정본 문서 0건에 영향이 없다. 정본 품목 3,082건과 대상 밖 정본 라인의 활성 상태는 변경하지 않는다. |
| 기본 재기동 | `SAMHAN_FULL_SEED_TEST_DATA` opt-in 및 삭제행 포함 멱등 조회는 변경하지 않았다. 기본 재기동으로 정리분이 되살아나지 않는다. |
| 물리 삭제/일괄 복구 | 물리 삭제는 없으며, `deleted_by='issue-1096-test-seed-cleanup'` actor로 전체 정리분을 한 묶음 식별·복구할 수 있다. |

혼합 4건의 정본 라인 4개는 개발책임자 결정에 따라 해당 문서 전체 삭제의 일부로 의도적으로 soft-delete된다. 그 예외를 제외한 정본 문서 영향은 0건이다.

## 2. 제거·이동·개명한 식별자 grep 전수

제거·이동·개명한 기존 식별자는 없다.

추가·변경 식별자는 다음 검색으로 전수 확인했다.

```text
rg -n "2026/07/17-(1|2|5|20)|2026/07/27-1|_issue_1096_cleanup_estimate_ids|mixedQaAndCanonical|hasCanonicalLine"
```

- `_issue_1096_cleanup_estimate_ids`: V117 임시 문서 대상 집합 1건
- 문서번호: 혼합 4건 + 순수 QA797 1건, 총 5건
- `issue-1096-test-seed-cleanup`: V117 기존 batch actor 유지
- `mixedQaAndCanonical`, `hasCanonicalLine`: 혼합 문서만 정상 복원시키는 판정 경계
- `QA797-`: 기존 순수 QA797 복원 차단 식별자 유지
- V31·V16 및 `SAMHAN_FULL_SEED_TEST_DATA` 식별자: 변경 없음

다른 서비스 마이그레이션은 추가하지 않았다. 수정 파일은 다음 3개다.

- `services/slip-service/src/main/resources/db/migration/V117__soft_delete_test_seed_documents.sql`
- `services/slip-service/src/main/java/com/samhanair/logis/slip/estimate/service/EstimateService.java`
- `services/slip-service/src/test/java/com/samhanair/logis/slip/issue1096/Issue1096S2FixContractTest.java`

## 3. 바꾼 파일을 참조하는 테스트 실행 결과

### 통과

```text
.\gradlew.bat :services:slip-service:test `
  --tests com.samhanair.logis.slip.issue1096.Issue1096S2FixContractTest `
  --tests com.samhanair.logis.slip.estimate.service.EstimateServicePriceMemoryTest `
  --tests com.samhanair.logis.slip.estimate.service.EstimateUpdateLineIdContractTest `
  --tests com.samhanair.logis.slip.estimate.revision.service.EstimateRestoreTest `
  --no-daemon --console=plain

BUILD SUCCESSFUL
4개 테스트 클래스 실행, 18 tests completed
```

`Issue1096S2FixContractTest`만 별도 실행한 라운드도 `BUILD SUCCESSFUL`이었다.

### 실행하지 않은 것

- 전체 `slip-service` 테스트 스위트: S3의 124초 timeout 제약 때문에 실행하지 않음
- Flyway 실제 적용 및 실제 DELETE/RESTORE 호출: DB 쓰기 금지
- 컨테이너 재빌드·재기동 및 full-seed 라이브 검증: 작업 지시로 금지

## 신규 파일

- `docs/dev-reports/2026-08-07-1096-s5-fix.md`
