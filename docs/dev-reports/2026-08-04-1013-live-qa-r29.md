# PR #1059 라이브 QA R29 — 항목 ⑤

- 일시: 2026-08-04 (Asia/Seoul)
- 범위: 다른 날짜의 연락처가 섞이지 않는지 확인
- 대상: `2026-08-03` ↔ `2026-08-01`
- 전제: R28에서 ①~④·⑥~⑧ PASS. 이번 라운드는 ⑤만 수행.
- 금지사항 준수: Docker 재배포/재시작, commit/push, 코드 수정, 실제 SMS 발송, DB 직접 변경 없음.

## 단계 기록

### 사전 상태

- `git pull`: 완료 (`Already up to date.`)
- 서비스/게이트웨이: 라이브 QA 절차에 따라 확인 예정
- 캡처 디렉터리: `docs/qa/1013-dispatch-inherit-real-qa/r29-real-qa/`
- 브라우저 연결: 실패. 브라우저 런타임의 사용 가능한 브라우저 목록이 `[]`로 반환됨.
- 복구 지침에 따라 별도 브라우저 자동화나 API/소스 기반 대체 검증은 수행하지 않음.

### 1단계

- 상태: 미실시 — 실제 GUI 브라우저가 없어 화면 조작 및 캡처 불가
- 판정: 보류
- 캡처: 없음

### 2단계

- 상태: 미실시 — 1단계 선행 및 실제 GUI 브라우저 필요
- 판정: 보류
- 캡처: 없음

### 3단계

- 상태: 미실시 — 2단계 선행 및 실제 GUI 브라우저 필요
- 판정: 보류
- 캡처: 없음

## 최종 판정

- 두 날짜 및 전표 수: PM 제시값 기준 `2026-08-03` 6건, `2026-08-01` 4건. GUI에서 전표 행을 직접 확인하지 못함.
- 항목 ⑤: 판정 불가(브라우저 미사용 가능). 섞임 여부 미검증.
- 머지 가능 여부: R29 항목 ⑤ 미검증 상태이므로 머지 불가 판단.

## 캡처 목록

- 캡처 없음 — 브라우저 미사용 가능으로 실 GUI 렌더 캡처를 생성하지 못함.

## 2026-08-04 재시도 — 로컬 Playwright Chromium fallback

- `git pull`: 완료 (`Already up to date.`)
- PM 지침에 따라 브라우저 스킬 연결은 재시도하지 않고 `clients/desktop/node_modules`의 Playwright Chromium과 실제 Vite renderer DOM을 사용한다.
- `VITE_MOCK_MODE=0`, `VITE_API_BASE_URL=http://127.0.0.1:8080`, `VITE_APP_VERSION=2026/08/04-1`로 renderer를 기동한다.
- 이번 재시도 범위는 항목 ⑤의 1~3단계뿐이며, QA 캡처는 `docs/qa/1013-dispatch-inherit-real-qa/r29-real-qa/`에 저장한다.

### 로컬 Playwright Chromium 실행 결과

- 로그인: `janyeonggu` / 기존 QA 자격으로 성공. 실제 renderer DOM 화면에서 수행했다.
- `VITE_MOCK_MODE=0`, `VITE_API_BASE_URL=http://127.0.0.1:8080`, `VITE_APP_VERSION=2026/08/04-1`, Vite `127.0.0.1:5180` 사용.
- A `2026-08-03`: 화면에 출고전표 6건 표시. 순수 숫자 `1` + X(`010-2026-0803`) 입력 후 미리보기에서 `2026/08/03-1` 문구에 X가 표시됨.
- B `2026-08-01`: 화면에 출고전표 4건 표시되지만 실제 전표는 `2026/08/01-8`~`-5`로 표시되어 `-1` 전표가 없음. A의 X를 유지한 채 B 미리보기에서는 X가 B 전표에 표시되지 않음.
- B에서 연락처를 Y(`010-2026-0801`)로 변경 후 미리보기: B에는 `-1` 전표가 없어 Y가 붙지 않음.
- A로 복귀 후 미리보기: `2026/08/03-1` 문구에 X가 아닌 Y가 표시됨. 날짜별 연락처가 분리 보존되지 않고 마지막 입력 Y로 덮여 섞임.

### 항목 ⑤ 판정

- 1단계: **PASS** — A의 `-1`에 X 표시.
- 2단계: **PASS(비혼입 관측)** — B 화면에 A의 X 표시 없음. 단, PM이 기대한 B의 `-1` 전표는 실 화면에 존재하지 않음.
- 3단계: **FAIL** — B의 `-1`/Y 매칭은 확인할 수 없었고, A 복귀 시 A의 `-1`에 X가 아닌 Y가 표시되어 날짜별 상태가 섞임.
- 최종 판정: **⑤ FAIL**. 머지 가능 여부: **불가**.

### 캡처 파일

- `r29-step1-A-1-X.png`
- `r29-step2-B-1-no-X.png`
- `r29-step3-B-1-Y.png`
- `r29-step3-A-return-1-X.png`
