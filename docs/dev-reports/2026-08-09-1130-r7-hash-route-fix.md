# PR #1130 R7 — 해시 라우트 수정 및 라이브 QA 재검증

## ① `goto` 전수 목록

해시라우터 하네스 대상 `goto` 5건을 전수 수정했다. 모두 `${BASE_URL}/#/...` 형식이다.

| 파일 | 화면 | 수정 경로 |
|---|---|---|
| `clients/desktop/playwright/1130-r6-backend-master-guard-real-qa/1130-r6-backend-master-guard-real-qa.spec.ts:79` | MANAGER 입고 검수 목록 | `${BASE_URL}/#/warehouse/inbound-inspections` |
| `clients/desktop/playwright/1130-r6-backend-master-guard-real-qa/1130-r6-sol-reconv-real-qa.spec.ts:49` | MANAGER 입고 검수 목록 | `${BASE_URL}/#/warehouse/inbound-inspections` |
| `clients/desktop/playwright/1130-r6-backend-master-guard-real-qa/1130-r6-sol-reconv-real-qa.spec.ts:70` | MANAGER 입고전표 상세 | `${BASE_URL}/#/purchases/${target.id}` |
| `clients/desktop/playwright/1130-r6-backend-master-guard-real-qa/1130-r6-sol-reconv-real-qa.spec.ts:86` | SALES 입고 검수 직접 진입 차단 | `${BASE_URL}/#/warehouse/inbound-inspections` |
| `clients/desktop/playwright/1130-r6-backend-master-guard-real-qa/1130-r6-sol-reconv-real-qa.spec.ts:105` | MASTER 권한설정 | `${BASE_URL}/#/admin/permission-matrix` |

디렉터리와 파일명은 모두 `-real-qa` 규약을 유지했다.

## ② 화면 도달 단정

- MANAGER 입고 검수 목록: `inbound-inspection-list-table`을 표시 단정.
- MANAGER 입고전표 상세: `완료 (처리 완료)` 버튼을 표시·활성 단정.
- SALES 권한 차단: `PermissionGuard` 계약대로 홈으로 redirect되므로 홈 고유 heading `대시보드`를 표시 단정. 사이드바/헤더 공통 요소는 도달 증거로 사용하지 않았다.
- MASTER 권한설정: `권한설정` heading과 `permission-matrix-table`을 표시 단정.
- R6 backend guard의 MANAGER 화면: `inbound-inspection-list-table`을 표시 단정.

모든 캡처는 `resolveQaShotsDir(...)`를 경유했다. 실 QA 실행 후 임시 렌더러 프로세스와 5173 포트를 회수했다.

## ③ 재실행 결과

실 API, mock 비활성, headless Chromium으로 R6 두 스펙을 재실행했다.

```text
5 passed (8.7s)
```

- R6 관측은 유효했다. 수정 전에는 해시 없는 URL 때문에 홈에 머물 가능성이 있었지만, 수정 후 실제 MANAGER 입고 검수 목록과 MASTER 권한설정 화면의 고유 요소가 각각 확인됐다.
- MANAGER 입고 검수 API/UI 표본은 `inspectionUiRowCount=0`이었다. 따라서 처리 완료 버튼 상태는 규정대로 `판정 불가 — UI 표본 0건`이다. DB 직접 INSERT는 하지 않았다.
- MANAGER 권한: `inbound.inspection = [VIEW, UPDATE]`.
- SALES API: 입고 검수 목록 HTTP 403, UI 직접 진입은 홈으로 차단.
- MASTER: 권한 매트릭스 200개 페이지가 확인됐고 `system.permission-admin` 7개 액션 전권 및 권한설정 화면 도달을 확인.

## 검증

```text
npx vitest run src/renderer/test-utils/harness-false-green-guard.test.ts
62 passed

npx vitest run src/renderer/test-utils/inbound-permission-contract.test.ts
1 passed

npm run typecheck
exit 0

npm run lint
exit 0 — 0 errors, 기존 warnings 153건

npm run build
exit 0
```

가드 GREEN 원문:

```text
✓ src/renderer/test-utils/harness-false-green-guard.test.ts (62 tests)
Test Files 1 passed
Tests 62 passed (62)
```

## 변경·신규 파일

수정:

- `clients/desktop/playwright/1130-r6-backend-master-guard-real-qa/1130-r6-backend-master-guard-real-qa.spec.ts`
- `clients/desktop/playwright/1130-r6-backend-master-guard-real-qa/1130-r6-sol-reconv-real-qa.spec.ts`

신규:

- `docs/dev-reports/2026-08-09-1130-r7-hash-route-fix.md`

V98 파일과 다른 page code는 수정하지 않았다. 커밋·푸시는 수행하지 않았다.
