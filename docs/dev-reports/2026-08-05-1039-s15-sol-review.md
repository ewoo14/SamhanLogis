```
git -C . rev-parse --show-toplevel     # D:/dev/Samhan-Public/.claude/worktrees/w1045
git -C . branch --show-current         # feat/1039-provisional-dispatch
git -C . rev-parse HEAD                # 605ac7a87d424c7a892d0ad8742e0d691d4048b1 이어야 함
```

실행 원문:

```text
D:/dev/Samhan-Public/.claude/worktrees/w1045
feat/1039-provisional-dispatch
605ac7a87d424c7a892d0ad8742e0d691d4048b1
```

HEAD 일치. 검증을 계속했다.

실행 환경 원문과 필드:

```text
container_name=/samhan-slip-service created=2026-08-05T02:50:44.702471161Z started=2026-08-05T02:51:02.147121178Z
container_name=/samhan-auth-service created=2026-08-05T02:50:44.708923711Z started=2026-08-05T02:51:02.151804764Z
container_name=/samhan-arologis-service created=2026-08-05T02:50:44.746655288Z started=2026-08-05T02:51:02.168848238Z
container_name=/samhan-api-gateway created=2026-08-05T02:50:37.64267995Z started=2026-08-05T02:50:51.017973805Z

flyway_database=slip_db flyway_version=105 installed_rank=67
flyway_database=auth_db flyway_version=94 installed_rank=94
flyway_database=arologis_db flyway_version=25 installed_rank=25
```

`/samhan-api-gateway`의 `created`는 Docker 원문이 소수점 끝의 0을 생략했을 뿐 PM 값 `2026-08-05T02:50:37.642679950Z`와 같은 시각이다. Flyway의 요청 수치 `105·94·25`는 `version` 최댓값이다. `slip_db`의 `installed_rank` 최댓값은 67이므로 두 필드를 혼용하지 않았다. 컨테이너 재배포·재빌드·중지 없이 읽기 GET·읽기 SQL·코드 읽기만 사용했다.

# PR #1045 S15 적대검증 보고서

## 답

있다. 실사용자가 화면으로 도달할 수 있는 잘못된 동작 4개를 확인했다. 이 가운데 권한 없는 수신 데이터 열람과 전송 부분성공의 양쪽 DB 분기는 단독으로도 머지 차단 사유다.

검증 기준은 `origin/main...HEAD` 40커밋 전체다.

## 결함 1. `arologis.dispatch.ops VIEW`가 없는 회계 계정도 수신 배차 그룹을 화면에서 읽는다

### ① 사용자가 밟는 화면 동선

1. 아로로지스 데스크톱에 `AROLOGIS_ACCOUNTANT` 계정으로 로그인한다.
2. 상단 배차 메뉴의 `가배차 분류`를 누른다.
3. 이 PR은 해당 화면을 `ReceivedGroupsPage`로 바꿨으므로 `수신 배차 그룹` 표가 열린다.
4. 회계 계정은 `arologis.dispatch.ops VIEW=false`인데도 날짜별 그룹번호·차량·운송사·전표 snapshot을 읽는다.

### ② 재현 근거

- `clients/arologis-desktop/src/renderer/routes/dispatches/DispatchesLayout.tsx:3`~`8`: 모든 로그인 관리자에게 `가배차 분류` 링크를 만든다.
- `clients/arologis-desktop/src/renderer/routes/dispatches/PreClassifyPage.tsx:1`~`2`: `ReceivedGroupsPage`를 그대로 재수출한다.
- `clients/arologis-desktop/src/renderer/routes/index.tsx:145`~`160`: 상위에는 로그인 여부만 보는 `ProtectedRoute`가 있고, `pre-classify`와 `received-groups`에는 `PermissionGuard`가 없다.
- `services/arologis-service/src/main/java/com/samhanair/logis/arologis/web/ReceivedDispatchGroupController.java:3`: 신규 `GET /admin/arologis/dispatch-groups`에 `@RequirePermission`도 `@PreAuthorize`도 없다.
- `services/arologis-service/src/main/java/com/samhanair/logis/arologis/config/SecurityConfig.java:41`~`52`: `/admin/**`의 기본선은 권한이 아니라 `authenticated()`뿐이다.
- 대조군인 기존 배차 조회는 `ArologisAdminController`에서 `ArologisPageCodes.DISPATCH_OPS`를 요구한다. 신규 수신 목록만 그 축에서 빠졌다.

신규 컨트롤러 엔드포인트 전수 결과:

- `CarrierAdminController` 7개: 전부 `hr.carriers` 또는 `dispatch.board` 가드 있음.
- `DispatchGroupAdminController` 11개: 전부 `dispatch.board` 가드 있음.
- `PreClassifyAdminController` 1개: `dispatch.board VIEW` 있음.
- `ArologisInternalController` 신규 GET 1개: `@PreAuthorize("hasRole('MASTER')")` 있음.
- `ReceivedDispatchGroupController` 내부 POST 1개: `/internal/**`의 internal principal 가드 적용.
- `ReceivedDispatchGroupController` 사용자 GET 1개: 권한 가드 없음. 이 결함이다.

### ③ 실 데이터 건수

```text
아로로지스 활성 관리자: AROLOGIS_MASTER 1, AROLOGIS_MANAGER 1, AROLOGIS_ACCOUNTANT 1
arologis.dispatch.ops VIEW: ACCOUNTANT=false, MANAGER=true, MASTER=true
권한 없이 화면에 도달하는 활성 계정: 1
현재 active received_dispatch_groups: 1
현재 그 계정이 읽을 수 있는 수신 그룹: 1
```

게이트웨이도 `/admin/arologis/**`를 인증만 거쳐 전달하고(`application.yml:676`~`682`), 이 메서드에는 후단 권한 판정이 없다. 따라서 독립 아로로지스 회계 계정뿐 아니라 경로를 아는 삼한 인증 계정에도 동일한 데이터 노출면이 생긴다. 현재 삼한 활성 계정은 11개다.

## 결함 2. 원격 수신은 성공했지만 응답이 유실되면 아로로지스에만 그룹이 남고 삼한의 `FAILED` 표시는 롤백된다

### ① 사용자가 밟는 화면 동선

1. 삼한 데스크톱 `배차 > 배차 그룹`에서 아로로지스 운송사와 전표가 지정된 그룹의 `아로로지스로 전송`을 누른다.
2. 아로로지스가 요청을 커밋한 직후 응답 연결이 끊기거나 slip-service의 5초 read timeout이 난다.
3. 삼한 화면에는 저장 실패 안내가 뜨고 그룹은 `FAILED`가 아니라 종전 `NOT_SENT`로 남는다.
4. 사용자는 미전송으로 믿고 그룹을 수정·삭제할 수 있지만, 아로로지스 `수신 배차 그룹` 화면에는 이미 수신본이 남아 양쪽이 갈라진다.

### ② 재현 근거

- `services/slip-service/src/main/java/com/samhanair/logis/slip/service/dispatchgroup/DispatchGroupService.java:114`~`136`: 하나의 `@Transactional` 메서드 안에서 원격 POST 후 `markTransferSent()`를 수행한다. 예외 시 `markTransferFailed()`를 호출한 다음 `BusinessException`을 다시 던진다.
- `shared/common/src/main/java/com/samhanair/logis/common/exception/BusinessException.java:7`: `BusinessException extends RuntimeException`이다. Spring 기본 롤백 규칙 때문에 같은 트랜잭션의 `markTransferFailed()`도 롤백된다.
- `services/arologis-service/src/main/java/com/samhanair/logis/arologis/service/dispatch/ReceivedDispatchGroupService.java:3`: 수신 저장은 별도 서비스의 독립 `@Transactional`이다. 동일 `groupNo`면 snapshot을 교체하고 그 트랜잭션을 커밋한다.
- `services/slip-service/src/main/java/com/samhanair/logis/slip/client/ArologisDispatchGroupClient.java:13`~`17`: read timeout은 5초이며, 원격 커밋과 응답 수신을 하나의 원자 작업으로 묶는 프로토콜은 없다.
- `clients/desktop/src/renderer/routes/DispatchGroupPage.tsx:43`: UI는 `FAILED`일 때 `전송 재시도`를 표시하도록 만들었지만, 위 예외 경로에서는 그 상태가 영속되지 않는다.

이 경로는 아로로지스 DB에 부분 반영이 남는가라는 질문에 `예`다. 수신 endpoint의 `groupNo` upsert는 재시도 중복은 막지만, 삼한이 `NOT_SENT` 상태에서 수정·삭제된 뒤 재시도하지 않는 경우의 고아 수신본은 회수하지 않는다.

### ③ 실 데이터 건수

```text
현재 삼한 SENT 그룹: 1
현재 아로로지스 수신 그룹: 1
현재 삼한 FAILED 그룹: 0
현재 관측된 부분성공 고아: 0
```

현재 고아가 0이라는 사실은 실패 경로를 닫지 않는다. 이 라운드는 POST를 금지받아 장애 주입을 하지 않았고, 위 분기는 두 독립 트랜잭션과 unchecked 예외 롤백으로 코드상 확정된다.

## 결함 3. 전송 완료 뒤 운송사 마스터를 고치면 삼한 그룹과 아로로지스 수신본이 영구히 다른 운송사를 표시한다

### ① 사용자가 밟는 화면 동선

1. 아로로지스로 그룹을 전송해 `SENT`로 만든다.
2. `인사 > 운송사 목록`에서 같은 운송사의 코드·이름·아로로지스 여부 또는 활성 상태를 수정한다.
3. 삼한 배차 그룹은 현재 carrier master를 다시 조인해 새 코드·이름을 표시한다.
4. 아로로지스 수신 화면은 전송 당시 snapshot의 옛 코드·이름을 계속 표시한다. `SENT` 그룹에는 재전송 버튼도 없고 BE도 즉시 반환하므로 수렴 경로가 없다.

### ② 재현 근거

- `clients/desktop/src/renderer/routes/CarrierListPage.tsx:16`~`24`, `32`~`40`: 인사 화면에서 운송사 코드·이름·`isArologis`·활성을 수정할 수 있다.
- `services/slip-service/src/main/java/com/samhanair/logis/slip/service/dispatchgroup/CarrierService.java:33`~`41`: update에는 SENT 그룹 참조 확인이 없다.
- `services/slip-service/src/main/java/com/samhanair/logis/slip/service/dispatchgroup/DispatchGroupService.java:154`~`160`: 삼한 그룹 응답은 매번 현재 carrier entity의 코드·이름·`isArologis`를 읽는다.
- 같은 파일 `122`~`123`: 이미 `SENT`면 원격 호출 없이 즉시 반환한다.
- `services/arologis-service/src/main/java/com/samhanair/logis/arologis/domain/ReceivedDispatchGroup.java:12`~`19`: 수신본은 carrier code/name을 값 snapshot으로 보존하며, 새 수신 호출 외 갱신 경로가 없다.

전송 완료 그룹 자체의 수정·삭제·전표 편입·운송사 재지정은 `ensureMutable()`로 막혀 있다. 누락된 경계는 그 그룹이 참조하는 운송사 마스터의 사후 변경이다.

### ③ 실 데이터 건수

```text
현재 SENT 그룹: 1
현재 SENT 그룹이 참조하는 수정 가능한 활성 아로로지스 운송사: 1
hr.carriers 쓰기 가능 계정(dev_manager, dev_master): 2
현재 양쪽 carrier code/name 불일치: 0
```

현재 불일치는 0이지만, 이미 전송된 실데이터 1건과 화면 쓰기 권한자 2명이 이 동선을 즉시 밟을 수 있다.

## 결함 4. 활성 OUTBOUND 119건이 8개 가배차 모드 어디에도 없고, 화면이 안내하는 복구도 일어나지 않는다

### ① 사용자가 밟는 화면 동선

1. 삼한 데스크톱 `배차 > 가배차 분류`에서 과거 날짜를 고른다. 예: `2026-08-04`.
2. 8개 실행 모드를 차례로 조회한다.
3. `source_warehouse_code`가 `2` 또는 `00003`이 아닌 일반 전표는 어느 모드에도 나타나지 않는다.
4. 화면은 `창고 정보를 확인한 뒤 다시 조회`하라고 하지만, 현재 UNKNOWN 120건 중 worker 재시도 대상은 0건이라 다시 조회해도 회복되지 않는다.

### ② 재현 근거

- `services/slip-service/src/main/java/com/samhanair/logis/slip/service/preclassify/JpaPreClassifySlipQuery.java:22`~`25`: `2=SANGIL`, `00003=CHOWOL`, 그 외 전부 `UNKNOWN`이다.
- `services/slip-service/src/main/java/com/samhanair/logis/slip/service/preclassify/PreClassifyService.java:46`~`64`: 일반 전표는 warehouse allow에 실패한다. UNKNOWN `STACK`만 `STACK_ONLY`가 아닌 모드에서 54행의 조기 return으로 일부 통과한다.
- 같은 파일 `31`~`32`: `unknownWarehouseCount`는 모드 필터 전에 센다.
- `clients/desktop/src/renderer/routes/ArologisPreClassifyPage.tsx:352`~`357`: 화면은 UNKNOWN 전부가 분류에서 제외됐다고 표시하고 창고 확인 후 재조회를 안내한다.
- `services/slip-service/src/main/resources/db/migration/V102__queue_source_warehouse_code_snapshot.sql:1`~`3`: 기존 행은 의도적으로 pending에 넣지 않는다.
- `services/slip-service/src/main/java/com/samhanair/logis/slip/repository/SlipRepository.java:55`~`68`: worker 후보는 `PENDING` 또는 stale `PROCESSING`뿐이다. `NOT_REQUESTED`는 재조회되지 않는다.

`2026-08-04`에는 UNKNOWN이 9건이다. 그중 DRAFT 일반 전표 3건(`2026/08/04-2`, `-3`, `-9`)은 어느 모드에도 안 나온다. 반대로 UNKNOWN `STACK`인 `2026/08/04-4`는 모드 1·2·3·6·7·8에는 나오는데도 경고는 UNKNOWN 9건 모두가 제외됐다고 말한다. 즉 누락뿐 아니라 경고 건수의 의미도 화면 결과와 모순된다.

### ③ 실 데이터 건수

```text
활성 OUTBOUND: 121
UNKNOWN warehouse: 120
8개 모드 어디에도 없는 UNKNOWN: 119
  DRAFT 114 (사용자가 가배차 대상으로 찾게 되는 상태)
  SENT 4
  CONFIRMED 1

UNKNOWN snapshot 상태:
  NOT_REQUESTED 119
  COMPLETED지만 비표준 코드(1) 1
  PENDING/PROCESSING 0

날짜 2026-08-04:
  활성 OUTBOUND 9
  UNKNOWN 9
  어느 모드에도 없는 행 8
  그중 DRAFT 3
  UNKNOWN STACK이 일부 모드에 나타나는 행 1
```

## 확인했으나 결함으로 판정하지 않은 경로

- S10 `dispatch-lookup` 응답은 `code`, `name`, `isArologis`, `isActive`만 포함한다. UUID와 `partner_id`는 노출하지 않는다. soft-deleted carrier는 entity SQL restriction으로 제외된다.
- 현재 carrier 2건은 모두 active라 비활성 실데이터 노출은 0건이다. 이후 비활성 운송사가 생겨도 배차 화면은 `isActive`로 버튼을 필터하고, BE의 `DispatchGroup.assignCarrier()`도 비활성을 거부한다.
- 게이트웨이 `/admin/carriers/**`가 태우는 나머지 carrier 경로는 모두 `hr.carriers` 가드를 가진다. `dev_dispatch`는 `dispatch.board VIEW=true`, `hr.carriers VIEW/쓰기=false`이고 해당 경계 밖 CRUD로 확장되지 않는다.
- `is_arologis=false` 외부 운송사 그룹은 현재 1건이다. 화면에 전송 버튼이 없고 BE도 409로 거부한다. 조용한 성공은 없다.
- SENT 그룹 1건의 직접 수정·삭제·전표 변경·운송사 재지정은 차단된다.
- 같은 `groupNo`를 다른 날짜에 만드는 것은 현재 active business key를 전역 유일하게 쓰겠다는 S4 문서 계약이다. 현재 교차 날짜 중복은 0건이며, 이 라운드에서는 결함으로 뒤집지 않았다.
- 주소 앞 10자 제외 키워드(`회수·회차·차용·대여·반납·자가`) 또는 legacy 운송사 marker에 걸리는 현재 활성 OUTBOUND는 0건이다.
- 현재 REGION rule master는 0건이고 비어 있지 않은 OUTBOUND 주소는 QA throwaway 1건뿐이어서, 실데이터 기반 `classifyRegion` 오분류 양성 사례는 나오지 않았다.

## 최종 판정

**머지 비권고.**

가장 먼저 막아야 할 것은 결함 1과 2다. 결함 1은 권한이 명시적으로 false인 활성 회계 계정 1명이 수신 데이터 1건을 정상 화면에서 읽는 인가 누락이다. 결함 2는 원격 커밋 뒤 응답 유실만으로 삼한과 아로로지스의 사실이 갈리고, 로컬 `FAILED` 표시마저 롤백되어 사용자가 잘못된 후속 수정·삭제를 하게 만든다. 결함 3은 이미 SENT인 실데이터 1건에서 사후 마스터 변경 시 영구 불일치를 만들며, 결함 4는 편집 단계 전표 114건을 8개 모드 모두에서 잃고 화면이 제시하는 복구 경로도 작동하지 않는다.

## 이 라운드가 보지 않은 것

- mode 5(`REGION_ONLY`) 양성 표본은 #1074 범위이므로 만들거나 재검증하지 않았다.
- 가배차 화면 2:1 레이아웃, 드래그·더블클릭, 차량 형태 테두리는 다음 슬라이스이므로 보지 않았다.
- 전송·삭제·수정 POST/PUT/PATCH/DELETE, DB 쓰기, 장애 주입은 하지 않았다. 결함 2는 실제 POST 재현이 아니라 트랜잭션 경계와 현재 양쪽 저장 모델로 확정했다.
- 컨테이너 재배포·재빌드·중지, Gradle 전체 스위트, 다른 트랙 #1057·#1061·#1063·#1066 파일은 건드리지 않았다.
- Electron GUI는 S13의 실기동 증거를 재사용했고 이번 라운드에서 다시 띄우지 않았다.
- REGION rule이 0건이므로 실제 rule 간 우선순위 충돌이나 키워드 중첩 오분류는 판단하지 않았다.
