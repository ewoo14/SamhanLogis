# 2026-08-13 #1143 Live QA 3 — CODEX SOL

## 결론

**실 사용자 경로에서 재현되는 결함이 1건 있다.**

- 제품 목록에서 `AM052BN6PBH1` 상세로 진입한 뒤 아무 값도 바꾸지 않고 저장하면 제품 `PATCH`가 `409 CONFLICT`로 실패한다.
- 사용자 응답 원문: `{"success":false,"code":"CONFLICT","message":"동시 편집 충돌 또는 제약 위반","data":null,...}`
- 같은 시각 product-service 원문: `SQLState: 23505`, `duplicate key value violates unique constraint "ux_product_spec_active"`.
- 목록으로 돌아가 같은 제품을 다시 여는 경로는 정상이며 대상도 `AM052BN6PBH1`로 동일했다. 실패 지점은 상세 무변경 저장이다.

fix가 표적한 무한 재렌더, API UUID 노출, 소프트삭제 타깃 부활은 재현되지 않았다.

## 실행 환경과 증거 무결성

- 브랜치/작업 위치: `feat/1111-1143-bundle-component`, `C:/dev/Samhan-Public/.claude/worktrees/w1111`
- DB: 공유 DB에는 `pg_dump` 읽기만 수행했다. GUI/API 쓰기는 격리 컨테이너 `samhan-1143-liveqa3-sol-pg`의 `product_db`에서만 수행했다.
- 복제: PowerShell 파이프를 사용하지 않고 dump 파일을 경유했다.
- 복제 직후 한글 확인 원문:

```text
        name
--------------------
 (주)한국냉동물류
 (주)서울택배
 대한화물서비스(주)
(3 rows)
```

- renderer: 최초 `5175`는 시작 명령의 로그 경로 인자 해석 실패 뒤 다른 worktree 프로세스가 점유했다. 해당 프로세스를 건드리지 않고 이 작업 전용 renderer를 `43475`에 띄웠다.
- Browser 런타임 조회 결과가 `[]`였으므로 중단 판정하지 않고 standalone Playwright Chromium으로 GUI를 걸었다.
- 실제 격리 product-service는 `43484`, renderer는 `43475`, 격리 PostgreSQL은 `43432`를 사용했다.
- 브라우저에서 product-service로 보내는 요청만 동일 응답을 CORS 허용 헤더와 함께 전달했다. 제품·분류 본문 및 저장 결과는 실제 격리 서비스 응답이다.
- DOM 데이터 문자 검사: `replacement=0`, `questionLines=[]`. 화면과 DB의 한국어도 직접 확인했으며 데이터 자리에 `?`가 없었다.
- 격리 환경에 `/app/version` 보조 endpoint가 없어 스크린샷 상단에 업데이트 확인 실패 배너가 보인다. 제품·분류 데이터 및 저장 응답과는 분리된 보조 호출이다.

## 13개 경로 실행 원문

### 1. 모달 부자재 추가 → 칩 설정 → 저장

```text
VISIBLE|quantity-sync-modal
VISIBLE|quantity-target-row-AWR-WG00N
VISIBLE|persisted-AWR-WG00N
ITEM_1|PASS|부자재 AWR-WG00N 추가→수량2·컬러·사각→PUT 200→재조회 유지; 렌더 loop 없음
RERENDER_ERROR_COUNT|0
```

저장 후 모달을 다시 열어 추가 칩과 설정 유지까지 확인했다. 무한 재렌더는 재현되지 않았다. 이후 추가한 타깃은 삭제하고 저장해 원래 활성 3건으로 복구했다.

### 2. 제품·분류 API 응답 UUID 전수 검사

응답 문자열 전체를 정규식으로 검사했으며 중첩 객체·배열도 포함한다.

```text
API|GET|/api/v1/products?category=HOME_MULTI&page=0&size=10000|http=200|bytes=100608|uuid=0
API|GET|/api/v1/classifications?estimateCategory=HOME_MULTI|http=200|bytes=1323|uuid=0
API|GET|/products?q=AM052BN6PBH1&size=20|http=200|bytes=1170|uuid=0
API|GET|/products/by-model/AM052BN6PBH1|http=200|bytes=2041|uuid=0
API|GET|/products/categories|http=200|bytes=1349|uuid=0
API|GET|/api/v1/products/AC100CS6PHH1SY/components|http=200|bytes=4211|uuid=0
ITEM_2|PASS|6개 제품·분류 응답 nested raw UUID=0
```

### 3. 활성 타깃 3건과 무변경 저장

```text
ITEM_3|PASS|규칙 재오픈 3칩→무변경 PUT 200→재조회 3칩
```

사후 DB 원문:

```text
 active_targets | deleted_targets | all_targets
----------------+-----------------+-------------
              3 |              53 |          56

 model_code |          name          | multiplier
------------+------------------------+-----------
 AWR-WE13N  | 유선리모컨(통합)       |          1
 FH-LFHLN   | 유연호스 L형 4WAY      |          1
 PC6NUDK1NW | 판넬 360CST 사각  WIFI |          1
```

복제 기준 소프트삭제 23건은 그대로 삭제 상태였다. Live QA의 replace-all 저장이 추가한 소프트삭제 30건 때문에 사후 누계가 53건이 되었지만, 활성 집합은 정확히 3건이며 `AWR-WG00N`은 활성으로 남지 않았다. 기존 삭제 23건이 되살아난 행은 0건이다.

### 4. 목록에서 상세 진입

```text
VISIBLE|product-catalog-table
VISIBLE|catalog-edit-AM052BN6PBH1
VISIBLE|product-form-AM052BN6PBH1
ITEM_4|PASS|목록 수정 클릭→AM052BN6PBH1 상세 도달, list/detail token 동일 대상
```

### 5. 저장 후 재조회 동일 대상

```text
PRODUCT_NOCHANGE_PATCH|http=409|body={"success":false,"code":"CONFLICT","message":"동시 편집 충돌 또는 제약 위반","data":null,...}
ITEM_5|FAIL|opaque token 무변경 PATCH 409, 목록 재조회 대상은 AM052BN6PBH1
```

백엔드 원문:

```text
SQL Error: 0, SQLState: 23505
ERROR: duplicate key value violates unique constraint "ux_product_spec_active"
```

### 6. 같은 대상 2회 토큰 일관성

```text
API|GET|/products/by-model/AM052BN6PBH1|http=200|bytes=2041|uuid=0
API|GET|/products/by-model/AM052BN6PBH1|http=200|bytes=2041|uuid=0
ITEM_6|PASS|동일 제품 2회 token 일치, 분류 token 8필드 안정 표현
```

### 7. 토큰 화면 비노출

```text
ITEM_7|PASS|상세 화면 body·URL에 opaque token/UUID 0건
```

상세 화면의 가시 텍스트와 현재 URL을 검사했다. 토큰과 UUID 모두 노출 0건이었다.

### 8. 종류별 특징 후보 변경

```text
ITEM_8|PASS|PANEL=(없음),기본,블랙,승강,공청 / REMOTE=(없음),기본,유선,컬러 / PANEL 복귀 동일
```

### 9. 형상 기본값과 활성 상태

```text
ITEM_9|PASS|형상=(없음),원형,사각 initial='' PANEL/REMOTE 항상 enabled
```

### 10. 특징 변경 저장·유지와 모델코드 불변

```text
ITEM_10|PASS|특징 블랙·형상 사각 저장/재조회 유지, 모델코드 PC6NUNK1NW 불변
```

### 11. 다섯 표면 노출

```text
ITEM_11|PASS|수량 동기화·비중·고정금액·반올림 단위·기본 구성품 화면 노출
```

### 12. 두 화면 특징·형상 후보 동일

```text
ITEM_12|PASS|구성품 화면과 수량동기화 모달 PANEL/REMOTE/형상 후보 완전 동일
```

### 13. 무변경 저장, AUTO 비중 검증, FIXED 기본 금액

```text
ITEM_13|PASS|무변경 PUT 200·AUTO AC110BXADHH1:6+AC110CNMDHH1:4 / 합 9 HTTP400 한국어 / FIXED AWR-WE13N=45,375
```

비중 한 항목을 `6→5`로 바꿨을 때의 응답/UI 문구:

```text
HTTP 400
자동 구성품 비중 합은 10이어야 합니다.
```

다시 `6`으로 저장해 `4+6`으로 복구했다. 별도 묶음에 `AWR-WE13N`을 GUI로 추가했을 때 API `fixedAllocationAmount=45375`와 화면 `45,375`를 확인한 뒤 해당 추가 행도 제거 저장했다.

## 스크린샷

모두 `1600×1000` PNG이며 총 8장이다.

1. `docs/qa/2026-08-13-1143-liveqa3/01-A1-modal-add-accessory-chip-config-save-reopen.png`
2. `docs/qa/2026-08-13-1143-liveqa3/02-A3-active-three-chips-after-nochange-save.png`
3. `docs/qa/2026-08-13-1143-liveqa3/03-B4-B7-list-to-detail-token-not-visible.png`
4. `docs/qa/2026-08-13-1143-liveqa3/04-C8-C9-panel-feature-options-shape-empty-enabled.png`
5. `docs/qa/2026-08-13-1143-liveqa3/05-C8-remote-feature-options-shape-enabled.png`
6. `docs/qa/2026-08-13-1143-liveqa3/06-C10-C11-feature-shape-persist-model-code-and-five-surfaces.png`
7. `docs/qa/2026-08-13-1143-liveqa3/07-C13-weight-sum-nine-http400-korean-message.png`
8. `docs/qa/2026-08-13-1143-liveqa3/08-C13-fixed-new-component-default-delivery-45375.png`

필수 증거인 A1 모달 저장 완주와 A3 활성 3건을 각각 1번, 2번 파일에 담았다.

## 미실행 항목

없음. 13개 항목을 모두 실행했으며, 5번은 미실행이 아니라 재현된 실패다.

## 도달 가능한 결함

1건: 제품 상세 무변경 저장 시 `409 CONFLICT` (`ux_product_spec_active`, SQLState `23505`).

## 라운드 종료 점검

삭제된 추적 파일 0건, `tools/.s24-build-only/build/deep/tracked-writer.mjs` 존재 확인. 이 라운드 전용 격리 컨테이너·dump 파일·임시 디렉터리·renderer/product-service 프로세스를 정리했으며 전용 포트 `43432`, `43475`, `43484`는 모두 닫혔다.
