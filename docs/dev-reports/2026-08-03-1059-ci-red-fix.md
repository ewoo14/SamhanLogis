# PR #1059 CI red fix 라운드

## 범위

- 대상: CI 실패 3잡 재현 및 수정
- 브랜치: `feat/1013-dispatch-inherit`
- 시작 HEAD: `8c1cea73f`
- 커밋/푸시/PR 조작: 하지 않음

## 진행 로그

### 시작

- 지정 보고서 파일을 작업 확인 전에 생성함.

### 저장소 시작 상태 확인

실행 명령:

```powershell
Get-Content AGENTS.md, docs/handoff/CURRENT-WORK.md, .codex/AGENTS.md -Encoding UTF8
git status --short
git branch --show-current
git log --oneline -5
```

### ① Frontend Desktop vitest 재현 시도

실행 명령:

```powershell
npm test -- src/renderer/test-utils/harness-false-green-guard.test.ts --reporter=verbose
```

원문:

```text
> @samhan/desktop@0.1.0 pretest
> node scripts/real-qa-scope.cjs --phase=test

[로컬 파생물 신선도 확인 실패] 검증 결과를 코드 결함으로 해석하지 마십시오.
- file: 의존 design-system dist이(가) 소스보다 오래됐습니다: ..\web\design-system\dist\index.d.ts
산출물=2026-08-02T04:29:43.389Z, 최신 소스=..\web\design-system\src\index.ts (2026-08-03T10:09:54.554Z)
코드 오류로 단정하지 말고 먼저 cd ..\web\design-system; npm run build
```

판정: `pretest`의 로컬 파생물 신선도 가드가 먼저 종료되어 vitest 본체는 실행되지 않음. CI 실패를 직접 확인하기 위해 pretest를 우회하여 동일 테스트 파일을 실행한다.

### ① Frontend Desktop vitest 직접 재현

실행 명령:

```powershell
npx vitest run src/renderer/test-utils/harness-false-green-guard.test.ts --reporter=verbose
```

실패 원문:

```text
× 하네스 거짓 green 가드 > H-2: 캡처 목적지로 쓰이는 docs/qa 경로 상수는 전부 resolveQaShotsDir 를 경유한다
  → 커밋 QA 증거로 직접 쓰는 경로 상수 발견 — resolveQaShotsDir() 경유 필수:
1013-dispatch-inherit-real-qa/dispatch-inherit-real-qa.spec.ts → const SHOTS: expected [ Array(1) ] to deeply equal []

Test Files 1 failed (1)
Tests 1 failed | 48 passed (49)
AssertionError ... harness-false-green-guard.test.ts:666:7
```

확증: 실패 대상은 PM 로그와 동일한 `dispatch-inherit-real-qa.spec.ts → const SHOTS`이다.

① 수정 후 재현 green 원문:

```text
✓ H-2: 캡처 목적지로 쓰이는 docs/qa 경로 상수는 전부 resolveQaShotsDir 를 경유한다
Test Files 1 passed (1)
Tests 49 passed (49)
```

### ② 문서 본문 단언 스펙 재현 및 판정

실행 명령:

```powershell
npx playwright test playwright/sp-08-3-dispatch-parity/sp-08-3-dispatch-parity.spec.ts --config=playwright.config.ts --reporter=line
```

원문(실패 시작 부분):

```text
Running 5 tests using 1 worker
[2/5] ... 현재 API/route 소스가 6개 기존 endpoint를 유지하고 SP-08-3-2 arologis history endpoint를 허용한다
1) ... Error: expect(received).toContain(expected)
Expected substring: "/admin/notifications/dispatch-batch/send"
```

로컬 소스 확증:

```text
clients/desktop/src/renderer/api/dispatchSmsApi.ts:102:
'/admin/notifications/dispatch-batch/preview'
services/arologis-service/.../web/DispatchSaveHistoryController.java:40:
@RequestMapping("/admin/arologis/dispatches/history")
clients/arologis-desktop/src/renderer/api/dispatchSaveHistoryApi.ts:71,91,102,113:
'/admin/arologis/dispatches/history...'
```

판정: 제품이 옳다. `f2029a336`의 개발책임자 A안이 자동 SMS `/send`, 발송 버튼·감사 경로를 제거하고 표시·편집·복사만 보존했으며, 현재 FE/BE도 `/preview`와 arologis history를 실제 구현한다. 따라서 제품 동작을 되살리지 않고, stale한 SP-08-3 문서 단언 스펙의 `/send` 기대와 계획 문서의 해당 endpoint 표기를 제품 계약에 맞게 고친다. arologis history endpoint 자체는 현재 구현되어 있어 삭제/우회하지 않는다.

PM가 제공한 CI 원문도 보존한다:

```text
clients/desktop/playwright/sp-08-3-dispatch-parity/sp-08-3-dispatch-parity.spec.ts:152:3
expect(sources).toContain('/admin/arologis/dispatches/history')
```

로컬 HEAD에서는 같은 stale matrix가 먼저 `/admin/notifications/dispatch-batch/send`에서 실패했고, 그 단언을 제품 계약에 맞춰 제거한 뒤 arologis history 구현 단언까지 포함해 ② 스펙이 통과했다.

### ③ Desktop Playwright mock 회귀 hard gate 실패 확정

사전 조건 명령:

```powershell
clients/web/design-system> npm run build
```

원문:

```text
✓ built in 5.47s
```

CI 동일 스위트 실행:

```powershell
clients/desktop> $env:CI='1'; npx playwright test --reporter=line
```

전체 실행은 로컬에서 304초 timeout으로 종료되어 최종 집계가 생성되지 않았다. 실행 중 생성된 실패 artifact에서 다음 스펙을 확정했다.

```text
Test: permission-groups-c5-followup.spec.ts
Location: playwright\\permission-groups-c5-followup\\permission-groups-c5-followup.spec.ts:46:3
Error: expect(received).toMatch(expected)
Expected pattern: /dynamicCanAccess\('dispatch\.batch',\s*'view'\)/
1 failed
8 passed (9)
```

직접 재현 명령:

```powershell
npx playwright test playwright/permission-groups-c5-followup/permission-groups-c5-followup.spec.ts --config=playwright.config.ts --reporter=line
```

판정: `AppLayout.tsx:603`의 현재 제품 코드는 `dynamicCanAccess('notification.dispatch-sms.send-audit', 'view')`이며, `49c511b98`에서 V92 정본으로 통일됐다. 실패 단언만 과거 `dispatch.batch`를 요구하므로 제품 코드는 변경하지 않는다.

### 수정 후 ②·③ 통합 재현 green 원문

실행 명령:

```powershell
npx playwright test playwright/sp-08-3-dispatch-parity/sp-08-3-dispatch-parity.spec.ts playwright/permission-groups-c5-followup/permission-groups-c5-followup.spec.ts --config=playwright.config.ts --reporter=line
```

원문:

```text
Running 14 tests using 1 worker
14 passed (7.8s)
```

### Frontend Desktop 후속 검증

실행 명령:

```powershell
npm run typecheck
```

원문 요약:

```text
> @samhan/desktop@0.1.0 typecheck
> ... tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.web.json --noEmit ...
✔ tests 50
ℹ pass 50
ℹ fail 0
```

판정: typecheck 종료 코드 0. 로컬에 이미 존재하던 미추적 real-QA 스펙 경고는 허용 모드로 처리되었고 본 라운드 수정 파일과 무관하다.

실행 명령:

```powershell
npm run lint
```

원문 끝:

```text
✖ 111 problems (0 errors, 111 warnings)
0 errors and 1 warning potentially fixable with the `--fix` option.
```

판정: 종료 코드 0. 기존 warning만 있으며 이번 수정 파일의 lint 오류는 없다.

실행 명령:

```powershell
npm run build
```

원문 끝:

```text
✓ built in 3.94s
```

판정: 종료 코드 0.

### 최종 변경 무결성 확인

실행 명령:

```powershell
git diff --check
npx vitest run src/renderer/test-utils/harness-false-green-guard.test.ts --reporter=dot
```

원문:

```text
Test Files 1 passed (1)
Tests 49 passed (49)
```

`git diff --check` 출력 없음, 종료 코드 0.

## 고친 파일

- `clients/desktop/playwright/1013-dispatch-inherit-real-qa/dispatch-inherit-real-qa.spec.ts` — QA 캡처 경로를 `resolveQaShotsDir()`로 감싸 커밋 증거 덮어쓰기 방지 계약 준수.
- `clients/desktop/playwright/sp-08-3-dispatch-parity/sp-08-3-dispatch-parity.spec.ts` — A안 제품 계약에 맞춰 자동 `/send` 단언 제거, preview 단언으로 정정.
- `clients/desktop/playwright/permission-groups-c5-followup/permission-groups-c5-followup.spec.ts` — 현재 V92 정본 page code 단언으로 정정.
- `docs/planning/2026-05-16_sp-08-3-dispatch-legacy-gas-parity.md` — 배차문자 endpoint 표기를 현재 표시·편집·복사 전용 계약으로 동기화.

## 새로 만든 파일

- `docs/dev-reports/2026-08-03-1059-ci-red-fix.md`

## 제한 사항

- `npx playwright test` 전체 652 스펙 로컬 실행은 304초 timeout으로 종료되었고, artifact로 ③ 실패 스펙을 확정했다. 수정 후 해당 ③ 스펙과 ② 스펙을 합친 14 테스트는 `14 passed (7.8s)`로 재현 green.
- 커밋, push, PR 조작, Docker/라이브QA는 수행하지 않음.

최종 실행:

```powershell
git diff --check
```

원문: 출력 없음, 종료 코드 0.

실행 명령:

```powershell
npm run build:capacitor
```

원문 끝:

```text
✓ built in 4.39s
```

판정: 종료 코드 0.

실행 명령:

```powershell
npm run build:web
```

원문 끝:

```text
✓ built in 4.83s
PWA v0.20.5
```

판정: 종료 코드 0.

관련 원문:

```text
?? docs/dev-reports/2026-08-03-1059-ci-red-fix.md
feat/1013-dispatch-inherit
8c1cea73f docs(#1013): R15 CI 원인 수집 중간 산출물 (세션 종료로 미완)
49c511b98 [FIX] #1013 R14 — V92 인가 3층 통일 · 선택 복사 key · 미매핑 표기
ae56d0330 docs(qa/#1013): 라이브QA — 실패 3건. 선택 복사 key 충돌 · 미매핑 표기 누락 · V92 권한 회수 실패
6c936fbf9 Merge origin/main into feat/1013-dispatch-inherit — 충돌 1건 해소
d25ade812 [FIX] #1013 R12 — 계승 본체(하차일별 그룹 문구) 구현 외 4건
```
