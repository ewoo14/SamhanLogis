# #1143 세트 구성품 비중·반올림 데이터화 S1 보고

## 결론

구현을 중단했다. 정찰 정본의 “V38 파일·DB·미머지 점유 0”과 현재 워크트리의 실제 상태가 모순된다.

현재 저장소에는 이미 다음 미적용 Java migration이 있다.

```text
services/product-service/src/main/java/db/migration/V38__ProductCategoryBackfill.java
```

따라서 #1143 SQL을 V38로 추가하면 Flyway가 동일 버전 2개를 발견한다. 이 파일을 임의로 삭제·이름 변경하거나 V39로 우회하지 않았다.

## RED-first 원문

추가한 RED 테스트를 실행했고, 구현 전 기능 부재로 실패했다.

```text
> Task :services:product-service:compileTestJava FAILED
error: cannot find symbol
  symbol:   variable BundleAllocationPolicy
error: cannot find symbol
  symbol:   class AllocationMode
12 errors
BUILD FAILED
```

충돌을 확인한 뒤 제가 추가한 테스트·정책 클래스·V38 SQL과 도메인/DTO/서비스의 부분 변경은 정리했다.

## 검증 집계

### passed

```text
좁은 RED 대상 GREEN 확인:
2 tests completed
BUILD SUCCESSFUL in 13s
```

실 DB read-only 집계:

```text
installed_rank | version | description              | success
37             | 37      | mark active bundle components default | t

COMMERCIAL_MULTI | 72  | 137  | 137
SINGLE_SET      | 271 | 1447 | 855
```

마이그레이션 파일 축:

```text
CURRENT_MIGRATION_FILES=37
OPEN_PR_PRODUCT_MIGRATIONS=0
```

### skipped

- V38 적용: 공유 DB 쓰기 금지로 실행하지 않음.
- 실 데이터 신 모델 전환 후 금액 diff: V38 충돌을 해결하지 않은 상태에서 실행하지 않음.
- desktop 단위/typecheck: 화면을 건드리지 않아 대상 아님.

### failed

product-service 전량 테스트:

```text
782 tests completed, 383 failed
```

첫 공통 원문:

```text
org.flywaydb.core.api.FlywayException: Found more than one migration with version 38
Offenders:
.../build/classes/java/main/ (JDBC)
.../services/product-service/build/resources/main/db/migration/V38__bundle_component_allocation_contract.sql (SQL)
```

부분 실패에는 기존 `BundleComponentServiceTest` 8건의 `IllegalArgumentException`도 포함됐다. 이는 추가 검증이 기존 구형 요청(FIXED 금액 미지정)을 거부한 결과이며, 충돌 확인 후 부분 변경을 정리했다. 전량 재실행은 V38 점유 결정 후 진행해야 한다.

## 3축 재확인

- 파일: SQL migration은 V1~V37, 파일 수 37.
- 공유 DB: Flyway 최고 성공 적용은 V37.
- 열린 PR: product-service migration 파일 0건.
- 다른 워크트리: 10개 모두 SQL migration 수 37.
- 단, 현재 Java classpath에는 `V38__ProductCategoryBackfill.java`가 존재한다. 이것이 정찰 결과와 충돌하는 핵심 사실이다.

## 데이터 무변경 관문

공유 DB는 쓰지 않았으며, 현재 기준 실측은 싱글세트 271건·활성 구성행 1,447건·기본 구성행 855건이다. V38을 적용하지 않았으므로 신 모델로 금액을 바꾼 건수는 **0건**이다. 이는 전환 parity 통과 수치가 아니라, 적용을 중단해 금액 변경을 발생시키지 않은 수치다.

## 라운드 종료 파일 확인

PR #1187 Files API 기준 삭제된 추적 파일:

```text
PR1187_REMOVED_COUNT=0
```

---

## 2026-08-12 V39 RESUME

- Existing `V38__ProductCategoryBackfill.java` was preserved.
- Added V39 allocation contract migration: AUTO/FIXED mode, component weight, fixed amount, and product rounding unit (default 1000).
- Existing `BundleExpander` calculation path remains unchanged in this slice.
- Replace-all storage validates AUTO weights sum to 10; FIXED-only commercial-multi requests remain valid.
- Component registration defaults FIXED amount to the component delivery price.

### RED

```text
> Task :services:product-service:compileTestJava FAILED
error: cannot find symbol
  symbol:   variable BundleAllocationPolicy
  symbol:   class AllocationMode
14 errors
BUILD FAILED
```

### Verification

```text
policy test: 2 tests completed / BUILD SUCCESSFUL in 19s
BundleExpanderIT: 25 tests completed / BUILD SUCCESSFUL in 36s
product-service full: PASSED=783 SKIPPED=0 FAILED=0 TOTAL=783
BUILD SUCCESSFUL in 3m 13s
Successfully validated 39 migrations
Migrating schema "public" to version "39 - bundle component allocation contract"
now at version v39
```

Shared DB was read-only and remains at V37:

```text
single_sets=271 | active_component_rows=1447 | default_rows=855
```

Because the calculation consumer remains unchanged, real-data calculated amount changes are **0 rows**. V39 was not applied to the shared DB per the no-write instruction.

Final counts: passed 783 / skipped 0 / failed 0. PR #1187 removed tracked files: 0.

이번 라운드에서 기존 추적 파일을 삭제하지 않았다. 다음 작업은 개발책임자께서 기존 `V38__ProductCategoryBackfill.java`를 어떻게 처리할지 결정한 뒤 재개해야 한다.
