# 2026-08-02 #1035 CI 실패 조사 및 수정

## 조사 기록
- 보고서 생성 및 제목 저장 완료.
- 저장소 지침(AGENTS.md, docs/handoff/CURRENT-WORK.md, .codex/AGENTS.md)을 읽는 중입니다.

### 확인: slip-units 로컬 실행
- 실행 명령: CI의 :services:slip-service:test + slip-units 필터 전체.
- 결과: Gradle 프로세스가 제한 시간 내 완료되지 않아 실행 도구가 종료되었습니다. 현재 생성된 JUnit XML 개수와 실패 상세를 추가 확인합니다.

### CI failure evidence (raw summary)
- `gh run view 30719436270 --log-failed` confirmed the same root failure in all three groups: `Caused by: java.lang.IllegalStateException at WarehouseCodeMapper.java:58`.
- `slip-units`: `790 tests completed, 1 failed`; JUnit: `790 tests run, 789 passed, 0 skipped, 1 failed`. Failing test: `PublicSlipAttachmentControllerIT.upload_hyphenSlipNoSlug_returns201_andStoresAttachment()`.
- `slip-it-public`: `89 tests completed, 57 failed`; JUnit: `89 tests run, 32 passed, 0 skipped, 57 failed`. First named failure: `DeliveryBatchControllerIT.autoGroup_managerRole_returns200()`; the remainder are context-load propagation failures.
- `slip-it-core`: `673 tests completed, 611 failed`; JUnit: `673 tests run, 62 passed, 0 skipped, 611 failed`. SlipCollab/cutoff/dispatch ITs fail at context load for the same reason.
- Classification: these are not failures caused by removing the external warehouse lookup. They are startup configuration failures before domain behavior runs.
- Direct cause: `application.yml` contains escaped placeholders such as `\${WAREHOUSE_UUID_HQ}`, so Spring receives the literal placeholder instead of the CI UUID environment value. No external client call appears in the failure path.
- Minimal fix applied: removed the four escaped/quoted environment placeholders in `services/slip-service/src/main/resources/application.yml`; no client, datasource, or external lookup code was restored.
- Local validation: `WarehouseCodeMapperStartupValidationTest` completed with `BUILD SUCCESSFUL` (18 actionable tasks, 2 executed, 16 up-to-date).
