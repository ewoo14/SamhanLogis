# #1008 일마감 옵션 정액 DC 1차 적대검증 — 도달성

## 0. 착수 확인

- 작업 브랜치: `feat/1008-daily-closing`
- 검증 기준 HEAD: `7c72a637f [FEAT] #1008 일마감에 옵션 정액 DC 6종 반영`
- 착수 시 작업트리 변경: 본 리뷰 보고서 1개만 untracked. 코드 변경 없음.
- 제한 준수: Git 쓰기, 코드 수정, Docker 재빌드·재기동, 실 DB 쓰기, 전체 테스트 스위트를 수행하지 않는다.

## 1. 선행 보고서 판독

- 구현 보고서의 주장: 옵션 정액 6종은 싱글 세트 재검증의 기대 납품금액에서 차감되고, 고정DC/전역DC 선택 우선순위와 저장 snapshot·`totalAmount`는 변경하지 않았다.
- 정찰 보고서의 레거시 근거: GAS는 카탈로그 구성품 납품가 합계에서 선택 옵션 정액 1종의 절댓값을 차감한 뒤 전표 구성품 VAT 포함 단가 합계와 비교한다(`Code.js:619-656`).
- 정찰 실 DB 선행 집계: 활성 210곳 중 옵션 정액 보유 46곳, 미보유 164곳. 이 수치는 아래에서 독립 재집계한다.
- 위 항목은 선행 보고서의 주장일 뿐이며, 아직 본 라운드의 독립 검증 결론으로 채택하지 않았다.

## 2. PR HEAD 변경면 확인

재현 명령:

```text
git show --stat --oneline --decorate --no-renames HEAD
```

출력 원문:

```text
7c72a637f (HEAD -> feat/1008-daily-closing, origin/feat/1008-daily-closing) [FEAT] #1008 일마감에 옵션 정액 DC 6종 반영
 docs/dev-reports/2026-08-02-1008-impl.md           | 122 +++++++++++++++++++++
 .../accounting/client/PartnerDcConfigClient.java   |  24 +++-
 .../accounting/service/DiscountRevalidator.java    |  54 ++++++++-
 .../accounting/service/MonthEndCloseService.java   |   4 +-
 .../service/DiscountRevalidatorTest.java           |  42 +++++++
 5 files changed, 236 insertions(+), 10 deletions(-)
```

- 운영 코드 변경은 DC 조회 응답 매핑, 재검증 계산, 호출부 전달에 한정된다.
- `DailyClosing` entity/repository, 일마감 snapshot 저장, 세금계산서, 회계 장부, 부가세 신고 코드는 diff에 없다.
- 다만 “diff에 없음”만으로 런타임 간접 영향 부재를 확정하지 않고 호출/데이터 흐름을 별도로 확인한다.

## 3. 적용 순서와 레거시 대조

재현 명령:

```text
Get-Content tools/legacy-gas/일마감 프로그램/Code.js (403-415, 619-656, 714-731행)
Get-Content services/accounting-service/.../DiscountRevalidator.java (133-176행)
```

핵심 출력 원문:

```text
레거시 650: var finalExpectedPrice = expectedPriceSum - discount;
레거시 654: if (Math.abs(invoicePriceSum) === Math.abs(finalExpectedPrice)) {
레거시 721: if (item._fixedDc != null) {
레거시 723: } else if (item._zone === 'COMM_MULTI') {
현행 134: if (isMulti(safeItemName, safeModelToken)) {
현행 135:     Integer expectedRate = expectedRate(fixedDc, globalDiscount, safeModelToken);
현행 147: if (isSingleSetDependent(safeModelToken)) {
현행 154:     effectiveDeliveryPrice = effectiveDeliveryPrice.subtract(optionDiscount.abs());
```

- 레거시와 현행 모두 고정DC/전역DC율은 **멀티 분기**, 옵션 정액은 **싱글 세트 분기**다. 같은 계산값에 율과 정액을 연속 적용하지 않으므로 “율 먼저/정액 먼저”에 따른 산술 순서 차이는 이 사용자 경로에서 발생하지 않는다.
- `fixedDc != null`이면 즉시 고정DC율을 반환하고, 그때만 전역DC율을 읽지 않는 기존 `fixedDc ?? globalRate` 우선순위가 유지된다. 새 우선순위 로직은 없다.
- 옵션 정액은 레거시와 동일하게 절댓값을 취해 기대 납품금액에서 차감한다.
- 단, 레거시는 **매칭된 세트명 `setName`**으로 옵션 종류를 고르지만 현행은 **집계 행의 `modelToken`**으로 고른다. 실 사용자 데이터에서 두 값이 실제로 같은지/도달하는지는 아래 실측 대상으로 남긴다.
