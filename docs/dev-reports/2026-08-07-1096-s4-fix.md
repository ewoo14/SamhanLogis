# PR #1097 / 이슈 #1096 — S4 fix 결과

- 작업일: 2026-08-07 KST
- 범위: D2′ 신규 full-seed opt-in 복구, QA797 및 cleanup provenance 일반 복원 차단
- 제약 준수: git 명령 없음, DB 쓰기 없음, 컨테이너 재빌드·재시작 없음, 전체 테스트 스위트 미실행

## 1. 새로 가능해진 상태·문서 조합과 각각의 결과

### RED-first

기존 `Issue1096S2FixContractTest`에 S4 계약을 먼저 추가하고 실행했다.

```text
Issue1096S2FixContractTest > redA_normalRestartCannotRecreateDeletedSeedGraph() FAILED
Issue1096S2FixContractTest > redB_cleanupPreservesCanonicalLinesAndRestoresOrFailsLoudly() FAILED
2 tests completed, 2 failed
BUILD FAILED
```

실패 원인은 기본 OFF와 명시적 full-seed ON의 분리 계약 및 QA797 복원 차단이 없었기 때문이다.

### GREEN 결과

| 상태·문서 조합 | 결과 |
|---|---|
| 기본 개발 셋업 재기동 + 정리된 테스트 전표·견적·주문 | **GREEN** — 기존 개별 seed flag와 공통 full-seed flag가 모두 기본 false이며, 시더는 삭제행 포함 결정적 번호를 계속 skip한다. |
| 명시적 `SAMHAN_FULL_SEED_TEST_DATA=true` + 신규 개발 DB | **GREEN** — dev profile에서 판매전표 100건·견적 40건·주문 30건 시더가 활성화된다. |
| full-seed로 생성된 문서 | **GREEN** — 기존 시더의 `Stage 2 시드`/`P2 시드`/`Seed sample` provenance를 유지해 다음 정리 대상과 구분 가능하다. |
| cleanup actor(`issue-1096-test-seed-cleanup`)가 삭제한 견적 일반 복원 | **GREEN** — 행은 보존하고 409로 차단한다. |
| 삭제 라인에 `QA797-*` provenance가 있는 견적 일반 복원 | **GREEN** — 행은 보존하고 409로 차단한다. |
| 순수 정본 견적 정상 복원 | **GREEN 유지** — QA 잔재 판정에 해당하지 않으며 S2/S3의 actor·라인 완전성 및 금액 재계산 경로를 유지한다. |

실 데이터 판정 수량은 선행 SOL 실측을 기준으로 다음과 같다.

```text
삭제 견적 7건 중 일반 복원 가능 4건 / 차단 3건
  차단 3건 = QA797 잔재 3건 (2026/07/22-1, 2026/07/22-2, 2026/07/27-1)
  최대 금액 = 9,999,999,999,999원
정상 복원 가능 4건은 QA797 또는 cleanup actor가 아닌 정본 경계다.
주문 DRAFT 정상 복원 3건은 S3 계약으로 가능하며, 타임스탬프 미세 차이로 막히는 건은 0건이다.
```

이번 라운드에서는 DB SELECT를 재실행하지 않았고, 위 수량은 `2026-08-07-1096-sol-s2-reconvergence.md`와 S3 실측을 재사용했다. 마이그레이션 V31/V117/V16은 추가·수정하지 않았다.

## 2. 제거·이동·개명한 식별자 grep 전수

제거·이동·개명한 기존 식별자는 없다. 새 식별자는 다음과 같이 전수 확인했다.

```text
SAMHAN_FULL_SEED_TEST_DATA
  infrastructure/.env.dev-seed                         기본 false 1건
  infrastructure/docker-compose.local-all.yml          slip/order 전달 2건
  slip/application.yml, partner-order/application.yml  서비스별 기본 false 2건

full-seed-test-data
  slip/EstimateSeeder, SlipSeeder                    명시적 OR 조건
  partner-order/PartnerOrderSeeder                    명시적 OR 조건

QA797-
  EstimateService 복원 차단 판정 + S4 계약 테스트

issue-1096-test-seed-cleanup
  V117 기존 삭제 provenance 및 EstimateService 복원 차단 판정 유지
```

S3의 `deletionBatchLines` 및 `deletedBy + deletedAt` 분모 계약은 변경하지 않았다. 물리 삭제 SQL이나 복원용 UPDATE는 추가하지 않았다.

## 3. 바꾼 파일을 참조하는 테스트 실행 결과

### 통과

```text
.\gradlew.bat :services:slip-service:test `
  --tests com.samhanair.logis.slip.issue1096.Issue1096S2FixContractTest `
  --tests com.samhanair.logis.slip.seed.EstimateSeederTest `
  --no-daemon --console=plain

2개 테스트 클래스·4개 테스트 / BUILD SUCCESSFUL

.\gradlew.bat :services:partner-order-service:test `
  --tests com.samhanair.logis.partnerorder.seed.PartnerOrderSeederTest `
  --no-daemon --console=plain

1개 테스트 클래스·2개 테스트 / BUILD SUCCESSFUL
```

두 실행 모두 변경 Java의 compileJava/compileTestJava를 포함했다.

### 실행하지 않은 것

- 전체 `slip-service`/`partner-order-service` 테스트 스위트: S3에서 확인된 timeout 제약 때문에 실행하지 않음
- DB migration 실제 적용 및 실 DB 복원 호출: DB 쓰기 금지 때문에 실행하지 않음
- 컨테이너 재빌드·재시작 및 full-seed 라이브 기동: 운영 트랙 공유 컨테이너 보호를 위해 실행하지 않음

## 신규 파일

- `docs/dev-reports/2026-08-07-1096-s4-fix.md`

## 변경 파일

- `services/slip-service/src/main/java/com/samhanair/logis/slip/seed/SlipSeeder.java`
- `services/slip-service/src/main/java/com/samhanair/logis/slip/seed/EstimateSeeder.java`
- `services/partner-order-service/src/main/java/com/samhanair/logis/partnerorder/seed/PartnerOrderSeeder.java`
- `services/slip-service/src/main/java/com/samhanair/logis/slip/estimate/service/EstimateService.java`
- `services/slip-service/src/main/resources/application.yml`
- `services/partner-order-service/src/main/resources/application.yml`
- `infrastructure/docker-compose.local-all.yml`
- `infrastructure/env-templates/.env.dev-seed`
- `services/slip-service/src/test/java/com/samhanair/logis/slip/issue1096/Issue1096S2FixContractTest.java`
