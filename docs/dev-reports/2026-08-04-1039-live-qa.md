# PR #1045 / 이슈 #1039 라이브 QA 보고서

- 실행일: 2026-08-04 (Asia/Seoul)
- 작업 디렉터리: `C:/dev/Samhan-Public/.claude/worktrees/t1039`
- 브랜치 / HEAD: `feat/1039-provisional-dispatch` / `09778d88b9cc884f46884775c289f8fe54221636`
- mock 모드: OFF

## 0. 환경 확인

- 프런트 포트: `18503`, HTTP 200 확인
- 실행 config: `vite.renderer.dev.config.ts`
- 런타임: `VITE_AROLOGIS_API_BASE=http://localhost:8080`, `VITE_VERSION_API_BASE_URL=http://localhost:8080`, `VITE_APP_VERSION=2026/08/04-1`, `VITE_MOCK_MODE=0`
- 호출 경로: `GET /admin/arologis/dispatches/pre-classify?from&to&mode` → 게이트웨이 `:8080` → `arologis-service`
- 컨테이너 생성 시각:
  - `samhan-arologis-service`: `2026-07-22T17:18:05.183753593Z`
  - `samhan-slip-service`: `2026-08-03T17:02:05.035143939Z`
  - `samhan-api-gateway`: `2026-07-31T15:15:50.070347996Z`
- DB 직접 INSERT/UPDATE: 수행하지 않음

## 1. 인증·배포 확인

- 실제 GUI에서 `dev_master` / `${QA_DEV_DEFAULT_PASSWORD}` 입력 후 로그인 버튼을 클릭했다.
- `POST http://localhost:8080/auth/admin/login`: HTTP 401, `인증 토큰이 없습니다`.
- 표준 `POST http://localhost:8080/auth/login`: HTTP 200으로 JWT 발급. 그러나 renderer는 `/auth/admin/login` 세션을 요구하여 `#/login`으로 복귀했다.
- `samhan-arologis-service`는 2026-07-22 생성본이다. `git show origin/main:services/arologis-service/src/main/java/com/samhanair/logis/arologis/service/PreClassifyService.java`로 현재 소스 존재를 확인했다. 현재 소스에는 `STACK_ONLY`/`REGION_ONLY` 및 상일·초월 창고명 필터가 있다.
- 재배포는 하지 않았다. 공유 스택 보호를 위해 PM 판단이 필요하다.

## 2. 모드별 결과

| 모드 | 조회 조건 | 기대 건수 | 실제 건수 | 캡처 파일 | 판정 |
|---:|---|---:|---:|---|---|
| 4 | `from=2026-01-01,to=2026-08-04,mode=STACK_ONLY` | 11 | API 2309 (`regionGroups=0`, `unclassified=2309`); GUI 로그인 차단 | `r15-04-mode-4-stack-blocked.png` | BLOCK |
| 5 | `from=2026-01-01,to=2026-08-04,mode=REGION_ONLY` | 12 | API 2309 (`regionGroups=0`, `unclassified=2309`); GUI 로그인 차단 | `r15-05-mode-5-region-blocked.png` | BLOCK |
| 1 | `mode=SANGIL_AND_CHOWOL_REGION_EXCLUDED` | 목적지 전표 생성 후 | API 2309; 목적지 전표 미생성; GUI 로그인 차단 | `r15-01-mode-1-sangil-chowol-excluded-blocked.png` | 미판정 |
| 2 | `mode=CHOWOL_REGION_EXCLUDED` | 목적지 전표 생성 후 | API 2309; 목적지 전표 미생성; GUI 로그인 차단 | `r15-02-mode-2-chowol-excluded-blocked.png` | 미판정 |
| 3 | `mode=SANGIL_REGION_EXCLUDED` | 목적지 전표 생성 후 | API 2309; 목적지 전표 미생성; GUI 로그인 차단 | `r15-03-mode-3-sangil-excluded-blocked.png` | 미판정 |
| 6 | `mode=SANGIL_AND_CHOWOL_REGION_INCLUDED` | 목적지 전표 생성 후 | API 2309; 목적지 전표 미생성; GUI 로그인 차단 | `r15-06-mode-6-sangil-chowol-included-blocked.png` | 미판정 |
| 7 | `mode=CHOWOL_REGION_INCLUDED` | 목적지 전표 생성 후 | API 2309; 목적지 전표 미생성; GUI 로그인 차단 | `r15-07-mode-7-chowol-included-blocked.png` | 미판정 |
| 8 | `mode=SANGIL_REGION_INCLUDED` | 목적지 전표 생성 후 | API 2309; 목적지 전표 미생성; GUI 로그인 차단 | `r15-08-mode-8-sangil-included-blocked.png` | 미판정 |

모든 캡처는 `docs/qa/1039-provisional-dispatch-real-qa/`에 실제 브라우저 화면으로 저장했다. 인앱 브라우저는 사용할 수 없어 Playwright Chromium의 실제 GUI 창으로 대체했다.

## 3. 실 API 경로로 생성한 QA 데이터

- `POST /inventory/warehouses` HTTP 201: `QA-1039-SANGIL`, `상일창고 QA-1039-상일`, id `d98f13e6-53cf-4f59-9a1c-efaf01f31bec`.
- `POST /inventory/warehouses` HTTP 201: `QA-1039-CHOWOL`, `초월창고 QA-1039-초월`, id `e6ce7153-2b8a-4cde-abea-82f79d8fe256`.
- 두 창고 모두 description에 `QA-1039-실 API 생성 — 삭제 금지`를 넣었고 삭제하지 않았다.
- 목적지 출고전표 2건은 `POST /slips`로 각각 시도했으나 모두 HTTP 403 `전표 변경 권한이 없습니다`. 생성된 전표는 없다.

## 4. 이 라운드가 보지 않은 것

- 목적지 창고를 가진 `QA-1039-` 출고전표 생성 및 모드 1·2·3·6·7·8의 positive 발화.
- renderer 내부의 모드 선택/조회 결과. `/auth/admin/login` 401로 로그인 화면을 벗어나지 못했다.
- 모드 4·5의 GUI positive 결과. API는 200이나 현재 배포 응답은 기대 11·12가 아닌 2309건이다.
- stale `arologis-service` 재배포 후 재검증.
