# GAS 전수조사 — 개발책임자 결정 기록

> 이 파일은 **결정의 정본**입니다. 구현 시 여기를 근거로 삼으십시오.
> 조사 근거: `2026-08-11-gasv2-CRITIC.md` (최종 후보 13건) → 3축 대조 후 상정.

---

## 0. 상정 전 3축 대조 (①코드 ②이슈 ③기존 결정)

Critic 이 올린 13건을 그대로 묻지 않았습니다. 이번 세션에 **이미 있는 것을 네 번 물었기** 때문에
`git ls-files` · `git grep` · `gh issue list --state all` (205건) 로 먼저 걸렀습니다.

| Critic 항목 | 대조 결과 | 처리 |
|---|---|---|
| **5. 기본구성품 0건 BUNDLE 72개** | **개발책임자께서 이번 세션에 이미 결정** — *"72개의 기본 구성품을 지정해 준다"*. `V37` 로 이행 완료(`6524e5083`) | **제외** |
| **12. 입출고 예측 존폐** | **이미 구현돼 있음** — `clients/desktop/src/renderer/routes/warehouse/inoutAnalysisModel.ts:113` 이 정확히 그 "고정 연도 비율"(`totalCurrent / totalPrevious` · 12개월) 이다. `#1012 CLOSED` | 질문을 *"존폐"* → *"이 계산식을 바꿀지"* 로 **변경** 후 상정 예정 |
| 8. 장기미발주 판정축 | `#1015 CLOSED` 화면 존재. 판정축 변경 질문은 유효 | 유지 |
| 9. 미배차 상태·문자 | 상태 문자열(`야적미배차`·`지방미배차`·`해당없음`)이 저장소에 **0건** — 미이식 확정 | 유지 |
| 1. 영업수수료 | `git grep commission` 서비스 코드 **0건** · 관련 이슈 **0건** — 유실 확정 | 유지 |
| 6. 원장 특례 계정 | `#1072 OPEN`(계정과목 이카운트 전환)이 인접하나 특례 부호는 별건 | 유지 |

---

## 1. 확정된 결정 (2026-08-11)

### D-G1. 영업수수료 정산 → **정산 도메인 신설 (A)**

```text
근거   git grep commission → 서비스 코드 0건 · 관련 이슈 0건 = 금액 기능 전체 유실
결정   versioned 수수료 계약 + 정산 엔티티로 이식한다
계약   카드 3% · 제경비 8%(수기율 허용) · 원천 3.3% · 설치 8%
       항목별 대칭 반올림 · 선지급은 지급액에만 반영
요구   요율을 버전으로 저장 — 8%→7% 로 바뀌어도 과거 정산이 재계산되면 안 된다
연결   세금계산서 연결까지 범위
```

#### 🆕 추가 결정 (2026-08-11 개발책임자) — **그룹웨어 결재로 연결**

> *"영업수수료 계산은 결재문서 작성으로 그룹웨어에 연결되어 **지출결의서 작성에 해당 영업수수료 문서가 포함될 수 있도록** 연결"*

```text
의미   영업수수료 정산은 계산으로 끝나지 않는다. 계산 결과가 **결재문서**가 되고,
       그 문서를 **지출결의서에 붙여** 결재를 올릴 수 있어야 한다.
       ⟹ 지급 근거가 결재 이력으로 남는다 (B안 '리포트만' 을 배제한 이유가 여기서 확정됨)
```

**이미 있는 연결 지점 (신규 설계 아님 — 확장):**

| 실물 | 좌표 | 역할 |
|---|---|---|
| `ApprovalReferenceDocType` | `groupware/domain/ApprovalReferenceDocType.java` | 결재 첨부가 참조하는 업무문서 유형 enum — 현재 `OUTBOUND_SLIP` · `INBOUND_SLIP` · `JOURNAL` · `TAX_INVOICE` · `STATEMENT` · `PARTNER_LEDGER` **6종** |
| `ApprovalAttachment` / `ApprovalAttachmentType` | 같은 패키지 | 결재문서에 업무문서를 참조로 매다는 구조 |
| `GROUPWARE_EXPENSE_REPORT` | `ApprovalTemplate.businessCode` · 문서양식 `docType` | **지출결의서** 양식이 이미 운영 중 (`#845`·`#911`·`#914` 라이브QA 존재) |

**구현 요구:**

```text
1  ApprovalReferenceDocType 에 영업수수료 정산서를 **7번째 유형으로 추가**
   (기존 6종을 건드리지 말 것 — 이미 결재 이력이 이 값들을 참조한다)
2  지출결의서(GROUPWARE_EXPENSE_REPORT) 작성 화면에서 정산서를 **참조 첨부로 고를 수 있어야** 한다
   🔑 "PDF 를 만들어 올린다" 가 아니라 **참조**다 — 정산 금액이 결재문서에서 조회돼야 한다
3  정산서 1건이 지출결의서 여러 건에 중복으로 붙는 것을 어떻게 다룰지 정할 것
   (기존 6종이 이 문제를 어떻게 다루는지 먼저 읽고 같은 규칙을 따를 것 — 새 규칙을 만들지 말 것)
4  결재 완료/반려가 정산서 상태에 영향을 주는지 — **PM 이 코드로 확정한 뒤 설계를 낸다.**
   기존 6종 중 상태 역전파가 있는 것이 있으면 그것을 따르고, 없으면 개발책임자께 올린다
```

🚨 **잃으면 안 되는 것**: 기존 6종 참조 유형과 지출결의서 양식(`#845`/`#911`/`#914` 로 라이브QA 통과분)의
동작이 바뀌면 안 됩니다. 구현 브리핑의 RED-B 표적을 **`ApprovalReferenceDocType` 기존 6종의 결재 첨부 렌더링**으로 지목하십시오.

#### 🆕 추가 결정 (2026-08-11 개발책임자) — **연결 버튼**

> *"영업수수료 계산 후 그룹웨어로 연결되는 **버튼을 삽입**"*

```text
의미   정산 화면에서 계산을 끝낸 뒤, 그 화면을 떠나지 않고 결재로 넘어갈 수 있어야 한다.
       사용자가 그룹웨어 메뉴를 따로 찾아 들어가 정산서를 다시 고르게 하면 안 된다
```

🚩 **이 버튼은 저장소 최초 사례입니다.** `git grep` 실측 — 업무화면에서 결재로 넘어가는
진입점(`결재 상신`·`결재요청` 버튼)이 **현재 한 건도 없습니다.** 참고할 기존 패턴이 없으므로
설계를 PM 이 냅니다(2026-08-07 규칙).

| 실물 | 좌표 |
|---|---|
| 결재 생성 API | `clients/desktop/src/renderer/api/groupwareApproval.ts:115` · `:134` (`POST /admin/groupware/approvals`) |
| 권한 키 | `groupware.approvals` (`view` / `update`) |

**구현 요구:**

```text
1  정산 화면에 버튼 1개. 누르면 지출결의서(GROUPWARE_EXPENSE_REPORT) 작성으로 이동하고
   **그 정산서가 참조 첨부로 이미 붙어 있는 상태**로 열려야 한다
   🚫 빈 지출결의서를 열고 사용자가 다시 고르게 하는 것은 이 결정을 만족하지 않는다
2  권한 — groupware.approvals 가 없는 사용자에게 버튼이 보이면 안 된다
   🔑 숨기기만 하지 말고 **백엔드도 거절**해야 한다 (권한은 두 층에서 다 지켜야 한다)
3  이미 결재가 올라간 정산서에서 버튼을 다시 누르면 어떻게 되는가 — 이 상태 조합을 먼저 열거할 것
4  라이브QA 필수 — 버튼 클릭 → 지출결의서가 정산 금액을 달고 열리는 화면을 스크린샷으로
```

🚨 **RED-B 표적**: *"정산 화면의 기존 계산·저장 동작"* 과 *"지출결의서를 그룹웨어 메뉴에서
직접 새로 만드는 기존 경로"*. 버튼을 붙이면서 이 둘 중 하나가 막히는 것이
이 저장소에서 가장 자주 나온 실패 방식입니다(fix 가 정상 경로를 막는다).

#### 🆕 추가 결정 (2026-08-11 개발책임자) — **회계 탭에 메뉴 신설**

> *"영업수수료 정산은 **회계 탭에 메뉴 신설** 요청"*

```text
의미   정산 화면은 독립 메뉴가 아니라 기존 회계 카테고리 안에 들어간다
⟹ 서비스 배치도 이것으로 정해집니다 — **accounting-service**
   (S1 에서 "어느 서비스에 둘지" 를 구현자 판단으로 남겼으나 이 결정이 확정합니다)
```

**좌표 (PM 실측):**

| 실물 | 좌표 |
|---|---|
| 회계 사이드바 카테고리 | `clients/desktop/src/renderer/components/AppLayout.tsx:909` — `<SidebarCategory label="회계">` |
| 경로 접두 · 권한 | `AppLayout.tsx:192` — `{ prefix: '/accounting', pageCode: 'accounting.reports', label: '회계' }` |
| 형제 메뉴 | `/accounting/sales-slips` · `/purchase-slips` · `/journals` · `/tax-invoices` · `/partner-ledger` · `/period-close` 등 **20+** |
| 권한 매트릭스 | `PermissionMatrixPage.tsx:108` — `label: '회계'` |

**구현 요구:**

```text
1  새 라우트를 /accounting/… 아래에 두고 SidebarCategory "회계" 의 activeTargets 에 추가
2  🚨 권한 pageCode 를 새로 만들 것인지 기존 accounting.* 를 쓸 것인지 정할 것.
   PermissionMatrixPage 에도 같이 반영해야 화면·백엔드가 어긋나지 않는다
   🔑 이 저장소는 권한을 mock 만 맞추고 백엔드를 안 맞춰 false-green 을 낸 적이 있다
      (#1130 — DRIVER 를 부여해도 161/161 통과). 전수 대조표를 요구할 것
3  RED-B 표적 = **기존 회계 메뉴 20여 개가 그대로 보이고 그대로 열린다**
   (카테고리에 항목을 더하면서 기존 것이 사라지는 것이 이 저장소의 단골 실패다)
```

#### 🆕 추가 결정 (2026-08-11 개발책임자) — **정산 자체가 문서다 · 번호 `YYYY/MM/DD-{문서번호}`**

> *"영업수수료 계산도 **하나의 문서**로 `YYYY/MM/DD-{문서번호}` 형태로 하여 이를 그룹웨어 문서에 연결"*

```text
의미   영업수수료 정산은 화면의 계산 결과가 아니라 **채번된 문서**다.
       번호가 있어야 결재가 그것을 가리킬 수 있고, 나중에 "어느 정산이 어느 결의서로 나갔나" 를 추적할 수 있다
```

🔑 **이 번호 체계는 발명 대상이 아닙니다 — 저장소 표준입니다.** `yyyy/MM/dd-N` 으로 이미 채번하는 문서가 5종:

| 문서 | 채번 |
|---|---|
| 분개 | `JournalNumberSequence` · `JournalNumberService` |
| 세금계산서 | `TaxInvoiceNumberSequence` (ISSUED 진입 시 채번 · DRAFT 는 NULL) |
| 입금보고서 | `CashReceiptNumberService` |
| 판매/구매 회계전표 | `SalesAccountingSlipNumberGenerator` · `PurchaseAccountingSlipNumberGenerator` |

공통 구현 형태 (`CashReceiptNumberService:24-33`):

```java
// 일자별 시퀀스 행을 insertIfAbsent 후 row lock 으로 잡고 N 을 증가시킨다
sequenceRepository.insertIfAbsent(UUID.randomUUID(), date);
return date.format(DATE_FMT) + "-" + seq.next();   // yyyy/MM/dd-N
```

**🚩 결정적 발견 — 그룹웨어 연결이 이 번호로 이미 설계돼 있습니다.**

`ApprovalAttachment` 는 참조 대상을 **UUID 가 아니라 문서번호 문자열**로 잡습니다:

```java
@Column(name = "ref_doc_no", length = 40)      private String refDocNo;
@Enumerated(EnumType.STRING)
@Column(name = "ref_doc_type", length = 30)    private ApprovalReferenceDocType refDocType;
```

⟹ 정산서에 `yyyy/MM/dd-N` 을 채번해 두면 **그룹웨어 쪽 스키마 변경 없이** enum 값 하나만 추가하면 연결됩니다.
`ref_doc_no` 는 40자라 `2026/08/11-1234` 는 여유롭게 들어갑니다.

**구현 요구:**

```text
1  영업수수료 정산 엔티티에 문서번호 컬럼 + **일자별 atomic 시퀀스 테이블**
   🚫 새 채번 방식을 만들지 말 것 — 위 5종 중 하나를 그대로 따를 것
   🔑 어느 날짜로 채번하는가를 먼저 정할 것 (정산 기준일? 작성일? 지급예정일?)
      세금계산서는 "ISSUED 진입 시 채번" 이라 DRAFT 는 번호가 없다 —
      정산서도 확정 전에는 번호 없이 둘지 결정 필요
2  ApprovalReferenceDocType 에 7번째 값 추가 · ref_doc_no 에 정산서 번호를 넣는다
3  🚨 채번된 번호가 **실제로 채워지는지** 확인할 것.
   이 저장소는 "조인 키로 쓰는 코드 컬럼이 비어 있고 UUID 만 채워진" 결함을 하루에 세 번 낸 적이 있다
   (#1060 주문 2,024건 중 조회 1건 · #1061 원장 후보 31건 전부 공백 · 12,276,000원 소실)
   ⟹ 구현 브리핑에 **"정산서 생성 직후 ref_doc_no 로 되찾아오는 왕복 테스트"** 를 RED 로 요구할 것
4  같은 날 여러 건이면 N 이 순증하는지 · 동시 생성 시 중복이 안 나는지 (row lock 이 그것을 막는다)
```

### ~~D-G2. 견적/주문 DC 상한 → 주문서 48% 로 통일~~ → 🚫 **철회 (2026-08-11)**

> *"견적앱은 원래 사용자가 자율 입력이 가능. 주문서는 48 상한이 맞음"* (개발책임자)

```text
🚩 철회 사유 — PM 이 틀린 전제로 선택지를 만들었다.
   PM 이 올린 것    "견적 49% clamp / 주문 48% clamp 로 갈라져 있다"
   LUNA 실측         견적앱은 clamp 가 **없다**. 45% + 티어보너스 최대 4% 로 49% 가 산출될 뿐
                     clients/web/estimate-app/views/index.ejs:13948 · :13955
                     주문앱만 0.48 clamp   clients/web/order-app/index.html:8127 · :8134
                     우리 백엔드는 상한이 사실상 없다 (0.9999)

⟹ 두 개의 clamp 가 아니라 **한쪽은 자유 입력 · 한쪽은 clamp** 였고, 그것이 의도된 설계였다.
   개발책임자 판정 = **현행이 옳다.** 견적은 자율 입력, 주문은 48% 상한.
   "통일" 은 존재하지 않던 문제를 푸는 것이었다.
```

🔑 **교훈** — 구현자가 *"전제가 틀렸다"* 며 멈춘 것이 잘못된 변경을 막았다.
브리핑에 넣은 *"제 전제가 틀렸다면 고치지 말고 중단·보고하십시오"* 가 이번에도 작동했다.
그리고 [[feedback_conflict_is_mostly_one_sided_blank]] 가 다시 맞았다 —
**올리기 전에 "고를 게 있긴 한가" 를 세지 않으면 선택지 자체가 잘못 만들어진다.**

🚩 **남은 실측 하나**: 주문서 48% 상한이 **레거시 프런트에만** 있고 우리 백엔드·데스크톱에는 없다.
   데스크톱으로 주문을 만들면 그 상한이 안 걸리는지 정찰 중(`2026-08-11-dc-cap-topology.md`).
   결과에 따라 별건으로 올린다. **이번 철회와는 별개 축이다.**

<details><summary>철회된 원문 (기록 보존)</summary>

### D-G2. 견적/주문 DC 상한 → **주문서 48% 로 통일 (B)** ⚠️ 권고와 다름

```text
PM 권고   A(문서별 현행 보존)
개발책임자 결정   B — 견적 단계부터 48% 가 상한
효과   견적 → 주문 금액이 절대 바뀌지 않는다
🚨 대가(반드시 처리)   49% 로 이미 나간 기존 견적서와 소급 불일치가 생긴다
   ⟹ 구현 시 **기존 견적 중 48% 초과 건이 몇 건인지 먼저 세고** 보고할 것.
      기존 행을 소급 변경할지는 그 숫자를 보고 다시 판단받는다 (임의 변경 금지)
   ⟹ 실외기 부재 페널티·티어 보너스 계산 자체는 유지하고 **clamp 값만** 48 로 통일
```

</details>

### D-G5. 입출고 예측 — 전년 자료가 없으면 **"예측 산출 불가"** 로 표시 (2026-08-11)

```text
결정   전년도 실적이 없는 품목은 0대로 예측하지 않고 "예측 산출 불가" 로 표시한다
좌표   clients/web/... 아니라 clients/desktop/src/renderer/routes/warehouse/inoutAnalysisModel.ts:113-116
       const forecastRate = totalPrevious > 0 ? totalCurrent / totalPrevious : 1
       forecast[i] = Math.round((previous[monthIndex] ?? 0) * forecastRate)
       ⟹ previous 가 0 이면 결과가 항상 0 이다
문제   화면의 0 이 "안 팔린다" 인지 "자료가 없다" 인지 구분되지 않는다.
       이 예측은 구매 발주에 쓰인다
대가   예측표에 빈칸이 늘어 한눈에 보기 어려워진다 (개발책임자가 감수하기로 함)
```

🚩 이 항목은 원래 *"입출고 예측 기능의 존폐"* 로 올라왔으나 **이미 구현돼 있어서**(`#1012 CLOSED`)
PM 이 질문을 *"계산식을 바꿀지"* 로 바꿔 상정했다. 3축 대조가 없었으면 없는 기능을 만들 뻔했다.

**구현 요구:**
```text
1  '자료 없음' 과 '실적 0' 을 구분하십시오 — 전년 합계가 0 인 것과 데이터 자체가 없는 것은 다릅니다
2  RED-B 표적 = **전년 실적이 있는 품목의 예측값이 하나도 바뀌지 않는다**
3  이 값을 읽는 다른 화면·집계가 있는지 grep 전수 (빈 값을 못 받는 소비자가 있으면 그것이 결함)
```

### D-G6. 정산 화면 권한 — **회계담당자 이상 기본 + 권한관리에서 메뉴 단위 관리** (2026-08-11)

> *"회계담당자 이상이 기본이며 해당 메뉴도 권한관리에서 관리"*

```text
기본값   회계담당자 이상이면 보인다
관리     그 메뉴를 **권한관리 화면에서 개별로 켜고 끌 수 있어야** 한다
⟹ 역할 게이트만으로 끝내지 않는다. 전용 pageCode 를 만들어
   PermissionMatrixPage 에 노출하고, 기본 부여 대상이 회계담당자 이상이다
```

**구현 요구:**
```text
1  전용 pageCode 신설 (기존 accounting.reports 재사용 아님)
2  PermissionMatrixPage.tsx:108 의 '회계' 그룹에 항목으로 등록
3  기본 부여 = 회계담당자 이상 역할. 마이그레이션으로 부여한다
4  🚨 권한은 **두 층에서 다 지켜야** 한다 — 화면에서 숨기고 백엔드도 거절
   🔑 이 저장소는 mock 만 맞추고 백엔드를 안 맞춰 false-green 을 낸 적이 있다
      (#1130 — DRIVER 를 부여해도 161/161 통과)
   ⟹ 구현 브리핑에 **7비트 exact × 모든 역할 전수 대조표** 와
      **뮤테이션 두 방향**(권한 없는 역할에 부여→RED / mock 에 초과 비트→RED)을 요구할 것
```

### D-G7. 정산서 확정 후 **기준일 수정 금지** (2026-08-11)

> 개발책임자 선택: *"확정 후 기준일 수정 금지"*

```text
문서번호는 정산 기준일(settlementDate)로 채번한다 (2026/08/11-3).
확정(CONFIRMED)하는 순간 기준일이 잠긴다. 고치려면 확정을 취소해야 한다.
⟹ 번호와 기준일이 **항상 일치**한다

왜 이 방향인가
  · 번호를 다시 채번하면 **이미 결재에 붙은 참조가 끊긴다**
    (그룹웨어 ApprovalAttachment 가 문서번호 문자열 ref_doc_no 로 참조한다)
  · 번호만 고정하고 기준일을 풀면 번호의 날짜와 실제 기준일이 달라져 조회가 헷갈린다
대가(감수함)  날짜 오타 하나를 고치려면 확정을 취소하고 결재를 다시 올려야 한다
```

**구현 요구 (S1 후속 라운드):**
```text
1  CONFIRMED 상태에서 settlementDate 변경 시도 → 거부
2  🚨 RED-B 표적 = DRAFT 상태에서는 기준일 수정이 **그대로 된다**
   (막는 fix 가 정상 경로까지 막는 것이 이 저장소의 단골 실패다)
3  확정 취소(CONFIRMED → DRAFT) 경로가 있는지 먼저 확인할 것.
   없으면 이 결정이 "영구 잠금" 을 뜻하므로 개발책임자께 다시 올린다
```

### D-G8. 제품구분(카테고리) 정비 — **품목명 자동분류 + 미분류** (2026-08-11)

> *"이상한데 품목명이 아니라 **제품구분**이 있고 거기서 실외기, 실내기 등을 모두 설정할 수 있어야 하는데?"*
> 신규(시트): *"품목명으로 자동 분류 후 나머지만 미분류"*
> 기존 3,084건: *"자동 916건 먼저 + 나머지 점진"*

```text
🚩 PM 이 두 번 잘못 짚었다
   1차  catL/catM/catS 를 봄        → 실외기·실내기 0건
   2차  product_category 를 봄       → 홈멀티/상업멀티/싱글 계열 구분이었다
   정답 categories 마스터에 실외기·실내기가 **이미 있다**. 축도 화면도 있고 값만 비어 있었다
   ⟹ 하마터면 "장비 역할 필드를 신설할까요" 라는 잘못된 선택지를 올릴 뻔했다.
      개발책임자 지적이 그것을 막았다
```

**실측 (PM · SOL 정찰)**

| 카테고리 | 사용 |
|---|---|
| 실외기 · 실내기 · 시스템 천장형 · 공조(HVAC) · 배관/부속 · 계장/제어 · 서비스/요금 | **0건** |
| 벽걸이형 | 1,121건 ← 시트 동기화가 신규 품목을 **전부 `INDOOR_WALL` 로 하드코딩**한 결과 |
| 이카운트 MIG-2 품목 | 1,963건 ← 적재가 몰아넣은 버킷 |

```text
활성 분모 3,084건 · 보수적 품목명 규칙으로 916건(29.70%) 확정 · 2,168건 애매
🚩 이카운트 재적재로는 못 채운다 — 실제 MIG-2 적재에 품목계층그룹 파일이 없었다
🚩 정상 sync 는 수동 카테고리를 보존하지만 **soft-delete 후 재등장하면 초기화 위험**
```

**설계**
```text
1  카테고리 마스터에 '미분류' 추가 (category_id 필수 계약을 지키면서 "아직 안 정해짐" 표현)
2  품목명 자동분류기를 **한 곳에** — 시트 동기화와 마이그레이션이 같은 규칙을 쓴다
3  시트 신규 품목 = 자동분류 시도 → 실패 시 '미분류'. INDOOR_WALL 하드코딩 제거
4  기존 데이터 = 자동분류 확정분 → 해당 카테고리 · 나머지 → '미분류'
   🚨 classification_manual = true 인 행은 건드리지 않는다 (사람이 정한 것)
   🚨 감사 테이블 + rollback (#1132 의 V37 을 본으로)
```

#### 🆕 추가 지시 (2026-08-11 개발책임자) — **세트 구성품도 분류 근거로 쓴다**

> *"이름과 **싱글 구성품들은 제품구분이 있을거라서** 그거보고 품목구분 분류 요청"*

`bundle_component.component_kind` 가 구성품 역할(OUTDOOR/INDOOR/REMOTE/PANEL/MATERIAL/ACCESSORY)을
이미 담고 있다. **그 행이 가리키는 품목의 제품구분을 역산**할 수 있다.

**PM 실측**

```text
component_kind 별 고유 품목코드   OUTDOOR 158 · INDOOR 192 · ACCESSORY 21 ·
                                  PANEL 16 · MATERIAL 15 · REMOTE 12
products 에 실재하는 활성 제품     401건 (역산 가능)
🔑 품목명 규칙으로는 못 잡던 **순증 94건**
충돌(한 품목이 2종 이상)           11건
```

**충돌 11건의 성격 — 규칙이 명확하다**

```text
무풍 1way 냉방전용 실내기  ->  ACCESSORY + INDOOR
무풍 1way 냉난방 실외기    ->  ACCESSORY + OUTDOOR
⟹ 전부 ACCESSORY 와의 충돌이고 **이름이 실내기/실외기를 명시**하고 있다
```

**확정 규칙 (우선순위)**

```text
1  classification_manual = true      → 건드리지 않는다 (사람이 정한 것)
2  품목명 규칙                        → 916건
3  세트 구성품 component_kind 역산     → 401건 (순증 94건)
4  둘이 충돌하면 **품목명이 이긴다**    → ACCESSORY 는 약한 신호다
5  그래도 안 잡히면 '미분류'
```

🚨 역산은 **한 방향만** 이다 — 구성품 행의 `component_kind` 로 그 **구성품 품목**을 분류하는 것이지
세트 자신을 분류하는 것이 아니다. 세트는 품목명 규칙으로만 판정한다.

🔑 **이것은 주문 40% 규칙(#1166)의 선행 슬라이스다.** 분류가 비어 있으면 규칙을 넣어도 한 건도 발동하지 않는다.

### D-G3. 원장 특례 계정 `9199/9549/1089` → **영향액 먼저 산출 후 명시 매핑 (A)**

```text
결정   결정 전에 read-only 데이터 대조로 영향액을 먼저 낸다
🚨 집PC 실측 = 3계정 모두 0건. 이것은 "결함 0" 이 아니라 **판정 불가**다
   ⟹ 회사PC 실 데이터에서 다시 셀 것 (양 PC 시드 상이)
산출   계정별 건수 · 잔액 · 레거시 특례 적용 시와 일반 chart 적용 시의 차액
그다음  0 이 아니면 versioned mapping 을 박고, 0 이면 그대로 닫는다
🚫 데이터를 보기 전에 chart 를 고치지 말 것
```

### D-G4. 거래처 4자리 비밀번호 → **한시 허용 후 강제 재설정 (A)**

```text
결정   이관 후 제한기간 동안 기존 hash 로 로그인 허용 · 첫 로그인 시 재설정 강제
       기간 종료 후 기존 hash 폐기
🚫 제외   평문 복원 · 영구 legacy hash 수용
연결   #1015 (재설정 화면은 이미 있음) — 이관 방식만 신규
미정   제한기간 길이(예: 30일)는 구현 착수 시 함께 확인받을 것
```

---

## 2. 아직 상정하지 않은 결정 (다음 배치)

| # | 항목 | 상태 |
|---|---|---|
| 2 | 용량·조합률·금지조합·최대연결·추천·분기관 정책군 | 상정 대기 (HOME 130% · COMM 103/120% · AJ025 예외 · 6개 분기관 경계) |
| 3 | 상업 받침대 고정가와 `fixedDC=0` 의 의미 | 상정 대기 |
| 4 | 장기미발주 판정축 전환 (`#1015`) | 상정 대기 |
| 5 | 미배차 상태·배차안내 문자 정본 | 상정 대기 (상태 문자열 저장소 0건) |
| 6 | 내일자 전표의 허용창고·금지거래처·배송 캘린더 | 상정 대기 |
| 7 | 에어디자이너·제이시스템 전용 fallback 의 영구성 | 상정 대기 |
| 8 | 입출고 예측 계산식 (**질문 변경됨** — 존폐 아님) | 상정 대기 |
| 9 | 교육 상태·담당자별 Sheet 배포 존폐 | 상정 대기 |

---

## 3. 조사 자체의 미결 (Critic 지적)

```text
🚩 분모 3,200 도 전수가 아니다
   같은 패턴으로 3,207 · AST 기준 이름 있는 실행 단위 최소 3,595
   누락: 메서드 10 · IIFE 19 · 조건부 함수 대입 10 · **인라인 handler 760**
   71파일 밖: golden/oracle 5 · 인쇄 규칙 5 · 서비스 규칙 엔진 6 · 규칙성 migration 11
🚩 데드코드 기준이 보고서마다 다르다 — 동일 계보 최소 8건이 반대로 판정됐다
🚩 **원본에만 있는 업무규칙 이름 257개** — 유실 후보. 성격 분류가 이 조사의 결론이다
⟹ v3 라운드에서 위 셋을 닫는다 (`2026-08-11-gasv3-remainder.md`)
```

분모가 두 번 연속 틀렸습니다(889 → 3,200 → 아직 아님).
**다음 라운드가 "전수" 를 자칭하려면** Critic 이 §9 에 적은 완료 조건을 전부 만족해야 합니다.
