# PR #1179 (#1094) rebase 보고서

## 작업 범위

- 기준 브랜치: `origin/main`
- 작업 브랜치: `feat/1094-docno-hyperlink-and-back`
- 이번 라운드: rebase 및 충돌 해소만 수행
- 라이브 QA, 새 기능, 리팩터링, 커밋, push는 수행하지 않음

## rebase 전 원문

```text
명령: git status --short --branch
## feat/1094-docno-hyperlink-and-back...origin/feat/1094-docno-hyperlink-and-back

명령: git rev-list --left-right --count origin/main...HEAD
219	6

명령: git diff --stat origin/main...HEAD
 clients/desktop/package-lock.json                  |  79 ++++++
 clients/desktop/package.json                       |   1 +
 .../cash-receipt-list/cash-receipt-list.spec.ts    |  31 +++
 .../renderer/routes/CashReceiptDetailPage.test.tsx |  64 ++++-
 .../src/renderer/routes/CashReceiptDetailPage.tsx  |  37 ++-
 .../renderer/routes/CashReceiptFormPage.test.tsx   |  44 +++-
 .../src/renderer/routes/CashReceiptFormPage.tsx    |  27 +-
 .../renderer/routes/CashReceiptListPage.test.tsx   |  28 +-
 .../src/renderer/utils/returnContract.test.ts      |  54 ++++
 .../desktop/src/renderer/utils/returnContract.ts  | 107 ++++++++
 docs/dev-reports/2026-08-07-1094-plan.md           |  28 ++
 ... (이하 44 files, +2,101/-31)

명령: git log --oneline --decorate origin/main..HEAD
e698b7683 docs(qa): 1094-s8-reconvergence-and-live-qa — 재수렴 보고서·라이브QA 캡처 (미커밋 잔재 구조)
0f3fdd747 [FIX] #1094 S7 — mutation 경로에서 복귀 identity 를 잃던 것 (S6 SOL 발견)
2b54d49fd [FIX] #1094 S5 — 복귀 계약 결함 3건을 규칙 하나로 (S4 SOL 라이브QA)
71ab1561a [FIX] #1094 S3 — jest-dom 미설정 · stale returnTo 캡처 (CI red 2잡)
d25e6c79f [FEAT] #1094 S2 — 공통 복귀 계약 + reference 구현 (슬라이스 1/4)
7fe6e09c0 docs(plan): #1094 번호 하이퍼링크·뒤로 가기 공통화 기획 — 조기 PR
```

## 충돌 해소 기록

### S2 적용 중 충돌 — `origin/main` 기준 채택

원문:

```text
Auto-merging clients/desktop/playwright/cash-receipt-list/cash-receipt-list.spec.ts
CONFLICT (content): Merge conflict in clients/desktop/playwright/cash-receipt-list/cash-receipt-list.spec.ts
Auto-merging clients/desktop/src/renderer/routes/CashReceiptDetailPage.test.tsx
CONFLICT (content): Merge conflict in clients/desktop/src/renderer/routes/CashReceiptDetailPage.test.tsx
Auto-merging clients/desktop/src/renderer/routes/CashReceiptDetailPage.tsx
CONFLICT (content): Merge conflict in clients/desktop/src/renderer/routes/CashReceiptDetailPage.tsx
Auto-merging clients/desktop/src/renderer/routes/CashReceiptListPage.tsx
CONFLICT (content): Merge conflict in clients/desktop/src/renderer/routes/CashReceiptListPage.tsx
Auto-merging clients/desktop/src/renderer/utils/returnContract.test.ts
CONFLICT (add/add): Merge conflict in clients/desktop/src/renderer/utils/returnContract.test.ts
Auto-merging clients/desktop/src/renderer/utils/returnContract.ts
CONFLICT (add/add): Merge conflict in clients/desktop/src/renderer/utils/returnContract.ts
```

`origin/main`의 `d59b0d435 [FEAT] #1094 상세 진입은 전표번호·문서번호 하이퍼링크로 · 상세에 뒤로 가기 (전 화면 공통) (#1105)`가 S2의 공통 구현을 이미 포함한다. 따라서 다음 6개 파일은 main 쪽 구현을 채택했다. 이것은 PR 의도(문서번호 링크와 뒤로가기)를 버린 것이 아니라 upstream 구현을 중복 적용하지 않은 것이다.

- `clients/desktop/playwright/cash-receipt-list/cash-receipt-list.spec.ts`: main의 링크/복귀 E2E를 유지.
- `clients/desktop/src/renderer/routes/CashReceiptDetailPage.test.tsx`: main의 상세 복귀 검증을 유지.
- `clients/desktop/src/renderer/routes/CashReceiptDetailPage.tsx`: main의 상세 화면과 DS `Card` 셸, 링크/복귀 구현을 유지.
- `clients/desktop/src/renderer/routes/CashReceiptListPage.tsx`: main의 DS 목록 표면과 entry-key 기반 anchor 연결을 유지.
- `clients/desktop/src/renderer/utils/returnContract.test.ts`: main의 entry-key 소비·분리·TTL/상한 테스트를 유지.
- `clients/desktop/src/renderer/utils/returnContract.ts`: main의 entry-key 기반 1회 소비, TTL 24시간, anchor 상한 50개 계약을 유지.

이 시점에서 S2만 중복되므로 다음 S3~S8 커밋의 적용 결과로 stale `returnTo`, mutation 복귀 identity, 검증 이력을 재확인한다.

### S3 적용 중 충돌 — `CashReceiptListPage.tsx`

원문:

```text
Auto-merging clients/desktop/src/renderer/routes/CashReceiptListPage.tsx
CONFLICT (content): Merge conflict in clients/desktop/src/renderer/routes/CashReceiptListPage.tsx
```

충돌 지점은 `columns` `useMemo` dependency였다. S3의 `[returnTo.pathname, returnTo.search]`는 stale `returnTo` 캡처를 막는 목적이고, main의 `[location.key, returnTo.pathname, returnTo.search]`는 같은 목적에 더해 entry-key 기반 저장/복원 identity 변경도 반영한다. main 쪽 세 항목을 채택해 두 의도를 모두 보존했다. S3의 나머지 변경(테스트 matcher 설정, Playwright selector 수정, package 의존성, S3 보고서)은 계속 적용한다.

### S5 적용 중 충돌 — 상세 복귀 2개 및 계약 테스트 1개

원문:

```text
Auto-merging clients/desktop/src/renderer/routes/CashReceiptDetailPage.test.tsx
CONFLICT (content): Merge conflict in clients/desktop/src/renderer/routes/CashReceiptDetailPage.test.tsx
Auto-merging clients/desktop/src/renderer/routes/CashReceiptDetailPage.tsx
CONFLICT (content): Merge conflict in clients/desktop/src/renderer/routes/CashReceiptDetailPage.tsx
Auto-merging clients/desktop/src/renderer/routes/CashReceiptListPage.tsx
Auto-merging clients/desktop/src/renderer/utils/returnContract.test.ts
CONFLICT (content): Merge conflict in clients/desktop/src/renderer/utils/returnContract.test.ts
```

`origin/main`의 해당 파일들은 이미 S5와 동일한 결함 해결(유효한 `returnEntryKey`면 `navigate(-1)`, 없으면 canonical `replace`, entry-key anchor 1회 소비/TTL/상한)을 포함한다. 세 파일 모두 main 쪽을 채택했다. S5의 의도와 S3~S4 검증을 버리지 않았고, 중복 patch만 제거했다. S5의 보고서·QA 캡처 등 비충돌 산출물은 rebase가 계속 적용하는지 후속 상태에서 확인한다.

### S7 처리

원문:

```text
Rebasing (5/6)
dropping 0f3fdd74723481cb9b4bb4b31ff27137f52b4d9e [FIX] #1094 S7 — mutation 경로에서 복귀 identity 를 잃던 것 (S6 SOL 발견) -- patch contents already upstream
```

S7의 mutation 복귀 identity 보정은 `origin/main`에 이미 동일 내용이 있어 중복 patch로 drop됐다. S7을 기능적으로 잃은 것이 아니라 upstream 구현을 유지한 것이다. S8은 다음 단계로 정상 적용됐다.

### rebase 완료 원문

```text
Rebasing (6/6)
Successfully rebased and updated refs/heads/feat/1094-docno-hyperlink-and-back.
```

## 검증

### 초기 파생물 가드 실패 원문

```text
[로컬 파생물 신선도 확인 실패] 검증 결과를 코드 결함으로 해석하지 마십시오.
- electron-updater가 설치된 node_modules에 없습니다. clients/desktop에서 npm ci 를 먼저 실행하십시오.
- file: 의존 design-system dist이(가) 없습니다: ..\web\design-system\dist\index.d.ts. cd ..\web\design-system; npm run build
```

조치로 `clients/desktop` 및 `clients/web/design-system`에서 `npm ci`를 실행하고 design-system `npm run build`, desktop `npm run build`를 실행했다. 이 작업은 추적 소스가 아닌 로컬 파생물만 생성했다.

### desktop 변경 모듈 전량 테스트 원문

명령:

```text
cd clients/desktop
npm test
```

결과: `Exit code: 0`

```text
✔ all actor display reads are resolver-bound
ℹ tests 5
ℹ pass 5

> @samhan/desktop@0.1.0 test
> vitest run

Test run completed successfully with exit code 0.
```

전체 `npm test`는 pretest의 actor-display-boundary 및 freshness gate와 desktop 전체 Vitest를 실행했다. 라이브 QA/Playwright 실환경 실행은 이번 라운드에서 하지 않았다.

### desktop typecheck 원문

명령:

```text
cd clients/desktop
npm run typecheck
```

결과: `Exit code: 0`

```text
> @samhan/desktop@0.1.0 typecheck
> node scripts/real-qa-scope.cjs --phase=typecheck && tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.web.json --noEmit && npm run typecheck:real-qa

ℹ tests 2
ℹ pass 2
```

typecheck는 node/web TypeScript 검사와 real-QA cleanup/scope 보조 검사를 포함해 통과했다. 로컬 미추적 real-QA spec 경고는 저장소 기존 로컬 파생물이며, 실행은 허용 모드로 통과했다.

## 삭제 파일 확인

최초 실행 원문:

```text
git diff --name-status origin/main...HEAD | grep '^D'
grep : The term 'grep' is not recognized as the name of a cmdlet, function, script file, or operable program.
```

PowerShell 동등 명령으로 재확인:

```text
git diff --name-status origin/main...HEAD | Select-String '^D'
```

첫 확인에서 build 과정의 작업트리 삭제 1건이 보였다.

```text
 D tools/.s24-build-only/build/deep/tracked-writer.mjs
```

HEAD에 존재하는 tracked 파일임을 확인하고 `git restore --source=HEAD -- tools/.s24-build-only/build/deep/tracked-writer.mjs`로 복구했다. 최종 재확인 결과:

```text
git diff --name-status origin/main...HEAD | Select-String '^D'
(출력 없음)
```

따라서 최종 작업트리에는 삭제 파일이 없다. 충돌 미해결 파일(`git diff --name-only --diff-filter=U`)도 출력 없음이다.
```
