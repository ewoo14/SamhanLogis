연관 Issue: #1089, #1090

## 트랙 개설 — 착수 전 확인

2026-08-06 등록. 둘 다 **품목 도메인의 판별 규칙**이라 한 트랙으로 묶습니다.
- `#1089` 세트 전개는 **기본 구성품만** — 견적서·주문서·구매전표·판매전표 (레거시 GAS 는 현행 유지)
- `#1090` 정액 할인 판별을 **모델명 → 품목 분류**로 (개발책임자 결정 A안)

### 🚨 둘 다 "이름으로 판별하던 것을 구조로 바꾸는" 축입니다
`#1114`(리모컨/실외기)도 같은 형태였고 `#896` 으로 흡수됐습니다. **이름 기반 판별이 남은 곳을 전수로** 찾는 것이 이 트랙의 핵심입니다.

### 정찰이 실측할 것
1. `BundleExpander` 의 현재 전개 규칙 — "기본 구성품" 이 데이터에서 무엇으로 표현되는가
2. 정액 할인의 현재 판별 — 모델명 정규식이 어디에 몇 곳인가 (`파일:줄` 전수)
3. **품목 분류** 축이 실제로 존재하는가 · 채움률이 몇 %인가. 🚨 조인·판별 키로 쓸 컬럼이 비어 있어 화면 값이 사라진 사고가 이 저장소에서 반복됐습니다
4. 🚫 **레거시 GAS 는 현행 유지** — 손대지 마십시오
5. 🚨 할인 용어 주의 — **고정DC**(`products.fixed_discount_rate`) / **전역DC**(`dc_configs`) / **기본 할인율**(`partners`)이 다릅니다. "약정DC" 는 없는 말입니다

### 🚨 금액에 닿습니다 — 정밀 경로