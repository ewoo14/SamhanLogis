# R82 fix 보고 — PR #1057 · 이슈 #874

역할: 구현자(LUNA)  
기준 HEAD: `4d4b862f7`  
범위: R81 지시서 F1/F2 fix. 커밋·push·merge·Docker·DB 쓰기·vendor 발송 없음.

## RED 원문

### RED-A(F1)

```text
4348703365 + AJ060MXHNBC1 에서 1,355,640 을 만든 뒤
1012555999 로 바꾸고 최근단가를 miss 시키면
1,433,850 + 거래처 전역DC 45% 적용 으로 수렴해야 한다.
```

반대급부 원문:

```text
① 새 거래처 최근단가가 있어도 자격 있는 전역DC가 최근단가보다 우선
② hasVariableDiscount=false 품목은 거래처 변경에서 전역DC를 받지 않고 최근단가 hit 또는 판매가 fallback 유지
③ 고정DC 품목은 새 거래처 전역DC 율과 무관하게 고정DC 유지
④ USER 단가는 bulk·DC 응답 어느 쪽도 덮지 않음
⑤ A→B→A를 빠르게 한 뒤 B의 늦은 DC/최근단가 응답이 마지막 A 라인을 바꾸지 않음
```

### RED-B(F2)

```text
DC를 지연시키고 자격 품목을 먼저 확정한 뒤 DC 성공 + 최근단가 miss를 완료하면
화면 단가·discountInfo·aria-live가 모두 같은 전역DC 율을 말해야 한다.
```

반대급부 원문:

```text
① 비대상 품목 + 최근단가 miss → 정가 + "판매가 적용"
② 전역DC 없고 최근단가 hit → 최근단가 + "거래처 최근단가 적용"
③ 5초 제한시간을 넘겨 도착한 DC는 확정된 라인과 안내를 뒤늦게 바꾸지 않음
```

## 변경 파일: 줄

- `clients/web/design-system/src/components/LineRow/LineRow.tsx:119` — `LineDraft.hasVariableDiscount` 보존 필드 추가.
- `clients/desktop/src/renderer/utils/usePartnerPriceRefresh.ts:16,41-45,70-73,90-96,134-180` — 선택적 할인 입력/거래처 DC를 해석해 고정/전역DC를 최근단가보다 우선하고 `discountInfo`를 outcome에 포함.
- `clients/desktop/src/renderer/routes/SlipFormPage.tsx:788,808,845,896,903,918,926-996,1084-1095` — 품목 자격 보존, 거래처 변경 시 새 DC와 함께 재가격, request sequence/stale guard, 완료 결과 기반 안내.
- `clients/desktop/src/renderer/utils/usePartnerPriceRefresh.test.ts:61-82` — bulk miss + 전역DC 우선 RED/GREEN 회귀.
- `clients/desktop/src/renderer/routes/SlipFormPage.test.tsx:101,653-706` — 거래처 변경 45% 재가격 및 지연 DC 안내 일치 회귀.

서버 재계산이나 `POST /slips` 계약은 변경하지 않았다.

## 분포 RED 실측 원문

```text
ACTIVE 3,179 중 hasVariableDiscount=true 802개, false 2,377개, null 0개
GET /api/products?status=ACTIVE&size=5000: true 802/802
POST /api/products/lookup 100개 chunk: true 802/802, null/false 0
카테고리: homemulti 107 + commercialMulti 314 + singleSets 190 + singleParts 191 = 802
R78 전후 계산식 대조: 새로 차단된 true 품목 0건
```

## 게이트 결과 원문

```text
npx vitest run src/renderer/utils/slipDiscount.test.ts src/renderer/utils/usePartnerPriceRefresh.test.ts src/renderer/routes/SlipFormPage.test.tsx --reporter=verbose
Test Files  3 passed (3)
Tests       82 passed (82)
```

```text
npx tsc -p tsconfig.web.json --noEmit
Exit code: 0
```

```text
.\gradlew.bat :services:product-service:test --tests '*ProductSummaryResponseTest' :services:slip-service:test --tests '*SlipServiceTest' --tests '*SlipCollabNotificationDispatchTest' :services:notification-service:test --tests '*NotificationServiceTest' --tests '*NotificationDispatchPersistenceTest' --tests '*NotificationCenterServiceTest' --no-daemon --console=plain
BUILD SUCCESSFUL in 14s
33 actionable tasks: 33 up-to-date
```

참고: 같은 Gradle 명령을 `clients/desktop`에서 처음 실행한 1회는 프로젝트 루트 오류로 실패했다. 루트에서 재실행한 위 결과가 권위 있는 결과다. design-system 선언 반영을 위해 `clients/web/design-system`에서 `npm run build`도 성공했다.

## 신규 파일 목록

- `docs/dev-reports/2026-08-06-874-r82-fix.md`

## 작업 트리 주의

기존 QA 산출물의 사용자 변경 파일은 건드리거나 정리하지 않았다. 이번 라운드 변경과 무관한 dirty 파일이 작업 트리에 이미 존재한다.
