# #851 게이트 0 표면 보강 개발 보고서 (R1·R2 라운드 fix)

작성일: 2026-07-27
작업 트리: `C:\dev\Samhan-Public\.claude\worktrees\851-gaps`
범위: B-1 real-QA 공유 하네스 재설계, B-2 arologis CI 정적 가드 2종(notion-zero-guard 보강), R2 기동·증거 보강
성격: **R1·R2 적대검증 라운드 fix** — R0(이월 구현)이 만든 `docs/dev-reports/2026-07-27-851-gate-gaps.md`(구판)를
전면 재작성한다. 구판의 서술 오류·인용 위조를 이 문서에서 전부 정정한다.

## 0. R0 대비 이번 라운드에서 무엇이 바뀌었나

| 결함 | R0(구판) 상태 | R1 fix(본 라운드) |
|---|---|---|
| R1-1 전역 baseURL 이 서로 다른 앱을 가리킴 | `use.baseURL` 전역 1개(5175) — 928(order-app)도 강제로 5175 를 받음 | `projects` 로 928 전용 오리진(5181) 분리, 나머지는 5175 유지 |
| R1-2 `AUDIT_BASE_URL` 이름 충돌 | 직접 참조 스펙 163개(감사 146개 + QA 17개)가 이미 쓰는 이름을 공유 config가 재사용 | 이 config 전용 신규 이름(`REAL_QA_RENDERER_BASE_URL`/`REAL_QA_ORDER_APP_BASE_URL`)으로 교체 |
| R1-3 notion-zero-guard 가 arologis 표면 미검사 | `SCAN_DIRS` 에 arologis 클라이언트 트리·`shared` 없음 | 두 클라이언트 트리 + `shared`(Java, src/main/ 한정) 추가 |
| 인용 위조(dev-report) | 변경 후 Error 줄 + 변경 전 Call log 줄을 섞은 인용 2건 | 전부 삭제, 본 문서의 모든 인용은 이 라운드에서 직접 실행해 얻은 원문만 사용 |
| V-3 (신규 발견) | 미기재 | §3 에 정직하게 기재 — 이 파일은 typecheck·lint·CI 어디에도 안 걸림. 판단·조치 §3 참고 |

## 1. B-1 재설계 — R1-1/R1-2 fix

### 1-1. 문제였던 것

R0 은 `clients/desktop/playwright.real-qa.config.ts` 의 `use` 에 다음 한 줄만 추가했었다.

```ts
baseURL: process.env['AUDIT_BASE_URL'] ?? 'http://127.0.0.1:5175',
```

상대경로 `page.goto('/…')` 를 쓰는 real-QA 스펙은 897 · 928 · 929-r4 **3개**다(직접 grep 재확인 —
`page.goto\(` 호출부를 897·928·929-r4·929-r5 네 파일 전부에서 직접 열어 대조했다. 929-r5 에서
매치된 유일한 상대경로 문자열은 Javadoc 주석 안의 예시(`{@code page.goto('/#…')}`)였고 실제
코드의 `page.goto` 는 전부 `${APP_BASE}/...` 형태의 절대 URL 이었다). 929-r5 는 자체 `QA_BASE_URL`
로 절대 URL 을 조립해 이 문제와 원래 무관하다 —
구판·기획서가 "4개"로 표기한 것은 과대 계수이며 이 문서와 기획서에서 정정한다(§5).

이 3개 중 897 · 929-r4 는 데스크톱 렌더러(HashRouter `/#/...`, `vite.renderer.dev.config.ts` 정본
포트 5175)를 목표로 하지만, **928 은 완전히 다른 Vite 앱인 주문 웹 order-app**(BrowserRouter `/`,
`clients/web/order-app`, 정본 포트 5181)을 목표로 한다. R0 처럼 전역 baseURL 을 5175 하나로
고정하면:

- 렌더러만 띄운 상태에서 928 을 돌리면 **데스크톱 앱의 DOM 을 로드**해 `web-version-notice` 를
  못 찾는 것으로 실패한다 — 하네스 설정 문제가 제품 결함처럼 읽힌다(R1-1 이 지적한 신호 악화).
- `AUDIT_BASE_URL`은 146개, `QA_BASE_URL`은 17개 스펙이 "자기 오리진" 기본값으로
  스펙 코드 안에서 직접 읽는다. 합집합은 163개이고, 기본 오리진이 5175가 아닌 파일은
  121개다. 928을 맞추려고 이 이름에 값을 주입하면 해당 스펙들이 동반 오작동한다(R1-2).

**독자 재확인**(R2에서 GNU grep으로 직접 재현):

```bash
cd clients/desktop
GREP_SCOPE=(--include='*-real-qa.spec.ts' --exclude='slip-form-3d-real-qa.spec.ts' --exclude='dispatch-collab-real-qa.spec.ts')
AUDIT_FILES=$(grep -rl "${GREP_SCOPE[@]}" -F "process.env['AUDIT_BASE_URL']" playwright)
QA_FILES=$(grep -rl "${GREP_SCOPE[@]}" -F "process.env['QA_BASE_URL']" playwright)
printf 'DIRECT_PROCESS_ENV_REAL_FILES=%s\n' "$(printf '%s\n' "$AUDIT_FILES" "$QA_FILES" | sort -u | sed '/^$/d' | wc -l)"
printf 'UNIQUE_DEFAULT_NOT_5175=%s\n' "$(printf '%s\n' "$AUDIT_FILES" "$QA_FILES" | sort -u | sed '/^$/d' | xargs grep -L -E 'process\.env.*5175' | wc -l)"
printf 'AUDIT_BASE_URL_ENTRIES=%s\n' "$(printf '%s\n' "$AUDIT_FILES" | sed '/^$/d' | wc -l)"
printf 'QA_BASE_URL_ENTRIES=%s\n' "$(printf '%s\n' "$QA_FILES" | sed '/^$/d' | wc -l)"
```
```
DIRECT_PROCESS_ENV_REAL_FILES=163
UNIQUE_DEFAULT_NOT_5175=121
AUDIT_BASE_URL_ENTRIES=146
QA_BASE_URL_ENTRIES=17
```

GNU grep에는 ripgrep의 `--glob` 옵션이 없으므로 위 명령은 `--include`/`--exclude`를 사용한다.
두 `--exclude`는 Git 무시 상태인 두 throwaway real-QA 파일을 하네스 대상 집계에서 제외해,
R1 당시의 `rg` 기준과 같은 대상 집합을 재현한다. 두 이름 모두 "이 공유 config 전용 override"
로 재사용하기엔 이미 과점유 상태임을 직접 확인했다. `QA_BASE_URL`도 928 자신의 정본 config가
쓰는 이름이라 **928 전용 override 이름으로도 못 쓴다**(다른 스펙이 동시에 끌려간다).

### 1-2. 설계 — Playwright `projects` 로 분리, 신규 이름으로 override

`clients/desktop/playwright.real-qa.config.ts` 를 다음처럼 재작성했다(전체는 파일 참고).

- 최상위 `use` 에서 `baseURL` 을 제거하고 `headless`/`viewport`/`actionTimeout`/`navigationTimeout`/
  `devices['Desktop Chrome']` 만 남긴다(두 프로젝트가 공통 상속).
- `projects: [{ name: 'order-app', testMatch: ['928-web-version-check-real-qa/**/*-real-qa.spec.ts'], use: { baseURL: ORDER_APP_BASE_URL } }, { name: 'renderer', testIgnore: 같은 글롭, use: { baseURL: RENDERER_BASE_URL } }]`.
  `renderer` 프로젝트는 `testMatch` 를 명시하지 않아 최상위 `testMatch: ['**/*-real-qa.spec.ts']` 를
  그대로 상속하고, `testIgnore` 로 928 글롭만 제외한다 — 두 프로젝트가 정확히 상호배타적으로
  전체 파일을 분할한다(중복도 누락도 없음, §1-3 실측).
- override 환경변수 이름을 이 config 전용 신규 이름으로 바꿨다 — `REAL_QA_RENDERER_BASE_URL`
  (기본 `http://127.0.0.1:5175`), `REAL_QA_ORDER_APP_BASE_URL`(기본 `http://127.0.0.1:5181`,
  928 정본 `playwright/928-web-version-check-real-qa/playwright.config.ts` 의 `QA_BASE_URL`
  기본값과 동일 포트). 두 이름 모두 저장소 전체에서 기존에 쓰인 적이 없음을 직접 확인했다(grep 0건).

### 1-3. 실측 — 대상/실행 수치

**전체 배치(변경 없음 확인)**:

```
cd clients/desktop
./node_modules/.bin/playwright.cmd test --config=playwright.real-qa.config.ts --list
```
```
Total: 548 tests in 172 files
```

**프로젝트 분할이 정확히 상호배타적인지(중복/누락 0)**:

```
./node_modules/.bin/playwright.cmd test --config=playwright.real-qa.config.ts --project=order-app --list
```
```
Total: 1 test in 1 file
```
```
./node_modules/.bin/playwright.cmd test --config=playwright.real-qa.config.ts --project=renderer --list
```
```
Total: 547 tests in 171 files
```

1 + 547 = 548, 1 + 171 = 172 — **네비게이션 설정 때문에 빠지는 스펙 0, 중복 실행 0**. 이것이
불변식 4 의 수치 증거다.

**투자 대상 4개 파일(897·928·929-r4·929-r5) 원 목록도 동일하게 유지**:

```
./node_modules/.bin/playwright.cmd test --config=playwright.real-qa.config.ts --list \
  playwright/929-r5-route-collision-real-qa/929-r5-route-collision-real-qa.spec.ts \
  playwright/929-r4-transport-guard-real-qa/929-r4-transport-guard-real-qa.spec.ts \
  playwright/897-column-hierarchy-real-qa/897-column-hierarchy-real-qa.spec.ts \
  playwright/928-web-version-check-real-qa/928-web-version-check-real-qa-real-qa.spec.ts
```
```
Total: 15 tests in 4 files
```
목록에 `[order-app] › 928-…`, `[renderer] › 897-…/929-r4-…/929-r5-…` 태그가 정확히 붙어 나온다
(원문은 위 §1-2 서술과 아래 §1-4 실행 로그에서 그대로 확인 가능).

### 1-4. 실측 — 서버 미기동 상태 실행 (RED-first, 실제 실행 원문)

명령(4개 파일, 서버 기동 없음, timeout 8000ms):

```
./node_modules/.bin/playwright.cmd test --config=playwright.real-qa.config.ts --reporter=line --timeout=8000 \
  playwright/929-r5-route-collision-real-qa/929-r5-route-collision-real-qa.spec.ts \
  playwright/929-r4-transport-guard-real-qa/929-r4-transport-guard-real-qa.spec.ts \
  playwright/897-column-hierarchy-real-qa/897-column-hierarchy-real-qa.spec.ts \
  playwright/928-web-version-check-real-qa/928-web-version-check-real-qa-real-qa.spec.ts
```

897 실패 원문(Error 줄과 Call log 줄이 이번에는 **항상 같은 절대 URL** — baseURL 이 설정되면
Call log 도 절대 URL 로 나온다는 것을 직접 확인):

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://127.0.0.1:5175/#/accounting/bank-transactions
Call log:
  - navigating to "http://127.0.0.1:5175/#/accounting/bank-transactions", waiting until "domcontentloaded"
```

929-r4 실패 원문:

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://127.0.0.1:5175/#/accounting/daily-closing
Call log:
  - navigating to "http://127.0.0.1:5175/#/accounting/daily-closing", waiting until "domcontentloaded"
```

929-r5 실패 원문(자체 `APP_BASE` 로 조립한 절대 URL — 그대로 5175 기본값):

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://127.0.0.1:5175/#/accounting/daily-closing
Call log:
  - navigating to "http://127.0.0.1:5175/#/accounting/daily-closing", waiting until "domcontentloaded"
```

요약 원문:

```
4 failed
    [order-app] › ...928-web-version-check-real-qa-real-qa.spec.ts:53:1 › 주문 웹 릴리스 등록 → ...
    [renderer] › ...897-column-hierarchy-real-qa.spec.ts:86:3 › 897 실 서버 U-gate › 입출금 내역: 폭·상세·실 캡처
    [renderer] › ...929-r4-transport-guard-real-qa.spec.ts:77:3 › ... QA-1 (D1) 일마감 거래처코드 필터 ...
    [renderer] › ...929-r5-route-collision-real-qa.spec.ts:134:3 › ... QA-1 일마감 — 형제 라우트 이름을 ...
  11 did not run
```

928 은 같은 조건(서버 미기동)에서 **별도 실행**으로 원문을 받았다(아래는 928 만 격리 실행한 원문 —
동일 표는 4개 합산 실행에서도 재현됨). 이 실행은 **실제 Docker 백엔드(게이트웨이:8080,
dashboard-service:8094)가 이미 기동 중**이던 상태에서 돌았기 때문에, 928 의 API 호출(로그인·
릴리스 등록/배포/조회/삭제)이 전부 실제로 수행됐다 — 즉 이 실패는 프런트엔드 페이지 로드
단계에서만 난 것이고 백엔드 계약은 실제로 왕복 검증됐다.

```
./node_modules/.bin/playwright.cmd test --config=playwright.real-qa.config.ts --reporter=line --timeout=8000 \
  playwright/928-web-version-check-real-qa/928-web-version-check-real-qa-real-qa.spec.ts
```
```
[오폭] 등록 전 SAMHAN_ORDER_WEB 행 수=0
[오폭] 등록 후 판정={"order":{"http":200,"latestVersion":"2026/07/26-92801","minSupportedVersion":"2026/07/26-92700","forceLevel":"MINOR","releaseNotes":"QA #928 throwaway 주문 웹 버전 안내 확인","releasedAt":"2026-07-26T00:00:00"},"estimate":{"http":404},"mobile":{"http":404}}
[정리] SAMHAN_ORDER_WEB 행 수 before=0, after=0

  1) [order-app] › ...928-web-version-check-real-qa-real-qa.spec.ts:53:1 › ...

    Error: page.goto: net::ERR_CONNECTION_REFUSED at http://127.0.0.1:5181/
    Call log:
      - navigating to "http://127.0.0.1:5181/", waiting until "load"

  1 failed
```

throwaway 릴리스는 `finally` 블록으로 실제 soft-delete 되어 before=0/after=0 으로 복원됨을
직접 확인했다(공유 실데이터에 잔여 없음).

### 1-5. 실측 — 서버 실제 기동 상태의 차등 검증(불변식 6)

"연결 거부는 오리진 정합을 증명하지 않는다"는 지적에 따라, **렌더러를 실제로 기동한 상태**에서
897(렌더러 대상)과 928(order-app 대상)을 **같이** 실행해 차등을 관찰했다. order-app 은 이 워크
트리에 `node_modules` 가 설치돼 있지 않아(별도 `npm install` 필요 — 이 config 라우팅 fix 범위를
넘는 환경 셋업이라 이번 라운드에서 수행하지 않음) 기동하지 않았다. 따라서 이 검증은
"렌더러는 살아있고 order-app 은 없음" 조건의 차등 증거다.

렌더러 기동:

```
cd clients/desktop
VITE_API_BASE_URL=http://localhost:8080 node_modules/.bin/vite dev --config vite.renderer.dev.config.ts
```
```
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5175/   →  200
```

실행(897 1개 테스트 + 928, timeout 20000ms):

```
./node_modules/.bin/playwright.cmd test --config=playwright.real-qa.config.ts --reporter=line --timeout=20000 \
  playwright/897-column-hierarchy-real-qa/897-column-hierarchy-real-qa.spec.ts:86 \
  playwright/928-web-version-check-real-qa/928-web-version-check-real-qa-real-qa.spec.ts
```

928(order-app 프로젝트) 원문 — **렌더러가 살아있는데도 여전히 5181 에서 거부**됨(전역 baseURL
이었다면 5175 로 붙어 엉뚱한 DOM 을 받았을 것):

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://127.0.0.1:5181/
Call log:
  - navigating to "http://127.0.0.1:5181/", waiting until "load"
```

897(renderer 프로젝트) 원문 — **연결 오류도 invalid URL 오류도 없이 네비게이션은 성공**하고,
실제 페이지 콘텐츠 단언(`header-page-title` 텍스트) 단계에서 시간 초과로 실패했다(이 실패
자체는 이번 config fix 의 대상이 아닌 별개 사안 — 로그인/모달 해제 등 화면 전제와 관련된
것으로 보이며 회귀 스코프 밖):

```
Test timeout of 20000ms exceeded.
Error: expect(locator).toContainText(expected) failed
Locator: getByTestId('header-page-title')
Expected substring: "입출금 내역"
Call log:
  - Expect "toContainText" with timeout 30000ms
  - waiting for getByTestId('header-page-title')
```

**같은 조건(렌더러만 생존)에서 928 은 거부되고 897 은 통과(네비게이션 단)**하는 이 차등이,
"928 이 실제로 다른 오리진으로 라우팅된다"는 것의 실제 증거다 — 서버가 하나도 없어 전부
똑같이 거부되는 것과는 다른 신호다.

렌더러 정리:

```
netstat -ano | grep ":5175" | grep LISTENING   →  PID 61156
taskkill /PID 61156 /T /F                       →  PID 61156(자식 62528 포함) 종료
curl(3s 타임아웃) http://127.0.0.1:5175/         →  000(연결 실패, 서버 없음 확인)
netstat -ano | grep ":5175" | grep LISTENING    →  매치 없음(정리 완료)
```

## 2. 929-r5 설계 결정 — 유지(뒤집지 않음)

`929-r5-route-collision-real-qa.spec.ts:30-39` 의 주석은 "공유 config 에 baseURL 이 없다"는
전제였으나, 이 전제는 R0 시점에 이미 stale 이었고(R0 가 baseURL 을 추가했으므로) 본 라운드
에서 다시 stale 해졌다(projects 분리). 주석을 **현재 상태에 맞게 정정**했다 — 공유 config 가
이제 프로젝트별 baseURL 을 갖는다는 사실은 반영하되, **929-r5 의 설계 결정(절대 URL 자체
조립)은 뒤집지 않았다**. 이유: 929-r5 는 슬라이스 단위로 렌더러를 임의 포트에 띄워 공유
config 의 프로젝트 구성과 무관하게 독립 실행하려는 목적이며, 이 목적은 이번 fix 로도 사라지지
않는다(공유 config 의 project 구성이 앞으로 늘거나 이름이 바뀌어도 929-r5 는 영향받지 않아야
한다). 이 스펙의 `page.goto` 호출 자체(절대 URL 문자열)는 변경하지 않았다 — 코드 변경은
주석뿐이다.

## 3. V-3 — 이 표면 자체의 게이트 커버리지 (정직한 기재)

직접 확인한 사실(아래 tsconfig.node.json 조치 **전** 상태 — R0 산출물 기준):

```
grep -n "playwright.real-qa.config" clients/desktop/tsconfig.node.json clients/desktop/tsconfig.web.json
  → 매치 없음
grep -rn "playwright.real-qa.config" .github/workflows/
  → 매치 없음(exit 1)
clients/desktop/package.json "lint": "eslint \"src/**/*.{ts,tsx}\""
  → glob 이 src/** 뿐이라 루트의 playwright.real-qa.config.ts 는 애초에 범위 밖
```

즉 **이 PR 이 바꾼 코드(이 파일)는 typecheck 에도, lint 에도, 어떤 CI 워크플로 실행에도 걸리지
않았다.** 이 사실을 숨기지 않고 그대로 기재한다. (CI 워크플로 실행 부재는 이 라운드에서
그대로 남아있다 — 아래 판단 3 참고. typecheck 는 아래 판단 1 로 조치했다.)

**판단**:

1. **tsconfig.node.json 에 추가함(조치함)** — `clients/desktop/tsconfig.node.json` 의 `include` 에
   `"playwright.real-qa.config.ts"` 를 추가했다. 근거: 이 파일 1개만 추가하는 것은 저장(diff)
   비용이 낮고(1줄), 격리 검증에서 새 오류가 없음을 직접 확인했다(아래). 이 PR 의 주제 자체가
   "게이트 0 표면을 닫는다"이므로, 이 PR 이 새로 건드린 파일이 스스로 같은 문제(게이트 0)를
   남기는 것은 일관성이 없다고 판단했다.

   ```
   cd clients/desktop && npx tsc -p tsconfig.node.json --noEmit
   ```
   ```
   (출력 없음 — 종료 코드 0, 새 오류 없음)
   ```

   다만 저장소 관례상 `playwright/**` 스펙 파일 172개 전체를 typecheck 범위에 새로 넣는 것은
   **하지 않았다** — 그건 이 PR 의 라우팅 fix 범위를 크게 넘는 별건(172개 스펙 전체의 latent
   타입 오류 유무를 이번에 처음 검사하게 되는 것이므로, 실패 시 파급 범위를 예측할 수 없다).
   이번엔 이 PR 이 실제로 수정한 설정 파일 1개만 좁게 넣었다.

   **정직한 부기**: `npm run typecheck`(= `tsc -p tsconfig.node.json && tsc -p tsconfig.web.json`)
   를 그대로 실행하면 이 워크트리에서는 `tsconfig.web.json` 단계에서 실패한다 —
   `@samhan/design-system` 심볼릭 링크는 걸려 있으나(`clients/desktop/node_modules/@samhan/
   design-system` → `clients/web/design-system`) 이 워크트리에서 `design-system` 이 빌드되지
   않아(`dist/` 없음, 직접 확인) `src/renderer/**` 여러 파일에서 `TS2307 Cannot find module`
   및 파생 `any` 오류가 다수 발생한다. 이는 **내 변경과 무관한 이 워크트리의 기존 환경
   상태**다(내가 건드린 `tsconfig.node.json`/`playwright.real-qa.config.ts` 어느 쪽도
   `src/renderer` 를 참조하지 않는다) — 그래서 검증은 `tsc -p tsconfig.node.json --noEmit`
   단독 실행으로 좁혔다(위 원문, exit 0). design-system 을 빌드해 `npm run typecheck` 전체를
   통과시키는 것은 이 config 라우팅 fix 의 범위를 넘는 별도 작업이라 하지 않았다.

2. **lint glob 은 바꾸지 않음(의도적 보류)** — `eslint "src/**/*.{ts,tsx}"` 는 `clients/desktop`
   루트의 다른 설정 파일들(`vite.web.config.ts`, `vite.capacitor.config.ts`,
   `vite.renderer.dev.config.ts`, `electron.vite.config.ts`, `vite.print-renderer.config.ts` 등,
   전부 직접 확인)도 전부 lint 범위 밖이다 — "루트 설정 파일은 lint 하지 않는다"는 이 저장소의
   **기존 컨벤션**이지 이 파일만의 누락이 아니다. 이 컨벤션 자체를 바꾸는 것은 이번 PR 의
   축(게이트 0 표면 2건)과 다른 별건이라 손대지 않았다.

3. **CI 실행 배선은 하지 않음(범위 초과 판단)** — 이 하네스에 CI 실행을 새로 붙이려면 데스크톱
   렌더러 + order-app 두 서버를 GitHub Actions 러너에서 기동하는 새 워크플로/잡을 설계해야
   한다. 이는 "라우팅이 틀렸다"는 R1-1/R1-2 지적의 fix 범위를 크게 넘는 별도 슬라이스이고,
   이 하네스가 "CI 배선 없는 로컬 수동 일괄 실행 전용"이라는 성격 자체는 이번 PR 이 만든 것이
   아니라 기존 상태다(§5 문서 정정 참고). 이번 라운드에서는 손대지 않고 사실만 기재한다.

## 4. B-2 — arologis 정적 가드 2종

### 4-1. arologis-ci.yml 잡 배선 — 변경 없음

`.github/workflows/arologis-ci.yml` 의 `notion-zero-guard`/`config-audit-guard` 잡 자체(R0 이
추가)는 이번 라운드에서 **수정하지 않았다** — R1 이 지적한 결함은 잡이 없다는 것이 아니라
**잡이 호출하는 스크립트가 arologis 표면을 스캔하지 않는다**(R1-3)는 것이었다. 트리거 `paths`
원문(직접 재확인, 변경 없음):

```
pull_request:
  paths:
    - 'services/arologis-service/**'
    - 'clients/arologis-desktop/**'
    - 'clients/web/design-system/**'
    - 'clients/arologis-mobile/**'
    - 'shared/**'
    - 'scripts/check-credential-plaintext.sh'
    - '.github/workflows/arologis-ci.yml'
```

공용 `ci.yml` 의 `paths-ignore`(직접 재확인):

```
paths-ignore:
  - 'services/arologis-service/**'
  - 'clients/arologis-desktop/**'
  - 'clients/arologis-mobile/**'
  - '.github/workflows/arologis-ci.yml'
  - '.github/workflows/arologis-deploy.yml'
```

`clients/arologis-desktop/**`·`clients/arologis-mobile/**` 만 바꾼 PR 은 `ci.yml` 에서 **제외**되고
`arologis-ci.yml` 에서만 실행된다 — 이 두 경로가 R1-3 RED 주입 위치로 적합한 이유다(§4-3).
`shared/**` 는 `ci.yml` paths-ignore 에 없어(=트리거됨) 두 워크플로 모두에서 실행된다(§4-4).

### 4-2. R1-3 결함 — notion-zero-guard 가 스캔 안 하는 표면

`scripts/check-notion-zero.sh` 의 `SCAN_DIRS`(수정 전):

```bash
SCAN_DIRS=(
  "clients/web"
  "clients/desktop/src"
  "clients/mobile-staff/src"
  "services"
)
```

`clients/arologis-desktop`·`clients/arologis-mobile`·`shared` 가 없다. arologis-ci 가 단독
책임지는 표면(위 paths) 중 서비스 백엔드(`services/arologis-service`)만 (`services` 로) 커버되고
클라이언트 2종은 전혀 안 걸린다.

**구판(R0) RED 증거가 무효였던 이유**: R0 는 `clients/desktop/src/__851_gate_gap_notion_probe.ts`
에 주입했다 — 이 경로는 ⓐ `clients/desktop/src` 가 이미 SCAN_DIRS 에 있어 애초에 "스캔되는" 곳이고
ⓑ 애초에 `arologis-ci.yml` 의 트리거 `paths` 에도 없다(`clients/desktop/**` 는 arologis-ci 트리거
목록에 없음, §4-1). 즉 그 RED 는 "새 arologis 잡이 arologis 표면을 잡는다"는 것을 증명하지 못한다.

### 4-3. fix — RED-first (본 라운드 직접 실행)

**Step 1 — fix 전, 결함 재현(arologis 트리거 경로 안에 probe 주입)**:

```
clients/arologis-desktop/src/__851_gate_gap_notion_probe.ts:
  export const gateGapNotionProbe = 'NOTION_TOKEN'
```
```
bash scripts/check-notion-zero.sh
```
```
[PASS] Notion runtime 의존 zero 확인 완료 — 위반 없음
종료 코드: 0
```
**실제 Notion 토큰 문자열이 arologis-desktop 소스에 있는데도 PASS** — R1-3 이 지적한 결함을
직접 재현했다.

**Step 2 — fix**: `SCAN_DIRS` 에 `clients/arologis-desktop/src`·`clients/arologis-mobile/src`·
`shared` 추가, `services` 전용이던 "src/main/ 만 검사" 필터를 `shared` 에도 적용(둘 다 동일한
Gradle Java 모듈 레이아웃 — `<module>/src/main/java`, `<module>/src/test/java`).

**Step 3 — fix 후, probe 3개 동시 주입(RED 기대) + src/test 제외 확인용 1개 추가**:

```
clients/arologis-desktop/src/__851_gate_gap_notion_probe.ts       (신규 SCAN_DIRS 대상)
clients/arologis-mobile/src/__851_gate_gap_notion_probe.ts        (신규 SCAN_DIRS 대상)
shared/common/src/main/java/com/samhanair/Gate851NotionProbeMain.java   (신규 SCAN_DIRS 대상, src/main)
shared/common/src/test/java/com/samhanair/Gate851NotionProbeTest.java  (src/test — 제외돼야 함)
```
```
bash scripts/check-notion-zero.sh
```
```
============================================================
[FAIL] Notion runtime 의존 발견 — SP-08-7 정책 위반
============================================================
  VIOLATION: .../clients/arologis-desktop/src/__851_gate_gap_notion_probe.ts:1:export const gateGapNotionProbe = 'NOTION_TOKEN'
  VIOLATION: .../clients/arologis-mobile/src/__851_gate_gap_notion_probe.ts:1:export const gateGapNotionProbeMobile = 'NOTION_TOKEN'
  VIOLATION: .../shared/common/src/main/java/com/samhanair/Gate851NotionProbeMain.java:5:    public static final String TOKEN_ENV = "NOTION_TOKEN";
============================================================
종료 코드: 1
```

`shared/common/src/test/java/.../Gate851NotionProbeTest.java` 는 **목록에 없다** — src/main/ 한정
필터가 `shared` 에도 정확히 적용됨을 확인했다(services 와 동일 동작).

**Step 4 — 4개 probe 전부 삭제, 원복 확인**:

```
rm clients/arologis-desktop/src/__851_gate_gap_notion_probe.ts \
   clients/arologis-mobile/src/__851_gate_gap_notion_probe.ts \
   shared/common/src/main/java/com/samhanair/Gate851NotionProbeMain.java \
   shared/common/src/test/java/com/samhanair/Gate851NotionProbeTest.java
bash scripts/check-notion-zero.sh
```
```
[PASS] Notion runtime 의존 zero 확인 완료 — 위반 없음
종료 코드: 0
```
`git status --porcelain` 로 임시 파일이 하나도 남지 않았음을 확인했다(§7 최종 파일 목록에
probe 파일 없음).

### 4-4. `shared/**` 포함 여부 — 포함하기로 판단(근거)

포함했다. 근거:

- `shared/*` 는 전부 Gradle Java 모듈이다(`shared/common/src/main/java/...` 등, 직접 확인 — TS/JS
  파일 0개). `services/*` 와 동일한 레이아웃이라 같은 필터 로직(src/main/ 한정)을 그대로
  재사용할 수 있어 구현 비용이 낮다.
- `shared/**` 는 `arologis-ci.yml` 과 `ci.yml` **둘 다의 트리거 경로**다(§4-1) — 즉 "arologis 만
  건드린 PR"이 `shared/*` 를 통해서도 Notion 의존을 반입할 수 있는 실제 경로가 있다. arologis
  표면을 실제로 게이트한다는 이번 라운드의 목표에 정확히 해당한다.
- 부수 효과: 이 한 줄 추가로 **공용 `ci.yml` 의 기존 notion-zero-guard 잡도** 같은 gap 이 있었다는
  점(트리거는 됐지만 스캔은 안 하던 것)이 함께 해소된다. `check-notion-zero.sh` 는 두 워크플로가
  공유하는 단일 스크립트라, 이 fix 는 arologis 전용 fix 인 동시에 기존 공용 가드의 latent gap 도
  닫는다 — 스코프를 벗어난 별도 변경이 아니라 같은 스크립트의 같은 배열에 항목을 추가하는
  것뿐이다.

### 4-5. B-2 config-audit-guard — 이번 라운드 재확인(현재 PASS)

R1 은 config-audit-guard 에 결함을 제기하지 않았고, 이번 라운드에서 이 잡의 배선(`arologis-ci.yml`)
도 수정하지 않았다. R0 의 RED 증거(SlipClient 포트 8086→8084 임시 주입 시 `MISMATCH` RED,
원복 후 PASS)는 재실행하지 않았지만, **현재 상태의 PASS 는 이번 라운드에서 직접 재확인**했다.

```
powershell.exe -NoProfile -ExecutionPolicy Bypass -File infrastructure/scripts/validate-config-audit.ps1 -Detailed
```
```
config-audit validation passed: 158 URL/template checks
```
R0 이 보고한 수치(158)와 일치한다.

## 5. 문서/주석 정정 내역

| 대상 | 정정 전 | 정정 후 |
|---|---|---|
| `docs/dev-reports/2026-07-27-851-gate-gaps.md`(본 문서, 구판) | "4개 파일" · 인용 위조 2건 · ":150 AUDIT_BASE_URL override 로 각 서버 origin 선택 가능"(거짓) · ":53 최종 전체 실행 권위는 CI 로 남기고"(거짓 — CI 는 이 하네스를 실행하지 않음) | 본 문서 전체 재작성(§0~§7) |
| `docs/superpowers/plans/2026-07-27-851-gate-gaps.md` | 작업1/작업2 체크리스트가 "대상 4개 파일"로 표기 | "대상 4개 파일을 조사, 그중 상대경로 의존 3개(897·928·929-r4) + 929-r5(절대 URL, 무관)"로 정정 |
| `docs/superpowers/specs/2026-07-27-851-gate-gaps-spec.md:13` | "…4개는 `Cannot navigate to invalid URL` 로 **조용히 빠진다**"(PM 기획 오류 — 실측과 반대) | "…4개는 `Cannot navigate to invalid URL` 로 **요란하게(각 파일 첫 테스트가 즉시 실패로) 실행 중단**된다 — '미실행'이 아니라 '실행되지만 실패'가 문제의 실제 성격" 으로 정정 |
| `clients/desktop/playwright.real-qa.config.ts` 헤더 주석 | "repo 전체 real-QA 스펙(83개)" | "172개 파일 · 548개 테스트(2026-07-27 실측)" |
| `929-r5-route-collision-real-qa.spec.ts:30-36`(구) | "공유 config 에는 use.baseURL 이 없다" | §2 참고 — 현재 상태(projects 분리)에 맞게 정정하되 929-r5 자체 설계는 유지 |
| `ROADMAP.md` | 이 슬라이스(R1 fix) 미기재 | "최신 진행 메모 (2026-07-27)" 절 신설, 이 PR 요약 1단락 추가(§7 커밋 목록에 반영) |

## 6. 서버 기동/정리 요약

| 서버 | 기동 명령 | 상태 확인 | 정리 |
|---|---|---|---|
| 데스크톱 렌더러(:5175) | `VITE_API_BASE_URL=http://localhost:8080 node_modules/.bin/vite dev --config vite.renderer.dev.config.ts` | curl 200 (기동), 200→000 아님(정상), netstat LISTENING 확인 | `taskkill /PID 61156 /T /F`(자식 62528 포함) → curl 000, netstat 리스너 없음 확인 |
| order-app(:5181) | 기동 안 함 | `clients/web/order-app` 에 `node_modules` 미설치(별도 `npm install` 필요 — 이 config 라우팅 fix 범위 밖으로 판단, §3 판단 3 과 같은 논리) | 해당 없음 |
| Docker 백엔드(게이트웨이:8080 등 14 컨테이너) | 이미 기동 중이던 기존 스택(직접 만들지 않음, `docker ps` 로 확인만 함) | curl `/actuator/health` → 200 | 이번 세션에서 내리지 않음(공유 스택, 다른 작업에 영향 주지 않기 위해 그대로 둠) |

928 라이브 실행이 이 Docker 백엔드에 실제로 쓰기(릴리스 등록/배포/삭제)를 했으나, 스펙 자체의
`finally` 블록이 즉시 soft-delete 로 정리했고 before=0/after=0 으로 직접 확인했다(§1-4).

## 7. 최종 파일 목록

- `clients/desktop/playwright.real-qa.config.ts` — B-1 재설계(projects 분리 + 신규 override 이름)
- `clients/desktop/tsconfig.node.json` — V-3 조치(이 config 파일을 typecheck 대상에 포함)
- `clients/desktop/playwright/929-r5-route-collision-real-qa/929-r5-route-collision-real-qa.spec.ts` — 주석만 정정(§2), `page.goto` 등 실행 코드 변경 없음
- `scripts/check-notion-zero.sh` — R1-3 fix(SCAN_DIRS 확장 + services/shared 공통 src/main/ 필터)
- `docs/dev-reports/2026-07-27-851-gate-gaps.md` — 본 문서(전면 재작성)
- `docs/superpowers/plans/2026-07-27-851-gate-gaps.md` — §5 표 정정
- `docs/superpowers/specs/2026-07-27-851-gate-gaps-spec.md` — §5 표 정정(PM 기획 오류 수정)
- `ROADMAP.md` — 최신 진행 메모 절 추가

`.github/workflows/arologis-ci.yml` 은 R0 산출물 그대로이며 이번 라운드에서 **수정하지 않았다**
(§4-1). 임시 probe 파일(notion 4종) 은 전부 삭제되어 목록에 없다 — `git status --porcelain` 으로
직접 확인.

## 8. R2 라운드 fix — 기동 경로와 증거 무결성

### 8-1. R2-1 renderer 기동 경로

#### 재현

수정 전 문서 명령에 `VITE_APP_VERSION`이 없었다. 사용자가 제시한 명령과 같은 작업 디렉토리·인자로
renderer를 기동하고 브라우저에서 body/pageerror를 확인했다.

```powershell
cd clients/desktop
$env:VITE_API_BASE_URL='http://localhost:8080'
.\node_modules\.bin\vite.cmd dev --config vite.renderer.dev.config.ts
```

```text
EXACT_RENDERER_BODY=""
EXACT_RENDERER_PAGEERRORS=["VITE_APP_VERSION에 릴리스 버전을 명시적으로 주입해야 합니다."]
```

원인은 `clients/desktop/src/renderer/version/versionCheck.ts:24-30`의 무주입 예외 정책과
`vite.renderer.dev.config.ts` 사용 주석의 환경변수 누락이었다. 정책을 약화하지 않고 주석의 실행
경로에 필요한 버전 주입을 추가했다.

#### fix 후 같은 경로 실행

```powershell
cd clients/desktop
$env:VITE_API_BASE_URL='http://localhost:8080'
$env:VITE_APP_VERSION='2026/07/26-92700'
.\node_modules\.bin\vite.cmd dev --config vite.renderer.dev.config.ts
```

실행 시 서버 종료를 보장하기 위해 위 명령의 자식 process를 `Start-Process`로 감싸 실행했으며,
작업 디렉토리·환경변수·Vite 인자는 동일했다. 브라우저 확인 원문:

```text
FIXED_RENDERER_HTTP=200
FIXED_RENDERER_BODY="세션 확인 중"
FIXED_RENDERER_PAGEERRORS=[]

VITE v5.4.21  ready in 374 ms
Local:   http://127.0.0.1:5175/
```

정리 원문:

```text
FIXED_RENDERER_KILL_PID=33828
SUCCESS: The process with PID 69112 (child process of PID 18752) has been terminated.
SUCCESS: The process with PID 42600 (child process of PID 33828) has been terminated.
SUCCESS: The process with PID 18752 (child process of PID 33828) has been terminated.
SUCCESS: The process with PID 33828 (child process of PID 22252) has been terminated.
FIXED_RENDERER_NETSTAT_AFTER
```

`FIXED_RENDERER_NETSTAT_AFTER` 다음에 `:5175 LISTENING` 행은 없었다.

### 8-2. R2-2 order-app 기동 경로

#### 재현 1 — clean preview

기존 `clients/web/order-app/dist`를 워크트리 안의 임시 백업으로 옮긴 뒤 `npm run preview`만 실행하고,
검증 후 원래 dist를 복원했다.

```text
CLEAN_INSTALL_PREVIEW_EXIT=1

> @samhan/order-app@0.4.0 preview
> vite preview

error when starting preview server:
Error: The directory "dist" does not exist. Did you build your project?
```

`:5181 LISTENING`은 없었고 `ORDER_APP_DIST_RESTORED=1`을 확인했다.

#### 재현 2 — build 후 잘못된 API 기본 경로

환경변수 없이 `npm run build` 후 preview를 실행했다.

```text
ORDER_BUILD_BEFORE_EXIT=0
ORDER_BUILD_PREVIEW_HTTP=200
ORDER_BUILD_PREVIEW_BODY="주문서 데이터를 불러오지 못했습니다.\n\n새로고침 후 다시 시도해주세요. 문제가 계속되면 관리자에게 문의해주세요.\n\n새로고침\n\n오류: HTTP 404"
ORDER_BUILD_PREVIEW_PAGEERRORS=["order-app bootstrap failed: HTTP 404"]
```

`vite.config.ts`가 build 시 `VITE_API_BASE_URL`을 `/api/v1`로 두어 preview origin `:5181`에
요청한 것이 원인이다.

#### fix

- `clients/web/order-app/package.json`의 `preview`를 `npm run build && vite preview`로 바꿔 깨끗한
  체크아웃에서도 build 없이 preview가 시작되지 않게 했다.
- `928` 정본 Playwright config의 `webServer`를 `npm run preview -- --host 127.0.0.1 --port 5181
  --strictPort`로 바꾸고, 그 process에 `VITE_API_BASE_URL=http://localhost:8080/api/v1` 및
  `VITE_APP_VERSION=2026/07/26-92700`을 주입했다. 기존 `QA_BASE_URL`은 Playwright 스펙 오리진
  override로만 남겼다.
- README와 Vite 주석에도 같은 환경변수·build 후 preview 계약을 적었다.

#### fix 후 같은 경로 실행

```powershell
cd clients/web/order-app
$env:VITE_API_BASE_URL='http://localhost:8080/api/v1'
$env:VITE_APP_VERSION='2026/07/26-92700'
npm run preview -- --host 127.0.0.1 --port 5181 --strictPort
```

실행 원문:

```text
> @samhan/order-app@0.4.0 preview
> npm run build && vite preview --host 127.0.0.1 --port 5181 --strictPort

> @samhan/order-app@0.4.0 build
> tsc -p tsconfig.json --noEmit && vite build

✓ 62 modules transformed.
✓ built in 387ms
Local:   http://127.0.0.1:5181/

FIXED_ORDER_APP_HTTP=200
FIXED_ORDER_APP_BODY="사업자등록번호\n조회\n① 사업자 등록·승인 안내\n본 시스템은 최초 1회 사업자 등록 및 승인 절차 완료 후 이용 가능합니다.\n승인 관련 문의: (주)삼한공조시스템 ☎ 02-3465-1331\n② 이용 환경 안내\n본 링크는 PC와 모바일 환경을 지원합니다.\nPC 또는 모바일로 접속하여 사용하시기 바랍니다.\n홈멀티\n싱글중대형\n상업멀티\n구형"
FIXED_ORDER_APP_PAGEERRORS=[]
```

정리 원문:

```text
FIXED_ORDER_APP_KILL_PID=33008
SUCCESS: The process with PID 23320 (child process of PID 32780) has been terminated.
SUCCESS: The process with PID 32780 (child process of PID 46560) has been terminated.
SUCCESS: The process with PID 46560 (child process of PID 68632) has been terminated.
SUCCESS: The process with PID 56808 (child process of PID 33008) has been terminated.
SUCCESS: The process with PID 68632 (child process of PID 33008) has been terminated.
SUCCESS: The process with PID 33008 (child process of PID 43220) has been terminated.
FIXED_ORDER_APP_NETSTAT_AFTER
ORDER_APP_DIST_RESTORED=1
```

`FIXED_ORDER_APP_NETSTAT_AFTER` 다음에 `:5181 LISTENING` 행은 없었다.

#### 928 정본 config 실행

```powershell
cd clients/desktop
node_modules\.bin\playwright.cmd test --config=playwright\928-web-version-check-real-qa\playwright.config.ts --reporter=line --timeout=60000
```

```text
[오폭] 등록 전 SAMHAN_ORDER_WEB 행 수=0
[오폭] 등록 후 판정={"order":{"http":200,"latestVersion":"2026/07/26-92801","minSupportedVersion":"2026/07/26-92700","forceLevel":"MINOR","releaseNotes":"QA #928 throwaway 주문 웹 버전 안내 확인","releasedAt":"2026-07-26T00:00:00"},"estimate":{"http":404},"mobile":{"http":404}}
[도달] 안내 문구=새 주문 웹 버전 2026/07/26-92801을 사용할 수 있습니다. 페이지 새로고침 나중에
[M2 라이브 조사] 작성 폼 DOM 수=5
[정리] SAMHAN_ORDER_WEB 행 수 before=0, after=0
1 passed (8.4s)
```

Playwright webServer 종료 후 별도 확인:

```text
POST_928_NETSTAT_5181
POST_928_DIST_EXISTS=1
POST_928_TEMP_BACKUP_EXISTS=0
```

`POST_928_NETSTAT_5181` 다음에 `:5181 LISTENING` 행은 없었다. `dist`는 Playwright가 생성한
정상 산출물로 남아 있으며, R2 임시 백업은 없다.

### 8-3. R2-3 수치·명령 무결성

#### 재현 불가 명령의 실제 출력

기존 문서 명령을 그대로 실행했다. 이 환경의 GNU grep은 `--glob`을 지원하지 않는다.

```text
grep: unrecognized option '--glob'
Usage: grep [OPTION]... PATTERNS [FILE]...
Try 'grep --help' for more information.
EXACT1_EXIT=2
```

#### fix 후 재현 명령과 실제 출력

`grep`의 GNU 옵션인 `--include`/`--exclude`를 사용했다. `--exclude` 두 개는 git 무시 상태인
`manual/slip-form-3d-real-qa.spec.ts`와 `dispatch-collab-real-qa.spec.ts`를 기존 R1 집계 대상에서
제외하기 위한 것이다.

```bash
cd clients/desktop
GREP_SCOPE=(--include='*-real-qa.spec.ts' --exclude='slip-form-3d-real-qa.spec.ts' --exclude='dispatch-collab-real-qa.spec.ts')
AUDIT_FILES=$(grep -rl "${GREP_SCOPE[@]}" -F "process.env['AUDIT_BASE_URL']" playwright)
QA_FILES=$(grep -rl "${GREP_SCOPE[@]}" -F "process.env['QA_BASE_URL']" playwright)
printf 'DIRECT_PROCESS_ENV_REAL_FILES=%s\n' "$(printf '%s\n' "$AUDIT_FILES" "$QA_FILES" | sort -u | sed '/^$/d' | wc -l)"
printf 'UNIQUE_DEFAULT_NOT_5175=%s\n' "$(printf '%s\n' "$AUDIT_FILES" "$QA_FILES" | sort -u | sed '/^$/d' | xargs grep -L -E 'process\.env.*5175' | wc -l)"
printf 'AUDIT_BASE_URL_ENTRIES=%s\n' "$(printf '%s\n' "$AUDIT_FILES" | sed '/^$/d' | wc -l)"
printf 'QA_BASE_URL_ENTRIES=%s\n' "$(printf '%s\n' "$QA_FILES" | sed '/^$/d' | wc -l)"
```

```text
DIRECT_PROCESS_ENV_REAL_FILES=163
UNIQUE_DEFAULT_NOT_5175=121
AUDIT_BASE_URL_ENTRIES=146
QA_BASE_URL_ENTRIES=17
```

기존의 잘못된 두 수치는 보고서와 공유 config 주석에서 제거하고 위 명령·수치로
교체했다. `AUDIT_BASE_URL`/`QA_BASE_URL` override는 계속 공유 config의 두 새 변수와
독립되어 있다.

### 8-4. R1 회귀 울타리 재확인

프로젝트 분할 및 두 override를 함께 지정한 목록 결과:

```text
REAL_QA_LIST_TOTAL
Total: 548 tests in 172 files
REAL_QA_LIST_ORDER_APP
Total: 1 test in 1 file
REAL_QA_LIST_RENDERER
Total: 547 tests in 171 files
REAL_QA_LIST_BOTH_OVERRIDES
Total: 548 tests in 172 files
```

Notion zero guard 실주입 RED:

```text
============================================================
[FAIL] Notion runtime 의존 발견 — SP-08-7 정책 위반
============================================================
  VIOLATION: .../clients/arologis-desktop/src/__851_r2_notion_probe.ts:1:export const r2NotionProbe = 'NOTION_TOKEN'
  VIOLATION: .../clients/arologis-mobile/src/__851_r2_notion_probe.ts:1:export const r2MobileNotionProbe = 'NOTION_TOKEN'
  VIOLATION: .../shared/common/src/main/java/com/samhanair/Gate851R2NotionProbeMain.java:6:    public static final String TOKEN_ENV = "NOTION_TOKEN";
NOTION_R2_INJECTION_EXIT=1
```

probe 3개를 삭제한 뒤:

```text
[PASS] Notion runtime 의존 zero 확인 완료 — 위반 없음
NOTION_R2_CLEAN_EXIT=0
```

config-audit guard 실주입 RED 핵심 출력:

```text
MISMATCH SAMHAN_SLIP_SERVICE_URL slip-service 8084 8086
CONFIG_AUDIT_R2_INJECTION_EXIT=1
config-audit validation failed: 1 issue(s)
```

원복 후:

```text
config-audit validation passed: 158 URL/template checks
CONFIG_AUDIT_R2_CLEAN_EXIT=0
```

### 8-5. R2 변경 파일

- `clients/desktop/vite.renderer.dev.config.ts` — renderer 명시 버전 주입 경로 주석 보강
- `clients/desktop/playwright.real-qa.config.ts` — renderer/order-app 기동 전제 및 현재 수치 보강
- `clients/desktop/playwright/928-web-version-check-real-qa/playwright.config.ts` — build 선행 preview와 API·버전 환경 배선
- `clients/web/order-app/package.json` — `preview`의 build 선행
- `clients/web/order-app/vite.config.ts` — real-QA preview 환경 계약 주석
- `clients/web/order-app/README.md` — clean preview 명령·환경변수 문서화
- `docs/dev-reports/2026-07-27-851-gate-gaps.md` — R2 재현·fix·라이브 원문·수치·정리 확인
- `docs/superpowers/plans/2026-07-27-851-gate-gaps.md` — 직접 참조 수치 정정
