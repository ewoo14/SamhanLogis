# PR #1061 거래처별 원장 표시 사양 정합 — 라이브QA

## 실행 준비

- 일시: 2026-08-03 (Asia/Seoul)
- 대상: worktree `C:/dev/Samhan-Public/.claude/worktrees/t1001b`, HEAD `ce53bc009`
- 데이터: `[DEV-SEED]` 로컬 DB. 실 운영 데이터로 간주하지 않음.
- 공유 Docker 스택은 재빌드하지 않음. 기존 host publish 포트 확인: gateway `8080`, slip `18086`, partner `8095`, postgres `5432`.
- standalone accounting-service는 host publish 다운스트림을 Spring Cloud simple discovery URL로 주입할 예정: slip `http://127.0.0.1:18086`, partner `http://127.0.0.1:8095`.
- 캡처 금지 조건을 확인함: JSON/터미널 화면·목업·fixture 이미지는 제출하지 않음.

### standalone 빌드/기동

- `:services:accounting-service:bootJar -x test --no-daemon`: 성공.
- jar SHA-256: `79F0D40BD7BFD7C218CF09E6E6A59237F427DB633BB65FD0D6A86D961A6331BF` (`services/accounting-service/build/libs/accounting-service.jar`).
- 첫 기동은 공유 DB의 미적용 V68 validation에서 중단됨. V68은 `cash_receipts.lines_json` DDL이므로 공유 DB에 적용하지 않음(사용자 금지사항 준수).
- 재기동 시 `SPRING_FLYWAY_IGNORE_MIGRATION_PATTERNS=*:ignored`만 주입하고, 다운스트림 URL은 host publish 포트로 주입함. DB write/DDL 없음.
- 두 번째 기동도 Hibernate schema validation에서 `cash_receipts.lines_json` 누락으로 중단됨. 공유 DB에 V68 DDL을 적용하지 않기 위해 `SPRING_JPA_HIBERNATE_DDL_AUTO=none`으로 validation만 끄고 재시도함.

## 시나리오 1 — 원장 화면 진입/기간 조회

- 조작: 실제 renderer에서 `/accounting/partner-ledger` 진입 → 시작일 `2026-02-01`, 종료일 `2026-03-31` 입력 → `조회` 클릭.
- 화면 결과: `거래처별 집계 총 19건` 목록이 렌더됨. 대상 행 `원주에어컨공업`은 화면 코드 `3211910527`, 매출 합계 `20,000,000원`, 수금 합계 `—`, 채권 잔액 `22,000,000원`으로 표시됨.
- 판정: 부분 실패. 집계 목록 자체는 표시됐으나, 사양의 대상 표시 코드 `P-2026-0017` 및 기대 매출 `12,276,000원`과 불일치함. `[DEV-SEED]` 데이터임.
- 캡처: `docs/qa/1001-partner-ledger-real-qa/01-period-query.png` (실 renderer 화면, JSON/터미널/목업 아님).

## 시나리오 2 — 거래처 선택/상세 원장

- 조작: 집계 목록에서 `원주에어컨공업` 행(화면 코드 `3211910527`)을 클릭.
- 화면 결과: Step 2 영역에 `원장 조회 실패: Request failed with status code 400` 오류 배너가 나타남. 요구한 `P-2026-0017` 행은 집계 화면에 표시되지 않았고, `INSPECTING` 1건·2라인·`12,276,000원`도 확인할 수 없었음.
- 원인 확인: partner-service host publish 포트 `8095`의 실제 `[DEV-SEED]` 거래처 코드는 `P-2026-0017`이며 `3211910527` 조회는 404. standalone accounting-service에 올바른 `P-2026-0017`을 직접 넣어도 공유 accounting DB가 V68의 `cash_receipts.lines_json` 컬럼을 갖고 있지 않아 JPA 조회가 500으로 종료됨. 공유 DB에 migration/DDL을 적용하는 것은 금지되어 수행하지 않음.
- 판정: 미실시(상세 원장을 성공 화면으로 확인하지 못함). 오류 배너 화면은 제출 캡처로 남기지 않음.
- 캡처: 없음. 금지된 오류 배너 화면이므로 폐기함.

## 시나리오 3 — 목록/상세 금액 일치

- 조작: 시나리오 2의 상세 원장 진입을 전제로 대상 거래처를 선택.
- 화면 결과: 상세 조회가 HTTP 400으로 끝나 상세 합계를 렌더할 수 없었음. 집계에는 `[DEV-SEED]` 매출 `20,000,000원`이 표시되지만, 요구 기대값 `12,276,000원`과도 일치하지 않음.
- 판정: 미실시. 목록/상세 금액 일치 여부를 성공 화면에서 비교할 수 없음.
- 캡처: 없음.

## 시나리오 4 — 누적잔액

- 조작: 상세 원장 본문 진입을 시도.
- 화면 결과: 상세 조회 오류로 본문과 누적잔액 행을 렌더할 수 없었음.
- 판정: 미실시. 누적잔액의 실제 값/0 고정 여부를 확인하지 못함.
- 캡처: 없음.

## 시나리오 5 — 인쇄 미리보기

- 조작: 상세 원장 성공 상태가 아니므로 `인쇄 미리보기`를 실행하지 않음.
- 화면 결과: 상세 데이터가 없는 상태에서 인쇄를 진행하면 오류 상태를 캡처하게 되므로 중단함.
- 판정: 미실시. 다중 페이지 합계/기말잔액 1회 표시와 음수 빨강을 확인하지 못함.
- 캡처: 없음.

## 캡처 산출물 목록

- `docs/qa/1001-partner-ledger-real-qa/01-period-query.png`
- 폐기: `02-selected-detail.png` — 실제 오류 배너 화면이라 제출하지 않음.

## 최종 판정

- PR #1061 라이브QA는 핵심 시나리오 2~3을 통과하지 못해 **PASS 아님**.
- 주요 차단 원인: 집계 행의 partnerCode가 `P-2026-0017`이 아닌 `3211910527`로 렌더되는 문제, 공유 accounting DB에 HEAD V68의 `cash_receipts.lines_json`이 없어 standalone 상세 JPA 조회가 실패하는 환경 불일치.
- 수행하지 않은 조치: 공유 Docker 이미지 재빌드, 공유 DB write/DDL, git commit/push.
