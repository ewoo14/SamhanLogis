# #1074 S5 최종 라이브QA — 배포 불일치로 판정 불가

## 환경 확인

결론: **판정 불가**. 배포 중인 `slip-service`가 현재 HEAD `bc5f26eed3af845fe1f350108415e8eb5c6e0619`와 다르므로, 사용자 지시의 배포 게이트에서 중단했다.

| 항목 | 확인값 |
|---|---|
| 작업 트리 | `C:\dev\Samhan-Public\.claude\worktrees\t1074` |
| 현재 HEAD | `bc5f26eed3af845fe1f350108415e8eb5c6e0619` |
| HEAD 시각 | 2026-08-08 06:36:03 KST |
| 배포 컨테이너 | `samhan-slip-service` · healthy |
| 배포 이미지 | `sha256:23efe120dea351dbe55283ea8c1eb85a5a98c853f08d437ed49ee0cffdae9c4a` |
| 이미지 생성 시각 | 2026-08-08 05:38:20 KST |
| 컨테이너 생성 시각 | 2026-08-08 05:38:23 KST |
| 직전 S2 커밋 | `b7ac5d6f84a9df3af747bb65282f08f9dc337feb` · 2026-08-08 05:37:42 KST |

시각만으로 추정하지 않았다. 배포 JAR의 `OutboundCutoffGuard.class`를 읽기 전용으로 검사한 결과, S4에서 제거된 메서드명이 실제로 남아 있었다.

```text
docker exec samhan-slip-service sh -lc "unzip -p /app/app.jar BOOT-INF/classes/com/samhanair/logis/slip/service/cutoff/OutboundCutoffGuard.class | grep -a -o 'assertWithinCutoffForCreation' || true"

결과:
assertWithinCutoffForCreation
```

반면 S4 보고서와 현재 HEAD는 `assertWithinCutoffForCreation` 잔여 참조 0건이며, HEAD 자체도 S4 되돌림 커밋이다. 따라서 현재 배포본은 **되돌리기 전 코드**다.

## 판정

- A 마감 후 익일 생성: **판정 불가**
- B 마감 전 정상 생성 및 차단 건수: **판정 불가**
- C 마감 후 당일 차단: **판정 불가**
- D 활성 마감 태그 6개 전수: **판정 불가**
- M/N 보존·불일치 오류·저장 불가능 조합 탐색: **판정 불가**
- 과거 출고일 신규 생성: **판정 불가**
- KST 날짜·UUID 0·금지 용어 0·권한 풀네임: **판정 불가**

HEAD와 일치하는 `slip-service` 재배포 후 처음부터 새 라운드로 다시 밟아야 한다.

## 안전 조치 및 산출물

- Docker 스택 재기동·재배포: 0건
- DB INSERT/UPDATE/DELETE: 0건
- DB SELECT: 0건
- 신규 QA 전표: 0건
- 기존 QA 전표 삭제: 0건
- Playwright/Vite 신규 프로세스: 0개
- 캡처: 0장
- 커밋·push·코드 수정: 0건
- 신규 파일: `docs/dev-reports/2026-08-08-1074-s5-final-live-qa.md` 1개

## 이 라운드가 보지 않은 것

배포 불일치 게이트에서 중단했으므로 A/B/C/D, 발화 조건 건수, 활성 태그 6개, M/N 조합, 과거 출고일 신규 생성, KST 날짜, 화면 UUID·용어·권한 표기를 실행하거나 판정하지 않았다. HEAD 일치 재배포가 확인되기 전에는 이 항목들에 대한 PASS/FAIL 결론이 없다.
