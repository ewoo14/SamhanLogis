# #883 S4 / PR #1175 CODEX LUNA 5.6 fix1 보고서

- 검증일: 2026-08-12
- 기준 HEAD: `d04a6de5e`
- 작업 범위: S4-SOL-1 `.btnMini` 복구, S4-SOL-2 RED-A real-QA 보강, S4-SOL-3 fixture 복구
- Git commit/branch 조작: 하지 않음

## 결론

`.btnMini`는 기준 스타일을 복구했고 설정 화면 7개 참조가 다시 실제 CSS selector를 사용한다. RED-A는 격리 auth 경로와 값 전수 단언으로 교체했다. 추적 하네스 fixture도 기준 내용으로 복구했다.

단, 현재 워크트리에는 격리 PostgreSQL/서비스가 없고 `39881, 39883, 39884, 39886, 39888, 39889, 39895, 39873, 39880` 포트가 모두 닫혀 있었다. 실행 중인 `samhan-*` 공유 스택으로 로그인하면 안 되므로 라이브 4/4와 RED-A 값 mutation은 실행하지 않았다. 보강한 스펙은 이 조건에서 자동 fallback하지 않고 4건을 skip한다.

## 1. S4-SOL-1 — `.btnMini` 사용 판정 재검증

### 복구

`clients/desktop/src/renderer/components/sales/sales.module.css:54`에 기준 `.btnMini` 규칙을 복구했다.

- `padding: 6px 10px`
- `font-size: 12px`
- `font-weight: 600`
- `cursor: pointer`
- 기존 `border-radius`, `background`, `color`, `font-family` 유지

현재 live 참조 7개:

- `EstimatePricingConfigPage.tsx:345,353` — 되돌리기/저장
- `SalesPartnerDcConfigPage.tsx:228,236` — 검색/CSV 일괄 업로드
- `SalesPartnerDcConfigPage.tsx:330,349` — 행 저장/이력 보기
- `SalesPartnerDcConfigPage.tsx:426` — 이력 닫기

### 44개 재집계 방법

기준 CSS와 기준 importer 8개를 분리해 다시 셌다.

1. 기준 CSS `f73be26701b579d4795a655eb32774469d960210`에서 주석을 제거한 뒤, 독립 selector 정의 패턴 `^\s*\.name\s*(?:\{|:)`으로 selector 이름을 추출했다. 결과는 **100개**다.
2. 다음 importer 8개의 전체 소스를 읽었다: `SalesSubNav`, `EstimateListPage`, `EstimatePricingConfigPage`, `SalesOrderApprovalsPage`, `SalesPartnerDcConfigPage`, `SalesPartnerOrderDetailPage`, `SalesPartnerOrderListPage`, `MergeConvertDialog`.
3. `styles['name']`, `styles["name"]`, dot access를 모두 수집하고, 조건부 literal, 문자열 연결, template literal, `classnames`/`classNames`/`clsx`, 객체 spread로 class key를 조립하는 표현을 별도로 검색했다.
4. 실제 runtime 참조는 **56개**, 기준 CSS에 없고 참조도 없는 dead selector는 **44개**였다.
5. `styles[변수]`처럼 해석을 확정할 수 없는 동적 index가 있었다면 보존했을 것이다. 이번 importer에는 해당 형태가 없었고, 조건부 class 조합은 모두 literal key였다. `className` 객체 spread는 있었지만 CSS module key를 동적으로 만드는 사용은 없었다.

재확인한 dead 44개:

```text
addrDockBox, addrDockOverlay,
autocompleteEmpty, autocompleteItem, autocompleteItemActive, autocompleteList, autocompleteWrap,
branchPlaceholder,
btn, btnAccent, btnDanger,
catBtn, catBtnActive,
dragHandle,
filterBar, filterSearch,
grid,
historyRow,
menuToolbar, menuToolbarBtn, menuToolbarBtnHistory, menuToolbarBtnPrimary, menuToolbarBtnSave, menuToolbarBtnSnapshotList,
modal, modalBackdrop, modalBody, modalFoot, modalHead, modalTitle,
noPrint,
printFooter, printHeader, printMeta, printPaper, printPaperLegacy, printPartnerBox, printPartnerInfo, printShell, printTableLegacy, printToolbar,
qtyInput,
ratioBad,
sendOrderToast
```

복구 후 재계산:

```text
BASE_SELECTOR_COUNT=100
RUNTIME_SELECTOR_COUNT=56
APPROVED_DEAD_COUNT=44
CURRENT_APPROVED_DEAD_REMAIN=0
CURRENT_BTNMINI=1
BTNMINI_LITERAL_REFS=7
```

### RED → GREEN

`EstimatePricingConfigPage.test.ts`에 실제 CSS 파일과 두 설정 페이지의 7개 참조를 읽는 계약 테스트를 먼저 추가했다.

- 복구 전: `1 failed, 2 passed` — `.btnMini` selector 없음으로 의도한 RED
- 복구 후: `3 passed`

이는 `.btnMini`를 일부러 제거한 mutation이 RED가 되는 증거다. 라이브 브라우저의 computed style 7개 단언은 아래 RED-A real-QA 스펙에 추가했다.

## 2. S4-SOL-2 — RED-A 보강

대상 파일: `clients/desktop/playwright/883-s4-order-ds-migration-real-qa/883-s4-order-ds-migration-real-qa.spec.ts`

보강 내용:

- `S4_REAL_QA_AUTH_BASE_URL`을 필수로 추가했다. 스펙 자체가 `${AUTH_BASE_URL}/auth/login`에 `dev_master`로 로그인하고 `samhanAuth`를 fresh Playwright context에 주입한다.
- 공유 gateway/공유 DB URL 기본값과 로그인 fallback을 제거했다. auth URL이 없으면 skip한다.
- baseline을 주문/견적 각각 `list`, `detail`, `print`로 확장했다.
- 각 화면 contract에 `documentNumber`, `documentDate`, `dueOrValidDate`, `amountValues`, `quantityValues`, `productValues`, `allStatusLabels`를 받는다. 상세에는 선택된 `currentStatusLabel`도 단언할 수 있다.
- 주문 상태 6종을 전수 단언한다: `진행중`, `보류`, `확인중`, `완료`, `취소`, `전환완료`.
- 견적 상태 5종을 전수 단언한다: `작성중`, `발송완료`, `수주완료`, `거절`, `전표변환완료`.
- 숫자는 body 전체 문자열만으로 통과하지 않는다. 목록은 문서번호가 있는 `row`, 상세는 `.detail-grid > div`의 label/value, 라인은 품목 row, 합계는 totals 영역으로 범위를 좁혀 단언한다.
- 주문 인쇄 popup HTML과 견적 브라우저 미리보기를 모두 열고 문자열 전수를 읽는다. popup/page는 `afterEach`와 각 테스트에서 닫는다.
- 설정 화면을 실제로 방문해 `.btnMini` 7개(`검색`, `CSV 일괄 업로드`, 행 `저장`, `보기`, `닫기`, `되돌리기`, 전역 `저장`)의 class/padding/font-size/cursor를 computed style로 단언한다.
- 스펙/디렉터리 이름은 계속 `883-s4-order-ds-migration-real-qa`, 증거 경로는 계속 `resolveQaShotsDir(...)/_local`이다.

스펙 구조 확인:

```text
4 tests in 1 file
```

현재 환경에서 실제 실행 결과:

```text
Running 4 tests using 1 worker
4 skipped
```

skip 사유는 격리 URL/ID/baseline 미제공이다. 실제 격리 포트도 모두 닫혀 있어 4/4 성공으로 기록하지 않았다. 따라서 RED-A 값 baseline mutation 역시 아직 실행하지 않았다. 격리 stack과 확장 baseline을 준비한 뒤, 정상 baseline 4/4와 금액·수량·상태·문서번호·날짜 중 하나를 바꾼 baseline의 RED를 각각 실행해야 한다.

## 3. S4-SOL-3 — 범위 밖 fixture 복구

기준 PR diff에서 삭제 파일 목록을 `git diff --name-status f73be26701b579d4795a655eb32774469d960210...HEAD`로 전수 확인했다.

```text
D  tools/.s24-build-only/build/deep/tracked-writer.mjs
```

삭제 목록은 이 1건뿐이며, 주문·견적 DS 변경과 무관한 하네스 fixture이므로 범위 밖이다. 파일은 다음 기준 내용으로 복구했다.

```js
const OUT = 'docs/qa/.s24-build-only.png'
```

복구 후 확인:

```text
FIXTURE_EXISTS=True
FIXTURE_CONTENT=const OUT = 'docs/qa/.s24-build-only.png'
```

이 파일은 저장소의 `build/` ignore 규칙에 걸리므로 현재 unstaged `git diff`에는 복구 파일이 나타나지 않는다. PM이 commit할 때 해당 tracked fixture를 force-add해야 최종 PR diff의 삭제도 사라진다. 작업 중 full Vitest가 이 fixture를 다시 삭제했으므로, 테스트 종료 후 마지막으로 복구했고 이후 전체 테스트는 다시 실행하지 않았다.

최종 파일시스템 기준 범위 밖 삭제: **0건**. 역사적 PR diff에서 보이는 삭제 1건은 위 fixture이며 복구 대상이다.

## 4. RED-B 회귀 및 정적 보존

이번 fix에서 주문/견적 도메인 값 계산·API·상태 매핑은 변경하지 않았다. 기존 DS 전환 변경분에 대해 다음을 확인했다.

- 주문·견적 상세의 `Card` + `detail-grid` 구조 유지
- `detail-grid-item-empty` 계약 유지
- 주문 협업 panel, 재고 조회 modal, 라인 참조 modal 유지
- 목록 정렬/필터/행 클릭/페이지 상태/권한 guard/자동 빈 행 로직 변경 없음
- 금액·수량·상태 11종·문서번호·날짜·인쇄 문자열을 보존하는 기존 코드 diff 밖

## 5. 검증 결과

| 명령 | 결과 |
|---|---|
| `npm test -- --run src/renderer/routes/EstimatePricingConfigPage.test.ts` | 3 passed |
| `npm test -- --reporter=dot` | 254 files passed, 2221 passed, 2 skipped |
| `npm run typecheck` | exit 0, 51 tests passed |
| `npm run lint` | exit 0, 0 errors / 173 warnings |
| `npx playwright test ...883-s4... --list` | 4 tests listed |
| real-QA 실행 | 격리 환경 부재로 4 skipped; 성공 주장하지 않음 |
| `git diff --check` | 통과 |

## 6. 변경 파일

- `clients/desktop/src/renderer/components/sales/sales.module.css`
- `clients/desktop/src/renderer/routes/EstimatePricingConfigPage.test.ts`
- `clients/desktop/playwright/883-s4-order-ds-migration-real-qa/883-s4-order-ds-migration-real-qa.spec.ts`
- `tools/.s24-build-only/build/deep/tracked-writer.mjs` — ignored tracked fixture, 물리 복구 완료

라이브 4/4와 RED-A 값 mutation은 공유 DB를 건드리지 않는 격리 stack을 PM이 준비한 뒤 재실행해야 한다.
