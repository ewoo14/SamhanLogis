# #910·#935 릴리스 feed 첫 구현 라운드

> 대상: `feat/910-release-feed` · Electron `clients/internal-chat-desktop` 1개
> 날짜: 2026-08-13

## 결론

`electron-updater 6.8.9`는 자체서명 인증서를 금지하지 않는다. 다만 Windows가 인증서를 신뢰하지 않으면 updater가 installer를 거부한다.

> **운영 선행조건: 자체서명 인증서의 신뢰 루트를 모든 사내 Windows PC에 배포해야 한다.**
> 신뢰 루트가 없으면 `Get-AuthenticodeSignature`가 `UnknownError`가 되고, `electron-updater`가 `ERR_UPDATER_INVALID_SIGNATURE`로 설치를 거부한다. 이 조건이 충족되지 않으면 자동 설치는 전부 막힌다.

이번 라운드는 그 조건을 코드와 사용자 표시까지 고정했지만, 현재 머신의 electron-builder 패키징 도구가 Windows 권한 문제로 중단되어 signed installer의 실제 `download → quitAndInstall → 재기동` 완주 증거는 남기지 못했다. 따라서 이 문서는 **첫 구현 라운드**이며, 해당 smoke는 다음 QA에서 재개해야 한다.

## 1. `electron-updater` 서명 검증 실측

확인 명령:

```powershell
npm view electron-updater@6.8.9 dist.tarball version --json
```

원문:

```text
{
  "dist.tarball": "https://registry.npmjs.org/electron-updater/-/electron-updater-6.8.9.tgz",
  "version": "6.8.9"
}
```

패키지 소스에서 확인한 원문(라인):

```text
NsisUpdater.js:52  const signatureVerificationStatus = await this.verifySignature(destinationFile);
NsisUpdater.js:56  throw newError(..., "ERR_UPDATER_INVALID_SIGNATURE");
NsisUpdater.js:87  publisherName = (await this.configOnDisk.value).publisherName;
NsisUpdater.js:88  if (publisherName == null) return null;
NsisUpdater.js:99  return await this._verifyUpdateCodeSignature(...);
windowsExecutableCodeSignatureVerifier.js:47  Get-AuthenticodeSignature -LiteralPath ... | ConvertTo-Json -Compress
windowsExecutableCodeSignatureVerifier.js:91  Sign verification failed, installer signed with incorrect certificate
```

즉 `publisherName`이 feed 설정에 있으면 검증하고, 검증 결과가 유효하지 않으면 설치를 중단한다. `publisherName`이 없을 때 검증을 건너뛰는 우회는 이번 구현에서 사용하지 않는다.

자체서명 실측 명령은 unsigned 9101 installer를 임시 복사한 뒤 `signtool.exe`로 서명하고, 같은 인증서를 CurrentUser 신뢰 루트에 넣었다가 제거하는 방식이었다.

원문:

```text
Successfully signed: ...\codex-signtool-release-probe\internal-chat-release.exe
UntrustedStatus : UnknownError
TrustedStatus   : Valid
Signer          : CN=Samhan Internal Release
```

미신뢰 상태의 실제 updater 오류는 다음처럼 renderer에 전달된다.

```text
ERR_UPDATER_INVALID_SIGNATURE: UnknownError
renderer status = {
  kind: 'error',
  message: '업데이트에 실패했습니다. 인터넷 연결을 확인한 뒤 다시 실행해 주세요.'
}
```

상세 `UnknownError`는 사용자 메시지에 노출하지 않지만, 오류 상태와 `다시 확인` 버튼은 표시한다. 따라서 현재 동작은 조용한 정상 통과가 아니라 사용자에게 실패를 보이는 동작이다.

## 2. RED 원문 / 수단 / GREEN 원문

### RED

추가한 계약 테스트를 먼저 실행했다.

```powershell
npm test -- --run src/main/app-shell.contract.test.ts
```

```text
Test Files 1 failed (1)
Tests 2 failed | 6 passed
× 릴리스 패키지는 서명되지 않으면 생성되지 않는다
  expected ... to match /forceCodeSigning:\s*true/
× 서명 신뢰 루트가 없는 경우를 운영 선행조건으로 문서화한다
  expected ... to contain '신뢰 루트'
```

### 고른 수단

- `electron-builder.yml`: `forceCodeSigning: true`로 unsigned release 생성 차단
- `win.publisherName: Samhan Internal Release`로 updater 검증 대상 DN 고정
- `scripts/build-internal-chat-desktop-release.cjs`: `INTERNAL_CHAT_UPDATE_URL`, `CSC_LINK`, `CSC_KEY_PASSWORD` 필수화
- 기존 `createElectronBuilderVersionArgs(...)`를 internal-chat wrapper에도 전달해 내부 semver와 사용자 표시 버전을 계속 분리
- `auto-update.test.ts`: 신뢰 루트 부재의 `UnknownError`가 사용자 오류 상태로 표시되고 상세값은 숨겨지는 회귀 가드
- README: **신뢰 루트 배포가 운영 선행조건**임을 명시

새 런타임 의존성, 채널 분리, 서버 API 변경, migration은 추가하지 않았다. generic feed는 기존 `INTERNAL_CHAT_UPDATE_URL` 경계를 그대로 사용한다.

### GREEN

```powershell
npm test -- --run src/main/app-shell.contract.test.ts src/main/auto-update.test.ts
```

```text
Test Files 2 passed (2)
Tests 11 passed (11)
```

```powershell
node --test scripts/app-build-version.test.cjs
```

```text
tests 16
pass 16
fail 0
```

## 3. 불변식 ①~④ 보증 방법

| 불변식 | 보증 방법 | 결과 |
|---|---|---|
| ① 사내 서버 feed 수신 | `publish.provider: generic`, `url: ${env.INTERNAL_CHAT_UPDATE_URL}` 유지. 생성된 `latest.yml`·installer·blockmap을 Python 내장 HTTP 정적 서버로 각각 GET/HEAD | `200 / 200 / 200` 확인 |
| ② 자체서명 감지→다운로드→설치 | updater의 서명 검증 조건과 trusted/untrusted Authenticode를 실측하고 오류 UI를 가드. 실제 signed package의 `quitAndInstall` 완주는 아래 미완료 사유 참조 | 조건부 확인; 완주 smoke 미완료 |
| ③ 기존 9앱 계약 무변화 | `scripts/app-build-version.cjs`는 변경하지 않음. 양 Electron wrapper의 package semver/날짜 표기 계약 테스트 `16/16` 통과. internal-chat만 builder 입력을 보강 | 통과 |
| ④ 날짜 버전 정본 | `createReleaseBuildEnvironment`와 `createElectronBuilderVersionArgs` 재사용. renderer/NSIS = `YYYY/MM/DD-N`, Electron package semver = `1.YYYYMMDD.N` | 통과 |

Feed smoke 원문:

```text
LatestStatus    : 200
InstallerStatus : 200
InstallerBytes  : 81942978
BlockmapStatus  : 200
latest.yml version: 1.20260813.9101
```

## 4. 변경 모듈 전량 테스트

```powershell
cd clients/internal-chat-desktop
npm test
npm run typecheck
npm run lint
npm run build
node --test ../../scripts/app-build-version.test.cjs
```

결과:

```text
internal-chat vitest: 2 files / 11 tests passed
typecheck: exit 0
lint: exit 0
electron-vite build: exit 0
root app-build-version contract: 16 passed / 0 failed
```

## 5. 판단 필요 / 못 한 것

1. **운영 판단 필요:** 자체서명 루트 인증서를 사내 PC의 `Trusted Root Certification Authorities`에 배포하고 갱신·폐기 절차를 운영해야 한다. 이 배포가 없으면 모든 자동 설치가 `UnknownError`로 막힌다.
2. **이번 라운드 미완료:** `npm run build:win`의 signed package. electron-builder 25.1.8이 `winCodeSign-2.6.0.7z`를 풀 때 macOS 심볼릭 링크 생성 권한을 요구해 실패했다.

원문:

```text
ERROR: Cannot create symbolic link : 클라이언트가 필요한 권한을 가지고 있지 않습니다.
...\darwin\10.12\lib\libcrypto.dylib
...\darwin\10.12\lib\libssl.dylib
ERR_ELECTRON_BUILDER_CANNOT_EXECUTE
```

이는 `electron-updater`의 자체서명 거부가 아니라 Windows 빌드 도구 캐시 압축 해제 권한 문제다. 따라서 이 라운드에서는 실제 사내 서버 업로드, signed `latest.yml` feed, packaged 앱의 `quitAndInstall`·재기동, 다운그레이드까지 완료했다고 주장하지 않는다. 다음 QA는 Developer Mode/심볼릭 링크 권한이 준비된 머신에서 signed 9101→9102 feed 왕복을 재개해야 한다.

3. 상용 인증서, stable/beta 채널, 다른 8개 앱, DB migration은 범위 밖으로 남겼다.

## 라운드 2

### RED: 현재 환경에서의 재현 결과

직전 라운드의 `winCodeSign` 심볼릭 링크 오류를 같은 명령으로 재현하려고 먼저 signed release의 필수 전제를 확인했다. 여유 메모리는 `25.74GB`로 중단 기준인 `1.0GB` 미만이 아니었다.

```powershell
$envStatus=[pscustomobject]@{
  INTERNAL_CHAT_UPDATE_URL=[bool]$env:INTERNAL_CHAT_UPDATE_URL
  CSC_LINK=[bool]$env:CSC_LINK
  CSC_KEY_PASSWORD=[bool]$env:CSC_KEY_PASSWORD
  DeveloperMode=(Get-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock' -Name AllowDevelopmentWithoutDevLicense -ErrorAction SilentlyContinue).AllowDevelopmentWithoutDevLicense
}
$envStatus | Format-List
npm run build:win
```

원문:

```text
INTERNAL_CHAT_UPDATE_URL : False
CSC_LINK                 : False
CSC_KEY_PASSWORD         : False
DeveloperMode            :

> @samhan/internal-chat-desktop@0.1.0 build:win
> node ../../scripts/build-internal-chat-desktop-release.cjs

[internal-chat-release] INTERNAL_CHAT_UPDATE_URL이(가) 필요합니다. 사내 feed와 자체서명 인증서를 지정하십시오.
```

따라서 이번 셸에서는 `winCodeSign` 단계와 signed installer를 실측하지 못했다. 직전 라운드에서 이미 기록된 `winCodeSign` RED 원문은 다음과 같다.

```text
ERROR: Cannot create symbolic link : 클라이언트가 필요한 권한을 가지고 있지 않습니다.
...\darwin\10.12\lib\libcrypto.dylib
...\darwin\10.12\lib\libssl.dylib
ERR_ELECTRON_BUILDER_CANNOT_EXECUTE
```

### 원인 및 고른 수단

직전 라운드의 원인은 `winCodeSign-2.6.0.7z` 캐시 압축 해제 중 Windows 심볼릭 링크 생성 권한 부족으로 확인되어 있다. 다만 라운드 2의 현재 실행은 그 원인을 재확인하기 전에 릴리스 필수 환경변수 누락으로 중단됐다. 인증서·feed 위치를 추측하거나 서명을 끄는 우회는 하지 않았다. 다음 실행에는 자체서명 PFX, 비밀번호, 사내 feed URL, 그리고 Developer Mode 또는 심볼릭 링크 생성 권한이 준비되어야 한다.

### `quitAndInstall → 재기동` 실측

실측하지 못했다. signed installer 생성 전제(`INTERNAL_CHAT_UPDATE_URL`, `CSC_LINK`, `CSC_KEY_PASSWORD`)가 현재 환경에 없어서 `download → quitAndInstall → 재기동` 구간에 도달하지 않았다. 따라서 성공했다고 주장하지 않는다.

### 불변식 ①~④ 보증 상태

| 불변식 | 라운드 2 보증 방법 | 결과 |
|---|---|---|
| ① signed installer 감지 → 다운로드 → 즉시 설치 | 실제 signed installer 및 packaged app 실행 | 미실측: 필수 환경변수 누락 |
| ② 신뢰 루트 부재 시 `UnknownError` 표시 | 직전 라운드의 Authenticode/updater 실측 및 회귀 테스트 기록 유지 | 직전 라운드 보증 유지; 라운드 2 재실행 못 함 |
| ③ 기존 9앱 버전 계약 무변화 | `scripts/app-build-version.cjs` 미변경 및 기존 계약 테스트 | 라운드 2 코드 변경 없음 |
| ④ `YYYY/MM/DD-{번호}` 정본 | `scripts/app-build-version.cjs` 계약 유지 | 라운드 2 코드 변경 없음 |

### 전량 테스트

이번 라운드는 보고서에 실패 원문만 추가했으며 코드/실행 모듈을 변경하지 않았다. signed build가 필수 환경변수 누락으로 시작되지 않아 변경 모듈 전량 테스트 및 `quitAndInstall` smoke는 실행하지 못했다.

### 못 한 것

실패 명령과 원문은 위 RED 절의 `npm run build:win` 출력 전부다. 이 상태에서는 `winCodeSign` 권한 문제를 해결했다고 판정할 수 없고, signed installer 재기동도 검증할 수 없다. 필요한 것은 자체서명 `CSC_LINK`/`CSC_KEY_PASSWORD`, `INTERNAL_CHAT_UPDATE_URL`, 그리고 `winCodeSign` 캐시가 심볼릭 링크를 만들 수 있는 Developer Mode 또는 동등한 권한이다.

### 라운드 2 재개 결과 — signed E2E 통과

직접 확인: `DeveloperMode=1`, `FreeGB=24.9`, Node `fs.symlink(junction/file)=OK`, `New-SelfSignedCertificate` 사용 가능, CurrentUser\My 생성 및 CurrentUser\Root import 성공. PFX/비밀번호는 저장소 밖 임시 경로와 환경변수로만 사용했다.

새 캐시에서 signed build가 성공했고 기존 심볼릭 링크 오류는 재발하지 않았다.

```text
• electron-builder version=25.1.8 os=10.0.26200
• downloading .../winCodeSign-2.6.0.7z size=5.6 MB parts=1
• downloaded .../winCodeSign-2.6.0.7z duration=734ms
• signing with signtool.exe path=release\2026-08-13-9102\win-unpacked\Samhan Internal Chat.exe
• signing file=release\2026-08-13-9102\Samhan Internal Chat-2026-08-13-9102-x64.exe
• building block map blockMapFile=release\2026-08-13-9102\Samhan Internal Chat-2026-08-13-9102-x64.exe.blockmap
• signing file=release\2026-08-13-9102\Samhan Internal Chat-2026-08-13-9102-x64-portable.exe
NSIS: Valid / Signature verified. / CN=Samhan Internal Release
portable: Valid / Signature verified. / CN=Samhan Internal Release
```

feed 실측:

```text
latest.yml HTTP 200 bytes=394
Samhan Internal Chat-2026-08-13-9102-x64.exe HTTP 200 content-length=81978344
Samhan Internal Chat-2026-08-13-9102-x64.exe.blockmap HTTP 200 content-length=86126
latest.yml version: 1.20260813.9102
```

9101 signed installer를 임시 CurrentUser 경로에 설치하고 실행한 실제 updater 왕복:

```text
Reset installer exit=0
Reset asar hash=C8CCFF7B2472BA681FDFE316D4EB37FCEDA954ED0D3C517012F7687F06445D07
Cache exists after clear=False
START pid=70804 oldHash=C8CCFF7B2472BA681FDFE316D4EB37FCEDA954ED0D3C517012F7687F06445D
T=23:48:45 procs=0 pending=3 changed=False
T=23:48:51 procs=4 pending=3 changed=True
NEW_HASH=05C9B1A1DE207EDE52596861AA5E1055CBF93F4D548270025102B41DAB33FE06
RESULT changed=True
DisplayName: Samhan Internal Chat 1.20260813.9102
DisplayVersion: 2026/08/13-9102
```

9101 app.asar 해시는 `C8CCFF7B2472BA681FDFE316D4EB37FCEDA954ED0D3C517012F7687F06445D07`, 9102 및 재기동 후 설치 app.asar 해시는 `05C9B1A1DE207EDE52596861AA5E1055CBF93F4D548270025102B41DAB33FE06`으로 일치했다. 재기동 후 main/renderer/gpu/utility 4개 프로세스를 확인했다. updater 캐시의 signed installer를 동일한 `--updated /S --force-run`으로 실행한 별도 확인도 exit `0` 및 4개 프로세스 재기동이었다.

| 불변식 | 결과 |
|---|---|
| ① 감지 → 다운로드 → 즉시 설치 → 재기동 | PASS — pending 3개, app.asar 교체, DisplayVersion 9102, 프로세스 4개 |
| ② 신뢰 루트 부재 `UnknownError` 사용자 표시 | PASS — 기존 실측 및 `auto-update.test.ts` 회귀 테스트의 `ERR_UPDATER_INVALID_SIGNATURE: UnknownError` |
| ③ 기존 9앱 계약 | PASS — `app-build-version.cjs` 미변경, 16/16 |
| ④ `YYYY/MM/DD-{번호}` | PASS — 9101/9102 입력 및 DisplayVersion 정본, latest.yml 내부 비교값만 semver |

CurrentUser Root 인증서는 동일 주체 2개였고 제거 재시도는 다음 원문으로 타임아웃됐다.

```text
Root certs before remove=2
command timed out after 20028 milliseconds
command timed out after 34036 milliseconds
```

따라서 이번 라운드에 새로 Root 제거 후 `Get-AuthenticodeSignature=UnknownError` 원문은 확보하지 못했다. 직전 라운드의 동일 조건 실측과 회귀 테스트는 보존하며 Root는 `Valid` 상태로 복구했다.

변경 모듈 전량 테스트 1회:

```text
npm test: Test Files 2 passed, Tests 11 passed
npm run typecheck: exit 0
npm run lint: exit 0
npm run build: exit 0
node --test ../../scripts/app-build-version.test.cjs: tests 16 / pass 16 / fail 0
```

이번 라운드의 저장소 변경은 이 보고서 갱신뿐이며 `forceCodeSigning: true`는 유지했다.

## 라운드 4

### 목적과 라운드 3 재현 불가 원인

라운드 3의 산문 절차는 `$work`를 초기화하지 않고 `$pfx = Join-Path $work 'release.pfx'`를 실행했다. 따라서 fresh PowerShell에서 인증서·빌드 단계에 도달하기 전에 `Join-Path`가 null 경로로 종료됐다. 라운드 4에서는 외부 변수를 전혀 요구하지 않는 단일 스크립트 [`scripts/run-internal-chat-release-feed.ps1`](../../scripts/run-internal-chat-release-feed.ps1)를 추가했다.

스크립트는 기본값으로 `2026-08-13`, `9101`, `9102`, feed port `19102`를 사용하며 인자 없이 실행할 수 있다.

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-internal-chat-release-feed.ps1
```

작업 경로와 무작위 인증서 비밀번호를 스스로 만들고, 자체서명 Code Signing 인증서를 `CurrentUser\My`에 생성한 뒤 PFX/CER를 임시 경로에 저장한다. 같은 CER를 `CurrentUser\Root`에 설치하고 signer/root 지문을 비교한 후에만 두 버전 빌드·서명·feed 게시·fresh cache·9101 설치·업데이트 왕복 검증을 진행한다. 각 실패는 `FAIL`과 non-zero로 끝나며, cleanup 항목은 각각 `cleanup PASS` 또는 `cleanup FAIL`을 출력한다. 비밀번호는 출력·저장하지 않는다. `forceCodeSigning: true`는 변경하지 않았다.

### fresh 셸 실행 원문 전문

대화형 세션 변수를 사용하지 않고 다음 명령 하나만 새 PowerShell 프로세스에서 실행했다.

```text
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\dev\Samhan-Public\.claude\worktrees\w910f\scripts\run-internal-chat-release-feed.ps1" -TimeoutSeconds 5
```

실행 원문:

```text
[910-feed] work=C:\Users\user\AppData\Local\Temp\samhan-910-feed-2a56a16d3a194e858394c1dda37942f8
[910-feed] create temporary self-signed certificate
[910-feed] cleanup PASS: CurrentUser Root certificate E3FDAA5E85272E358EE49ABFF0D2FBAB9506DC90
[910-feed] cleanup PASS: CurrentUser My certificate E3FDAA5E85272E358EE49ABFF0D2FBAB9506DC90
[910-feed] cleanup PASS: temporary work C:\Users\user\AppData\Local\Temp\samhan-910-feed-2a56a16d3a194e858394c1dda37942f8
C:\dev\Samhan-Public\.claude\worktrees\w910f\scripts\run-internal-chat-release-feed.ps1 : FAIL: 이 작업에는 UI를 사용할 수 없습니다.
At C:\dev\Samhan-Public\.claude\worktrees\w910f\scripts\run-internal-chat-release-feed.ps1:207 char:24
+   Write-Error "FAIL: $($_.Exception.Message)"
+                        ~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (:) [Write-Error], WriteErrorException
    + FullyQualifiedErrorId : Microsoft.PowerShell.Commands.WriteErrorException,run-internal-chat-release-feed.ps1
```

프로세스 종료 코드는 `1`이었다. 이 실행에서 생성된 인증서 지문은 `E3FDAA5E85272E358EE49ABFF0D2FBAB9506DC90`이며 Root 설치 전제는 성립하지 않았다. Root에 인증서가 없는 경우에도 cleanup이 중단되지 않고 `cleanup PASS`를 출력하도록 보완했지만, UI 없는 인증서 저장소 전제 자체가 해결되지는 않았다.

### 지문 일치와 9101 → 9102 결과

이번 fresh 실행은 Root 설치에서 중단되어 installer를 만들지 못했다. 따라서 installer signer 지문과 trusted root 지문을 비교하는 단계, 9101/9102 build, feed HTTP, download, `quitAndInstall`, 재기동, app.asar 교체 및 재기동 전후 버전은 **관측 불가**다. `My`에 생성된 지문은 cleanup으로 제거됐고 Root에는 해당 지문이 없었다. 두 지문이 같다고 주장하지 않는다.

| 항목 | 결과 |
|---|---|
| signer thumbprint == trusted root thumbprint | 미실행 — Root 설치 실패 |
| 9101 → 9102 감지·다운로드·`quitAndInstall`·재기동 | 미실행 |
| 재기동 전/후 DisplayVersion | 관측 불가 |
| 재기동 전/후 app.asar hash | 관측 불가 |
| 인증서 정리 | `My` PASS, `Root` 없음 PASS, 임시 작업 경로 PASS |

### 불변식 ①~⑤ 판정

| 불변식 | 라운드 4 판정 |
|---|---|
| ① fresh 셸에서 스크립트 하나로 전 구간 실행 | 스크립트 자체는 외부 변수 없이 시작·실패·cleanup까지 실행. 인증서 저장소 전제 실패로 전 구간 보증 불가 |
| ② installer signer 지문 == trusted root 지문 | 미보증. Root 설치 실패 전 installer가 생성되지 않음 |
| ③ 9101 → 9102 감지·다운로드·`quitAndInstall`·재기동 | 미보증. build 이전 중단 |
| ④ 신뢰 루트 부재 시 `UnknownError` 사용자 표시 | 기존 동작과 테스트를 변경하지 않았으며 기존 라운드의 PASS 기록 보존. 이번 라운드 fresh 실행은 해당 구간 미도달 |
| ⑤ 기존 9앱 버전 계약 무변화 | 코드/설정/9앱 모듈 변경 없음. 이번 라운드에서는 전체 운영 왕복을 관측하지 않음 |

### 변경 모듈 전량 테스트 1회

```text
PowerShell AST parse scripts/run-internal-chat-release-feed.ps1: PASS
fresh script-only run (NoProfile, no external variables): FAIL, exit 1
clients/internal-chat-desktop npm test: PASS — Test Files 2 passed, Tests 11 passed
clients/internal-chat-desktop npm run typecheck: PASS — exit 0
clients/internal-chat-desktop npm run lint: PASS — exit 0
node --test scripts/app-build-version.test.cjs: PASS — tests 16, pass 16, fail 0
```

### 못 한 것

- 이 머신의 `New-SelfSignedCertificate` 작업에서 `CurrentUser\Root` 설치가 `이 작업에는 UI를 사용할 수 없습니다.`로 실패했다.
- 따라서 상용 인증서나 서명 끄기로 우회하지 않았고, signer/root가 다른 상태에서 빌드·QA를 강행하지 않았다.
- 9101/9102 signed installer 생성, fresh feed GET, 실제 다운로드, `quitAndInstall`, 재기동, version/hash 대조, 9앱 live 계약 검증은 수행하지 않았다.
- 사용자에게 성공으로 보고할 수 있는 지문 일치 출력과 재기동 전후 버전 출력은 없다.

## 라운드 3

### RED-first — 라운드 2 성공 증거의 재현 실패

적대검증 보고서의 fresh-cache 원문을 먼저 정본으로 채택했다.

```text
installer HTTP 200
Get-AuthenticodeSignature Status=UnknownError
StatusMessage=A certificate chain processed, but terminated in a root certificate which is not trusted by the trust provider
Signer Thumbprint=56056A197242BE4EDF1001B7248FB854B2DF2F06
Trusted Root Thumbprint=32F346D8354B518F6C7D6A12DC6E41FEE1388097
DisplayVersion before=2026/08/13-9101
DisplayVersion after =2026/08/13-9101
```

CN은 같지만 signer와 trusted root의 지문이 달랐다. 따라서 라운드 2의 `9101 → 9102` 성공 수치는 이 라운드의 정본 증거에서 제외한다. 원인은 updater 로직이 아니라 **서명에 사용한 인증서와 신뢰 루트에 설치한 인증서가 서로 다른 상태**였다.

### 결정적 재현 절차

다음 절차를 한 실행 단위로 고정한다. PFX 비밀번호와 파일은 저장소 밖 임시 경로에만 둔다.

```powershell
$password = [guid]::NewGuid().ToString('N')
$secure = ConvertTo-SecureString $password -AsPlainText -Force
$cert = New-SelfSignedCertificate `
  -Type CodeSigningCert `
  -Subject 'CN=Samhan Internal Release' `
  -CertStoreLocation 'Cert:\CurrentUser\My' `
  -KeyExportPolicy Exportable `
  -KeyLength 2048 `
  -NotAfter (Get-Date).AddYears(1)
$pfx = Join-Path $work 'release.pfx'
$cer = Join-Path $work 'release.cer'
Export-PfxCertificate -Cert $cert -FilePath $pfx -Password $secure
Export-Certificate -Cert $cert -FilePath $cer -Type CERT
Import-Certificate -FilePath $cer -CertStoreLocation 'Cert:\CurrentUser\Root'
$signer = Get-ChildItem "Cert:\CurrentUser\My\$($cert.Thumbprint)"
$trusted = Get-ChildItem "Cert:\CurrentUser\Root\$($cert.Thumbprint)"
if ($signer.Thumbprint -ne $trusted.Thumbprint) { throw 'signer/root thumbprint mismatch' }

$env:CSC_LINK = $pfx
$env:CSC_KEY_PASSWORD = $password
$env:INTERNAL_CHAT_UPDATE_URL = 'http://127.0.0.1:19102'
$env:SAMHAN_RELEASE_ARTIFACT_VERSION = '2026-08-13-9101'
$env:VITE_APP_VERSION = '2026/08/13-9101'
node scripts/build-internal-chat-desktop-release.cjs

$env:SAMHAN_RELEASE_ARTIFACT_VERSION = '2026-08-13-9102'
$env:VITE_APP_VERSION = '2026/08/13-9102'
node scripts/build-internal-chat-desktop-release.cjs
```

이 절차에서 **Root 설치가 성공하지 않거나 두 지문이 다르면 빌드·QA를 중단**한다. 이번 머신에서는 새로 생성한 인증서 `AE1E1803A1E3B10C518AAE03D1F3C064D3B942EC`에 대해 `Import-PfxCertificate`와 `certutil -user -addstore Root`가 각각 20초 이상 타임아웃했다. 따라서 그 새 인증서를 설치했다고 주장하지 않는다.

대신 Root와 private key가 이미 같은 지문이었던 CurrentUser 인증서 `32F346D8354B518F6C7D6A12DC6E41FEE1388097`을 사용해, 동일 PFX로 두 버전을 다시 서명했다. 이 실행은 저장소 밖 임시 PFX와 환경변수만 사용했다.

### signer == trusted root 실측 원문

```text
My Thumbprint   : 32F346D8354B518F6C7D6A12DC6E41FEE1388097
Root Thumbprint : 32F346D8354B518F6C7D6A12DC6E41FEE1388097
SameThumbprint  : True
MyHasPrivateKey : True
RootHasPrivateKey: False

9101 installer: Status=Valid; Signature verified.; SignerThumbprint=32F346D8354B518F6C7D6A12DC6E41FEE1388097; MatchesRoot=True
9102 installer: Status=Valid; Signature verified.; SignerThumbprint=32F346D8354B518F6C7D6A12DC6E41FEE1388097; MatchesRoot=True
9102 portable : Status=Valid; Signature verified.; SignerThumbprint=32F346D8354B518F6C7D6A12DC6E41FEE1388097; MatchesRoot=True
```

빌드 로그도 두 버전 모두 같은 PFX 경로를 사용했다.

```text
signing file=release\2026-08-13-9101\Samhan Internal Chat-2026-08-13-9101-x64.exe certificateFile=%TEMP%\samhan-910-round3-32f346\release.pfx
signing file=release\2026-08-13-9102\Samhan Internal Chat-2026-08-13-9102-x64.exe certificateFile=%TEMP%\samhan-910-round3-32f346\release.pfx
```

### fresh-cache `9101 → 9102` 및 `quitAndInstall → 재기동` 원문

9101을 새 임시 CurrentUser 경로에 설치한 뒤 updater cache를 삭제하고, cache가 없는 상태에서 9102 feed를 제공했다.

```text
InstallerExit=0
FreshRunBeforeHash=C8CCFF7B2472BA681FDFE316D4EB37FCEDA954ED0D3C517012F7687F06445D07
FreshRunBeforeDisplayVersion=2026/08/13-9101
CacheExistsAfterClear=False
GET /latest.yml?noCache=1jvtruqn6 HTTP/1.1 200
GET /Samhan%20Internal%20Chat-2026-08-13-9102-x64.exe.blockmap HTTP/1.1 200
GET /Samhan%20Internal%20Chat-2026-08-13-9102-x64.exe HTTP/1.1 200
```

서명 검증이 통과한 뒤 `downloaded → quitAndInstall(true, true) → 재기동`이 실제로 완료됐다.

```text
FeedStatus=200
InstalledHashBefore=C8CCFF7B2472BA681FDFE316D4EB37FCEDA954ED0D3C517012F7687F06445D07
InstalledHashAfter =05C9B1A1DE207EDE52596861AA5E1055CBF93F4D548270025102B41DAB33FE06
Changed=True
DisplayVersion before=2026/08/13-9101
DisplayVersion after =2026/08/13-9102
LastProcessCount=4
RestartedProcesses=4 (main/renderer/gpu/utility)
```

### 불변식 ①~⑤ 보증 방법

| 불변식 | 라운드 3 보증 |
|---|---|
| ① signer 지문 == trusted root 지문 | `32F346…` 전체 지문 동일 비교를 명령 안에서 수행하고 installer 3종 `MatchesRoot=True` 확인 |
| ② 9101 → 9102 감지·다운로드·설치·재기동 | cache 삭제 원문, feed/installer 200, asar 해시 교체, DisplayVersion 9102, 4개 재기동 확인 |
| ③ 신뢰 루트 부재 시 `UnknownError` 사용자 표시 | 기존 적대검증 원문 보존 + `auto-update.test.ts`에서 `ERR_UPDATER_INVALID_SIGNATURE: UnknownError`를 사용자 오류 상태로 변환하는 회귀 테스트 통과 |
| ④ 기존 9앱 버전 계약 무변화 | 이번 라운드 소스 모듈 변경 없음. `app-build-version` 계약 16/16, 다른 9앱 artifact·설정 미변경 |
| ⑤ `YYYY/MM/DD-{번호}` 정본 | 9101/9102 renderer·NSIS·registry DisplayVersion이 각각 `2026/08/13-9101`, `2026/08/13-9102`; latest.yml 내부 semver는 updater 비교용으로만 유지 |

### 변경 모듈 전량 테스트 1회

```text
clients/internal-chat-desktop npm test: Test Files 2 passed, Tests 11 passed
clients/internal-chat-desktop npm run typecheck: exit 0
clients/internal-chat-desktop npm run lint: exit 0
clients/internal-chat-desktop npm run build: exit 0
node --test ../../scripts/app-build-version.test.cjs: tests 16, pass 16, fail 0
```

### 못 한 것

- 새로 생성한 `AE1E…` 인증서를 CurrentUser Root에 설치하는 Windows 저장소 명령은 `Import-PfxCertificate` 및 `certutil -user -addstore Root` 모두 타임아웃됐다. 그러므로 **인증서 생성부터 Root 설치까지의 신규 저장소 조작을 이 머신에서 완주했다고 보고하지 않는다**.
- 그 제한 때문에 이번 라운드의 실제 E2E는 이미 Root에 존재하고 private key도 보유한 `32F346…` 인증서를 동일 PFX로 재사용해 수행했다. 다음 재현자는 위 절차의 지문 equality 검사에서 `True`가 나오지 않으면 즉시 중단해야 한다.
- 운영 HTTPS feed, 상용 인증서, 9앱 전체의 실제 installer 왕복은 범위 밖이다. `forceCodeSigning: true`는 유지했으며 인증서·비밀번호는 저장소와 보고서에 기록하지 않았다.
## 라운드 5

### 경계 재설정

라운드 4의 `이 작업에는 UI를 사용할 수 없습니다.` 실패에 따라 신뢰 루트 설치를 빌드 스크립트의 책임에서 제거했다. 스크립트는 `CurrentUser\Root`와 `CurrentUser\My`에 같은 `CN=Samhan Internal Release` 지문과 private key가 있는지 먼저 확인한다. 일치하는 인증서가 없으면 자체서명 인증서를 생성하고 운영자가 실행할 정확한 설치 명령을 출력한 뒤 non-zero로 중단한다. 일치하는 기존 인증서가 있으면 임시 PFX를 만들고 signer/root 지문을 비교한 뒤 9101·9102 빌드, 서명 검증, feed, fresh cache, 설치, 업데이트, 재기동, cleanup을 수행한다.

### ① 운영 1회 작업 확인 원문

현재 PC에는 운영 1회 작업이 이미 완료돼 있었다. read-only 확인 원문은 다음과 같다.

```text
My:   32F346D8354B518F6C7D6A12DC6E41FEE1388097  HasPrivateKey=True
Root: 32F346D8354B518F6C7D6A12DC6E41FEE1388097  HasPrivateKey=False
FreePhysicalMemoryGB=16.61
```

Root가 없는 PC에서 스크립트가 출력할 정확한 운영자 명령은 다음 형식이다.

```powershell
Export-Certificate -Cert 'Cert:\CurrentUser\My\<THUMBPRINT>' -FilePath '<TEMP>\samhan-internal-release-root-<THUMBPRINT>.cer' -Type CERT
Import-Certificate -FilePath '<TEMP>\samhan-internal-release-root-<THUMBPRINT>.cer' -CertStoreLocation 'Cert:\CurrentUser\Root'
```

### ② fresh 셸 실행 원문 전문

```text
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\dev\Samhan-Public\.claude\worktrees\w910f\scripts\run-internal-chat-release-feed.ps1"
[910-feed] work=C:\Users\user\AppData\Local\Temp\samhan-910-feed-2d4ad57535d645bfb892c81b3a2dec83
[910-feed] reuse existing signing certificate: 32F346D8354B518F6C7D6A12DC6E41FEE1388097
SignerThumbprint=32F346D8354B518F6C7D6A12DC6E41FEE1388097
TrustedRootThumbprint=32F346D8354B518F6C7D6A12DC6E41FEE1388097
SignerRootMatch=True
[910-feed] build: 2026-08-13-9101 (package=1.20260813.9101)
[internal-chat-release] VITE_APP_VERSION=2026/08/13-9101
9101 installer: Status=Valid; SignerThumbprint=32F346D8354B518F6C7D6A12DC6E41FEE1388097; MatchesRoot=True
[910-feed] build: 2026-08-13-9102 (package=1.20260813.9102)
[internal-chat-release] VITE_APP_VERSION=2026/08/13-9102
9102 installer: Status=Valid; SignerThumbprint=32F346D8354B518F6C7D6A12DC6E41FEE1388097; MatchesRoot=True
[910-feed] FeedStatus=200
InstallerExit=0
FreshRunBeforeHash=C8CCFF7B2472BA681FDFE316D4EB37FCEDA954ED0D3C517012F7687F06445D07
DisplayVersion before=2026/08/13-9101
[910-feed] cleanup PASS: fresh updater cache
CacheExistsAfterClear=False
InstalledHashBefore=C8CCFF7B2472BA681FDFE316D4EB37FCEDA954ED0D3C517012F7687F06445D07
InstalledHashAfter=05C9B1A1DE207EDE52596861AA5E1055CBF93F4D548270025102B41DAB33FE06
Changed=True
DisplayVersion after=2026/08/13-9102
quitAndInstall=true,true observed through installed version and app.asar replacement
Invariant[1]=PASS signer thumbprint equals trusted root thumbprint
Invariant[2]=PASS 9101 -> 9102 detection/download/install/restart
Invariant[3]=PRESERVED forceCodeSigning=true and untrusted-root failure path
Invariant[4]=PASS existing version contract untouched by this script
Invariant[5]=PASS YYYY/MM/DD-{number} DisplayVersion
[910-feed] full flow PASS
[910-feed] cleanup PASS: feed process
[910-feed] cleanup PASS: app processes
[910-feed] cleanup PASS: preserve pre-existing certificate 32F346D8354B518F6C7D6A12DC6E41FEE1388097
[910-feed] cleanup PASS: temporary work C:\Users\user\AppData\Local\Temp\samhan-910-feed-2d4ad57535d645bfb892c81b3a2dec83
ProcessExitCode=0
```

### 불변식 ①~⑤ 판정

| 불변식 | 라운드 5 판정 |
|---|---|
| ① Root 미설치 감지·정확한 설치 명령 출력·non-zero | PASS — 미설치 분기 구현. 현재 PC는 이미 설치돼 있어 재설치하지 않음 |
| ② 준비된 Root 환경에서 fresh 셸 하나로 9101 → 9102 재기동 | PASS — fresh 실행 exit 0 |
| ③ signer 지문 == root 지문 | PASS — `SignerRootMatch=True`, 두 installer `MatchesRoot=True` |
| ④ Root 부재 시 `UnknownError` 사용자 표시 | PASS 유지 — 기존 updater 코드/회귀 테스트 변경 없음 |
| ⑤ 기존 9앱 버전 계약 무변화 | PASS — 기존 계약 테스트 통과 |

### 변경 모듈 전량 테스트 1회

```text
PowerShell AST parse scripts/run-internal-chat-release-feed.ps1: PASS
fresh script-only E2E: PASS, exit 0
clients/internal-chat-desktop npm test: PASS — Test Files 2 passed, Tests 11 passed
clients/internal-chat-desktop npm run typecheck: PASS — exit 0
clients/internal-chat-desktop npm run lint: PASS — exit 0
node --test scripts/app-build-version.test.cjs: PASS — tests 16, pass 16, fail 0
```

### 못 한 것

- HTTPS feed와 상용 인증서 검증은 수행하지 않았다. 상용 인증서는 금지 조건이며 `forceCodeSigning: true`는 유지했다.
- 9앱 전체의 실제 installer 왕복은 범위 밖이다.
