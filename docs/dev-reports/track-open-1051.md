연관 Issue: #1051

## 트랙 개설 — 착수 전 확인

2026-08-01 등록. PR #1082(정찰)이 머지됐고 **본 fix 가 남아 있습니다.**

### 🚨 오늘 밤 같은 계열이 또 나왔습니다
`#1113` 진단:
```
재고 201행이 참조하는 product UUID 101개 중
  활성 카탈로그에 있는 것      1개
  is_deleted=true 로만 있는 것 100개  (id·model_name 정확히 일치)
활성 카탈로그(3,083행)는 모델명 세트 자체가 다름
```
**이 이슈(#1051)의 전표↔품목과 같은 뿌리**일 가능성이 큽니다.

### 정찰이 실측할 것
1. PR #1082 정찰이 남긴 수치를 **현행 DB 에서 다시** 세십시오 (2주 지났습니다)
2. 🚨 끊긴 참조가 **삭제 때문인지 / 세대 교체 때문인지 / 애초에 잘못된 UUID 인지** 가르십시오. `#1113` 에서 이 셋을 가르는 것이 결정적이었습니다
3. `created_at`·`created_by` 로 **언제 어느 경로가 만든 행인지** 판정. QA 잔재와 실 데이터를 섞지 마십시오
4. 조인 키 채움률 — `product_id` 와 코드 컬럼 각각

### 🚫 하지 말 것
DB 직접 수정 · 끊긴 행 삭제. 원인을 확정해 선택지를 올리면 제가 설계합니다.