# #1039 R6 — 재수렴 결함 3건 fix

## 확인 1 — 신규 OUTBOUND 생성 경로 전수 확인 (결함 ①)

기준점: `bf8655c20 [FIX] #1039 CI red 2건 — 출처 없는 창고 조회 500 · Playwright 계약`.

확인 명령:

```powershell
rg -n "Slip\.createOutbound\(|setSourceWarehouseCode" services/slip-service/src/main/java
```

출력 원문:

```text
services/slip-service/src/main/java/com/samhanair/logis/slip/publish/SlipPublishService.java:146: Slip slip = Slip.createOutbound(...
services/slip-service/src/main/java/com/samhanair/logis/slip/publish/SlipPublishService.java:150: slip.setSourceWarehouseCode(req.warehouseCode());
services/slip-service/src/main/java/com/samhanair/logis/slip/publish/SlipPublishService.java:226: Slip slip = Slip.createOutbound(...
services/slip-service/src/main/java/com/samhanair/logis/slip/publish/SlipPublishService.java:230: slip.setSourceWarehouseCode(req.warehouseCode());
services/slip-service/src/main/java/com/samhanair/logis/slip/publish/SlipPublishService.java:327: Slip slip = Slip.createOutbound(...
services/slip-service/src/main/java/com/samhanair/logis/slip/publish/SlipPublishService.java:329: slip.setSourceWarehouseCode(req.warehouseCode());
services/slip-service/src/main/java/com/samhanair/logis/slip/estimate/service/EstimateToSlipConverter.java:63: Slip slip = Slip.createOutbound(...
services/slip-service/src/main/java/com/samhanair/logis/slip/mobile/service/MobilePartnerOrderService.java:114: Slip slip = Slip.createOutbound(...
services/slip-service/src/main/java/com/samhanair/logis/slip/service/SlipDuplicateService.java:94: copy = Slip.createOutbound(...
services/slip-service/src/main/java/com/samhanair/logis/slip/service/SlipService.java:251: slip = Slip.createOutbound(...
```

| 실제 생성 경로 | 파일:줄 | 현재 source warehouse code | 판정 |
|---|---|---|---|
| 일반 작성 `POST /slips` | `SlipService.java:231-257` | 저장하지 않음; 요청 `CreateSlipRequest.java:49-56`도 UUID만 받음 | 결함 |
| 모바일 주문 | `MobilePartnerOrderService.java:107-130`; `MobilePartnerOrderRequest.java:25-34` | 저장하지 않음; UUID만 받음 | 결함 |
| 견적 자동 변환 | `EstimateToSlipConverter.java:52-70,114` | placeholder UUID만 있고 code 없음 | 출처 없음은 UNKNOWN 유지, 실제 창고 확정 시 별도 갱신 필요 |
| 전표 복사 | `SlipDuplicateService.java:80-99` | source warehouse ID만 복사, code 복사 없음 | 결함 |
| 견적/주문/주문병합 내부 발행 | `SlipPublishService.java:121-150`, `:201-230`, `:304-329` | `req.warehouseCode()` 저장 | 이미 충족 |
| seed | `SlipSeeder.java:342` | 사용자 생성 경로 아님 | 이번 결함 경로에서 제외 |

업무 구분의 권위는 표시명이 아니라 `warehouseCode` 원천이며, `WarehouseInternalClient`가 UUID 조회 응답의 `code`를 읽을 수 있는 내부 API 계약은 `inventory-service/src/main/java/com/samhanair/logis/inventory/web/InternalWarehouseController.java:86-105`에 있다. 따라서 일반/모바일은 선택된 UUID를 inventory의 code로 확인해 저장하고, 복사는 원본 code를 그대로 승계한다. 견적 placeholder는 실제 원천이 아니므로 추측 code를 넣지 않는다.

## 확인 2 — 결함 ① RED (실 API 일반 작성 경로)

`PublicSlipControllerIT.createOutbound_withWarehouseCode_preservesSourceWarehouseCode`를 추가하여 `POST /slips`로 실제 전표를 만든 뒤 저장된 원천 code를 검증했다. 테스트는 기존 API가 요청의 code를 계약으로 받지/저장하지 않는 상태에서 먼저 실행했다.

```text
실행: .\gradlew.bat :services:slip-service:test --tests "com.samhanair.logis.slip.delivery.it.PublicSlipControllerIT.createOutbound_withWarehouseCode_preservesSourceWarehouseCode" --no-daemon

PublicSlipControllerIT > createOutbound_withWarehouseCode_preservesSourceWarehouseCode() FAILED
    org.opentest4j.AssertionFailedError at PublicSlipControllerIT.java:151

1 test completed, 1 failed
BUILD FAILED
```

실패 원문:

```text
expected: "00003"
 but was: null
```

이는 API 생성 경로가 source warehouse code를 영속화하지 않는 결함을 직접 재현한 RED이다.

## 확인 3 — 결함 ① 일반·모바일 GREEN

수정 내용:

- `SlipService.java` 일반 `POST /slips` 생성 직후 `WarehouseInternalClient.findWarehouseCode(sourceWarehouseId)`의 권위 code를 snapshot한다.
- `MobilePartnerOrderService.java` 모바일 OUTBOUND 생성 직후 같은 원천 조회를 수행한다.
- 조회 결과가 없으면 code를 만들지 않고 `UNKNOWN`으로 남긴다. 조회 장애는 삼키지 않는다.
- 견적 자동 변환은 placeholder 창고만 알고 실제 업무 code를 모르므로 추측하지 않는다.

```text
실행: .\gradlew.bat :services:slip-service:test --tests "com.samhanair.logis.slip.delivery.it.PublicSlipControllerIT.createOutbound_withWarehouseCode_preservesSourceWarehouseCode" --tests "com.samhanair.logis.slip.mobile.service.MobilePartnerOrderServiceTest" --no-daemon

> Task :services:slip-service:test

BUILD SUCCESSFUL in 50s
18 actionable tasks: 3 executed, 15 up-to-date
```

## 확인 4 — 결함 ② Samhan Public 화면의 세 상태 충돌

재수렴 보고서의 생산 코드 확인을 다시 대조했다.

- `clients/desktop/src/renderer/routes/ArologisPreClassifyPage.tsx:345-353`은 error 문구와 빈 결과 문구만 렌더한다.
- `clients/desktop/src/renderer/api/arologisDispatchApi.ts:62-66`의 `PreClassifyResponse`에는 `unknownWarehouseCount`가 없다.
- 독립 아로로지스 화면은 이미 `clients/arologis-desktop/src/renderer/routes/dispatches/PreClassifyPage.tsx:493-507` 및 `clients/arologis-desktop/src/renderer/api/arologisDispatch.ts:67-72`에서 조회 실패·UNKNOWN·정상 0건을 분리한다.

따라서 수정 대상은 Samhan Public 화면과 그 응답 타입이며, 정상 0건 문구는 유지하고 UNKNOWN 안내만 추가해야 한다.

## 확인 5 — 결함 ② RED

Samhan Public 응답 타입과 화면에 UNKNOWN 상태 계약을 요구하는 표적 Playwright 테스트를 먼저 추가해 실행했다.

```text
실행 위치: clients/desktop
실행: npx playwright test playwright/sp-08-3-2-arologis-history/sp-08-3-2-arologis-history.spec.ts --config=playwright.config.ts --grep "Samhan Public pre-classify"

Running 1 test using 1 worker
Error: expect(received).toContain(expected)
Expected substring: "unknownWarehouseCount"
at sp-08-3-2-arologis-history.spec.ts:122:17
1 failed
```

RED 원인은 API 타입과 Samhan Public 화면 모두 `unknownWarehouseCount`를 소비하지 않는 실제 계약 누락이다.

## 확인 7 — 결함 ③ RED (inventory 단건 404)

기존 `WarehouseInternalClientTest`의 404 계약을 “부분 조회에서 해당 창고만 없음”으로 명시해 먼저 실행했다.

```text
실행: .\gradlew.bat :services:slip-service:test --tests "com.samhanair.logis.slip.client.WarehouseInternalClientTest.창고명_조회_404는_해당창고없음으로부분응답한다" --no-daemon

WarehouseInternalClientTest > 창고명_조회_404는_해당창고없음으로부분응답한다() FAILED
    java.lang.IllegalStateException at WarehouseInternalClientTest.java:59
        Caused by: org.springframework.web.client.HttpClientErrorException$NotFound at WarehouseInternalClientTest.java:59

1 test completed, 1 failed
BUILD FAILED
```

현재 client는 404도 전체 조회 장애 예외로 변환한다. 404만 `Optional.empty()`로 축약하고 timeout/403/5xx 및 응답 계약 오류는 계속 예외로 전파해야 전체 장애 구분을 보존한다.

## 확인 8 — 결함 ③ GREEN

`WarehouseInternalClient.findWarehouseName`에서 HTTP 404만 `Optional.empty()`로 반환하도록 수정했다. 403/5xx/timeout/응답 파싱 실패는 기존 `IllegalStateException` 전파를 유지했다. 컨트롤러의 `orElse(null)`와 결합하면 해당 창고 한 건은 표시명 없이 계속 응답되고, 다른 행은 처리된다.

```text
실행: .\gradlew.bat :services:slip-service:test --tests "com.samhanair.logis.slip.client.WarehouseInternalClientTest" --no-daemon

> Task :services:slip-service:test

BUILD SUCCESSFUL in 17s
18 actionable tasks: 2 executed, 16 up-to-date
```

## 확인 6 — 결함 ② GREEN

수정:

- `clients/desktop/src/renderer/api/arologisDispatchApi.ts`의 `PreClassifyResponse`에 `unknownWarehouseCount`를 추가했다.
- `clients/desktop/src/renderer/routes/ArologisPreClassifyPage.tsx`에 UNKNOWN 안내를 추가했다.
- 정상 0건 문구는 `unknownWarehouseCount === 0`일 때만 렌더하도록 조건을 좁혔다.
- 조회 실패 문구는 기존 `query.isError` 경로를 유지했다.

```text
실행 위치: clients/desktop
실행: npx playwright test playwright/sp-08-3-2-arologis-history/sp-08-3-2-arologis-history.spec.ts --config=playwright.config.ts --grep "Samhan Public pre-classify"

Running 1 test using 1 worker
[1/1] ... Samhan Public pre-classify는 UNKNOWN과 정상 0건을 구분한다
1 passed (3.4s)
```

## 확인 9 — 결함 ① 회귀 표면 GREEN 및 복사 경로 보강

추가 보강: `SlipDuplicateService.java:94`에서 OUTBOUND 복사본이 원본의 `sourceWarehouseCode`를 그대로 승계하도록 했다. 기존 원본이 UNKNOWN이면 그대로 null이며, 추측 backfill은 하지 않는다. 견적 변환의 placeholder도 원천 code가 없으므로 null 유지가 의도된 정책이다.

```text
실행: .\gradlew.bat :services:slip-service:test --tests "com.samhanair.logis.slip.service.SlipServiceTest" --tests "com.samhanair.logis.slip.mobile.service.MobilePartnerOrderServiceTest" --tests "com.samhanair.logis.slip.delivery.it.PublicSlipControllerIT.createOutbound_withWarehouseCode_preservesSourceWarehouseCode" --no-daemon

> Task :services:slip-service:test

BUILD SUCCESSFUL in 42s
18 actionable tasks: 2 executed, 16 up-to-date
```

## 확인 10 — fix 후 모드별 산출 건수 재측정

공유 DB에는 write/DDL을 수행하지 않았고, V101 미적용 상태의 읽기 전용 집계만 실행했다. 따라서 기존 2,303행은 모두 provenance UNKNOWN으로 계산된다. 신규 전표의 code 보존은 별도 test DB의 실제 `POST /slips` RED→GREEN 테스트에서 확인했다.

```text
실행: docker exec samhan-postgres psql -U samhan -d slip_db -P pager=off -c "SELECT COUNT(*) AS active_outbound, COUNT(*) FILTER (WHERE delivery_tag='STACK') AS stack_total, COUNT(*) FILTER (WHERE delivery_tag='REGION') AS region_total, 0 AS known_source_code, COUNT(*) AS unknown_source_code, COUNT(*) FILTER (WHERE delivery_tag='STACK') AS mode1, COUNT(*) FILTER (WHERE delivery_tag='STACK') AS mode2, COUNT(*) FILTER (WHERE delivery_tag='STACK') AS mode3, 0 AS mode4, 0 AS mode5, COUNT(*) FILTER (WHERE delivery_tag='STACK') AS mode6, COUNT(*) FILTER (WHERE delivery_tag='STACK') AS mode7, COUNT(*) FILTER (WHERE delivery_tag='STACK') AS mode8 FROM slips WHERE slip_type='OUTBOUND' AND is_deleted=false;"

 active_outbound | stack_total | region_total | known_source_code | unknown_source_code | mode1 | mode2 | mode3 | mode4 | mode5 | mode6 | mode7 | mode8
-----------------+-------------+--------------+-------------------+---------------------+-------+-------+-------+-------+-------+-------+-------+-------
            2303 |          11 |           12 |                 0 |                2303 |    11 |    11 |    11 |     0 |     0 |    11 |    11 |    11
(1 row)

실데이터 V101 이력 확인:
 installed_rank | version | description | success
----------------+---------+-------------+--------
(0 rows)
```

## 확인 11 — 최종 변경 범위 점검

```text
실행: git diff --check; git status --short; git diff --stat

git diff --check: 출력 없음, exit 0
변경 파일: 10개
신규 파일: docs/dev-reports/2026-08-03-1039-r6-fix.md
Flyway 파일 변경: 없음
```

이번 라운드에는 공유 DB write/DDL, Docker image rebuild, slip-service 전체 suite, 기존 2,303행 provenance backfill을 수행하지 않았다.
