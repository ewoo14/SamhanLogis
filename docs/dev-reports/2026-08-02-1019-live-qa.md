# PR #1024 / 이슈 #1019 품목명 재사용 차단 — 라이브 QA 보고서

## 1. 배포본 나이 및 실행 방식

- 확인 시각: 2026-08-02 (KST)
- 실행 중 서비스: `samhan-product-service`
- 이미지: `infrastructure-product-service`
- 컨테이너 생성 시각: `2026-07-31T14:25:25.972348444Z`
- 배포본 나이: 확인 시각 기준 약 1일
- 접근 경로: 공유 스택의 API Gateway `:8080` 경유 대상임을 확인
- 브랜치/HEAD: `fix/1019-product-name-unique` / `df281ad01`
- 소스 확인: `origin/main`에도 ProductService 경로가 존재하며, 현재 HEAD의 변경은 활성 품목명 충돌 검사·수정 시 자기 자신 제외·재활성화 검사이다.
- GUI 실행: 브라우저 스킬의 in-app Browser 및 기본 브라우저 연결을 시도했으나 이 세션에서 사용 가능한 브라우저가 없어 중단했다.
- 정적 게이트/API JSON/터미널 출력은 GUI 라이브 QA의 대체 증거로 사용하지 않았다.

## 2. 실 DB 검증 전후 행 수

GUI 조작 및 쓰기 시나리오를 실행하지 못했으므로 데이터 변경은 없었다.

| 항목 | 전 | 후 |
|---|---:|---:|
| `products` 전체(soft-delete 제외) | 1,220 | 1,220 |
| `ACTIVE` | 1,216 | 1,216 |
| `DISCONTINUED` | 4 | 4 |
| 동명 그룹 | 187 | 187 |
| 동명 행 | 700 | 700 |
| `외부 통신 모듈 MIM-N10` 단종 행 | 1 | 1 |

※ 요청서에 적힌 186그룹/696행과 달리, 현재 공유 DB 실측은 187그룹/700행이었다.

## 3. 시나리오별 라이브 QA 결과

각 시나리오의 캡처 파일은 생성하지 못했다. 실제 GUI 브라우저가 연결되지 않아 화면을 조작할 수 없었기 때문이다. 합성 이미지나 API 응답 캡처는 만들지 않았다.

### 시나리오 1 — 활성 품목명 중복 등록 시도

- 캡처: **미생성** (`<슬러그>-real-qa` 캡처 없음)
- 결과: 미실시. 중복 차단 및 오류 메시지의 충돌 모델코드 표시를 GUI에서 확인하지 못했다.

### 시나리오 2 — 기존 동명 품목의 다른 필드만 수정

- 캡처: **미생성**
- 결과: 미실시. 기존 동명 데이터의 정상 저장 여부를 GUI에서 확인하지 못했다. 이 트랙의 급소 시나리오이므로 통과 판정하지 않는다.

### 시나리오 3 — 이름을 바꾸지 않고 저장

- 캡처: **미생성**
- 결과: 미실시. 동일 이름 저장이 차단되지 않는지 GUI에서 확인하지 못했다.

### 시나리오 4 — 단종 품목명으로 새 품목 등록

- 캡처: **미생성**
- 결과: 미실시. `외부 통신 모듈 MIM-N10` 단종 행은 실DB에서 확인했으나, 새 품목 등록 허용 여부는 GUI에서 확인하지 못했다.

### 시나리오 5 — 재활성화 시 ACTIVE 동명 충돌 409

- 캡처: **미생성**
- 결과: 미실시. 재활성화 조작 및 화면의 409 오류 표시를 확인하지 못했다.

## 4. 판정

**BLOCK / 라이브 QA 미완료.**

배포본과 실DB 행 수는 확인했지만, 사용자 요청의 필수 조건인 실제 GUI 조작과 단계별 실제 캡처를 수행할 수 없었다. 따라서 PR #1024의 동작을 통과로 판정하지 않는다.

## 5. 하지 못한 것

- 로그인 화면 및 품목 관리 화면 실제 조작
- 시나리오 1~5의 저장/차단/409 결과 확인
- 단계별 GUI 캡처 생성
- throwaway 품목 생성 및 정리
- standalone JAR 실행: GUI 브라우저가 연결되지 않은 상태에서 실행해도 필수 화면 증거를 만들 수 없어 실행하지 않았다.

## 6. 새로 만든 파일

- `docs/dev-reports/2026-08-02-1019-live-qa.md`
- 캡처 파일: 없음 (실제 캡처를 만들 수 없는 환경이므로 합성·fixture 파일을 만들지 않음)
