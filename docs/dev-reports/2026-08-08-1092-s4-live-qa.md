# #1092 S4 라이브QA — PR #1121 슬라이스 1

## 1. 환경 확인

1. **프런트 출처**: `C:\dev\Samhan-Public\.claude\worktrees\t1092\clients\desktop`에서 renderer 전용 Vite를 실행했다. 검증 HEAD는 `efb5ca4998fb9310077898fc8160a6334d116e3f`였다. 실행 프로세스 명령행도 `C:\dev\Samhan-Public\.claude\worktrees\t1092\clients\desktop\node_modules\...\vite.js`를 가리키는 것을 확인했다.
2. **빈 포트와 strictPort**: 시작 전 `5196`에 LISTENING 소켓이 0개인 것을 확인한 뒤 `vite ... --port 5196 --strictPort`로 실행했다. Vite 로그의 실제 주소는 `http://127.0.0.1:5196/`였다.
3. **실제 호출 API 확인**: Playwright의 `response` 이벤트로 화면 요청을 기록했다. 기본 목록은 `GET http://127.0.0.1:8080/slips/estimates?page=0&size=50` 200, 통합 ON은 `GET .../slips/estimates?page=0&size=10000` 200과 `GET .../api/v1/partner-orders?page=0&size=10000` 200을 실제 호출했다. 앱은 `VITE_API_BASE_URL=http://127.0.0.1:8080`, mock OFF로 실행했다.
4. **백엔드 출처**: Docker 라벨 `com.docker.compose.project.working_dir`와 compose 파일 경로가 `C:\dev\Samhan-Public\.claude\worktrees\t1096\infrastructure`인 것을 확인했다. 본 QA의 주 검증 시간에는 gateway `:8080`, slip-service `:18086`, partner-order-service `:18088`가 healthy였다. 이 스택은 중지·재시작하지 않았다. 보강 검증 시각인 02:57경 외부에서 스택이 교체되어 gateway가 slip/partner-order 미등록 503을 반환한 사실은 B9에 별도 기록한다.
5. **환경 파일**: 이 워크트리의 `infrastructure/.env.local` 존재를 `True`로 확인했다. 보고서에는 비밀번호를 기록하지 않았다.

캡처는 저장소에 설치된 Playwright Chromium을 `headless: true`, 1600×1000 viewport로 실행하여 실제 DOM과 실제 서버 응답으로 만들었다. 합성 이미지나 목록 fixture는 사용하지 않았다.

## 2. 종합 판정

**BLOCK — 통합 목록의 사용자 계약 결함 2건**

1. 필수 열인 **작성자**가 통합 목록에 없다. 실제 헤더는 `구분 · 문서번호 · 거래처 코드 · 거래처 · 작성일 · 합계 · 상태`였다.
2. 값이 없는 필드를 빈칸으로 두지 않고 `—`로 채운 셀이 53개였다. 구성은 종합견적서 거래처 코드 45개, 주문서 거래처명 4개, 주문서 작성일 4개다.

브리프의 고정 표본 `43 / 4`와 달리 주 검증 직전 실 API는 `estimates 45 / partner_orders 4`를 반환했다. 화면은 현재 API 값 `45 / 4`를 누락 없이 표시했으나, 브리프 기준 견적 표본은 2건 불일치한다. 제품 행 누락과 데이터 환경 변동을 구분하기 위해 둘을 별도로 판정했다.

## 3. A — 통합 조회

### A-1. 두 계열 및 행 수

- **발화 조건**: 실 API `estimates 45건`, `partner_orders 4건`(모두 0 초과).
- **관찰**: 통합 ON 후 49행, 종합견적서 45행, 주문서 4행.
- **판정**: 화면/API 일치 **PASS**. 브리프의 `43 / 4` 고정 표본과는 견적 2건 불일치하여 **환경 표본 FAIL**.
- **증거**: `02-unified-list-49-rows.png`.

### A-2. 계열 구분

- **발화 조건**: 종합견적서 45건, 주문서 4건.
- **관찰**: 첫 열에 `종합견적서` 또는 `주문서`가 모든 행에 표시됐다.
- **판정**: **PASS**.

### A-3. 열·용어·UUID·빈값

- **발화 조건**: 통합 행 49건, 화면 텍스트 전체.
- **관찰**:
  - `작성자` 열이 없고 `거래처 코드` 열이 추가돼 있다.
  - 화면 텍스트에 `담당`은 0건이었다.
  - 화면 텍스트의 UUID 정규식 일치는 0건이었다. 상세 화면에서도 사용자 가시 UUID는 0건이었다.
  - `—` 셀 53건, 정확한 `0` 셀은 0건이었다. 금액 `₩0`은 실제 금액 데이터이므로 빈값 대체 건수에는 포함하지 않았다.
- **판정**: 작성자 열 **FAIL**, 빈값 계약 **FAIL**, `담당` 금지 **PASS**, UUID 비노출 **PASS**.

### A-4. 작성일 교차 정렬

- **발화 조건**: 작성일이 있는 종합견적서 45건, 작성일이 있는 주문서 **0건**.
- **관찰**: 주문서 4건 모두 작성일이 `—`로 표시되어 마지막 4행에 모였다. 계열 전환은 1회였다.
- **판정**: **판정 불가**. 주문서 측 비교 가능한 작성일 표본이 0이므로 교차 정렬 PASS로 쓰지 않는다.

### A-5. 페이징/전량 보존

- **발화 조건**: 합산 49건.
- **관찰**: 통합 목록은 별도 페이지 버튼 없이 49행 전부를 한 DOM에 표시했다. 두 계열의 현재 API 행 수가 모두 보존됐다.
- **판정**: 현재 표본에서 **PASS**.

## 4. B — 기존 기능 회귀

### B 발화 조건 집계

실 API `includeDeleted=true&size=10000` 기준 전체 견적 2,038건, 활성 45건, 삭제 1,993건이었다. 삭제 중 복원 가능 4건, 복원 불가 1,989건이며, `size=50` 기준 41페이지였다. 활성 상세 클릭 표본 45건, 신규 작성 버튼 1개, 기간 `2026-08-07` 표본 13건, 거래처명 `부산냉난방테크` 표본 13건이었다.

| 항목 | 실제 클릭/관찰 | 판정 | 증거 |
|---|---|---|---|
| 복원 버튼 전파 차단·실패 배너 | 삭제 포함 2페이지의 복원 버튼을 클릭하고 해당 POST만 409로 실패 주입했다. URL은 바뀌지 않았고 `복원할 수 없는 견적서입니다.`가 danger 색상 `rgb(153, 27, 27)`로 표시됐다. DB 변경은 발생하지 않았다. | PASS | `04-restore-failure-danger-banner.png` |
| 복원 불가 버튼 미노출 | 전체 1,989건. 2페이지 삭제 50행 중 복원 가능 4행에만 버튼이 있고 나머지 46행에는 없었다. | PASS | `04-restore-failure-danger-banner.png` |
| 삭제 배지·취소선·행 클릭 차단 | 1페이지 삭제행 5건 모두 `삭제됨` 배지와 `line-through`. 삭제행 클릭 전후 URL 동일. | PASS | `03-deleted-toggle-badge-strikethrough.png` |
| 삭제행 토글 | OFF→ON에서 1/41, 50행이 표시되고 ON→OFF에서 활성 45행으로 돌아왔다. | PASS | `03-deleted-toggle-badge-strikethrough.png`, `05-deleted-off-back-to-first-active-page.png` |
| 페이지 이동 후 토글 OFF 복귀 | 삭제 포함 2/41에서 OFF 클릭 후 페이지네이션이 사라지고 활성 45행 첫 목록으로 복귀했다. | PASS | `05-deleted-off-back-to-first-active-page.png` |
| 기간 시작·종료 | 시작 `2026-08-07` 요청 200/13행, 종료까지 같은 날짜 요청 200/13행. 요청 URL에 `startDate`, `endDate`가 각각 포함됐다. | PASS | `06-date-start-end-filters.png` |
| 거래처명 | `부산냉난방테크` 입력 후 13/13행이 모두 일치했다. | PASS | `07-partner-name-filter.png` |
| 신규 견적 | 버튼 실제 클릭 후 `#/sales/estimates/new`로 이동했다. | PASS | `08-new-estimate-route.png` |
| 활성행 상세 | 활성행 실제 클릭 후 `#/sales/estimates/<내부 식별자>` 상세로 이동했고, 상세 화면 가시 텍스트에 UUID는 없었다. | PASS | `09-active-row-detail-route.png` |

### B-9. 통합 토글 ON 상태

- **발화 조건**: 삭제 1,993건, 복원 가능 4건, 기간 표본 13건, 거래처 표본 13건, 활성 상세 45건, 신규 버튼 1개.
- **실제 클릭 PASS**: 삭제 토글, 2페이지 이동, 복원 실패/전파 차단, 토글 OFF 후 활성 첫 목록 복귀, 거래처명 필터(기존/통합 각각 13행), 신규 이동, 활성행 상세 이동.
- **비활성 정책 여부**: 기간 시작·종료, 거래처명, 삭제 포함, 신규 버튼은 모두 enabled였다. 통합 모드에서 의도적으로 비활성화된 기존 기능은 없었다.
- **기간 시작·종료 보강 결과**: 통합 ON 상태에서 실제 입력까지 했으나, 외부 스택 교체 시점과 겹쳐 slip/partner-order 요청 6개가 모두 503이었다. 발화 가능한 응답 행이 0이므로 이 부분만 **판정 불가**이며 PASS로 쓰지 않는다. 일반 목록 OFF 상태의 동일 입력은 위 B-6에서 200/13행으로 검증됐다.
- **증거**: `10-unified-on-restore-error.png`, `11-unified-on-filter-controls.png`, `11b-unified-on-date-filters.png`(503 보강 시점).

## 5. C — 한쪽 API 실패

- **발화 조건**: 실패 대상 주문서 4건, 유지 대상 견적 45건.
- **방법**: 통합 목록의 주문서 GET만 Playwright에서 503으로 실패 주입했다. 견적 GET은 실제 gateway로 보내 200 응답을 받았다.
- **관찰**: 통합 목록 45행 전부가 종합견적서로 남았고 주문서 행은 0건이었다. `주문서 목록을 불러오지 못했습니다. 가능한 데이터만 표시합니다.` 오류 배너가 화면에 보였다.
- **판정**: **PASS**. 조용한 빈 목록이 아니며 정상 계열이 보존됐다.
- **네트워크 증거**: 주문서 `size=10000` 503, 견적 `size=10000` 200.
- **캡처**: `12-partner-orders-503-estimates-remain.png`.

## 6. 신규 파일 목록

신규 보고서는 `docs/dev-reports/2026-08-08-1092-s4-live-qa.md`다. 그 밖의 신규 파일은 모두 `docs/qa-shots/1092-s4-live-qa/` 아래의 실제 Playwright headless 캡처다.

1. `01-baseline-estimates.png`
2. `02-unified-list-49-rows.png`
3. `03-deleted-toggle-badge-strikethrough.png`
4. `04-restore-failure-danger-banner.png`
5. `05-deleted-off-back-to-first-active-page.png`
6. `06-date-start-end-filters.png`
7. `07-partner-name-filter.png`
8. `08-new-estimate-route.png`
9. `09-active-row-detail-route.png`
10. `10-unified-on-restore-error.png`
11. `11-unified-on-filter-controls.png`
12. `11b-unified-on-date-filters.png`
13. `12-partner-orders-503-estimates-remain.png`

## 7. 프로세스·데이터 안전

- DB INSERT/UPDATE/DELETE를 직접 실행하지 않았다.
- 복원 실패는 브라우저 네트워크에서 해당 POST만 409로 응답해 실제 DB를 변경하지 않았다.
- t1096 Docker 스택을 중지·재시작하지 않았다.
- 종료 후 본 라운드의 Vite `5196` LISTENING 0, 해당 Vite node 0, headless Chrome 0, headless-shell 0을 확인했다.
- 커밋·push·제품 코드 수정은 하지 않았다.

## 8. 이 라운드가 보지 않은 것

- 주문서 작성일이 있는 실데이터가 0건이라 두 계열 간 작성일 정렬의 정상/역전 사례를 판정하지 못했다.
- 통합 ON 상태의 기간 시작·종료 성공 응답은 보강 시 외부 스택 교체로 503이어서 판정하지 못했다.
- 실제 복원 성공으로 DB 상태가 바뀌는 경로는 실행하지 않았다. 버튼 노출, 전파 차단, 한국어 실패 배너만 검증했다.
- 반대 방향인 견적 API 실패 시 주문서 4건만 남는 경우는 실행하지 않았다.
- 현재 합산 49건을 넘는 대규모 통합 목록의 성능과 10,000건 경계는 보지 않았다.
- Electron 패키징 앱은 보지 않았고, t1092의 renderer 전용 Vite 실화면만 검증했다.
