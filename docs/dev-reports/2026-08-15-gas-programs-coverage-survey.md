# 레거시 GAS 27개 대체 여부 전수조사 (2026-08-15)

## 개발책임자 지시

> *"모든 GAS를 다 넣을 필요는 없어. 지금 우리 시스템이 없을 때 대용으로 만들어놓은게 많아서."*

⟹ **목표는 이식이 아니라 분류.** "이건 아직 필요하다" 를 고를 수 있게 근거를 붙였다.
종합견적서·주문서는 별도 조사 완료 → `2026-08-15-gas-parity-estimate-order.md`

## 집계

| ✅ 대체완료 | ⚠️ 부분대체 | ❌ 미대체 | 🔧 이관도구 | ❓ 판단불가 | 합계 |
|---:|---:|---:|---:|---:|---:|
| 17 | 2 | 3 | 1 | 4 | 27 |

🔑 **✅ 판정은 route · 사이드바 메뉴 · pageCode 를 모두 확인한 것만** 붙였다.
route 만 보고 대체완료로 적지 않았다 — 이 저장소에서 "만들었는데 사용자가 못 닿는" 사례가 최근에만 여섯 건 나왔다.

---

## ❌ 미대체 3건 — 그중 2건은 **결함이 아니라 결정**이다

```text
에어디자이너 전용 주문서 인식   2,340 / 723
제이시스템 전용 주문서 인식     2,877 / 819
  둘 다 PDF·이미지 주문서를 OCR 해 품목·가격·DC 를 계산하고 이카운트 주문으로 전송
  2026-06-29 개발책임자 지시로 발주서 OCR 권한·메뉴·구현이 제거됐다
    V76__remove_ocr_page_permissions.sql
    playwright/menu-relocate/menu-ia-contract.spec.ts:268  ← 회귀 가드까지 있다
  ⟹ 의도적 폐기다. 되살리려면 그 결정을 뒤집는 판단이 필요하다
```

**실제로 대응이 없는 것은 하나다.**

```text
교육안내 자동상태변경   100 / 0
  등록마감 경과 → 신청불가 · 문자파일 존재 → 발송완료 로 상태 자동 변경
  현행 route · service · pageCode 검색 결과 0건
```

## ⚠️ 부분대체 2건 — **화면은 있는데 끝까지 안 간다**

```text
가입고처리   177 / 1,020
  GAS  주문·배달예정 Excel 을 입고전표로 묶어 이카운트에 일괄 전송
  현행  구매관리·입고전표 수기 작성은 있다
  없는 것  Excel 일괄 변환·전송

알리고 자동 업로드   511 / 1,005
  GAS  거래처·외부 주소록을 그룹별 CSV 로 만들어 Drive/Notion 동기화
  현행  /admin/aligo-address-book 에서 CSV 생성·sync UI 제공
  없는 것  실 알리고 전달 — aligoAddressBookApi.ts:86 기준 외부 미전달 mock
  🚩 오늘 SES 메일(#1221 ③)과 같은 패턴이다 — 성공으로 보이지만 안 나간다
```

## 🔧 이관도구 1건

```text
비밀번호 일괄 암호화   100 / 0
  Notion 의 4자리 평문 비밀번호를 일괄 해시로 전환한 1회성 도구
  현행 신규 계정은 저장 시 BCrypt (AuthService.java:175)
  ⟹ 애초에 이식 대상이 아니다
```

## ❓ 판단불가 4건 — **프로그램이 아니라 데이터다**

```text
가배차용 지역별 분류표 · 거래처 DC정보 · 단톡방리스트 · 발송금지리스트
  전부 ZIP → 중첩 ZIP → CSV 2개. Code.js·HTML 없음
  ⟹ 이식 대상이 아니라 "그 데이터가 시스템에 적재됐는가" 를 볼 대상이다
```

🚩 오늘 일마감에서 **DC조건 0/13 · 출고가 1/13** 이 나왔고 개발책임자가 *"원천은 맞는데 데이터가 안 들어왔다"* 로 확정했다. `거래처 DC정보` CSV 가 그 데이터일 가능성이 있다 — **확인 대상**.

---

## ✅ 대체완료 17건

| GAS | 현행 |
|---|---|
| DPS 입고기록 비교 | `/warehouse/dps-compare` |
| 품목별 DPS 입고내역 비교 | `/warehouse/dps-compare/by-product` |
| 가배차분류리스트 | `/arologis/pre-classify` (8개 실행모드·권역/지방 탭) |
| 지방가배차분류리스트 | `/arologis/pre-classify` 의 지방가배차(시도) 탭 |
| 미배차리스트 | `/arologis/unassigned` |
| 배차안내문자 | `/arologis/dispatch-sms` |
| 운송사-실배차내역 비교 | `/arologis/dispatch-reconcile` |
| 거래처 업데이트 프로그램 | `/admin/partners` + 이카운트 XLSX 적재 API |
| 거래처별 원장생성 프로그램 | `/accounting/partner-ledger` |
| 거래처별 일괄 거래명세서 생성 | `/accounting/statement-batch` |
| 계산서일괄등록양식 생성 | `/accounting/hometax-export` |
| 영업수수료 계산 | `/accounting/sales-commission-settlements` |
| 일마감 프로그램 | `/accounting/daily-closing` |
| 내일자 전표 이미지 생성 | `/sales/next-day-slip` |
| 전표정리리스트 | `/sales/slip-cleanup` |
| 입출고 내역 | `/inventory/inout-analysis` |
| 입출고 분석 | `/inventory/inout-analysis` (같은 화면에 통합) |

🔑 배차 계열 5개가 전부 아로로지스로 갔다. 회계 계열 5개는 `/accounting/**` 로 갔다.

---

## 2026-08-15 개발책임자 확정 — 조사 후 결정

### 🚨 파리티 기준 (모든 GAS 대체 작업에 적용)

> *"레거시와 결과 표시 형식까지도 똑같이 구현되어야해."*

```text
기능이 있다        ≠ 파리티
사용자가 닿는다     ≠ 파리티
✅ 파리티 = 기능 + 결과 표시 형식이 레거시와 같다
```

🚩 **이 문서의 `✅ 대체완료` 17건은 이 기준으로 검증된 것이 아니다.**
`✅` 는 *"route · 메뉴 · pageCode 가 있어 사용자가 닿는다"* 까지다.

🔑 **일마감이 반례다.** route·메뉴·pageCode 가 전부 있었는데 마감 실행·역마감이 통째로 없었다.
⟹ 17건 전부 기능·표시형식 대조를 따로 돌려야 한다.

### 폐기 확정 3건

```text
에어디자이너 전용 주문서 인식   불필요 (개발책임자 확정)
제이시스템 전용 주문서 인식     불필요 (개발책임자 확정)
교육안내 자동상태변경          불필요 (개발책임자 확정)
```

⟹ 백로그에서 제외한다. 2026-06-29 OCR 제거 결정과 일관된다.

### 완전 구현 필요 2건

#### 가입고처리

> *"xlsx 파일을 레거시처럼 그대로 인식하여 이를 입고전표(현재 2개의 창고만 지정)로 자동생성하는 시스템이야. 완전한 구현이 필요해."*

```text
입력    xlsx — 레거시와 같은 인식 방식
출력    입고전표 자동 생성
제약    창고는 현재 2개만 지정
현행    수기 입고전표만 있음 — 일괄 변환·전송 없음
```

#### 알리고 자동 업로드 — **레거시와 방식이 다르다**

> *"이카운트 기초 거래처리스트를 엑셀로 첨부하던 방식이 아닌 우리 기초거래처 데이터를 그대로 가져와서 알리고 프로그램에 추후 넣는 시스템이야. 여기서는 멘트 즉, 발송문구를 설정할 수 있도록 해야해."*

```text
레거시   이카운트 기초 거래처리스트 엑셀을 첨부
우리     우리 기초거래처 데이터를 그대로 가져온다      ← 방식이 다르다
추가     발송문구(멘트) 설정 기능
현행     CSV 생성 화면은 있으나 실 알리고 전달이 mock
         aligoAddressBookApi.ts:86 — 외부 미전달
```

🚩 **이건 레거시 파리티가 아니라 개선이다.** 엑셀 첨부 단계를 없애는 것이 목적이다.

### 데이터 4종 — 확인 진행 중

> *"단톡방리스트와 발송금지리스트, 거래처DC리스트, 지역별 분류표는 모두 우리 메뉴가 있을텐데 노션이 데이터 원천이며 그대로 가져온게 맞는지 확인이 필요해."*

CSV 행 수 대 DB 행 수 대조 중. 특히 **거래처 DC정보** 는 일마감 `DC조건 0/13` 의 원천일 가능성이 있다.
