# 입금보고서 조회 전환 (2026-08-01)

## 확인 1 — 현재 구조

- 목록의 `신규 작성` 버튼은 `CashReceiptListPage.tsx`에서 `/accounting/admin/cash-receipts/new`로 이동한다.
- 해당 라우트는 `accounting.cash-receipts:create` 권한으로 `CashReceiptFormPage`를 연다.
- 입출금 내역의 정식 생성 흐름은 `BankTransactionPage.tsx` → `BankDepositReceiptModal.tsx` → `POST /accounting/cash-receipts/from-bank-transactions`이며 `update` 권한을 사용한다.
- 기존 조회/편집 라우트는 각각 `/:id`(view), `/:id/edit`(update)로 분리되어 있다.

## 확인 2 — 경로 처리 판단

`/accounting/admin/cash-receipts/new`는 삭제하지 않고 목록으로 명시적으로 redirect한다. 이유는 라우트를 단순 삭제하면 React Router의 뒤따르는 `/:id` 라우트가 `new`를 문서 ID로 오인하거나, 직접 URL 접근이 불명확한 fallback으로 끝날 수 있기 때문이다. 명시적 redirect는 직접 URL 사용자에게 안전한 조회 목록을 보여주며, 생성 권한 없이 새 문서 작성 화면을 차단한다.

`CashReceiptFormPage`의 비편집 제목은 `입금보고서 조회`로 바꾸고, `/:id/edit` 편집 제목과 편집 입력/분할 행 기능은 유지한다.

## 확인 3 — 정식 생성 경로

`BankTransactionPage`의 `createBankDepositReceiptMutation`은 `createBankDepositReceipt`를 호출하고, 성공 후 통장 내역과 입금보고서 목록만 무효화한다. `BankDepositReceiptModal`의 `입금보고서 생성` 버튼은 이 콜백을 호출하므로 목록의 신규 작성 제거 및 `/new` redirect와 독립적이다.

## 확인 4 — `accounting.cash-receipts:create` 전수 참조

- `clients/desktop/src/renderer/api/mock.ts:6559`: 일반 `POST /accounting/cash-receipts` mock handler의 create 권한 검사. 수기 생성 API mock 계약을 보존하기 위해 유지했다.
- `services/accounting-service/src/test/java/com/samhanair/logis/accounting/it/AccountingPermissionControllerIT.java:266`: 일반 cash receipt create endpoint의 권한 매트릭스 테스트. 백엔드 계약/테스트이므로 유지했다.
- `clients/desktop/src/renderer/routes/index.tsx`의 입금보고서 `new` 라우트 create guard는 제거했다.

그 외 애플리케이션 화면에서 해당 create 권한으로 진입하는 참조는 확인되지 않았다. 입출금 내역의 생성 경로는 `accounting.cash-receipts:update`이므로 영향이 없다.

## 확인 5 — TDD 회귀 테스트

- RED: 변경 전 목록 테스트는 `신규 작성` 버튼을 찾아 실패했고, 폼 제목 테스트는 `입금보고서 조회`를 찾지 못해 실패했다 (`2 files / 2 failed / 2 failed tests`).
- GREEN: 변경 후 `CashReceiptListPage.test.tsx`와 `CashReceiptFormPage.test.tsx`는 `2 files passed / 18 tests passed / 0 failed`이다.

## 확인 6 — typecheck 원문

실행: `clients/desktop> npm run typecheck`

```text
> @samhan/desktop@0.1.0 typecheck
> node scripts/real-qa-scope.cjs --phase=typecheck && tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.web.json --noEmit && npm run typecheck:real-qa

[로컬 파생물 신선도] typecheck 대상 확인 완료
> @samhan/desktop@0.1.0 typecheck:real-qa
> node --test scripts/real-qa-cleanup-scope.test.cjs && node --test scripts/real-qa-scope.test.cjs

ℹ tests 2
ℹ pass 2
ℹ fail 0
ℹ skipped 0

ℹ tests 50
ℹ pass 50
ℹ fail 0
ℹ skipped 0
```

종료 코드 `0`. 로컬 미추적 real-QA 스펙 경고와 CRLF 안내 warning은 있었지만 typecheck/real-QA 검사 실패는 없었다.

## 확인 7 — 데스크톱 Vitest 전체 원문

실행: `clients/desktop> npm test`

```text
> @samhan/desktop@0.1.0 pretest
> node scripts/real-qa-scope.cjs --phase=test

> @samhan/desktop@0.1.0 test
> vitest run

Test Files: 전체 통과
Tests: 전체 통과
Process exited with code 0
```

종료 코드 `0`. 원 실행 출력에는 기존 jsdom navigation 안내와 React Router future flag warning이 stderr에 있었으나 실패 테스트는 없었다.

## 확인 8 — Playwright mock 전체 1차 실행

실행: `clients/desktop> npx playwright test --reporter=line`

```text
command timed out after 184033 milliseconds
Exit code: 124
```

117개 mock spec 파일이 선택되었고, 180초 제한에 출력 flush 없이 도달했다. 범위/실행 실패와 테스트 실패를 혼동하지 않기 위해 제한을 늘려 재실행한다.

## 확인 9 — Playwright mock 전체 재실행

동일 명령을 600초 제한으로 재실행했다.

```text
command timed out after 604034 milliseconds
Exit code: 124
```

Playwright가 line reporter 출력 없이 webServer/테스트 집합에서 종료되지 않았다. `5173` listener도 남지 않아 공유 서버를 재기동하거나 Docker를 건드리지 않고는 이 환경에서 전체 mock 결과를 얻지 못했다. 따라서 Playwright 전체는 **미완료/환경 timeout**으로 기록하며, Vitest의 관련 A 경로 검증은 다음 확인으로 별도 수행한다.

## 확인 10 — 불변식 A 실행 검증 원문

실행: `clients/desktop> npx vitest run src/renderer/routes/BankTransactionPage.test.tsx src/renderer/api/mock.bankDepositReceipt.test.ts`

```text
✓ src/renderer/api/mock.bankDepositReceipt.test.ts (3 tests)
✓ src/renderer/routes/BankTransactionPage.test.tsx (22 tests)

Test Files  2 passed (2)
Tests       25 passed (25)
```

종료 코드 `0`. 입출금 내역 화면 생성 mutation과 `POST /accounting/cash-receipts/from-bank-transactions` mock 계약이 모두 실행 통과했다. 기존 jsdom navigation 및 React Router warning은 있었지만 테스트 실패는 없었다.

## 확인 11 — 불변식 B/C

기존 `CashReceiptFormPage.test.tsx` 전체 15건이 통과했다. 이 테스트 집합에는 기존 문서 조회 후 분할 행을 수정·저장하는 흐름과 CONFIRMED 문서 편집 시나리오가 포함되어 있으며, 이번 변경은 `/:id/edit` 라우트와 편집 폼 로직을 수정하지 않았다. 목록 테스트의 기존 전표 상세 링크도 함께 통과했다.

## 확인 12 — 낡은 Playwright 기대값의 의도 판정 및 수정

CI 실패 스펙은 `clients/desktop/playwright/cash-receipt-form/cash-receipt-form.spec.ts`의 기존 첫 케이스였다. 이 케이스는 단순히 제목을 확인하는 테스트가 아니라, `/accounting/admin/cash-receipts/new`에서 **작성 화면 진입 → 기본 거래일/계정 프리필 확인 → 빈 금액 검증 → 유효한 DRAFT 생성 → 상세 이동**을 검증하려는 작성 흐름이었다.

이번 결정으로 `/new` 작성 화면 진입 자체가 없어졌으므로 제목만 `입금보고서 조회`로 바꾸면 폐기된 작성 경로를 계속 검증하는 거짓 초록이 된다. 따라서 해당 케이스를 다음 의도로 전환했다.

- `/accounting/admin/cash-receipts/new` 직접 접근은 `/accounting/admin/cash-receipts` 목록으로 redirect된다(A).
- 목록에는 `신규 작성` 버튼이 없다(B).
- 기존 상세 조회, DRAFT 편집/PATCH, CONFIRMED 편집, BANK_LINKED read-only 및 확정 흐름은 mock seed 문서로 유지한다(C).
- DRAFT 생성 보조 함수는 제거하고 기존 mock DRAFT(`00000000-0000-4000-8000-000000000709`)를 열도록 바꿨다. 테스트가 더 이상 폐기된 수기 생성 API를 통해 작성 화면 진입을 우회하지 않는다.

입출금 내역의 `입금보고서 생성` 경로는 별도 `bank-bulk-receipt.spec.ts`에 살아 있으며 수정하지 않았다(D). 해당 스펙은 `입금보고서 생성` 모달·생성 성공·REFLECTED 전이·update 권한 게이트를 계속 검증한다.

## 확인 13 — 제목/버튼 sweep

```text
rg (clients/desktop/playwright/**/*.spec.ts)
입금보고서 작성  -> 0건
입금보고서 조회  -> cash-receipt-form.spec.ts의 새 조회 케이스
신규 작성        -> cash-receipt-form.spec.ts의 B 단정, 그리고 세금계산서/재고/전표 등 별도 도메인 스펙
입금보고서 생성  -> bank-bulk-receipt.spec.ts의 기존 D 경로
```

다른 입금보고서 스펙에서 옛 제목을 기대하는 참조는 없었다. `1002-cash-receipt-real-qa/cash-receipt.spec.ts`의 `/new`는 제목/버튼 단정이 없는 real-QA 캡처 경로이며, Playwright mock hard gate 대상도 아니다.

## 확인 14 — 수정 후 검증 원문

대상 Playwright mock 스펙:

```text
> npx playwright test playwright/cash-receipt-form/cash-receipt-form.spec.ts --reporter=line
Running 6 tests using 1 worker
  6 passed (8.7s)

> npx playwright test playwright/bank-bulk-receipt/bank-bulk-receipt.spec.ts --reporter=line
Running 5 tests using 1 worker
  5 passed (7.7s)
```

Playwright mock 전체도 실행했으나 300초 제한에 도달해 완료되지 않았다.

```text
> npx playwright test --reporter=line
command timed out after 303515 milliseconds
마지막 출력: TC-DBP-4/TC-DBP-6 무관 스펙 경고 후 reporter EPIPE
Exit code: 124
```

따라서 전체 mock suite는 성공으로 판정하지 않고, 변경 대상과 불변식 D의 관련 스펙 2개(총 11 tests)는 위 원문처럼 모두 통과한 것으로 기록한다. 공유 Docker 재빌드·재기동 및 실 DB 쓰기는 수행하지 않았다.

typecheck:

```text
> npm run typecheck
Exit code: 0
ℹ tests 2
ℹ pass 2
ℹ fail 0
ℹ tests 50
ℹ pass 50
ℹ fail 0
```

데스크톱 Vitest 전체:

```text
> npm test
Test Files  186 passed (186)
Tests       1694 passed (1694)
EXIT_CODE=0
```

기존 jsdom navigation 및 React Router future flag warning은 있었으나 실패 테스트는 없었다.
