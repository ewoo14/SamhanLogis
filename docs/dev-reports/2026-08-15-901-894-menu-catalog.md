# 901/894 결정 B — 서버 메뉴 catalog 구현 보고

## 실측

- 본체 `AppLayout.tsx` 배차 메뉴: 11개.
- 아로로지스 데스크톱: 기본 4개 + 권한 보유 시 `수신 배차 그룹` 1개.
- 본체 `dispatch.board`는 `배차현황`과 `배차 그룹`을 함께 덮어 pageCode와 메뉴가 1:1이 아니다.
- 본체 공식 메뉴에 없는 `/arologis/manual`은 catalog에서 제외했다.

## 변경

- `auth-service`에 코드 정본 `MenuCatalog`와 UUID 비공개 `MenuCatalogEntry`를 추가했다.
- `GET /auth/admin/menu-catalog`를 추가했다.
- 현재 인증 주체의 `VIEW` 권한과 catalog를 서버에서 교집합한다.
- `PARTNER`와 잘못된 account identity는 빈 목록으로 fail-closed한다.
- 두 FE는 서버 catalog를 렌더링 데이터로 사용한다.
- 아로로지스 기존 route `PermissionGuard`는 유지했다.

## TDD 증거

### RED

실행:

```text
./gradlew --no-daemon :services:auth-service:test --tests com.samhanair.logis.auth.menu.MenuCatalogTest
```

결과: `compileTestJava FAILED`, `MenuCatalog`·`MenuCatalogEntry` 미존재로 8개 컴파일 오류.

### GREEN

실행:

```text
./gradlew --no-daemon :services:auth-service:test --tests com.samhanair.logis.auth.menu.MenuCatalogTest --tests com.samhanair.logis.auth.web.MenuCatalogControllerTest
```

결과: `6 tests completed`, `BUILD SUCCESSFUL`.

추가 실행:

- 아로로지스 `DispatchesLayout.test.tsx`: 3/3 통과
- 아로로지스 typecheck: 통과
- 본체 typecheck + real-QA scope: 통과
- 본체 harness false-green guard: 62/62 통과

## 실제 API QA

- auth-service만 redeploy했다. readiness: `healthy`, actuator `200/UP`.
- 로그인 없는 catalog 조회: `401`.
- `QA_DEV_MANAGER_LOGIN_ID`: 로그인 성공, catalog 16개 (`samhan-public` 11개 + `arologis` 5개), UUID 누출 없음.
- `QA_KIMGICHEOL_LOGIN_ID` SALES: 로그인 성공, catalog 0개.
- 두 계정의 renderer 메뉴 화면 전환은 관측 불가했다. 기존 5175 renderer가 POST 로그인 후 `/login`에 남아 실제 메뉴 캡처를 만들지 못했다. 로그인 화면 캡처는 증거로 세지 않았고 제거했다.

## SHA-256

실제 메뉴 캡처가 생성되지 않아 제출 이미지의 SHA-256 증거는 없다. 이미지 0개를 QA 통과로 간주하지 않는다.

