# PR #1057 `feat/874-set-riusage-global-dc` — 라이브 QA 3차

## 실행 정보

- 실행일: 2026-08-03 (KST)
- 브랜치/커밋: 기록 예정
- 범위: 시나리오 2~5
- 원칙: 실 API·실 GUI 경로, 기존 데이터 수정·삭제 금지, DB 직접 INSERT 금지

## 진행 로그

### 시작

- `git pull`: 이미 최신 상태. PM 기준 커밋 `907577317`까지 반영 확인 예정.

### 발화 조건 측정

제외 잔재를 제거한 `accounting_db` read-only 집계:

| 원천 | displayed_rows | model_rows | eligible_rows |
|---|---:|---:|---:|
| sales_lines | 0 | 0 | 0 |
| sales_allocations | 0 | 0 | 0 |
| tax_invoice_lines | 13 | 0 | 0 |

`POSTED` 모델축 행이 없으므로 실 GUI throwaway 생성으로 진행.

### Throwaway 1 — 수락 차단

- 경로: MANAGER `dev_manager` → 새 판매전표 → 창고 `HQ-001 본사창고` → `(주)삼한공조시스템` → 모델 `AC060CS1DBC1SY` 선택 → 표식 `QA-874-R3-AC060` → 저장 → 전송 → 수락.
- 전표: `2026/08/03-5`, 구성품은 `AC060CN1DBC1`, `AC060CX1DBC1`, `PC1BWSK3NW`, `AR-EC05`로 자동 확장됨.
- 저장/전송: 성공.
- 수락: 실패(409). 원문: `inventory-service 호출 실패(409 CONFLICT): {"success":false,"code":"CONFLICT","message":"재고 부족 — 가용 인스턴스 0 < 필요 1 (productCode=AC060CN1DBC1)"...}`
- 캡처: `r3-01-throwaway-saved-detail.png`, `r3-02-throwaway-sent-detail.png`.

### Throwaway 2 — 재고 확보 시도

- 경로: MANAGER → 새 입고전표 → `HQ-001` → `(주)삼한공조시스템` → 표식 `QA-874-R3-INBOUND-AC060`.
- `AC060CN1DBC1`, `AC060CX1DBC1`는 실 GUI 품목 검색에서 원문 `검색 결과 없음`.
- `PC1BWSK3NW`, `AR-EC05`만 선택 가능했으므로 저장하지 않고 중단했다. 기존 데이터 변경 없음.
- 현재 `inventory_db` read-only 재고: `010001 AVAILABLE 1`, `010001 SHIPPED 2`; 대상 구성품 가용 재고 없음.

### 시나리오 판정

| 시나리오 | 판정 | 근거/캡처 |
|---|---|---|
| 2. R15 숨겨진 불일치 | 미실시 — 발화 차단 | 수락 전 재고 409. 일마감 화면에 `상세 전표가 없습니다`, `모델별 재검증 결과가 없습니다`. `r3-03-daily-closing-blocked-no-posted.png` |
| 3. R17 과잉 불일치 | 미실시 — 발화 차단 | `POSTED` 모델축 행 0건. |
| 4. 순서 무관성 | 미실시 — 발화 차단 | 비교할 회계 매출전표 배분·전기 결과 없음. |
| 5. 목록 항목 클릭 | 미실시 — 발화 차단 | 일마감 이력/상세/재검증 행 0건이라 클릭 대상 없음. |

### 결함/차단 재현

1. `dev_manager`로 `#/sales/new` 진입.
2. `HQ-001`, `(주)삼한공조시스템`, 모델 `AC060CS1DBC1SY`, 메모 `QA-874-R3-AC060` 입력 후 저장.
3. 전표 `2026/08/03-5`에서 `완료 (저장)` → `완료 (전송)` → `완료 (수락)`.
4. 응답 `409 CONFLICT`: `inventory-service 호출 실패(409 CONFLICT): ... 재고 부족 — 가용 인스턴스 0 < 필요 1 (productCode=AC060CN1DBC1)`.

이는 2차의 `productCode 필수` 409와 다른 차단이다. 이번에는 구성품 코드가 채워진 상태에서 inventory 가용 재고가 0이다.

### 종료 시 발화 조건 재측정

throwaway 수락 실패 후에도 제외 잔재를 제거한 집계는 변하지 않았다: `sales_lines 0/0/0`, `sales_allocations 0/0/0`, `tax_invoice_lines 13/0/0`.

### 새 파일 목록

- `docs/dev-reports/2026-08-03-874-live-qa-3.md`
- `docs/qa/874-riusage-global-dc-real-qa/r3-01-throwaway-saved-detail.png`
- `docs/qa/874-riusage-global-dc-real-qa/r3-02-throwaway-sent-detail.png`
- `docs/qa/874-riusage-global-dc-real-qa/r3-03-daily-closing-blocked-no-posted.png`
