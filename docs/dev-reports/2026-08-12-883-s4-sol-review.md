# #883 S4 / PR #1175 CODEX SOL 5.6 적대검증 1차

- 검토일: 2026-08-12
- 대상 HEAD: `c2af88f46fa1c28b0ddf98746f08ac198e1a438f`
- 비교 기준: `f73be26701b579d4795a655eb32774469d960210`
- 판정: **머지 차단 — HIGH 2건, MEDIUM 1건**
- 값 불변 판정: **주문·견적 금액/수량/상태/문서번호/날짜 및 인쇄 문자열 회귀는 발견하지 못했다.**

## 1. 결함 요약

| ID | 심각도 | 판정 |
|---|---:|---|
| S4-SOL-1 | HIGH | 살아 있는 `.btnMini` selector가 삭제되어 거래처 DC 설정·견적 가격 설정 버튼 7개가 무스타일로 렌더된다. |
| S4-SOL-2 | HIGH | 추가된 RED-A real-QA 스펙이 새 Playwright context의 인증 세션을 만들지 않으며, 목록/상태 전수/주문 인쇄를 요구 수준으로 단언하지 않는다. |
| S4-SOL-3 | MEDIUM | 작업 범위와 무관한 추적 하네스 fixture `tools/.s24-build-only/build/deep/tracked-writer.mjs`가 PR에서 삭제됐다. |

## 2. S4-SOL-1 — 살아 있는 `btnMini` 삭제

### 증거

- 기준 CSS에는 `.btnMini`가 있었다: `sales.module.css` 기준 commit line 92.
- 현재 CSS에는 `.btnMini` 정의가 0건이다.
- 현재 코드의 살아 있는 참조는 7건이다.
  - `EstimatePricingConfigPage.tsx:345,353`
  - `SalesPartnerDcConfigPage.tsx:228,236,330,349,426`
- HEAD 앱의 `/#/sales/partner-dc-config`에서 `검색` 버튼의 실제 `class` 속성은 `null`이었다.
- 캡처: `docs/qa/883-s4-sol-review-real-qa/_local/07-btn-mini-missing.png`
- Vitest가 통과한 이유도 확인했다. `EstimatePricingConfigPage.test.ts`는 문자열 계약 위주이며 CSS module 반환값/계산 스타일을 단언하지 않는다.

### 삭제 44개 직접 감사

- PR 문서가 “미사용”으로 분류한 44개 selector를 별도 배열로 고정해 `sales.module.css` importer 전체에서 `styles['name']`와 `styles["name"]`를 직접 검색했다.
- 결과: `DELETED44_COUNT=44`, `DELETED44_LIVE_REFS=0`.
- importer의 모든 `styles[...]` 표현을 조사했다. template literal, 문자열 연결, 변수 index 조립은 없었고 literal/conditional literal만 있었다.
- 따라서 **명시된 44개 자체는 죽은 selector가 맞다.** 결함은 정리 과정에서 그 44개 밖의 살아 있는 `.btnMini`까지 같이 삭제한 것이다.

### 구현자 지시서

- 불변식: `sales.module.css`를 import하는 모든 `styles[...]` 참조는 빌드 후 비어 있지 않은 class name으로 해석되어야 한다. 기존 설정 화면의 버튼 크기·padding·font·cursor 계약도 유지해야 한다.
- 전수 좌표: 위 7개 참조를 모두 처리한다. 일부 화면만 임시 inline style로 막지 않는다.
- 권장 수정: 기준 `.btnMini`를 현행 token으로 복원하거나 7개 버튼을 design-system `Button size="sm"`으로 한 번에 이관한다.
- 회귀표적: 거래처 DC 설정의 검색/CSV/저장/이력 버튼, 견적 가격 설정의 되돌리기/저장 버튼, loading/empty/error/disabled 상태를 모두 렌더 검증한다.
- 테스트: CSS module class가 실제로 존재하는지 또는 design-system 버튼 class가 부여되는지 RTL/브라우저 단언을 추가한다.
- RED-A 값과 RED-B 가격 계산·권한·입력 계약은 건드리지 않는다.
- **제 전제가 틀렸다면 고치지 말고 중단·보고.**

## 3. S4-SOL-2 — RED-A real-QA가 요구 게이트를 증명하지 못함

대상: `clients/desktop/playwright/883-s4-order-ds-migration-real-qa/883-s4-order-ds-migration-real-qa.spec.ts`

### 증거

- `ready`는 URL/주문 ID/견적 ID/baseline만 본다(line 37). 인증 세션 또는 storage state는 준비하지 않는다.
- skip 문구는 “격리 서비스 URL·세션”을 요구하지만(line 58), 파일에는 `samhanAuth` 주입, 로그인, storageState, API proxy 중 어느 것도 없다.
- 주문 목록은 문서번호 하나만 단언한다(lines 65-66).
- 견적 목록도 문서번호 하나만 단언한다(lines 79-80).
- 상세는 baseline에 들어 있는 단일 상태 문자열만 본다. 주문 6종/견적 5종 상태의 종류와 라벨 전수 계약이 없다.
- baseline type에는 날짜 필드가 없어 날짜 불변을 표현할 수 없다.
- 인쇄는 견적만 선택적으로 실행한다(lines 90-99). 주문 인쇄는 검증하지 않는다.
- `BASE_URL` 등을 주지 않으면 스펙 전체가 skip되므로 “파일 존재”만으로 RED-A 수행 증거가 되지 않는다.

### 구현자 지시서

- 불변식: fresh Playwright context에서 공유 DB 로그인 없이 격리 auth/session을 스펙 자체가 재현해야 한다.
- baseline 좌표를 주문/견적 각각 `documentNumber`, `documentDate`, `due/valid date`, `amounts`, `quantities`, `allStatusLabels`로 확장한다.
- 주문 목록·상세, 견적 목록·상세에서 각 값을 해당 row/field/table cell 단위로 단언한다. `body.contains`만으로 서로 다른 필드의 같은 숫자를 통과시키지 않는다.
- 주문 6종 `진행중/보류/확인중/완료/취소/전환완료`, 견적 5종 `작성중/발송완료/수주완료/거절/전표변환완료`를 전수 단언한다.
- 주문 인쇄 HTML과 견적 브라우저 미리보기를 모두 검증한다.
- 디렉터리·파일명 `-real-qa`, `resolveQaShotsDir()`, HashRouter URL, Chromium-1217, 격리 포트, 종료 cleanup 계약을 유지한다.
- 재현 데이터는 이 보고서 §5 표와 동일한 값을 사용하면 된다.
- RED-B 표적: permission denied/disabled, empty/error, 목록 복귀 scroll, 페이지 2, 편집/삭제/복원/전환을 별도 테스트로 오염시키지 말고 읽기 전용 검증과 분리한다.
- **제 전제가 틀렸다면 고치지 말고 중단·보고.**

## 4. S4-SOL-3 — 무관한 추적 fixture 삭제

### 증거

PR diff에 아래 삭제가 포함된다.

```text
tools/.s24-build-only/build/deep/tracked-writer.mjs
- const OUT = 'docs/qa/.s24-build-only.png'
```

이 파일은 `harness-false-green-guard.test.ts:1527-1540`의 build-only probe 좌표다. 주문·견적 셸 변경과 관계가 없고, 과거에도 전체 suite cleanup 뒤 추적 fixture를 복원하도록 검토된 좌표다.

### 구현자 지시서

- 불변식: 제품 PR은 테스트 실행이 삭제한 추적 fixture를 diff에 포함하지 않는다.
- 위 파일을 기준 내용 1줄로 복원한다.
- 복원 후 desktop 전체 테스트가 다시 이 파일을 삭제하면 테스트 종료 후 tracked fixture 복원 여부를 확인하고, 제품 변경과 분리해 하네스 원인을 보고한다.
- **제 전제가 틀렸다면 고치지 말고 중단·보고.**

## 5. RED-A 실제 문자열 검증

### 격리 구성

- 공유 DB에 로그인하지 않았다.
- 격리 PostgreSQL 4개 DB 축 + Redis/Rabbit, 실제 Spring 서비스 7개를 별도 Docker network에 기동했다.
- 서비스: auth `39881`, user `39883`, product `39884`, slip `39886`, partner-order `39888`, dc-config `39889`, partner `39895`.
- renderer: `39873`, 브라우저 API placeholder: `39880`.
- Playwright는 `clients/desktop`에서 headless Chromium-1217로 직접 실행했다. 브라우저 UA의 Chromium 버전은 Playwright-1217 번들 기준 Chrome 147이었다.
- QA 스펙/디렉터리는 모두 `-real-qa`였고 증거는 `resolveQaShotsDir()`의 `_local` 경로에만 썼다.
- 최종 실행: **4/4 통과, 5.3초**.
- 기동한 Vite/서비스/DB/Redis/Rabbit/network는 검증 종료 후 모두 정지·제거했다. 빌린 격리 DB 3개는 원래 상태인 stopped로 되돌렸다.

### 화면/API 문자열

| 표면 | 단언한 실제 문자열 |
|---|---|
| 주문 목록 | `2026/08/12-2`, `2026/08/12`, `600,000원`, `완료` |
| 주문 상세 | `2026/08/12-2`, `완료`, `합계 600,000원`, 수량 `1`, `QA-HVAC-001`, `격리 QA 전열교환기`, 납기 `2026-08-20` |
| 견적 목록 | `2026/08/11-6`, `2026-08-11`, `2026-09-10`, `₩930,000`, `작성중` |
| 견적 상세 | `2026/08/11-6`, `작성중`, 수량 `1`, 공급가 `845,455`, VAT `84,545`, 총합 `930,000`, `QA-HVAC-001` |
| 견적 미리보기 | `2026/08/11-6`, `2026년 08월 11일`, `2026년 09월 10일`, `QA-HVAC-001`, 수량 `1`, `₩ 930,000` |
| 주문 인쇄 HTML | title/document `2026/08/12-2`, `QA-HVAC-001`, `격리 QA 전열교환기`, `600,000` |

텍스트 증거:

- `docs/qa/883-s4-sol-review-real-qa/_local/01-order-list-text.txt`
- `docs/qa/883-s4-sol-review-real-qa/_local/02-order-detail-text.txt`
- `docs/qa/883-s4-sol-review-real-qa/_local/03-estimate-list-text.txt`
- `docs/qa/883-s4-sol-review-real-qa/_local/04-estimate-detail-text.txt`
- `docs/qa/883-s4-sol-review-real-qa/_local/05-estimate-print-text.txt`
- `docs/qa/883-s4-sol-review-real-qa/_local/06-order-print-html.txt`

### 상태 라벨 전수

- 주문 실제 목록: `진행중`, `보류`, `확인중`, `완료`, `취소`, `전환완료` 6종을 모두 확인했다.
- 견적 실제 목록: `작성중`, `발송완료`, `수주완료`, `거절`, `전표변환완료` 5종을 모두 확인했다.
- 정적 대조: design-system `OrderStatusBadge.tsx:20-25`와 기존 `PARTNER_ORDER_STATUS_LABEL`(`sales.ts:398-403`)은 key/문자열이 정확히 같다. 상태 병합·누락은 없다.

### 가격 규칙

- 주문 격리 저장 결과: 40% 규칙 결과 `600,000`; 목록/상세/인쇄가 모두 같은 값을 표시했다.
- 견적 격리 dc-config 직접 계산: `appliedRate=0.0700`, `finalPrice=930000`; 목록/상세/미리보기가 모두 같은 값을 표시했다.
- 이 PR은 가격 계산 API/도메인 코드를 변경하지 않았다.

## 6. 다른 상세 셸과의 구조 대조

| 기준 | 주문 상세 | 견적 상세 | Slip/Transfer 대조 |
|---|---|---|---|
| 셸 | `Card` + `detail-grid` | `Card` + `detail-grid` | 동일 계열 |
| 헤더 | 문서번호 + copy + status + 우측 action | 문서번호 + status + 우측 action | 데스크톱 헤더/액션 분리 패턴 일치 |
| 빈 값 class | `detail-grid-item-empty`, 표시 `-` | `detail-grid-item-empty` | Slip `DetailGridItem`과 동일 class 계약 |
| 라인 | 별도 Card/table | 같은 Card 내부 table | 상세 메타와 라인 분리 원칙 유지 |
| 고유 요소 | 협업 panel, 재고조회 modal, 라인 참조 modal 모두 유지 | 담당/협업/버전이력 유지 | 누락 없음 |

라이브로 주문의 협업 panel을 확인했고, 재고조회 modal과 기준 정보 조회 modal을 실제로 열었다.

견적 비고의 빈 값 literal은 `EstimateDetailPage.tsx:561`에서 여전히 `(빈 값)`이고, 주문/Transfer의 `-` 및 Slip 일부의 `—`와 글자 자체는 다르다. 다만 `detail-grid-item-empty` class는 정확히 붙고 이 literal은 전환 전 견적의 기존 표현을 보존한 것이므로 이번 값 회귀 결함으로 세지 않았다.

## 7. RED-B 및 정적 회귀 검토

- `npm test -- --reporter=dot`: exit 0, desktop Vitest 전량 통과.
- `npm run typecheck`: exit 0.
- `npm run lint`: exit 0, **0 errors / 173 warnings**. 경고는 기존 repository 부채이며 이번 변경 파일의 신규 error는 없다.
- 정렬/필터/페이지네이션/행 클릭: list diff는 주문번호·상태 renderer 교체뿐이며 handler와 query/filter/page state는 유지됐다.
  - 주문: `SalesPartnerOrderListPage.tsx:470,488,503,612,622,633`
  - 견적: `EstimateListPage.tsx:569,582,592,635`
- 권한/disabled/menu 조건은 상세 action을 이동하면서 기존 guard를 유지했다. MASTER 실제 화면에서 허용 action 노출과 inventory 미선택 disabled를 확인했다.
- 자동 빈 행, 입력 UX, 종합견적서 custom merge/sort, 문서번호 path, 가격 계산 코드는 diff 밖이며 관련 전체 Vitest가 통과했다.
- 전표번호/문서번호 링크 및 뒤로가기 계약 코드는 이 PR에서 변경되지 않았다.

## 8. 이 라운드가 라이브로 보지 않은 표면

결함이 있어 재작업 지시로 반환하므로 다음 표면은 실제 mutation까지 수행하지 않았다.

- 비-MASTER/부분 권한 role의 메뉴 숨김과 버튼 disabled 실브라우저 조합
- 주문/견적 편집·삭제·복원·전환 write 성공/실패
- 50건 초과 page 2 이동과 실제 뒤로가기 scroll 좌표 복원
- mobile viewport의 collapsible/action bar
- 주문 인쇄 버튼이 여는 blob popup 자체; 이번에는 동일 backend 인쇄 HTML 문자열을 직접 검증했다.
- 목록/상세의 모든 empty/error fixture 조합; 정상 목록·정상 상세·두 modal·설정 화면 error 상태는 밟았다.

재검토 시 S4-SOL-1~3 수정 후 위 미관찰 표면 중 권한, page 2/scroll, empty/error를 우선 추가한다.
