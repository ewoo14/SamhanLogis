연관 Issue: #1064

## 트랙 개설 — 착수 전 확인

2026-08-03 등록.

### 결함의 성격
**화면 버튼과 API 전이가 어긋나 GUI 만으로는 입고를 끝낼 수 없습니다.** 이 저장소에서 *"할 수 없는 것을 하라고 안내"* 는 반복 결함이고(`#1074`·`#1045`), 이번은 그 극단입니다 — 안내조차 없이 경로가 막혀 있습니다.

### 정찰이 실측할 것
1. 입고 전표의 **상태 전이 그래프**를 도메인에서 전수로 (`파일:줄`)
2. **화면이 노출하는 버튼 전수**와 대조 — 🚨 축은 *"상태 → 다음 전이"* 가 아니라 **"도메인이 허용하는 액션 전부 vs 화면이 노출하는 액션 전부"** 입니다. `#1057` R22 가 전자로 물어 반려 버튼을 통째로 잃은 전례가 있습니다
3. 🚨 **실 데이터로 세십시오** — 지금 각 상태에 몇 건이 걸려 있는가. 막힌 전표가 실제로 있는가
4. 출고 전표는 되는데 입고만 안 되는 것인지 대조

### RED 방향 (미리 겁니다)
- A 입고를 **GUI 만으로 끝까지** 진행할 수 있다
- B 🚨 되면 안 되는 전이가 열리지 않는다 — 한쪽만 보면 반대가 깨집니다