# PR #1170 D-G1 S4a fix6 — S4a 재검토6 BLOCKING 2건 수정 보고

- 대상: `feat/dg1-s4-settlement-screen` / PR #1170 최신 HEAD
- 지시서: `docs/dev-reports/2026-08-11-dg1-s4a-sol-review6.md`
- git 조작: 0건
- 공유 DB write: 0건
- 판정: **두 blocking 수정 완료. PM commit 후 최종 CI를 진행할 수 있는 상태**

## 1. 수정 내용

### SOL-S4A-R6-01 — 비ASCII canonical inventory 가드

`clients/desktop/src/renderer/test-utils/accounting-slip-permission-contract.test.ts`에 다음을 추가했다.

- generator·생성 DB snapshot·permission checker를 Windows/Node strict UTF-8 decoder로 읽는다.
- 파일 전체 hash 대신 비ASCII가 포함된 **전체 라인 inventory**를 canonical exact assertion으로 고정했다.
- BOM은 소스 파일의 encoding marker로만 제거해 semantic inventory에 포함하지 않는다.
- 정상 파일은 strict decode와 inventory를 모두 통과한다.
- 같은 테스트 안에서 실제 문자열 변이 5종을 만들어 가드가 실패하는지 검증한다. 별도 테스트를 늘리지 않아 desktop exact count를 보존했다.

비ASCII 전수 inventory:

| 대상 | 전수 구성 | 줄 수 | 적대 변이 결과 |
|---|---|---:|---|
| generator | 한국어 오류/주석 12줄 + `×` 1줄 | 13 | `×→U+FFFD`, 한글 오류 valid-Unicode mojibake 모두 RED |
| DB-derived snapshot | `×` 1줄 | 1 | `×→…` RED |
| permission checker | `↔` 2줄 + 한국어 JSDoc 3줄 + `×` 2줄 | 7 | `↔→→`, JSDoc 한글 변이 RED |

실제 mutation 실행:

| mutation | 결과 |
|---|---|
| generator `×` → `�` | RED: 1 failed / 15 passed |
| generator `DB 파생 스냅샷 갱신 중단` → `DB ?뚯깮 ...` | RED: 1 failed / 15 passed |
| checker `↔` → `→` | RED: 1 failed / 15 passed |
| checker JSDoc `역할` → `役割` | RED: 1 failed / 15 passed |
| snapshot `×` → `…` | RED: 1 failed / 15 passed |
| 정상 원복 | GREEN: 15 passed |

### SOL-S4A-R6-02 — real-QA 경로 정규화

디렉터리와 파일명을 모두 `-real-qa`로 변경했다.

```text
clients/desktop/playwright/
  dg1-s4a-sales-commission-settlement-real-qa/
    dg1-s4a-sales-commission-settlement-real-qa.spec.ts
```

- 기존 `playwright.real-qa.config.ts`의 전역 `testMatch`와 `testIgnore`는 변경하지 않았다.
- 캡처 경로는 기존처럼 `resolveQaShotsDir()`만 사용한다.
- 구현 보고서와 과거 S4a QA 보고서의 실행 경로 참조를 새 경로로 동기화했다.

## 2. 검증 결과

### PowerShell 5.1 generator

실행 명령:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/refresh-accounting-permission-db-snapshot.ps1
```

| 항목 | 결과 |
|---|---|
| 실행 1/2 | exit 0 / exit 0 |
| SHA-256 | `8AA5EB82337B18E02F5D3CB4694F9E7F8F0D7F76AF2292973471EE0AF37BCA2B` / 동일 |
| 산출물 | 15,818 bytes, LF 413, CRLF 0 |
| BOM / U+FFFD | 없음 / 0 |
| 임시 DB·네트워크 | generator finally에서 제거 |

### 코드·회귀

| 검증 | 결과 |
|---|---|
| accounting-slip permission contract | 15/15 GREEN |
| desktop 전체 Vitest | 248 files passed; **2,167 passed / 1 skipped**; exit 0 |
| accounting-service 전체 | **1,871 tests / 0 failures / 0 errors / 10 skipped**; BUILD SUCCESSFUL |
| `tsc -p tsconfig.node.json --noEmit` | exit 0 |
| `tsc -p tsconfig.web.json --noEmit` | exit 0 |
| 새 스펙 narrow real-QA scope | exit 0 (`REAL_QA_ALLOW_UNTRACKED=1` + 명시 경로) |
| cleanup scope test | 2/2 GREEN |

`npm run typecheck:real-qa` 전체 실행은 새 파일이 아직 Git index에 없는 상태라 scope 집합 불일치 1건으로 차단됐다. 이는 개발책임자의 `git 조작 금지` 및 PM commit 대행 지시를 따르기 위해 스테이징하지 않은 결과다. 새 스펙을 명시한 narrow scope와 cleanup/TypeScript 자체 검사는 통과했다.

### 직접 Playwright 라이브 QA

- 실행 위치: `clients/desktop`
- 실행 경로: `playwright/dg1-s4a-sales-commission-settlement-real-qa/dg1-s4a-sales-commission-settlement-real-qa.spec.ts`
- renderer project, headless Chromium, 전용 renderer port `59911`
- `VITE_MOCK_MODE=1`, API port `59999`로 외부 상태 격리
- 결과: **5 passed (6.2s)**
- `resolveQaShotsDir()`가 생성한 캡처: `docs/qa/2026-08-11-dg1-s4a/_local/`
- 기동 PID `93284` 종료 확인, 전용 port listener 0

## 3. 변경 파일

- `clients/desktop/src/renderer/test-utils/accounting-slip-permission-contract.test.ts`
- `clients/desktop/playwright/dg1-s4a-sales-commission-settlement-real-qa/dg1-s4a-sales-commission-settlement-real-qa.spec.ts`
- `docs/dev-reports/2026-08-11-dg1-s4a-implementation.md`
- `docs/dev-reports/2026-08-11-dg1-s4a-sol-review.md`
- `docs/dev-reports/2026-08-11-dg1-s4a-sol-review6.md`
- `docs/dev-reports/2026-08-12-dg1-s4a-fix6.md`

`playwright.real-qa.config.ts`는 변경하지 않았다.
