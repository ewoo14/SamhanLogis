# #894 S1 채팅 독립 앱 이식 — 첫 구현 라운드

## 1. 본체 전수 확인과 재사용

본체의 S1 자산을 먼저 확인했다.

| 자산 | 근거 | 독립 앱에서의 재사용 |
|---|---|---|
| 채팅방 목록·새 대화 화면 | `clients/desktop/src/renderer/routes/ChatRoomsPage.tsx:11-32` | `src/renderer/ChatApp.tsx`에 같은 흐름 이식 |
| 대화 화면·메시지 전송·SSE 재조회 | `clients/desktop/src/renderer/routes/ChatRoomPage.tsx:9-46` | `ChatApp.tsx`의 room 화면과 `subscribeToChatRoom`에 이식 |
| 채팅 타입·REST client | `clients/desktop/src/renderer/api/messengerApi.ts:87-103` | `src/renderer/api/chatApi.ts`에 동일 경로·봉투·타입 이식 |
| SSE endpoint | `clients/desktop/src/renderer/realtime/chatRealtimeClient.ts:4-6` | `chatApi.ts`가 동일 `/stream` endpoint를 사용 |
| 백엔드 room/message 계약 | `services/groupware-service/src/main/java/com/samhanair/logis/groupware/controller/ChatRoomController.java:27-78` | 백엔드 변경 없이 동일 계약 호출 |
| 직접방 생성 요청 | `ChatDirectRoomRequest.java:6` | `participantId: UUID`를 본체와 동일하게 전송 |
| 사용자 표시 응답 | `ChatRoomResponse.java:5-8` | `roomCode`, 이름·부서·사번만 화면 표시 |
| 디자인 시스템 | `clients/web/design-system/src/index.ts:8-12`, package peer React `^18.3.0` | `Button`, `Card`, `Input` import |
| 독립 앱 기존 셸·자동 업데이트 | `clients/internal-chat-desktop/src/main/index.ts:4`, `src/main/auto-update.ts:2`, `README.md:3` | main/preload/자동 업데이트 흐름 보존 |

본체의 채팅 파일은 삭제·이동·수정하지 않았다.

## 2. RED 원문 (양방향)

### ① 독립 앱에서 S1이 아직 안 되는 상태

초기 실행:

```text
npm test -- --run src/renderer/chat-app.test.tsx
'vitest' is not recognized as an internal or external command,
operable program or batch file.
```

의존성 설치와 Vitest 수집 설정을 맞춘 뒤 구현 직전 재실행:

```text
Error: Failed to resolve import "./ChatApp" from "src/renderer/chat-app.test.tsx".
```

즉 독립 앱에는 S1 화면/API client가 없었다.

### ② 본체 무변화 경계

RED 테스트에는 다음 경계 계약을 포함했다.

```text
it('독립 앱은 본체 채팅 구현을 import하지 않는다', ...)
```

구현 전에는 위 테스트 파일 자체가 `./ChatApp` 부재로 수집 단계에서 중단되어 독립 앱 RED와 함께 실패했다. 구현 후에는 이 경계 테스트가 통과했고, 이번 라운드의 `apply_patch` 대상은 `clients/internal-chat-desktop/**`와 이 보고서뿐이었다. 본체 무변화는 독립 앱이 `clients/desktop`를 import하지 않는 구조 테스트와 본체 파일 미수정으로 보증했다.

## 3. 고른 수단과 이유

- 본체와 버전을 맞춰 React `18.3.1`, React Router `6.27.0`, TanStack Query `5.59.20`을 사용했다.
- 본체 화면의 데이터 흐름을 유지하되 독립 앱은 자체 `chatApi.ts`를 두어 본체 파일을 runtime import하지 않게 했다.
- 상태·재조회는 본체와 같은 TanStack Query 패턴을 사용했다.
- 방 식별자는 본체와 같은 `roomCode`만 URL에 사용했다.
- 화면 컴포넌트는 design-system의 `Button`, `Card`, `Input`을 우선 사용했다.
- 독립 Electron 셸의 `MemoryRouter` 초기 경로는 `/chat`로 설정했다.
- 로컬 file package에서 React가 중복 로드되는 문제를 피하기 위해 테스트·Electron renderer 모두 독립 앱 React를 alias했다.

## 4. GREEN 원문과 typecheck

```text
npm run typecheck
Exit code: 0
tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.web.json --noEmit
```

```text
npm test
Test Files  3 passed
Tests       11 passed
```

```text
npm run build
✓ out/main/index.js
✓ out/preload/index.cjs
✓ out/renderer/index.html
```

빌드 시 TanStack Query의 `use client` module directive 무시 경고는 있었지만 build는 성공했다. Electron `postinstall` 503은 발생하지 않았다.

## 5. 불변식별 보증

1. **독립 앱에서 채팅방 신설·대화** — `ChatApp.tsx`와 `chatApi.ts`, `chat-app.test.tsx`의 방 목록/새 대화/메시지 조회·전송 테스트.
2. **본체 무변화** — 본체 채팅 코드를 이동·삭제하지 않았고 독립 앱은 본체 모듈을 import하지 않는다. 경계 테스트 통과.
3. **design-system 우선** — `ChatApp.tsx`에서 `Button`, `Card`, `Input`을 `@samhan/design-system`에서 import.
4. **백엔드 계약 불변** — room/message controller, DTO, migration을 변경하지 않고 기존 endpoint와 envelope을 그대로 호출.
5. **UUID 사용자 노출 방지** — 화면과 URL에는 roomCode·이름·부서·사번만 표시. 단, 아래 선재 위반인 `participantId` UUID 요청은 개발책임자 결정에 따라 본체와 동일하게 유지.

## 6. 판단이 필요해 남긴 것 / 못 한 것

- **선재 UUID 위반**: `participantId`는 `ChatDirectRoomRequest.java:6`의 UUID 계약이며 새 대화 요청에 실린다. 이번 라운드에서는 제거하지 않았다. UUID 제거 별도 트랙으로 등록한다.
- **이슈 제목의 “편집” 계약 부재**: 확인된 백엔드 계약에는 방 생성, 목록, 메시지 조회·전송, 읽음, SSE만 있고 채팅방 편집 endpoint/권한은 없다(`ChatRoomController.java:27-78`). 편집 API·권한은 만들지 않았다. PM 판단 질문으로 상신한다.
- S3 자동 로그인, S4 부재중, S5 알림, S6 파일전송, S7 이모티콘은 구현하지 않았다.
- 실 백엔드 인증 연결과 실시간 서버 QA는 S3 인증 전제 및 후속 QA 범위로 남겼다.
- `npm install` 결과 audit 취약점 22건은 이번 S1 코드와 무관해 수정하지 않았다.

## fix 라운드 1

### RED 원문 (양방향)

독립 앱 production renderer와 같은 `MemoryRouter basename="/chat" initialEntries={['/chat']}`를 테스트에 재현하고, 실제 API 계약과 같은 `201 + roomCode` 성공 mock 뒤 방 화면 진입을 단정했다.

```text
npm test -- --run src/renderer/chat-app.test.tsx
Test Files 1 failed
Tests 3 passed | 1 failed
× production basename 환경에서 방 생성 직후 생성된 대화방에 진입한다
→ Unable to find an element by: [data-testid="chat-room-page"]
No routes matched location "/chat/CHAT-20260813-000001"
DOM: <body><div /></body>
```

본체 채팅 양방향 기준은 본체의 기존 계약 테스트로 재현했다.

```text
clients/desktop/src/renderer/routes/ChatRoomPage.fix1.test.tsx
6 tests passed
방 목록·방 열기·메시지 표시·생성 실패 안내·전송 실패 안내·상대 표시 계약 통과
```

### 원인

방 생성 API 응답 파싱은 `createDirectChatRoom()`에서 `res.data.data`를 반환하고, 응답의 `roomCode`도 정상적으로 mutation `onSuccess`까지 전달된다. 쿼리 무효화도 성공 콜백에서 시작된다. 실패 지점은 그 이후 라우팅이다.

독립 앱의 `ChatApp` route는 basename 내부에서 `index`와 `:roomCode`를 사용한다. 그런데 생성 성공 콜백과 방 목록 링크가 `/chat/{roomCode}` 절대 경로를 사용했다. basename `/chat`가 이미 적용된 router에서 이 값은 내부 `/{roomCode}`로 해석되어 `:roomCode` route와 일치하지 않는다. React Router가 `No routes matched location`을 기록하고 `ChatApp`이 렌더링할 요소가 없어져 QA의 빈 `#root`가 발생했다.

### 고른 수단과 왜 그것인가

독립 앱의 router 경계를 유지하면서 생성 성공 이동과 방 목록 링크를 basename 내부의 상대 경로로 바꿨다. 생성 후 이동은 `navigate(encodeURIComponent(room.roomCode))`, 방 목록은 `Link to={encodeURIComponent(room.roomCode)}`, 방 화면의 목록 복귀는 `Link to=".."`로 고정했다.

이 수단은 이미 `MemoryRouter basename="/chat"`와 `ChatApp`의 중첩 route가 제공하는 경계를 그대로 사용하므로 백엔드 계약·응답 파싱·쿼리 상태·본체 채팅을 건드리지 않는다. `roomCode`만 URL에 남기고 UUID 표시도 새로 만들지 않는다. 실패를 숨기거나 root fallback을 추가하는 방식보다, 실제로 매칭되어야 할 route 입력을 바로잡는 최소 변경이다.

### GREEN 원문

```text
clients/internal-chat-desktop/src/renderer/chat-app.test.tsx
Test Files 1 passed
Tests 4 passed
production basename 환경에서 방 생성 직후 chat-room-page와 채팅 heading 렌더링 통과
```

본체 회귀 기준:

```text
clients/desktop/src/renderer/routes/ChatRoomPage.fix1.test.tsx
Test Files 1 passed
Tests 6 passed
```

### 필수 검증 결과

독립 앱 패키지(`clients/internal-chat-desktop`)에서 실행했다.

```text
npm run typecheck
Exit code: 0

npm test
Test Files 3 passed
Tests 12 passed
Exit code: 0

npm run build
Exit code: 0
out/main/index.js
out/preload/index.cjs
out/renderer/index.html 및 renderer assets 생성
```

빌드의 기존 TanStack Query `use client` module directive 무시 경고와 테스트의 기존 업데이트 실패 상세 로그/React Router future warning은 있었지만 모두 프로세스 실패가 아니었다.

### 남긴 것

- 백엔드, migration, 본체 `clients/desktop` 채팅 구현은 변경하지 않았다.
- participantId UUID 요청 계약은 개발책임자 결정대로 유지했다. 화면·URL·링크에 UUID를 추가 노출하지 않았다.
- QA 데이터 `CHAT-20260813-000017` 및 `QA1201 본체 회신 20260813134323`은 재사용·변경하지 않았다.
- S3~S7, 채팅방 편집 API, UUID 별도 트랙은 범위 밖으로 남겼다.

## 화면 정본 단계

### RED 원문 (양방향)

직급 정렬 계약을 구현하기 전에 정렬 테스트만 남기고 실행했다.

```text
> .\\gradlew.bat :services:user-service:test --tests 'com.samhanair.logis.user.service.MessengerDirectoryServiceTest' --no-daemon
...
error: cannot find symbol
  symbol: class MessengerDirectoryService
```

프론트 화면 계약 테스트는 directory/me mock과 직원 클릭 대화 생성 계약을 추가한 뒤 첫 실행에서 데이터 로드 전 컨테이너를 잡는 테스트 오류가 났고, 실제 텍스트를 기다리도록 테스트를 고쳤다. 본체 무변화 경계는 기존 독립 앱 테스트의 본체 모듈 비-import 계약으로 유지했다.

### 직급 순서표

`services/user-service/src/main/java/com/samhanair/logis/user/service/MessengerDirectoryService.java`의 `JOB_TITLE_ORDER` 한 곳에만 다음 순서를 정의했다.

```text
대표 > 전무 > 이사 > 부장 > 차장 > 과장 > 주임 > 사원
```

목록에 없는 직급·직무는 `Integer.MAX_VALUE`로 보내며 이름이나 새 직급을 추론하지 않는다. 동일 직급은 정본 미결정이므로 repository 결과 순서를 유지했다.

### GREEN 원문 및 검증

```text
:services:user-service:test --tests MessengerDirectoryServiceTest
BUILD SUCCESSFUL

:services:groupware-service:test --tests ChatRoomServiceTest
BUILD SUCCESSFUL

clients/internal-chat-desktop/npm test
Test Files 3 passed
Tests 13 passed

npm run typecheck
Exit code: 0

npm run build
Exit code: 0
```

빌드의 기존 TanStack Query `use client` module directive 경고와 테스트의 기존 Electron updater 상세 오류/React Router future warning은 있었으나 모두 프로세스 실패가 아니었다.

### 불변식 ①~⑤ 보증 방법

1. **화면 정본** — 개별 화면에서 내 정보가 직원 목록 위에 오고, directory가 대표부터 정렬되며, 직원 클릭은 `employeeCode` 기반 새 direct endpoint를 호출한다.
2. **본체 앱 무변화** — 기존 `clients/desktop` 채팅 파일과 기존 UUID direct endpoint를 수정하지 않았고, 독립 앱은 본체 채팅 모듈을 import하지 않는다.
3. **UUID 비노출** — 새 사용자 경계 directory/me 응답과 화면에는 `employeeCode`, 이름, 직책, 부서만 사용한다. UUID 해석은 groupware↔user-service 내부 호출에서만 수행한다. URL은 기존처럼 `roomCode`만 사용한다.
4. **design-system 우선** — `Button`, `Card`, `Input`을 계속 `@samhan/design-system`에서 사용하고 직원 목록은 화면 전용 행으로만 구성했다.
5. **기존 계약 보존** — 기존 `GET /admin/groupware/chat/rooms`, 기존 `POST /direct`, 메시지·SSE 계약은 유지하고 `POST /direct/by-employee-code`, `/users/messenger/directory`, `/users/messenger/me`를 추가했다.

### 안 정해진 것 3가지

- 동일 직급 내 정렬: 가장 단순하게 repository 반환 순서를 유지했다.
- 개별 목록에서 이미 대화가 있는 직원의 표시: 마지막 메시지·안읽음 수를 추가하지 않고 이름·직책·부서만 표시했다.
- 자리비움 자동 전환 기준: 이번 라운드 범위인 직원 목록 기반을 우선해 자리비움 판정을 구현하지 않았다. 상태 아이콘 라운드에서 질문으로 상신한다.

### 못 한 것

- 이번 라운드에서 상태 아이콘, 그룹별 페이지, 검색 모달은 구현하지 않았다.
- `accounts.enabled=false`를 user-service의 employees directory에서 교차 서비스로 확인하는 계약은 아직 연결하지 못했다. 현재 directory는 `termination_date IS NULL`을 재직 기준으로 사용한다. 계정 비활성까지 보장하려면 auth-service 내부 계정상태 조회 계약을 다음 백엔드 보완으로 추가해야 한다.
- 실 DB 기반 라이브 QA와 새 user-service endpoint의 gateway 실측은 수행하지 않았다.

## 상태·그룹별 단계

### RED 원문 (양방향)

독립 앱의 상태·탭·그룹 목록·검색 모달 계약을 먼저 추가하고 실행했다.

```text
Test Files 1 failed
Tests 3 failed
× 상태 아이콘과 개별·그룹별 전환을 제공한다
  → Unable to find a label with the text of: 홍길동 상태: 접속
× 그룹방을 안읽음 먼저 ...
  → Unable to find ... button ... 그룹별
× 돋보기 모달 ...
  → Unable to find ... button ... 그룹별
```

본체 양방향 경계는 이번에도 독립 앱 테스트의 `clients/desktop` runtime import 금지 계약과
변경 파일 경계로 유지했다. `git diff --name-only -- clients/desktop`는 빈 결과였다.

백엔드 그룹 생성 계약도 구현 전에 다음 컴파일 RED를 확인했다.

```text
ChatRoomServiceTest.java:65: error: cannot find symbol
  ChatRoom room = service.createGroup(creator, List.of("E001"), null);
```

상태 판정 서비스도 구현 전에 다음 RED를 확인했다.

```text
error: cannot find symbol
  var service = new MessengerPresenceService();
```

### 상태 판정과 이유

- `AVAILABLE`: 사용자별 desktop/mobile 세션 집합에 하나라도 있으면 판정한다.
- `AWAY`/`ABSENT`: 사용자가 `PUT /users/messenger/presence/status`로 직접 설정한 값만 사용한다.
- `OFFLINE`: 수동 상태가 없고 세션 집합이 비어 있을 때 판정한다.
- 자동 자리비움 시간은 정본에 없으므로 도입하지 않았다. 현재 상태 저장은 기존 서비스 구조를 건드리지 않는 서비스 인스턴스 세션 저장이며, 영속화·다중 인스턴스 공유는 후속 운영 결정으로 남긴다.
- 독립 데스크톱은 UUID가 아닌 `desktop-...` opaque session key로 시작 시 join, 언마운트 시 leave를 호출한다. 모바일은 같은 presence session 계약을 소비할 수 있다.

### 구현 계약

- 기존 1:1 endpoint와 응답 shape는 유지했다.
- 신규 그룹 계약은 `POST/GET /admin/groupware/chat/groups`이며 요청은 `employeeCodes[]`와 선택적 `roomName`만 사용한다.
- 그룹 목록은 호출자가 참여자인 room만 조회하고, 메시지 기록도 기존 `requireParticipant` 권한 경계를 통과한다.
- 기존 `chat_rooms`의 `GROUP`, `room_name`, `chat_room_participants`, `messages.room_id/sequence_no/status`를 재사용해 migration을 추가하지 않았다. 따라서 이 단계의 migration 번호 충돌은 발생하지 않는다.
- 사용자 상태 응답에는 `presenceStatus`만 추가하고 UUID는 포함하지 않는다. 그룹 목록·생성 요청·화면·URL은 `roomCode`, `employeeCode`, 표시명만 사용한다.

### GREEN 원문

```text
clients/internal-chat-desktop
npm test -- --run src/renderer/chat-app.test.tsx
Test Files 1 passed
Tests 8 passed
```

```text
:services:groupware-service:test --tests ChatRoomServiceTest
BUILD SUCCESSFUL
```

```text
:services:user-service:test --tests MessengerPresenceServiceTest
BUILD SUCCESSFUL
```

```text
npm run typecheck
Exit code: 0
```

```text
npm test
Test Files 3 passed
Tests 17 passed
Exit code: 0
```

```text
npm run build
Exit code: 0
out/main/index.js
out/preload/index.cjs
out/renderer/index.html 및 renderer assets 생성
```

build의 기존 TanStack Query `use client` module directive 경고와 테스트의 기존
Electron updater 상세 오류/React Router future warning은 있었으나 실패하지 않았다.

### 불변식 ①~⑤ 보증 방법

1. **화면 정본** — 개별/그룹 탭, 상태 아이콘 4종, 내 정보 상단, 안읽음 우선·최신순 그룹 정렬, 이름/참여자 표시, 검색·복수 선택·단톡방 생성 테스트를 추가했다.
2. **본체 앱 무변화** — `clients/desktop` 파일을 수정하지 않았고 import 경계 테스트와 diff 경계를 확인했다.
3. **UUID 비노출** — 신규 프론트 API 타입과 그룹 생성 요청은 `employeeCode[]`; 그룹 DTO와 presence DTO에는 UUID가 없다. URL은 `roomCode`만 사용한다.
4. **design-system 우선** — 기존 `Button`, `Card`, `Input`을 유지하고 검색 모달도 해당 컴포넌트를 사용했다.
5. **퇴사자/기록 보존** — directory는 기존 `terminationDate IS NULL` 필터를 유지하고, group room은 soft-delete와 참여자 검증으로 기록을 보존한다. `accounts.enabled` 교차 조회는 아직 추가하지 않았다.

### 안 정해진 것 3가지

- 동일 직급 내 정렬: 기존 repository 반환 순서를 유지했다.
- 개별 목록 안읽음 표시: 이번 단계에서 추가하지 않고 이름·직책·부서·상태만 표시했다.
- 자리비움 자동 전환 기준 시간: 자동 전환하지 않고 수동 `AWAY`만 허용했다.

### 최종 검증 명령

```text
npm run typecheck
npm test
npm run build
```

백엔드 관련 targeted test와 compile도 함께 실행한다.

### 못 한 것

- 실 gateway 인증·실 DB·desktop/mobile 양쪽 세션을 연결한 라이브 QA는 수행하지 않았다.
- 상태 저장은 현재 서비스 인스턴스 메모리 기반이며 Redis/DB 영속화와 다중 인스턴스 presence 공유는 후속 결정이다.
- `accounts.enabled` 교차 서비스 확인은 기존 단계의 미해결 범위로 유지했다.

## fix 라운드

대상: PR #1201 `feat/894-s1-chat-port`, fix 기준 HEAD `b658c9ab6`.
시작 전에 `docs/qa/2026-08-13-1201-liveqa/REPORT.md`의 `## 화면 정본 검증`을 읽고, 7번 본체 채팅 회귀부터 코드 경로를 재현했다. 공유 스택은 이 작업 브랜치로 재기동하지 않았다.

### 7번 우선 재현과 공통 뿌리 판정

RED 원문:

```text
LOGIN=200
URL=http://localhost:5182/#/chat
BODY=...환영합니다, [DEV-SEED] 개발매니저 님...대시보드...
sidebar chat 진입 후 채팅방 링크 수=0
```

원인은 두 계약 경계였다. 웹 배포가 `BrowserRouter`만 선택하여 기존 본체의 `/#/chat` 레거시 deep-link를 hash로 해석하지 않았고, 목록 API는 모든 방을 1:1 상대 조회로만 변환하여 그룹/시스템 방 또는 상대가 없는 보존 방 하나가 전체 응답을 실패시킬 수 있었다. 후자는 UI에서 `rooms.data`가 없어 링크 0개처럼 보이는 경로다.

1번 user API prefix와 5번 group API 경로는 **gateway 경로 계약 불일치라는 같은 뿌리**로 묶었다. 7번은 이식 대상에 본체 경로와 기존 방 목록 계약을 보존하지 못한 **본체 호환성 뿌리**로 별도 묶되, 동일한 API/라우팅 계약 회귀로 함께 회귀 검증했다.

### 1. user API prefix 누락

- RED 원문: `GET /users/messenger/directory 404`, `GET /api/users/messenger/directory 200`.
- 원인: 독립 앱 user-service 호출만 gateway `/api` prefix 없이 생성됨.
- fix: `clients/internal-chat-desktop` user directory/me/presence 요청에 `/api` prefix를 적용하고 URL exact contract test 추가.
- GREEN 원문: `/api/users/messenger/directory` 경로 계약 테스트 통과; internal-chat-desktop 전체 `npm test` 4 files / 20 tests 통과.

### 2. employeeCode 전원 null

- RED 원문: `재직자 24명 중 employeeCode=null 20명`; 실 DB `ecount_code` 25명 전원 null.
- 원인: 화면이 실제로 사용 가능한 업무 식별자 fallback 없이 nullable `ecount_code`만 전달함.
- fix: user-service directory/me와 employee-code 조회에서 `ecount_code`가 없으면 `loginId`를 업무 식별자 fallback으로 사용. UUID는 화면에 넣지 않음.
- GREEN 원문: `employeeCode`가 null인 실측 전원-null 경로를 fallback 계약으로 보완; user-service directory 테스트 및 전량 테스트 BUILD SUCCESSFUL.

### 3. 비활성 직원 노출

- RED 원문: `accounts.enabled=false`로 설정한 `dev_sales`가 directory에 계속 표시.
- 원인: user-service의 termination 필터만 있고 auth-service `accounts.enabled` 교차 조회가 미연결.
- fix: auth-service 내부 `GET /auth/internal/accounts/{id}` 상태 계약과 user-service `AuthClient.isEnabled`를 추가하고, directory에서 `terminationDate IS NULL` 및 enabled account ID를 모두 만족하는 직원만 반환.
- GREEN 원문: 종료 직원은 목록에서 숨고 기존 방/기록은 유지되며, disabled account 제외 회귀 테스트 통과.
- 뿌리: 2번과 함께 `activeOnly` directory 계약/계정 상태 경계로 묶음.

### 4. 접속 상태 합성 오류

- RED 원문: `PUT presence ABSENT 200`, `POST presence/sessions/mobile-qa1201 200`, `directory: dev_manager=ABSENT`; 세션이 있는데 수동 상태가 우선.
- 원인: manual status를 active session 조회보다 먼저 반환.
- fix: active desktop/mobile session이 하나라도 있으면 `AVAILABLE`을 우선하고, 세션이 없을 때만 수동 `AWAY/ABSENT`, 그 외 `OFFLINE`을 반환.
- GREEN 원문: `MessengerPresenceServiceTest` 전량 및 user-service 전량 `BUILD SUCCESSFUL`.

### 5. 그룹 API 경로 불일치

- RED 원문: `프런트 GET/POST /admin/groupware/chat/groups → 404`, `서버 실제 mapping /admin/groupware/chat/rooms/groups`.
- 원인: 프런트가 controller의 room 하위 mapping을 누락.
- fix: internal-chat-desktop 그룹 GET/POST를 `/admin/groupware/chat/rooms/groups`로 정정하고 exact URL test 추가.
- GREEN 원문: group URL contract test 통과; groupware controller/service 전량 테스트 `BUILD SUCCESSFUL`.
- 뿌리: 1번과 같은 gateway/API 경로 계약 불일치.

### 6. 그룹 메시지 sequence unique 충돌

- RED 원문: `POST .../CHAT-20260814-000018/messages 500`, `duplicate key value violates unique constraint "ux_messages_room_sequence"`, `(room_id, sequence_no)=(..., 1)`.
- 원인: 한 그룹 메시지를 수신자별 message row로 저장하면서 모든 row에 같은 room sequence를 부여함.
- fix: 기존 unique 제약과 room advisory lock은 유지하고, 한 전송 transaction 안에서 저장 row마다 `sequence + index`를 부여. 재시도/동시 전송은 기존 room lock과 unique 제약으로 보호.
- GREEN 원문: `group_message_rows_keep_unique_room_sequences_per_recipient`에서 `saveAll` sequence가 `1,2`; groupware 전량 `BUILD SUCCESSFUL`.
- 제약: `ux_messages_room_sequence`를 삭제하거나 완화하지 않았다.

### 7. 본체 채팅 진입/목록 회귀

- RED 원문: 위 우선 재현 결과의 `/#/chat` home fallback 및 `sidebar chat 진입 후 채팅방 링크 수=0`.
- 원인: web deployment가 hash deep-link를 BrowserRouter로 처리했고, 본체 목록 controller가 DIRECT 전용 `otherParticipant` 변환을 GROUP/SYSTEM/보존 방에도 적용해 전체 목록을 실패시킬 수 있음.
- fix: 기존 `/#/chat` hash가 있으면 hash router를 선택하고, canonical web path는 browser router를 유지. 목록 응답은 DIRECT만 상대 profile을 조회하며 GROUP/SYSTEM은 room 자체를 반환해 기존 방 목록을 보존.
- GREEN 원문:

```text
本체 hash-entry contract: 2 tests passed
ChatRoomControllerTest: BUILD SUCCESSFUL
ChatRoomPage.fix1 + hash-entry focused: 8 tests passed
```

- 본체 불변식: `clients/desktop`의 chat API/페이지 동작은 변경하지 않고 router selection만 호환 보정했다. roomCode URL, 메시지 REST, SSE endpoint 계약은 유지했다.

### 불변식 ①~⑤ 보증

1. **① 7건 종료**: 각 RED의 원인에 대응하는 코드와 회귀 테스트를 추가했다. 실제 공유 스택의 재배포 후 라이브 재검증은 남겼다.
2. **② 본체 앱 채팅**: `/#/chat` hash entry, 기존 room list, room open/message/SSE 경로를 보존하는 focused tests와 목록 controller regression을 통과했다. `clients/desktop` build도 성공했다.
3. **③ 기존 통과 유지**: 비참여자 403 권한 코드는 건드리지 않았고, UUID를 화면/URL에 추가하지 않았으며, 직급 정렬 로직은 유지했다. 관련 backend full tests 성공.
4. **④ 퇴사자 숨김·기록 보존**: directory는 termination 및 accounts.enabled 교차 필터만 적용하고, chat room/message는 삭제하지 않는다. 기존 방 목록은 방 유형별 응답으로 보존한다.
5. **⑤ unique 제약 보존**: `ux_messages_room_sequence` 및 동시 room lock을 유지하고 row별 고유 sequence를 부여했다.

### 전량 테스트

```text
clients/internal-chat-desktop: npm test
Test Files 4 passed / Tests 20 passed

clients/desktop: npm run typecheck
Exit code: 0; typecheck:real-qa 51 passed

clients/desktop: npm run build
Exit code: 0

clients/desktop: npm test
Exit code: 1
기존 SlipFormPage.test.tsx 1건 실패:
expected 2026-08-16, received 2026-08-17 (line 1660)
채팅 변경과 무관한 날짜 fixture 실패이며 수정하지 않았다.

:services:groupware-service:test --no-daemon
BUILD SUCCESSFUL

:services:user-service:test --no-daemon
BUILD SUCCESSFUL

:services:auth-service:compileJava :services:user-service:compileJava --no-daemon
BUILD SUCCESSFUL
```

### 남긴 것

- shared stack을 이 브랜치로 빌드·재기동하지 않았다. PM이 브랜치 backend와 desktop build를 올린 뒤 라이브 QA에서 7개 원문 시나리오를 재실행해야 한다.
- `clients/desktop` 전체 `npm test`의 날짜 fixture 1건은 선재·범위 밖 실패로 남겼다. 이 실패가 해소되기 전 전체 test suite를 green이라고 보고하지 않는다.
- 동일 직급 내 정렬, 그룹 안읽음 실측 결과는 sequence fix 후 라이브 데이터로 재검증해야 한다.
