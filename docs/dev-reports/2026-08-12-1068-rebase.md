# PR #1172 rebase 검증 보고서

- 일자: 2026-08-12
- 워크트리: C:\dev\Samhan-Public\.claude\worktrees\w1068
- 브랜치: feat/1068-slip-header-autofill
- rebase 후 HEAD: 6afe95109
- 기준: origin/main (2d5374cd0)
- 범위: rebase, RED-A, main 변경과의 의미 회귀, 금액 외 전량 테스트
- 제외: 전잔·후잔·원장 조회 및 금액 결함 3건

## 1. 결론

origin/main을 fetch한 뒤 rebase했다. 충돌은 없었고, PR의 5개 커밋은 git range-diff에서 모두 '='로 대응되었다. 금액 코드와 금액 테스트는 수정하지 않았다.

rebase 후 PartnerMasterLoadIT는 통과했다. 따라서 이번 CI RED의 원인은 사용자 제공 전제대로 낡은 기대값이었다.

다만 공유 스택만 실행 중인 환경에서 로그인하면 dev_master.last_login_at을 쓰게 되므로, 격리 DB 실서비스 라이브 QA는 실행하지 않았다. 대신 DB를 전혀 사용하지 않는 로컬 renderer mock 스펙 5건만 보조 검증했다. 이를 실서비스 라이브 QA 결과로 간주하지 않는다.

## 2. RED-A

### rebase 전 재현

실행 명령:

    .\gradlew.bat :services:partner-service:test --tests "*PartnerMasterLoadIT" --no-daemon --rerun-tasks --no-build-cache --no-parallel

결과: 8 tests completed, 1 failed.

    PartnerMasterLoadIT.java:104
    expected: 7253
    but was: 7255

### rebase

    git fetch origin main
    git rebase origin/main

rebase 충돌 0건. origin/main에 포함된 주요 변경은 다음과 같다.

- 6a219fa8a — #1166 제품구분 + 주문 40% 규칙
- b1f3a08ec — #1169 D-G7 정산서 확정 취소 경로
- 72cab52eb — #1171 끊긴 참조의 재고 잔고 관용 처리

### rebase 후

동일한 PartnerMasterLoadIT 명령을 재실행했다. 결과는 8 tests completed, 0 failures, 0 errors, 0 skipped이다.

현재 정본 테스트 기대값은 7255로 동기화되어 있다.

## 3. 제거·변경 목록

### 충돌 해소로 제거·변경된 파일

없다. 충돌 자체가 0건이므로 충돌 선택으로 삭제되거나 수정된 파일은 0개다. PR 커밋의 내용 보존은 git range-diff da09abcec..35af40230 origin/main..HEAD에서 5개 커밋 모두 '='로 확인했다.

### main에서 새로 포함·변경된 영역

- #1166: desktop 상품 catalog/mock, product-service 상품구분, partner-order 주문 40% 계산·확정 경로 및 관련 migration/QA
- #1169: groupware/accounting 정산서 확정·취소와 기준일 잠금, 관련 migration/QA
- #1171: inventory 잔고의 끊긴 참조 관용 처리와 desktop 재고 화면/QA
- partner 정본 테스트: PartnerMasterLoadIT의 행수 기대값 7253 → 7255

특히 PR과 직접 겹치는 desktop clients/desktop/src/renderer/api/mock.ts는 main 변경을 유지한 상태로 rebase되었고, 상품·partner-order·재고 관련 테스트를 별도 실행해 의미 회귀를 확인했다.

## 4. 금액 변경 금지 확인

이번 작업에서 금액 관련 구현을 수정하지 않았다.

- 전잔·후잔·원장 조회 코드: 변경 없음
- SOL이 보고한 금액 결함 3건: 변경 없음
- rebase 충돌 해소용 금액 코드 편집: 없음

금액 산식, 동일일 순서, 금액 정본, correlation key의 판정은 하지 않았다.

## 5. rebase 후 검증

### Desktop

clients/desktop에서 fresh Vitest JSON 결과:

    661 suites / 661 passed
    2,217 tests / 2,215 passed / 0 failed / 2 skipped

추가 결과:

- npm run typecheck: 통과
- npm run lint: 0 errors, 163 warnings
- npm run build: 통과

### backend 모듈

모든 명령은 --rerun-tasks --no-build-cache --no-parallel 조건으로 모듈별 fresh 실행했다.

| 모듈 | tests | failed | errors | skipped |
|---|---:|---:|---:|---:|
| accounting-service | 1,895 | 0 | 0 | 10 |
| partner-service | 339 | 0 | 0 | 0 |
| partner-auth-service | 80 | 0 | 0 | 0 |
| partner-order-service | 533 | 0 | 0 | 0 |
| dc-config-service | 79 | 0 | 0 | 0 |
| product-service | 781 | 0 | 0 | 0 |
| inventory-service | 629 | 0 | 0 | 1 |
| groupware-service | 268 | 0 | 0 | 0 |
| 합계 | 4,604 | 0 | 0 | 11 |

accounting의 1,895건은 이전 1,870건을 재사용한 수치가 아니다. main의 #1169 반영 후 현재 HEAD에서 다시 집계된 수치다.

처음 여러 모듈을 하나의 Gradle 프로세스로 동시에 실행한 batch에서는 stale classpath/프로세스 간섭성 실패가 발생했다. 이를 성공으로 숨기지 않고 각 모듈을 독립 fresh 실행했다. 독립 실행의 partner-order 전체 결과는 533 tests, 0 failures, 0 errors이다.

## 6. RED-B 보존 점검

현재 HEAD에서 통과한 자동화/renderer 검증으로 다음 회귀는 발견되지 않았다.

- product/partner-order 영역과 헤더 자동채움 영역의 테스트 통과
- 거래처 master 행수 7,255 정합
- 0원과 조회 실패의 UI 문자열 분리
- accounting/partner 장애 시 fail-open 저장 경로 관련 mock renderer 5 tests
- 거래처 없는 화면 및 UUID 비노출 관련 mock renderer test
- accounting fingerprint/TC-V3 및 격리 DB 실제 row 검증은 금액 외 라이브 QA 증거에 의존하므로 아래 제한을 적용했다.

## 7. 라이브 QA 제한 및 보조 실행

공유 Docker 스택(samhan-*)만 현재 실행 중이었다. 사용자 지시대로 그 스택에는 로그인하지 않았다. 이전 격리 DB와 실제 서비스 프로세스는 이미 종료되어 있었고, 이 워크트리에는 해당 격리 환경을 재기동하는 재현 가능한 런처가 없었다.

실행한 보조 검증:

- 실행 위치: clients/desktop
- 실행기: Playwright 직접 실행
- 브라우저: headless Chromium 1217
- 스펙 디렉터리/파일: playwright/2026-08-12-1068-sol-review-real-qa/2026-08-12-1068-sol-review-real-qa.spec.ts
- 캡처 resolver: resolveQaShotsDir(...)
- 캡처: docs/qa/2026-08-12-1068-sol-review-real-qa/_local/
- 결과: 5 passed
- 사용 DB: 없음

이 5건은 mock renderer 보조 결과이며, 격리 PostgreSQL과 실제 partner/accounting/slip 서비스의 라이브 QA를 대체하지 않는다. 로컬 renderer는 검증 후 종료했고 port 5175 listener가 남지 않은 것을 확인했다. 공유 포트와 공유 컨테이너는 중지하지 않았다.

## 8. 작업 트리 및 권한

- commit: 하지 않음
- push: 하지 않음
- PR 조작: 하지 않음
- rebase: 수행함
- 최종 작업 트리: 보고서 파일 추가 외 dirty 변경 없음

PM이 이후 격리 DB 런처를 제공하거나 재기동할 수 있을 때, 위 -real-qa 스펙을 실제 서비스/격리 DB에 연결해 자동채움 5필드, 사용자 수정값, 장애 fail-open, partnerless, fingerprint 및 TC-V3 mutation RED를 다시 증명해야 한다.

