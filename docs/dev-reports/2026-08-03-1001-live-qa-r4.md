# #1001 거래처별 원장 — 라이브 QA R4

- 일시: 2026-08-03 KST
- 대상: PR #1061 / `feat/1001-ledger-spec-rest` / HEAD `28815b3c9`
- 운영 조건: PM 배포 스택 사용. 재배포·코드 수정·커밋·push·기존 데이터 변경 없음.

## 진행 로그

### 시작

- 보고서 파일을 QA 시작 전에 생성함.
- 다음: read-only SQL로 조회 조건의 실제 원장 라인 수 확인.

### ① 발화 조건 실측 — read-only SQL

- 제외 기준: `2026/06/24-901/-902`, `2026/08/03-1/-2/-3/-4`, `2026/08/03-QA-1013-%`를 제외 대상으로 기록했다. `accounting_db.journals`의 `source_type='SLIP'` + `2026-06-19 system` 29건도 업무 금액에서 제외한다.
- 최초 후보 `partnerCode=P-2026-0017`은 `slip_db.slips`에서 해당 코드·라인이 **0건**이었다. PM 지시에 따라 이 조건으로 조회하지 않았다.
- 라인이 확인되는 조건으로 전환: `partnerCode=P-2026-0018`, 기간 `2026-07-01 ~ 2026-07-31`.
- read-only SQL 결과: 활성 판매전표 131건, 활성 라인 250건, `slip_lines.line_total` 합계 `50,979,401원` (2026-07-16~2026-07-27).
- 같은 조건의 직접 SQL 집계는 화면용 집계와 자료원이 다를 수 있으므로, 이후 실제 GUI에서 집계와 상세를 같은 화면으로 확인한다.
- 발화 조건 판정: **조회 가능(라인 250건)**.

### 브라우저 전환

- 인앱 브라우저 시도 결과 원문: `No browser is available`.
- 지시된 대체 경로로 `clients/desktop`의 Vite renderer를 `VITE_MOCK_MODE=0`, `VITE_API_BASE_URL=http://127.0.0.1:8080`, `VITE_APP_VERSION=2026/08/03-1`, `127.0.0.1:5178`에서 실행했다.
- HashRouter는 `http://127.0.0.1:5178/#/accounting/partner-ledger`에서 정상 렌더됨. 해시 없는 `/accounting/partner-ledger`는 로그인 경로로 수렴하여 사용하지 않았다.
- 실 로그인: `dev_master` / MASTER.

### ① 발화 조건 재선정 — 업무 분개 라인

- P-2026-0018 / 2026-07 조건은 SQL상 250개 판매 라인이지만 상태가 `DRAFT/CANCELED`였고, GUI 상세가 0건이었다. 업무 원장 발화 조건으로 부적합하여 판정에 사용하지 않았다.
- 제외 기준을 적용한 `accounting_db` read-only SQL에서 `partner_id`가 P-2026-0005에 대응하는 `P-2026-0005`, 기간 `2026-07-01 ~ 2026-07-31`을 선정했다.
- 실측: 분개 38건, 분개 라인 73건, 차변 합계 5,661,070원, 대변 합계 5,551,070원. 2026-06-19 `source_type='SLIP'`/`created_by='system'` 잔재와 지정 QA 번호는 제외했다.
- 발화 조건 판정: **조회 가능(업무 분개 라인 73건)**.

### ② 실 GUI 집계·상세 — 같은 화면

- 필터 입력: 화면에 보이는 사업자번호 `1653510155`가 아니라 API 계약값 `P-2026-0005`를 입력했다.
- 집계 화면: 거래처명 `대구HVAC솔루션`, 화면 식별자 `1653510155`, 매출 합계 `—`, 수금 합계 `277,000`, 채권 잔액 `-277,000`.
- 해당 집계 행을 실제 클릭한 뒤 같은 화면의 상세 원장에 다음 3개 라인이 표시됐다: `2026/07/04-28` 대변 120,000원, `2026/07/04-31` 대변 80,000원, `2026/07/07-1` 대변 77,000원. 상세 합계 대변 `277,000원`, 기말 잔액 `-277,000원`.
- 매출 합계가 공란인 동안 상세에 표시된 것은 매출 라인이 아니라 수금(`입금보고서`) 라인이고, 수금 집계는 277,000원으로 채워져 있었다.
- 같은 화면 증거: `docs/qa/1001-ledger-real-qa/05-aggregate-detail-p0005-same-screen.png`.

### ③ 행 클릭·인쇄

- 행 클릭: PASS. Step 2 원장 표와 3개 라인이 렌더됐다.
- 인쇄 미리보기 버튼 클릭: PASS. 실제 HashRouter 인쇄 화면 `#/print/partner-ledger?partnerCode=P-2026-0005&from=2026-07-01&to=2026-07-31`이 열렸고, 인쇄 양식에 동일한 3개 라인·합계 277,000원·기말 잔액 -277,000원이 표시됐다.
- 인쇄 증거: `docs/qa/1001-ledger-real-qa/06-print-p0005-opened.png`.

### ④ 직전 결함 재현 판정

- **정확한 결함(집계 금액 전부 공란 ↔ 상세 245,612,215원): 재현하지 못함.** 이번 발화 조건의 상세 금액은 245,612,215원이 아니라 277,000원이며, 매출 집계만 공란이고 수금 집계는 정상 표시됐다.
- P-2026-0018 조건도 상세 자체가 0건이어서 245,612,215원 대조 조건이 성립하지 않았다.
- 따라서 이번 라운드의 실 GUI 결과만으로 해당 결함 없음/있음을 단정하지 않고, **해당 금액 결함 미재현(환경 데이터 불일치)**으로 판정한다.

### ⑤ 새 결함

- 재현 가능한 새 제품 결함: **없음**.
- 관찰 사항: P-2026-0005에서 매출 라인이 없는 기간에는 매출 합계가 `—`로 보이고 수금 277,000원은 집계·상세·인쇄에 일관되게 표시됐다. 이는 이번 데이터의 문서 종류와 일치한다.
- P-2026-0018은 SQL에 250개 `DRAFT/CANCELED` 판매 라인이 있으나 GUI 거래 내역에는 노출되지 않았다. QA 잔재/업무 유효 상태 여부를 이 라운드에서 제품 결함으로 확정하지 않는다.

### ⑥ 새 파일 목록

- `docs/dev-reports/2026-08-03-1001-live-qa-r4.md`
- `docs/qa/1001-ledger-real-qa/01-aggregate-p0018.png`
- `docs/qa/1001-ledger-real-qa/02-aggregate-detail-same-screen.png`
- `docs/qa/1001-ledger-real-qa/03-print-opened.png`
- `docs/qa/1001-ledger-real-qa/04-aggregate-p0005.png`
- `docs/qa/1001-ledger-real-qa/05-aggregate-detail-p0005-same-screen.png`
- `docs/qa/1001-ledger-real-qa/06-print-p0005-opened.png`

참고: 작업 트리의 `clients/desktop/playwright/1001-live-qa/capture.mjs`는 이번 GUI 실행에서 생성·수정하지 않았으며, 기존 미추적 파일로 보존했다. `_local` 디렉터리는 캡처 경로 가드 확인 중 생성된 뒤 비우고 제거했다.
