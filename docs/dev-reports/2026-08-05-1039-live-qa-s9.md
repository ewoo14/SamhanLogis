# 이슈 #1039 / PR #1045 — S9 실서버 라이브 QA

- 실행일: 2026-08-05 (Asia/Seoul)
- 워크트리: `C:/dev/Samhan-Public/.claude/worktrees/t1039`
- 브랜치: `feat/1039-provisional-dispatch`
- HEAD: `02f5e2a7400bf1a8328d23ef1c9f8377a22f66d6`
- 원칙: mock OFF, 컨테이너 조작 금지, DB 직접 쓰기 금지, 생성 데이터 `QA-1039-` 표식 유지

## 0. 환경 확인

### 0.1 경로·버전·실행 config

| 항목 | 실제 |
|---|---|
| 삼한 renderer | `http://127.0.0.1:18509/` 기동 확인 |
| 아로로지스 renderer | `http://127.0.0.1:18510/` 기동 확인 |
| 실행 config | 삼한 `vite.renderer.dev.config.ts`, 아로로지스 `vite.renderer.dev.config.ts`; `VITE_APP_VERSION=2026/08/05-1039` |
| mock | mock OFF; 실 API 사용 |

실행 포트: 삼한 renderer `18509`, 아로로지스 renderer `18510`. 삼한 renderer는 최초 잘못된 버전 문자열(`2026/08/05-1039-s9`)로 기동 거부되어 `2026/08/05-1039`로 재기동. 인앱 브라우저는 Chromium 설치 후에도 `No browser is available`이 지속되어 실제 Chromium GUI 세션으로 진행.

### 0.2 화면 API 경로와 수신 컨테이너

| 화면/API | 수신 컨테이너 |
|---|---|
| 삼한 `/admin/carriers`, `/admin/dispatch-groups`, `/admin/dispatch-groups/**` | `samhan-api-gateway:8080` → 해당 서비스 라우트 |
| 삼한 `/auth/login` | `samhan-api-gateway:8080` → `samhan-auth-service` |
| 아로로지스 `/auth/admin/login`, `/admin/arologis/dispatch-groups` | `samhan-arologis-service:8097` 직접 |

### 0.3 컨테이너 생성 시각

`docker inspect -f '{{.Created}}'` 실측: gateway `2026-08-04T22:34:18.879154069Z`, slip `2026-08-04T22:13:20.425967767Z`, arologis `2026-08-04T22:13:20.44841083Z`. PM 기준 시각과 일치.

### 0.4 실 백엔드 응답 증거

실 응답: `POST http://127.0.0.1:8080/auth/login` + `dev_manager/${QA_DEV_DEFAULT_PASSWORD}` 성공(`role=MANAGER`, timestamp 실 응답); `GET http://127.0.0.1:8097/actuator/health`=`UP`; `POST http://127.0.0.1:8097/auth/admin/login` + `admin/${QA_AROLOGIS_ADMIN_PASSWORD}` 성공(`role=AROLOGIS_MASTER`). mock 응답 아님.

## ② 배차 그룹 생성

- 클릭 경로: 삼한 로그인 → 인사/배차 메뉴 → `배차 그룹` → `그룹 번호`/`차량 표시명` 입력 → `그룹 생성`
- 기대: `QA-1039-GROUP-S9`가 지정일 그룹으로 생성되고 미전송 상태로 표시
- 실제: 그룹 생성 성공. `QA-1039-GROUP-S9`, 차량 `QA-1039 차량`, 현장 `0건`, 운송사 `운송사 미지정`, 상태 `미전송` 표시
- 캡처: `02-dispatch-group-real-qa.png`
- 판정: PASS

## ③ 현장 담기

- 클릭 경로: `배차 그룹` → 편입 대상 `그룹 1` 선택 → 검색어 `2026/08` 입력 → `검색` → 첫 결과 `그룹에 편입`
- 기대: 가배차를 거치지 않은 활성 INBOUND 전표가 검색 결과에 나타나고 선택 그룹에 편입
- 실제: 실 API `GET /admin/slips/search?q=2026%2F08&limit=20&slipType=INBOUND` 200. `2026/08/03-4` 등 활성 구매전표 4건 표시. 첫 항목 클릭 후 그룹 현장 `0건→1건`, 편입 API 200
- 캡처: `03-inbound-search-real-qa.png`
- 판정: PASS (PM 실측 46건 전체 목록이 아니라 검색어 일치 4건을 확인)

## ④ 운송사 설정

- 클릭 경로: 배차 그룹 화면에서 활성 운송사 목록 확인 및 인사 > 운송사 목록 진입 시도
- 기대: 시드 AROLOGIS 및 신규 `QA-1039-NON-ARO` 운송사를 조회/등록하고 그룹에 지정
- 실제: 실 API `GET /admin/carriers`가 403으로 응답. 배차 그룹 화면에 `활성 운송사 0개`, `운송사 미지정` 표시. 현재 `dev_manager` 권한으로 운송사 목록 메뉴도 노출되지 않아 신규 운송사 생성·활성/비활성 전환·그룹 지정 불가
- 캡처: `02-dispatch-group-real-qa.png` (운송사 0개/미지정)
- 판정: BLOCK — 권한/실 API 403. 기능 결함인지 QA 계정 권한 누락인지는 미판정

## ⑤ 선택 전송

- 클릭 경로: 운송사 지정 단계 미완료로 전송 버튼 클릭 불가
- 기대: `is_arologis=false` 그룹은 전송 시도 자체가 없고, `is_arologis=true`만 전송
- 실제: 활성 운송사 0개 및 운송사 미지정 상태여서 전송 조건 미충족. false 운송사 생성 및 시도 없음 여부를 GUI로 검증하지 못함
- 캡처: 없음 (진입 차단)
- 판정: BLOCK / 미판정 — 운송사 권한 차단

## ⑥ 아로로지스 수신

- 클릭 경로: 아로로지스 renderer `18510` 접속 → 관리자 로그인 화면 → `admin`/`${QA_AROLOGIS_ADMIN_PASSWORD}` 입력 → `로그인`
- 기대: 로그인 후 수신 배차 그룹 화면에서 전송된 그룹을 읽기 전용으로 표시
- 실제: 실 `POST http://localhost:8097/auth/admin/login`은 200 응답(로그인 응답 본문 실 확인)이나 GUI 후속 흐름은 `로그인 중 오류가 발생했습니다`를 표시하고 수신 화면 진입 실패
- 캡처: `06-arologis-login-real-qa.png`
- 판정: BLOCK — 로그인 API 200 이후 앱 인증 후속 처리 실패. 수신 그룹 중복/읽기 전용 여부 미판정

## 운송사 목록

## ① 가배차 8모드 (마지막 시도)

- 클릭 경로: 삼한 GUI에서 배차 메뉴의 `가배차리스트` 클릭 시도
- 기대: 기존 전표에서 8모드 발화 및 결과 표시
- 실제: 메뉴 클릭 locator가 30초 타임아웃되어 화면 진입·8모드 발화 결과를 확인하지 못함. PM의 A안(기존 전표에는 `source_warehouse_code` 없음) 사유와 일치 가능성은 있으나 실 GUI 결과로 확정하지 않음
- 캡처: 없음
- 판정: 미판정 — GUI 세션 타임아웃

## 이 라운드가 보지 않은 것

- 운송사 목록 등록·수정·비활성화 및 비아로로지스(`is_arologis=false`) 운송사 생성은 `/admin/carriers` 403으로 보지 못함.
- `is_arologis=false` 그룹 전송 시도 자체가 없는지, `is_arologis=true` 선택 전송 성공 여부를 보지 못함.
- 같은 그룹 2회 전송 멱등성 및 아로로지스 중복 생성 여부를 보지 못함.
- 아로로지스 수신 목록/상세의 편집·재분류 차단을 로그인 후속 오류로 보지 못함.
- 운송사 미지정·이미 전송됨·비활성 운송사·전표 보유 그룹 삭제 각각의 차단 사유 문구를 보지 못함. 다만 미지정 상태에서 전송 버튼이 나타나지 않는 것까지는 확인.
- 판매전표 가배차 결과 편입(③은 INBOUND 검색 편입만 확인) 및 8모드 세부 결과.

## 최종 요약

- PASS: ② 그룹 생성, ③ INBOUND 검색 편입
- BLOCK: ④ 운송사 권한/API 403, ⑤ 선택 전송, ⑥ 아로로지스 GUI 인증 후속
- 미판정: ① 가배차 8모드, 멱등성, 읽기 전용, 차단 사유 상세
- 신규 데이터: `QA-1039-GROUP-S9`, 차량 `QA-1039 차량`, 편입 전표 1건. 삭제하지 않음.
- 신규 파일: `docs/dev-reports/2026-08-05-1039-live-qa-s9.md`, `docs/qa/1039-s9/screenshots/00-login-real-qa.png`, `02-dispatch-group-real-qa.png`, `03-inbound-search-real-qa.png`, `06-arologis-login-real-qa.png` 및 실행 로그 디렉터리 `docs/dev-reports/qa-runtime-1039/`.
