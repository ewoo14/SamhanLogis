# S1d 구형 baseline · 실 시트 sync — 정찰

조기 PR 개설용 자리표시. Codex 정찰 결과를 이 파일에 누적한다.

- 2026-08-02 정찰: `docs/dev-reports/2026-07-13-773-s5-purchase-render-modelname.md:55-56` 원문은 S1d를 `구형 baseline·실 시트 sync·Google 자격`으로만 명시하며, 당시 #773 완주 범위 밖의 불변 잔여로 분리했다.
- 2026-08-02 정찰: #875의 2026-07-29 기록과 #777 결정에 따르면 `구형 baseline`은 구형 제품에도 인상 전/후 가격을 둘지 검토하던 과거 표현이며, 개발책임자가 `구형 인상 미적용`으로 확정해 현행은 구형을 단일가로 판정한다.
- 2026-08-02 정찰: 따라서 **S1d 구형 baseline은 구형 제품의 인상 전/후 기준가격 여부를 판정하는 잔여였고, 결론은 구형에는 인상을 적용하지 않아 단일 기준가격만 사용한다는 것**이다.
- 2026-08-02 정찰: 이 워크트리와 공유 메인 디렉터리에는 `.gitignore` 대상인 루트 `.clasp.json` 및 `tools/legacy-gas/종합견적서-live/`가 현재 남아 있지 않았지만, 저장소 기록상 바인딩 `rootDir`은 `tools\\legacy-gas\\종합견적서-live`였다.
- 2026-08-02 실측: `npx --yes @google/clasp list` 읽기 전용 조회가 성공해 27개 GAS 프로젝트 중 라이브 `종합견적서` 프로젝트가 실제로 존재하고 현재 자격으로 조회됨을 확인했다(스크립트 ID는 보고서에 기록하지 않음).
- 2026-08-02 실측: 격리 임시 경로에서 `npx --yes @google/clasp pull`만 실행해 라이브 `종합견적서`의 8개 파일(`Code.js`, `index.html`, manifest 및 자산 HTML)을 읽기 전용 원격 조회로 확보했으며 `clasp push`는 실행하지 않았다.
- 2026-08-02 라이브 pull: 현행 라이브 GAS는 `종합 견적서` 원본 스프레드시트를 열며, `getOldProducts_()`가 `구형` 탭의 2행부터 A:I를 값+수식으로 읽는다.
- 2026-08-02 라이브 pull: `구형` 판정 입력은 A 품명, B 모델코드, C 단위, D 출고가, F 납품가, F 수식의 `$I$1` 포함 여부(구형 50% 고정DC 판정), H 비고, I 규격이다.
- 2026-08-02 라이브 pull: 인상 가격 함수는 `홈멀티_단가인상`, `상업멀티_단가인상`, `상업멀티 구성_단가인상`, `싱글 세트_단가인상`, `싱글 구성품_단가인상`을 읽지만 `구형_단가인상`은 읽지 않는다.
- 2026-08-02 실 시트 read: Service Account의 `spreadsheets.readonly`로 원본 `종합 견적서`를 읽었고, 기본/인상 쌍 5종 10탭과 단일 `구형` 탭이 모두 실재했다.
- 2026-08-02 실 시트 read: 실 `구형` 탭은 43행×9열이며, 2행 이후 품명 A·모델코드 B·출고가 D가 각각 41건, 납품가 F가 39건, F 수식이 `$I$1`을 참조해 구형 50% 고정DC로 판정되는 행이 30건이었다.
