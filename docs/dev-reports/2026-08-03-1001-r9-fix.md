# #1001 R9 수정 보고서

## 기준 및 QA 산출물 확인

- [DEV-SEED/단위 fixture] QA commit `ad9c78bb7`의 보고서는 대상 HEAD가 `ce53bc009`였고 standalone accounting jar SHA-256이 `79F0D40BD7BFD7C218CF09E6E6A59237F427DB633BB65FD0D6A86D961A6331BF`라고 기록한다.
- [DEV-SEED/단위 fixture] `git show ce53bc009`에서 `SalesAggregateService.java:111-120`에 R6의 `PartnerLedgerSalesClient.find(... partnerId)` 및 출고 line 합계 적용이 실제 포함되어 있음을 확인했다. 따라서 jar가 ce53 이전이면 하네스 문제지만, ce 기반 jar라면 R6 변경은 선택 거래처 분기에서만 실행되고 전체 집계 분기에는 아직 적용되지 않은 제품 경로 문제다.
- [실데이터/로컬 read-only] partner DB의 대상 거래처는 `partner_code=P-2026-0017`, `biz_no=321-19-10527`, 이름 `원주에어컨공업`이다. 화면은 사업자번호를 표시하는 것이 저장소 규율상 맞지만, 상세 조회 API의 `partnerCode`에는 내부 업무 코드 `P-2026-0017`을 보내야 한다. UUID는 화면/응답에 노출하지 않는다.

## R6 전체 집계 경로 RED

- [DEV-SEED/단위 fixture] 선택 거래처 필터 없이 집계를 호출하고 journals 401 공급가액 20,000,000원과 동일 거래처 출고 slip 품목 합계 12,276,000원을 함께 주입했다. 현재 R6 구현은 `filterPartnerId != null`일 때만 slip 합계를 적용하므로 전체 집계는 20,000,000원으로 남는 것을 재현했다.

```text
SalesAggregateServiceTest > unfilteredAggregateUsesOutboundLedgerSalesForEachPartner() FAILED
    org.opentest4j.AssertionFailedError at SalesAggregateServiceTest.java:248
1 test completed, 1 failed
> Task :services:accounting-service:test FAILED

- [DEV-SEED/단위 fixture] 첫 구현 시 helper를 aggregate 메서드 중간에 삽입해 Java 중괄호 위치가 잘못되었고 compile RED가 발생했다. 수정 전 원문:

```text
SalesAggregateService.java:119: error: illegal start of expression
private void applyLedgerSalesTotal(LocalDate from, LocalDate to,
12 errors
> Task :services:accounting-service:compileJava FAILED

## R6 전체 집계 경로 수정 및 GREEN

- [DEV-SEED/단위 fixture] `filterPartnerId`가 없는 전체 집계에서도 journals로 생성된 각 partnerId마다 동일한 `PartnerLedgerSalesClient` 출고 slip 조회를 수행하고, 응답 line 합계가 있으면 그 거래처의 `salesTotal`만 교체하도록 helper 위치를 바로잡았다. 수금/채권 계산, 거래처 식별자, UUID 비노출은 변경하지 않았다.
- [DEV-SEED/단위 fixture] 수정 후 targeted test GREEN 원문:

```text
> Task :services:accounting-service:test

BUILD SUCCESSFUL in 17s
21 actionable tasks: 2 executed, 19 up-to-date
```

## 19건 전후 집계 원천 측정

- [실데이터/로컬 read-only] 대상 기간 `2026-02-01` 이상 `2026-04-01` 미만의 기존 journals 집계는 19 거래처·292,000,000원이었다. R6 출고 slip 원천을 같은 19 거래처에 제한하면 3 거래처·3 전표·9 라인·58,498,000원이었다.
- [실데이터/로컬 read-only] 전체 slip 상태/기간 측정은 25 거래처로 확장되므로, 이는 대상 19 거래처 전후 비교에 사용하지 않고 후보 확인용으로만 기록한다. 대상 19 거래처에 실제로 대응하는 slip 행은 위 3 거래처이다.

```text
docker exec ... slip_db ... (target 19 partners)
 partner_rows | slip_rows | line_rows | slip_sales_total
--------------+-----------+-----------+------------------
            3 |         3 |         9 |      58498000.00
```

- [실데이터/로컬 read-only] 3개 변경 후보를 journals에서 다시 산출하려던 첫 SQL은 `j.partner_id`가 없는 스키마를 사용해 실패했다. 이는 읽기 전용 조회 오류이며 데이터는 변경하지 않았다.

```text
ERROR:  column j.partner_id does not exist
HINT:  Perhaps you meant to reference the column "jl.partner_id".
```

- [실데이터/로컬 read-only] 분개 스키마를 확인하는 두 번째 조회에서도 `journal_lines.account_id`를 가정했으나 실제 컬럼은 `account_code`였다. 이 조회 역시 읽기 전용이며 변경은 없다.

```text
ERROR:  column jl.account_id does not exist
HINT:  Perhaps you meant to reference the column "jl.account_code".
```

- [실데이터/로컬 read-only] `journal_lines.account_code='401'`로 재산출한 19건의 기존 집계는 292,000,000원이었다. 이 중 출고 slip이 대응하는 3건은 다음과 같다. `0beb...`: 기존 20,000,000원 → slip 12,276,000원, `124a...`: 기존 24,000,000원 → slip 21,575,400원, `e5c6...`: 기존 7,000,000원 → slip 24,646,600원.
- [실데이터/로컬 read-only] 따라서 수정 후 19건 행 수는 19건 그대로이고 총합은 `292,000,000 - 51,000,000 + 58,498,000 = 299,498,000원`이다. 변경 근거가 있는 위 3건만 바뀌며 나머지 16건은 기존 값이 유지된다.

```text
 partner_id                              | slip_rows | line_rows | slip_sales_total
-----------------------------------------+-----------+-----------+------------------
 0beb5a9c-00c1-3b69-aa42-e32bd6dc77d2    |         1 |         2 |      12276000.00
 124a483d-c4e9-37a3-8997-84409dfa2c8d    |         1 |         4 |      21575400.00
 e5c62496-47df-3a07-a3d7-c28fa7123675    |         1 |         3 |      24646600.00
```

## QA jar 및 상세 조회 400 판정

- [DEV-SEED/단위 fixture] 현재 worktree에 남아 있는 `accounting-service.jar`의 SHA-256은 `79F0D40BD7BFD7C218CF09E6E6A59237F427DB633BB65FD0D6A86D961A6331BF`로 QA 보고서의 jar와 일치한다. `git show ce53bc009`에도 R6 변경이 존재하므로 QA jar가 R6 이전인 경우가 아니다. R6 전체 목록 미반영은 제품의 무필터 집계 경로 문제였다.
- [DEV-SEED/단위 fixture] 화면 행 클릭은 표시용 `bizNo`가 아니라 `row.partnerCode`를 `handleSelectPartner`에 전달하고, API는 그 값을 `partnerCode` query parameter로 보낸다. 따라서 `P-2026-0017`을 요청해야 하며, `3211910527`을 API에 수동 입력한 QA 재현은 화면 표시값을 내부 조회키로 오인한 입력이다.
- [실데이터/로컬 read-only] partner 조회 결과는 `partner_code=P-2026-0017`, `biz_no=321-19-10527`, 이름 `원주에어컨공업`이다. 사업자번호 형태를 표시하고 내부 partner code로 조회하는 현재 계약은 UUID를 노출하지 않으며, 해당 400을 없애기 위해 검증을 완화하지 않았다.

```text
SHA256  79F0D40BD7BFD7C218CF09E6E6A59237F427DB633BB65FD0D6A86D961A6331BF
```

## 수정 후 해당 테스트 GREEN

- [DEV-SEED/단위 fixture] 전체 회계 테스트 스위트는 실행하지 않고 `SalesAggregateServiceTest` 클래스만 실행했다.

```text
> Task :services:accounting-service:test

BUILD SUCCESSFUL in 15s
21 actionable tasks: 1 executed, 20 up-to-date
```

## 최종 파일/정적 검증

- [DEV-SEED/단위 fixture] 상세 경로 근거는 `clients/desktop/src/renderer/routes/PartnerLedgerPage.tsx:278`, `:496`, `:501`, `:517`에 있다. `onClick`은 `row.partnerCode`를 선택하고 표시 칸은 `row.bizNo`를 숫자만 남겨 표시한다. 요청 생성은 `clients/desktop/src/renderer/api/partnerLedgerApi.ts:254`에서 `partnerCode`를 query parameter로 보낸다.
- [DEV-SEED/단위 fixture] `git diff --check`는 출력 없이 성공했다. 개별 diff numstat은 `SalesAggregateService.java +20/-10`, `SalesAggregateServiceTest.java +21/-0`이다.
- [DEV-SEED/단위 fixture] R9 신규 파일은 `docs/dev-reports/2026-08-03-1001-r9-fix.md` 1개다. 구현 변경은 기존 `SalesAggregateService.java`와 기존 `SalesAggregateServiceTest.java`에만 했다. `clients/desktop/playwright/1001-live-qa/`는 작업 시작 전부터 존재한 QA 산출물이며 이번 라운드 신규 파일로 세지 않았다.
```
```
