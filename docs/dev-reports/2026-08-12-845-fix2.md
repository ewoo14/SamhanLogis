# PR #1158 (#845) fix2

## 측정 0 — 범위·기준 고정

- 대상: D-1 금액, D-2 다라인 PDF 표 마감, D-3 fallback legacy 8열 계약
- 불변식: 합계 = 공급가액 + 부가세, 모든 PDF 페이지 표 폐쇄, legacy 열 8개 구성·순서·문구 일치
- 회귀 게이트: 실 데이터 70건 열림 / 0건 막힘, UUID 화면·PDF 0건, DS-4 활성화 게이트 무변경
- 실행 제한: git 명령 미사용, 공유 Docker 스택 미사용

집계:

- passed: 0
- skipped: 0
- failed: 0

## 측정 1 — RED-first

### D-1 RED

실행 명령:

```text
cd clients/desktop
npm exec vitest run src/renderer/print/approvalSlipLineLink.test.ts
```

실행 원문:

```text
FAIL  src/renderer/print/approvalSlipLineLink.test.ts
  × D-1 fix2: 합계는 저장된 공급가액 별칭이 아니라 공급가액과 부가세의 합이다
    AssertionError: 112,233 + 11,223 = 123,456
    expected: "123456"
    received: "112233"
```

판정: failed 1 (결함 재현 RED)

### D-2 RED

실행 명령:

```text
cd clients/desktop
npm exec vitest run src/renderer/print/DocumentRenderer.test.tsx -t "D-2 fix2"
```

실행 원문:

```text
FAIL  src/renderer/print/DocumentRenderer.test.tsx
  × D-2 fix2: 반복 표는 Chromium 페이지 분할에서도 행 하단 테두리를 보존하는 border model을 사용한다
    AssertionError: expected source to match /border-collapse:\s*separate/
    received: border-collapse: collapse;
```

판정: failed 1 (결함 재현 RED)

### D-3 재대조

격리 복제의 한글 보존 확인 및 원문 대조는 `?` 화면을 근거로 사용하지 않는다. 개발책임자가 재확정한 legacy 원문은 다음과 같다.

```text
품목 | 모델명 | 규격 | 수량 | 공급가액 | 부가세 | 합계 | 비고
```

현재 fallback 렌더 계약은 이 8열과 일치하므로, D-3은 변경 전부터 동작 RED가 발생하지 않는 상태다. 이를 임의 변경하지 않고 회귀 테스트로 고정한다.

집계:

- passed: 0
- skipped: 0
- failed: 2 — D-1, D-2 RED

실제 RED 실행 원문:

```text
RUN v2.1.9 C:/dev/Samhan-Public/.claude/worktrees/w1158/clients/desktop
src/renderer/print/approvalSlipLineLink.test.ts (5 tests | 2 failed)
  × 참조 출고전표의 SlipDetail.lines를 인쇄 품목 행으로 보여준다
    - Expected lineTotal: "2000"
    + Received lineTotal: "1818"
  × D-1 fix2: 합계는 저장된 공급가액 별칭이 아니라 공급가액과 부가세의 합이다
    expected "123456", received "112233"
Test Files 1 failed; Tests 2 failed | 3 passed (5)

src/renderer/print/DocumentRenderer.test.tsx (22 tests | 1 failed)
  × D-2 fix2: 반복 표는 Chromium 페이지 분할에서도 행 하단 테두리를 보존하는 border model을 사용한다
    expected source to match border-collapse: separate; received border-collapse: collapse;
Test Files 1 failed; Tests 1 failed | 21 passed (22)
```

## 측정 2 — 최소 수정 후 단위 GREEN

실행 명령:

```text
cd clients/desktop
npx vitest run src/renderer/print/approvalSlipLineLink.test.ts src/renderer/print/DocumentRenderer.test.tsx
```

실행 원문:

```text
✓ src/renderer/print/approvalSlipLineLink.test.ts (5 tests)
✓ src/renderer/print/DocumentRenderer.test.tsx (22 tests)
Test Files 2 passed (2)
Tests 27 passed (27)
```

집계:

- passed: 27
- skipped: 0
- failed: 0

## 측정 3 — 인쇄 관련 전량

실행 명령:

```text
cd clients/desktop
npx vitest run src/renderer/print
```

실행 원문:

```text
Test Files 29 passed (29)
Tests 253 passed (253)
Duration 8.42s
```

stderr에는 기존 React Router v7 future flag warning만 있었고 테스트 실패는 없었다.

집계:

- passed: 253
- skipped: 0
- failed: 0

## 측정 4 — 격리 실사용 경로·실데이터 70건

격리 포트 `40275(renderer)`, `40280(gateway)`, `40232(postgres)`에서 기존 `reconvergence-live.cjs`를 실행했다. 공유 Docker 스택은 사용하지 않았다.

실행 원문:

```text
{"loginHttp":200,"tokenPresent":true,"role":"MASTER"}
{"scenario":"reference-12-lines","detailRows":12,"expectedRows":12,"headers":["품목","모델명","규격","수량","공급가액","부가세","합계","비고"],"firstRow":["???_6HP ???","AJ060MXHNBC1","?? 1w","1","112,233","11,223","123,456","-"],"uuidVisible":false}
{"scenario":"reference-48-lines","detailRows":48,"expectedRows":48,"headers":["품목","모델명","규격","수량","공급가액","부가세","합계","비고"],"firstRow":["???_6HP ??? QA-1","AJ060MXHNBC1-QA1","?? 1w","1","112,233","11,223","123,456","-"],"uuidVisible":false}
{"scenario":"no-reference","detailRows":0,"expectedRows":0,"uuidVisible":false}
{"scenario":"broken-reference","detailRows":0,"expectedRows":0,"uuidVisible":false}
{"scenario":"non-default-connected","detailRows":0,"expectedRows":0,"uuidVisible":false}
{"allApprovalPaths":{"listHttp":200,"total":70,"opened":70,"blocked":0,"blockedDocs":[]}}
{"pageErrors":[]}
```

판정: D-1 화면 첫 품목은 `112,233 + 11,223 = 123,456`으로 원본 금액 불변식을 만족한다. 무참조·끊긴 참조·비기본 연결 문서의 legacy DOM은 유지됐다.

집계:

- passed: 10 — 로그인, 12행 금액, 48행 금액, 무참조, 끊긴 참조, 비기본 연결, 3개 UUID 0, pageErrors 0, 70건 열림/막힘
- skipped: 0
- failed: 0

## 측정 5 — fresh PDF 페이지별 표 마감·금액·UUID

`reconvergence-live.cjs`가 생성한 fresh PDF를 PyMuPDF로 전 페이지 읽고, 각 표 셀의 마지막 사각형 하단 좌표와 페이지 높이를 대조했다.

실행 원문:

```text
reference-12-lines.pdf|PAGES=1
PAGE=1|HEADER=True|UUID=False|LAST_TABLE_Y1=745.88|BOTTOM_GAP=97.01
reference-48-lines.pdf|PAGES=3
PAGE=1|HEADER=True|UUID=False|LAST_TABLE_Y1=817.12|BOTTOM_GAP=25.76
PAGE=2|HEADER=True|UUID=False|LAST_TABLE_Y1=824.62|BOTTOM_GAP=18.26
PAGE=3|HEADER=True|UUID=False|LAST_TABLE_Y1=516.38|BOTTOM_GAP=326.51
```

48라인 PDF 1·2·3페이지 모두 표 마지막 셀 사각형이 페이지 경계보다 각각 25.76pt, 18.26pt, 326.51pt 위에서 닫혔다. 중간 페이지 마지막 행 테두리 잘림은 재현되지 않았다. PDF 텍스트에서도 첫 행 합계 `123,456`이 확인됐다.

집계:

- passed: 10 — 12라인 PDF 1페이지, 48라인 PDF 3페이지, 페이지별 3/3 표 마감, 헤더 3/3, D-1 PDF 금액, UUID 0
- skipped: 0
- failed: 0

## 측정 6 — D-3 한글 원문 검증 및 legacy 대조

격리 PostgreSQL의 현재 복제본에서 `name ~ '[가-힣]'`를 직접 조회했다.

실행 원문:

```text
approval_templates|8|0|?????
document_templates|330|0|ADVQA CONSOLE 58184-1785156611147-51bd6c76-9e35-4d24-8d32-0812c245351c
```

복제본 자체가 이미 한글을 보존하지 못한 상태이므로 `?` 값은 legacy 문구 판정에 사용하지 않았다. 개발책임자가 재확정한 공유 원문인 `품목 | 모델명 | 규격 | 수량 | 공급가액 | 부가세 | 합계 | 비고`와 fresh DOM/PDF 헤더를 대조했고 8열·순서·문구가 일치했다. D-3은 현행 동작 결함이 아니라 재오염 방지용 `LEGACY_FALLBACK_DETAIL_COLUMNS` 계약 고정으로 처리했다.

집계:

- passed: 1 — fresh DOM/PDF legacy 헤더 8/8 일치
- skipped: 1 — 격리 복제 원문 한글 검증(복제본 0건이라 원문 판정 불가)
- failed: 0

## 측정 7 — typecheck

실행 명령:

```text
cd clients/desktop
npm run typecheck
```

실행 원문:

```text
src/renderer/print/PartnerLedgerView.tsx(252,61): error TS2550: Property 'at' does not exist
src/renderer/routes/PartnerLedgerPage.tsx(896,79): error TS2550: Property 'at' does not exist
src/renderer/routes/PartnerLedgerPage.tsx(897,61): error TS2550: Property 'at' does not exist
src/renderer/routes/PartnerLedgerPage.tsx(916,58): error TS2550: Property 'at' does not exist
src/renderer/routes/warehouse/inoutAnalysisModel.ts(89,29): error TS2550: Property 'at' does not exist
src/renderer/routes/warehouse/inoutAnalysisModel.ts(151,22): error TS2550: Property 'at' does not exist
```

요청대로 기존 무관 `.at` 6건은 수정하지 않았다. fix2 변경 모듈에서 새 오류는 확인되지 않았다.

집계:

- passed: 0
- skipped: 0
- failed: 6 — 기존 무관 `.at` 오류 6건(수정 금지)

## 측정 8 — 변경 모듈 lint

실행 명령:

```text
cd clients/desktop
npx eslint src/renderer/print/approvalRenderModel.ts src/renderer/print/DocumentRenderer.tsx src/renderer/print/templateSchema.ts src/renderer/print/approvalSlipLineLink.test.ts src/renderer/print/DocumentRenderer.test.tsx
```

실행 원문:

```text
✖ 1 problem (0 errors, 1 warning)
approvalSlipLineLink.test.ts:9:35 기존 @typescript-eslint/consistent-type-imports warning
```

집계:

- passed: 5 — 오류 0
- skipped: 0
- failed: 0

## 최종 집계

의도적으로 결함을 재현한 RED 2건은 이력 집계에서 별도 기록하고, 수정 후 검증 집계에는 중복하지 않았다.

- passed: 306 — fix2 단위 27 + 인쇄 전량 253 + 격리 실사용 10 + PDF 페이지별 10 + D-3 계약 1 + lint 5
- skipped: 1 — 격리 복제본 한글 원문 판정(복제본 0건)
- failed: 6 — 기존 무관 typecheck `.at` 오류 6건
- RED history: failed 2 — D-1 금액, D-2 테두리

핵심 불변식:

- 합계: 첫 품목 `112,233 + 11,223 = 123,456` 화면/PDF 확인
- 다라인 PDF: 3페이지 모두 표가 페이지 안에서 닫힘; 1페이지 25.76pt, 2페이지 18.26pt, 3페이지 326.51pt 하단 여백
- legacy fallback: `품목 | 모델명 | 규격 | 수량 | 공급가액 | 부가세 | 합계 | 비고` 8/8 일치
- 실 데이터: 열림 70 / 막힘 0
- UUID: 화면/PDF 0건
- DS-4 활성화 게이트: 관련 전량 테스트 통과, 게이트 변경 없음

## 라운드 종료 — 삭제된 추적 파일

git 명령 없이 worktree index(`DIRC`)를 직접 읽어 19,371개 추적 경로의 디스크 존재 여부를 대조했다.

```text
INDEX_SIGNATURE=DIRC
INDEX_VERSION=2
INDEX_ENTRIES=19371
MISSING_TRACKED_FILES=0
```

삭제된 추적 파일은 **0개**다.
