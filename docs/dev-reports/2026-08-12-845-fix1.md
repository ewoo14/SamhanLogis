# PR #1158 (#845) fix 라운드 1

- 검증일: 2026-08-12 (Asia/Seoul)
- 범위: D-1 인쇄 미리보기/PDF 품목 누락 수정, 인쇄 회귀, desktop typecheck, 실데이터 70건 열림 계수
- 작업 규칙: git 명령 미사용, 공유 Docker 스택 쓰기 금지

## 측정 0 — 적대검증 근거 및 수치 정정

선행 보고서 `docs/dev-reports/2026-08-12-845-sol-review.md`를 먼저 읽었다. 원인은 API/렌더 모델이 아니라 활성 기본 양식에 `DETAIL` 요소가 없어 `slipLineItems`가 최종 렌더 결과에 소비되지 않는 것이었다.

선행 실측의 올바른 참조 분포는 다음과 같다.

- 참조 없음: **65건** (기존 보고서의 64건은 오류)
- 출고전표 참조는 있으나 전표 없음: 5건
- 전표는 있으나 활성 라인 0건: 0건
- 전표와 활성 라인이 모두 있음: 0건

## RED-first — D-1 재현 테스트

API 200이나 render model 배열만 검사하지 않고, `GROUPWARE_DEFAULT`를 실제 `DocumentRenderer`에 넣은 뒤 최종 HTML에 모델명과 12개 DETAIL 행이 있는지 단정하는 테스트를 먼저 추가했다.

실행 명령:

```text
npx vitest run src/renderer/print/DocumentRenderer.test.tsx -t "D-1 RED"
```

실행 원문:

```text
 RUN  v2.1.9 C:/dev/Samhan-Public/.claude/worktrees/w1158/clients/desktop

 ❯ src/renderer/print/DocumentRenderer.test.tsx (20 tests | 1 failed | 19 skipped)
   × compileApprovalDocument and DocumentRenderer > D-1 RED: 기본 양식은 연결된 출고전표 12개 품목을 인쇄 결과에 렌더한다
     → expected '<div><div class="no-print" style="mar…' to contain 'AJ060MXHNBC1'

 Test Files 1 failed (1)
 Tests 1 failed | 19 skipped (20)
```

집계 (RED):

- passed: 0
- skipped: 19
- failed: 1 (기대한 렌더 품목 모델명 미출력)

## GREEN — 최소 수정 후 재현 테스트

수정은 canonical `GROUPWARE_DEFAULT`이고 연결 상태일 때만 렌더러 내부에 DETAIL fallback을 추가하는 한 곳이다. 기존 legacy 양식의 저장 스키마와 DS-4 활성화 게이트는 변경하지 않았다.

실행 명령:

```text
npx vitest run src/renderer/print/DocumentRenderer.test.tsx -t "D-1 RED"
```

실행 원문:

```text
 RUN  v2.1.9 C:/dev/Samhan-Public/.claude/worktrees/w1158/clients/desktop

 ✓ src/renderer/print/DocumentRenderer.test.tsx (20 tests | 19 skipped) 24ms

 Test Files 1 passed (1)
 Tests 1 passed | 19 skipped (20)
 Duration 3.67s
```

집계 (GREEN):

- passed: 1
- skipped: 19
- failed: 0

## 측정 3 — desktop typecheck

실행 명령:

```text
npm run typecheck
```

실행 원문:

```text
> @samhan/desktop@0.1.0 typecheck
> node scripts/real-qa-scope.cjs --phase=typecheck && tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.web.json --noEmit && npm run typecheck:real-qa

[로컬 파생물 신선도] typecheck 대상 확인 완료 — 이 확인은 design-system dist 최신성 · electron-updater 설치 버전 일치만 봅니다. node_modules 의 file: 링크 무결성이나 그 외 일반 의존성 상태는 다루지 않으며, 그런 문제는 이어지는 tsc/vitest 원본 오류로 드러납니다.
src/renderer/print/PartnerLedgerView.tsx(252,61): error TS2550: Property 'at' does not exist
src/renderer/routes/PartnerLedgerPage.tsx(896,79): error TS2550: Property 'at' does not exist
src/renderer/routes/PartnerLedgerPage.tsx(897,61): error TS2550: Property 'at' does not exist
src/renderer/routes/PartnerLedgerPage.tsx(916,58): error TS2550: Property 'at' does not exist
src/renderer/routes/warehouse/inoutAnalysisModel.ts(89,29): error TS2550: Property 'at' does not exist
src/renderer/routes/warehouse/inoutAnalysisModel.ts(151,22): error TS2550: Property 'at' does not exist
```

exit 1이다. 오류 6건은 이번 변경 파일 밖의 선재 `.at` 오류이며, 이번 라운드에서 수정하지 않았다.

집계:

- passed: 0
- skipped: 0
- failed: 1 (typecheck 명령 exit 1; 기존 `.at` 6건, PR 변경과 무관)

## 측정 4 — 실데이터 70건 열림/막힘 재계수 시도

공유 Docker 스택은 사용하지 않았다. 읽기 전용으로 `sol1158-*` 컨테이너를 확인했으나 실행 중/중지 상태 모두 0건이었고, 워크트리에도 이전 격리 복제 dump가 없었다. 따라서 이번 라운드에서 실데이터 DB를 재기동·재조회할 수 없었으며, 선행 보고서의 `opened=70 / blocked=0`을 재측정 결과로 재사용하지 않았다.

읽기 전용 확인 원문:

```text
docker ps -a --filter "name=sol1158" --format "{{.Names}}\t{{.Status}}"
```

출력: (없음)

집계:

- passed: 0
- skipped: 1 (격리 실데이터 dump/서비스 부재; 공유 스택 사용 금지 준수)
- failed: 0

참고 기준값(재측정 아님): 선행 적대검증 보고서의 실데이터 전수 원문은 `total=70, opened=70, blocked=0`이다. 이 라운드의 최종 판정에는 재계수 미완료로 별도 표시한다.

## 측정 5 — 변경 범위 `.at` 귀속 및 DS-4 확인

변경 대상 렌더러/테스트/라운드 보고서 범위의 `.at(` 검색 원문:

```text
ADDED_AT_IN_CHANGED_SCOPE=0
```

DS-4 활성화 게이트와 migration 파일은 수정하지 않았다. 이번 수정은 `DocumentRenderer.tsx`의 canonical `GROUPWARE_DEFAULT` + `CONNECTED` 조건부 DETAIL fallback과 해당 렌더 회귀 테스트 및 보고서만 포함한다.

## 라운드 종료 — 삭제된 추적 파일

git 명령 없이 binary index(`DIRC`)를 직접 읽어 현재 추적 경로의 디스크 존재 여부를 대조했다.

실행 원문:

```text
INDEX_SIGNATURE=DIRC
INDEX_VERSION=2
INDEX_ENTRIES=19370
UNIQUE_TRACKED_PATHS=19370
MISSING_TRACKED_FILES=0
```

삭제된 추적 파일은 **0개**다.

## 최종 집계

- passed: 343 (focused 93 + print 전체 250)
- skipped: 1 (실데이터 70건 재계수: 격리 dump/서비스 부재)
- failed: 1 (desktop typecheck: 기존 `.at` 6건, PR 변경과 무관)

기능 회귀의 최종 fresh 실행 원문:

```text
Test Files 29 passed (29)
Tests 250 passed (250)
Duration 5.73s
```

실데이터 기준 70건 열림/0건 막힘은 선행 적대검증에서 확인된 값이며, 이번 라운드에는 격리 실행 자산 부재로 재측정하지 못했다. 따라서 본 라운드는 코드·렌더 회귀는 통과했지만 실데이터 재계수까지 완결된 것으로 보고하지 않는다.

## 측정 2 — 변경 모듈 전량 인쇄 회귀

실행 명령:

```text
npx vitest run src/renderer/print
```

실행 원문 요약 (실행 프로세스 원문 집계):

```text
Test Files 29 passed (29)
Tests 250 passed (250)
Duration 5.68s
```

집계:

- passed: 250
- skipped: 0
- failed: 0

## 측정 1 — focused 인쇄/미리보기 회귀

실행 명령:

```text
npx vitest run src/renderer/print/approvalSlipLineLink.test.ts src/renderer/print/ApprovalDocView.test.tsx src/renderer/print/ApprovalDocView.real-render.test.tsx src/renderer/print/approvalRenderGolden.test.tsx src/renderer/print/documentTemplateEditorPreview.test.tsx src/renderer/print/DocumentRenderer.test.tsx
```

실행 원문:

```text
✓ approvalSlipLineLink.test.ts (4 tests)
✓ documentTemplateEditorPreview.test.tsx (13 tests)
✓ approvalRenderGolden.test.tsx (19 tests)
✓ DocumentRenderer.test.tsx (20 tests)
✓ ApprovalDocView.real-render.test.tsx (8 tests)
✓ ApprovalDocView.test.tsx (29 tests)

Test Files 6 passed (6)
Tests 93 passed (93)
Duration 4.59s
```

stderr에는 기존 React Router v7 future flag warning만 있었고 테스트 실패는 없었다.

집계:

- passed: 93
- skipped: 0
- failed: 0
