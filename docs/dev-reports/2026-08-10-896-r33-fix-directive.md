# R33 fix 지시서 — 시트 동기화가 수량동기화 규칙 계약을 우회하는 2축

> 대상 PR `#1126` · 기준 HEAD `17becb74a` (R31)
> 작성 = PM (좌표 전수는 PM 이 직접 실측 · 릴레이 아님)
> 발견 = R32 (HIGH 1건) + PM sweep (축 1개 추가 발견)

---

## 0. 이번에 하지 마십시오 (범위 밖)

```text
🚫 MED "409 를 받은 사용자가 규칙에 도달할 수 없다" (UI 경로) — 다음 라운드
🚫 D15 수량동기화 작성 진입점 본체 전환 — 별도 슬라이스
🚫 D11 세트 구성품 기반 받침 규칙 — 별도 슬라이스
🚫 ProductService / ClassificationService 의 기존 가드 손대기 (이미 덮여 있음 · 아래 표)
🚫 git 조작 일체 (커밋은 PM 대행) · 재배포
```

부족하면 **①(가드 2축)만 완성하고 저장·보고**하십시오. ②(화면 표시)는 그 다음입니다.

---

## 1. 불변식 (무엇이 참이어야 하는가)

> **기존 품목의 `catL`(역할) 또는 `상태`를 바꾸는 모든 운영 쓰기는,
> 저장 후에도 그 품목을 참조하는 enabled·비삭제 수량동기화 규칙의 계약을 유지해야 한다.
> 🚨 자동 배치(시트 동기화)도 예외가 아니다.**

수단은 지정하지 않습니다. 어떻게 만족시킬지는 구현자가 고릅니다.

---

## 2. 좌표 전수 (PM 실측 · `17becb74a` 기준)

### 가드가 없는 곳 — 이번 대상

| 좌표 | 코드 | 축 |
|---|---|---|
| `ProductSheetSyncService.java:1347` | `p.changeStatus(sheetStatus)` | **상태** (`resultingActive`) |
| `ProductSheetSyncService.java:1403` | `p.changeClassifications(catL, catM, catS)` | **역할** (`catL` 이름) |

🔑 `:1347` 은 **R32 도 지목하지 않은 것**입니다. 규칙 계약이 `resultingActive` 를 포함하므로
시트가 품목을 단종·미판매로 내리면 `:1403` 과 같은 방식으로 규칙이 조용히 깨집니다.
같은 파일 `:619` 주석의 자기 교훈 — *"값 하나씩 막는 가드는 다음 값이 뚫는다"* — 이 **축 단위로 반복된 형태**입니다.

### 이미 가드가 있는 곳 — 🚫 손대지 마십시오

| 좌표 | 가드 |
|---|---|
| `ProductSheetSyncService.java:1385-1394` | usageScope NONE 전환 — skip + `log.warn` + `preservedByRuleProductOccurrences++` |
| `ProductService.java:743` | `assertResultingRoleSatisfiesQuantitySyncRules` (상품/비상품 변경) |
| `ProductService.java:760` | `assertResultingRoleSatisfiesQuantitySyncRules` (L/M/S 분류 저장) |
| `ProductService.java:622-627` | `assertResultingStateSatisfiesQuantitySyncRules` — **역할 인자까지 전달**하므로 일반 수정 PATCH 의 `goodsType`·`catL` 도 덮임 |
| `ClassificationService.java:67` | 분류명 변경 |

### 범위 밖 (규칙이 걸릴 수 없음)

```text
ProductSheetSyncService.java:1323, :1326   신규 품목 생성 분기 (existing.isEmpty())
```

---

## 3. D1 — 개발책임자 결정 (반대급부의 형태)

> **위반 품목만 건너뛰고 보고한다. 전체 중단이 아니다. 조용한 건너뜀은 금지.**

시트 동기화는 배치입니다. 한 품목 때문에 전체가 멈추면 나머지가 stale 이 됩니다.
반대로 조용히 건너뛰면 *"동기화했는데 안 바뀐"* 상태가 남습니다.

### 보고가 가는 곳 — 3곳 전부

```text
① 동기화 응답 본문    ✅ 이미 있음 — preservedByRuleProductOccurrences
                         ProductSheetSyncService.java:2149 (필드) · :256 (summary 합산)
② 로그 (warn)         ✅ 이미 있음 — :1389-1391 패턴
③ /admin/sheet-sync   ❌ 미충족 — 화면이 이 카운터를 렌더하지 않는다
                         clients/desktop/src/renderer/api/sheetSyncApi.ts:34, :66 에 타입만 존재
                         SheetSyncPage.tsx 에 참조 0건 (grep 전수 확인)
```

⟹ **③ 을 이번에 채웁니다.** 기존 usageScope 가드조차 지금은 사용자 눈에 보이지 않으므로,
새로 추가하는 2축도 같은 카운터/표시 경로를 타야 합니다.
건너뛴 **품목 수 + 모델코드 + 위반 rule key** 가 사용자에게 도달해야 합니다.

---

## 4. 양방향 RED (둘 다 GREEN 이어야 합니다)

### RED-A — 결함이 재현되는 테스트를 **먼저** 쓰고 RED 원문을 제출

```text
A-1  활성·비삭제 규칙이 참조하는 classificationManual=false 품목이
     시트 동기화로 반대 역할 L분류에 재배치될 때, 규칙 역할이 유지된다(=그 필드 갱신을 건너뛴다)
A-2  같은 품목을 시트가 단종/미판매 상태로 내릴 때도 같은 방식으로 보호된다
A-3  건너뛴 사실이 응답 본문 · 로그 · 화면 세 곳에 모두 나타난다
```

### RED-B — 표적을 명시합니다 (🚨 "기존 동작 불변" 은 표적이 아닙니다)

```text
B-1  건너뜀이 0건인 경우, 시트 동기화 결과가 기존과 **완전히 동일**하다
     (정상 품목의 분류·상태·가격·노출·속성 갱신이 그대로 일어난다)
B-2  기존 usageScope 가드(:1385-1394)의 동작·카운터가 변하지 않는다
     — 그 가드가 세던 건수를 새 가드가 흡수하거나 이중 계상하지 않는다
B-3  규칙이 0건인 카탈로그에서 시트 동기화 결과가 순수 레거시와 exact diff 0
```

**뮤테이션 RED** — fix 를 되돌리면 A 가 다시 RED 가 되는지 원문으로 제출하십시오.

---

## 5. 잃으면 안 되는 것 (이 PR 이 이미 지키고 있는 것)

```text
규칙 0건이면 순수 레거시와 exact diff 0
규칙 소비는 살아 있다
비상품 납품가 입력 시 수량 1                          (R10)
#1133 품목 상태 축 — 단종·미판매 미표시 · 품절 잠금    (R10 병합)
multiselect-chip-count 하위호환                        (R8)
R31 이 세운 역할 가드 3곳이 그대로 동작                (ProductService:743,:760 · ClassificationService:67)
```

바꾼 파일을 참조하는 **기존 테스트를 전부 실행**하고, 빨개진 것이 있으면
**고치지 말고 무엇을 지키던 테스트인지 먼저 보고**하십시오.

---

## 6. 보고 의무

```text
· 변경 줄 수와 늘어난 이유
· 신규 파일은 **목록으로** 보고 (PM 이 git status --porcelain 으로 회수합니다)
· 제거·개명한 식별자가 있으면 grep 전수 결과
· 새로 가능해진 상태·화면 조합을 열거하고 각각 밟은 결과
```

🚨 **제 전제가 틀렸다면 고치지 말고 중단·보고하십시오.**
특히 `:1347` 이 실제로는 다른 경로에서 이미 보호되고 있다면, 그 근거(`파일:줄`)를 들어 되돌려 주십시오.
셋째 가능성이 있으면 두 축 중 하나에 욱여넣지 말고 그것을 내십시오.
