# 2026-08-03 주문서 앱 접근권한 설정 라이브QA 재실시

## 시작 및 기동 증명

- 작업 디렉토리: `C:/dev/Samhan-Public/.claude/worktrees/t1015b`
- 브랜치: `feat/1015-order-app-access`
- HEAD: `96b2ffa635e574f38c7b45dd0ccdc4a11eb262c9`
- 데이터 출처: 로컬 DB dev 시드(`[DEV-SEED]`), 실데이터가 아님.
- `partner-auth-service` standalone jar 빌드: `BUILD SUCCESSFUL`
- jar SHA-256: `92A815030EB19BD6D10586B269E5EA9B4305E8630D170963E3810C4B315B72CC`
- 기동 포트: 정상 downstream 확인 `18423`, 의도적 실패 확인 `18425`; renderer `18424`/`18426`.
- QA 원칙: Flyway 비활성(`SPRING_FLYWAY_ENABLED=false`), Hibernate `validate`, 공유 DB read-only. Docker 공유 스택 이미지는 재빌드하지 않음.
- 브라우저 surface: in-app/Chrome 브라우저가 이 환경에서 제공되지 않아 Playwright headless Chromium fallback을 사용했다. 화면 데이터 API는 현재 jar와 Docker publish downstream에서 읽었고, 인증/권한 bootstrap만 테스트 하네스로 주입했다.

## 시나리오 1 — 화면 진입 · 대상 목록

- 조작: 실제 Vite renderer에서 `판매 → 주문서 앱 접근권한 설정` 경로로 진입했다.
- 화면 결과: `주문서 앱 접근권한 설정`, `전체 2건`과 거래처 목록 2건(`1068689215`, `2118712345`)이 렌더됐다.
- 판정: PASS.
- 캡처: `docs/qa/1015-order-app-access-real-qa/05-screen-entry-and-preview.png`

## 시나리오 2 — 장기미발주 판정

- 조작: 정상 downstream publish 포트(`partner-order 18088`, `slip 18086`)를 standalone jar에 주입한 상태에서 `비밀번호 초기화 대상 미리보기`를 확인했다.
- 화면 결과: `현재 대상 0건`과 함께 `주문·출고 조회 실패로 2건의 판정이 보류되었습니다. (ORDER, SHIPMENT)`가 표시됐다.
- 이전 QA의 `현재 대상 0건 + 보류 2건`과 동일했다. 현재 SHA에서 사업자번호 활동 조회 키 변경 자체의 회귀는 없었고, 두 downstream 활동 조회가 보류되어 후보를 장기미발주로 오판하지 않았다.
- 판정: PASS(보류 안전성 확인). 양성 후보 수 변화는 확인되지 않음.
- 캡처: `docs/qa/1015-order-app-access-real-qa/06-long-unused-preview-current-key.png`

## 시나리오 3 — 보류(deferred) 표시

- 조작: 두 번째 standalone jar를 `SAMHAN_PARTNER_ORDER_SERVICE_URL=http://127.0.0.1:1/intentional-order-failure`, `SAMHAN_SLIP_SERVICE_URL=http://127.0.0.1:1/intentional-shipment-failure`로 기동하고 같은 화면을 새로 진입했다.
- 화면 결과: 실제 렌더 화면에 `주문·출고 조회 실패로 2건의 판정이 보류되었습니다. (ORDER, SHIPMENT)`가 표시됐다. 거래처 목록 2건도 유지됐다.
- 판정: PASS. 의도적 downstream 실패가 장기미발주 후보로 오판되지 않고 deferred로 표시됐다.
- 캡처: `docs/qa/1015-order-app-access-real-qa/07-intentional-downstream-failure-deferred.png`

## 종합 판정

- 세 시나리오 모두 실시·통과했다.
- 현재 SHA에서 화면 진입·대상 목록, 사업자번호 활동 조회 결과, downstream 실패 보류 표시를 실화면으로 재확인했다.
- 관리자 복구/비밀번호 초기화 버튼은 공유 DB write 금지 조건에 따라 누르지 않았다.
- 모든 캡처는 실제 renderer 화면이며 JSON 응답·브라우저 JSON 탭·터미널 출력·합성/목업 이미지를 제출하지 않았다.

## 신규 캡처 목록

- `docs/qa/1015-order-app-access-real-qa/05-screen-entry-and-preview.png`
- `docs/qa/1015-order-app-access-real-qa/06-long-unused-preview-current-key.png`
- `docs/qa/1015-order-app-access-real-qa/07-intentional-downstream-failure-deferred.png`
