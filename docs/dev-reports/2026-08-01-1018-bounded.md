# 2026-08-01 PR #1018 바운드 작업 보고서

## 작업 범위

- 남김: 실 창고 UUID 주입, 통합테스트 placeholder UUID의 실 UUID 교체, 정찰·검토 기록.
- 제거: 기동 시 실재성 검증, 검증용 DataSource/JDBC 연결/설정 클래스/검증 전용 테스트, 검증 때문에 생긴 컨테이너 구성·`AbstractPostgresIT`·`V999` 변경.
- 금지: `git` 명령, 공유 Docker 재빌드·재기동, 실 DB 쓰기, 다른 서비스 수정, 무관한 리팩터링.
- 불변식: `slip-service` 전체 테스트 실패 0, 모든 환경 설정 실 UUID 주입, `dedicatedDataSourceTrio` 원계약, 원래 커넥션 풀 개수, placeholder 과거 전표 5건 조회 보존.

## 확인 1 — 작업 문서·범위

- `AGENTS.md`, `docs/handoff/CURRENT-WORK.md`, `.codex/AGENTS.md`를 확인했다.
- 저장소 규칙상 이 작업은 `slip-service`에 한정하고, 문서 보고서에 매 확인 결과를 누적한다.
- 사용자 지시로 `git` 명령과 공유 Docker 재빌드·재기동 및 실 DB 쓰기를 수행하지 않는다.

## 확인 2 — 정찰 결과

- 환경 설정 6곳과 `slip-service` 설정에서 창고 매핑 UUID가 `11111111-1111-1111-1111-000000000001`~`004`로 주입되어 있다.
- 현재 `slip-service/src/main/resources/application.yml`에는 `warehouse-validation` JDBC 설정이 남아 있어, 기동 검증 제거 대상이다.
- `infrastructure/terraform/templates/user_data.sh`와 CI에는 해당 UUID 외에 검증/풀 관련 주석과 설정 흔적이 있어 원상 복귀 여부를 확인한다.
- 정찰 검색은 출력량 제한으로 잘렸으며, 결과 원문은 이후 파일별 확인으로 보완한다.

## 확인 3 — 검증 하네스 흔적

- `WarehouseCodeMapper`가 `@PostConstruct`에서 `findExistingWarehouseIds`/`validateConfiguredWarehouses`를 호출하고 있다.
- `WarehouseValidationDataSourceConfig.java`, `WarehouseValidationDataSourceConfigTest.java`, `WarehouseCodeMapperValidationTest.java`는 검증 전용 산출물이다.
- `AbstractPostgresIT`가 `app.publish.warehouse-validation.*`를 동적 주입하고, 테스트 fixture에 창고 마스터 SQL이 있다. 이 중 공용 테스트 하네스 변경은 원래 상태 확인 후 되돌린다.
- `dedicatedDataSourceTrio`는 `SlipDataSourceConfig`의 가격기억 전용 계약으로 확인되었으며, 이번 바운드에서 건드리지 않는다.

## 설계 확정

- `WarehouseCodeMapper`는 설정된 문자열 UUID를 그대로 해석·반환하는 기존 역할만 남긴다. `@PostConstruct`, JDBC `DriverManager` 조회, 검증 properties 의존성은 제거한다.
- 검증 전용 설정 클래스와 검증 전용 테스트 2개는 제거한다.
- `application.yml`의 `warehouse-code-map` 및 실 UUID 기본값은 유지하고, 그 아래 검증용 JDBC 설정만 제거한다.
- `AbstractPostgresIT`에서는 검증 DB 생성/시드와 검증 JDBC 동적 속성만 제거한다. 공용 slip DB Testcontainers 및 원래 테스트용 pool 설정은 유지한다.
- 테스트 UUID는 현재 fixture의 실 UUID를 기준으로 placeholder만 교체한다. 저장/조회 동작은 바꾸지 않는다.

## 변경 1 — 기동 검증 제거

- `WarehouseCodeMapper`에서 inventory DB JDBC 연결·실재성 조회·검증 예외 경로를 제거하고, 설정 매핑 로깅과 `resolve`만 유지했다.
- `WarehouseValidationDataSourceConfig.java` 및 검증 전용 테스트 2개를 제거했다.
- `application.yml`에서 검증 JDBC 설정만 제거하고, 네 개의 `WAREHOUSE_UUID_*` 매핑 기본값은 유지했다.
- `AbstractPostgresIT`에서 검증용 inventory DB 생성/시드와 `app.publish.warehouse-validation.*` 동적 속성만 제거했다. slip DB 컨테이너와 기존 pool 설정은 유지했다.
- 검증 전용 `inventory-warehouse-master.sql` fixture를 제거했다. `V999` 마이그레이션은 애초에 발견되지 않았다.

## 확인 4 — 잔여 참조·환경 파일

- `slip-service` 내부에 `warehouse-validation`, `WarehouseValidation`, `findExistingWarehouseIds`, `validateConfiguredWarehouses`, `inventory-warehouse-master`, `V999` 잔여 참조가 없다.
- `dedicatedDataSourceTrio` 계약을 구성하는 `PriceMemoryJdbcAccess` 및 관련 테스트는 변경하지 않았다.
- 요청된 `.env.example`, `docker-compose.local-all.yml`, `docker-compose.prod.yml`, `env-templates/slip-service.env` 경로는 이 워크트리에 존재하지 않았다. 실제 파일 위치를 별도로 탐색한다.
- CI와 Terraform user-data에는 실 UUID 4개가 확인되었으나, `WAREHOUSE_DB_*`는 검증 제거 후 사용처가 없는지 확인한다.

## 변경 2 — 환경 주입 정리

- 실제 위치인 `infrastructure/.env.example`, `infrastructure/docker-compose.local-all.yml`, `infrastructure/docker-compose.prod.yml`, `infrastructure/env-templates/slip-service.env`, `infrastructure/terraform/templates/user_data.sh`에서 검증 전용 `WAREHOUSE_DB_NAME`을 제거했다.
- 위 파일들의 `WAREHOUSE_UUID_HQ/HUBAL/ANSEONG/CHANGWON` 실 UUID 주입은 유지했다.
- `.github/workflows/ci.yml`의 실 UUID 주입도 유지했다.

## 확인 5 — 테스트 실행 시작

- 실행 명령: `./gradlew.bat :slip-service:test --no-daemon`
- 공유 Docker 재빌드·재기동 및 실 DB 쓰기는 수행하지 않는다. 테스트가 필요로 하는 Testcontainers는 일회성 컨테이너로만 허용한다.

### 실행 시도 5-1 원문

```text
> .\\gradlew.bat :slip-service:test --no-daemon
FAILURE: Build failed with an exception.
* What went wrong:
Cannot locate tasks that match ':slip-service:test' as project 'slip-service' not found in root project 'samhan-public'.
BUILD FAILED in 7s
```

- 실패 원인: Gradle 프로젝트 경로를 잘못 지정했다. 코드/테스트 실패가 아니다.

### 확인 5-2 — Gradle 프로젝트 경로

- `settings.gradle`과 `gradlew projects --no-daemon` 원문을 확인해 `slip-service`의 실제 경로가 `:services:slip-service`임을 확인했다.

### 실행 시도 5-3 원문

```text
> .\\gradlew.bat :services:slip-service:test --no-daemon
Script failed
Exit code: 124
command timed out after 1204046 milliseconds
```

- 테스트 완료 요약이 출력되지 않았다.
- 따라서 이 시도는 `slip-units`/`slip-it-core`/`slip-it-public`의 성공 또는 실패 판정에 사용할 수 없다.
- 20분 동안 Gradle Java worker가 실행 중이었고, 결과 XML은 이번 실행 완료 결과로 갱신되지 않았다.

### 확인 5-4 — 타임아웃 후 정리

- 현재 실행에서 시작된 Gradle Java 프로세스(시각 2026-08-01 12:16)를 확인했다.
- 해당 테스트 실행 프로세스만 종료하고, 공유 서비스/Docker 프로세스는 건드리지 않는다.

### 실행 시도 5-5 — 컴파일 원문

```text
> .\\gradlew.bat :services:slip-service:cleanTest :services:slip-service:compileTestJava --no-daemon
> Task :services:slip-service:cleanTest
> Task :services:slip-service:compileTestJava UP-TO-DATE
BUILD SUCCESSFUL in 14s
9 actionable tasks: 1 executed, 8 up-to-date
```

- production/test 소스 컴파일은 성공했다. 이는 전체 테스트 통과를 의미하지 않는다.

### 실행 시도 5-6 — 단위 테스트 원문

```text
> .\\gradlew.bat :services:slip-service:test --tests '*Test' --no-daemon --console=plain
> Task :services:slip-service:test
BUILD SUCCESSFUL in 53s
18 actionable tasks: 1 executed, 17 up-to-date
OpenJDK 64-Bit Server VM warning: Sharing is only supported for boot loader classes because bootstrap classpath has been appended
```

- `*Test` 패턴 실행은 성공했다. 통합 `*IT`를 포함한 전체 테스트 성공과 동일하지 않다.

### 확인 5-7 — 단위 결과 집계

- Gradle 실행 결과는 `BUILD SUCCESSFUL`이었다.
- 결과 XML 기준으로 이번 `*Test` 실행 산출물은 총 742 tests, failures 0, errors 0, skipped 0으로 확인되는 파일들이 생성되었다. XML 집계 스크립트는 일부 한글 테스트명 인코딩으로 개별 파일 파싱 경고를 냈으므로, 최종 근거는 Gradle 종료 원문으로 삼는다.

## 변경 3 — 통합테스트 UUID 교체

- `OutboundCutoffGuardIT`의 창고 `sourceWarehouseId` placeholder를 실 창고 UUID `11111111-1111-1111-1111-000000000001`로 교체했다.

### 실행 시도 5-8 — UUID 교체 IT 원문

```text
> .\\gradlew.bat :services:slip-service:test --tests 'com.samhanair.logis.slip.it.cutoff.OutboundCutoffGuardIT' --no-daemon --console=plain
OutboundCutoffGuardIT > 10 tests FAILED
Caused by: org.flywaydb.core.internal.exception.FlywaySqlException
Caused by: java.sql.SQLTransientConnectionException at HikariPool.java:686
10 tests completed, 10 failed
BUILD FAILED in 1m 6s
```

- UUID 문자열 교체 자체는 컴파일되었으나, IT 실행은 커넥션 풀 고갈로 실패했다.
- 이는 불변식 A를 만족하지 못한 상태이며, 원인과 하네스 원복 범위를 추가 확인한다.

## 변경 4 — 테스트 하네스 pool 원복

- `AbstractPostgresIT`에서 메인 DataSource와 가격기억 DataSource를 각각 `maximum-pool-size=1`, `minimum-idle=0`으로 덮어쓰던 동적 속성 및 검증 회고 주석을 제거했다.
- `application.yml`의 기존 운영 pool 값(`DB_POOL_MAX` 기본 20, 가격기억 pool 기본 4)은 변경하지 않았다.
- 이 변경은 기동 검증을 되살리는 것이 아니라, 검증 도입 라운드에서 생긴 테스트 전용 pool 축소를 제거해 원래 pool 계약으로 되돌리는 것이다.

### 실행 시도 5-9 — pool 원복 후 UUID 교체 IT 원문

```text
> .\\gradlew.bat :services:slip-service:test --tests 'com.samhanair.logis.slip.it.cutoff.OutboundCutoffGuardIT' --no-daemon --console=plain
> Task :services:slip-service:test
BUILD SUCCESSFUL in 1m 12s
18 actionable tasks: 2 executed, 16 up-to-date
```

- `OutboundCutoffGuardIT` 10건은 실패 없이 완료됐다.

### 실행 시도 5-10 — `slip-service` 전체 테스트 원문

```text
> .\\gradlew.bat :services:slip-service:cleanTest :services:slip-service:test --no-daemon --console=plain
> Task :services:slip-service:cleanTest
> Task :services:slip-service:test
BUILD SUCCESSFUL in 10m
19 actionable tasks: 2 executed, 17 up-to-date
OpenJDK 64-Bit Server VM warning: Sharing is only supported for boot loader classes because bootstrap classpath has been appended
```

- 이번 실행은 `cleanTest` 후 `slip-service`의 `slip-units`, `slip-it-core`, `slip-it-public` 전체 Gradle test task를 수행했다.
- 통합테스트는 Testcontainers의 일회성 PostgreSQL에서 실행됐다. 공유 Docker 스택 재빌드·재기동 및 실 DB 쓰기는 수행하지 않았다.

### 확인 5-11 — 전체 결과 집계 원문

```text
suites=205 tests=1530 failures=0 errors=0 skipped=0
===== validation refs =====
(출력 없음)
```

- 불변식 A: `slip-service` 전체 1530 tests, failures 0, errors 0, skipped 0.
- `dedicatedDataSourceTrio` 관련 계약 테스트도 전체 실행에 포함되어 통과했다.

## origin/main 비교 요약

사용자 지시의 `git` 명령 금지 때문에 VCS 기반 `origin/main` diff 명령은 실행하지 않았다. 이 보고서는 이번 바운드 작업에서 실제로 변경한 파일을 기준으로 남은 diff를 요약한다.

- 남김: `infrastructure/.env.example`, `infrastructure/docker-compose.local-all.yml`, `infrastructure/docker-compose.prod.yml`, `infrastructure/env-templates/slip-service.env`, `infrastructure/terraform/templates/user_data.sh`, `.github/workflows/ci.yml`, `services/slip-service/src/main/resources/application.yml`의 실 UUID 주입.
- 남김: `OutboundCutoffGuardIT`의 placeholder → 실 UUID 교체.
- 되돌림/제거: `WarehouseCodeMapper`의 기동 JDBC 실재성 검증, `WarehouseValidationDataSourceConfig`, 검증 전용 테스트 2개, 검증 전용 inventory fixture, `application.yml`/환경 파일의 검증 DB 설정, `AbstractPostgresIT`의 inventory DB 생성·검증 속성·검증용 pool 축소.
- 유지: `SlipDataSourceConfig`와 `PriceMemoryJdbcAccess`/`dedicatedDataSourceTrio` 계약, 운영 `application.yml` pool 설정, 공용 slip Testcontainers 하네스.
- `V999` 마이그레이션은 이 워크트리에 존재하지 않았으며 남은 V999 참조도 없다.
- 위 목록과 보고서 파일 외에 다른 서비스 코드나 무관한 리팩터링은 변경하지 않았다.

## 최종 불변식 확인

- A 통과: 1530 tests / 0 failures / 0 errors / 0 skipped.
- B 통과: 6개 지정 환경/배포 설정과 slip 매핑에 네 실 UUID 주입 확인.
- C 통과: `dedicatedDataSourceTrio` 계약 및 계약 테스트 미변경·통과.
- D 통과: 검증용 추가 DataSource/JDBC 및 테스트 pool 축소 제거. 운영 pool 기본 개수 미변경.
- E: 기동 검증 경로를 제거해 placeholder UUID를 가진 과거 전표 조회를 차단하지 않으며, 조회 경로 자체를 변경하지 않았다. 실 DB 쓰기 없이 코드/전체 테스트로 확인했다.

## 금지사항 확인

- `git` 명령: 실행하지 않음.
- 공유 Docker 재빌드·재기동: 실행하지 않음.
- 실 DB 쓰기: 실행하지 않음.
- 다른 서비스 수정: 실행하지 않음.

## 확인 6 — CI 잔여 설정 제거

- 최종 검색에서 `.github/workflows/ci.yml`의 검증 전용 `WAREHOUSE_DB_NAME` 잔여를 발견해 제거했다.
- CI의 네 `WAREHOUSE_UUID_*` 실 UUID 주입은 유지했다.

## 확인 7 — 최종 검증

```text
===== forbidden/validation refs after final cleanup =====
(출력 없음)
===== required UUID definitions =====
infrastructure/.env.example => 4 matching lines
infrastructure/docker-compose.local-all.yml => 4 matching lines
infrastructure/docker-compose.prod.yml => 4 matching lines
infrastructure/env-templates/slip-service.env => 4 matching lines
infrastructure/terraform/templates/user_data.sh => 4 matching lines
.github/workflows/ci.yml => 4 matching lines
services/slip-service/src/main/resources/application.yml => 4 matching lines
===== test summary =====
tests=1530 failures=0 errors=0 skipped=0
```

- 검증 전용 참조와 `WAREHOUSE_DB_NAME` 잔여 없음.
- 지정된 7개 설정 위치 모두 네 UUID 정의/주입 확인.
- 보고서 자체가 이번 작업의 누적 기록이다.

### 확인 8 — 제거 대상 파일·계약

```text
WarehouseValidationDataSourceConfig.java exists=False
WarehouseValidationDataSourceConfigTest.java exists=False
WarehouseCodeMapperValidationTest.java exists=False
inventory-warehouse-master.sql exists=False
dedicatedDataSourceTrio refs=20
```

- 검증 전용 파일은 제거되었고 `dedicatedDataSourceTrio` 관련 production/test 참조는 보존되어 있다.

### 확인 5-7 — 단위 결과 집계

- Gradle 실행 결과는 `BUILD SUCCESSFUL`이었다.
- 결과 XML 기준으로 이번 `*Test` 실행 산출물은 총 742 tests, failures 0, errors 0, skipped 0으로 확인되는 파일들이 생성되었다. XML 집계 스크립트는 일부 한글 테스트명 인코딩으로 개별 파일 파싱 경고를 냈으므로, 최종 근거는 Gradle 종료 원문으로 삼는다.
