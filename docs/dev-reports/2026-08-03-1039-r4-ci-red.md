# #1039 R4 — CI red 해소

## 확인 1 — 작업 기준점

```text
git status --short; git log --oneline -3

?? docs/dev-reports/2026-08-03-1039-r4-ci-red.md
5381e4878 [FIX] #1039 마이그레이션을 V62 에서 V101 로 이동 — 배포본 기동 차단 해소
1590f6204 [FIX] #1039 적대검증 결함 7건 — 창고 업무구분 보존 · 장애 전파 · 야적 우선
5afc31178 [REVIEW] #1039 R1 CODEX SOL 5.6 적대검증 — 도달 가능한 결함 7건
```

## 확인 2 — slip-it-core 실패 원인과 판정

```text
실행: .\gradlew.bat :services:slip-service:test --tests "com.samhanair.logis.slip.it.SlipOutboundInternalControllerIT" --no-daemon

SlipOutboundInternalControllerIT > findOutboundSlipSummaries_returnsDtoContract_withoutUuid() FAILED
    java.lang.AssertionError: Status expected:<200> but was:<500>
    at SlipOutboundInternalControllerIT.java:144

SlipOutboundInternalControllerIT > findOutboundSlipSummaries_includesDeliveryTag_withoutUuid() FAILED
    java.lang.AssertionError: Status expected:<200> but was:<500>
    at SlipOutboundInternalControllerIT.java:171

7 tests completed, 2 failed
BUILD FAILED
```

판정: 테스트의 UUID 비노출 계약이 낡은 것이 아니라 구현이 fixture의 출처 없는 창고를 500으로 바꾼 결함이다.

- `services/slip-service/src/test/java/com/samhanair/logis/slip/it/SlipOutboundInternalControllerIT.java:192-202`의 fixture는 `Slip.createOutbound(...)`에 UUID를 넣지만 source warehouse code는 넣지 않는다. 두 실패 테스트의 전표도 같은 `persistOutbound` 경로를 사용한다(`:137-138`, `:163-165`).
- `services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java:326-328`은 `findWarehouseName(...).orElseThrow()`로 표시명 미존재를 예외 처리하여 500을 반환한다. 실행 결과의 원문도 `NoSuchElementException` 및 해당 줄을 가리킨다.
- 응답 DTO에는 UUID 필드가 없고(`services/slip-service/src/main/java/com/samhanair/logis/slip/web/dto/OutboundSlipResponse.java:31-40`), 새 창고 정보는 표시명·업무 구분 필드(`:36-37`)이다. 따라서 창고 정보가 필요한 새 계약은 유지하면서, 출처 없는 행은 UNKNOWN/표시명 없음으로 정상 응답해야 소비자 계약과 UUID 비공개를 함께 지킨다.

```text
실패 원문(테스트 XML):
java.util.NoSuchElementException: No value present
    at java.base/java.util.Optional.orElseThrow(Optional.java:377)
    at com.samhanair.logis.slip.web.SlipInternalController.lambda$findOutboundSlipsForDispatch$0(SlipInternalController.java:327)
Resolved Exception: java.util.NoSuchElementException
Status = 500
```

## 확인 3 — ① 수정 및 GREEN

수정: `services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java:327`의 창고명 `Optional.empty()` 처리만 `orElse(null)`로 변경했다. `warehouseCodeMapper.businessType(...)`는 원천 code가 없을 때 `UNKNOWN`을 반환하며, inventory client 호출 자체의 예외는 catch하지 않아 장애와 정상적인 출처 없음이 분리된다. UUID 필드는 DTO에 추가하지 않았다.

```text
실행: .\gradlew.bat :services:slip-service:test --tests "com.samhanair.logis.slip.it.SlipOutboundInternalControllerIT" --no-daemon

> Task :services:slip-service:test

BUILD SUCCESSFUL in 41s
18 actionable tasks: 2 executed, 16 up-to-date
```

`findOutboundSlipSummaries_returnsDtoContract_withoutUuid` 및 `findOutboundSlipSummaries_includesDeliveryTag_withoutUuid`를 포함한 대상 클래스 7건이 모두 GREEN이다.

## 확인 4 — Desktop Playwright 실패 원문과 판정

```text
실행: gh run view 30760761183 --log-failed

1 failed
    [chromium] playwright/sp-08-3-2-arologis-history/sp-08-3-2-arologis-history.spec.ts:107:3
    pre-classify는 권역/지방 programType을 분리하고 effect deps에 programType을 포함한다

Error: expect(received).toContain(expected)
Expected substring: "[date, from, programType, regionQuery.data, regionalQuery.data, tab, to]"
Received source: PreClassifyPage.tsx
  }, [date, executionMode, from, programType, regionQuery.data, regionalQuery.data, tab, to])

656 passed (10.0m)
[guard] expected=656 unexpected=1 skipped=0 flaky=0
```

판정: 화면 변경 자체가 의도된 변경이다. `PreClassifyPage.tsx:193-206`은 모드가 바뀐 조회 결과를 새 AUTO 저장으로 반영하기 위해 `executionMode`를 effect dependency와 `autoSaveKey`에 포함한다. 따라서 모드를 제거해 기존 정적 기대값에 맞추면 PR의 화면 상태 불변식을 되돌리게 된다. 실패는 snapshot 픽셀 회귀가 아니라 이 의도된 dependency 추가를 반영하지 않은 정적 mock 계약이다.

화면/동작 변경 목록:

- `executionMode`가 auto-save dependency에 추가됨.
- `autoSaveKey`가 모드별로 달라져 모드 변경 결과가 새 저장으로 기록됨.
- `programType` 분기·조회·복원 및 창고 모드 Select 자체는 변경 대상이 아님.

## 확인 5 — Desktop 대상 테스트 GREEN

정적 mock 회귀 계약에서 의도된 `executionMode` dependency를 기대하도록 테스트만 갱신했다. snapshot 파일은 변경하지 않았다.

```text
실행 위치: clients/desktop
실행: npx playwright test playwright/sp-08-3-2-arologis-history/sp-08-3-2-arologis-history.spec.ts --config=playwright.config.ts --grep "pre-classify는 권역/지방"

Running 1 test using 1 worker
[1/1] ... pre-classify는 권역/지방 programType을 분리하고 effect deps에 programType을 포함한다
1 passed (3.7s)
```
