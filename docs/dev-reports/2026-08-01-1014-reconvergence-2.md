# 2026-08-01 1014 머지 직전 재수렴 2차 검증 보고서

## 검증 기준

- 작업 브랜치: `feat/1014-doc-autosave`
- 시작 HEAD: `8758ae00a`
- 시작 상태: 이 보고서 파일만 untracked, 그 외 작업트리 변경 없음.
- 금지사항 준수: git 쓰기, 코드 수정, Docker 재빌드·재기동, 실 DB 쓰기 없음.

## fix 표면 확정

재현 명령:

```text
git diff --stat 9d31aa7d8..HEAD
git diff --name-only 9d31aa7d8..HEAD
```

출력 원문 요약:

```text
12 files changed, 603 insertions(+), 15 deletions(-)
```

production 표면은 `LedgerImageService`, `LedgerSnapshotService`, `AccountingReportController`, `partnerLedgerApi.ts`, `PartnerLedgerPage.tsx`이며, 계약 Playwright와 persistence/service 테스트가 함께 변경됐다. 이후 검증은 이 전체 표면을 대상으로 한다.

## 각도 1 — 공유 실 DB의 기존 행·배치번호 분포

실 사용자 경로 재현 여부: 현재 공유 서비스는 `t1014` HEAD가 아닌 `t1008` 이미지다. 따라서 branch 화면 경로 검증에는 사용할 수 없고, 과거 행 호환 표본과 기존 배치번호 분포만 읽기 조사했다.

재현 명령:

```text
docker exec samhan-postgres psql -X -v ON_ERROR_STOP=1 -U samhan -d accounting_db \
  -c "SELECT version, success FROM flyway_schema_history WHERE version = '95';" \
  -c "SELECT COUNT(*) AS all_batches FROM tax_invoice_batches;" \
  -c "SELECT length(batch_no) AS batch_no_length, COUNT(*) AS row_count FROM tax_invoice_batches GROUP BY length(batch_no) ORDER BY batch_no_length;" \
  -c "SELECT MIN(length(batch_no)) AS min_length, MAX(length(batch_no)) AS max_length, COUNT(*) FILTER (WHERE length(batch_no) > 20) AS over_20, COUNT(*) - COUNT(DISTINCT batch_no) AS duplicate_rows FROM tax_invoice_batches;"
```

출력 원문:

```text
 version | success
---------+---------
(0 rows)

 all_batches
-------------
           0
(1 row)

 batch_no_length | row_count
-----------------+-----------
(0 rows)

 min_length | max_length | over_20 | duplicate_rows
------------+------------+---------+----------------
            |            |       0 |              0
(1 row)
```

영향 건수: 기존 배치 0건, 기존 `PARTNER_LEDGER` 표본 0건. 따라서 기존 번호와의 실데이터 충돌·과거 행 복원 호환은 **재현 불가**이며 통과로 판정하지 않는다. 분포 역시 표본 0건이라 “20자 번호가 기존 의미를 잃지 않는다”는 주장을 실 DB로 입증할 수 없다.

공유 배포본 식별 명령과 출력 원문:

```text
docker inspect samhan-accounting-service --format '{{json .Config.Labels}}'
... "com.docker.compose.project.working_dir":"C:\\dev\\Samhan-Public\\.claude\\worktrees\\t1008\\infrastructure" ...

docker ps --filter name=samhan-accounting-service --format "{{.Names}} {{.Status}}"
samhan-accounting-service Up 6 hours (healthy)
```

현재 공유 서비스가 다른 워크트리 산출물이므로, 이후 live HTTP 200/403은 기존 baseline 재현으로만 기록한다.

## 결함 후보 R2-1 — 기존 권한 baseline의 MASTER 403

1. 실 사용자 경로 재현: 기존 원장 데이터가 있는 거래처 `P-2026-0005`(대구HVAC솔루션)의 원장을 공유 accounting-service HTTP endpoint로 조회했다. ACCOUNTANT/MANAGER는 200, SALES는 403이지만 확정 baseline상 허용돼야 하는 MASTER가 403이다. 실 DB의 활성 MASTER 대상자 집합 2명 모두 같은 결과다.
2. 재현 명령: `auth_db`에서 각 로그인 계정의 내부 식별자를 읽어 `X-User-Id` 헤더에만 사용하고, `GET http://127.0.0.1:8087/accounting/journals/ledger-data?partnerCode=P-2026-0005&from=2020-01-01&to=2030-12-31`을 등급별 호출했다. 내부 식별자는 출력·보고하지 않았다.
3. 출력 원문:

```text
dev_accountant [DEV-SEED] 개발회계 ACCOUNTANT status=200
dev_manager [DEV-SEED] 개발매니저 MANAGER status=200
dev_master [DEV-SEED] 개발마스터 MASTER status=403
dev_sales [DEV-SEED] 개발영업 SALES status=403
tax_invoice_batches before=0 after=0

dev_master [DEV-SEED] 개발마스터 MASTER status=403
kimmiseon 김미선 MASTER status=403
master_total=2 master_blocked=2
```

실 데이터 영향 건수: 활성 MASTER 2명 중 2명 차단. 단, 공유 서비스가 `t1014` HEAD 배포본이 아니므로 이 결과만으로 fix가 만든 회귀라고 귀속할 수는 없다. 요구 baseline 수치 `200 / 403`은 현재 공유 runtime에서 재현되지 않는다.

## 각도 4 — 지정 Playwright 및 이력 client 수치 재현

재현 명령과 출력 원문:

```text
> npx playwright test playwright/sp-08-6-5-accounting-daily-* --reporter=line
Running 5 tests using 1 worker
5 passed (4.6s)

> npx vitest run src/renderer/api/partnerLedgerHistory.test.ts
Test Files  1 passed (1)
Tests       2 passed (2)
```

수치 판정: 요구된 Playwright `5 passed`와 이력 client `2 passed`는 fresh 재현됐다. 이 명령은 화면 렌더·클릭이 아니라 계약 실행이므로, 화면 도달성과 복원 결과 동일성은 별도 축으로 남긴다.

## 각도 2 — 독립 PostgreSQL 자동저장 persistence

실 사용자 경로 재현 여부: 공유 실 DB에는 쓰지 않고, branch production service와 repository를 연결하는 Testcontainers PostgreSQL에서 실제 commit 후 SELECT를 실행했다.

재현 명령과 출력 원문(내부 작성자 값은 보고서에서 비공개):

```text
> .\gradlew.bat :services:accounting-service:test --tests "*LedgerSnapshotPersistenceIT" --no-daemon --no-build-cache --rerun-tasks --console=plain
BUILD SUCCESSFUL in 1m 14s
21 actionable tasks: 21 executed

tests=1 skipped=0 failures=0 errors=0
SELECT batch_no, processed_by, has_snapshot FROM tax_invoice_batches:
{batch_no=LED20260801194127897, processed_by=[내부값 비공개], has_snapshot=true}
```

판정: 단일 자동저장은 DB에 남고, 작성자 값 일치 assertion과 snapshot 존재 assertion이 통과했다. 배치번호 `LED20260801194127897`은 20자다. 영향 건수: 독립 DB에서 생성 1건 중 저장 성공 1건.

## 각도 4 — accounting-service 전체 수치 재현

재현 명령과 출력 원문:

```text
> .\gradlew.bat :services:accounting-service:test --no-daemon --no-build-cache --rerun-tasks --console=plain
BUILD SUCCESSFUL in 8m 11s
21 actionable tasks: 21 executed

> TEST-*.xml 전체 DOM 집계
FILES=203 TESTS=1718 SKIPPED=10 FAILURES=0 ERRORS=0 PARSE_ERRORS=0
AccountingPermissionControllerIT TESTS=78 SKIPPED=0 FAILURES=0 ERRORS=0
```

수치 판정: 요구값 `1,718 tests · 10 skipped · 0 failures`는 fresh 재현됐다. 권한 IT도 78건, 실패·오류 0으로 재현됐다. 다만 실제 공유 runtime의 MASTER 2명은 앞 절처럼 403이므로, IT 수치가 live baseline 불일치를 해소하지는 않는다.

## 각도 1 — read-only 제거가 건드린 조회 경로

1. 실 사용자 경로: `/accounting/partner-ledger`에서 거래처 row의 `원장 보기`를 누르면 `GET /accounting/journals/ledger-data`가 `LedgerImageService.getLedger`를 호출한다.
2. 재현 명령과 출력 원문:

```text
> git log -p --follow -- LedgerImageService.java
기존 주석: read-only — 외부 client 2종 의존
최초 snapshot 도입: 기존 조회 메서드 내부에 repository.save(batch) 추가
현재: class-level readOnly 제거, getLedger 2개 overload에 @Transactional

> 현재 LedgerImageService public method 전수 확인
getLedger(String, LocalDate, LocalDate)
getLedger(String, LocalDate, LocalDate, actor)
```

원래 read-only였던 이유는 이 클래스가 순수 조회만 하던 시기의 경계였다. 현재 클래스에는 다른 public 조회 메서드가 없어 annotation 제거로 별도 endpoint가 write transaction으로 바뀌지는 않는다. 이력 조회·복원은 별도 `LedgerSnapshotService`에서 계속 `@Transactional(readOnly = true)`다.

영향 건수: 별도 조회 경로 차단 0건 확인. 다만 branch runtime이 공유 환경에 없으므로 write transaction 전환 전후의 실제 HTTP 지연 비교는 재현하지 못했다.

## 작성자 자기 문서 접근 정책

실 DB 권한 집합 읽기 결과:

```text
print_allowed | print_allowed_view_denied
--------------+--------------------------
            8 |                         0

system_master_accounts
----------------------
                     2
```

현재 일반 작성 가능 계정 8명은 모두 이력 VIEW도 있어 자기 snapshot 접근이 권한상 막히지 않는다. 그러나 snapshot 모델에는 대상자/참여자 집합 자체가 없고 `processed_by` audit만 있다. 따라서 확정 정책인 “작성자를 대상자 집합에 실제 포함”은 구현·실데이터 양쪽에서 확인할 대상이 없다. 현재 기존 snapshot 0건이므로 실 사용자 차단 영향 건수는 0건이며, 이 라운드에서는 직접 도달 결함으로 게이트하지 않는다. MASTER 2명의 403은 별도 결함 후보 R2-1에 포함했다.

## 각도 3 — 이력 화면 도달성과 복원

실 사용자 경로: 거래처 row를 선택하면 같은 렌더에서 원장 조회와 이력 조회가 모두 활성화되고, 이력 행의 `복원` 버튼은 복원 API 결과의 원장을 같은 Step 2 표에 넣는다.

재현 명령과 출력 원문:

```text
> rg -n "const ledgerQuery|const historyQuery|queryFn: .*getLedgerHistory|enabled: !!selectedPartner|handleRestore|setRestoredLedger|invalidateQueries|refetch" PartnerLedgerPage.tsx
244:  const ledgerQuery = useQuery<LedgerData>({
247:    enabled: !!selectedPartner,
250:  const historyQuery = useQuery({
252:    queryFn: () => getLedgerHistory(...)
253:    enabled: !!selectedPartner,
273:  const handleRestore = async (batchNo: string) => {
275:    setRestoredLedger(restored.ledger)
627:  onClick={() => void handleRestore(item.batchNo)}
```

도달성 판정: production 화면의 목록·버튼·복원 결과 배선은 존재한다. 그러나 선택 순간 자동저장 GET과 이력 GET이 동시에 시작되고, 저장 완료 뒤 이력을 invalidate/refetch하는 연결은 검색 결과 0건이다. 이력 GET이 먼저 끝나면 방금 생성한 snapshot이 목록에 나타나지 않는 경로가 존재한다.

실 데이터 영향 건수: 공유 DB 기존 이력 0건이므로 현재 실측 차단 0건. branch backend 미배포와 in-app 브라우저 대상 0개로 실제 화면에서 목록 표시·복원 클릭·저장 내용 동일성은 재현하지 못했다. 따라서 이 항목은 **도달 가능 경합 후보**로 기록하되 실제 화면 게이트 결함으로 확정하지 않는다.

## 각도 2 — 연속·동시 요청

이 기능은 사용자가 내용을 갱신하는 edit/update autosave가 아니라 GET 결과를 새 snapshot으로 append한다. 따라서 “마지막 입력값” lost update 개념은 적용되지 않는다. 단, 동시 원장 조회에는 별도 충돌 축이 있다.

```text
LedgerImageService.java:51  yyyyMMddHHmmssSSS
LedgerImageService.java:128 "LED" + LocalDateTime.now().format(BATCH_TIME)
V13__tax_invoice_batch.sql:42 CREATE UNIQUE INDEX ... (batch_no) WHERE is_deleted = false
```

같은 millisecond에 실행된 두 요청은 같은 20자 배치번호를 만들고 활성 배치번호 unique index와 충돌할 수 있다. 이번 라운드는 branch HTTP runtime과 동시 호출 하네스가 없어 실제 사용자 요청으로 재현하지 못했으므로 게이트 결함으로 확정하지 않는다. 기존 실 DB 행 0건이며 실측 영향 건수도 0건이다.

## 최종 판정

**재수렴 FAIL — 현재 증거로 머지 승인 불가.**

- 확인된 실 사용자 차단: 기존 원장 데이터가 있는 거래처 조회에서 MASTER 2명 중 2명이 403. 요구 baseline `ACCOUNTANT/MANAGER/MASTER 200 · SALES 403`과 불일치한다.
- branch에 귀속 가능한 확정 차단 결함: 0건. 공유 runtime이 `t1014` HEAD가 아니어서 MASTER 403의 branch 귀속은 확정할 수 없다.
- 수치 재현: accounting-service `1,718 / skipped 10 / failures 0 / errors 0`, Playwright `5 passed`, 이력 client `2 passed`.
- DB 저장 재현: 독립 PostgreSQL 1건 저장 성공, 작성자 일치, `has_snapshot=true`, 배치번호 20자.
- 기존 행 호환·번호 분포: 실 DB 대상 행 0건으로 재현 불가. 통과 판정하지 않는다.
- 실제 화면 이력·복원: branch backend 미배포 및 사용 가능한 in-app 브라우저 없음으로 미재현. production 배선은 있으나 저장/이력 동시 시작 후 갱신 부재 경합 후보가 남는다.

## 이번 라운드가 보지 않은 축

- `t1014` HEAD를 배포한 live HTTP/DB 검증: 공유 Docker 재빌드·재기동 금지로 미수행.
- 기존 `PARTNER_LEDGER` 행 복원 호환과 기존 배치번호 의미·충돌 분포: 공유 DB 대상 행 0건으로 표본 없음.
- Desktop 실제 화면에서 이력 목록 표시, 복원 클릭, 저장 내용과 복원 내용의 시각적 동일성: 브라우저 대상 없음.
- 같은 millisecond 동시 요청의 배치번호 unique 충돌: branch HTTP runtime과 동시 호출 하네스 없음.
- write transaction 전환 전후의 live 지연·처리량 비교: branch runtime 미배포.
- 다른 문서 계열의 자동저장·이력: 거래처원장 첫 적용 범위 밖.

## 보고서 무결성

- 보고서 외 작업트리 변경 0건.
- 내부 식별값 형식 일치 0건.
- 코드·git·공유 Docker·실 DB 쓰기 없음.
