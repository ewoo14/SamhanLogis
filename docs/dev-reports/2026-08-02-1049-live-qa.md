# PR #1050 / 이슈 #1049 검색 결과 선택 모달 — 라이브QA 캡처 재실행

## 1. 실행 방식

- 실행일: 2026-08-03 (KST)
- 대상 브랜치: `feat/1049-search-modal-multiselect`
- 프론트: `clients/desktop`에서 `vite.web.config.ts`로 기동한 BrowserRouter 웹 화면
- 프론트 주소: `http://127.0.0.1:5233` (`VITE_APP_VERSION=2026/08/03-1049`)
- 백엔드 게이트웨이: `http://127.0.0.1:8080`
- 인증: 실 게이트웨이 `dev_master` 로그인. 화면 표기는 `[DEV-SEED] 개발마스터 · MASTER`.
- 데이터: 로컬 DB의 `[DEV-SEED]` 데이터. 실 운영 데이터가 아니며, 본 실행은 검색·선택·화면 반영만 수행했고 공유 DB write는 하지 않았다.
- 배포본 생성 시각 확인:
  - `samhan-api-gateway`: `2026-07-31T15:15:50.070347996Z`
  - `samhan-product-service`: `2026-07-31T14:25:25.972348444Z`
  - `samhan-groupware-service`: `2026-07-31T15:47:42.739008746Z`
  - `samhan-partner-service`: `2026-07-23T13:40:46.849980189Z`
- 브라우저: 로컬 `clients/desktop/node_modules/.bin/playwright`와 Chromium. 캡처는 모두 실 DOM 화면에서 저장했다.
- 실행 원문:

```text
VITE_API_BASE_URL=http://127.0.0.1:8080
VITE_APP_VERSION=2026/08/03-1049
vite dev --config vite.web.config.ts --host 127.0.0.1 --port 5233 --strictPort
node C:\Users\user\.codex\tmp\t1049-live-capture.mjs
LIVE_QA_DONE 8 screenshots
```

## 2. 시나리오별 실제 입력·선택·확정·반영

| 대상 | 상황 | 실제 조작 및 화면 관찰 | 캡처 |
|---|---|---|---|
| 품목 | 2건 이상 | `AJ` 입력 → 실 검색 모달 → checkbox 2개 실제 체크 → `선택 확정` → 상단 칩 2개(`AJ030RXH4BC1`, `AJ060MXHNBC1`)와 `2건 추가` 반영 | `01-product-multi-confirmed.png` |
| 품목 | 정확 코드 | `AJ040RXH4BC1` 입력 → 검색 모달 없음 → 기존 dropdown 단건 확정 → `AJ040RXH4BC1 · 실외기_4HP 다배관` 칩 추가, 입력란은 다시 검색 대기 상태 | `02-product-exact-dropdown-reflected.png` |
| 수신자 | 2건 이상 | `김` 입력 → 실 검색 모달 → checkbox 2개 실제 체크 → `선택 확정` → 수신자 칩 `김은지 · 회계팀`, `김기철 · 영업2팀` 2개 반영 | `03-recipient-multi-confirmed.png` |
| 수신자 | 정확 코드 | `dev_accountant` 입력 → 검색 모달 없음 → 기존 dropdown 단건 클릭 → `[DEV-SEED] 개발회계 · 회계팀` 칩과 선택 라벨 반영 | `04-recipient-exact-dropdown-reflected.png` |
| 거래처 | 2건 이상 | `010` 입력 → 실 검색 모달 → radio 후보 실제 선택 → `선택 확정` → 거래처 입력값 `청담 동양파라곤 / 개인고객` 반영 | `05-partner-multi-confirmed.png` |
| 거래처 | 정확 코드 | `010-2564-8488` 입력 → 검색 모달 없음 → 기존 dropdown 단건 클릭 → `향남부평-(박동수)` 입력값 반영 | `06-partner-exact-dropdown-reflected.png` |
| 담당자 | 2건 이상 | `김` 입력 → 실 검색 모달 → checkbox 2개 실제 체크 → `선택 확정` → 결재선 칩 `김은지 (회계팀)`, `김기철 (영업2팀)` 2개 반영 | `07-assignee-multi-confirmed.png` |
| 담당자 | 정확 코드 | `dev_accountant` 입력 → 검색 모달 없음 → 기존 dropdown 단건 클릭 → `[DEV-SEED] 개발회계 (회계팀)` 칩과 선택 라벨 반영 | `08-assignee-exact-dropdown-reflected.png` |

모든 다건 시나리오는 후보 표시만 확인하지 않고 checkbox/radio를 실제 조작한 뒤 확정 버튼을 눌렀다. 모든 정확 코드 시나리오는 검색 모달이 열리지 않고 기존 dropdown 선택 경로로 화면 값이 반영되는 것을 확인했다.

## 3. 판정

**라이브QA PASS — 요청한 4종 × 2상황 8/8 캡처 완료.**

- 모달 다중 선택: 품목·수신자·거래처·담당자 모두 실제 선택 확정 및 화면 반영 PASS.
- 정확 코드: 4종 모두 모달 없이 기존 dropdown 경로 및 화면 반영 PASS.
- 캡처에는 JSON·터미널 출력·합성 이미지·fixture 화면을 사용하지 않았다.
- 실행 중 공통 업데이트 확인 endpoint `GET /app/version`가 `404`를 반환해 상단 업데이트 실패 안내가 보였으나, 앱 부팅·대상 화면·검색 API는 정상 동작했다. 이번 PR 검색 모달 시나리오의 차단 결함으로 판정하지 않았다.

## 4. 하지 못한 것

- 요청 최소 범위 밖인 1건/0건 상황은 이번 캡처 실행에서 추가하지 않았다.
- 메신저 발송, 결재 문서 생성, 입금자명 매핑 저장, 품목 추가 저장 등 공유 DB write가 필요한 동작은 수행하지 않았다.
- 모바일/Capacitor/Electron HashRouter 경로는 이번 게이트 범위가 아니므로 확인하지 않았다.
- 품목 화면에서 배포본의 `Maximum update depth exceeded` 콘솔 경고가 관찰되었으나, 화면/API가 계속 동작하는 상태에서 이번 요청의 8개 시나리오 판정만 수행했다.

## 5. 새 산출물 경로

- `docs/dev-reports/2026-08-02-1049-live-qa.md`
- `docs/qa/1049-search-modal-real-qa/01-product-multi-confirmed.png`
- `docs/qa/1049-search-modal-real-qa/02-product-exact-dropdown-reflected.png`
- `docs/qa/1049-search-modal-real-qa/03-recipient-multi-confirmed.png`
- `docs/qa/1049-search-modal-real-qa/04-recipient-exact-dropdown-reflected.png`
- `docs/qa/1049-search-modal-real-qa/05-partner-multi-confirmed.png`
- `docs/qa/1049-search-modal-real-qa/06-partner-exact-dropdown-reflected.png`
- `docs/qa/1049-search-modal-real-qa/07-assignee-multi-confirmed.png`
- `docs/qa/1049-search-modal-real-qa/08-assignee-exact-dropdown-reflected.png`
