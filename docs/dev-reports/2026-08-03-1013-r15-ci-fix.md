# PR #1059 R15 CI red 닫기 보고서

- 작업 경로: `D:\dev\Samhan-Public\.claude\worktrees\w1059`
- 기준 HEAD: `49c511b98`
- 시작 시각: 2026-08-03 (Asia/Seoul)
- 원칙: 3개 실패 잡의 원문을 먼저 기록한 뒤 원인별 수정·검증을 순서대로 기록한다.

## 진행 로그

### 1. 작업 시작

- 보고서 파일을 생성했다.
- 아직 CI 원문 수집과 코드 수정 전이다.

### 2. 수정 전 실패 원문 수집 — 문서 본문 단언 스펙

- `gh run list --commit 49c511b98 --json databaseId,workflowName,conclusion` 결과는 실행 중 런이 노출되지 않아 `[]`였다. PR #1059 check 상세에서 문서 런 `30798135855`, 잡 `91636298932`를 확인해 `gh run view 30798135855 --job 91636298932 --log-failed`로 원문을 확보했다.
- 실패 원문 발췌:

```text
      187 |
      188 |     expect(sources).toContain('/admin/notifications/dispatch-batch/preview')
    > 189 |     expect(sources).toContain('/admin/notifications/dispatch-batch/send')
          |                     ^
      190 |     expect(sources).not.toContain('/arologis/dispatch-sms/preview')
      191 |     expect(sources).not.toMatch(/\/arologis\/dispatch-sms\/send(?![-\w])/)
      192 |     expect(sources).toContain('/admin/arologis/dispatches/history')

        at /home/runner/work/Samhan-Public/Samhan-Public/clients/desktop/playwright/sp-08-3-dispatch-parity/sp-08-3-dispatch-parity.spec.ts:189:21

      1 failed
        [chromium] › ... › 현재 API/route 소스가 6개 기존 endpoint를 유지하고 SP-08-3-2 arologis history endpoint를 허용한다
      34 passed (4.0s)
    ##[error]Process completed with exit code 1.
```

- 수정 전 판정: `sources`에 `/admin/notifications/dispatch-batch/send`가 없어 SP-08-3 문서/소스 계약 단언이 실패했다. 이 실패는 H-2 경로 가드와 별개의 원인인지 확인이 필요하다.

### 3. 수정 전 실패 원문 수집 — Frontend Desktop

- PR check 상세에서 CI 런 `30798135835`, 잡 `91636664929`를 확인했다. `gh api repos/ewoo14/Samhan-Public/actions/jobs/91636664929/logs`로 잡 로그를 확보했다.
- 실패 원문:

```text
 FAIL  src/renderer/test-utils/harness-false-green-guard.test.ts > 하네스 거짓 green 가드 > H-2: 캡처 목적지로 쓰이는 docs/qa 경로 상수는 전부 resolveQaShotsDir 를 경유한다
AssertionError: 커밋 QA 증거로 직접 쓰는 경로 상수 발견 — resolveQaShotsDir() 경유 필수:
1013-dispatch-inherit-real-qa/dispatch-inherit-real-qa.spec.ts → const SHOTS: expected [ Array(1) ] to deeply equal []

- Expected
+ Received

- Array []
+ Array [
+   "1013-dispatch-inherit-real-qa/dispatch-inherit-real-qa.spec.ts → const SHOTS",
+ ]

❯ src/renderer/test-utils/harness-false-green-guard.test.ts:666:7
Test Files  1 failed | 197 passed (198)
Tests       1 failed | 1746 passed (1747)
##[error]Process completed with exit code 1.
```

- 수정 전 판정: PM이 확인한 대로 라이브QA 스펙의 `const SHOTS`가 resolver를 우회해 H-2에서 실패했다. Frontend Desktop은 `npm test`의 이 가드를 포함하므로 Desktop Playwright와 같은 원인이다.
