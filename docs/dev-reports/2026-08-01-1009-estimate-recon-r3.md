# 2026-08-01 T1009 견적 계승 대조 조사 — 3라운드

## 판정표

| 항목 | 레거시 원문 + `파일:행번호` | 웹 원문 + `파일:행번호` | 같음 / 다름 / 확인불가 | 다르면 무엇이 |
|---|---|---|---|---|
| 1. AC/AP 모델코드 위치 분기 | `if (m.startsWith('AC') && m.length >= 9) {`<br>`if (m[7] === '6' && m[8] === 'P') is360 = true;`<br>`if (m[7] === '4' && (m[8] === 'P' \|\| m[8] === 'D')) is4way = true;`<br>`if (m[7] === '1' && (m[8] === 'P' \|\| m[8] === 'D')) is1way = true;`<br>`}`<br>`if (m.startsWith('AP') && m.length >= 9) {`<br>`if (m.length >= 11 && m[10] === 'C') {`<br>`if (m[8] === 'D') isStand = true;`<br>`} else {`<br>`if (m[8] === 'P') isStand = true;`<br>`}`<br>`if (m.length >= 11 && m[8] === 'D' && m[10] === 'H') isDeluxe = true;`<br>(`tools/legacy-gas/종합견적서/index.html:2204-2215`) | `if (m.startsWith('AC') && m.length >= 9) {`<br>`if (m[7] === '6' && m[8] === 'P') is360 = true;`<br>`if (m[7] === '4' && (m[8] === 'P' \|\| m[8] === 'D')) is4way = true;`<br>`if (m[7] === '1' && (m[8] === 'P' \|\| m[8] === 'D')) is1way = true;`<br>`}`<br>`if (m.startsWith('AP') && m.length >= 9) {`<br>`if (m.length >= 11 && m[10] === 'C') {`<br>`if (m[8] === 'D') isStand = true;`<br>`} else {`<br>`if (m[8] === 'P') isStand = true;`<br>`}`<br>`if (m.length >= 11 && m[8] === 'D' && m[10] === 'H') isDeluxe = true;`<br>(`clients/web/estimate-app/views/index.ejs:2338-2349`) | **같음** | 차이 없음. 정확한 위치는 0-based `m[7]`·`m[8]`·`m[10]`, 즉 사람 기준 **8·9·11번째 문자**다. 앞선 “7·8번째 자리” 표현은 인덱스와 사람 기준 자릿수를 혼용했다. |
| 2. `AP230`/`AP290` 예외 | `if (m.startsWith('AP230') \|\| m.startsWith('AP290')) {`<br>`isStand = true;`<br>`isDeluxe = false;`<br>`}` (`tools/legacy-gas/종합견적서/index.html:2217-2220`) | `if (m.startsWith('AP230') \|\| m.startsWith('AP290')) {`<br>`isStand = true;`<br>`isDeluxe = false;`<br>`}` (`clients/web/estimate-app/views/index.ejs:2351-2354`) | **같음** | 차이 없음. 양쪽 모두 AP 일반 판정 뒤에 실행되어 stand를 참, deluxe를 거짓으로 덮어쓴다. |
| 3. 1등급 `F` 위치 규칙 | `if ((m.startsWith('AC') \|\| m.startsWith('AP')) && m.length >= 9 && m[8] === 'F') {`<br>`isGrade1 = true;`<br>`}` (`tools/legacy-gas/종합견적서/index.html:2222-2224`) | `if ((m.startsWith('AC') \|\| m.startsWith('AP')) && m.length >= 9 && m[8] === 'F') {`<br>`isGrade1 = true;`<br>`}` (`clients/web/estimate-app/views/index.ejs:2356-2358`) | **같음** | 차이 없음. `m[8]`은 사람 기준 9번째 문자이므로, 정확히는 접두 규칙이 아니라 AC/AP 모델의 **9번째 문자 F** 규칙이다. |
| 4. `sheetPrice` 우선 분기 | 홈: `computed = (sheetPrice > 0 && !homeCustomListPrices.has(model)) ? sheetPrice : listPrice;` (`tools/legacy-gas/종합견적서/index.html:3967`)<br>상업: `computed = (sheetPrice > 0 && !commCustomListPrices.has(model)) ? sheetPrice : listPrice;` (`tools/legacy-gas/종합견적서/index.html:4078`) | 홈: `computed = (sheetPrice > 0 && !homeCustomListPrices.has(model)) ? sheetPrice : listPrice;` (`clients/web/estimate-app/views/index.ejs:4344`)<br>상업: `computed = (sheetPrice > 0 && !commCustomListPrices.has(model)) ? sheetPrice : listPrice;` (`clients/web/estimate-app/views/index.ejs:4455`) | **같음** | 차이 없음. 양쪽 모두 변동DC 미적용 `else`에서 `sheetPrice > 0`이고 해당 모델의 수동 출고가가 없을 때 sheetPrice를 우선한다. |
| 5. 유연호스 I형 강제 단가와 거래처 checkbox | 거래처 원천: `showIHose:          chk('유연호스I형'),` (`tools/legacy-gas/종합견적서/Code.js:2139`)<br>홈 적용: `const useIHose = (dc.showIHose === true);` / `setCheck('home_hose_i', useIHose);` (`tools/legacy-gas/종합견적서/index.html:2308,2344`)<br>홈 조건부: `const showIHose = document.getElementById('home_hose_i')?.checked;`<br>`if (!showIHose && /유연호스\s*I형/i.test(rawName)) return 7000;` (`tools/legacy-gas/종합견적서/index.html:3933-3934`)<br>싱글세트 무조건: `if(/유연호스I형\|I형유연호스/i.test(norm)) return 7000;` (`tools/legacy-gas/종합견적서/index.html:4720`) | 거래처 설정: `showIHose: dc.showIHose === true,` (`clients/web/estimate-app/lib/code.js:2019`)<br>홈 적용: `const useIHose = (dc.showIHose === true);` / `setCheck('home_hose_i', useIHose);` (`clients/web/estimate-app/views/index.ejs:2566,2602`)<br>홈 조건부: `const showIHose = document.getElementById('home_hose_i')?.checked;`<br>`if (!showIHose && /유연호스\s*I형/i.test(rawName)) return 8000;` (`clients/web/estimate-app/views/index.ejs:4310-4311`)<br>싱글세트 무조건: `if(/유연호스I형\|I형유연호스/i.test(norm)) return 8000;` (`clients/web/estimate-app/views/index.ejs:5100`) | **다름** | 레거시는 **7,000원**, 웹은 **8,000원**이다. PM 제공 스키마와 레거시 `Code.js:2139`가 일치하고 `index.html:2308,2344`가 그 값을 홈 checkbox에 적용하므로 거래처별 on/off가 실제 홈 가격 분기에 들어간다. 따라서 “항상 7,000원”은 부정확하다. `return 7000`/`return 8000` 전수검색 결과 직접 강제 단가 지점은 각각 5곳(`legacy index.html:3934,3977,4001,4045,4720`; `web index.ejs:4311,4354,4378,4422,5100`)이며 모두 7,000→8,000으로 달라졌다. 이 중 `setUnitPrice`의 싱글세트 경로는 양쪽 모두 checkbox 조건 없이 이름만 맞으면 강제한다. |
| 6. 거래처 `싱글 할인` 열의 실제 금액 사용 | `const idxDisc   = idx('싱글 할인');` (`tools/legacy-gas/종합견적서/Code.js:1449`)<br>`singleDiscount: parseKRNumber_(row[idxDisc])` (`tools/legacy-gas/종합견적서/Code.js:1471`) | `singleDiscount: 0,` (`clients/web/estimate-app/lib/directory.js:84`) | **같음(금액 결과), 다름(값 보존)** | 레거시는 별도 거래처 Sheets(`SpreadsheetApp.openById(SRC_SHEET_ID).getSheetByName(CUSTOMERS_NAME)`, `Code.js:1434`)에서 값을 읽어 고객 객체에 저장하지만, tracked `Code.js` 3,204행과 `index.html` 19,183행을 `singleDiscount|single.?discount|싱글\s*할인`으로 전수검색한 결과 이 필드를 다시 읽는 금액식은 **0건**이다. 즉 실제 금액에는 쓰이지 않는다. 웹의 0 고정은 원값을 버리지만 현재 금액 결과는 같다. 주석의 “싱글 할인”(`index.html:15437,15586`)은 360/4way/1way/stand/deluxe/1등급 적용내역을 뜻하며 이 거래처 열을 읽지 않는다. 이는 **찾아봤더니 사용처가 없음**이지 단순 미발견이 아니다. |
| 7. 나머지 확인불가 행 | 상업멀티 `computed = (sheetPrice > 0 && !commCustomListPrices.has(model)) ? sheetPrice : listPrice;` (`tools/legacy-gas/종합견적서/index.html:4078`) | `computed = (sheetPrice > 0 && !commCustomListPrices.has(model)) ? sheetPrice : listPrice;` (`clients/web/estimate-app/views/index.ejs:4455`) | **같음** | 1라운드의 확인불가 7행 중 별도 규칙으로 남은 것은 없다. `sheetPrice`가 홈·상업 두 행이어서 본 보고서 항목 4가 두 행을 함께 확정했고, 이 행은 그중 상업멀티 행을 명시적으로 재기록한 것이다. |

## 1라운드 확인불가 7행 대응

1. 홈멀티 `sheetPrice` → 항목 4에서 **같음**.
2. 상업멀티 `sheetPrice` → 항목 4 및 7에서 **같음**.
3. AC 위치 분기 → 항목 1에서 **같음**.
4. AP 위치 분기·예외 → 항목 1·2에서 **같음**.
5. 1등급 F 위치 규칙 → 항목 3에서 **같음**.
6. 유연호스 I형 단가 → 항목 5에서 **다름(7,000원 대 8,000원)**.
7. 거래처 `싱글 할인` → 항목 6에서 **금액식 미사용 확정**.

## 알려진 제약

- 이번 정찰에서 읽을 수 있었던 레거시 서버 원문은 `tools/legacy-gas/종합견적서/Code.js` **tracked 사본 3,204행**이다.
- PM이 확인한 live `Code.js`는 **3,577행**이므로 차이 **373행은 보지 못했다**. 따라서 “싱글 할인 사용처 없음”은 tracked `Code.js` 3,204행과 함께 tracked `index.html` 19,183행 안에서의 확정이며, 보지 못한 live 373행에 새 사용처가 있는지는 단정하지 않는다.
- 반면 항목 1~5의 계산 원문은 tracked 레거시 `index.html`과 현행 웹 `index.ejs`를 직접 문자 단위로 대조한 결과다.
