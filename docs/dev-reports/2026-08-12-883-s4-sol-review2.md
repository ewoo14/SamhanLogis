# PR #1175 S4 SOL 재검토2

- 검토 HEAD: `2a0bdbef7def558b541b45c302fd876e90766779`
- 검토 범위: 요청된 차단 3건의 수정 여부만 확인
- 제외: 라이브 QA, 전체 테스트 재실행, 새 각도 탐색
- 구현자 제시 검증 수치(재실행하지 않음): Vitest 254 files / 2,221 passed · typecheck · lint 0

## ① `.btnMini` 복구 — PASS

- CSS 정의: `clients/desktop/src/renderer/components/sales/sales.module.css:54`에 `.btnMini` 규칙이 존재한다.
- 연결 경로: 아래 두 설정 화면 모두 같은 `../components/sales/sales.module.css`를 `styles`로 import한다.
  - `SalesPartnerDcConfigPage.tsx`: `styles['btnMini']` 5개(228, 236, 330, 349, 426행)
  - `EstimatePricingConfigPage.tsx`: `styles['btnMini']` 2개(345, 353행)
- 판정: CSS 모듈 정의와 설정 화면 7개 참조가 직접 연결되어 있으므로 복구 확인.

## ② 삭제 selector의 동적 조립 사용 — PASS

- 대상: `sales.module.css`를 import하는 runtime 파일 8개와 1차 승인 삭제 selector 44개.
- `styles[...]` 접근 126건을 grep 대조한 결과 모두 `styles['literalKey']` 형태였고, `styles[변수]`·조건식을 index로 쓰는 비리터럴 접근은 0건이다.
- ``styles[`x${y}`]`` 형태, selector 문자열 연결, `clsx`/`classNames`/`classnames`, CSS module 키 객체 spread는 모두 0건이다.
- 조건부 class 조합은 `styles['dirty']` 등 현존 literal key만 선택한다. `STATUS_CLASS[a.status]`도 객체 값이 현존 selector 6개의 literal 접근으로 고정되어 있다.
- 판정: 승인 삭제 selector 44개 중 동적 조립으로 사용되는 selector 없음.

## ③ 범위 밖 삭제 0건 — FAIL

- 좌표: `tools/.s24-build-only/build/deep/tracked-writer.mjs`
- 재현:

  ```powershell
  git diff --name-status --diff-filter=D origin/main...HEAD
  ```

  출력:

  ```text
  D	tools/.s24-build-only/build/deep/tracked-writer.mjs
  ```

- 판정: 삭제 1건이 남아 있으므로 범위 밖 삭제 0건 조건 불충족.

## 최종 판정

차단. ①·②는 통과했으나 ③이 실패했다. `tracked-writer.mjs` fixture를 `origin/main` 기준으로 복구한 뒤 삭제 목록 0건을 재확인해야 한다.
