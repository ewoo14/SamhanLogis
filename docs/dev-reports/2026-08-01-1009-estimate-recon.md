# 레거시 종합견적서 가격·계산 규칙 대조 보고서

| 레거시 규칙 (원문 인용 + `Code.js:행번호`) | 현행 대응 위치 (`파일:행번호`) 또는 없음 | 같은가 / 다른가 / 확인불가 | 다르다면 구체적으로 무엇이 다른가 |
|---|---|---|---|
| `const DISCOUNT_RATE_HOME = 0.45;   // 홈멀티 전역 할인율 (기본값)` (`Code.js:59`) | `services/dc-config-service/src/main/resources/db/migration/V4__add_estimate_config.sql:7-8`, `services/dc-config-service/src/main/java/com/samhanair/logis/dcconfig/service/PriceCalculationService.java:115-118` | 같은가 | 레거시 45%와 현행 기본값 45%가 같고, 현행은 `dc_configs`의 거래처별 홈·상업율을 선택한다. |
| `const DISCOUNT_RATE_COMM = 0.45;   // 상업멀티 전역 할인율 (기본값)` (`Code.js:60`) | `services/dc-config-service/src/main/resources/db/migration/V4__add_estimate_config.sql:8`, `services/dc-config-service/src/main/java/com/samhanair/logis/dcconfig/service/PriceCalculationService.java:115-118` | 같은가 | 레거시 45%와 현행 기본값 45%; 현행은 거래처별 `commercialDiscountRate`로 대체 가능하다. |
| `const DISCOUNT_360_AMT        = 0;     // 360 할인액` 및 동일한 `DISCOUNT_4WAY_AMT`, `DISCOUNT_STAND_AMT`, `ONEWAY_DISCOUNT_AMT`, `DELUXE_DISCOUNT_AMT`, `FIRSTGRADE_DISCOUNT_AMT` (`Code.js:62-67`) | `services/dc-config-service/src/main/resources/db/migration/V1__init_dc_config.sql:56-61`, `services/dc-config-service/src/main/java/com/samhanair/logis/dcconfig/service/PriceCalculationService.java:129-135` | 같은가 | 레거시 기본값 6종은 모두 0이고 현행 `dc_configs`에도 같은 6종 정액 필드와 차감 경로가 있다. |
| `const UNIT_ROUND_TO           = 0;       // 0이면 단위 처리 없음, 그 외 10/100/1000` / `const UNIT_ROUND_MODE         = 'ROUND'; // ROUND / CEIL / FLOOR` (`Code.js:68-69`) | `services/dc-config-service/src/main/java/com/samhanair/logis/dcconfig/service/PriceCalculationService.java:142-153`, `services/dc-config-service/src/main/resources/db/migration/V2__add_unit_processing_flag.sql:1-20` | 다른가 | 레거시는 0이면 금액을 그대로 반환하지만 현행은 `unitRoundTo`가 0/NULL이어도 `setScale(0, HALF_UP)`으로 1원 단위 반올림한다. |
| `if (isVarChecked && listPrice > 0) {` → `const finalRate = (parsedFixed !== null) ? parsedFixed : globalRate;` → `computed = Math.round(listPrice * (1 - finalRate));` (`index.html:3928-3970`) | `services/dc-config-service/src/main/java/com/samhanair/logis/dcconfig/service/PriceCalculationService.java:60-67,98-119`, `clients/web/estimate-app/views/index.ejs:4305-4347` | 다른가 | 현행 웹은 같은 고정DC 우선·전역DC fallback을 포팅했지만, 현행 서비스는 `fixedDiscountRate`가 percent(예: 45.00)이면 0.45로 정규화한다. 레거시 `parseFixedDc`는 1 초과를 퍼센트로 처리하며 결과는 동일하다. 서비스의 `hasVariableDiscount=false`는 전역DC 0%로 처리한다. |
| `computed = (sheetPrice > 0 && !homeCustomListPrices.has(model)) ? sheetPrice : listPrice;` (`index.html:3928-3970`) | `clients/web/estimate-app/views/index.ejs:4305-4347`; `services/product-service/src/main/java/com/samhanair/logis/product/domain/Product.java:646-653` | 확인불가 | 현행 웹 포팅에는 있지만, 서비스 계산 요청은 `listPrice`만 받으므로 레거시의 `sheetPrice`/수동 출고가 우선 분기를 서비스 코드만으로 확인할 수 없다. |
| `computed = (sheetPrice > 0 && !commCustomListPrices.has(model)) ? sheetPrice : listPrice;` (`index.html:4039-4081`) | `clients/web/estimate-app/views/index.ejs:4416-4458`, `services/dc-config-service/src/main/java/com/samhanair/logis/dcconfig/service/PriceCalculationService.java:60-67` | 확인불가 | 현행 웹에는 동일 분기가 있으나, 백엔드 `PriceCalculationService`는 `sheetPrice` 인자를 받지 않고 `listPrice`만 계산하므로 서비스 경로의 동일성은 확인불가이다. |
| `const calc = (val, rateAmt) => Math.max(0, val - rateAmt);` 및 6종 `if` 순차 적용 (`index.html:3963-3970`) | `services/dc-config-service/src/main/java/com/samhanair/logis/dcconfig/service/PriceCalculationService.java:64-67,122-136` | 다른가 | 레거시 싱글 경로는 6종 정액DC를 싱글 본체에 순차 차감한다. 현행 서비스는 `HOMEMULTI`/`COMMERCIAL_MULTI`에 6종을 적용하지 않도록 명시하고, 그 외 라인에만 6종 합계를 한 번에 차감한다. |
| `if (m[7] === '6' && m[8] === 'P') is360 = true;` / `if (m[7] === '4' && (m[8] === 'P' || m[8] === 'D')) is4way = true;` / `if (m[7] === '1' && (m[8] === 'P' || m[8] === 'D')) is1way = true;` (`index.html:2199-2201`) | `services/product-service/src/main/java/com/samhanair/logis/product/service/VariableDiscountDetector.java:105-122`, `services/product-service/src/main/java/com/samhanair/logis/product/domain/Product.java:142-145,616-618` | 확인불가 | 현행은 동일한 6비트 순서의 `discountFlags`를 저장·전달하지만 실제 정규식 상수의 AC 위치 7/8 규칙은 확인하지 못했다. |
| `if (m.length >= 11 && m[10] === 'C') { if (m[8] === 'D') isStand = true; } else { if (m[8] === 'P') isStand = true; }` 및 `AP230`, `AP290` 예외 (`index.html:2203-2210`) | `services/product-service/src/main/java/com/samhanair/logis/product/service/VariableDiscountDetector.java:111-122`, `services/product-service/src/main/java/com/samhanair/logis/product/domain/Product.java:616-618` | 확인불가 | 현행 6비트 저장은 확인했지만 AP230/AP290 예외까지 동일하다는 근거는 확인하지 못했다. |
| `if ((m.startsWith('AC') || m.startsWith('AP')) && m.length >= 9 && m[8] === 'F') isGrade1 = true;` (`index.html:2212-2214`) | `services/product-service/src/main/java/com/samhanair/logis/product/domain/Product.java:616-618` | 확인불가 | 현행에 6-bit `discount_flags` 저장은 있으나, 이 행만으로 1등급 F 접두 규칙의 동일 여부는 확인불가이다. |
| `if (isAcc) return v;` (`index.html:2997-3000`) — 싱글 부자재는 옵션 정액 할인 대상에서 제외 | `clients/web/estimate-app/views/index.ejs:3255-3258`, `services/dc-config-service/src/main/java/com/samhanair/logis/dcconfig/service/PriceCalculationService.java:122-136` | 다른가 | 현행 웹은 같은 제외 규칙이지만, 현행 서비스는 category가 `HOMEMULTI`/`COMMERCIAL_MULTI`가 아니면 `is360` 등 flags에 따라 정액DC를 적용하며 `isAcc`/부자재 제외 조건이 없다. |
| `let setPrice = baseL + panelDelta + remoteDelta + matIncludedTotal;` → `const unitExtras = setPrice - Math.round(Number(baseL) || 0);` → `setPrice = baseAdjusted + unitExtras;` (`index.html:5095-5150`) | `services/product-service/src/main/java/com/samhanair/logis/product/web/dto/ExpandRequest.java:12-17`, `services/product-service/src/main/java/com/samhanair/logis/product/service/BundleExpander.java:76-123,291-326` | 같은가 | 현행은 `setUnitOverride`/`deliveryPrice`를 세트 단가로 사용하고, 싱글세트 옵션 선별 후 가정용 6:4·비가정 4:6 재배분까지 구현한다. |
| `if(/유연호스I형|I형유연호스/i.test(norm)) return 7000;` 및 `if (!showIHose && /유연호스\s*I형/i.test(rawName)) return 7000;` (`index.html:3942-3943, 4061-4062, 4715-4720`) | `clients/web/estimate-app/views/index.ejs:4351-4364`, `services/product-service/src/main/java/com/samhanair/logis/product/domain/Product.java:646-653` | 확인불가 | 현행 웹에는 7,000원 고정 규칙이 포팅됐지만, 현행 서비스의 Product/BundleExpander 경로에서 동일 품목명 분기를 확인하지 못했다. |
| `const ratioIn = isHousehold ? 6 : 4;` / `const ratioOut = isHousehold ? 4 : 6;` (`index.html:4827-4828`) | `services/product-service/src/main/java/com/samhanair/logis/product/service/BundleExpander.java:291-317` | 같은가 | 현행에도 가정용 6:4, 비가정 4:6이 그대로 있다. 현행은 이어서 고정부품 선차감·천원 단위 배분까지 `:318-383`에서 처리한다. |
| `const sup = Math.round(Math.abs(total) / 1.1);` / `const vat = Math.abs(total) - sup;` (`Code.js:1849-1850`) | `clients/web/estimate-app/lib/code.js:461-468`, `clients/desktop/src/renderer/utils/vatRounding.ts:19-22` | 다른가 | 레거시는 1.1로 나눈 공급가를 `Math.round`하고 잔액을 VAT로 삼는다. 현행 estimate web도 `vatRate`(기본 10%)로 나눠 `Math.round`하지만, desktop 공통 VAT 포함 분리는 `100/110` 정수 나눗셈으로 원 미만을 절사한다. |

| `const idxDisc   = idx('싱글 할인');` (`Code.js:1449`) | `services/partner-service/src/main/java/com/samhanair/logis/partner/domain/PartnerPriceDiscount.java:47-60` | 확인불가 | 레거시는 거래처 시트의 `싱글 할인` 열을 읽는 것까지 확인했지만 실제 금액 계산식 연결은 확인하지 못했다. 현행에는 `partners`의 기본 할인율 `basic_discount_rate`가 있으나 레거시 견적 경로와 동일 적용이라는 근거는 확인불가이다. |

| `const price  = idxPrice >= 0 ? parseKRNumber_(row[idxPrice]) : 0;` / `const list   = idxList  >= 0 ? parseKRNumber_(row[idxList])  : 0;` (`Code.js:407-408`) — 홈멀티 단가는 시트의 납품가·출고가에서 읽음 | `services/product-service/src/main/java/com/samhanair/logis/product/domain/Product.java:75,190`, `clients/web/estimate-app/views/index.ejs:3936-3937` | 같은가 | 현행에도 품목 `sellingPrice`/`deliveryPrice`가 있고 웹 포팅도 `r.price`/`r.list`를 사용한다. 다만 레거시의 Sheets 원본이 현행 DB로 동기화된 값이라는 운영 경로까지는 코드 대조만으로 확인불가이다. |
| `let fixedDcVal = r['고정DC'] ?? r.fixedDC;` (`index.html:3946`) — 품목별 고정DC 우선 | `services/product-service/src/main/java/com/samhanair/logis/product/domain/Product.java:109-114`, `services/dc-config-service/src/main/java/com/samhanair/logis/dcconfig/service/PriceCalculationService.java:98-105` | 같은가 | 레거시 `고정DC`와 현행 `products.fixed_discount_rate`가 모두 품목별 값이며, 값이 있으면 전역DC보다 우선한다. 현행 저장 스케일은 percent(45.00), 레거시 입력 스케일은 분수(0.45)도 허용한다. |

| `if(item.isDisc) {` / `p = Math.round(p * (1 - rateVal/100));` / `p = Math.round(Number(item.sheetPrice)||0);` (`index.html:2249-2268`) — 구형 품목은 기본 입력 50% 할인율, 비대상은 시트 납품가 | `clients/web/estimate-app/views/index.ejs:2384-2402`, `clients/web/estimate-app/lib/code.js:421-424` | 같은가 | 현행 웹은 동일한 `oldProductDiscountRate` 설정과 `isDisc` 분기를 사용한다. 다만 현행 서비스 `PriceCalculationService`에는 구형 품목 전용 경로가 없다. |
