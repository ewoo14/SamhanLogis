# #1002 입금보고서 편집 — 트랙 착수 (조기 PR)

- 이슈: #1002
- 브랜치: `feat/1002-cash-receipt-edit`
- 착수: 2026-07-31
- 단계: **정찰(recon)** — 구현 전

## 이 트랙이 다루는 것

| # | 요구 | 상태 |
|---|---|---|
| ① | 거래처 재지정 | 정찰 중 |
| ② | 입금액 분할 등록 | 정찰 중 |

**개발책임자 확정** — 입금보고서는 **결재문서가 아니다**(입금내역을 그룹화한 것). 용어 `돈들어온` → **`입금`**.

## 이번 라운드의 경계

- 산출물은 **정찰 보고서 1개**. 소스 코드 변경 없음.
- 공유 DB 는 **읽기 전용 조회만**. Docker 이미지 재빌드·서비스 재기동 금지.

## 이 트랙의 핵심 위험 — 무결성 경계

입금 데이터는 회계 원장·전표·매출채권 잔액에 반영된다. **편집·분할이 이미 반영된 회계 수치를 어떻게 바꾸는지**가 이 이슈의 위험 지점이다. 정찰 단계에서 연쇄로 바뀌는 값을 전부 목록화한다.

> 무결성 도메인(회계 원장·감사·권한)의 편집 가부 정책은 착수 전 개발책임자 확인 사항이다. 정찰 결과에 정책 결정이 필요한 항목이 나오면 **PM 이 임의로 정하지 않고 올린다.**

## 용어 규율

- **전표**(한글) — "슬립" 금지. 영문 식별자 `slipId` 는 유지.
- 권한 표기 풀네임. 화면 UUID 노출 금지.

## 다음 단계

1. 정찰 보고서 게시 (본 PR 코멘트, 1:1)
2. 정책 미결 항목이 있으면 개발책임자 확인
3. 슬라이스 확정 → 구현 → 적대검증 → 라이브QA → 머지
