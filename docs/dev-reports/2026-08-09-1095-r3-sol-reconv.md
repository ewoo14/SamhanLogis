# PR #1133 / Issue #1095 — SOL 5.6 R3 재수렴 적대검증

- 검증일: 2026-08-09 KST
- 질문: 실 사용자 경로로 재현 가능한 결함이 있는가
- 결론: **있음 — 머지 차단 결함 3건**
- git commit / push: 수행하지 않음

## 1. 환경 확인

| 항목 | 실측 |
|---|---|
| 워크트리 | `C:\dev\Samhan-Public\.claude\worktrees\t1095` |
| 브랜치 | `feat/1095-sheet-product-status` |
| 검증 HEAD | `1fb6e0a4cc92b816cbeafb5a7c090d543f641198` |
| product-service | 격리 컨테이너 `t1095-product-1fb6`, `127.0.0.1:28084 -> 8084` |
| estimate-app | `http://127.0.0.1:5195` |
| desktop | `http://127.0.0.1:5295`, product 호출만 `http://127.0.0.1:5296` 프록시를 거쳐 `:28084` |
| product health | `GET http://127.0.0.1:28084/actuator/health` → `{"status":"UP"}` |

`product-service`만 현 HEAD에서 clean build해 재배포했다.

```text
로컬 services/product-service/build/libs/product-service.jar
SHA-256 10E9D16508107D2BB0374F9EE25ECDDDF20FBA4F41A5D13D9A55D0AE79715690

컨테이너 /app/app.jar
sha256 10e9d16508107d2bb0374f9ee25ecdddf20fba4f41a5d13d9a55d0ae79715690
```

따라서 배포 JAR은 검증 대상 `1fb6e0a4c` 소스의 clean build와 동일하다. 실제 estimate-app 호출은 다음 네 endpoint였고 모두 HTTP 200이었다.

```text
/products/internal/estimate-catalog/products?category=HOME_MULTI&scope=ESTIMATE
/products/internal/estimate-catalog/products?category=SINGLE_SET&scope=ESTIMATE
/products/internal/estimate-catalog/products?category=COMMERCIAL_MULTI&scope=ESTIMATE
/products/internal/estimate-catalog/products?category=LEGACY&scope=ESTIMATE
```

데스크톱은 다른 서비스에는 기존 `:8080` gateway를 사용하고 `/api/products...`만 위 JAR로 보냈다. DB INSERT/UPDATE와 저장 버튼 클릭은 하지 않았다. 공유 DB의 `QA-`, `S18`, `QA797` 잔재는 결함으로 세지 않았다.

## 2. 판정

| ID | 심각도 | 실 사용자 결함 |
|---|---|---|
| R3-F1 | BLOCKER | `안전재고 알림`과 `견적품목 관리`가 상태 필터 없는 공용 검색을 사용한다. 안전재고에서는 단종·미판매가 실제 설정 대상으로 선택되고, 견적품목 관리에서는 단종이 실제 추가 후보가 된다. |
| R3-F2 | BLOCKER | `OUT_OF_STOCK` 품목을 데스크톱 견적서에서 선택하면 수량 입력이 활성 상태다. “보이되 수량만 잠금” 계약을 위반한다. |
| R3-F3 | BLOCKER | 같은 `OUT_OF_STOCK` 표본을 판매전표에서 선택하면 `POST /slips/expand-line`이 HTTP 500이고 선택이 지워진다. 품절 후보를 사용할 수 없다. |

원 결함은 종합견적서 웹, 데스크톱 견적·판매전표, 주문 웹의 R2 대상 경계에서는 해소됐다. 그러나 “품목을 고르는 모든 화면”을 원문 grep한 결과 R2가 세지 않은 두 화면이 남아 R3-F1이 재현됐다. 이것이 R2 문서의 “후보 경로 네 경계뿐” 외 **셋째 가능성**이다.

## 3. 각도 1 — 원 결함 해소

R1 SQL을 그대로 다시 실행했다.

```sql
SELECT p.status, e.estimate_category, COUNT(*) AS count
FROM products p
JOIN product_estimate_exposure e
  ON e.product_id = p.id
 AND e.is_deleted = false
WHERE p.is_deleted = false
  AND p.usage_scope IN ('ESTIMATE', 'BOTH')
GROUP BY p.status, e.estimate_category
ORDER BY p.status, e.estimate_category;
```

```text
ACTIVE|COMMERCIAL_MULTI|382
ACTIVE|HOME_MULTI|107
ACTIVE|LEGACY|39
ACTIVE|SINGLE_SET|223
DISCONTINUED|COMMERCIAL_MULTI|24
DISCONTINUED|HOME_MULTI|14
DISCONTINUED|LEGACY|1
DISCONTINUED|SINGLE_SET|50
NOT_FOR_SALE|COMMERCIAL_MULTI|2
NOT_FOR_SALE|SINGLE_SET|12
OUT_OF_STOCK|SINGLE_SET|3
```

발화 조건은 R1과 동일하다.

| 상태 | DB 후보 | exact-JAR estimate API | 판정 |
|---|---:|---:|---|
| `DISCONTINUED` | 89 | 0 | 종합견적서 후보에서 제거됨 |
| `NOT_FOR_SALE` | 14 | 0 | 종합견적서 후보에서 제거됨 |
| `OUT_OF_STOCK` | 3 | 3 | 부정 목록에서 살아남음 |
| `ACTIVE` | 751 | 751 | 오차단 0 |

카테고리별 exact-JAR 응답 원문은 다음과 같다.

```text
HOME_MULTI       107 = ACTIVE 107
SINGLE_SET       226 = ACTIVE 223 + OUT_OF_STOCK 3
COMMERCIAL_MULTI 382 = ACTIVE 382
LEGACY            39 = ACTIVE 39
```

종합견적서 실 GUI에서도 단종 `AC072BSCPBH2SY`와 미판매 `AF60F17D11LS`는 0행, 품절 `AR60F09C13WS`는 1행이었다. 데스크톱 견적·판매전표에서도 두 금지 상태를 정확 검색했을 때 품목명/선택이 채워지지 않았고 `/slips/expand-line` 호출은 0회였다.

- `docs/qa/2026-08-09-1095-r3/01-discontinued-hidden.png`
- `docs/qa/2026-08-09-1095-r3/02-not-for-sale-hidden.png`

## 4. 각도 2 — 반대급부

### 4.1 종합견적서 웹

`OUT_OF_STOCK` 3건은 모두 API에 남았다. 표본 `AR60F09C13WS`는 실 화면에 `품절`로 보였고 사용자 수량 input은 없었다. `ACTIVE` 표본 `AC060CS6PBH1SY`는 수량 2 입력 후 소계 `3,320,000`으로 계산됐다. 전체 `ACTIVE` 751건의 API 누락은 0건이다.

- `docs/qa/2026-08-09-1095-r3/03-out-of-stock-visible-locked.png`
- `docs/qa/2026-08-09-1095-r3/04-active-visible-quantity.png`

### 4.2 데스크톱 반대급부 결함

같은 품절 표본은 `product_type=BUNDLE`, `usage_scope=BOTH`다.

```sql
SELECT model_name, status, usage_scope, product_type
FROM products
WHERE status = 'OUT_OF_STOCK' AND is_deleted = false
ORDER BY model_name;
```

```text
AR60F07D11WS|OUT_OF_STOCK|BOTH|BUNDLE
AR60F09C13WS|OUT_OF_STOCK|BOTH|BUNDLE
AR60F16C14WS|OUT_OF_STOCK|BOTH|BUNDLE
```

- 견적서 작성: `AR60F09C13WS` 선택 후 품목명·단가가 채워졌지만 수량 input은 `enabled`, 값 `1`이었다 → R3-F2.
- 판매전표 작성: 동일 품목 선택 직후 `POST /slips/expand-line` → HTTP 500, 화면은 “세트 구성품을 불러오지 못했습니다. 다시 선택해 주세요.”를 표시하고 선택값을 비웠다 → R3-F3.

- `docs/qa/2026-08-09-1095-r3/07-estimate-out-of-stock-quantity-unlocked-defect.png`
- `docs/qa/2026-08-09-1095-r3/08-slip-out-of-stock-selection-fails-defect.png`

즉 부정 목록 자체는 `OUT_OF_STOCK`을 살렸지만, 사용자 계약은 웹 한 경로에서만 보존됐다.

## 5. 각도 3 — 후보 경로 전수

grep 기준은 `ProductAutocomplete`, `ProductMultiSelectAutocomplete`, `searchProducts`, `getProducts`, `estimate-catalog`, `isSelectableProductStatus`였다. 관리 목록처럼 품목 상태를 보아야 하는 화면은 제외하고, 새 문서/설정에 품목을 **고르는** 소비처를 전수했다.

| 사용자 화면 / 경계 | 원문 경로 | R2 반영 | 실측 |
|---|---|---:|---|
| 종합견적서 웹 | `ProductRepository.findExposedCatalog` → `ProductCatalogResponse` → `estimate-app/lib/db-catalog.js` | O | 단종 0, 미판매 0, 품절 3, ACTIVE 751 |
| 데스크톱 견적서 작성 | `EstimateFormPage.tsx:1113-1119, 2207` | O | 단종·미판매 선택 안 됨. 품절 수량 미잠금(R3-F2) |
| 데스크톱 판매전표 작성 — 반응형 두 렌더 | `SlipFormPage.tsx:2487-2488, 2568-2569` | O | 단종·미판매 선택 안 됨. 품절 선택 500(R3-F3) |
| 파트너 주문 웹 | `order-app/src/samhanApi.ts:407-416` | O | 상태 필터 원문 및 대상 테스트 통과 |
| 안전재고 설정 | `SafetyStockAlertsPage.tsx:292-298` → raw `searchProducts` | **X** | 단종 `AC072...`, 미판매 `AF60...` 모두 실제 설정 선택 완료, 저장 버튼 활성(R3-F1). 저장은 누르지 않음 |
| 견적품목 관리의 기초품목 추가 | `EstimateItemsCatalogPage.tsx:993, 1251-1255` → raw `searchProductsApi` | **X** | 단종 `AC072...` chip과 `1건 추가` 노출(R3-F1) |
| 공용 디자인시스템 | `ProductAutocomplete.tsx` | 타입만 반영 | `status` 필드만 추가하며 자체 필터 없음. 위 두 raw consumer 누락을 막지 못함 |
| 데스크톱 product API | `productApi.ts:54, 103-104` | mapper/helper만 반영 | raw 검색은 관리 화면 보존을 위해 전체 상태 반환. 소비처 필터 누락 시 그대로 노출 |

R3-F1 실 GUI:

- `docs/qa/2026-08-09-1095-r3/05-safety-stock-discontinued-visible-defect.png`
- `docs/qa/2026-08-09-1095-r3/06-estimate-items-discontinued-visible-defect.png`

대상 단위 검증은 desktop `productApi.search-modal.test.ts` 3/3, order-app `samhanApi.test.ts` 23/23 통과했다. 이 통과는 raw consumer 두 곳의 누락을 반증하지 않는다.

## 6. 각도 4 — 기존 저장 문서

상태 품목은 현재 실 데이터 기준 100개다(`DISCONTINUED` 83개 품목 + `NOT_FOR_SALE` 14 + `OUT_OF_STOCK` 3). 노출행 89는 한 단종 품목의 복수 카테고리 노출을 포함한다.

```sql
SELECT COUNT(*) AS line_count,
       COUNT(DISTINCT estimate_id) AS document_count,
       COALESCE(SUM(line_total), 0) AS amount
FROM estimate_lines
WHERE is_deleted = false AND product_id IN (<상태 품목 100개 UUID>);

SELECT COUNT(*), COUNT(DISTINCT slip_id), COALESCE(SUM(line_total), 0)
FROM slip_lines
WHERE is_deleted = false AND product_id IN (<상태 품목 100개 UUID>);

SELECT COUNT(*), COUNT(DISTINCT partner_order_id), COALESCE(SUM(subtotal), 0)
FROM partner_order_lines
WHERE is_deleted = false AND product_id IN (<상태 품목 100개 UUID>);
```

```text
estimate_lines|0|0|0
slip_lines|0|0|0
partner_order_lines|0|0|0
```

snapshot은 R1과 같이 실제 선택 수량 배열만 펼쳤다.

```sql
SELECT COUNT(*), COUNT(DISTINCT q.id), COALESCE(SUM(q.total_amount), 0)
FROM quote_snapshots q
CROSS JOIN LATERAL (
  VALUES ('homeQty'), ('singleQty'), ('commQty'), ('oldQty')
) k(key)
CROSS JOIN LATERAL jsonb_array_elements(q.snapshot_state->'core'->k.key) e(elem)
WHERE q.is_deleted = false
  AND (e.elem->>1)::numeric > 0
  AND (e.elem->>0) IN (<상태 품목 100개 모델명>);
```

```text
selected_snapshot_items|0|0|0
```

R1·R2와 동일하게 실제 저장 표본은 0건이다. 열어서 금액 불변을 확인할 문서가 없으므로 **판정 불가**다.

## 7. 증거 무결성 및 실행 결과

- 캡처는 모두 `resolveQaShotsDir()` 경유이며 `_local`을 사용하지 않았다.
- `resolveQaCredential`은 각 테스트 본문의 `try/catch` 뒤 `test.skip` 규약을 지켰다.
- 최종 실 UI 시나리오: estimate-app 1/1 통과, desktop 세 시나리오 개별 통과.
- 데스크톱 상단 updater 404와 `/logs/front` 503은 현재 HEAD 상태 필터 결함으로 세지 않았다.
- DB 직접 INSERT/UPDATE와 기존 문서 저장은 수행하지 않았다.
- 검증 종료 후 전용 estimate/desktop/proxy 프로세스와 `t1095-product-1fb6` 컨테이너를 중지하고 `test-results` 파생물을 제거했다.

## 8. 신규 생성 파일

```text
docs/dev-reports/2026-08-09-1095-r3-sol-reconv.md
docs/qa/2026-08-09-1095-r3/estimate-catalog-live.spec.cjs
docs/qa/2026-08-09-1095-r3/desktop-candidate-leak-live.spec.cjs
docs/qa/2026-08-09-1095-r3/product-service-proxy.cjs
docs/qa/2026-08-09-1095-r3/01-discontinued-hidden.png
docs/qa/2026-08-09-1095-r3/02-not-for-sale-hidden.png
docs/qa/2026-08-09-1095-r3/03-out-of-stock-visible-locked.png
docs/qa/2026-08-09-1095-r3/04-active-visible-quantity.png
docs/qa/2026-08-09-1095-r3/05-safety-stock-discontinued-visible-defect.png
docs/qa/2026-08-09-1095-r3/06-estimate-items-discontinued-visible-defect.png
docs/qa/2026-08-09-1095-r3/07-estimate-out-of-stock-quantity-unlocked-defect.png
docs/qa/2026-08-09-1095-r3/08-slip-out-of-stock-selection-fails-defect.png
```

커밋과 push는 수행하지 않았다.
