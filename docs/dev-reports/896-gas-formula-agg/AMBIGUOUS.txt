

########## range-estimate-ejs-3.json
[A001] index.ejs:4444-4449 | if (flags.is360 && d360 > 0) v = calc(v, d360); ... if (flags.isGrade1 && dFirst > 0) v = calc(v, dFirst); | (a) 6개 할인은 서로 배타적이고 실제로는 한 플래그만 참이라 순차 차감이 곧 단일 차감이다 (b) 의도적으로 중복 차감이다(예: 1등급 4WAY 는 d4way + dGrade1 둘 다 빠진다) | (a)면 전환 시 '첫 매칭 하나만' 규칙으로 굳혀야 하고, (b)면 누산 순서까지 계약이다. flags 정의(2368행)를 보면 is4way 와 isGrade1 은 동시에 참일 수 있어 현재 코드는 (b)로 동작한다
[A002] index.ejs:4435-4440 + 4442 | const d360 = parseInt(el('#ss_disc_360')?.value || 0, 10); const calc = (val, rateAmt) => Math.max(0, val - rateAmt); | (a) 6개 값은 '정액 할인 금액(원)' 이다 (b) 변수명이 disc/rate 라 '%' 로 입력받는 화면일 수 있다 | 인자명이 rateAmt(=율+금액 혼합)라 코드만으로 확정 불가. %라면 전환 시 val*(1-r) 이어야 하는데 지금은 val-r 이라 값이 조용히 달라진다
[A003] index.ejs:4416, 4461, 5139 | if (!window.SHOW_I_HOSE && /유연호스\s*I형/i.test(rawName)) { return 8000; } | (a) I형 유연호스의 실제 판매단가가 8,000원으로 고정이다 (b) '표시용 임시가'이고 실제로는 마스터 단가를 써야 한다 | 세 곳(홈/상업/싱글)에 같은 리터럴이 흩어져 있고 셋 다 반올림·DC·옵션을 전부 우회한다. 동적 전환 시 이 8000 을 어느 테이블 컬럼으로 옮길지 확정 필요
[A004] index.ejs:5182-5185 | const unitExtras = setPrice - Math.round(Number(baseL) || 0); const baseAdjusted = adjustSingleSetBasePrice(s, baseL); setPrice = baseAdjusted + unitExtras; | (a) 판넬·리모컨 차액과 자재비에는 할인을 적용하지 않는 것이 업무 규칙이다 (b) 구현 편의로 이렇게 됐고 원래는 전체에 할인이 걸려야 한다 | (b)라면 할인율 45% 기준으로 옵션 차액만큼 금액이 과다. 파생 결과(setPrice)를 다시 소스로 쓰는 2단 계산이라 전환 시 순서를 그대로 옮겨야 값이 유지된다
[A005] index.ejs:5264-5265 | const ratioIn = isHousehold ? 6 : 4; const ratioOut = isHousehold ? 4 : 6; | (a) 가정용은 실내기 60%/실외기 40%, 그 외는 실내 40%/실외 60% 라는 회계·전표 배분 규칙이다 (b) 인쇄 표시용 임의 배분이다 | 이 비율이 전표 금액을 가르므로 (a)면 품목 카테고리별 마스터 값이어야 한다. '가정용 에어컨' 판정도 catL 문자열 정규식이라 분류명이 바뀌면 조용히 4:6 으로 넘어간다
[A006] index.ejs:5268-5274 | const wallMounts = indoorParts.filter(p => /벽걸이/.test(p.name)); if (wallMounts.length > 0) { wallMounts.forEach(w => fixedParts.push(w)); ... } | (a) 가정용 세트의 벽걸이는 원 단가를 그대로 유지해야 한다(배분 대상 아님) (b) 실내기의 일종이라 배분에 들어가야 하는데 빠진 것이다 | (a)로 동작 중. 벽걸이 단가가 fixedSum 에 들어가 배분 총액이 줄어들므로 나머지 실내/실외기 단가에 직접 영향
[A007] index.ejs:5276-5280 | const setUnit = Math.round(Number(setUnitOverride)) || Math.round(Number(s.price)) || Math.round(Number(setBasePriceRightFirst(s))); | (a) override 가 0 이면 '지정 안 함'으로 보고 폴백하는 것이 의도다 (b) 0 은 '단가 0 으로 배분하라'는 뜻인데 || 때문에 폴백돼 버리는 버그다 | renderSingle 의 수량 변경 핸들러(6465)가 실제로 0 을 넘긴다. 그 호출은 활성 모델 집합만 쓰므로 현재는 무해하지만, 전환 시 ?? 로 바꾸면 동작이 달라진다
[A008] index.ejs:4965 vs 5055 | (home) const isBad = ratio > 130;  (comm) const isBad = parseFloat(ratioTxt) >= limit; | (a) 두 섹터의 경계 처리(초과 vs 이상)와 반올림 기준(원값 vs 소수1자리 포맷값) 차이가 의도된 것이다 (b) 한쪽이 실수다 | 상업은 103.00 도 bad, 홈은 130.0 은 정상. 상업은 102.96 이 '103.0' 으로 반올림돼 bad 가 된다. 한도 130/103/120 의 출처도 코드에만 있음
[A009] index.ejs:5052 | const limit = hasStrict ? 103.0 : 120.0; | (a) 9개 키워드(프라임/한랭지/표준형/냉난방/가스히트펌프/GHP/프레스티지/동시냉난방/공장전원)에 해당하는 실외기 계열은 삼성 사양상 조합비 상한이 103% 다 (b) 그 중 일부는 다른 상한이다 | 특히 '냉난방' 은 매우 광범위한 단어라 일반 상업 실외기 다수가 103 으로 걸릴 수 있다. 실 데이터에서 몇 행이 매칭되는지 확인 필요
[A010] index.ejs:4920 | var arr = inCount > 2 ? RECOMMEND_DATA.homeEx : RECOMMEND_DATA.home; | (a) 실내기 3대 이상이면 확장 추천표 (b) 2 는 다른 뜻(2WAY 등)의 잔재다 | 추천 실외기 HP 가 통째로 바뀌는 분기인데 근거가 코드에만 있음
[A011] index.ejs:5016 | const missingBranch = (inCount - outCount) - branchCount; | (a) 실내기 1대당 분기관 1개가 필요하되 실외기 직결 1대는 분기관이 없어도 된다 (b) 실외기 대수와 무관하게 계산해야 한다 | 실외기가 실내기보다 많으면 음수가 되어 경고가 안 뜬다(>=1 조건). 실외기 여러 대일 때 이 공식이 맞는지 확인 필요
[A012] index.ejs:5195 | const isHideMat=p=>/유연호스\s*I형|운임|절삭/i.test(...); | (a) 세트 '구성품 전개'에서만 운임·절삭을 숨기는 것이고 견적 본문에는 별도 특수행으로 들어간다 (b) 운임·절삭이 견적에서 아예 빠진다 | 메모리 규칙(feedback_freight_cutting_amount_first_entry)은 운임·절삭이 견적서에 포함돼야 한다고 못박고 있다. 여기 제외가 '세트 구성품 목록에서만' 인지 확인 필요 — 특수행(SPECIAL_ROW_SOURCE.CATALOG_SPECIAL)으로 별도 렌더되는 경로가 5800-5928 에 있어 (a)로 보이지만 코드만으로 단정 불가
[A013] index.ejs:5942-5943 | let val = r['고정DC'] ?? r.fixedDC; if(typeof val === 'number' && val <= 1) val = (val*100); | (a) 데이터가 0~1 비율과 0~100 퍼센트 두 형태로 섞여 들어와 방어하는 것이다 (b) 정확히 1 인 값은 '1%' 인데 100% 로 뒤집힌다 | 경계값 1 의 해석이 갈린다. 실 데이터에 고정DC=1 인 행이 있는지 세어야 함
[A014] index.ejs:4801-4818 | if (raw === '') { ... COMM_MANUAL_PANEL.delete(model); ... } (4741-4750) 뒤에 조건 없이 if (rec) { ... COMM_MANUAL_PANEL.add(model); ... } | (a) 뒤 블록이 잘못 남은 중복 코드이고 비우면 잠금이 풀려야 한다 (b) 비워도 '사용자가 손댄 행' 으로 계속 잠가 두는 것이 의도다 | 현재는 (b)로 동작 — 4741-4750 의 delete 가 즉시 무효화된다. 홈멀티(6004-6019)는 delete 만 하고 재추가하지 않아 두 섹터 동작이 다르다
[A015] index.ejs:5370-5371 | const v = Math.max(q, parseInt(inp.value||'0',10)||0); if (v !== q){ inp.value = st.qty; inp.dispatchEvent(new Event('input')); } | (a) '현재 입력값이 실외기 수량보다 크면 실외기 수량으로 되돌린다' 가 의도다 (b) 조건이 반대로 쓰였고 원래는 v === q 가 아닐 때가 아니라 현재값이 부족할 때 채워야 한다 | st.qty 는 항상 q 와 같은 값이라 결국 '현재값 > q 이면 q 로 낮춘다' 로만 동작하고, 현재값 < q 인 경우(예: 0)는 v===q 가 되어 아무것도 안 한다. 즉 받침대 자동 채움이 실제로는 일어나지 않는다
[A016] index.ejs:4492-4495 | computed = (sheetPrice > 0 && !commCustomListPrices.has(model)) ? sheetPrice : listPrice; | (a) 출고가를 수동으로 고치면 납품가도 그 출고가와 같아지는 것이 의도다(주석이 그렇게 적혀 있음) (b) 수동 출고가에 전역 할인율을 곱해야 한다 | 주석은 (a)를 말하지만 변동DC 미체크 상태라 할인이 0% 가 되어 납품가=출고가가 된다. 업무상 맞는지 확인 필요
[A017] index.ejs:6218-6221 vs 6192-6202 | tdSub.innerHTML = `${fmt(unit*effQ)}...` (소계는 effQ) 인데 수량 표시는 displayQ(수동값 우선) | (a) 수동으로 구성품 수량을 바꿔도 소계는 자동 수량으로 계산하는 것이 의도다 (b) 소계도 displayQ 로 계산해야 한다 | (b)라면 수동 수량 입력 시 화면 수량과 소계가 불일치. 다만 이후 입력 이벤트에서 재계산될 수 있어 초기 렌더 한정 문제일 가능성
[A018] index.ejs:5192 vs 6051 | (계산) p.setModel === s.model  vs  (표시) p.setModel === s.model || p.refId === s.id || p.refModel === s.model | (a) 표시만 넓게 보여주고 계산은 엄격히 하는 것이 의도다 (b) 두 기준이 같아야 한다 | refId/refModel 로만 연결된 구성품은 화면에 뜨지만 explodeSetParts 에는 안 들어가 배분·금액에 반영되지 않는다(activeMatch 없음 -> 수량 0)


########## range-estimate-ejs-4.json
[A019] index.ejs:7577 (sumOld) vs 7445 (renderOld) | sumOld: `finalP = Math.round(listP * 0.5)` / renderOld: `autoPrice = Math.round(listPrice * (1 - discountRate))` 후 roundByConfig | 갈래① 합계의 0.5 가 옛 기본값이 남은 것이고 화면 할인율이 정본 ② 합계가 정본이고 화면 입력은 표시용 ③ 둘 다 의도(행은 편집 가능, 합계는 고정 기준) | ①이면 화면 할인율을 45 등으로 바꿔도 총합이 안 따라오는 값 불일치가 상시 발생(같은 화면에서 행 소계 합 ≠ 총합). ②면 행 소계 표시가 틀린 것. roundByConfig 반올림도 총합에는 미적용이라 반올림 차이까지 누적. 어느 쪽이 업무 기준인지 코드만으로 확정 불가
[A020] index.ejs:8318-8320 | `if (iCnt >= 2 && sOut > 0) { b25 = h6; b15 = iCnt - sOut - h6; }` | h6(6HP 단배관 실외기)는 sOut(단배관 실외기 전체)의 부분집합이므로 b15 에서 6HP 가 두 번 차감된다. 갈래① 의도된 식(6HP 는 25.12 로 대체되므로 15.09 에서 한 번 더 빼는 것이 맞다) ② sOut 이 아니라 '6HP 아닌 실외기 수' 를 빼야 하는데 오기 | ①이면 그대로 두면 되고, ②면 6HP 실외기가 있는 견적마다 15.09 분기관이 6HP 대수만큼 적게 잡힌다. 실제 견적서 정답과 대조가 필요
[A021] index.ejs:7965-7967 | `if (FOOT_FLAT && !HOME_MANUAL_FOOT.has(FOOT_FLAT)) { homeQty.set(FOOT_FLAT, 0); }` | '발통포함' 체크와 무관하게 평발통(SI-AL700a)은 항상 0. 갈래① 홈멀티에는 평발통이 자동 부여되지 않는 것이 규칙(수동으로만 입력) ② 원형/평 구분 로직이 미구현이고 0 은 임시값 | ①이면 유지, ②면 싱글중대형처럼 모델별 원형/평 분기(7997)가 홈멀티에도 필요
[A022] index.ejs:8432-8446 | `const hose1L = HOSE_1W; const hose4L = pickHoseModel('4way'); const hose1I = HOSE_I_1W;` | HOSE_1W/HOSE_I_1W/HOSE_4W 는 index.ejs:4516-4521 에서 `HOMEMULTI.find(...)` 로 정한 **홈멀티 카탈로그 모델코드**인데 상업멀티 want 맵의 키로 쓰인다. 갈래① 두 카탈로그가 같은 유연호스 모델코드를 공유하므로 무해 ② 상업 카탈로그에 없는 코드라 8544 requireCommCatalogRow_ 가 null 을 반환해 상업 호스 자동수량이 조용히 반영되지 않음 ③ 상업 전용 상수가 있어야 하는데 누락 | ②라면 상업멀티 유연호스는 수동 입력에만 의존하게 되고 오류도 안 뜬다(throw 대상 모델 2개에 호스가 없음). 실 카탈로그에서 해당 모델코드가 COMMULTI 에 존재하는지 확인 필요
[A023] index.ejs:8633-8636 vs 8060 / index.ejs:4532 | 공청 swap: `base.replace(/NUF(K1N|K1NW|DK1N|DK1NW)/i, m=>m.replace('NUF','NUC').replace('K1','K4'))` | 'PC4NUFK1N' 에 적용하면 정규식 대안이 왼쪽부터 매칭돼 'NUFK1N' 이 잡히고 결과는 **PC4NUCK4N**. 그런데 PANEL_SWAP_4WAY_TO_AIR(8060) 와 PANEL_MODELS.a4No(4532) 는 **PC4NUCK1N** 이다(WiFi 형 PC4NUFK1NW→PC4NUCK4NW 는 양쪽 일치). 갈래① 상업(4way 미내장 공청)은 PC4NUCK4N 이 맞고 홈멀티만 PC4NUCK1N ② 어느 한쪽이 오타/버그 | ②라면 4Way 미내장 + 공청판넬 견적에서 상업과 홈멀티가 서로 다른 판넬 모델을 산출한다. 어느 코드가 실제 판매 모델인지 확인 필요
[A024] index.ejs:8637-8640 | `if(panelOpt==='동작감지'){ if(/PC1Y.NWK1NW/i.test(base)) return 'PC1YNRK1NW'; if(/PC1Z.NWK1NW/i.test(base)) return 'PC1ZNRK1NW'; }` | 정규식이 'PC1Y' + 임의1자 + 'NWK1NW' = 11자를 요구하는데 실제 모델은 10자('PC1YNWK1NW'). 게다가 swap() 은 4Way 기본코드로만 호출된다(8663,8667). 갈래① 점(.)이 오타이고 원래 /PC1YNWK1NW/ 였다 ② 지금은 8656-8659 의 인피니트 분기가 동작감지를 처리하므로 죽은 코드 | ①이면 지금 어떤 입력으로도 이 분기가 안 타므로 무해하지만 전환 시 되살리면 동작이 바뀐다. 삭제/보존 판단 필요
[A025] index.ejs:6681-6693 vs 6739-6744 | 같은 이름의 KIT_WHITELIST 가 필터용 11개, 렌더용 19개 | 갈래① 필터에는 일부만 노출하는 의도 ② 한쪽만 갱신되어 벌어진 표류 | ②라면 실외기확장 미체크 상태에서 시운전모듈·중앙제어기·SPI KIT 등 8개가 **표에는 보이는데 필터 목록에는 안 잡히는** 비대칭이 생긴다. 확장 판정 정규식도 필터는 'GHP'(6700), 렌더는 '가스히트펌프'(6750) 로 다르다
[A026] index.ejs:6720-6723 vs 6769-6772 | 판넬 중분류 라벨이 '1-Way/2-Way/4-Way/360'(필터) vs '1Way/2Way/4Way/360'(렌더) | 갈래① 필터 UI 와 표 그룹 라벨을 일부러 다르게 표기 ② 중복 구현 중 한쪽만 수정 | ②라면 필터에서 고른 값과 표의 그룹명이 문자열로 안 맞아 필터가 걸리지 않을 수 있다. 매칭 로직(applyCommFilter, 구간 밖)이 어느 라벨을 쓰는지 확인 필요
[A027] index.ejs:6948-6950 | `if(typeof val === 'number' && val <= 1) val = (val*100);` | 고정DC 값 1 이 '1%' 인지 '100%' 인지 코드로 확정 불가. 갈래① 데이터는 항상 비율(0~1) 저장이라 1=100% ② 정수 % 저장이 섞여 있어 1=1% | ②인 행이 하나라도 있으면 그 품목은 화면에 100% 할인으로 표시된다. 실 데이터에서 fixed_discount_rate 의 값 분포(0~1 인지 0~100 인지, 정확히 1 인 행이 있는지)를 세어야 판정 가능
[A028] index.ejs:7365 vs 7340 | 구성품 소계 `fmt(unit*effQ)` 인데 표시 수량은 `displayQ = isManual ? commCustomPartQtys.get(...) : effQ` | 갈래① 소계는 자동 수량 기준이 맞다 ② 표시 수량과 소계가 어긋난 버그 | ②라면 사용자가 구성품 수량을 손으로 고친 직후 렌더가 다시 돌 때 '수량 3 · 소계는 1개분' 같은 불일치가 남는다(7090 의 이벤트 경로에서는 effQ 에 수동값이 반영되므로 경로에 따라 결과가 다르다)
[A029] index.ejs:7905 vs 7081/7293 | 싱글 구성품만 `Math.floor(q * defQ)`, 상업 구성품은 `v * defQ` / `(parseInt(setQty)||0) * defQ` (floor 없음, defQ 는 parseFloat 라 소수 가능) | 갈래① 싱글만 소수 구성수량이 존재해 내림이 필요 ② 세 곳이 같은 규칙이어야 하는데 표류 | ②라면 defQ 가 소수인 구성품에서 싱글과 상업의 수량이 달라진다. 구성표에 소수 수량이 실제로 있는지 세어야 확정
[A030] index.ejs:8519-8533 vs 8552-8561 | '판넬제외'·'리모컨 제외'·'받침대 제외' 는 want 를 0 으로 만들지만, 반영 단계에서 그 모델이 수동잠금이면 `return` 으로 건너뛴다 | 갈래① 사용자가 직접 넣은 수량은 제외 옵션보다 우선한다 ② 제외 옵션이 절대 우선이어야 한다 | ①이면 '판넬제외' 를 켜도 손으로 넣은 판넬이 남아 견적에 포함된다(받침대만 8561 에서 `!isBaseExcluded` 조건으로 제외 옵션이 이기도록 예외 처리되어 있어 계열마다 규칙이 다르다)
[A031] index.ejs:6908 · 7472 · 8719 | 일반 행의 수동 판정이 `isManual = (q > 0)` (comm/old) 와 `isManual = (q !== 0 || qtyInp.value === '0')` (home) 로 다르다 | 갈래① 계열별로 의도된 차이 ② 같은 규칙이어야 하는데 표류 | 이 값이 '파란 굵은 글씨'(사용자 입력 표시)만 좌우하는지, 아니면 재계산 보호에도 쓰이는지에 따라 영향이 다르다. 코드상 여기서는 표시 전용이나 홈멀티는 8726 에서 값 덮어쓰기 여부까지 좌우한다
[A032] index.ejs:7069 · 8828 · 7088 | 소계 계산 시 단가를 `value.replace(/[^0-9]/g,'')` 로 파싱 — '-' 가 제거된다. 반면 수량 파싱은 `[^0-9-]` 로 '-' 를 허용(7022,7537) | 갈래① 단가는 음수가 될 수 없다는 전제 ② 절삭 행(항상 음수, handleFreightInput 2996 `val = -Math.abs(val)`)을 고려하지 못한 파싱 | ②라면 절삭 행의 소계가 양수로 표시된다. 다만 절삭 행은 특수행이라 이 경로(.price-input 이 있는 일반행 갱신)를 타는지 실행으로 확인 필요
[A033] index.ejs:7231 vs 6822 | 전송용 세트 단가는 `getRealCommPrice(p.model)`, 화면 표시 단가는 `commUnitPrice(model)` + `commCustomPrices` | 갈래① 두 함수가 같은 값을 돌려준다 ② 전송 금액과 화면 금액의 출처가 달라 사용자가 고친 단가가 전송에 안 실린다 | ②면 화면에서 단가를 수정해도 전송/인쇄 금액이 다르다. 두 함수 정의(2403, 4455)는 이 구간 밖이라 대조 필요
[A034] index.ejs:7119 | `tfoot.colSpan = 15;` 인데 6794-6806 의 헤더는 th 12개(그중 colSpan 4 하나) | 갈래① 모바일/PC 열 토글까지 감안한 최대치 ② 열 추가·삭제 후 갱신 안 된 잔재 | 표 하단 합계 줄의 병합 폭이 실제 열 수와 어긋나면 레이아웃이 깨지거나 빈 칸이 남는다


########## range-estimate-ejs-5.json
[A035] index.ejs:9430-9431 | `const v = parseFloat(String(inp.value).replace(/[^0-9.]/g,'')); return isNaN(v) ? 0 : (v > 1 ? v/100 : v);` | 갈래A: 사용자가 45 를 넣으면 45%, 0.45 를 넣으면 45% 로 둘 다 받아 주려는 관용 파서 / 갈래B: 원래 비율(0~1) 입력란인데 실수 방어를 붙인 것 / 갈래C: 퍼센트 입력란인데 소수 입력을 허용한 것 | 갈래마다 경계값 해석이 갈린다 — 값 1 은 갈래A/B 에서 100% 할인, 갈래C 에서는 1% 여야 한다. 1% 를 표현할 방법이 현재 코드엔 없다. 또한 음수 부호가 정규식에서 제거돼 -5 를 넣으면 5(=5%)가 된다
[A036] index.ejs:9392 | `const isKyungdong = fullAddr.includes('경동') && fullAddr.includes('/');` | 갈래A: 특정 거래처(경동) 전용으로 규격칸에 출고가를 함께 실어 보내는 업무 규칙 / 갈래B: 주소에 '/' 를 넣어 표기하는 특정 현장 양식과 결합된 임시 코드 / 갈래C: 레거시 잔재 | 어느 갈래든 '주소 문자열' 이 조건이라 관계없는 주소에 '경동' 과 '/' 가 우연히 들어가면 모든 전송행 규격 뒤에 출고가가 붙어 외부로 나간다. 동적 전환 시 이 조건을 거래처 코드/플래그로 옮길지 여부가 값이 갈리는 지점
[A037] index.ejs:8921·8925 vs 10053·10138 | `document.getElementById('home_rate')?.value || '45'` / `rRate.value = 45` / `setVal('comm_rate', 45)` | 갈래A: 45 는 CONFIG.homeDiscount(0.45)·commDiscount(0.45)의 사본이며 동기화 누락 / 갈래B: 화면 기본값은 CONFIG 와 독립인 별도 정책 | 갈래A 면 CONFIG 를 바꿔도 초기화 버튼을 누르는 순간 45 로 되돌아가 값이 조용히 틀어진다(구형만 getOldDiscountPercent() 로 CONFIG 를 따른다). 갈래B 면 현행 유지
[A038] index.ejs:9211-9213 | `prev.fixedDc = (nDc > pDc) ? nDc : pDc;` | 갈래A: 병합되는 행 중 가장 유리한(큰) 고정DC 를 채택하는 업무 규칙 / 갈래B: 값이 0으로 덮이는 것을 막는 방어 코드 | 갈래A 면 의도된 MAX 누산이라 이관해야 하고, 갈래B 면 '0이 아닌 값 우선' 이 맞아 서로 다른 값이 나온다(예: 0.10 과 0.05 병합 시 A=0.10, B=먼저 온 값)
[A039] index.ejs:8827 vs 8840·8855·9494·9570·9658 | `value.replace(/[^0-9]/g,'')` (음수 부호 제거) 대 `value.replace(/[^0-9-]/g,'')` (음수 허용) | 갈래A: 싱글 세트 단가에는 음수가 있을 수 없어 의도적으로 다르게 둔 것 / 갈래B: 단순 불일치 | 갈래B 면 절삭·마이너스 조정 같은 음수 단가가 싱글 소계(8828)와 getSetUnitNowById(8957)에서 부호를 잃고 양수로 계산된다
[A040] index.ejs:9696-9709 | `if (item.catL === '판넬') { add('타공사이즈(mm)', s.cool_kw); add('전산볼트간격(mm)', s.cool_power); }` | 갈래A: 판넬 스펙 시트에서 냉방성능 컬럼을 타공사이즈 용도로 재사용하기로 한 데이터 규약 / 갈래B: 판넬 전용 필드가 따로 있는데 잘못 매핑 | 갈래A 면 필드 의미가 카테고리에 따라 달라지므로 동적 전환 시 매핑 테이블이 필요하고, 갈래B 면 현재 화면 수치 자체가 틀린 값이다. 홈(cool_kw/cool_power)과 그 외(cool_cap_kcal/cool_pow_kw)가 서로 다른 필드를 읽는 것도 함께 확인 필요
[A041] index.ejs:9718-9723 | `if (name/model 에 '에어콤보') add('냉매가스', s.gas); else add('덕트구경', s.gas);` | 갈래A: 에어콤보만 냉매를 쓰고 나머지 전열교환기는 덕트라 같은 컬럼을 두 의미로 쓰는 규약 / 갈래B: 라벨 오기 | 갈래A 면 품명 문자열이 아니라 제품 속성으로 갈라야 하고, 갈래B 면 한쪽 라벨이 계속 틀린 채 인쇄된다
[A042] index.ejs:10591 | `var whCode = whSelect ? whSelect.value : '00003';` | 갈래A: '00003' 이 기본 출고창고라는 업무 규칙 / 갈래B: 개발 중 임시값 | 갈래A 면 CONFIG 로 옮겨야 하고, 갈래B 면 select 가 없는 화면에서 잘못된 창고로 전표가 나간다. 집PC 시드에는 창고 코드 '00003' 이 존재하지 않아 재현이 불가하다는 점도 함께 확인 필요
[A043] index.ejs:10940 | `const fileName = "종합견적(상세)";` | 갈래A: 저장 파일명은 항상 상세 양식이라는 규칙 / 갈래B: previewMode==='SIMPLE' 일 때 '(기본)' 이어야 하는데 누락 | 갈래B 면 기본 견적서를 저장해도 파일명이 '(상세)' 라 수령자가 문서를 오인한다
[A044] index.ejs:10768-10770 | `parts.some(p => p.includes('초과 주의!')) ? parts.join(' / ') : `조합비: ${parts.join(' / ')}`` | 갈래A: 경고가 있으면 '조합비:' 접두를 떼는 표시 규칙 / 갈래B: 플래그를 따로 두지 않아 자기 출력 문자열을 다시 검사한 임시 구현 | 갈래B 면 경고 문구를 조금만 바꿔도 접두 규칙이 조용히 깨진다
[A045] index.ejs:10239-10241 | `['ss_disc_360','ss_disc_4way','ss_disc_stand','ss_disc_1way','ss_disc_deluxe','ss_disc_grade1'].forEach(id => el('#'+id).value = 0);` | 갈래A: 싱글 개별 할인은 매번 사용자가 넣는 값이라 초기화가 0이 맞다 / 갈래B: CONFIG 의 oneWayDiscount·deluxeDiscount·firstGradeDiscount·singleSetDiscount 가 기본값이어야 하는데 반영 누락 | 갈래B 면 서버에서 정한 기본 할인이 초기화 한 번으로 사라진다
[A046] index.ejs:10144-10146 | `setVal('comm_panel','기본판넬'); setVal('comm_p360','원형'); setVal('comm_remote','무선');` | 갈래A: 상업은 데이터 기본값 개념이 없어 코드 고정이 정답 / 갈래B: HOME_DEFAULTS·SINGLE_DEFAULTS 에 대응하는 COMM_DEFAULTS 가 있어야 하는데 없다 | 갈래B 면 상업만 서버에서 기본 옵션을 못 바꾼다(홈·싱글은 바꿀 수 있음)
[A047] index.ejs:8872-8889 | `maps = [homeQty(home), singleQty(single), commQty(comm), oldQty(old)]` 이지만 플래그는 hasHome/hasComm/hasOld 만 세움 | 갈래A: 싱글은 표시할 단일 할인율이 없어 의도적으로 제외 / 갈래B: 누락 | 갈래B 면 싱글만 선택했을 때 할인율 뱃지가 아예 뜨지 않는다(현행 동작)
[A048] index.ejs:9873-9895 | 장비스펙 수집 소스가 COMMULTI·homeQty·SINGLE_SETS 3개뿐 | 갈래A: 구형(OLD)은 스펙 데이터가 없어 의도적 제외 / 갈래B: 누락 | 갈래B 면 구형 제품만 선택한 견적에서 '선택된 장비가 없습니다' 가 뜬다
[A049] index.ejs:10815-10818 | `if(selSingle===0) document.querySelectorAll('#singleBody .qty-input').forEach(...selSingle++)` | 갈래A: 맵 동기화 실패 대비 폴백 / 갈래B: 맵이 진실원이 아닌 화면 우선 설계 | 갈래A 라면 홈·구형에는 같은 폴백이 없어 섹션마다 집계 기준이 다르다. 또한 세트 전개행(.part-qty-single)이 같은 .qty-input 클래스를 쓰는지에 따라 중복 계수 위험
[A050] index.ejs:9404 | `v = p.list || p.listPrice || p.cprice || p.price || 0;` | 갈래A: 값이 0 이면 '없음' 으로 보고 다음 후보로 넘어가는 것이 의도 / 갈래B: 0 도 유효한 출고가 | 갈래B 면 출고가 0 인 품목(운임·절삭 기준선 등)이 다음 후보의 값으로 조용히 대체된다
[A051] index.ejs:9412-9416 | `addP(COMM_PARTS); addP(COMMULTI); addP(SINGLE_PARTS); addP(HOMEMULTI); addP(OLD_PRODUCTS, true);` | 갈래A: 동일 모델이 여러 소스에 있으면 마지막(구형) 값이 맞다 / 갈래B: 섹션별로 자기 소스 값을 써야 한다 | 갈래B 면 같은 모델이 구형과 신형에 모두 있을 때 신형 전송행의 출고가가 구형 값으로 표시된다
[A052] index.ejs:10551-10552 | 전표생성 클릭 리스너를 `fixFootersForMobile()` 안에서 addEventListener 로 등록 | 갈래A: 이 함수가 앱 생애 1회만 호출된다는 전제 / 갈래B: 리사이즈/화면전환마다 호출된다 | 갈래B 면 호출 횟수만큼 리스너가 쌓여 버튼 한 번 클릭에 전표가 여러 번 생성될 수 있다. 호출 지점 전수 확인 필요(이 구간 밖)
[A053] index.ejs:11025-11029 | `if(val === '블랙판넬' || val === '승강판넬'){ if(is4Way || is360) opts.push(val); }` | 갈래A: 해당 판넬은 4way·360 세트에만 실제로 적용돼 라벨도 그때만 붙인다 / 갈래B: 라벨만 감추고 가격/구성에는 반영된다 | 갈래B 면 세트명에는 안 나오는데 금액에는 반영돼 견적서 상 근거가 사라진다. 가격 경로(구간 밖)와 대조 필요
[A054] index.ejs:11042 | `var isAC = /^AC/i.test(s.model||'');` | 갈래A: AC 로 시작하는 모델은 자재가 기본 포함이라 라벨이 불필요 / 갈래B: 자재 옵션 자체가 적용되지 않는 계열 | 두 갈래의 금액 결과가 다르다(A=금액 포함·라벨만 생략, B=금액도 미적용). 모델 접두 문자열을 데이터 플래그로 옮길 때 어느 쪽인지 확정 필요
[A055] index.ejs:10209 | `if (/(분기관|AXJ)/i.test(k)) targets.push(k);` | 갈래A: 'AXJ' 가 분기관 모델 접두라 품명·모델 어느 쪽이 와도 잡으려는 것 / 갈래B: 분기관 판별용 카테고리가 따로 있는데 문자열로 대신한 것 | 갈래B 면 AXJ 로 시작하는 다른 품목이 생겼을 때 분기 초기화가 관계없는 수량을 지운다
[A056] index.ejs:9601-9602 | `if(item.name === '품 명' || item.name === '품명') return;` | 갈래A: 원본 시트 헤더 행이 데이터에 섞여 들어와 코드로 걸러낸다 / 갈래B: 과거 데이터에만 있던 잔재 | 갈래A 면 데이터 정제로 옮겨야 하고, 갈래B 면 죽은 코드다. 어느 쪽이든 '품 명'(공백 포함) 두 변형만 걸러 다른 변형은 통과한다
[A057] index.ejs:9547-9554 | CATALOG_SPECIAL 행이 `unit:'식', fixedDc:0, hasSetDc:false, spec:'\u200B'` 로 전송되며 9545 에서 읽은 userSpec 이 쓰이지 않음 | 갈래A: 특수행은 규격을 싣지 않는 규칙 / 갈래B: userSpec 반영 누락 | 갈래B 면 사용자가 특수행에 적은 규격이 조용히 사라진다


########## range-estimate-ejs-6.json
[A058] index.ejs:12723 | csum<=1200 ? '1509' : csum<=2000 ? '2512' : ... : '4119' | ①인라인 폴백이 구버전 임계값이고 codeByCumulativeSum(150/406/464/696/986)이 정본 ②용량 단위가 서로 다른 두 체계(예: 100W 단위 vs W 단위)라 둘 다 맞다 ③폴백은 죽은 코드다 | 갈래에 따라 codeByCumulativeSum 이 정의되지 않은 환경에서 분기관 코드가 통째로 달라진다. 어느 쪽이 업무 정본인지 코드만으로 확정 불가
[A059] index.ejs:12634 vs 12658 vs 13234(범위 밖) | 실외기 cap = r.능력/capacity 원값, 실내기 cap = 모델명 AM 뒤 3자리, 비율 계산은 실내기 합에 *0.1 | ①실내기 AM 숫자는 0.1kW 단위(예: AM145 = 14.5kW)이고 실외기 '능력' 은 kW 라 0.1 배율이 단위 환산이다 ②배율 0.1 이 업무상 안전계수다 ③둘 다 | 동적 전환 시 '용량' 컬럼의 단위를 무엇으로 저장할지가 갈린다. 단위를 잘못 잡으면 조합비가 10배/0.1배로 틀어진다
[A060] index.ejs:11396 | sec.rows.find(r => r.qty === 1 && r.type !== 'set-head' && r.source !== SPECIAL_ROW_SOURCE.CATALOG_SPECIAL) | ①수량 1 인 행이라야 단가 조정이 소계와 1:1 이라 안전해서 ②수량 1 = 부자재/운임 같은 '식' 단위 행을 노린 것 ③우연한 구현 | 갈래에 따라 절삭이 붙는 품목이 달라진다. 수량 1 행이 없을 때만 별도 '절삭' 행이 생기므로 견적서 모양 자체가 갈린다
[A061] index.ejs:11406-11407 | target.price -= rem; target.sub -= rem; | ①절삭을 기존 품목 단가에 녹여 숨기는 것이 의도 ②원래는 항상 별도 '절삭' 행이어야 하는데 예외 경로다 | 메모리 규칙(절삭은 항상 음수 별도행, 수량 1 자동)과 어긋난다. 전자면 품목 단가가 원단가와 달라지고, 후자면 이 분기는 결함
[A062] index.ejs:11441,11470-11473 | getCustoms('single', ..., false, 'EA', 'SINGLE') 만 hasList=false | ①싱글 사용자정의는 출고가 개념이 없다 ②DOM 에 .custom-list 입력이 없어서 ③누락 | 갈래에 따라 출고가 열 표시 시 싱글 사용자정의 행만 0 으로 나온다
[A063] index.ejs:11117,11264 | .price-input.value.replace(/[^0-9]/g, '') | ①구성품 단가는 항상 양수라 마이너스를 지워도 무해 ②getCustoms(11440)는 [^0-9-] 로 음수를 허용하므로 여기 [^0-9] 는 실수 | 후자면 음수 단가 구성품(절삭성 행)이 부호를 잃고 합계가 2*|금액| 만큼 커진다
[A064] index.ejs:11118,11266,11060,11437 | if (pq !== 0) / if (qty !== 0) | ①음수 수량 입력을 의도적으로 허용(차감 행) ②0 만 걸러내려던 것이고 음수는 고려 안 함 | 전자면 음수 수량이 정상 업무 기능이라 DB 제약을 걸면 안 되고, 후자면 방어가 필요
[A065] index.ejs:12622,12644 | m.length >= 7 && m.charAt(6) === 'X' / 'N' | ①삼성 모델코드 7번째 자리가 실외기/실내기 구분자라는 규칙 ②현재 데이터에만 맞는 우연 | 마스터에 실내/실외 구분 컬럼을 두면 되는 문제인지, 코드 규칙 자체를 데이터로 옮겨야 하는지가 갈린다
[A066] index.ejs:12248 | const excludeKeywords = ['유연호스', '발통', '일자발', '방진가대']; | ①BO 시스템에 등록되지 않은 품목이라 업로드 불가 ②자사 자재라 매출 집계 대상이 아님 | 목록을 데이터로 옮길 때 기준 컬럼이 '품목 종류' 인지 'BO 등록 여부' 인지가 갈린다
[A067] index.ejs:11531 | if (searchStr.includes('운임')) return '#BFBFBF'; | ①운임 행을 시각적으로 구분하는 표시 규칙 ②운임이 특별 취급 대상임을 나타내는 업무 표식 | 메모리상 운임은 금액 선입력 + 수량 1 자동 행이다. 색만 다른 것인지, 다른 규칙과 연동되는지 확정 불가
[A068] index.ejs:11010 | rSel.value !== '기본' && rSel.value !== '무선' | ①'무선' 이 기본 사양이라 라벨에 안 적는다 ②'무선' 은 별도 취급 | 옵션 마스터로 옮길 때 '무선' 을 is_default 로 볼지 별도 플래그로 볼지가 갈린다
[A069] index.ejs:11025-11026 | if(val === '블랙판넬' || val === '승강판넬'){ if(is4Way || is360) opts.push(val); } | ①블랙/승강 판넬은 4way·360 형상에만 물리적으로 장착 가능 ②라벨 표기 규칙일 뿐 실제 구성품은 별개 | 전자면 옵션 호환성 제약을 데이터로 저장해야 하고, 후자면 표시 규칙에 그친다
[A070] index.ejs:11042-11045 | var isAC = /^AC/i.test(s.model||''); if(!isAC){ ... '자재포함' } | ①AC 계열은 자재가 기본 포함이라 라벨 불필요 ②AC 계열은 자재 옵션 자체가 없다 | 옵션 적용 가능 범위를 모델 접두로 판정하는 규칙을 데이터로 옮길 때 기준이 갈린다
[A071] index.ejs:11074 | if (hasPart(/360/i) && hasPart(/사각/i)) opts.push('사각'); | ①360 원형 세트에 사각 판넬을 끼운 조합을 '사각' 으로 표기 ②360 모델의 사각 변형 | 옵션 값 조합에서 파생되는 라벨인지, 별도 옵션 값인지 확정 불가
[A072] index.ejs:12886 | const ROWS = Math.max(sortedInds.length, 24); | ①24 는 최소 표시 행수(여유칸) ②실외기 1대당 실내기 최대 대수 관련 | 후자면 24 가 업무 상한이라 데이터로 옮겨야 하고, 전자면 UI 값
[A073] index.ejs:13142 | phantoms.push({ model: 'AM'+String(val).padStart(3,'0'), cap: val, isPhantom: true }) | ①선택 목록에 없는 용량을 임시로 표시하기 위한 표시용 가짜 코드 ②실제 존재하는 모델을 역추정하려는 것 | 후자면 실제 모델 마스터 조회로 대체해야 하고, 전자면 표시 전용이라 코드 합성이 남아도 된다
[A074] index.ejs:11217-11223 | const gMatch = String(gVal).match(/([1-5A-Za-z/]+)/); gradeTxt = ' ' + gMatch[1] + '등급'; | ①등급은 1~5 숫자 또는 영문 코드 ②영문 허용은 'A/B' 같은 병기 대응 | 정규식이 임의 영문 단어 첫 토막도 잡으므로, 등급 값의 허용 도메인을 확정해야 한다
[A075] index.ejs:12750-12755, 13330(범위 밖) | el.textContent = String(v + extraVal) → commQty.set(m, finalVal) | ①추가 분기관은 자동 산출값에 더해지는 수동 보정 ②추가분만 별도 품목 | 상업멀티 수량이 REPLACE 로 덮어써지므로, 사용자가 상업멀티에서 직접 넣은 분기관 수량은 사라진다. 의도인지 확인 필요


########## range-estimate-ejs-7.json
[A076] index.ejs:13234, 13242 | indoorSum += (parseInt(c.dataset.cap||0,10) * 0.1) | ①data-cap 이 실제 용량의 10배로 저장돼 0.1 이 단위 환산(예: 45→4.5kW) ②0.1 이 업무상 환산계수(정격 대비 비율) ③칩 종류와 무관한 고정 계수 | ①이면 DB 전환 시 용량 컬럼 단위만 맞추면 되고 계수는 소멸, ②면 계수를 마스터 컬럼으로 빼야 하며, ③이면 실외기/실내기별 factor 를 못 넣어 접속률이 조용히 틀어진다
[A077] index.ejs:13251-13253 | const tight = /프라임|한랭지|표준형|냉난방|가스히트펌프|GHP|프레스티지|동시냉난방|공장전원/i.test(name); const limit = tight ? 103.0 : 120.0 | ①제조사 접속률 상한 스펙을 계열명으로 근사한 것 ②사내 영업 기준 ③과거 특정 사고 대응으로 추가된 임시값 | 품명 텍스트가 바뀌면(예: '표준형' 표기 삭제) 한도가 120 으로 조용히 완화된다. 제품 마스터에 접속률 상한 컬럼을 두어야 하는지, 계열 코드로 매핑해야 하는지 확정 필요
[A078] index.ejs:13253 | limit 은 경고 표시(ratio-bad)만 하고 저장·전송을 막지 않는다 | ①경고만이 의도 ②차단이어야 하는데 미구현 | 동적 전환 시 '경고' 로 옮기면 현행 유지, '제약' 으로 옮기면 기존에 통과되던 견적이 막힌다
[A079] index.ejs:13661-13667 | getTierBonusRate: 1억↑0.04 / 5천만↑0.03 / 3천만↑0.02 / 1천만↑0.01 | ①금액 구간별 추가 할인(누진 볼륨 디스카운트) ②영업 정책상 한시 프로모션 ③특정 거래처군 전용 | 확정 모델(고정DC 우선·전역DC·반올림)에 이 축이 없다. dc_configs 로 옮길지, 별도 '구간 보너스' 테이블을 신설할지 개발책임자 확인 필요
[A080] index.ejs:13670-13687 | isStandard45(rate) = |rate-0.45|<0.001, 그리고 실내기 단독이면 45%→40% | ①45% 가 '표준 할인율'이고 그 외(특가·개별율)에는 보너스·하향을 적용하지 않는다는 의도 ②45% 는 현재 CONFIG 기본값일 뿐이고 조건이 사실상 '기본율일 때' 를 뜻함 | ②라면 CONFIG.homeDiscount 를 0.44 등으로 바꾸는 순간 40% 하향과 구간 보너스가 통째로 죽는다. 조건을 '설정 기본율과 같은가' 로 바꿔야 하는지 확인 필요
[A081] index.ejs:13683-13687 | 실내기 단독(실외기 수량 0)이면 할인율 45%→40% 로 하향 | ①실내기만 사는 경우 마진 보전 ②세트 판매 유도 | 하향 폭 5%p 와 판정 기준(실외기 수량 0)이 데이터로 표현될 축인지 불명. '실외기' 판정이 품명·대분류 정규식이라 표기가 바뀌면 조용히 뒤집힌다
[A082] index.ejs:13718-13744 | 적요(REMARKS) 안의 '45%' 문자열을 '46%' 등으로 split/join 치환 | ①적요에 할인율이 텍스트로 박혀 있어 사후 보정이 불가피 ②원래 적요 생성 시점에 조정율을 넘겨야 하는데 래퍼 구조상 못 해서 우회 | ②라면 동적 전환 시 적요 생성에 rate 를 주입하면 이 치환은 통째로 제거 대상. 그대로 옮기면 본문에 우연히 들어간 '45%'(예: 성능 문구)까지 바뀐다
[A083] index.ejs:13312-13315 | 분기관 코드 6종 → AXJ-YA1509N/2512N/2812M/2815M/3419M/4119M 하드코딩 매핑 | ①모델 마스터에서 코드로 조회 가능 ②이 6종이 고정 카탈로그 | 마스터에 없는 모델을 commQty.set 하면 화면·전송에서 어떻게 되는지 미확인(파일 4514-4515 는 HOMEMULTI 에서 find 로 존재를 확인해 쓰고 있어 방식이 다르다)
[A084] index.ejs:13328-13332 | finalVal = 자동집계 v + 사용자 추가입력 extraVal → commQty.set (REPLACE) | ①추가입력은 자동집계에 대한 보정 가산 ②추가입력이 별도 항목 | set 이라 사용자가 상업멀티 화면에서 직접 넣은 분기관 수량은 이 함수가 돌 때마다 덮인다. 의도된 덮어쓰기인지 확인 필요
[A085] index.ejs:13928-13929 | whText.includes('초월창고') → '삼성창고 (초월 무갑)' | ①전표 인쇄용 표기 규칙 ②창고 마스터의 표시명이 따로 있어야 함 | 창고를 DB 로 옮길 때 '표시명' 컬럼이 필요한지, 아니면 이 치환이 특정 시점 임시 대응인지 불명. 다른 창고에는 이런 치환이 없다
[A086] index.ejs:13980 | 담당부서 칸 '영업1팀' 고정 | ①현재 이 앱을 쓰는 조직이 하나뿐 ②담당자 마스터에 부서가 있는데 안 쓰는 것 | 담당자별 부서로 동적화하면 기존 전표와 표기가 달라진다
[A087] index.ejs:14285-14291 + 14461 | getInvoiceInnerContent(d, priceMap) 인데 유일 호출부가 priceMap=null | ①priceMap 경로는 미래/외부용 확장 ②과거 기능이 죽은 것 | 현재 명세서 단가는 항상 it.unitPrice. 동적 전환 시 priceMap 을 살릴지 제거할지 판단 필요. 또한 priceMap[model] 이 0 이면 falsy 로 무시되어 unitPrice 로 넘어간다(0원 단가 표현 불가)
[A088] index.ejs:14294-14304 | lineSupply = Math.round(lineTotal / (1+vatRate)) 를 행마다 반올림한 뒤 합산 | ①행 단위 반올림이 회계 규칙 ②총액 기준 1회 반올림이어야 함 | 행이 많을수록 총액 반올림 방식과 원 단위 차이가 누적된다. 어느 쪽이 정본인지 회계 확인 필요(무결성 도메인)
[A089] index.ejs:14363-14377, 14443 | 공급자 TEL·등록번호·성명·상호·주소, 예금주·계좌 2건 하드코딩 | ①회사 고정 정보 ②설정으로 빼야 할 값 | 담당/법인 변경 시 코드 수정이 필요하다. 설정 테이블로 옮길 대상인지 확인 필요
[A090] index.ejs:14899-14902 | /^010-\d{4}-\d{4}$/ 로 인수자 번호 검증 | ①실무상 휴대폰만 받는다 ②일반 유선·안심번호도 받아야 하는데 막혀 있다 | ②라면 현재 실사용자가 전표를 못 만드는 경로가 존재. 검증 규칙을 설정화할지 확인 필요
[A091] index.ejs:14991-14994 | new Date(now + 9*60*60*1000) 후 toISOString 절단으로 '오늘' 산출 | ①GAS/브라우저 타임존이 UTC 일 수 있어 KST 보정 ②이미 KST 브라우저면 9시간을 두 번 더하는 셈 | ②라면 매일 15:00(KST) 이후 출고일 기본값이 내일로 밀린다. 실제 실행 환경 타임존 확인 필요
[A092] index.ejs:13847-13857 | 조회 기간 상한 7일 클램프 + toISOString().slice(0,10) | ①API 부하 제한 ②업무상 조회 범위 | toISOString 은 UTC 기준이라 KST 자정 부근에서 하루 어긋날 수 있다. 상한 7 을 설정값으로 뺄지 확인 필요
[A093] index.ejs:15400-15409 | 배송주소에 '야적/'·'지방/' 접두를 붙였다 뗐다 한다 | ①레거시 이카운트 표기 방식을 그대로 계승 ②태그(delivery_tag)로 표현해야 함 | 메모리 규칙(feedback_region_is_a_tag_not_address_prefix)은 REGION/STACK 을 태그로 규정한다. 이 화면은 여전히 주소 문자열 접두로 구현돼 있어 전환 시 어느 쪽이 정본인지 확정 필요
[A094] index.ejs:15417-15427 | 태그가 있고 출고일이 있으면 요청사항 앞에 '{출고일 일}상{익일 일}하 ' 를 강제 삽입. 익일이 일요일이면 하루 더 미루되 (야적 && 출고일이 토요일) 이면 미루지 않는다 | ①상차일/하차일 표기이며 일요일 배송 불가가 원칙 ②야적은 토요일 상차→일요일 하차가 가능 ③월 경계에서 일(day)만 쓰므로 31상1하 같은 표기가 나온다 | 예외 조건(야적+토요일)의 근거가 코드에 없다. 공휴일은 전혀 고려하지 않으며, 일(day) 숫자만 쓰므로 월이 넘어가면 사람만 해석 가능하다
[A095] index.ejs:13454-13473 | 실내기 판정: cat.includes('실내')||cat.includes('전열')||(/^AM\d{3}/i.test(model) && !cat.includes('패널') && !cat.includes('리모컨')) | ①대분류 표기가 불완전해 모델 접두로 보완 ②AM 접두가 실내기 계열의 확정 규칙 | 대분류 문자열이나 모델 네이밍이 바뀌면 분기계산 버튼이 조용히 비활성화된다. 품목 마스터에 '실내기/실외기' 플래그가 있는지 확인 필요


########## range-estimate-ejs-8.json
[A096] index.ejs:15417-15427 (및 15463-15471 중복) | next.setDate(d.getDate()+1); if(next.getDay()===0){ if(!(chkYard && d.getDay()===6)) next.setDate(next.getDate()+1); } | 갈래A '상/하'는 상차일/하차일이고 일요일은 작업 불가라 월요일로 민다, 단 야적은 토요일 상차면 일요일 하차 허용 / 갈래B 야적일 때만 예외인 것이 아니라 야적+토요일이라는 특정 조합만 예외인 별도 규정 / 갈래C 공휴일은 전혀 고려하지 않음이 의도인지 미구현인지 | 갈래에 따라 메모의 하차일이 하루 달라지고 이 문자열이 그대로 이카운트 전표 메모로 나간다. 요일 규칙을 데이터(공휴일/영업일 캘린더)로 뺄지 결정 필요
[A097] index.ejs:15736 / 15836 | v = v > 1 ? v/100 : v | 갈래A 사용자가 % 로 입력한다고 보고 1 초과만 100 으로 나눔(=1 은 100%로 해석) / 갈래B '1' 은 1% 의도인데 1.0(100%)로 읽히는 경계 결함 / 갈래C 원래 비율(0~1)과 퍼센트(1~100)를 한 칸에서 받으려는 관용 파싱 | 갈래B 라면 고정DC 1 을 넣은 품목이 100% 할인으로 표기된다. 임계값(0.01, 1)을 데이터로 뺄 때 어느 단위를 정본으로 삼을지 확정 필요
[A098] index.ejs:15731-15741 checkGlobalTarget | return v < 0.01 || isNaN(v); | 갈래A '고정DC 가 사실상 없는 품목'만 전역DC 대상이라는 뜻(고정DC 우선 원칙과 일치) / 갈래B 0.01(=1%) 미만은 무시한다는 별도 최소 할인율 정책 / 갈래C isNaN 검사가 v<0.01 뒤에 있어 사실상 도달하지 않는 잔재 | 1% 미만 고정DC 를 넣은 품목이 전역DC 대상으로 다시 잡힌다. 임계 1%를 설정값으로 뺄지 0 으로 낮출지 확인 필요
[A099] index.ejs:15696-15701 fmtMoney | const m = Math.floor(n/10000); const c = Math.floor((n%10000)/1000); return '-' + ... | 갈래A 적요 표기 전용이라 1,000원 미만 절사는 무해 / 갈래B 이 문자열이 이카운트 ADD_TXT_06_T 로도 나가므로 실제 할인액과 표기가 어긋나면 회계 확인 시 불일치 | 갈래B 라면 예: 15,500원 할인이 '-1만5천' 으로 표기돼 500원이 사라져 보인다. 표기 단위를 데이터로 뺄지 원단위 표기로 바꿀지 확인 필요
[A100] index.ejs:15894-15901 카드수수료 적요 | for (let i = 1; i < items.length; i++) { ... isInserted = true; break; } | 갈래A 0번은 배송주소 슬롯이라 의도적으로 제외 / 갈래B 빈 적요가 하나도 없으면 수수료 문구가 조용히 사라지는 것이 허용된 동작 / 갈래C isInserted 를 만들고 쓰지 않는 것으로 보아 실패 시 대체 처리가 빠진 미완성 | 갈래C 라면 적요가 꽉 찬 전표에서 '수수료 N원 포함' 이 누락된 채 금액만 올라간다
[A101] index.ejs:16666-16680 applyCardFeeLogic 타겟 | let target = rows.find(r => r.qty===1 && r.type!=='set-head'); ... target.price += fee; | 갈래A 어느 행에 얹히든 총액만 맞으면 된다 / 갈래B 특정 행(예: 설치비·운임 같은 식 단위 행)에 얹는 것이 원래 규칙 / 갈래C 수량 1 조건은 '단가=소계'라 계산이 단순한 행을 고르려는 편의 | 갈래B 라면 전표마다 수수료가 다른 품목 단가에 섞여 품목 단가 이력이 오염된다. 대상 행 선정 규칙을 데이터로 선언할지 확인 필요
[A102] index.ejs:16684-16720 applyCutoffLogic | const rem = total % unit; ... target.price -= rem; | 갈래A 총액을 절삭 단위로 내림하는 것이 목적이고 대상 행 단가가 음수가 돼도 무방 / 갈래B rem 이 대상 행 단가보다 크면 음수 단가가 생기므로 하한 클램프가 있어야 한다 / 갈래C 대상 행이 없을 때만 별도 '절삭' 행을 만드는 현재 방식이 정본이고 항상 별도 행이어야 한다는 규칙은 없음 | 갈래B 미구현 시 소액 전표에서 품목 단가가 음수가 되어 전표에 그대로 전송된다
[A103] index.ejs:17813-17815 저장 VAT | vatDisplay === 'exc' ? ... Math.floor(screenTotal*0.1) ... : ... Math.floor(screenTotal/11) | 갈래A VAT 표기가 'none'(미표기)일 때도 화면 합계를 VAT 포함으로 간주하는 것이 의도 / 갈래B 'none' 은 별도 처리가 필요한데 else 에 묻힌 것 / 갈래C 0.1·11 은 getVatDivisor()(CONFIG.vatRate) 로 통일돼야 하는데 누락 | 갈래B 라면 미표기 견적의 저장 공급가/부가세가 실제와 다르게 남는다. 세율을 설정값으로 뺄 때 11 도 함께 유도식(1 - 1/(1+vatRate))으로 바꿔야 함
[A104] index.ejs:17368-17416 ABSOLUTE_LOCK 과거호환 | if (!/(판넬|패널|panel|리모컨|리모콘|유연호스|분\s*기\s*관|분기관|발통|일자발)/i.test(nm)) window.ABSOLUTE_LOCK.add(k); | 갈래A 파생(자동계산) 부속을 이름으로 판별해 수동잠금에서 빼려는 것 / 갈래B 이 정규식이 곧 '파생 부속' 의 정의라서 신규 품목명이 늘면 갱신해야 하는 목록 / 갈래C 과거 스냅샷 전용 호환 코드라 신규 데이터에는 영향 없음 | 갈래B 라면 품목명이 바뀐 신규 부속이 수동잠금으로 잘못 승격돼 복원 후 자동 재계산이 막힌다. 파생 여부를 이름이 아니라 데이터 플래그로 판별할지 확인 필요
[A105] index.ejs:15866-15873 dcInfoArr | if (commRate>=0.01 && ...) push(COMM); if (homeRate>=0.01 && ...) push(HOME); | 갈래A 이카운트 비고란 순서가 상업→홈 으로 정해져 있음 / 갈래B 순서에 의미가 없고 우연 / 갈래C OLD(구형) 전역율이 요약에서 빠진 것이 의도 | 갈래C 가 아니라면 구형 전역할인이 적용된 전표의 비고란에 그 할인이 표기되지 않는다(적요에는 들어감)
[A106] index.ejs:16514-16521 재고조회 기본일 | kst.setUTCDate(kst.getUTCDate() + 20); | 갈래A 통상 납기 리드타임이 20일이라 그 시점 재고를 본다 / 갈래B 임의로 넉넉히 잡은 값 | 데이터로 빼려면 이 20 이 업무 상수인지 확인 필요
[A107] index.ejs:16602-16626 enforceDateLimit | if (diffDays > 7) ... | 갈래A 서버 조회 부하 때문에 7일로 제한 / 갈래B 업무상 조회 범위가 주 단위 / 갈래C 임시 방어 | 상한 7 을 설정값으로 뺄지 결정 필요
[A108] index.ejs:17575-17576 targetSize | const targetSize = 450000; | 갈래A GAS 파라미터 크기 한계(약 500KB)를 감안한 여유값 / 갈래B 임의 값 | 저장 백엔드가 GAS 가 아니게 되면 이 상한과 이미지 축소 루프 전체가 불필요해질 수 있음
[A109] index.ejs:17525-17547 loadSnapshotByCustomer | 주석 '거래처명으로 최근 30건 조회' 인데 코드에는 건수 제한이 없음 | 갈래A 서버 getQuoteHistoryByCustomer 가 30건으로 자른다 / 갈래B 주석이 옛 구현의 잔재 | 갈래B 면 대량 조회가 그대로 프런트로 온다. 서버 코드 확인 필요(이 파일 범위 밖)


########## range-estimate-ejs-9.json
[A110] 17812-17815 handleSaveSnapshot VAT 분배 | vatDisplay === 'exc' ? Math.floor(screenTotal * 0.1) : Math.floor(screenTotal / 11) | 갈래① 부가세율 10% 가 코드 상수이고 두 리터럴(0.1, 11)이 같은 세율의 두 표현이다 ② 'inc' 경로의 11 은 세율이 아니라 '세금포함 총액에서 세액을 떼는 관례식' 이다 ③ 내림 방향(Math.floor)이 회계 규칙인지 표시 편의인지 | 세율을 동적화할 때 11 을 (1+r)/r 로 일반화하지 않으면 VAT 포함 경로만 틀어진다. 또한 'exc' 경로는 원 단위 절사가 총액에 더해지고 'inc' 경로는 공급가에서 빠져 반올림 방향이 반대다
[A111] 18871-18872 Math.round(sumSub / effQty) vs 19270-19272 Math.floor(totalSum / setQty) | 같은 '세트 단가 = 구성품합 / 세트수량' 역산이 두 곳에서 반올림 방식이 다르다 | 갈래① 의도된 차이(입력 경로별로 다른 규칙) ② 한쪽이 복사 시점의 사고 ③ 둘 중 하나가 나중에 추가된 신규 경로 | 어느 쪽으로 통일하느냐에 따라 세트 단가가 1원 단위로 달라진다. 게다가 소계는 sumSub/totalSum 를 그대로 쓰므로 단가 × 수량 ≠ 소계 인 행이 생긴다
[A112] 18776 pNode.value.replace(/[^0-9]/g, '') · 18795 · 19245 | 같은 파일의 18818 · 18837 · 19242 · 18926 · 19298 은 /[^0-9-]/ 를 쓰는데 이 세 곳만 마이너스를 제거한다 | 갈래① 단순 누락/오타 ② 구성품 단가·출고가에는 음수가 올 수 없다는 전제 ③ 음수를 절댓값으로 보겠다는 의도 | 절삭 같은 음수 금액이 구성품·출고가로 들어오면 부호가 사라져 소계가 반대 방향으로 틀어진다. 개발책임자 경고의 '절삭은 항상 음수' 와 직접 충돌할 수 있어 확인 필요
[A113] 18841-18846 maxQty = pos.length ? Math.max(...pos) : (neg.length ? Math.min(...neg) : 0) | 세트 대표 수량을 구성품 수량 중 최대(양수) 또는 최소(음수)로 정한다 | 갈래① '가장 많이 들어가는 구성품 개수가 곧 세트 수량' 이라는 업무 규칙 ② 음수 구성품(절삭·할인)만 있는 세트를 표현하기 위한 예외 ③ 0 을 분모로 쓰지 않으려는 방어 코드일 뿐 | 이 값이 세트 단가 역산의 분모라 해석이 틀리면 단가 전체가 틀어진다. 확정 모델의 'SUM 누산' 과 달리 여기서는 MAX/MIN 선택이다
[A114] 18895-18912 cp.qty = partQtyVal / effectiveMainQty (SINGLE_PARTS 는 sp.defaultQty 까지) | 화면에서 바꾼 구성품 수량을 세트 1개당 계수로 역환산해 전역 소스 배열에 되쓴다 | 갈래① 사용자가 바꾼 비율을 세트 정의로 승격시키는 것이 의도 ② 재렌더 시 값 유지를 위한 임시 저장일 뿐 ③ 스냅샷 저장 대상 값을 맞추기 위한 것 | ②·③ 이면 원본 세트 정의가 세션 중에 오염된다. 싱글에서만 defaultQty 를 덮는 이유가 코드에 없다. 소수 계수가 생기면 18757 의 Math.floor(setQty * defQ) 가 다음 입력부터 다른 값을 낸다
[A115] 19310 parseInt(e.target.getAttribute('data-def') || '1', 10) vs 18756 parseFloat(qtyInp.dataset.def || '1') | 같은 data-def 를 한쪽은 정수로 한쪽은 실수로 읽는다 | 갈래① 구성품 계수는 항상 정수라 차이가 없다 ② 0.5 같은 소수 계수가 실재해 지움(복원) 경로에서만 잘린다 ③ 18895 의 나눗셈 때문에 소수 계수가 사후적으로 생길 수 있다 | ②·③ 이면 구성품 수량을 지웠다 되돌릴 때만 수량이 달라진다(재현이 어렵다)
[A116] 19500-19510 setElVal 의 if (!man && hasAuto) { el.value = ''; } | 되돌리기가 이전 값이 아니라 빈 값을 넣는다 | 갈래① 자동 계산 값이므로 비우면 상위 계산이 다시 채운다는 전제 ② 자동 재계산 트리거가 없는 필드(spec-input, list-price-input)에서는 값이 그냥 사라진다 | 전제가 깨지는 필드가 하나라도 있으면 Ctrl+Z 가 입력을 지우는 동작이 된다. spec-input·fix-dc-inp 가 자동 재계산 대상인지 확인 필요
[A117] 18451 text.includes('    ') · 18457 row.split(/\t|    /) | 공백 4칸을 열 구분자로 취급한다 | 갈래① 특정 외부 화면(HTML 표 복사 등)에서 탭 대신 공백이 오는 것에 대한 대응 ② 임시 방편 ③ 들여쓰기된 텍스트를 열로 오인하는 버그 | 품목명·규격에 공백 4칸이 들어 있으면 한 셀 값이 두 셀로 쪼개져 옆 칸을 덮어쓴다
[A118] 18650-18652 ensureCustomBlankRow 의 검사 선택자 7개에 '.mo-name' 이 없다 | 갈래① PC 입력과 값이 서로 복사되므로(18561, 18564) 불필요 ② nameCols===1 인 구형은 한 요소가 두 클래스를 다 가져 무해 ③ 모바일에서만 입력하는 경로가 있으면 빈행이 생기지 않는다 | ③ 이면 모바일에서 사용자정의 품목을 계속 추가할 수 없다. '자동 빈행' 규칙의 실제 발화 조건 확인 필요
[A119] 17644 hLim 표시 vs 4970 HOME_RATIO_LIMIT = 130 · 5060 COMM_RATIO_LIMIT = limit | 홈멀티 한계만 130 상수이고 상업멀티는 계산된 값이다 | 갈래① 홈멀티는 규격상 130% 고정이라 상수가 맞다 ② 데이터로 빼야 할 값인데 홈만 남았다 | 동적 전환 시 홈만 상수로 남아 조합비 경고 색(빨강) 판정이 데이터와 어긋난다. 값 자체는 내 구간 밖(4970)이지만 소비 지점이 17644
[A120] 17789-17801 while (totalPayload > targetSize && (quality > 0.3 || scaleFactor > 0.5)) { if (quality > 0.3) quality -= 0.1; ... } | 0.7 에서 0.1 씩 빼면 4회 후 0.29999999999999993 이라 조건이 거짓이 된다 | 갈래① 실질 하한 0.3 을 의도한 것이고 오차는 무해 ② toDataURL 에는 Number(quality.toFixed(2)) 를 넘기므로 실제 인코딩 품질은 0.3 이다 ③ 부동소수 오차를 감안하지 않은 코드 | 실제 최저 품질과 코드가 말하는 0.3 이 미세하게 다르다. 다만 이 imgBase64 는 17810 주석대로 저장에 쓰이지 않아 영향 범위가 불분명하다 — 계산은 하는데 결과를 안 쓴다는 점 자체가 확인 대상
[A121] 18669 parseInt(tr.querySelector('.custom-qty').value || '0', 10) | 수량은 콤마를 제거하지 않는데 바로 아래 18670 단가는 제거한다 | 갈래① 수량 입력은 oninput 에서 콤마를 못 넣게 막으니 무해 ② 붙여넣기(setCellValue, 18268)로 '1,000' 이 직접 들어올 수 있다 ③ 복원(setElVal, 19512)에서 금액만 콤마 포맷하므로 수량엔 안 들어온다 | ② 가 가능하면 수량 1,000 이 1 로 계산돼 소계가 조용히 1/1000 이 된다
[A122] 18129-18161 updateCellSelectionSum | 선택 셀 값을 단위 구분 없이 그대로 더한다 | 갈래① 사용자가 같은 열만 선택한다는 전제의 편의 기능 ② 수량 열과 금액 열을 섞어 선택하면 의미 없는 합이 나오지만 그대로 표시 ③ '-' 포함 문자열도 Number 로 해석돼 음수가 합산된다 | 합계 배지가 업무 수치로 인용되면 잘못된 값이 전파된다. 이 배지를 결재/보고에 쓰는지 확인 필요


########## range-gas-est-code-1.json
[A123] Code.js:41 | "oldDiscount: 0.5" | ①구형 품목 단가에 곱하는 비율(출고가×0.5) ②출고가에서 빼는 할인율(출고가×(1-0.5)) ③표시용 라벨값 | 두 해석의 결과가 우연히 같아지는 경우가 있어 구간 안에서는 구분 불가. 1719-1759행 getOldProducts_ 는 D열(출고가)과 F열(납품가)만 넘기고 곱셈은 프런트(index.html)에서 한다. 0.5 가 어느 쪽이냐에 따라 구형 단가가 2배 차이
[A124] Code.js:1746-1755 | "price: row[3] // D열: 출고가 (할인 기준액), sheetPrice: row[5] // F열: 납품가 (할인 아닐 때 쓸 금액)" | ①isDisc=true 면 D×0.5, false 면 F 를 그대로 ②isDisc 와 무관하게 둘 다 내려보내고 화면이 고름 ③D 는 표시용, F 는 전송용 | 선택 로직이 이 구간에 없다. 잘못 고르면 구형 전 품목의 단가가 통째로 틀어진다
[A125] Code.js:1041-1053 vs 1278-1281 | 홈: "iCoolKw=coolCols[0]; iCoolKcal=coolCols[1]" / 상업: "iCoolKcal=coolCols[0]; iCoolKw=coolCols[1]" | ①두 시트의 실제 열 순서가 정말 반대다 ②둘 중 하나가 버그다 | 반대로 읽으면 냉방능력 kW 자리에 kcal/h 값(약 860배)이 들어가 스펙표가 조용히 틀린다. 시트 원본을 봐야 확정 가능 — 코드만으로는 못 가른다
[A126] Code.js:1798-1799 | "const isStar = String(custRec.name||'').trim().startsWith('*');" | ①거래처명 앞 '*' = 특수 거래처 표식이고 과거 분기가 있었다 ②단순 잔재 | 값을 쓰는 곳이 함수 안에 없다. 되살려야 하는 규칙인지 버려도 되는지 코드로 확정 불가 — 개발책임자 확인 필요
[A127] Code.js:1855-1860 | "if (/경동.*[\\/:]/.test(String(order?.addr||''))) rawSpec = String(it.list||0);" | ①거래처 '경동' 대상 특례로 규격칸에 출고가를 표기해야 한다 ②배송주소에 '경동'이 들어간 모든 주문에 적용된다(거래처 무관) ③디버깅 잔재 | 코드는 ② 로 동작한다. 조건이 거래처가 아니라 **배송주소 문자열**이고 '/' 나 ':' 가 뒤따라야 한다는 형태 제약까지 있다. 오탐 시 규격 자리에 금액이 찍힌 전표가 이카운트로 넘어간다
[A128] Code.js:1849-1853 | "sup=Math.round(|total|/1.1); priceEx=Math.round(priceVat/1.1)" | ①단가와 합계를 각각 나누는 것이 의도다 ②합계에서 역산한 뒤 단가를 유도해야 한다 | priceEx×qty 와 SUPPLY_AMT 가 원 단위로 어긋날 수 있다(예 priceVat=1,000, qty=3 → priceEx=909, 909×3=2,727 vs supply=2,727 은 일치하지만 priceVat=1,005, qty=3 → 914×3=2,742 vs round(3015/1.1)=2,741). 이카운트가 어느 쪽을 권위로 보는지는 코드에 없다
[A129] Code.js:2075-2081 | 주석 "사업자등록번호 기준 필터" vs 코드 "property:'거래처코드', number:{equals:Number(raw)}" | ①노션의 '거래처코드' 속성에 실제로 사업자번호가 들어 있다 ②주석이 낡았고 호출부(2172행 10자리 검사)가 틀렸다 ③둘 다 맞는데 거래처마다 다르다 | 로컬 dc_config_db 는 partner_id(UUID)로 연결돼 있어 어느 쪽이 원본 키인지 GAS 만으로는 확정 불가
[A130] Code.js:2178-2183 | "notion.homeDiscount !== 0" 일 때만 덮어쓰기 | ①0% 는 '미설정'을 뜻하는 센티널이다 ②0% 설정을 지원해야 하는데 막혀 있는 결함이다 | ① 이면 그대로 두면 되고 ② 면 거래처별 '할인 없음' 이 영구히 불가능하다. dc_configs 는 rate NULL 허용이라 DB 는 '미설정' 을 NULL 로 표현할 수 있다 — 이관 시 0 과 NULL 을 어떻게 가를지 결정 필요
[A131] Code.js:1682-1700 | "const chun = Math.round((abs%10000)/1000);" | ①천 단위 반올림이 의도다 ②내림이어야 한다 | 9,600원이면 man=0, chun=10 이 되어 '-10천' 이라는 표기가 나온다(‘-1만’ 이 아님). 실제 노출되는 라벨인지 확인 필요
[A132] Code.js:1628-1632 | "home.push({cap:data[i][2], hp:data[i][4]}); homeEx.push({cap:data[i][3], hp:data[i][4]})" | ①home 과 homeEx 는 같은 HP 행에 대한 두 가지 용량 기준(일반/확장)이다 ②homeEx 는 별도 테이블인데 hp 열을 공유하는 것이 버그다 | 로컬 odu_recommendation_lookup 에는 타입이 HOME_MULTI(8행)·MULTI_HEATING_COOLING(24행) 두 종뿐이라 homeEx 에 대응하는 타입이 없다. 이관 시 세 번째 타입이 필요한지 확정 필요
[A133] Code.js:910 | "const qty = idxQty>=0 ? (row[idxQty]||'').toString().trim() : '1';" | ①구성품 기본 수량은 1이다 ②'수량' 열이 있는데 셀이 비면 빈 문자열이 그대로 나가는 것이 의도다 | 열이 없을 때만 '1' 이고 열이 있고 비면 '' 이다. 소비 측에서 ''→0 으로 읽으면 구성품이 사라지고 ''→1 로 읽으면 반대. 문자열로 내보내는 것도 의도 불명
[A134] Code.js:1899,1896 | "VAT_AMT: String(vatAmt)" 는 부호 보존 vs "USER_PRICE_VAT: String(Math.abs(priceVat))" 는 절댓값 | ①이카운트가 USER_PRICE_VAT 만 양수를 요구한다 ②실수다 | 반품·차감 행(음수)에서 단가가 양수로 전송되어 이카운트 화면과 우리 화면이 달라 보일 수 있다


########## range-gas-est-code-2.json
[A135] Code.js:2277-2279 | var um = unitSel.match(/(\d+)\s*원?/); if (um) roundTo = Number(um[1]); | ① 노션 '단위처리' select 이름이 항상 콤마 없는 '1000원' 형태다(현행 동작 정상) ② '1,000원' 처럼 콤마가 섞인 항목이 있다(첫 숫자 덩어리만 잡혀 roundTo=1) ③ '천원단위' 처럼 숫자가 없는 표기가 있다(roundTo=null) | ②면 그 거래처의 단가 반올림이 사실상 사라지고 원 단위 그대로 나간다. 노션 select 옵션 실제 목록을 확인해야 확정 가능
[A136] Code.js:2280-2282 | if (/반올림/.test(unitSel)) 'ROUND'; else if (/올림/.test(unitSel)) 'CEIL'; else if (/내림/) 'FLOOR'; | ① 포함 매칭 + else-if 순서가 의도된 설계다 ② 우연히 '반올림' 을 먼저 검사해 맞은 것이고 실제로는 완전 일치 매칭이어야 한다 | ②라면 동적 전환 시 순서를 바꾸거나 정확 매칭으로 옮기는 순간 '반올림' 이 전부 CEIL 로 뒤집힌다. 규칙을 옮기기 전에 어느 쪽인지 확인 필요
[A137] Code.js:2268-2271 | var codeNum = num('거래처코드'); var key = String(codeNum).replace(/[^\d]/g, ''); | ① 노션 '거래처코드' 컬럼에 실제로는 사업자등록번호 10자리가 들어 있다(2075-2079 도 사업자번호를 이 속성으로 필터하며 주석이 '사업자등록번호 기준 필터', initDcConfigFromNotion:2172 는 10자리를 요구) ② 사업자번호와 거래처코드가 섞여 들어 있다 ③ 진짜 거래처코드다 | 소비자(getCustomerDataAsync:1418-1423)는 사업자번호로 먼저 찾고 실패 시 거래처코드로 찾는다. ③이면 사업자번호 조회가 항상 헛돌고 코드 폴백만 동작한다. 컬럼의 업무 의미 확인 필요
[A138] Code.js:2270 | String(codeNum) — 노션 number 속성 | ① DC설정 키에 선행 0 이 붙는 코드 체계가 없다 ② 창고코드 '00003' 처럼 선행 0 을 쓰는 코드가 거래처에도 있다 | ②면 해당 거래처는 맵에서 영구 미매칭이 되어 기본 DC 로 떨어진다(오류 없이 조용히)
[A139] Code.js:2612-2618 | const whMap = { '초월창고': '00003', '상일물류': '2', '온라인창고': '14', '서초창고': '1' }; | ① 재고 표시 대상이 이 4곳으로 확정된 업무 규칙이다 ② 전체 창고 중 자주 쓰는 4개만 발췌한 것이고 창고가 늘면 함께 늘어야 한다 | ②면 신규 창고 재고가 표에서 빠지고(2675 조건에서 버려짐) 사용자는 그 사실을 알 수 없다. 창고 목록을 데이터로 뺄지 여부가 갈린다
[A140] Code.js:2668-2678 | if (matrix[pCode] && matrix[pCode][wName] !== undefined) { matrix[pCode][wName] = qty; } | ① 이카운트 재고 API 가 품목×창고당 정확히 1행을 준다(대입이 안전) ② 로트·구분·프로젝트별로 여러 행이 올 수 있다(그렇다면 합산이어야 한다) | ②면 재고가 마지막 행 값으로만 표시되어 과소/과대 표시된다. 확정 모델의 'SUM 누산' 과 반대 방향이라 그대로 옮기면 안 된다
[A141] Code.js:2506 | items: parsedItems.reverse().map(...) | ① 프론트가 품목을 역순으로 쌓아 저장하므로 조회 시 되돌리는 보정이다 ② 최신 입력 우선 표시 정책이다 ③ 버그 | 저장(saveOrderToNotion:2353)은 받은 배열 그대로 인코딩한다. ①이 아니면 이력 재사용 시 품목 순서가 뒤집힌다. reverse 는 원본 배열도 변형한다
[A142] Code.js:2976-2977 | const idxPrice = idxPrices.length ? idxPrices[idxPrices.length - 1] : findIdx_(H, ['납품가']); | ① 시트에 인상 전/후 납품가가 나란히 있고 오른쪽이 최신이다 ② 오른쪽이 특정 등급(대리점가 등)이다 ③ 중복 헤더 자체가 실수다 | 어느 쪽이든 열이 하나 추가되면 읽는 값이 조용히 바뀐다. 시트 실물의 '납품가' 열 개수와 의미를 확인해야 한다
[A143] Code.js:3004-3006 | readSheet('상업멀티', out.comm, false); readSheet('상업멀티 구성', out.comm, false); | ① 두 탭의 모델명 집합이 겹치지 않는다 ② 겹치면 '구성' 탭 값이 이겨야 한다 ③ 반대로 세트 탭이 이겨야 한다 | 현재는 나중 호출(구성)이 무조건 이긴다. 겹치는 모델이 있는지 시트에서 세어야 판정 가능
[A144] Code.js:2985-3000 | 싱글은 if (!targetObj[model]) targetObj[model] = {} 후 필드 단위 갱신 / 멀티는 targetObj[model] = price 스칼라 대입 | ① 싱글만 list·price 두 값이 필요해 구조를 달리한 의도된 설계다 ② 정리되지 않은 역사적 차이다 | 동적 전환 시 한 규칙(예: 전부 객체)으로 통일하면 홈멀티/상업멀티 소비자 코드의 값 형태가 바뀐다. 누산 규칙이 버킷마다 다르다는 점을 그대로 보존할지 결정 필요
[A145] Code.js:2989,2993,2998 | if (lPrice > 0) ... if (pPrice > 0) ... if (price > 0) ... | ① 0 은 '값 없음' 을 뜻하므로 무시가 맞다 ② 0 이 유효한 단가다(운임·절삭 계열은 0 이 정상 기준선이라는 기존 결정이 있다) | ②면 0원 항목이 '인상 전 단가 없음' 으로 처리되어 비교 화면에서 빠진다. 음수 단가(절삭)는 어느 해석에서도 저장되지 않는다
[A146] Code.js:2739-2752 | if (imgChunks1.length < 100) {...} else if (imgChunks2.length < 100) {...} else if (imgChunks3.length < 100) {...} | ① 미리보기 이미지가 600,000자를 넘는 일이 없다 ② 큰 견적서는 초과분이 잘려 미리보기가 깨진 채 저장된다 | else 절이 없어 초과분이 예외도 로그도 없이 사라진다. 저장은 성공(true)으로 보고된다
[A147] Code.js:2356-2362 | for (let i = 0; i < itemsString.length; i += 2000) { chunks.push(...) } | ① 품목 데이터가 200,000자(100 chunk)를 넘지 않는다 ② 넘으면 노션이 요청을 거부한다 | ②면 2404 가 로그만 남기고 함수는 정상 종료하므로 프론트는 전송 성공으로 인지하고 발송 이력이 유실된다. saveQuoteSnapshot 은 같은 분할에 3버킷 상한을 두는데 이쪽엔 없다
[A148] Code.js:2478-2480 | const slipNo = p['전표번호']?.number || 0; if (!slipNo || !date) return; | ① 전표번호 0 은 존재하지 않는다 ② 0 이 유효한 전표번호다 | ②면 해당 이력이 조회 결과에서 조용히 사라진다. 또한 조회 기준이 '생성날짜' 여도 행 필터는 '출고일' 을 보므로 출고일이 빈 행은 발송시각 조회에서도 빠진다
[A149] Code.js:2365 vs 2585 | pureManagerName = info.manager.split(' ')[0] (직급 제거) / fullName = name + ' ' + rank (직급 결합) | ① 노션 '담당자' select 는 직급 없는 순수 이름만 담기로 한 규칙이다 ② 이름 자체에 공백이 있는 경우를 고려하지 않았다 | ②면 담당자명이 잘려 저장되고 이력 조회에서 다른 사람처럼 보인다. 두 규칙이 한 파일 안에서 정반대라 어느 쪽이 정본인지 확인 필요
[A150] Code.js:3085 | '&display=5&start=1&sort=random' | ① 다양한 후보를 보여주려는 의도된 랜덤이다 ② 예시 코드를 그대로 복사한 것이고 정렬 기준이 있어야 한다 | 3041 의 중복 제거가 선착순이라 ②면 같은 검색어에 대해 최종 표시 목록이 호출마다 달라진다(재현 불가한 QA 결과의 원인이 될 수 있음)
[A151] Code.js:3143-3145 | return !/^[가-힣]+(동|리|가)$/.test(part); | ① 행정동/리 표기만 제거하려는 의도다 ② '현대동'·'상가'·'○○가' 같은 실제 건물명 토큰도 함께 지워진다 | ②면 title(건물명)이 빈 문자열이 되고, 그 결과 3125·3193 의 꼬리 제거가 아무것도 하지 않아 주소 끝에 건물명이 남는다
[A152] Code.js:3025 | const BUILDING_API_KEY = 'REDACTED_JUSO_BUILDING_API_KEY'; | ① 미구현 잔재다(이 파일 내 사용처 0건, grep 결과 선언 1줄뿐) ② 다른 스크립트 파일에서 쓰인다 | ①이면 동적 전환 대상에서 제외해도 되지만, 건물명 조회 기능이 기획에 남아 있다면 미구현 항목으로 추적해야 한다
[A153] Code.js:2230,2237-2239 | if (sources.length > 0 && sources[0].id) dataSourceId = sources[0].id; | ① 해당 노션 DB 의 data_source 는 항상 1개다 ② 여러 개일 수 있고 그때 임의의 첫 번째를 읽는다 | ②면 DC 설정을 엉뚱한 소스에서 읽어 전 거래처 할인율이 틀어진다. fetchNotionDcConfig_(2050-2056) 도 같은 규칙이라 영향 범위가 같다
[A154] Code.js:2893-2897 | page_size: 30, 커서 루프 없음 | ① 주석대로 '최근 30건' 이 의도된 상한이다 ② 페이징 구현이 빠진 것이다 | ②면 거래처별 견적 이력이 31건째부터 조회 불가. 같은 파일의 getQuoteHistory 는 100건씩 전량 수집이라 두 함수의 정책이 다르다
[A155] Code.js:2419-2420 | const baseProp = (dateField === '발송시각') ? '생성날짜' : '출고일'; | ① 노션 '생성날짜' 속성이 곧 발송시각이다(saveOrderToNotion:2385 가 전송 시각을 넣는다) ② 재전송·수정 시 생성날짜가 갱신되지 않아 실제 발송 시각과 어긋난다 | ②면 발송시각 기준 기간 조회 결과가 실제와 다르다. 화면 라벨과 저장 속성명이 달라 확인이 필요
[A156] Code.js:2373 | '거래처코드': { number: Number(info.bizCode) || 0 } | ① 거래처코드는 항상 숫자다 ② 문자를 포함하는 거래처코드가 있다 | ②면 0 으로 저장되어 발송 이력에서 거래처 식별이 불가능해진다(오류 없이 조용히)


########## range-gas-ord-code-1.json
[A157] Code.js:34 `oldDiscount: 0.5` | 구형 품목 할인율 0.5 를 config 로 주입 | 갈래A: 구형 출고가 x 50% 가 확정 업무 규칙이고 거래처별로 바뀌지 않는다 / 갈래B: 다른 할인처럼 거래처별(노션 DC)로 조정돼야 하는데 아직 배선이 안 된 것 / 갈래C: 이미 무의미(index.html 에 `oldDiscount` 참조 0건 — grep 실측) | 영향: A면 상수로 이관, B면 dc_configs 에 항목 추가, C면 제거. 어느 쪽인지 코드만으로 확정 불가
[A158] Code.js:684-685, 1077-1078 `useK2 = /\$L\$2/i.test(priceFormula)` | 시트 셀 '수식 원문'에 $L$2 참조가 있으면 true | 갈래A: $L$2 = 전역 할인율 셀이고 useK2 는 '이 품목은 출고가에서 할인율로 계산된다'는 뜻 / 갈래B: $L$2 는 인상률/환율 등 다른 파라미터 셀 | 영향: 동적 전환 시 이 플래그를 품목 속성(예: 할인 적용 대상 여부)으로 바꿔야 하는데, $L$2 셀의 실제 의미를 확인하지 않으면 반대 의미로 이관될 수 있음. 스프레드시트 L2 셀 원문 확인 필요
[A159] Code.js:806-810 `matKey = 'D4' | 'D7' | 'D8'` ($D$7/$D$8 참조로 결정) | 싱글 세트 납품가 수식이 참조하는 자재가 셀 | 갈래A: '싱글 자재가격' 시트의 특정 행(D4/D7/D8)이 각각 다른 자재 등급 단가이고 세트마다 어느 자재를 쓰는지를 뜻한다 / 갈래B: 이미 폐기된 옛 배선 | 영향: index.html 에서 matKey 참조 0건(실측)이라 현재 소비처가 없음. A면 자재 등급을 데이터 컬럼으로 승격해야 하고, B면 버려도 됨
[A160] Code.js:1932-1944 `form[5].indexOf('$I$1') > -1` → isDisc | 구형 시트 F열 수식에 $I$1 이 있으면 할인 대상 | 갈래A: $I$1 = 구형 할인율 셀(=0.5)이고 isDisc 는 'D열 출고가에 할인율을 곱하라'는 뜻 / 갈래B: $I$1 은 다른 계수 | 영향: index.html 5곳이 이 플래그로 금액을 가른다. 동적 전환 시 '할인 적용 여부'를 품목 속성으로 만들어야 하는데 $I$1 셀 의미 확인이 선행돼야 함
[A161] Code.js:1870 `return (homeHit || singleHit) ? '2' : '00003';` | 창고 코드 2 / 00003 | 갈래A: '2'=상일 창고, '00003'=초월 창고이고 인피니트·360·1등급 등은 특정 창고에만 재고가 있다 / 갈래B: 품목별 창고가 아니라 '한 주문은 한 창고' 라는 이카운트 제약 때문에 대표 창고를 고르는 편법 | 영향: A면 품목 마스터에 창고 컬럼을 두면 되고, B면 주문 분할 규칙이 필요하다. 현재는 혼합 주문에서 히트 품목 하나가 전체 창고를 '2'로 끌고 간다 — 이것이 의도인지 확인 필요
[A162] Code.js:1874-1892 `formatWonDiscountLabel_` | man/chun 축약과 항상 '-' 접두 | 갈래A: 표시 전용 라벨이라 1000원 미만 반올림 소실은 무방 / 갈래B: 이 적요가 회계·정산에서 읽히므로 금액이 정확해야 한다 | 영향: B면 라벨을 원 단위 그대로(예: '-35,000') 로 바꿔야 한다. 또 abs%10000 이 9500~9999 면 'N만10천' 이 나오는데(예: 39,600 → '3만10천') 이것이 허용 표기인지 불명
[A163] Code.js:2083-2084 `globalDcIndex = (globalDcText && merged.length > 1) ? 1 : -1` | 전역 DC 를 두 번째 라인 적요에만 | 갈래A: 첫 줄은 주소 전용이라 두 번째 줄에 넣는 관행 / 갈래B: 원래 모든 라인에 넣어야 하는데 중복 방지로 한 줄만 남긴 것 | 영향: 전송 라인이 1줄인 주문에서는 전역 할인율이 이카운트에 전혀 기록되지 않는다. 이것이 허용인지 결함인지 코드만으로 판정 불가
[A164] Code.js:2176-2182 세트 DC 라벨 index 대조 | 저장 index 는 merged 기준, 비교 대상 idx 는 수량>0 필터 후 lineIndex 기준 | 갈래A: 실무상 수량 0 라인이 세트 헤드 앞에 오는 일이 없어 무해 / 갈래B: 어긋나면 라벨이 엉뚱한 줄에 붙거나 사라진다 | 영향: 동적 전환 시 '세트 할인 라벨은 그 세트의 첫 전송 라인' 이라는 불변식으로 다시 써야 하는지 확인 필요
[A165] Code.js:2122-2128 부가세 1.1 분리 | 공급가 = round(|합계|/1.1), 부가세 = |합계| - 공급가, 단가는 별도로 round(단가/1.1) | 갈래A: 라인 합계 기준이 정본이고 PRICE 는 참고값 / 갈래B: 단가 기준이 정본 | 영향: 두 값이 반올림 때문에 priceEx*qty != supply 가 될 수 있다(예: 단가 100,000 x 3 → supply 272,727, priceEx*qty 272,728). 이카운트에서 어느 쪽을 신뢰하는지 확인 필요. 또 부가세율 10%가 리터럴이라 면세/영세 품목 처리 경로가 없음
[A166] Code.js:2148-2150 규격 중복 억제(U+200B) | 직전 라인과 같으면 제로폭 공백 | 갈래A: 인쇄물에서 같은 규격이 반복돼 보이지 않게 하려는 표시 규칙 / 갈래B: 이카운트 SIZE_DES 를 빈 값으로 두면 오류가 나서 넣는 더미 문자 | 영향: A면 표시 계층으로 옮기고 데이터에는 규격을 그대로 실어야 하고, B면 전송 계층에 남겨야 한다
[A167] Code.js:1965-1967 SET 행 제거 조건 | unit==='SET' && section==='SET' && sendAsSet!==true | 갈래A: section 값에 'SET' 이라는 별도 구분이 실제로 존재한다 / 갈래B: section 은 HOME/COMM/SINGLE 셋뿐이고(2070-2072 에서 그 셋만 검사) 이 조건은 사실상 절대 참이 되지 않는 죽은 필터다 | 영향: B면 세트 헤더 행이 그대로 이카운트로 전송되고 있다는 뜻. section 값 집합을 실제 화면 데이터로 확인해야 함
[A168] Code.js:1152-1155 상업 구성품 basePrice | 주석은 '출고가 우선', 코드는 `priceVal || listVal`(납품가 우선) | 갈래A: 코드가 맞고 주석이 낡았다 / 갈래B: 주석이 의도이고 코드가 버그다 | 영향: 상업 구성품 단가가 통째로 바뀐다. 두 열의 실제 값 분포를 봐야 판별 가능
[A169] Code.js:2063-2068 discountPctText | 계산해 놓고 어디에도 쓰지 않음(Code.js 전체 참조 1건) | 갈래A: 적요에 넣던 것을 globalDcText 로 대체하며 남은 잔재 / 갈래B: 넣어야 하는데 배선이 빠진 결함 | 영향: B면 현재 적요에 단일 할인율 표기가 누락되고 있는 것
[A170] Code.js:1606-1610, 1631-1635 `getRange(1, 1, 2, 24)` | 옵션 기본값을 시트 1~2행 24열에서만 읽음 | 갈래A: 옵션이 24개를 넘지 않는다는 확정 전제 / 갈래B: 그냥 넉넉히 잡은 숫자 | 영향: B라면 25열째부터 추가된 옵션이 조용히 무시된다. 실제 옵션 열 개수 확인 필요


########## range-gas-ord-code-2.json
[A171] Code.js:2220 | USER_PRICE_VAT: String(Math.abs(priceVat)) | ① 이카운트가 VAT포함단가 필드에 음수를 허용하지 않아 절대값을 넣고 부호는 SUPPLY_AMT/VAT_AMT 로만 전달하는 설계 ② 단순 실수(같은 라인의 PRICE 는 부호 유지) | ①이면 신규 구현도 절대값을 유지해야 이카운트 전송이 성공하고, ②면 절삭·반품 라인의 VAT포함단가가 양수로 뒤집혀 이카운트 화면 표시가 실제와 반대가 된다. 코드만으로는 확정 불가
[A172] Code.js:2265-2272 | const st = qt * pr; totalAmt += st; | ① 메일 합계는 '고객이 주문한 VAT포함 총액' 이라 이카운트 전송값과 별개로 원본에서 재계산하는 것이 의도 ② 두 경로가 같아야 하는데 반올림(2119 Math.round, 2122 Math.round)이 빠져 우연히 갈라진 것 | ②라면 소수 수량·소수 단가일 때 메일 합계와 이카운트 전표 합계가 달라진다(반올림 시점 차이). 실제 데이터에 소수 수량이 있는지 미확인
[A173] Code.js:2645-2649 vs 2651-2656 | notion.homeDiscount !== 0 (홈/상업만) vs typeof === 'number' (정액 6종) | ① 노션에서 0 은 '미설정' 을 뜻하고 정액 할인 0 은 '할인 없음' 이라 의도적 비대칭 ② 홈/상업만 방어 코드가 남은 실수 | ①이면 '이 거래처는 전역DC 0%' 를 노션으로 표현할 방법이 없다(항상 기본 45% 로 되돌아감). ②면 0% 설정 거래처가 조용히 45% 할인을 받는다 — 금액에 직접 닿는 갈래
[A174] Code.js:2589-2599 | unitSel.match(/(\d+)\s*원?/) | ① 노션 '단위처리' select 가 항상 '1000원 반올림' 같은 한 가지 형식이라 첫 숫자=단위 ② '1,000원' 처럼 쉼표가 들어가면 1 만 잡힌다 | ②가 실제로 존재하면 반올림 단위가 1000 대신 1 이 되어 반올림이 사실상 무효화된다. 노션 select 옵션 실물 목록 확인 필요
[A175] Code.js:2986-2992 | const daysToSunday = dayOfWeek === 0 ? 0 : 7 - dayOfWeek; | ① '임시승인은 그 주 일요일 자정까지' 인데 자정 처리를 빠뜨려 승인 시각과 같은 시각으로 만료 ② 일요일에 임시승인하면 즉시 만료되는 것이 의도 | ①이면 일요일 승인분이 사실상 0일 권한이고, 다른 요일도 '일요일 그 시각' 에 끊긴다. getDay() 가 스크립트 타임존 기준이라 KST 경계에서 하루가 밀릴 수 있는 것도 미확정
[A176] Code.js:2729-2742 vs 2825-2833 | checkAuthStatus 는 '장기미발주', tryLogin 은 '장기미사용'·'잠김' | ① 노션 select 옵션이 실제로 여러 이름으로 존재 ② 한쪽이 오타/구버전 잔재 | ②면 해당 상태가 한 함수에서만 차단되고 다른 함수는 통과시킨다(예: '잠김' 상태 계정이 checkAuthStatus 에서는 ERROR '알 수 없는 상태'). 노션 '승인상태' select 옵션 실물 확인 필요
[A177] Code.js:2780-2796 | pastPwList 검사만 하고 과거1~5 에 쓰는 코드가 이 파일 구간에 없음 | ① 다른 곳(수동 운영/별도 스크립트)에서 이력을 채운다 ② 이력 밀어넣기 로직이 미구현 | ②면 재사용 금지가 사실상 작동하지 않는다(과거 슬롯이 항상 비어 있어 어떤 비밀번호도 재사용 가능). 현재PW 는 검사 대상에도 없어 같은 비밀번호 재설정이 통과
[A178] Code.js:2513-2518 vs 2886-2890 | DC DB 는 '거래처코드' number, 인증 DB 는 '거래처코드' title(문자열) | ① 두 DB 설계가 원래 다름 ② 한쪽이 사업자번호이고 다른 쪽이 이카운트 거래처코드인데 이름만 같음 | ②면 마이그레이션 시 조인 키를 잘못 잡을 위험. 주석(2512)은 '사업자등록번호 기준 필터' 인데 속성명은 '거래처코드' 라 코드만으로는 어느 값인지 확정 불가
[A179] Code.js:2419-2420 | if (/AJ0|AJ1|AM0|AM1/.test(m)) return true | ① 홈멀티 제품 모델코드 접두가 실제로 이 4종 ② 일부만 열거된 부분 목록 | ②면 신제품 홈멀티가 홈멀티로 인식되지 않아 홈DC 대신 상업DC 가 적용될 수 있다. 접두 고정(^)이 아니라 부분일치라 다른 계열 모델에 우연히 포함될 가능성도 있음
[A180] Code.js:3190 | const mappedItems = parsedItems.reverse() | ① 저장 시점(saveOrderToNotion) 배열이 역순이라 되돌리는 보정 ② 프론트 표시 취향 | ②면 이력 화면의 품목 순서가 실제 전표 순서와 반대다. 저장 경로(3233)는 merged 순서를 그대로 넣으므로 ①의 근거를 이 구간에서는 찾지 못함
[A181] Code.js:2217 (계산은 2148-2150) | SIZE_DES 가 직전 라인과 규격이 같으면 제로폭 문자 \u200B | ① 이카운트 인쇄물에서 같은 규격 반복 표기를 지우려는 표시 규칙 ② 데이터 자체를 비우는 것이 의도 | ①이면 신규 구현에서 '표시 억제' 로 옮겨야 하고, ②면 전표 데이터에 규격이 실제로 비어 저장된다. 어느 쪽인지 코드만으로 확정 불가
[A182] Code.js:3237-3242 | 2000자 분할만 하고 조각 개수 상한 없음 | ① 실무상 품목 수가 적어 100조각(20만자)을 넘을 일이 없다 ② 상한 검사 누락 | ②면 대형 주문에서 노션 저장이 실패하는데 3274 catch 가 로그만 남기고 주문은 성공으로 끝난다(이력 유실)


########## range-order-html-1.json
[A183] index.html:1460 | "singleSetDiscount: 0" | CONFIG 기본값에는 있으나 normalizePartnerConfig(1513-1530) 의 정규화 목록에는 없다 | 갈래① 미사용 잔재 ② 구간 밖에서 소비되며 서버 cfg 값이 Object.assign 으로 그대로(정규화 없이) 통과 ③ 견적앱 전용 키가 복제로 남은 것 | ②라면 서버가 48 을 주면 0.48 로 보정되지 않고 48 이 그대로 쓰여 금액이 100배 틀어진다. 동적화 전에 소비처 확인 필요
[A184] index.html:1450 | "return due < String(effectiveDate);" | 날짜를 Date 가 아니라 문자열 사전식으로 비교 | 갈래① 양쪽 다 'YYYY-MM-DD' 고정이라 사전식이 정확 ② effectiveDate 가 'YYYY/MM/DD' 나 ISO datetime 이면 비교가 뒤집힘 ③ due 가 빈 문자열이면 !due 로 false → 항상 인상 후 단가 | 인상 전/후 단가 선택이 통째로 뒤집히는 지점. 실제 저장 포맷 확인 필요
[A185] index.html:1735-1740 | "if(flags.is4way && d4way > 0) v = Math.max(0, v - d4way); ... if(flags.isGrade1 && dFirst > 0) v = Math.max(0, v - dFirst);" | 6개 플래그가 배타가 아니라 순차 차감 | 갈래① 의도된 누산(4way 1등급이면 두 정액 모두 차감) ② 원래 배타여야 하는데 else if 가 빠진 것 ③ isGrade1 만 다른 축이라 중복이 정상 | ①/③이면 그대로, ②면 실제 단가가 정액 하나만큼 더 빠진다. getModelFlags 상 is4way+isGrade1 동시 성립이 가능한 모델코드가 실재하는지 세어봐야 판정 가능
[A186] index.html:1564,1571 | "Math.min(Math.max(v,0),0.99)" | 고정DC 상한 0.99 | 갈래① 100% 할인(무상) 금지가 업무 규칙 ② 0 나눗셈/음수 방어용 임의값 | ①이면 데이터로 옮길 때 상한도 설정값이어야 하고, ②면 100% 정책이 생기면 조용히 99% 가 된다
[A187] index.html:1510 | "return n > 1 ? n / 100 : n;" | 임계가 > 1 이라 정확히 1 은 보정되지 않는다 | 갈래① 1 = 100% 할인이라 의도적으로 통과 ② 1 은 1% 를 뜻하는 수기값일 수 있어 경계 버그 | ①이면 100% 할인이 가능해지고, ②면 1% 가 100% 로 오작동. parseFixedDc(1562,1570)도 동일 임계를 쓴다
[A188] index.html:1670 | "const toInt=v=>{const m=String(v||'').replace(/[^\\d]/g,'');return m===''?0:Math.min(999999,parseInt(m,10));};" | 주문앱은 음수 부호를 제거해 음수 수량이 불가. 견적앱(estimate-app/views/index.ejs:3240)은 "[^0-9-]" 로 음수를 허용하고 Math.max(-999999,...) 로 하한을 둔다 | 갈래① 주문앱에는 음수 수량 개념이 없는 게 맞다 ② '절삭'(항상 음수)을 주문앱은 다른 경로로 다룬다 ③ 복제 시 누락 | 절삭/운임 행의 주문앱 취급이 견적앱과 다르다는 신호. 실제 처리 코드는 3329줄 이후(다른 담당 구간)
[A189] index.html:2088-2094 vs 2189-2202 | needTriple_ 은 "parts.length >= 3", buildTripleSpecRows_ 는 "if(parts.length!==3) return null" | 갈래① '/' 4조각 이상 데이터가 실재하지 않아 무해 ② 실재하면 배관길이/고낙차 행이 triple 로도 일반 행으로도 렌더되지 않고 통째로 사라진다 | ②면 스펙 표에서 값이 조용히 소실. spec_detail_map 의 maxPipe/maxDrop 중 '/' 4개 이상인 행 수를 세어야 확정
[A190] index.html:1908-1909 vs 1998-1999 vs 2059-2060 | 홈멀티 판넬은 "holeSize: s.cool_kw, boltGap: s.cool_power", 싱글/상업 판넬은 "['타공사이즈(mm)', s.cool_cap_kcal], ['전산볼트간격(mm)', s.cool_pow_kw]" | 갈래① 스코프마다 원천 스펙 스키마가 실제로 다르다 ② 한쪽이 필드명 오타/복제 잔재 | 같은 '타공사이즈' 를 스코프별로 다른 필드에서 읽는다. 동적화 시 필드 매핑을 스코프별로 유지할지 통합할지 판단 필요
[A191] index.html:2168 | "['덕트구경', clean_(s.gas)]" | 냉매가스 필드를 '덕트구경' 으로 표시 | 갈래① 전열교환기 행은 gas 컬럼에 덕트구경을 저장하는 관행 ② 필드 재사용 실수 | ①이면 데이터 모델에 명시적 컬럼이 필요하고, ②면 잘못된 값이 표시 중. 원천 데이터 확인 필요
[A192] index.html:1649-1660 | "if(wantAI) score += /(ai|동작감지)/i.test(t) ? -6 : 3; ... score += /(블랙|승강)/i.test(t) ? 2 : -1;" | 가중치 -6/+3, -4/+2, -2/0, +2/-1 | 갈래① 우선순위 서열만 의미 있고 절대값은 임의 ② 특정 조합에서 순위가 뒤집히도록 정밀하게 맞춘 값 | ①이면 규칙 테이블(우선순위 목록)로 데이터화 가능, ②면 값 자체를 보존해야 한다. 실제 판넬 후보 집합으로 두 방식의 선택 결과가 같은지 대조해야 확정
[A193] index.html:1589 | "const m = s.match(/(\\d{2})(\\d{2})(\\d{2})/);" | 비고에서 첫 연속 6자리를 YYMMDD 로 해석 | 갈래① 비고에는 입고예정일만 숫자로 적힌다 ② 모델코드/수량 등 다른 6자리가 먼저 걸리면 엉뚱한 날짜가 된다(예: '2512 분기관 3419' 는 공백 제거 후 '2512분기관3419' 라 매치 안 되지만 '251203' 류는 매치) | ②면 존재하지 않는 '예정' 라벨이 붙거나 품절 상태가 가려진다. 실제 비고 값 분포를 세어야 판정
[A194] index.html:1802-1803 | "const tot = ratioIn + ratioOut; let indoor = Math.round(remain * ratioIn / tot);" | 분모 0 검사 없음 | 갈래① 호출부가 항상 양수 비율을 준다 ② 실내기/실외기가 모두 0대인 세트가 있으면 NaN → 이후 roundK(NaN)=NaN 이 단가로 흘러간다 | ②면 금액이 NaN 으로 표시/저장될 수 있다. 호출부는 이 구간 밖이라 확인 필요
[A195] index.html:1710 | "if(scope==='single'){t=t.replace(/실링용\\s*드레인펌프/gi,'실링용 드레인펌프');}" | 치환 좌/우변이 사실상 동일 | 갈래① 공백 정규화가 목적 ② 원래 다른 문자열로 바꾸려다 만 미완성 ③ 복제 잔재 | ②면 의도된 표시명이 누락된 상태. 개발책임자 확인 필요
[A196] index.html:1622 | "// 여차하면 '중형'으로 간주 return 'm';" | 대/중/소형 키워드가 없을 때 무조건 중형 | 갈래① 업무상 기본이 중형 ② 매칭 실패를 감춘 폴백 | ②면 잘못된 크기의 판넬이 조용히 선택된다
[A197] index.html:1420-1431 | isExpansionModel 의 "m.substring(5, 7) === 'CS'", "m.substring(8, 10) === '24'" | 모델코드 자릿수 계약이 코드 밖 어디에도 선언돼 있지 않다 | 갈래① 삼성 모델코드 명명 규칙이 실제로 고정 자릿수 ② 예외 모델이 있어 오분류 | ②면 확장형이 아닌 세트가 SINGLE_SETS 에서 통째로 사라진다(1433 필터). getModelFlags(1473-1500)의 m[7]/m[8]/m[10] 도 같은 전제 위에 있다
[A198] index.html:1453-1455 | "homeDiscount: 0.45, commDiscount: 0.45" | 서버 config 미제공 시 45% 를 조용히 적용 | 갈래① 45% 가 회사 표준 기본 할인 ② 개발용 임시값 | ②면 파트너 DC 미설정 상태에서 잘못된 금액이 표시된다. 실패 시 0 이나 오류가 아니라 45% 로 계산되는 것이 의도인지 확인 필요
[A199] index.html:2036-2037 vs 1868 | renderCommSpec_ 은 name/disp/feat/catM 를 이어 붙여 "/전열교환기|erv/i" 로 ERV 판정, openSpecModalByItem 은 "M==='전열교환기'" 완전일치로 판정 | 갈래① 두 판정이 항상 같은 결과 ② catM 이 '전열교환기(ERV)' 같은 값이면 모달은 안 열리고(=차단) 렌더러만 ERV 로 본다 | ②면 특정 품목의 스펙 모달이 열리지 않는다


########## range-order-html-2.json
[A200] index.html:3509-3510 | const v = Math.max(q, parseInt(inp.value||'0',10)||0); if (v !== q){ inp.value = st.qty; ... } | ① v 는 비교에만 쓰고 실제 대입은 st.qty(=실외기 수량 q) — 즉 현재 입력값이 q 보다 크면 q 로 '내려쓴다' ② 원래는 inp.value = v (큰 쪽 유지) 였어야 하는데 오작성 ③ 의도적으로 항상 실외기 수량과 1:1 강제 | ①②는 사용자가 손으로 늘린 받침대 수량이 되돌려지는지 여부가 갈린다(잠금이 없으면 되돌아감). 동적 전환 시 '받침대 수량 = max(현재, 실외기수)' 인지 '= 실외기수' 인지 확정 필요
[A201] index.html:3221-3224 | const limit = hasStrict ? 103.0 : 120.0; box.classList.toggle('bad', parseFloat(ratioTxt) >= limit); | ① 103/120 이 삼성 카탈로그 규격이라 데이터로 빼야 한다 ② 실외기 계열별 표라 계열마다 달라야 한다 ③ 코드 상수로 유지 | 또한 비교 대상이 원본 ratio 가 아니라 소수 1자리로 반올림한 표시 문자열이다 — 102.96% 는 103.0 으로 표시되며 경고가 켜진다. 홈멀티는 130 초과(>)이고 상업은 이상(>=)이라 경계 방향도 다르다
[A202] index.html:4402-4407 vs 4339-4350 | KIT_WHITELIST | ① 표(19개)와 필터(10개)가 의도적으로 다르다 ② 한쪽이 갱신되고 다른 쪽이 누락됐다 | 필터 옵션에는 안 뜨는데 표에는 보이는 품목 9종(호환중계기(ERV용)·시운전모듈 2종·터치중앙제어기·16실중앙제어기·SIM 중계기·전력 분배기·외부접점중계기·SPI KIT)이 생긴다. 어느 쪽이 정본인지 확인 필요
[A203] index.html:2770-2776, 3283-3294 | priceFrom(s,{...,formulaAware:true}) | ① priceFrom(2679)이 formulaAware 를 읽지 않으므로 무효 인자 ② 예전에 수식 셀 처리 로직이 있었고 제거되면서 호출부만 남았다 | 무효라면 삭제 가능하지만, 시트에 '=수식' 문자열이 들어오는 경우 처리가 실제로 필요한지 확인 필요
[A204] index.html:3316-3318 | const unitExtras = setPrice - baseL; const baseAdjusted = adjustSingleSetBasePrice(s, baseL); setPrice = baseAdjusted + unitExtras; | ① 할인은 세트 기준가에만 걸고 판넬·리모컨 차액과 자재비에는 안 건다 ② 옵션 가감분에도 걸어야 한다 | ①이면 판넬 업그레이드분·자재비는 정가로 청구된다. 금액에 직접 닿는 갈림
[A205] index.html:3406-3407 | const ratioIn = isHousehold ? 6 : 4; const ratioOut = isHousehold ? 4 : 6; | ① 세금계산/납품 내역 표기용 관행 비율 ② 실제 원가 비중 ③ 임의값 | 세트 단가를 실내·실외기 행으로 쪼개 전송하는 금액이라 값이 그대로 전표에 남는다. 근거 확인 필요
[A206] index.html:2812 | rateAmt < 1 ? Math.round(val * (1 - rateAmt)) : Math.max(0, val - rateAmt) | ① CONFIG 값이 0~1 이면 비율, 1 이상이면 정액이라는 이중 규약 ② 항상 비율인데 1 이상 방어 코드 | 정확히 1 (=100% 할인) 을 넣으면 비율이 아니라 '1원 차감' 으로 해석된다. 경계값 의도 확인 필요
[A207] index.html:2805-2819 | 6종 할인 순차 적용 | ① 여러 플래그가 동시에 참이면 곱해서 누적하는 게 맞다 ② 하나만 적용해야 한다(우선순위) | 예: 4way + 1등급이 동시에 참인 모델은 두 번 깎인다. getModelFlags(1473)가 배타적으로 나오는지 확인 필요
[A208] index.html:2731-2737 vs 2855-2863 | homeUnitPrice 는 useK2 가 아니면 고정DC 를 아예 안 본다 / commUnitPrice 는 useK2 가 아니어도 고정DC 단독 분기가 있다 | ① 홈멀티에는 고정DC 개념이 없다 ② 홈멀티에 분기가 누락됐다 | ②라면 홈멀티에서 고정DC 가 있는데 useK2 가 꺼진 품목은 할인이 통째로 빠진다
[A209] index.html:3015 | /...|ghp방진|acl-korg hp|acl-korg hp|acl-korghp/i | ① 'acl-korg hp' 가 두 번 반복된 단순 중복 ② 서로 다른 공백 변형을 노린 것인데 같아졌다 | 정규식 동작에는 영향 없지만 의도한 변형(예: 'ACL-KORG HP07')이 빠졌는지 확인 필요
[A210] index.html:3083, 4565, 4010, 4179 | if(manual && q === 0) e.target.value = '0'; | ① '명시적 0' 을 잠금으로 남겨 자동 계산이 다시 채우지 못하게 하는 의도 ② 0 은 빈칸과 같아야 한다 | ①이면 '0 을 입력해 파생품을 끄는' 것이 정식 기능이고, 전환 시 이 상태를 저장해야 한다
[A211] index.html:4321 | sel('판넬변경',['판넬제외','기본판넬','블랙판넬','승강판넬','공청판넬','동작감지'],...) | ① '동작감지' 판넬이 pickPanelBy(1636)/pickCommPanelModel 에서 처리된다 ② 목록에만 있고 계산에서는 기본판넬로 떨어진다 | 이 구간에서는 comm_panel 소비처가 pickPanelBy 뿐이라 확정 불가. 싱글 pickPanelRow(3266)에는 '동작감지' 분기가 없다
[A212] index.html:2649 | mdl.startsWith('AF70') && (mdl.substring(8,10)==='24' || mdl.substring(8,10)==='25') | ① 모델코드 9~10번째 자리가 연식 코드라는 규약 ② 특정 모델군만 걸리는 임시 처리 | 자리수 규약이면 데이터(연식 컬럼)로 빼야 하고, 임시 처리면 모델 목록으로 빼야 한다. 규약 확인 필요
[A213] index.html:3654, 4043 | cls.catL==='냉난방 스탠드' && cls.catM==='프레스티지' && size==='13' 인 세트를 표와 필터에서 모두 숨김 | ① 단종/판매중지 ② 중복 등록 제거 ③ 특정 거래처 전용 | 하드코딩된 단일 조합 배제라 데이터(판매중지 플래그)로 옮길지 판단 필요
[A214] index.html:2528-2544 | 계열별 HP 목록 | ① 카탈로그 표를 그대로 옮긴 것 ② 누락 있음 | 실측: 한랭지·isExtra 는 '대' 등급이 없고, 프라임은 26·28·30 이 어느 등급에도 없다. 표 원본과 대조 필요
[A215] index.html:2574-2580 | const plus = (s.match(/\+/g)||[]).length; return plus; | ① 조각 n 개짜리 세트는 분기관 n-1 개가 맞다 ② 조각 수만큼 필요하다 | 괄호가 없는 단품 세트는 0 을 반환한다. 분기관 수량 계산의 근거 확인 필요
[A216] index.html:2594-2595 | fixed 맵에 'L:부자재' 키가 두 번 나온다('197,225,165' 와 '200,230,201') | ① 오타 ② 의도 없음 | JS 객체 리터럴이라 뒤(200,230,201)가 이긴다. 앞의 색은 죽은 값
[A217] index.html:2707, 2747, 2787, 2833, 3300 | 'I형 유연호스 8000' 이 5곳에 각각 하드코딩 | ① 시트 단가가 부정확해 임시 고정 ② 실제 확정 단가 | 5곳의 품명 판정 정규식이 서로 다르다(2707/2747/2787/2833 은 /유연호스\s*I형/, 3300 은 공백 제거 후 두 어순). 한 곳으로 모을 때 판정 기준도 통일 필요
[A218] index.html:3392-3395 | Math.round(Number(setUnitOverride)) || Math.round(Number(s.price)) || ... | ① 0 은 '값 없음' 이다 ② 0 원 세트가 존재할 수 있다 | ②라면 0 원 세트가 setBasePriceRightFirst 값으로 되살아나 전개 금액이 생긴다
[A219] index.html:3867-3876 | 정렬 비교 함수가 '실외기 받침대' vs '인테리어핏' 두 값만 명시 비교 | ① 이 두 분류만 순서 문제가 있었다 ② 일반 분류 순서표가 필요하다 | 비교가 비추이적일 수 있어(a<b, b<c 인데 a,c 는 _idx 순) 엔진에 따라 결과가 달라질 여지가 있다
[A220] index.html:2403-2405 | /^AM/i + /실외기/ 또는 /dvm|프라임|표준형|한랭지|상부토출/ 로 실외기 판정 | ① 실내기 품명에는 이 단어들이 절대 안 나온다 ② 오분류 가능 | '분기관' 만 명시 제외라, 예컨대 품명에 '프라임' 이 든 실내기가 있으면 실외기로 잡힌다. 실 데이터 분포 확인 필요
[A221] index.html:3860, 4040, 4413 | 세 화면 모두 /운임|절삭/ 행을 표에서 제외 | ① 주문서 화면에는 안 띄우고 전송/인쇄 단계에서만 붙인다 ② 아예 취급 안 한다 | 개발책임자 메모리상 종합견적서에서 운임·절삭은 금액 입력 시 수량 1 이 붙어 포함되는 계열이다. 이 앱(주문서)이 어느 레거시 문서 기준인지 확인 필요 — 구간 밖(전송 로직)에 별도 처리가 있는지도 함께
[A222] index.html:3329 | const isHideMat=p=>/유연호스\s*I형|운임|절삭/i.test(kind+' '+name); | ① 세트 전개에서 운임·절삭·I형호스는 항상 빼는 게 맞다 ② 금액에는 반영돼야 하는데 표시만 빼야 한다 | ①이면 세트 단가 안에 이미 녹아 있다는 전제이고, ②면 전개 총합이 세트 단가와 어긋난다. 위 항목과 같은 갈림
[A223] index.html:2444-2475 | 덕트 분기 — 무선을 골라도 유선 모델(AWR-WE13N) 반환 | ① 덕트형은 무선 리모컨이 없어 강제 유선 ② 버그 | 주석은 '무선·유선 모두 유선으로' 라 ①로 보이나 업무 근거는 코드에 없다
[A224] index.html:2478-2481 | pickHoseModel 은 kind '2way' 에 대해 '' 반환 | ① 2way 실내기는 유연호스가 없다 ② 누락 | commIndoorKind(2410)는 '2way' 를 반환하므로 2way 실내기가 존재한다는 뜻이다
[A225] index.html:4500-4502 vs 3944/4116 | 상업만 lIndex 폴백이 없다 | ① 상업은 중분류가 항상 있어서 불필요 ② 누락 | 중분류가 빈 상업 행은 mIndex 가 0 으로 고정돼 같은 톤으로 그려진다(표시만 영향)


########## range-order-html-3.json
[A226] index.html:4413 | `if (/운임|절삭/i.test(nm)) return false;` (renderOld 4819 · sumOld 4891 · openPreview 6189 · buildSendRows 6627 · 싱글 5146/5174/5189/5199 도 동일) | 갈래① 주문앱은 운임·절삭을 '표시도 계산도 전송도 하지 않는다'가 확정 사양이다 / 갈래② 종합견적서 규칙(금액 입력 시 수량 1 자동, 절삭은 음수)이 주문앱에도 있어야 하는데 누락된 것이다 / 갈래③ 문서(주문서)별로 취급이 다른 것이 정본이다 | ①이면 동적 전환 시 운임·절삭 품목에 '이 문서에서 제외' 플래그가 필요. ②면 주문 금액이 운임/절삭만큼 틀리게 전송되고 있다는 뜻. 어느 레거시 문서가 이 앱의 정본인지 확인 필요
[A227] index.html:4831 | `finalPrice = Math.round(listPrice * 0.5)` (isDisc 는 '수식에 $I$1 있음' 주석) | 갈래① 0.5 는 시트 셀 $I$1 의 현재 값이고 셀이 바뀌면 따라가야 하는 **동적 할인율**이다 / 갈래② 구형 품목 전용 고정 50% 정책이라 리터럴이 맞다 / 갈래③ $I$1 은 거래처별 값이라 전역DC(dc_configs)로 옮겨야 한다 | ①·③이면 0.5 를 하드코딩한 5곳(4831·4871·4900·6198·6638)이 전부 틀어진다. isDisc 플래그가 '수식 존재 여부' 만 담고 **비율 값을 안 담는 것**이 핵심 위험
[A228] index.html:5177 | `if(/^(AP230DAPDHH1S|AP290DAPDHH1S)$/i.test(mdl)) flat+=q; else round+=q;` | 갈래① 이 2개 모델만 일자발이 필요한 실제 사양이다 / 갈래② 실외기 크기·형상에서 파생되는 규칙인데 당시 해당 모델이 2개뿐이라 열거한 것이다 | ②면 신규 대형 실외기가 추가될 때마다 조용히 원형발통으로 잘못 계산된다. 모델 열거를 품목 속성(예: 발통 형상)으로 바꿔야 하는지 확인 필요
[A229] index.html:5528 | `b1509 = indoorCount - singleOutCount - sixHpSingleCount;` (주석 '엑셀 수식 적용') | 갈래① 6HP 단배관은 2512 를 쓰므로 1509 계산에서 두 번 빼는 것이 맞다(실외기로 한 번, 6HP 로 또 한 번) / 갈래② 원 엑셀 수식이 `실내기 - 실외기` 였고 6HP 차감은 나중에 덧붙은 것이라 이중 차감이다 | ①이면 6HP 단배관 n대일 때 1509 가 (실내기-2n), ②면 (실내기-n). 실측: 6HP 가 섞이면 두 해석의 분기관 수량이 6HP 대수만큼 달라진다. 원 엑셀 수식 원문 확인 필요
[A230] index.html:5462 | `setR(REMOTE_COLOR_AIRCOMBO, Math.max(0, cntCombo), 'AWR-WG00N');` 가 5457 의 `if (opt === '제외') return;` **뒤**에 있다 | 갈래① 리모컨 '제외' 는 에어콤보 리모컨까지 포함해 전부 제외하는 것이 의도다 / 갈래② 에어콤보 컬러 리모컨은 필수 부속이라 '제외' 와 무관하게 나가야 하는데 게이트 위치가 잘못됐다 | ②면 리모컨 제외 선택 시 에어콤보 리모컨이 누락된 채 주문된다. 주석은 '별도 가산' 이라 적혀 있어 ②를 시사하나 코드는 ①로 동작
[A231] index.html:5301-5347 | 1WAY 분기(5329-5341)에는 `return` 이 없고 360/4WAY 분기(5318·5325)에는 있다 | 갈래① 의도된 비대칭 — 1WAY 인피니트는 1WAY 판넬과 인피니트 판넬 **둘 다** 필요하다 / 갈래② 실수로 return 이 빠졌고 인피니트는 1WAY 카운터에 들어가면 안 된다 | ②면 '1Way 인피니트 중형' 같은 품목 1대에 판넬이 2장 붙는다. 실제 그런 품목명이 카탈로그에 있는지 세어 봐야 판별됨
[A232] index.html:5736 | `want.set(hose1, n1w)` (REPLACE) vs 5719/5750/5801 `want.set(m, (want.get(m)||0) + q)` (ADD) | 갈래① 호스는 계산 결과가 항상 전량이라 덮어쓰기가 맞다 / 갈래② 판넬/리모컨과 같은 누산이어야 하는데 호스만 빠진 것이다 | ①이면 문제 없음. ②라면 pickHoseModel 이 판넬 매핑과 같은 모델을 돌려주는 경우 앞선 누산이 지워진다. 실측으로 겹치는 모델이 있는지 확인 필요
[A233] index.html:5771-5776 | `if(row && isCommPumpRow(row)) commQty.set(model, 0);` — 파생 계산이 사용자 입력 수량을 0 으로 되돌린다 | 갈래① PUMP_MAP 의 왼쪽(키)이 결과품, 오른쪽(값)이 입력 실내기라 실내기 중 '펌프' 품명 행만 지우는 방어 로직이다 / 갈래② 실제로는 사용자가 직접 넣은 펌프 수량이 조용히 사라지는 결함이다 | ②면 사용자가 드레인펌프를 수동 주문할 방법이 없다. isCommPumpRow 의 정의(내 구간 밖)와 실 데이터에서 이 조건에 걸리는 행 수를 세야 판별됨
[A234] index.html:5232·5240·5248·5263·5278 | PANEL_SWAP_4WAY_TO_AIR · INF_BASE · findHomePanelModel · pickInfinitePanelModel · inferInfiniteSize 5개가 정의만 되고 호출 0회 | 갈래① 리팩터링 잔재로 삭제 대상이다 / 갈래② 실사용 인라인 코드(5388-5405·5409-5414·5345)가 나중에 복사돼 나온 것이고 이 함수들이 '원본 사양' 이다 | ②면 동적 전환의 기준을 어느 쪽으로 삼느냐에 따라 결과가 달라질 수 있다. 현재는 값이 일치해 차이가 안 보이지만 findHomePanelModel 의 '기본' 우선 점수식(-2/+2/-1)은 pickPanelBy 와 알고리즘 자체가 다르다
[A235] index.html:5901 vs 5907 | `wifi = /WIFI\s*내장|WIFI내장/i...` 와 `hasWifi = /WIFI\s*내장|Wi[-\s]*Fi\s*내장/i...` 가 별개 정규식 | 갈래① 360 과 1/4WAY 의 품명 표기 관행이 실제로 달라 일부러 나눈 것이다 / 갈래② 한쪽만 나중에 'Wi-Fi' 표기를 지원하도록 고쳐진 표류다 | ②면 품명이 'Wi-Fi 내장' 인 360 실내기는 wifi===null 이 되어 MAP360 조회 자체를 건너뛰고 **판넬이 아예 안 붙는다**. 실 데이터에서 'Wi-Fi' 하이픈 표기 행 수를 세야 함
[A236] index.html:6041 | `const t = Number(String(cell.textContent||'').replace(/[^\d]/g,''));` | 갈래① 표시 문자열에서 콤마·원 표시를 벗기는 안전 폴백이다 / 갈래② 화면 표시값을 전송 단가로 승격시키는 경로라 표시 포맷이 바뀌면 금액이 틀어진다 | 두 갈래 모두 사실이나, `[^\d]` 제거는 **음수 부호와 소수점도 지운다** — 절삭 같은 음수 금액이 이 경로를 타면 부호가 사라져 양수가 된다. 싱글 세트에 음수 단가가 존재할 수 있는지 확인 필요
[A237] index.html:6425-6460 | 병합 키가 `model + '||' + price` 이고 name 은 뒤 항목으로 덮어쓴다 | 갈래① 같은 모델·같은 단가면 표시명이 달라도 한 행으로 묶는 것이 사양이다 / 갈래② 단가가 다르면 별도 행으로 남기는 것이 사양이다(고정DC·할인 플래그가 행마다 다를 수 있어서) | 현 코드는 ①+②를 동시에 만족하나, 병합된 행은 **첫 항목의 fixedDc/할인 플래그를 유지하고 name 만 뒤 것을 쓴다** — name 과 플래그의 출처가 서로 다른 행이 된다. 의도인지 확인 필요


########## range-order-html-4.json
[A238] clients/web/order-app/index.html:7226 | 'csum<=1200 ? 1509 : csum<=2000 ? 2512 : csum<=2800 ? 2812 : csum<=3100 ? 2815 : csum<=3800 ? 3419 : 4119' | 갈래A: 인라인 폴백은 죽은 코드이고 codeByCumulativeSum(150/406/464/696/986)만 정본이다 / 갈래B: 두 사다리는 단위가 다르다 — 인라인은 3자리 원값(072=72) 누적이 아니라 다른 스케일(예: ×10 또는 W 단위) 기준이었고 리팩터 때 한쪽만 갱신됐다 / 갈래C: 둘 다 살아 있는 서로 다른 규칙이다 | 영향: DB 로 옮길 때 어느 임계값 집합을 정본으로 시드하느냐에 따라 분기관 코드가 통째로 달라진다. 같은 파일 안에서 임계값 사다리가 3벌(codeByCumulativeSum·codeByOutdoorHP·firstBranchByOutdoorCap)이고 인라인까지 4벌이다
[A239] clients/web/order-app/index.html:7157 vs :7732 | 실내기 cap 을 7157 은 parseInt(AM 뒤 3자리) 원값으로 쓰고, 7732 는 같은 값에 *0.1 을 곱해 kW 로 쓴다 | 갈래A: 코드 판정(누적합)은 의도적으로 3자리 원값 단위, 조합비만 kW 단위 / 갈래B: 둘 중 하나가 단위 버그 | 영향: codeByCumulativeSum 의 임계값 150/406/464/696/986 이 '3자리 원값 누적' 기준인지 'kW 누적' 기준인지가 확정되지 않으면 DB 이관 시 10배 어긋난다
[A240] clients/web/order-app/index.html:7701 vs :7745-7747 | limitByOutdoor 는 /CST\s*UV|PREMIUM|한랭/i → 103:120, updateBranchRatios 는 /프라임|한랭지|표준형|냉난방/ → 103.0:120.0 | 갈래A: limitByOutdoor 는 폐기된 구버전(호출부 없음) / 갈래B: 서로 다른 입력(모델원문 vs 품명)에 대한 별개 규칙 / 갈래C: 둘을 합집합으로 봐야 한다 | 영향: 한도 103 이 걸리는 품목 집합이 갈래마다 다르다. '표준형'·'냉난방'이 103(타이트) 쪽인 것은 직관과 반대로 보이므로 업무 확인 필요
[A241] clients/web/order-app/index.html:7710-7717 firstBranchByOutdoorCap | outCap 임계 140/260/280/340/410 → AXJ-YA 모델 문자열 직접 반환. 이 파일 6601-8800 구간 안에 호출부가 없다 | 갈래A: 죽은 코드 / 갈래B: 구간 밖(다른 에이전트 범위)에서 호출된다 / 갈래C: 견적앱에만 살아 있는 규칙의 잔재 | 영향: 살아 있다면 '첫 분기관' 규칙이 recomputeBranchCodes 의 '첫 칸은 항상 -' 규칙과 정면 충돌한다
[A242] clients/web/order-app/index.html:7223 | 'if(filled.length > 1)' — 첫 번째로 채워진 칸의 코드는 항상 '-' | 갈래A: 실외기 직결 첫 구간에는 분기관이 필요 없다는 업무 규칙 / 갈래B: 인덱스 off-by-one 버그 | 영향: 실내기 1대만 배치된 실외기는 분기관 수량이 0으로 집계되어 pushBranchPartsToCommFromBadges 가 상업멀티 분기관 수량을 0(빈칸)으로 덮어쓴다
[A243] clients/web/order-app/index.html:8093-8099 getTierBonusRate | 1천만/3천만/5천만/1억 → +1%p/+2%p/+3%p/+4%p, 상한 48% | 갈래A: 금액 구간별 추가 할인(누진) 정책이 실재하고 DB(estimate_configs 확장)로 옮겨야 한다 / 갈래B: 특정 시기 프로모션이라 이관 대상이 아니다 | 영향: 이관하면 모든 45% 거래처의 납품가가 금액대에 따라 바뀐다. 현재 dc_configs·estimate_configs 어디에도 티어 테이블이 없다
[A244] clients/web/order-app/index.html:8102-8104 isStandard45 | 'Math.abs(rate - 0.45) < 0.001' | 갈래A: 표준 45% 거래처에만 보너스/페널티를 준다는 명시적 정책 / 갈래B: 45%를 '기본값'의 동의어로 쓴 임시 구현이라 거래처별 할인율이 다양해지면 규칙이 무력화된다 | 영향: 44%·46% 거래처는 티어 보너스도, 메인장비 미포함 페널티(0.40)도 전혀 받지 않는다. DB 로 옮길 때 '기본율 플래그'로 바꿀지 '0.45 비교'를 유지할지 갈린다
[A245] clients/web/order-app/index.html:8115-8121 | noMain 이면 45%→40% 로 내리고, 그 결과 isStandard45 가 거짓이 되어 티어 보너스는 적용되지 않는다 | 갈래A: 의도된 우선순위(페널티가 보너스를 무효화) / 갈래B: 실행 순서에서 비롯된 부작용 | 영향: 부속만 1억원어치 주문하면 40%만 적용된다. 순서를 바꾸면 44%가 된다
[A246] clients/web/order-app/index.html:8152-8178 | r.REMARKS(대문자) 안의 '45%' 를 새 율로 치환 | 갈래A: 실제로 동작하는 표기 동기화 / 갈래B: 죽은 코드 — buildSendRows(6541-6657)가 만드는 행에는 REMARKS 키가 없고 OLD 행만 소문자 remarks 를 가지며 section 이 'OLD' 라 대상에서 제외된다 | 영향: 갈래B 면 적요의 할인율 표기가 조정 전 값으로 남는다. 실행 확인 필요
[A247] clients/web/order-app/index.html:6637-6643 | 구형 isDisc=true 는 정가×0.5, false 는 sheetPrice 를 그대로 사용 | 갈래A: 두 경로가 원래 다른 가격 원천(정가 기반 계산가 vs 시트에 이미 확정된 납품가) / 갈래B: sheetPrice 도 실제로는 정가×할인율이 미리 계산된 값이라 0.5 와 중복 | 영향: estimate_configs.old_product_discount_rate(0.5000)로 이관할 때 isDisc=false 행의 단가 원천을 무엇으로 볼지에 따라 구형 전 품목 단가가 달라진다
[A248] clients/web/order-app/index.html:6627 | '품 명' 문자열 정확 일치로 헤더행 제외 | 갈래A: 시트 헤더가 데이터로 섞여 들어오는 것을 막는 방어 / 갈래B: 공백 개수가 다른 실제 품목명('품  명' 등)이 있으면 걸러지지 않는다 | 영향: 시트 원본의 공백 표기에 의존. 7745 에서도 '품    명'/'품명'/'품 목 명' 3종을 폴백으로 읽고 있어 컬럼명 공백이 일정하지 않음이 이미 드러난다
[A249] clients/web/order-app/index.html:7773-7790 | pushBranchPartsToCommFromBadges 가 분기관 6종 수량을 commQty 에 REPLACE 로 설정하고 0 이면 빈칸으로 만든다 | 갈래A: 분기계산 결과가 항상 정본이라 사용자 수기 입력을 덮어쓰는 것이 맞다 / 갈래B: 사용자가 직접 넣은 분기관 수량은 보존해야 한다 | 영향: 분기 화면에 들어갔다 나오기만 해도 수기 입력 분기관 수량이 소실될 수 있다


########## range-order-html-5.json
[A250] clients/web/order-app/index.html:9103-9121 vs 9192-9217 | 'function updateTopControls()' 가 같은 스코프에 두 번 정의 | (a) 9192 가 의도된 최신본이고 9103 은 지우다 만 잔재 (b) 9103 이 원본이고 9192 가 잘못 추가된 중복 | 호이스팅으로 9192 가 항상 이김. 두 정의는 동작이 반대(9120 은 전환버튼 항상 숨김 / 9213 은 모바일에서 표시). 어느 쪽이 정본인지 코드로 확정 불가 — 개발책임자 확인 필요
[A251] clients/web/order-app/index.html:8903 vs 9269 vs 9807 | 합계 계산이 세 곳에서 서로 다른 식 | (a) 상세모달은 서버 subtotal 을 신뢰(권위=서버), 나머지는 화면 재계산 (b) 전부 qty*price 여야 하는데 상세모달만 누락 | 서버 subtotal 과 qty*price 가 다른 항목(운임·절삭·할인 반영 행 등)이 있으면 화면마다 다른 금액이 보임. 어느 것이 권위인지 확인 필요
[A252] clients/web/order-app/index.html:8904 | 'const reversedItems = d.items.slice().reverse();' | (a) 서버가 역순으로 주므로 되돌리는 것 (b) 최근 입력 항목을 위에 보이려는 의도 | 서버 정렬이 바뀌면 표시 순서가 조용히 뒤집힘. 근거가 코드에 없음
[A253] clients/web/order-app/index.html:9253 | 'const targetSize = 450000;' | (a) 저장 백엔드(구 Notion/현 DB) 필드 길이 상한 (b) google.script.run 페이로드 상한 (c) 임의 안전값 | 상한을 넘겨도 차단하지 않고 그대로 전송하므로(9327 하한 도달 시) 실패 조건이 무엇인지 확인 필요. 단위도 '문자 수' 라 UTF-8 바이트와 다름
[A254] clients/web/order-app/index.html:9327-9340 | 'while (totalPayload > targetSize && (quality > 0.3 || scaleFactor > 0.5))' | (a) 0.3/0.5 는 화질 마지노선 (b) 임의값 | 하한에 도달해도 초과 페이로드를 그대로 저장 시도 → 실패 시 사용자에게는 '저장 실패' 만 노출. 초과 시 어떻게 해야 하는지 정책 미정
[A255] clients/web/order-app/index.html:9503-9510 | 'restoreLegacyDerivedQty(...)' 2단 계산 | (a) 구형 스냅샷은 파생 규칙이 달라 저장값을 그대로 살려야 함 (b) 자동 재계산이 맞고 저장값은 참고만 | 현행은 자동계산과 다르면 저장값으로 덮고 수동잠금까지 걸어 이후 자동 계산에서 제외됨. 규칙 변경 시 구형 스냅샷 값이 영구히 고착
[A256] clients/web/order-app/index.html:9506 | "restoreLegacyDerivedQty('commercial', commQty, shot.core.commQty)" | (a) 스코프 이름 체계가 'commercial' 로 통일되어 있고 상태변수만 comm 축약 (b) 이름 불일치 버그 | 2349 restoreSnapshotManualQtyLocks 도 comm→commercial 로 변환하므로 (a) 로 보이나, targetScope_ 의 키 집합을 확인해야 확정 가능
[A257] clients/web/order-app/index.html:9691-9697 | 'catch (e) { return []; }' | (a) 의도적 무해화 (b) 객체를 기대하는 호출부와 어긋난 실수 | 배열을 받은 applySnapshot 은 form/core/branch 가 모두 undefined 라 '전부 초기화' 로 진행하고 '복원 완료' 를 표시함. 복원 실패를 사용자가 알 수 없음
[A258] clients/web/order-app/index.html:9729 | 'expireTime = Date.now() + (3 * 3600 * 1000);' | (a) 서버 세션 수명과 맞춘 값 (b) 화면 전용 임의값 | 서버 세션이 더 짧으면 타이머가 남아 있어도 요청이 실패, 더 길면 조기 종료. 서버 값과 대조 필요
[A259] clients/web/order-app/index.html:9532-9540 및 8742, 8754 | 기본 조회 범위 7일이 세 곳(MAX_DAYS 상수 1곳 + 리터럴 2곳)에 흩어짐 | (a) 셋 다 같은 정책 (b) 기본값 7일과 최대 7일은 별개 정책인데 우연히 같은 수 | 정책을 바꿀 때 한 곳만 고치면 조용히 어긋남
[A260] clients/web/order-app/index.html:9854-9879 | 튜토리얼 하드코딩 모델코드 9종 | (a) 시연 전용 고정 시나리오 (b) 카탈로그와 동기화되어야 하는 값 | 해당 모델이 단종되거나 화면에서 사라지면 10013 무한 재시도로 튜토리얼이 그 스텝에서 멈춤(에러 없음)
[A261] clients/web/order-app/index.html:9867, 9871, 9877-9879 | 'input[data-model="…"]' 가 컨테이너 미지정 | (a) 모델코드가 전 섹션에서 유일하다는 전제 (b) 실수 | 같은 모델코드가 싱글·상업 양쪽 표에 있으면 DOM 상 먼저 나오는 요소가 잡힘. 홈멀티만 data-m(4010), 싱글·상업은 data-model(4179·4565) 로 속성명이 다름
[A262] clients/web/order-app/index.html:10046-10053 | 0.6 / 0.4 임계 사이 구간에 분기 없음 | (a) 의도적 히스테리시스(중간대에서 흔들리지 않게) (b) else 누락 | 0.4~0.6 에 있는 타깃은 직전 스텝의 박스 위치를 그대로 물려받아 타깃을 가릴 수 있음
[A263] clients/web/order-app/index.html:9213 | "sw.textContent=(mobileActive==='home')?'싱글중대형 보기':'홈멀티 보기';" | (a) 전환 버튼은 home↔single 2개만 오가는 것이 정책 (b) 4섹션 모두 순환해야 하는데 미구현 | comm/old 활성 상태에서 버튼은 '홈멀티 보기' 로 표시되는데 제목(9214)은 4분기라 라벨과 상태가 어긋남


########## done-clients_web_estimate-app_views_index.ejs_1-2200.json
[A264] index.ejs:1753-1761 | `<select id="selCutUnit">…<option value="10000">10,000원 단위` | ①최종 총액을 그 단위로 절사하는 '반올림 단위' ②`절삭`이라는 별도 음수 행의 금액 단위 ③둘 다 | 갈래에 따라 이관 위치가 완전히 달라짐 — ①이면 dc_configs 계열 반올림 규칙, ②면 전표 라인 생성 규칙. 확정 모델의 반올림은 거래처 마스터인데 이 컨트롤은 전표별 사용자 선택이라 축이 다름. 계산 본체(goPreview/goFinal)는 구간 밖
[A265] index.ejs:1745-1748 vs 1889-1892 | `id="chkCardPay"` / `id="chkCardPayPv"` | ①한 상태의 두 미러(syncVatCardPv 가 양방향 동기화) ②독립된 두 상태 ③한 방향만 동기화 | ②·③이면 전표작성에서 켠 카드결제가 미리보기 금액에 반영 안 되거나 그 반대가 됨. 이관 시 '카드 수수료 적용 여부'가 필드 1개인지 2개인지가 갈림. syncVatCardPv 본체는 구간 밖
[A266] index.ejs:1766-1781 | `name="optVatDisplay" value="inc|exc|none"` — 라벨 'VAT포함 표기/VAT별도 표기/표기삭제' | ①셋 다 라벨만 바뀌고 금액 동일 ②inc↔exc 는 금액이 다르고 none 은 라벨만 ③셋 다 금액 변환 | ①이면 순수 표시 규칙(데이터 이관 대상 아님), ②·③이면 세율이 붙는 금액 규칙. 라벨의 '표기'라는 단어는 ①을 시사하지만 근거가 단어뿐이라 확정 불가
[A267] index.ejs:1768,1773,1778 | `onchange="syncVatFromOrderInfo(); if(window.activeSector==='preview') goPreview();"` | ①의도적 — final(전표업로드목록)은 VAT 표기와 무관 ②누락 — chkCardPay(1746)·selCutUnit(1754)은 둘 다 부르는데 여기만 goFinal 이 빠짐 | ②라면 final 화면을 보는 중 VAT 표기를 바꾸면 화면이 갱신 안 되는 도달 가능 결함. goFinal 이 VAT 를 읽는지에 달렸고 그 본체는 구간 밖
[A268] index.ejs:2061-2068 | `<span data-k="1509">1509 <b>0</b> <input class="extra-branch" min="0">` | ①extra-branch = 자동 산출 개수에 더하는 가산값 ②자동 산출을 덮어쓰는 대체값 ③별도로 집계되는 추가 발주분 | 확정 모델(기준 N→종속 1, SUM 누산)에는 '사람이 손으로 넣는 가산' 축이 없다. ①이면 수동 오프셋 축 추가, ②면 REPLACE 누산이라 모델과 충돌. 6코드가 무엇인지(분기관 모델코드로 보이나) 라벨이 없어 확정 불가
[A269] index.ejs:105 | `#wrapHome tfoot, #wrapSingle tfoot, #wrapComm tfoot{ display:none!important; }` | ①합계는 다른 위치(모바일 서랍·상단 배지)에서만 보여주는 것이 의도 ②임시로 죽여 둔 것 ③homeTotal 등은 계산 결과 저장소로만 쓰임 | 계승 검증 시 '합계가 안 보인다'를 미계승으로 오판할 소지. homeTotal 에는 `data-testid="dc-final-price"` 가 붙어 테스트는 이 값을 읽는데 사용자는 이 위치에서 못 본다
[A270] index.ejs:1654-1660 | `value="00003">초월창고` / `value="2">상일창고` / `value="14">온라인` / `value="1">서초` | ①이카운트 원본 코드가 실제로 zero-padding 이 섞임 ②하나(00003)만 padding 실수 ③서버에서 정규화함 | 조인 키로 쓸 때 '2'와 '00002', '00003'과 '3' 이 갈린다. 이 구간에 정규화 코드가 없다. 집PC 시드에 창고 코드 `2`·`00003` 이 없다는 기존 실측이 있어 실 데이터 대조 필요
[A271] index.ejs:1793-1798 | `id="chkYard">야적적용` / `id="chkLocal">지방적용` | ①전표 태그(delivery_tag STACK/REGION)만 세팅 ②금액(운임 등)에 영향 ③둘 다 | 이 두 체크박스에는 onchange 가 없어 어디서 읽는지 구간 내로는 알 수 없다. ②면 금액 축이라 정밀 경로, ①이면 태그 데이터. 기존 메모리의 '지방·야적은 태그'는 전표 도메인 이야기이고 이 견적앱 체크박스가 같은 것인지는 확정 불가
[A272] index.ejs:1603-1609 | `<!-- <tfoot>…<strong id="oldTotal">0</strong>… -->` | ①구형 탭은 합계를 쓰지 않기로 한 것 ②임시 주석이고 되살려야 함 ③다른 곳에서 oldTotal 을 만들어 쓴다 | ②·③이면 구형 탭 합계가 계승 대상. 다른 3탭에는 같은 구조가 살아 있고 colspan 이 6 이 아니라 3 이라 구형 탭 7열 구성에 맞춰져 있음 — 한때 동작했을 가능성


########## done-clients_web_estimate-app_views_index.ejs_2201-4400.json
[A273] clients/web/estimate-app/views/index.ejs:4250-4254 applyHomeMultiPriceVat | `const finalVat = roundByConfig(base); return finalVat;` | ① 원래 VAT 를 붙이려다 미구현으로 남은 죽은 함수 ② prefix 인자를 빠뜨린 버그(예: 'home' 이 들어가야 함) ③ 의도적으로 항등(이름만 잘못) | 호출처가 0 건이라 현재 값 영향은 없다. 다만 이관 시 '홈멀티 VAT 규칙이 있다'고 오해하면 없는 규칙을 만든다. 어느 쪽인지 코드로 확정 불가 — 확인 필요
[A274] clients/web/estimate-app/views/index.ejs:3040-3043 handleFreightInput 소계 | `subCell.textContent = fmt(val);` (수량을 곱하지 않음) | ① 운임/절삭은 항상 수량 1 이므로 소계=단가 라는 전제를 코드로 고정한 것 ② 수량×단가 계산을 빠뜨린 것 | 지금은 같은 핸들러가 수량을 1 로 강제하므로 값이 일치한다. 그러나 다른 경로가 이 행의 수량을 2 이상으로 바꾸면 소계가 조용히 틀어진다. '운임 행은 수량이 항상 1' 이 불변식인지 확인 필요
[A275] clients/web/estimate-app/views/index.ejs:4200 basesForSetPiecesByExistingRule_ | `const nmOne = nm.replace(/\(([^)]*)\)/, `${hp}HP`);` | ① 괄호를 없앤 형태가 맞다 ② 8490줄의 `(${hp}HP)` 처럼 괄호를 남기는 것이 맞다 | 같은 변환이 두 곳에 다른 형태로 존재한다(4200 괄호 제거 / 8490 괄호 유지). chooseBaseModel 의 hasExactHP 정규식은 `(^|[^0-9.])`/`([^0-9.]|$)` 라 괄호 유무로 매칭이 갈릴 수 있다. 다만 4195 함수는 호출처가 0 건이라 현재는 8490 만 동작 — 어느 쪽이 정본인지 확인 필요
[A276] clients/web/estimate-app/views/index.ejs:4350 유연호스 I형 8000 | `if (!showIHose && /유연호스\s*I형/i.test(rawName)) return 8000;` | ① I형을 안 쓰는 거래처에는 L형 단가 8000원을 대신 매긴다 ② I형 자체의 고정 단가가 8000원이다 ③ 카탈로그 단가를 무시하는 임시 하드코딩 | 리터럴 8000 이 4350·4392·4416·4461 네 곳에 반복되고, 게이트 소스도 두 종류(4350·4461 은 DOM 체크박스, 4392·4416 은 window.SHOW_I_HOSE)라 옵션 갱신 시점에 따라 값이 갈릴 수 있다. 8000 의 업무 의미 확인 필요
[A277] clients/web/estimate-app/views/index.ejs:3214-3224 pickPanelBy prefer 점수 | `if(wantAI) score += /(ai|동작감지)/i.test(t) ? -6 : 3; … score += /(블랙|승강)/i.test(t) ? 2 : -1;` | ① 가중치 크기(-6/3, -4/2, -2/0, 2/-1)에 업무적 우선순위 의미가 있다 ② 단순히 '옵션 일치 > 기본 > 블랙·승강 회피' 순서를 만들기 위한 임의 숫자다 | 데이터로 옮길 때 숫자를 그대로 옮길지, 순위(1·2·3)로 바꿀지가 갈린다. 특히 '블랙|승강' 에 +2(=후순위)를 주는 이유가 코드에 없다 — 확인 필요
[A278] clients/web/estimate-app/views/index.ejs:3370-3396 splitIndoorOutdoorToK | `if(outdoor>0){indoor-=mod;outdoor+=mod;} else {indoor+=(1000-mod);outdoor-=(1000-mod);}` | ① 잔여 1000 미만은 항상 실외기로 몰아준다 ② 실외기를 천 단위로 맞추는 것이 우선이고 실내기는 종속이다 ③ else 분기(outdoor<=0)는 방어 코드일 뿐 실제로 밟히지 않는다 | ratioIn/ratioOut 의 출처가 이 구간 밖(5287줄 호출부)이라 비율이 데이터인지 상수인지 여기서는 확정 불가. 배분 우선순위(실외기 우선)가 업무 규칙인지 확인 필요
[A279] clients/web/estimate-app/views/index.ejs:3541-3542 renderHomeSpec_ 판넬 필드 재해석 | `holeSize: s.cool_kw || '', boltGap: s.cool_power || ''` | ① 판넬 스펙 시트가 냉방 칼럼을 타공사이즈·전산볼트간격으로 재활용해 저장하고 있다 ② 필드 매핑 실수다 | 같은 재해석이 3640-3641(renderSingleSpec_ 판넬: cool_cap_kcal→타공사이즈, cool_pow_kw→전산볼트간격)과 3705-3706(renderCommSpec_ 판넬)에도 있는데 **원본 필드명이 서로 다르다**(cool_kw vs cool_cap_kcal). 데이터로 옮기려면 판넬 스펙의 정본 필드명을 확정해야 함 — 확인 필요
[A280] clients/web/estimate-app/views/index.ejs:3154-3167 getStockState_ 날짜 파싱 | `const m = s.match(/(\d{2})(\d{2})(\d{2})/); const y = 2000 + parseInt(m[1],10);` | ① 비고에 YYMMDD 만 적힌다는 전제 ② 6자리 이상 숫자(모델코드 조각·수량 등)가 섞이면 오탐을 감수한 것 | 공백을 모두 지운 뒤 첫 6연속 숫자를 잡으므로 모델코드나 숫자 나열이 붙으면 엉뚱한 날짜로 읽힌다. 또 월(m[2])·일(m[3]) 범위 검증이 없어 '99' 같은 값도 Date 가 롤오버한다. 의도된 허용 범위 확인 필요
[A281] clients/web/estimate-app/views/index.ejs:3083-3090 numInp step1000 | `if (v>0 && v<1000) v = v*1000; if (v%1000!==0) v = Math.round(v/1000)*1000;` | ① 사용자가 '30' 을 치면 30,000원을 뜻한다는 편의 규칙 ② 만원 단위 입력을 원 단위로 환산하는 규칙 | 999 를 입력하면 999,000 이 되고 1,000 을 입력하면 1,000 이 된다(불연속). 적용 대상이 step===1000 인 입력칸 전체라 어떤 칩이 여기 해당하는지도 이 구간만으로는 확정 불가 — 확인 필요
[A282] clients/web/estimate-app/views/index.ejs:2362-2363 죽은 모델 상수 | `const INDOOR_AY047 = (HOMEMULTI.find(r=>/^AY047BA1SBA$/i.test(r?.model||''))||{}).model || null;` | ① 과거 규칙(특정 실내기·판넬 자동 추가 등)의 잔재 ② 다른 스크립트 블록에서 쓰이는데 grep 이 못 잡은 것 ③ 앞으로 쓸 자리를 잡아 둔 것 | grep 결과 정의부 1건뿐이라 현재 동작에는 영향이 없다. 이 두 모델코드에 붙어 있던 규칙이 무엇이었는지 모르면 이관 시 규칙 하나를 통째로 잃을 수 있다 — 확인 필요
[A283] clients/web/estimate-app/views/index.ejs:2414-2437 getRealOldPrice 커스텀 분기 | `if(item.isDisc){p=Math.round(p*(1-rateVal/100));} else { if(!isCustom){p=Math.round(Number(item.sheetPrice)||0);} }` | ① isDisc 가 아닌 품목은 고정가를 쓰되 사용자가 출고가를 고치면 그 값을 그대로 납품가로 쓴다 ② 고친 출고가에도 할인을 걸어야 한다 ③ 고정가를 항상 우선해야 한다 | 같은 계산이 2875-2880(handlePriceInput)에도 있는데 **그쪽에는 isCustom 분기가 없다**. 즉 같은 상황에서 두 경로가 다른 값을 낸다. 어느 쪽이 정본인지 확인 필요
[A284] clients/web/estimate-app/views/index.ejs:4220-4226 countBranchForSet | `const plus = (s.match(/\+/g)||[]).length; return plus;` | ① 실외기 N 대를 잇는 데 분기관은 N-1 개('+' 개수)라는 규칙 ② 세트 구성 데이터에 분기관 수량이 따로 있어야 하는데 이름 파싱으로 대신하고 있는 것 | 괄호가 없으면 0 을 반환하므로 단일 실외기 세트는 분기관 0 이 된다. 호출부 8496 은 `branchCnt += countBranchForSet(nm) * q` 로 세트 수량을 곱한다. 'N-1' 이 실제 업무 규칙인지 확인 필요
[A285] clients/web/estimate-app/views/index.ejs:4143-4147 parseSetHPs 소수 HP | `.map(s=>s.replace(/[^0-9]/g,''))` | ① 세트 괄호 안에는 정수 HP 만 들어온다는 전제 ② 소수 HP 도 들어오는데 '.' 이 제거되는 버그 | chooseBaseModel 은 hasExactHP(nm,'3.5')·(nm,'7.5') 로 **소수 HP 를 명시적으로 다룬다**. 그런데 parseSetHPs 는 '3.5' 를 '35' 로 바꾼다. 세트 이름에 소수 HP 가 실제로 등장하는지, 등장하면 어떻게 처리해야 하는지 확인 필요
[A286] clients/web/estimate-app/views/index.ejs:4056-4063 vs 3203-3208 way 종류 집합 | commIndoorKind 는 '2way' 를 반환 ↔ pickPanelBy matchKind 는 360/4way/1way 만 처리 | ① 2way 는 판넬·호스가 없어 의도적으로 빈 것 ② 2way 규칙이 누락된 것 | pickCommPanelModel(4130) 경유로 2way 를 넘기면 matchKind 가 항상 false 라 판넬을 못 고르고, pickHoseModel(4122)도 2way 에는 '' 를 반환한다. 2way 실내기가 실재하는지, 실재한다면 판넬·호스가 정말 없는지 확인 필요


########## critic.json
[A287] clients/web/order-app/src/quantitySync.ts:158 | if (factors.some((factor) => Math.abs(factor * multiplier - 1) > 1e-9)) return selectionError('S-03 설정이 legacy 수량과 일치하지 않습니다.') | (a) legacy 대조 기간에만 두는 임시 안전장치다 (b) factor·multiplier 는 영구적으로 곱이 1 이어야 한다는 계약이다 (c) S-03 이라는 특정 규칙에만 걸리는 제약이다 | (a)면 DB 로 1:2·1:0.5 파생을 표현할 수 있고 코드만 풀면 된다. (b)면 quantity_sync_rules 스키마에 factor/multiplier 두 컬럼을 두는 의미가 사라진다(항상 서로 역수). (c)면 다른 규칙을 열 때 이 검사를 어디까지 복제할지 결정해야 한다. 모듈 상단 주석은 'shadow 경계'라고만 적고 있어 어느 쪽인지 코드로 확정되지 않는다.
[A288] clients/web/order-app/src/quantitySync.ts:207-208 vs clients/web/order-app/src/samhanApi.ts:230-232 | targetQuantity = roundingMode === 'FLOOR' ? Math.floor(raw) : raw  //  vs  if (!Number.isInteger(quantity) || quantity < 1) throw | (a) 파생 수량은 항상 정수여야 하고 roundingMode 기본값 NONE 이 잘못이다 (b) 소수 수량이 정당하고 confirmLines 의 정수 게이트가 지나치게 좁다 (c) 두 층이 만나는 경로가 아직 없어 문제되지 않는다 | (a)면 roundingMode 기본을 FLOOR/ROUND 로 바꿔야 하고, (b)면 주문 확정 계약을 소수 허용으로 넓혀야 한다. (c)라면 shadow 를 실경로로 붙이는 순간 '전송 전체 실패'로 드러난다 — 수량 1건 때문에 주문 전체가 거부된다.
[A289] clients/web/estimate-app/views/index.ejs:2345-2361 vs clients/web/estimate-app/lib/code.js:148-174 | 클라이언트 CONFIG 기본값 14키 vs 서버 DEFAULT_ESTIMATE_CONFIG 20키 | (a) 서버가 권위 정본이고 클라이언트 리터럴은 부팅 실패 시 폴백이다 (b) 둘 다 살아 있고 화면은 클라이언트 값, 전표는 서버 값을 쓴다 (c) 하나는 죽은 코드다 | 키 이름이 서로 다르다: 클라이언트는 singleSetDiscount, 서버는 singleDiscount + singleOneWayDiscount. 클라이언트에는 discount360/discount4way/discountStand 기본값이 아예 없고 서버 buildDefaultDcConfig_ 는 그 3키를 만든다. (b)라면 같은 견적의 화면 금액과 전표 금액이 갈릴 수 있다. 어느 쪽을 지우고 어느 쪽으로 모을지는 업무 판단이 필요하다.
[A290] clients/web/estimate-app/lib/code.js:346-364 vs tools/legacy-gas/일마감 프로그램/Code.js:383-388 | formatWonDiscountLabel_ 의 chun = Math.round((abs%10000)/1000)  vs  fmtMinusUnit 의 if (v % 10000 === 0) ... if (v % 1000 === 0) ... else 원 단위 | (a) 견적서 라벨(반올림 축약)이 정본이다 (b) 일마감 라벨(정확 나눗셈만 축약)이 정본이다 (c) 둘은 서로 다른 화면의 별개 표기라 통일 대상이 아니다 | 15,500원 정액DC 를 견적서는 '-1만6천', 일마감은 '-15500' 으로 쓴다. 같은 거래처 DC 를 두 화면에서 본 사용자가 다른 숫자를 읽는다. (c) 라면 전환 후에도 두 규칙을 둘 다 보존해야 한다.
[A291] tools/legacy-gas/일마감 프로그램/Index.html:1234-1236 | rowData['합계'] = unit * qty; rowData['공급가액'] = Math.round(unit / 1.1); rowData['부가세'] = unit - rowData['공급가액']; | (a) 공급가액/부가세도 수량을 곱해야 하는데 빠진 결함이다 (b) 이 화면의 공급가액 컬럼은 '단가의 공급가' 라는 뜻이라 의도된 것이다 | (a)면 수량 2 이상 행에서 합계와 (공급가액+부가세)가 어긋나고 일마감 검증이 통과시킨 값이 계산서 단계에서 틀어진다. (b)면 컬럼 라벨이 오해를 부르는 것뿐이다. 코드만으로는 가릴 수 없다 — 레거시 화면에서 수량 2 이상 행을 열어 봐야 한다.
[A292] clients/web/estimate-app/lib/db-catalog.js:178-180 | // homeEx 는 legacy 시트 별도 컬럼(확장형) — 현 OduRecommendationLookup 이 미분리. return { comm, home, homeEx: home.slice() }; | (a) 확장형 추천 세트가 실제로 일반형과 같아 무해하다 (b) 다른 세트인데 DB 미분리로 값이 조용히 대체됐다 | 주석이 스스로 '미분리' 라고 적고 있어 (b) 쪽 신호가 강하다. (b)라면 확장형 홈멀티 추천 실외기가 지금 틀린 값을 내고 있고, 그것을 알아채는 장치가 없다. legacy 시트의 확장형 컬럼과 현 DB 행을 대조해야 확정된다.
[A293] tools/legacy-gas/거래처 발송 주문서/Code.js:1359 vs tools/legacy-gas/종합견적서/Code.js:1123 | 주문서: idx(H, ['성능(kcal/h)(최소/정격/최대)','성능(kcal/h)(최소/정격/최대)'])  vs  견적서: idx(H, ['성능(kcal/h)(최소/정격/최대)','성능(kcal/h) (최소 / 정격 / 최대)']) | (a) 주문서 쪽 복붙 실수다 (b) 주문서 시트에는 공백 변형 헤더가 없어 의도적으로 같게 뒀다 | (a)면 주문서 앱은 공백이 들어간 헤더를 가진 시트에서 냉난방 성능 컬럼을 못 찾아 규격이 비게 된다. 전환 시 별칭 목록을 DB 로 옮길 때 어느 쪽을 정본으로 삼을지 정해야 한다.
[A294] clients/web/order-app/dist/index.html:4911-4912 vs clients/web/order-app/index.html:5162-5163 | dist: if(FOOT_ROUND) homeQty.set(FOOT_ROUND,Math.max(0,totalRoundHome));  vs  src: if(totalRoundHome > 0) setHomeDerivedQty_(FOOT_ROUND, '발통세트', totalRoundHome, '발통 파생'); | (a) dist 는 낡은 빌드이고 소스가 정본이다 (b) dist 가 현재 배포본이라 사용자가 겪는 동작은 dist 쪽이다 | dist/index.html 은 마지막으로 커밋 930f14e52 에서 갱신됐고 소스와 612줄 삭제/180줄 추가 차이가 있다. (b)라면 이번 조사의 소스 기준 결론이 실사용 동작과 다르다. dist 를 재빌드해 대조하기 전에는 어느 쪽이 배포되는지 코드만으로 확정할 수 없다.
[A295] clients/web/estimate-app/lib/code.js:2258-2283 | return (homeHit || singleHit) ? '2' : '00003';  및 :2384 WH_CD: whCd || '100' | (a) '2'(상일)·'00003'(초월)·'100' 세 창고코드가 모두 현행이다 (b) 일부는 이미 폐기된 코드다 | 사용자 메모리 실측 기록에 집PC 기준 창고 코드 '2' 와 '00003' 이 DB 에 없다고 남아 있다. 실 데이터에 이 세 코드가 각각 몇 건 있는지 세지 않고 전환하면 전표가 존재하지 않는 창고로 나간다. 업무 의미(어느 창고인가)는 코드로 확정 불가.
[A296] clients/web/estimate-app/lib/code.js:2359-2363 | const split = splitVatAmount_(total, ...); const unitSplit = splitVatAmount_(priceVat, ...); const priceEx = unitSplit.supply; | (a) 전표의 권위 금액은 SUPPLY_AMT/VAT_AMT(합계 기준 분리)이고 PRICE 는 참고용이다 (b) PRICE x QTY 가 SUPPLY_AMT 와 같아야 한다 | 단가 분리와 합계 분리를 따로 하므로 priceEx*qty 와 supply 가 어긋날 수 있다(예: 단가 1,000원·수량 3 → priceEx 909, 909x3=2,727 이지만 total 3,000 의 supply 는 2,727. 단가 1,100·수량 3 이면 priceEx 1,000, 3,000 vs supply 3,000 — 값에 따라 갈린다). (b)가 요구사항이라면 현재 계산은 특정 금액대에서 어긋난다.
[A297] tools/legacy-gas/영업수수료 계산/Index.html:339-340 | var supply = xround(subtotal / 1.1); var vat = subtotal - supply; | (a) 세금계산서 발행 기준이 소계(선지급 차감 전)다 (b) 실제 지급액(payout)을 기준으로 분리해야 한다 | 화면은 '차인 지급액 (VAT포함)' 을 payout 으로 표시하면서 바로 아래 공급가/부가세는 subtotal 기준으로 낸다. 선지급 공제가 있는 건에서 공급가+부가세 != 차인지급액 이 된다. 어느 쪽이 회계상 옳은지는 업무 확인이 필요하다.
[A298] tools/legacy-gas/계산서일괄등록양식 생성/Index.html:325 | raw.filter(r => (...) && !String(r['거래처명'] || r['전표번호'] || '').includes('합계')) | (a) 엑셀 소계행을 걸러내려는 것이고 거래처명에 '합계' 가 든 거래처는 없다 (b) 거래처명까지 검사하는 것이 과도하다 | (b)라면 상호에 '합계' 가 들어간 실제 거래처의 계산서가 통째로 누락되고 오류 없이 조용히 빠진다. 실 거래처 목록에서 상호에 '합계' 가 든 건수를 세야 확정된다.
[A299] 범위 판단 | tools/legacy-gas 아래 30개 도구 중 이번 조사가 훑은 것은 종합견적서·거래처 발송 주문서 2개(35,730줄)뿐이고 나머지 28개 도구 47개 파일 37,066줄은 미조사다 | (a) 이번 전환 범위는 견적/주문 2앱으로 한정된다 (b) '',GAS에 하드코딩된 수식을 모두'' 는 30개 도구 전부를 뜻한다 | (b)라면 미조사분에 계산 관련 줄이 1,373줄 있고, 그 안에 영업수수료 12단계 금액 체인·일마감 할인검증 엔진·거래명세서/원장/계산서 금액 매핑이 들어 있다. 범위 결정 전에는 '전수' 라고 말할 수 없다.


########## kind-금액.json
[A300] sumOld 의 0.5 (종합견적서/index.html:7144 · index.ejs:7577 · 거래처 발송 주문서/index.html:4574 · order-app:4900) | `finalP = Math.round(listP * 0.5)` | 갈래① 합계만 구식 코드가 남은 버그이고 #old_rate/oldDiscount 를 따라야 한다 / 갈래② 구형 '표시 할인율'과 '실제 계상 할인율'이 원래 별개다(50% 는 회계상 고정) | ①이면 행 소계와 합계가 지금 어긋나 있다(할인율을 40 으로 바꾸면 즉시 재현) · ②라면 오히려 getRealOldPrice 쪽이 틀린 것이라 전표 금액이 잘못 나간다. 어느 쪽인지 코드로 확정 불가
[A301] I형 유연호스 고정단가 7000 vs 8000 | 종합견적서/index.html:3934·3977·4001·4045·4720 = 7000 / 나머지 3개 앱 = 8000 | 갈래① 견적서만 갱신이 늦어 7000 이 남은 것이고 8000 이 현행 / 갈래② 견적(대외 제시가)과 주문(발주가)이 원래 다른 값 | ①이면 legacy 견적서를 쓰던 기간의 견적서가 1,000원씩 낮다 · ②라면 index.ejs 가 7000→8000 으로 바꾼 것이 회귀다. index.ejs 가 이미 8000 으로 갔으므로 이 판정이 소급된다
[A302] incActive 의 부등호 방향 (order-app:1450 `return due < String(effectiveDate)`) vs 레거시 (거래처 발송 주문서/index.html:2463 `if (due >= PRICE_INC_DATE ...)`) | 갈래① *_INC 테이블의 의미가 '인상 후 단가'(레거시 해석)였는데 신규가 '인상 전 단가'로 재정의했다 / 갈래② 레거시가 처음부터 반대였고 신규가 바로잡은 것 | 어느 쪽이든 같은 납기일에 두 앱이 서로 다른 단가를 낸다. 주석은 '인상전'이라 하지만 주석은 근거가 아니다 — 실제 *_INC 시트의 값이 base 보다 큰지 작은지를 세어야 확정된다
[A303] singleUnitPrice 의 calc 이중 모드 | 거래처 발송 주문서/index.html:2542 · order-app:2812 `rateAmt < 1 ? Math.round(val*(1-rateAmt)) : Math.max(0, val-rateAmt)` vs 종합견적서/index.html:4026 · index.ejs:4442 `Math.max(0, val - rateAmt)` | 갈래① 주문 계열의 이중 해석이 정본이고 견적 계열이 비율 케이스를 빠뜨렸다 / 갈래② 정액 전용이 정본이고 주문 계열이 과잉 방어를 넣었다 | CONFIG.discount360 등이 0.05 같은 값으로 들어오면 견적은 5원 차감, 주문은 5% 차감 — 금액이 수십만원 단위로 갈린다. dc_configs 실데이터에서 이 6개 필드가 1 미만인 행이 몇 건인지 세야 판정 가능
[A304] commUnitPrice 고정DC 분기 조건 | 거래처 발송 주문서/index.html:2594 `fixedDc != null && fixedDc > 0` vs order-app:2858 `fixedDc != null` | 갈래① `> 0` 이 의도적 가드였고 신규가 빠뜨렸다 / 갈래② 고정DC 0% 도 '출고가 그대로가 납품가'라는 명시적 의사표시라 분기를 타야 맞다 | ②라면 고정DC 를 0 으로 적어 둔 품목의 납품가가 시트 납품가 → 출고가로 뛴다(할인 0). 고정DC 컬럼에 0 이 실제로 몇 건 들어 있는지 확인 필요
[A305] 받침대/방진가대 고정가 우회 | 거래처 발송 주문서/index.html:2571 에만 존재, 나머지 3개 앱에 없음 | 갈래① 받침대는 DC 대상이 아니라는 업무 규칙이고 계승 누락이다 / 갈래② legacy 주문서 한정 임시 보정이었다 | ①이면 신규 order-app 과 견적앱이 받침대를 할인해 팔고 있다. 정규식이 이름·표시명·모델을 합쳐 소문자 비교하므로 대상 품목 수는 셀 수 있다
[A306] 절삭 타깃 탐색 방향 | 화면 buildQuoteSections(종합견적서/index.html:11065)는 `for (let i = sections.length-1; i>=0; i--)` 역순 / 전송 applyCutoffLogic(16218)은 `rows.find(...)` 정순 | 갈래① 둘 중 하나가 오타 / 갈래② 화면은 '마지막 섹션에서 깎아 보기 좋게', 전송은 '첫 행에서 깎아 이카운트 정렬 유지' 라는 의도적 분리 | 같은 견적에서 화면상 깎인 품목과 전표상 깎인 품목이 다르다. 금액 총액은 같지만 품목별 단가가 달라 거래처 클레임 대상이 될 수 있다
[A307] 카드수수료/절삭 계산 시점에 사용자정의 품목이 빠지는 것 | buildSendRows(종합견적서/index.html:9322-9367 · index.ejs:9628-9680) 가 조정 후에 custom rows 를 push | 갈래① 순서 버그 / 갈래② 사용자정의 품목은 수수료 대상이 아니라는 규칙 | 화면(buildQuoteSections)은 getCustoms 를 절삭 뒤에 부르므로 화면 총액도 같은 성질이지만, 화면은 절삭 후 total 을 재계산하지 않는 경로가 있어 화면 총액과 전표 총액이 다를 수 있다
[A308] fmtMoney(클라이언트 floor) vs formatWonDiscountLabel_(서버 round) | 종합견적서/index.html:15352 `Math.floor((n % 10000)/1000)` vs Code.js:1688 `Math.round((abs % 10000)/1000)` | 갈래① 같은 라벨이어야 하는데 한쪽이 틀렸다 / 갈래② 서로 다른 자리에 쓰이는 별개 라벨이라 무관 | 정액 DC 가 34,500원이면 클라이언트 '-3만4천', 서버 '-3만5천'. 적요에 둘 다 나갈 수 있어 같은 전표에 다른 라벨이 찍힌다
[A309] buildQuoteSections 의 DOM 구성품 단가 읽기 (종합견적서/index.html:10938 `replace(/[^0-9]/g,'')`) vs buildSendRows (9267 `replace(/[^0-9-]/g,'')`) | 갈래① 화면 쪽이 '-' 를 빠뜨린 버그 / 갈래② 구성품은 음수가 될 수 없어 무해 | 세트 구성품이 음수 단가를 가질 수 있는지가 관건. explodeSetParts 는 Math.max(0,...) 로 클램프하지만 사용자가 직접 입력하면 음수가 들어간다
[A310] getActiveFixedDc 의 `replace(/[^0-9.]/g,'')` (종합견적서/index.html:9127) | 마이너스 기호를 제거한다 | 갈래① 고정DC 는 음수일 수 없으니 무해 / 갈래② 음수 고정DC(=할증)를 입력하면 조용히 같은 크기의 할인으로 뒤집힌다 | parseFixedDc(2841)는 `-?\d+` 로 음수를 읽은 뒤 clamp(0,0.99)로 0 으로 만드는데, getActiveFixedDc 는 부호만 지워 양수로 만든다 — 같은 입력에 두 함수가 다른 답을 낸다
[A311] applyHomeMultiPriceVat (종합견적서/index.html:3836 · index.ejs:4252) 의 `roundByConfig(base)` prefix 누락 | 갈래① 홈멀티는 단위처리 대상이 아니다 / 갈래② prefix 를 빠뜨린 버그 | 이 파일의 roundByConfig 는 prefix 없으면 `if(!prefix) return v` 로 원본을 그대로 돌려준다. 반면 legacy 주문서판 roundByConfig 는 prefix 인자가 없어 항상 CONFIG 로 처리한다 — 같은 이름의 함수가 두 앱에서 시그니처부터 다르다
[A312] VAT 표기 라디오(inc/exc/del)가 금액을 바꾸지 않는 것 | getVatLabel(종합견적서/index.html:11145)은 라벨만 만들고 총액 산식에 vatRate 가 어디에도 안 들어간다 | 갈래① 모든 단가가 이미 VAT 포함가라 표기만 바꾸면 된다 / 갈래② 'VAT 별도' 선택 시 총액을 /1.1 해야 하는데 미구현 | 견적서 총액이 '(VAT 별도)' 라벨을 달고도 VAT 포함 금액을 보여주고 있다. 어느 쪽이 의도인지 코드만으로 확정 불가
[A313] explodeCommSets_ 의 'Q' 센티넬 (종합견적서/index.html:6813) | 견적서에만 존재 | 갈래① 구성표 수량칸에 실제로 'Q' 문자가 들어 있고 '세트 수량과 동일'을 뜻한다 / 갈래② 과거 시트 잔재라 지금은 해당 행이 없다 | 주문서에는 이 분기가 없어 같은 구성표를 읽어도 수량이 다르게 나온다(parseInt('Q')=NaN → ||1 → 세트수량×1 로 우연히 같은 값이 되긴 한다). 실제로 'Q' 가 든 행이 몇 건인지 세야 한다
[A314] explodeCommSets_ 부품 없을 때 (종합견적서:6795-6804 세트 본행 1건 반환 vs 거래처 발송 주문서:4436-4439 빈 배열) | 갈래① 주문서가 의도적으로 '구성 없는 세트는 못 보낸다'로 막는다 / 갈래② 반환 누락 버그 | ②라면 구성표에 등록되지 않은 세트를 주문하면 화면에는 수량이 보이는데 전송 목록에서 통째로 사라진다


########## kind-수량.json
[A315] tools/legacy-gas/종합견적서/index.html:8986 vs 거래처 발송 주문서/index.html:2692 | EST `REMOTE_360_DEFAULT = /(AR-?EC05)/i` · ORD `= /(AR-?KH05)/i` | 갈래① 견적앱이 오래된 값이고 360CST 전용 리모컨은 AR-KH05 가 맞다 갈래② 견적앱은 의도적으로 360도 무선 리모컨(AR-EC05)을 쓴다 갈래③ 카탈로그에 AR-KH05 가 없던 시절의 잔재 | 영향: 견적앱은 setR 이 ADD 라 360CST 대수와 1·4WAY+벽걸이 대수가 AR-EC05 한 줄에 합쳐진다(주문앱은 두 모델로 분리). 같은 조합에서 리모컨 품목 구성이 달라짐 — 어느 쪽이 정본인지 코드만으로 확정 불가
[A316] tools/legacy-gas/종합견적서/index.html:7814-7818 (setR = ADD) vs 거래처 발송 주문서/index.html:5038-5043 (homeQty.set = REPLACE) | 홈 리모컨 수량 반영 방식 | 갈래① ADD 가 의도(여러 통이 같은 모델을 가리키면 합산해야 한다) 갈래② REPLACE 가 의도이고 견적앱 ADD 는 초기화가 선행되므로 실질 동일하다고 본 것 | 영향: 서로 다른 두 통이 같은 모델을 지목하는 순간에만 갈린다. 실제로 견적앱에서 REMOTE_360_DEFAULT==REMOTE_WIRELESS 라 이 상황이 상시 발생한다 — 합산이 맞는지 확인 필요
[A317] tools/legacy-gas/종합견적서/index.html:7885 `if (iCnt >= 2 && sOut > 0)` vs 거래처 발송 주문서/index.html:5096 `if (singleOutCount > 0)` | 홈 분기관 계산 발동 조건 | 갈래① 실내기 2대 이상이어야 분기가 생긴다는 업무 규칙(견적앱이 맞다) 갈래② 실외기만 있으면 계산은 하되 뺄셈 결과가 음수라 어차피 0 이 된다(주문앱이 단순화한 것) | 영향: 실내기 1대 + 단배관 실외기 1대일 때 견적앱은 계산 자체를 안 하고, 주문앱은 b1509 = 1-1-0 = 0. 결과는 같지만 실내기 1대 + 6HP 실외기 1대면 견적앱 2512=0, 주문앱 2512=1 로 갈린다
[A318] tools/legacy-gas/종합견적서/index.html:7861 (에어콤보·전열교환기 포함) vs 거래처 발송 주문서/index.html:5074 (제외) | 홈 분기관 실내기 집계 범위 | 갈래① 에어콤보·전열교환기도 분기관이 필요하다 갈래② 별도 배관이라 분기관을 쓰지 않는다 | 영향: 전열교환기가 포함된 견적에서 1509 분기관 수량이 견적앱만 늘어난다
[A319] tools/legacy-gas/종합견적서/index.html:3774 `isCoolTop && ['30','32','34']` vs 나머지 3소스 `isCoolTop 중=[...'30'] 대=['32','34']` | 냉방전용 상부토출 30HP 받침대 | 갈래① 레거시 견적서가 옛 규격이고 30HP 는 S2중이 맞다 갈래② 웹 3소스가 잘못 좁혔고 30HP 는 S2대가 맞다 | 영향: 30HP 상부토출 실외기 1대의 받침대 모델이 S2대↔S2중으로 갈린다. 다수결(3:1)이 정답이라는 보장 없음
[A320] tools/legacy-gas/거래처 발송 주문서/index.html:6812-6819 `csum <= 150` vs 나머지 3소스 `csum < 150` | 분기관 규격 경계 포함 여부 | 갈래① 이하(<=)가 맞고 웹 이식 때 실수로 미만이 됐다 갈래② 미만(<)이 맞고 레거시 주문서가 틀렸다 | 영향: 누적 용량이 정확히 150·406·464·696·986 일 때만 규격이 한 단계 다르다. 실 데이터에서 이 값이 얼마나 나오는지 세지 않으면 영향 규모 불명
[A321] tools/legacy-gas/거래처 발송 주문서/index.html:6838-6896 (하류 잔여용량·첫 칸 강제·마지막 칸 코드 없음) vs 나머지 3소스 (위→아래 누적합·마지막 칸 강제·1개 초과면 전 칸 코드) | 분기관 규격 산정 알고리즘 | 갈래① 하류 잔여 방식이 배관 실물에 맞는 원본이고 견적앱이 단순화했다 갈래② 누적합이 원본이고 레거시 주문서가 실험적으로 바꾼 것이다 | 영향: 같은 실내기 배치에서 6개 칸의 규격이 통째로 달라진다. 웹 주문앱은 레거시 주문서를 버리고 견적앱 방식을 택했다 — 이 변경이 승인된 것인지 이식 실수인지 코드에 근거 없음
[A322] clients/web/order-app/index.html:3380 `qty: qty * (parseInt(p.qty, 10) || 1)` vs 나머지 3소스 `qty: qty` | 싱글 세트 구성품 전개 수량 | 갈래① 대당 개수를 곱하는 것이 맞고 세 소스가 버그다 갈래② 곱하지 않는 것이 맞고(구성품 단가가 이미 세트 단가를 배분받은 값이라 개수를 곱하면 이중 계상) 웹 주문앱이 버그다 | 영향: 대당 개수가 2 이상인 구성품이 있는 세트에서 전송 수량·금액이 배로 갈린다. explodeSetParts 는 단가를 세트 단가에서 배분하므로 곱셈이 들어가면 합계가 세트 단가와 안 맞는다 — ②가 유력하나 확정 불가
[A323] tools/legacy-gas/종합견적서/index.html:6858 `if(String(p.qty).toUpperCase() === 'Q') defQ = 1;` | 구성품 수량 시트값 'Q' 의 의미 | 갈래① '세트 수량과 같음(Quantity 연동)' 이라는 센티널 갈래② 단순 오타/미입력 표식 | 영향: explodeCommSets_(견적앱)은 'Q' 를 세트수량으로, renderCommSetParts 는 계수 1 로 처리해 결과가 같다. 주문앱은 'Q' 처리가 아예 없어 parseInt→NaN→1 로 우연히 같아진다. 의미가 ②라면 전부 잘못 처리 중
[A324] tools/legacy-gas/거래처 발송 주문서/index.html:5111-5124 `let ... nWall=0; ... n1w += nWall;` | 벽걸이 유연호스 처리 | 갈래① 원래 벽걸이도 1WAY 호스를 받아야 했는데 주석 처리로 꺼진 것 갈래② 의도적으로 제외했고 nWall 은 잔재 | 영향: 현재는 nWall 이 끝까지 0 이라 무효. 되살리면 벽걸이 대수만큼 1WAY 호스가 늘어난다. 견적앱에는 nWall 변수 자체가 없다
[A325] tools/legacy-gas/종합견적서/index.html:7752-7770 vs 거래처 발송 주문서/index.html:4900-4934 | 판넬 집계 분기 순서(인피니트 우선 vs 360/4WAY early return + 1WAY 후 인피니트 재검사) | 갈래① 인피니트를 먼저 봐야 한다(견적앱) 갈래② 형상(360/4WAY/1WAY)을 먼저 봐야 한다(주문앱) | 영향: 품명에 '1WAY 인피니트' 가 함께 있으면 주문앱은 1WAY 통과 인피니트 통에 이중 계상되어 판넬 수량이 실내기 대수의 2배가 된다. 실 카탈로그에 그런 품명이 몇 건인지 세지 않으면 실제 발생 여부 불명
[A326] tools/legacy-gas/종합견적서/index.html:4932-4953 `const v = Math.max(q, parseInt(inp.value||'0',10)||0); if (v !== q){ inp.value = st.qty; ... }` | recalcCommAccessories 의 v 사용 | 갈래① `if (v !== q)` 가 아니라 `inp.value = v` 여야 했다(더 큰 값 유지) 갈래② 항상 계산값으로 되돌리는 것이 의도이고 v 는 죽은 코드 | 영향: 현재는 사용자가 받침대 수량을 실외기 대수보다 크게 넣으면 계산값으로 되돌아간다. 작게 넣으면 그대로 유지된다(비대칭)
[A327] clients/web/order-app/index.html:5720-5730 `if(kind==='4way' || kind==='360') n4w += q;` vs 나머지 3소스 `else nNormal += q;` | 상업 4WAY 유연호스 집계 범위 | 갈래① kind 미판별 실내기(덕트·실링 등 이미 제외된 것 외)를 4WAY 호스에 넣는 것이 원래 의도 갈래② 형상이 확정된 것만 넣는 것이 맞고 else 는 과다 계상이었다 | 영향: commIndoorKind 가 ''(빈값)인 실내기 대수만큼 4WAY 호스가 갈린다. 실 카탈로그에서 kind 미판별 상업 실내기가 몇 건인지 세야 규모를 안다
[A328] clients/web/order-app/index.html:2388-2407 (품명 기반) vs 나머지 3소스 (모델코드 7번째 문자 N/X) | 상업 실내기·실외기 판별 기준 | 갈래① 모델코드 위치 규칙이 원본이고 웹 주문앱이 스냅샷 복원 문제 때문에 바꾼 임시 조치 갈래② 품명 기반이 새 정본이고 나머지가 구식 | 영향: 판넬·호스·리모컨·펌프·받침대·분기관·필터 — 상업 파생 수량 전부의 소스 집합이 갈린다. 웹 주문앱 주석은 'snapshot/구형 저장분에 catL 이 빠질 수 있다' 는 방어 목적을 말하지만 원본 규칙을 대체할 의도였는지는 불명
[A329] tools/legacy-gas/종합견적서/index.html:8063 `want.set('AXJ-TA3419M', ... + branchCnt)` | 세트 실외기 분기관을 규격 계산 없이 3419 한 종으로 몰아 주는 것 | 갈래① 세트 실외기는 분기관 규격이 항상 3419 로 고정 갈래② 규격 계산 미구현 상태의 임시값 | 영향: 분기계산 보드는 6규격으로 나누는데 여기만 1종. 두 경로가 같은 상업멀티 분기관 수량을 서로 다르게 쓴다(보드는 REPLACE, 여기는 ADD)
[A330] tools/legacy-gas/종합견적서/index.html:3752-3755 `if(isGHP) { want.push('GHP방진가대'); want.push('ACL-KORGHP07'); }` | GHP 는 HP 조건 없이 항상 2품목 | 갈래① GHP 실외기는 마력 무관하게 같은 받침대를 쓴다 갈래② HP 표가 아직 안 만들어진 것 | 영향: 다른 시리즈는 전부 HP 구간별인데 GHP 만 무조건이다. 규칙 형태가 달라 확정 모델(동기화 factor/multiplier)에 안 맞는다
[A331] tools/legacy-gas/종합견적서/index.html:3772-3774 | isCold(한랭지)·isExtra(프레스티지/동시냉난방/공장전원)에 S2대 행이 아예 없음 | 갈래① 해당 시리즈에는 26HP 이상 제품이 없어 필요 없다 갈래② 누락 | 영향: 한랭지 26HP 이상, 프레스티지 22HP 이상 실외기는 받침대가 하나도 안 붙는다(경고도 없음)
[A332] clients/web/order-app/index.html:5760 `if (sum > 0) want.set(pump, sum);` vs 나머지 3소스 `want.set(pump, sum);` | 드레인펌프 합계 0 처리 | 갈래① 0 도 명시적으로 써야 이전 값이 지워진다(나머지 3) 갈래② seed 단계에서 이미 0 이므로 안 써도 된다(웹 주문앱) | 영향: isCommDerivedRow 가 그 펌프를 파생행으로 못 잡으면 seed 가 안 돼 이전 수량이 남는다
[A333] tools/legacy-gas/종합견적서/index.html:11064 `const found = sec.rows.find(r => r.qty === 1 && r.type !== 'set-head');` | 절삭 나머지를 흡수할 대상 행 선정 | 갈래① 수량 1 인 행이면 단가에서 빼도 총액만 맞으면 된다 갈래② 특정 성격의 행(운임 등)을 노린 것인데 조건이 느슨하다 | 영향: 뒤 섹션의 수량 1 짜리 실제 제품 단가가 조용히 깎인다. 어느 행이 걸릴지는 섹션 순서에 달렸다


########## kind-조건클램프코드.json
[A334] tools/legacy-gas/거래처 발송 주문서/index.html:6813-6818 vs 나머지 3파일 | 레거시 주문서만 `if(csum <= 150) return '1509'` 이고 estimate-app:12670·legacy 종합견적서:12339·order-app:7170 은 `if(csum < 150)` 이다 | 갈래① 이하(<=)가 원본이고 현행 3파일이 옮기며 바뀐 것 ② 미만(<)이 원본이고 레거시 주문서만 오타 ③ 주문서는 downstream 누산이라 경계 의미가 애초에 달라 의도적 | 영향: 누적 용량이 정확히 150·406·464·696·986 일 때 분기관 코드가 한 단계 달라진다. 어느 쪽이 맞는지 코드만으로 확정 불가 — 개발책임자 확인 필요
[A335] legacy 거래처 발송 주문서/index.html:6866-6874 vs estimate-app:12712-12726 | 분기관 누산 방향이 정반대(downstream = totalCap - before 로 아래쪽 남은 용량 / csum += val 로 위쪽 누적) 이고 강제 대상도 첫 칸 vs 마지막 칸 | 갈래① 레거시 주문서가 원본 업무규칙이고 견적앱이 단순화하며 바꿨다 ② 견적앱이 원본이고 주문서가 별도 규칙 ③ 화면 구조(out-drop vs out-slot)가 달라 같은 결과를 다른 방식으로 낸다 | 영향: 같은 실내기 배치에서 칸마다 다른 분기관 코드가 나온다. 현행 order-app:7195-7264 는 이미 견적앱 방식으로 바뀌어 있어 레거시 주문서 동작이 계승 대상인지 확인 필요
[A336] tools/legacy-gas/종합견적서/index.html:3934,3977,4001,4045,4720 = 7000 vs 나머지 3파일 = 8000 | I형 유연호스 강제 단가 | 갈래① 단가 인상(7000→8000)이 반영된 것이고 8000 이 현행 ② 견적 레거시가 다른 기준을 쓰던 것 ③ 애초에 시트에서 읽어야 할 값이 코드에 박힌 것 | 영향: I형 호스 옵션이 꺼진 상태의 모든 견적/주문 금액. 이 값을 동적으로 바꿀 때 기준 시점을 언제로 잡을지 확인 필요
[A337] order-app/index.html:2812 · legacy 거래처 발송 주문서:2542 `rateAmt < 1 ? Math.round(val*(1-rateAmt)) : Math.max(0, val-rateAmt)` vs estimate-app:4442 · legacy 종합견적서:4026 `Math.max(0, val - rateAmt)` | 옵션 할인 6종의 해석 | 갈래① 주문앱은 CONFIG(실수 가능)에서 읽으니 비율 해석이 필요하고 견적앱은 화면 정수 입력이라 정액만 필요 ② 정액이 원 규칙이고 주문앱이 나중에 비율을 끼워 넣었다 | 영향: 옵션 할인값이 0.05 같은 실수로 들어오면 주문앱은 5% 할인, 견적앱은 0원 할인(정수 파싱). 반대로 1 이 들어오면 주문앱은 1원 정액, 견적앱도 1원 — 경계 1 의 의미가 확정되지 않음
[A338] order-app/index.html:1715-1743(adjustSingleSetBasePrice, 정액만) vs order-app/index.html:2806-2819(singleUnitPrice, 비율 또는 정액) | 같은 파일 안에서 같은 CONFIG 6값을 서로 다르게 해석한다 | 갈래① 세트 기준가에는 정액만 적용하는 것이 의도 ② 한쪽이 갱신 누락 | 영향: 같은 옵션 할인 설정으로 싱글 단품과 세트가 서로 다른 금액이 된다
[A339] order-app/index.html:8127,8134 `Math.min(calcH + hBonus, 0.48)` vs estimate-app:13692,13699 `calcH += hBonus` | 티어 보너스 상한 | 갈래① 0.48 은 주문 전용 정책 ② 견적앱이 상한 반영을 빠뜨렸다 | 영향: 1억 이상 홈멀티에서 견적은 49%, 주문은 48% 로 갈린다
[A340] estimate-app/index.ejs:13644(isIndoorOnly, 실외기 유무) vs order-app/index.html:8074-8082(isNoMainUnit, 전열교환기 제외 + 실내기·벽걸이도 메인) | 45%→40% 페널티 발동 조건 | 갈래① 두 앱의 업무 성격이 달라 의도된 차이 ② 한쪽이 나중에 넓혀졌고 다른 쪽 반영이 안 됐다 | 영향: 실내기만 담은 견적은 40%, 같은 구성의 주문은 45% 유지
[A341] estimate-app/index.ejs:4942-4948(hasAJ025 && !hasOtherOut && hasRestrictedIn → '조합 불가') | 주문앱에 대응 규칙이 없다 | 갈래① 견적 단계에서만 막으면 되는 규칙 ② 주문앱에 반영 누락 | 영향: 견적에서 '조합 불가'로 막힌 구성이 주문앱에서는 그대로 통과한다. 접두 4개(AJ025/AJ072/AM072/AM083)의 업무 의미도 코드에 설명이 없다
[A342] estimate-app/index.ejs:8314 `if (iCnt >= 2 && sOut > 0)` vs order-app:5524 `if (singleOutCount > 0)` | Y형 분기관 수량 발동 조건 | 갈래① 실내기 1대면 분기관이 필요 없다는 견적앱 규칙이 맞다 ② 주문앱 조건이 원본이고 견적앱이 방어를 추가했다 | 영향: 실내기 1대 + 단배관 실외기 1대 구성에서 견적은 분기관 0, 주문은 b1509 = 1-1-0 = 0 으로 결과는 같지만, 실내기 1 · 실외기 0(단배관 없음)에서는 둘 다 0. 실제로 갈리는 조합이 있는지 실데이터 확인 필요
[A343] estimate-app/index.ejs:8293(실내기 집계에 에어콤보·전열교환기 포함) vs order-app:5502(제외) | 분기관 수량 산정의 실내기 정의 | 갈래① 에어콤보/전열교환기도 분기관을 쓴다 ② 안 쓴다 | 영향: 전열교환기가 섞인 구성에서 분기관 수량이 앱마다 다르다. 업무 의미 확인 필요
[A344] estimate-app/index.ejs:5370 `const v = Math.max(q, parseInt(inp.value||'0',10)||0); if (v !== q){ inp.value = st.qty; ... }` | 비교에 쓴 v 와 실제 기록하는 st.qty 가 다른 값이다 | 갈래① 의도적(기존 입력이 더 크면 산출값으로 되돌린다) ② v 를 써야 하는데 st.qty 를 쓴 버그 ③ st.qty === q 라서 실질 동일 | 영향: 사용자가 손으로 늘린 받침대 수량이 조용히 산출값으로 되돌아갈 수 있다
[A345] estimate-app/index.ejs:16697-16700(applyCutoffLogic, 정순 첫 행) vs estimate-app/index.ejs:11383-11400(미리보기, 섹션 역순) | 자동 절삭 차감 대상 선택 | 갈래① 두 경로가 서로 다른 산출물을 만들어야 해서 의도적 ② 한쪽이 나중에 고쳐졌고 다른 쪽이 남았다 | 영향: 화면 미리보기와 실제 전송 행에서 서로 다른 품목의 단가가 깎인다
[A346] estimate-app/index.ejs:2354 `comboWarnRate: 0` | 이 키를 읽는 코드가 소스 전체에 없다 | 갈래① 조합비 한도 130/103/120 을 이 값으로 대체하려던 미완 작업 ② 다른 의미(경고 임계)의 별도 값 | 영향: 조합비 한도 동적화의 의도된 착지점이 이것인지 확인 필요
[A347] order-app/index.html:2652-2656 `mdl.startsWith('AF70') && (mdl.substring(8,10)==='24' || mdl.substring(8,10)==='25')` → 무풍클래식 승격 | 견적앱에는 이 규칙이 없고 대신 Q9000·무풍콤보 갤러리프로 분류가 있다 | 갈래① 모델코드 9~10번째가 연식이라 24/25년형은 클래식이라는 업무규칙 ② 특정 시점의 임시 대응 | 영향: 같은 품목이 두 앱에서 다른 중분류로 잡혀 색상·정렬·표시가 달라진다. 모델코드 자릿수 규약의 업무 의미 확인 필요
[A348] estimate-app/index.ejs:4965 `ratio > 130`(초과) vs estimate-app:5054 `parseFloat(ratioTxt) >= limit`(이상, 포맷 문자열 재파싱) | 홈/상업 조합비 판정 | 갈래① 의도된 차이 ② 한쪽이 실수 | 영향: 조합비가 정확히 한도값일 때 홈은 정상, 상업은 경고. 상업은 소수 1자리로 반올림한 문자열을 다시 파싱하므로 103.04% 가 103.0 으로 표시되고 판정도 103.0 기준이 된다
[A349] order-app/index.html:7701 `limitByOutdoor: /CST\s*UV|PREMIUM|한랭/ → 103:120` vs order-app:7747 `tight: /프라임|한랭지|표준형|냉난방/ → 103.0:120.0` vs order-app:3216 `hasStrict: /(프라임|한랭지|표준형|냉난방)/ → 103.0:120.0` | 같은 한도를 세 가지 다른 정규식으로 판정한다 | 갈래① 화면마다 다른 규칙이 맞다 ② 하나가 정본이고 나머지는 낡았다 | 영향: 같은 실외기가 화면에 따라 다른 한도로 평가된다. 어느 것이 권위인지 확인 필요
[A350] legacy-gas/종합견적서/Code.js:385,790 `if (hdrRow < 0) hdrRow = 3;` vs 거래처 발송 주문서/Code.js:367 `if (hdrRow < 0) return {};` | 헤더 탐색 실패 처리 | 갈래① 로더 종류마다 의도적으로 다르다 ② 한쪽이 낡았다 | 영향: 헤더가 없는 시트에서 한쪽은 3행을 헤더로 오독해 잘못된 데이터를 만들고 다른 쪽은 빈 맵을 낸다
[A351] estimate-app/index.ejs:5943 `if(typeof val === 'number' && val <= 1) val = (val*100);` vs estimate-app:9431 `return isNaN(v) ? 0 : (v > 1 ? v/100 : v);` | 경계 1 에서 스케일 변환 방향이 반대인 두 규칙이 같은 파일에 있다 | 갈래① 하나는 표시용(퍼센트) 하나는 계산용(비율)이라 의도적 ② 100% 할인(1.0)을 표현하려는 순간 양쪽이 충돌 | 영향: 할인율 1(=100%)을 넣으면 한쪽은 100 으로 다른 쪽은 1 로 읽는다
[A352] estimate-app/index.ejs:4392 `!window.SHOW_I_HOSE`(전역) vs estimate-app:4350 `!document.getElementById('home_hose_i')?.checked`(DOM) | 같은 견적앱 안에서 I형 호스 게이트 소스가 두 가지 | 갈래① 섹션별 체크박스가 정본이고 전역은 싱글 전용 ② 한쪽이 낡았다 | 영향: 섹션 체크박스를 켜도 싱글 부자재/세트는 전역값을 보므로 8000 강제가 풀리지 않는다. estimate-app:2602 주석에 *'상업멀티 오염 방지 위해 건드리지 않음'* 이라 적혀 있어 의도적일 가능성이 있으나 그 의도가 무엇인지는 확정 불가
[A353] legacy 거래처 발송 주문서/index.html:1342 `PRICE_INC_DATE = '2026-07-01'` 과 due 문자열 비교 5곳 | 현행 order-app 에는 이 게이트가 통째로 없고 견적앱은 체크박스(chkSingleInc)로 대체돼 있다 | 갈래① 인상 반영이 끝나 게이트를 걷어낸 것 ② 이관 중 누락 ③ 서버 단가 자체가 인상단가로 교체돼 필요 없어진 것 | 영향: 납기일 기준 인상단가 규칙이 계승 대상인지 확인 필요. 계승 대상이라면 HOME_INC/COMM_INC/SINGLE_INC/SINGLE_PARTS_INC 4개 맵의 출처도 함께 정해야 한다
[A354] estimate-app/index.ejs:4444-4449(singleUnitPrice) 와 estimate-app:3308-3313(adjustSingleSetBasePrice) 가 같은 6개 옵션 할인을 각각 적용한다 | 세트 안의 단품에 두 번 적용되는지 | 갈래① 경로가 배타적이라 이중 적용 없음 ② 세트 구성품을 단품 단가로 계산하는 경로가 있으면 이중 적용 | 영향: 이중 적용 시 옵션 할인이 2배. calcSetUnitPrice 는 baseL(세트 기준가)에만 adjust 를 걸지만 partUnitPrice/singleUnitPrice 가 구성품에 별도로 걸리는 경로가 있는지 실행 확인 필요
[A355] legacy-gas/거래처 발송 주문서/Code.js:2982,2995 `Math.max(createdTime, logTime, shipTime)` / `Math.max(standardExpTime, tempExpTime)` | 만료 기준 시각 선택 | 갈래① 가장 늦은 이벤트를 기준으로 유효기간을 연장하는 것이 의도 ② 특정 이벤트가 기준이어야 하는데 최댓값으로 뭉갠 것 | 영향: 주문 열람 만료 시점. 업무 의미 확인 필요
[A356] estimate-app/index.ejs:4920 `inCount > 2 ? RECOMMEND_DATA.homeEx : RECOMMEND_DATA.home` | 경계가 2 초과(=3대 이상) | 갈래① 3대 이상이 확장 구성이라는 업무규칙 ② 2대 이상이어야 하는데 부등호 실수 | 영향: 실내기 3대 경계에서 추천 실외기가 달라진다
[A357] estimate-app/index.ejs:8474-8478 `want.set(pump, sum)`(0도 기록) vs order-app:5766-5773 `if (sum > 0) want.set(pump, sum)` | 드레인펌프 수량 0 처리 | 갈래① 견적은 항상 0 으로 리셋하는 것이 맞고 주문은 사용자 입력 보존이 맞다 ② 한쪽이 실수 | 영향: 실내기를 지웠을 때 펌프가 견적에서는 0 이 되고 주문에서는 남는다
[A358] order-app/index.html:5572(setHomeDerivedQty_ 는 폴백 코드로도 수량 기록) vs order-app:5588(setSingleDerivedQty_ 는 실제 모델 있을 때만 기록) | 카탈로그 누락 시 처리 | 갈래① 홈은 코드가 확정적이라 폴백이 안전하고 싱글은 id 라 위험하다 ② 비대칭이 실수 | 영향: 카탈로그에 없는 홈 품목이 수량을 갖고 전송될 수 있다
[A359] estimate-app/index.ejs:8505 `if (branchCnt > 0) want.set('AXJ-TA3419M', ...)` | 세트 실외기에서 나온 분기관을 항상 AXJ-TA3419M 한 종류로만 가산한다 | 갈래① 세트 내부 분기관은 규격이 고정 ② 용량에 따라 달라야 하는데 단순화한 것(같은 파일 안에 용량별 분기관 코드 6종 사다리가 따로 있다) | 영향: 세트 실외기 구성의 분기관 규격. 업무 의미 확인 필요


########## kind-할인.json
[A360] sumOld() 0.5 하드코딩 | estimate-app/views/index.ejs:7577 `finalP = Math.round(listP * 0.5)` (legacy 종합견적서 index.html:7144 동일) | 갈래A: 구형 합계는 원래 항상 50% 기준이 맞다(의도된 고정) / 갈래B: #old_rate·CONFIG.oldDiscount 를 반영해야 하는데 승계 누락 | 영향: old_rate 를 50 이외로 두면 행 소계 합(getRealOldPrice·roundByConfig 반영)과 하단 '구형 합계'가 어긋난다. 게다가 sumOld 는 roundByConfig('old') 도 안 탄다. 어느 쪽이 정본인지 코드만으로 확정 불가
[A361] buildSendRows 구형 적요 퍼센트 출처 | estimate-app/views/index.ejs:9609 `const dcText = '(' + getOldDiscountPercent() + '% DC)'` | 갈래A: 적요는 '계약상 기준 할인율'(CONFIG)이라 사용자가 화면에서 바꾼 값과 달라도 맞다 / 갈래B: 화면에서 계산에 쓴 #old_rate 를 적어야 한다 | 영향: 사용자가 old_rate 를 40 으로 바꾸면 단가는 40% 로 계산되는데 적요에는 '(50% DC)' 가 찍힌다
[A362] 정액 할인 6종의 단위 — 금액인가 비율인가 | order-app/index.html:2812 / 거래처 발송 주문서 index.html:2542 `const calc = (val, rateAmt) => rateAmt < 1 ? Math.round(val*(1-rateAmt)) : Math.max(0, val - rateAmt)` vs estimate-app:4442 `const calc = (val, rateAmt) => Math.max(0, val - rateAmt)` | 갈래A: 주문 계열의 이중 해석이 의도된 편의 기능(0.1 을 넣으면 10%) / 갈래B: 정액 전용이 정본이고 이중 해석은 사고 방지용 방어 코드가 굳은 것 | 영향: 같은 dc_configs 값이라도 주문앱과 견적앱의 세트 단가가 달라진다. discount360=0.5 면 견적앱은 '0.5원 차감', 주문앱은 '50% 할인'. DB 로 옮길 때 컬럼 단위를 하나로 정하지 않으면 조용히 틀어진다
[A363] 상업멀티 고정DC 단독 적용 여부 | order-app/index.html:2858 `else if (fixedDc != null && currentListPrice > 0)` vs estimate-app:4488 (해당 분기 없음) vs 거래처 발송 주문서 index.html:2594 `else if (fixedDc != null && fixedDc > 0 && ...)` | 갈래A: 변동DC 체크와 무관하게 고정DC 는 항상 적용되어야 한다(주문 계열) / 갈래B: 고정DC 는 변동DC 가 켜진 행에서만 전역율을 대체하는 값이다(견적 계열) | 영향: 세 소스가 서로 다른 답을 낸다. 특히 현행 order-app 은 `fixedDc > 0` 가 빠져 고정DC=0(또는 '0%') 인 행에서 납품가 대신 출고가가 그대로 단가가 된다
[A364] I형 유연호스 고정단가 7000 vs 8000 | 종합견적서 index.html:3934/3977/4001/4045 = 7000 · estimate-app:4350/4393/4417/4461 = 8000 · 주문 계열 전부 8000 | 갈래A: 8000 이 최신 단가이고 legacy 견적서만 갱신이 안 된 것 / 갈래B: 견적과 주문의 계약 단가가 원래 다르다 | 영향: 같은 품목의 단가가 앱마다 1,000원 다르다. 어느 쪽이 정본인지 확인 필요
[A365] 상업 받침대·방진가대 DC 제외 게이트 | 거래처 발송 주문서 index.html:2571 `if (/방진가대|받침대|발통세트|si-al600a|si-al700a/i.test(baseName) && sheetPrice > 0) return roundByConfig(sheetPrice);` · order-app/index.html commUnitPrice(2825-2867) 에는 없음 | 갈래A: legacy 규칙이 유효한데 현행 주문앱이 승계를 빠뜨렸다 / 갈래B: 의도적으로 제거했다 | 영향: 받침대류에 변동DC 나 고정DC 가 걸린 행이 있으면 legacy 는 납품가 고정, 현행은 출고가×(1-율). 실 데이터에서 해당 품목의 useK2/고정DC 분포를 세야 판정 가능
[A366] getTierBonusRate 등급 보너스의 성격 | estimate-app:13661 / order-app:8093 `sum>=1억→0.04, 5천만→0.03, 3천만→0.02, 1천만→0.01` | 갈래A: 계약상 볼륨 리베이트라 DB(거래처별 또는 전역 구간표)로 옮겨야 한다 / 갈래B: 영업 정책이라 코드 상수로 두는 게 맞다 | 영향: 4개 임계액과 4개 율이 두 앱에 중복 하드코딩. 주문앱만 0.48 상한이 있어 억 단위 주문에서 견적 49% ↔ 주문 48% 로 갈린다
[A367] 45% 일 때만 보너스/페널티가 작동하는 게이트 | estimate-app:13670 / order-app:8102 `Math.abs(rate - 0.45) < 0.001` | 갈래A: '표준 할인율 거래처' 에게만 주는 혜택이라 맞다 / 갈래B: 45 가 그냥 옛 기본값이라 거래처별 DC 시대에는 조건이 무의미하다 | 영향: dc_configs 에 48% 로 등록된 거래처는 실내기 단독 페널티도, 볼륨 보너스도 전혀 받지 않는다. DB 화하면 이 조건이 사실상 죽는다
[A368] 실내기 단독/메인장비 부재 판정 기준 차이 | estimate-app:13633 isIndoorOnly(실외기만 카운트) vs order-app:8054 isNoMainUnit(전열교환기 제외 + 실외기·실내기·벽걸이를 메인으로) | 갈래A: 견적과 주문의 업무 규칙이 실제로 다르다 / 갈래B: 한쪽이 나중에 고쳐졌고 다른 쪽이 안 따라갔다 | 영향: 실내기만 담은 견적은 견적앱에서 45→40 페널티를 맞지만, 주문앱에서는 실내기가 '메인' 이라 페널티가 없다
[A369] 시트 '할인' / '1WAY할인' / '싱글 할인' 컬럼의 용도 | 종합견적서 Code.js:1403-1404, 1449 · 거래처 발송 주문서 Code.js:1652-1653, 1679 | 갈래A: UI 정액 6종과 별개로 품목 자체에 붙는 할인이다 / 갈래B: 폐기된 옛 컬럼이고 지금은 안 쓴다 | 영향: 이 종류축 훑기에서 프런트 소비처를 찾지 못했다. DB 이관 시 컬럼을 살릴지 버릴지 판단 필요
[A370] estimate_config 의 singleDiscount / singleOneWayDiscount 2키 | lib/code.js:165-166, 435-436 | 갈래A: UI 정액 6종 중 '싱글 일반'과 '1way' 에 해당한다 / 갈래B: 이름만 남은 미사용 키다 | 영향: 이 2키는 buildDefaultDcConfig_/bootstrap t.config 어디에서도 discount360/oneWayDiscount 로 매핑되지 않는다. 즉 DB 에 값을 넣어도 프런트에 도달하지 않는다
[A371] CONFIG.singleSetDiscount | estimate-app:2349 · order-app:1460 · 종합견적서 index.html:2187 · 거래처 발송 주문서 index.html:1347 | 갈래A: 예약 키 / 갈래B: 사장된 키 | 영향: 4개 소스 모두 선언만 하고 읽는 곳이 없다(grep 0). DB 이관 대상에서 뺄지 확인 필요
[A372] renderHomeOptions/renderCommOptions 의 truthy 폴백 | estimate-app:6635, 7800 `(window.DISCOUNT_RATE_COMM ? Math.round(...*100) : 45)` | 갈래A: 0% 할인 거래처가 존재하지 않으니 무해 / 갈래B: 0% 가 45% 로 뒤집히는 결함 | 영향: dc_configs 에 0 을 저장하는 순간 화면에는 45% 가 뜬다. GAS initDcConfigFromNotion 도 `!== 0` 가드로 0 을 무시하므로 '0% 할인' 을 표현할 방법이 현재 체계에 없다
[A373] reset* 함수의 45 리터럴 | estimate-app:10053, 10138 (legacy 9742, 9827 동일) | 갈래A: 초기화는 시스템 기본값으로 돌아가는 게 맞다 / 갈래B: 거래처 DC 로 돌아가야 한다 | 영향: 거래처 48% 를 적용한 뒤 '초기화' 를 누르면 45% 가 되고, 사용자는 그걸 모른 채 전표를 만들 수 있다
[A374] formatWonDiscountLabel_ 의 천 단위 처리 | GAS Code.js:1688 `Math.round((abs%10000)/1000)` vs 프런트 fmtMoney estimate-app:15699 `Math.floor((n%10000)/1000)` | 갈래A: 둘 중 하나가 오타 / 갈래B: 서버/프런트가 다른 문서를 만들어 무관 | 영향: 15,600원이면 서버 '-1만6천', 프런트 '-1만5천'. 같은 전표의 REMARKS 와 ADD_TXT_06_T 가 다르게 표기될 수 있다
