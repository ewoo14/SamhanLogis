# #1055 라이브 QA 보고서 (2026-08-04)

## 환경 확인

- 작업 디렉터리: `C:/dev/Samhan-Public/.claude/worktrees/t1055`
- 브랜치: `fix/1055-zero-stock-warehouse-visibility`
- HEAD: `11d51ae3e4dc3b330f7cb9d86be5915558baaaa6`
- 프런트 포트/config: `18501`, `clients/desktop/vite.renderer.dev.config.ts`로 실행 예정
- API 경로/컨테이너: `GET /inventory/warehouses`, `GET /inventory/balances` → 게이트웨이 `samhan-api-gateway:8080` 경유 → `samhan-inventory-service:8085`; mock OFF 예정
- 컨테이너 배포본 나이: `samhan-inventory-service` 생성 `2026-08-01T16:33:24.17043812Z`; `samhan-api-gateway` 생성 `2026-07-31T15:15:50.070347996Z`
- `inventory_db.flyway_schema_history` 최고 버전: `23`
- 발화 조건 SQL 실측: `HQ-001 본사창고 HEADQUARTERS 15`, `CS-001 거래처 위탁창고 CONSIGNMENT 0`, `VH-001 1호차 차량재고 VEHICLE 0`, `VR-001 가상창고 VIRTUAL 0`

## 시나리오

<!-- 시나리오 종료마다 아래에 결과를 append 한다. -->

### 시나리오 1 — 재고 현황 화면의 창고 필터 전체 목록

- 클릭 경로: 실 로그인(`dev_master`) → `창고 운영` 펼치기 → `재고 현황` → 창고 필터 열기
- 기대: 발화 조건의 활성 창고 4개(`HQ-001`, `CS-001`, `VH-001`, `VR-001`)가 모두 필터에 표시
- 실제: 필터에 `전체 창고`, `본사창고`, `1호차 차량재고`, `거래처 위탁창고`, `가상창고`가 모두 표시됨. 실 네트워크에서 `GET http://127.0.0.1:8080/inventory/warehouses` 호출 확인. mock 호출은 확인되지 않음.
- 캡처: `docs/qa/1055-real-qa/03-warehouse-menu-expanded.png`, `docs/qa/1055-real-qa/04-stock-balance-page.png`, `docs/qa/1055-real-qa/05-warehouse-filter-open.png`
- 판정: **PASS** — 0재고 창고가 재고 현황의 창고 필터에서 사라지는 현상은 재현되지 않음.

### 시나리오 2 — 창고별 필터 조회(0재고 창고 포함)

- 클릭 경로: 재고 현황 창고 필터에서 `본사창고` → `1호차 차량재고` → `거래처 위탁창고` → `가상창고`를 각각 선택하고 매번 `조회` 클릭
- 기대: 각 창고를 선택할 수 있고, 선택한 창고 기준 결과가 표시되어야 함. 0재고 창고도 선택지에서 유지되어야 함.
- 실제: `본사창고`와 `1호차 차량재고`는 각각 실제 재고 행 50개가 표시됨. `거래처 위탁창고`와 `가상창고`는 선택값이 유지된 상태에서 `조회 결과가 없습니다.`가 표시됨. 두 0재고 창고가 필터에서 제거되거나 다른 창고로 치환되지는 않음. 실 네트워크에서 `GET /inventory/balances?page=0&size=50` 호출 확인.
- 캡처: `docs/qa/1055-real-qa/07-filter-1.png`, `07-filter-2.png`, `07-filter-3.png`, `07-filter-4.png`, `08-all-filter-restored.png`
- 판정: **PASS (필터 가시성 기준)** — 0재고 창고의 빈 결과는 현재 화면의 재고 행 모델상 자연스러운 빈 상태로 관찰됨. 0재고 창고를 별도 0행 placeholder로 표에 보여야 하는 요구까지는 이 라운드의 발화 조건만으로 미판정.

### 시나리오 3 — 조회 전/후 화면 및 실 API 응답 연결

- 클릭 경로: `재고 현황` 진입 → 전체 창고 상태에서 `조회` 클릭
- 기대: 조회 전 안내가 조회 후 실제 재고 행으로 바뀌고, 게이트웨이 경유 실 inventory API가 응답
- 실제: 조회 전 `"조회" 버튼을 눌러 재고 현황을 불러오세요.`가 표시되었고, 조회 후 HQ-001/VH-001 행과 수량이 표시됨. 요청 기록은 `GET /inventory/warehouses`, `GET /inventory/balances?page=0&size=50`이며 게이트웨이 `:8080` 경유. 단, 로그인 초기 공통 요청에서 `/auth/me` 401 두 건과 `/app/version`, `/app/notices/active`, `/logs/front` 503이 관찰되었으나 대상 재고 API는 200으로 화면이 정상 렌더링됨.
- 캡처: `docs/qa/1055-real-qa/06-stock-balance-queried.png`
- 판정: **PASS** — 대상 기능은 실 API로 조회·렌더링됨. 공통 401/503은 이번 결함의 대상 API가 아니며 별도 조사하지 않음.

## 이 라운드가 보지 않은 것

- 창고 신규 등록/수정/삭제, soft-delete 상태, DB 쓰기 경로
- `dev_master` 이외 역할의 권한별 재고 현황 접근
- 첫 페이지(50행) 이후 페이지네이션 및 export
- 재고 이동·입출고 내역·안전재고 등 인접 화면의 창고 필터
- 0재고 창고를 표에 placeholder 행으로 표시해야 하는 별도 제품 요구 여부
- 공통 `/auth/me` 401 및 `/app/*`, `/logs/front` 503의 원인

## 신규 파일

- `docs/dev-reports/2026-08-04-1055-live-qa.md`
- `docs/qa/1055-real-qa/*.png` (실 GUI 캡처 12장; 셀렉터 디버깅 캡처 1장 포함)
