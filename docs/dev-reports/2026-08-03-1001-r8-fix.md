# #1001 R8 수정 보고서

## D2 RED

- [DEV-SEED/단위 fixture] 같은 날짜의 `2026/08/01-10` 매출과 `2026/08/01-2` 입금을 역순 입력했다. 현재 adapter는 입력 순서를 유지해 문자열 순서와 같은 결과를 냈고, 숫자 업무 순서 기대에 실패했다.

```text
src/renderer/api/partnerLedgerApi.test.ts (2 tests | 1 failed)
expected [ '2026/08/01-10', '2026/08/01-2' ] to deeply equal [ '2026/08/01-2', '2026/08/01-10' ]
Tests 1 failed | 1 passed (2)

## D3 수정 및 GREEN

- [DEV-SEED/단위 fixture] 합계·기말잔액 두 행을 HTML `<tfoot>`에서 단일 summary `<tbody data-testid="partner-ledger-print-summary">`로 옮겼다. 따라서 한 장 문서와 80-line multi-page-sized 문서 모두 summary 구조가 한 번만 존재하고, 인쇄 엔진의 `table-footer-group` 페이지 반복 대상이 아니다. 행 자체를 제거하지 않아 마지막 페이지에 계속 배치된다.
- [DEV-SEED/단위 fixture] 1-line 단일 페이지 fixture와 80-line multi-page-sized fixture를 같은 테스트 파일에서 모두 GREEN 확인했다.

```text
✓ src/renderer/print/PartnerLedgerView.test.tsx (2 tests)
Test Files 1 passed (1)
Tests 2 passed (2)

## D4 RED (재현 보정)

- [DEV-SEED/단위 fixture] 음수 running balance와 closing balance가 둘 다 `-100`이라 첫 RED assertion은 selector 중복으로 중단됐다. 출력 원문:

```text
TestingLibraryElementError: Found multiple elements with the text: -100
Test Files 1 failed (1)
Tests 1 failed | 2 passed (3)
```

- [DEV-SEED/단위 fixture] 동일 음수 셀 전체를 검사하도록 assertion을 보정한 뒤, production 인쇄 balance 셀에 색상 분기가 없다는 본 RED를 다시 확인한다.

- [DEV-SEED/단위 fixture] 두 번째 RED는 test 환경에 `toHaveStyle` matcher가 등록되지 않아 assertion helper 오류로 끝났다. 원문:

```text
Error: Invalid Chai property: toHaveStyle
Tests 1 failed | 2 passed (3)
```

- [DEV-SEED/단위 fixture] DOM inline style을 직접 확인하는 assertion으로 다시 보정한다.

- [DEV-SEED/단위 fixture] 보정된 D4 RED 원문은 인쇄 누적잔액 셀의 inline color가 비어 있음을 확인했다.

```text
AssertionError: expected '' to be 'rgb(220, 38, 38)'
Tests 1 failed | 2 passed (3)

## D4 수정 및 GREEN

- [DEV-SEED/단위 fixture] 인쇄의 기초잔액·각 행 누적잔액·기말잔액 셀에만 음수 조건 `#DC2626`을 적용했다. 양수·0에는 inline 색을 넣지 않으며 `formatBalance`의 `-X`/`—` 표기를 유지한다. 합계의 0 balance는 색상 대상이 아니다.
- [DEV-SEED/단위 fixture] 음수 running/closing balance와 0 표시를 포함한 인쇄 targeted GREEN 원문:

```text
✓ src/renderer/print/PartnerLedgerView.test.tsx (3 tests)
Test Files 1 passed (1)
Tests 3 passed (3)

## 데스크톱 typecheck GREEN

- [DEV-SEED/단위 fixture] `clients/desktop`에서 요청된 `npm run typecheck`를 실제 실행했다. TypeScript 두 project와 real-QA typecheck가 종료 코드 0이었다.

```text
> @samhan/desktop@0.1.0 typecheck
> node scripts/real-qa-scope.cjs --phase=typecheck && tsc -p tsconfig.node.json --noEmit && tsc -p tsconfig.web.json --noEmit && npm run typecheck:real-qa

✔ real-QA 공식 수집 집합은 현재 Git 추적 집합과 이름 단위로 일치한다
ℹ tests 50
ℹ pass 50
ℹ fail 0
```

## 최종 범위 및 신규 파일

- [DEV-SEED/단위 fixture] D2는 adapter 정렬과 누적잔액만, D3는 인쇄 summary 반복 구조만, D4는 인쇄 잔액 색상만 수정했다. VAT·typecheck를 제외한 다른 이월/범위 밖 표면은 변경하지 않았다.
- [DEV-SEED/단위 fixture] `git diff --check`는 출력 없이 종료 코드 0이었다. accounting-service 전체 suite는 실행하지 않았다.
- 신규 파일: `docs/dev-reports/2026-08-03-1001-r8-fix.md` (현재 104줄).
- 변경 파일별 `git diff --numstat`: `clients/desktop/src/renderer/api/partnerLedgerApi.test.ts +17/-0`, `partnerLedgerApi.ts +31/-1`, `PartnerLedgerView.module.css +1/-1`, `PartnerLedgerView.test.tsx +48/-0`, `PartnerLedgerView.tsx +5/-5`.
```
```
```

## D2 수정 및 GREEN

- [DEV-SEED/단위 fixture] 정렬 기준은 `date` 오름차순 → 문서번호 끝의 숫자 시퀀스 오름차순 → 동일 문서번호의 `SALE` 우선 → 원본 입력 index 순으로 고정했다. 같은 날짜·같은 번호·같은 유형까지 같은 경우 원본 index를 최종 tie-breaker로 사용한다.
- [DEV-SEED/단위 fixture] `buildPartnerLedgerLines`가 이 순서로 문서를 먼저 정렬한 뒤 누적잔액을 계산하도록 수정했다. 행 집합과 금액은 정렬 전후 동일하게 2행, 차변 100, 대변 40이며, 수정 후 잔액은 `-40`, `60`이다.
- [DEV-SEED/단위 fixture] D2 targeted GREEN 원문:

```text
✓ src/renderer/api/partnerLedgerApi.test.ts (2 tests)
Test Files 1 passed (1)
Tests 2 passed (2)

## D3 RED

- [DEV-SEED/단위 fixture] 80개 line의 multi-page-sized 원장을 렌더링해 합계/기말잔액 summary가 한 번인 구조를 먼저 요구했다. 현재 구현은 `<tfoot>`를 사용해 인쇄 엔진의 페이지별 footer 반복 경로를 막지 못하고, summary 단일 구조 계약도 없다.

```text
PartnerLedgerView > keeps print summary one time for a multi-page-sized ledger FAILED
Unable to find an element by: [data-testid="partner-ledger-print-summary"]
Test Files 1 failed (1)
Tests 1 failed | 1 passed (2)
```
```
```
