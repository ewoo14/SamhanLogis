# 이슈 #1055 / PR #1067 라이브 QA R2

- 실행일: 2026-08-04
- 실행자: Codex 라이브 QA
- 범위: PR 코드가 실제 실행 중인 백엔드와 GUI 조회 결과 표에서 0재고 창고를 어떻게 표현하는지 검증

## 1. 환경 확인

- 워크트리: `C:\dev\Samhan-Public\.claude\worktrees\t1055`
- `git rev-parse --show-toplevel`: `C:/dev/Samhan-Public/.claude/worktrees/t1055`
- 브랜치: `fix/1055-zero-stock-warehouse-visibility`
- HEAD: `0dfe23de2c9c7e349ddef1f3dfe0856656aacbc9`
- renderer: `18501`, 실행 config: `vite.renderer.dev.config.ts`
- 실행 방식: `clients/desktop`에서 Vite renderer를 `VITE_API_BASE_URL=http://127.0.0.1:8080`, `VITE_APP_VERSION=2026/08/04-1055`로 실행. `VITE_MOCK_MODE` 미설정(mock OFF). renderer `GET http://127.0.0.1:18501/`는 HTTP 200.
- 호출 API: `GET /inventory/balances`, `GET /inventory/warehouses`; 게이트웨이 `samhan-api-gateway:8080`이 inventory-service로 전달.
- 컨테이너 생성 시각:
  - `samhan-inventory-service`: `2026-08-04T10:39:09.502630221Z` — PM 재배포 기준 `10:39:09Z` 이후, 측정 진행.
  - `samhan-api-gateway`: `2026-07-31T15:15:50.070347996Z`.
- `inventory_db.flyway_schema_history` 최고 버전: `23`.

### 발화 조건 SQL 원문

```text
      code      |         name          |     type     | stock_rows
----------------+-----------------------+--------------+------------
 HQ-001         | 본사창고              | HEADQUARTERS |         15
 QA-1039-CHOWOL | 초월창고 QA-1039-초월 | HEADQUARTERS |          0
 CS-001         | 거래처 위탁창고       | CONSIGNMENT  |          0
 QA-1039-SANGIL | 상일창고 QA-1039-상일 | HEADQUARTERS |          0
 VH-001         | 1호차 차량재고        | VEHICLE      |          0
 VR-001         | 가상창고              | VIRTUAL      |          0
(6 rows)
```

### PR 계약(측정 기준)

PR의 `StockService`/`StockBalanceResponse` 변경 계약은 다음과 같이 해석했다.

> 활성 `VIRTUAL` 창고는 실제 잔액 행이 없어도 잔액이 존재하는 각 품목마다 표시 전용 행을 합성한다. 합성 응답은 `availableQty=0`, `reservedQty=0`, `totalQty=0`, `version=null`이며, GUI는 세 수량을 `—`로 표시한다. 일반 `HEADQUARTERS`/`CONSIGNMENT`/`VEHICLE` 0재고 창고는 조회 결과에 합성하지 않는다.

### 실 백엔드 응답 증거

실 계정 `dev_manager`로 게이트웨이에 로그인한 뒤 mock 없이 호출했다.

```text
POST http://localhost:8080/auth/login                         200
GET  http://localhost:8080/inventory/balances?page=0&size=100 200
GET  http://localhost:8080/inventory/warehouses               200

GET /inventory/balances 응답 일부:
{"productCode":"AR05TXEAAWKNEU-11","warehouseCode":"VR-001",
 "warehouseType":"VIRTUAL","availableQty":0,"reservedQty":0,
 "totalQty":0,"version":null}

전체 응답 메타: totalElements=302, totalPages=4
GET /inventory/warehouses 응답: HQ-001, VH-001, CS-001, VR-001,
QA-1039-SANGIL, QA-1039-CHOWOL (6개)
```

## 2. 시나리오 1 — 전체 조회 결과에 VIRTUAL 0재고 행 표시

- 클릭 경로: 로그인 → `창고 운영` → `재고 현황` → 창고 `전체 창고` 유지 → `조회` 클릭.
- 기대(위 PR 계약): 품목별로 `VR-001 / 가상창고 / VIRTUAL` 표시 전용 행이 나오고 세 수량은 `—`; 일반 0재고 창고 `CS-001`, `QA-1039-SANGIL`, `QA-1039-CHOWOL`은 표에 나오지 않음.
- 실제: 화면 하단 `총 302건`, 첫 페이지에 품목별 `VR-001 / 가상창고 / 가상` 행과 `— / — / —`가 표시됨. `CS-001` 및 `QA-1039-*`는 표에 없음. 실 API 응답의 `version:null`도 확인됨.
- 캡처: [scenario-1-all-balances-real-qa.png](2026-08-04-1055-live-qa-r2/screenshots/scenario-1-all-balances-real-qa.png)
- 판정: **PASS**

## 3. 시나리오 2 — VIRTUAL 필터의 합성 행과 GUI `—` 표시

- 클릭 경로: 창고 선택 `<select>` → `가상창고` 선택 → `조회` 클릭.
- 기대(위 PR 계약): 결과는 VIRTUAL 행만이며 수량 세 칸 모두 `—`로 표시.
- 실제: 화면 `총 101건`, 첫 페이지의 모든 행이 `VR-001 / 가상창고 / 가상`; 첫 페이지에서 `VR-001` 50행, `—` 150개(세 수량×50) 확인. `HQ-001`, `VH-001`, `CS-001` 없음.
- 캡처: [scenario-2-virtual-filter-real-qa.png](2026-08-04-1055-live-qa-r2/screenshots/scenario-2-virtual-filter-real-qa.png)
- 판정: **PASS**

## 4. 시나리오 3 — 일반 0재고 창고는 표에 합성하지 않음

- 클릭 경로: 창고 선택 `<select>` → `거래처 위탁창고 (CS-001)` 선택 → `조회` 클릭.
- 기대(위 PR 계약): 일반 `CONSIGNMENT` 0재고 창고는 표시 전용 행을 만들지 않으므로 조회 결과가 비어 있음.
- 실제: 표에 `조회 결과가 없습니다.`가 표시되고 행/요약 카운트가 없음. `VR-001` 등 다른 창고 행도 없음.
- 캡처: [scenario-3-zero-consignment-real-qa.png](2026-08-04-1055-live-qa-r2/screenshots/scenario-3-zero-consignment-real-qa.png)
- 판정: **PASS**

## 5. 최종 판정

**PASS — PR #1067의 조회 결과 계약을 실 재배포 백엔드와 실 GUI에서 확인했다.**

- PR 이후 재배포된 `samhan-inventory-service`에서 VIRTUAL 합성 응답과 `version:null` 확인.
- 실제 renderer에서 VIRTUAL 행이 나타나고 세 수량이 `—`로 표시됨.
- 일반 0재고 창고는 표에 임의 합성되지 않음.
- `QA-1039-SANGIL`, `QA-1039-CHOWOL`은 삭제·수정하지 않음.

## 6. 이 라운드가 보지 않은 것

- R1에서 이미 PASS한 창고 필터 목록 가시성은 재측정하지 않음. 이번 라운드는 조회 결과 표의 0재고 계약만 측정.
- VIRTUAL 이외 일반 창고에 향후 0재고 표기 정책을 추가하는 변경은 검증 범위가 아님.
- 페이지 2 이후의 모든 개별 품목 행을 GUI로 전수 클릭 검증하지는 않음. 단, API 전체 메타와 첫 페이지의 합성 패턴 및 VIRTUAL 필터 결과로 계약을 검증함.
- 생성/수정/삭제, Excel export, 실시간 이벤트, 권한별 차이는 검증하지 않음.

## 7. 산출물 및 변경 제한

- 신규 보고서: `docs/dev-reports/2026-08-04-1055-live-qa-r2.md`
- 신규 캡처 3장: `docs/dev-reports/2026-08-04-1055-live-qa-r2/screenshots/`
- 코드 수정, DB 직접 쓰기, 컨테이너 중지/재빌드, `git add`/`commit`/`push` 없음.
