# PR #1145 R9 SOL 5.6 적대검증 — 도달성 재수렴

검증 대상: `C:\dev\Samhan-Public\.claude\worktrees\t1144` · HEAD `c1f884fada655ef531f6cc0f455a4d22bcaf5442`  
검증일: 2026-08-09 KST  
금지 준수: commit/push 0회 · 다른 워크트리/main 접근 0회 · 실 DB 쓰기 0회 · V99 변경 0

## 최종 판정

```text
도달 가능한 결함: 1건

1. 실 사용자 경로: auth_db 권한이 체크인 projection 이후 변경됐는데 desktop mock 계약만 통과한 빌드가 배포되면,
   실제 역할 사용자는 mock QA와 달리 화면 진입 403 또는 과다 허용을 만난다.
   재현 절차: auth_db가 없는 컨테이너 이름으로 갱신 스크립트를 실행하면 exit 1이지만,
   갱신을 호출하지 않는 CI의 npm test는 체크인 projection만 읽어 DB 없이 green이다.
   원문 근거: scripts/refresh-accounting-permission-db-snapshot.ps1:15-30은 수동 갱신만 fail하고,
   .github/workflows/ci.yml:779-806 및 qa-e2e.yml:92-114에는 갱신 호출이 없다.
   PR head 원격 Frontend Desktop은 pass했다. 따라서 "DB를 읽지 못하면 조용히 통과하지 않는다"는
   갱신 도구에는 참이지만 PR hard gate 전체에는 거짓이며, stale projection을 CI가 검출하지 못한다.

머지 불가
```

별도 차단: 요구된 라이브 GUI QA는 앱 HTTP 기동까지 성공했지만 Browser 런타임이 `[]`여서 클릭·화면 캡처가 **관측 불가**다. 이를 결함 0으로 환산하지 않았다. 또한 종료 시점 원격 `Desktop Playwright (mock 회귀 hard gate)`도 pending이다.

## 1. 기준 GREEN과 양방향 가드

기준 실행:

```text
npx vitest run src/renderer/test-utils/accounting-slip-permission-contract.test.ts --reporter=verbose
Test Files 1 passed (1)
Tests 8 passed (8)
```

요청한 두 뮤테이션은 모두 원복 후 source diff 0을 확인했다.

| 뮤테이션 | 직접 관측 | 판정 |
|---|---|---|
| `PERMISSION_BITS_BY_ROLE.MASTER`에만 `r9.sol.sentinel` 추가 | `mock ↔ snapshot page-code catalog`, 122 대 123, exit 1 | RED |
| mock 역할 정규식에만 `AUDITOR` 추가 | `mock ↔ snapshot role catalog`, `AUDITOR` 초과, exit 1 | RED |

구현 근거: `permission-contract-checker.ts:15-21`이 mock 역할 정규식을 추출하고, `:34-35`가 page/role 양쪽 집합을 exact 비교한다. `:38-40`은 비트 묶음 전체에서 snapshot page catalog를 역산하므로 R8의 단방향 누락이 닫혔다.

## 2. 354셀 동결 exact 뮤테이션

각 뮤테이션은 한 변수만 바꾸고 같은 R8 계약 테스트로 재현한 뒤 원복했다.

| 요구 시나리오 | 대상/관측 원문 | 판정 |
|---|---|---|
| 차이 1 증가 | `MASTER × accounting.sales-slip.accounting` view `1→0`; `0111000` 대 `1111000`, exit 1 | RED |
| 차이 1 감소 | `MASTER × accounting.tax-invoice.list` download를 DB에 맞춰 `1111011→1111001`; 테스트는 동결값 `1111011`을 요구, exit 1 | RED |
| 동결 목록 1항목 삭제 | `MASTER × partners.delete` 삭제; 실제 `1001100` 대 DB `1111100`, exit 1 | RED |
| 실제 차이가 아닌 항목 1개 추가 | `MASTER × accounting.sales-slip.accounting` 동일값 항목 추가; 역할별 actual/frozen set 110 대 111, exit 1 | RED |

특히 “줄어도 실패”는 실제 1비트 차이를 소진한 뮤테이션으로 밟았다. `permission-contract-checker.ts:108-119`의 셀 exact와 `:121-135`의 역할별·전체 divergence 배열 exact가 모두 살아 있다. 동결 목록은 비교 우회가 아니라 허용된 기대값이며, 실제 차이 집합을 다시 산출해 목록 전체와 대조한다.

초기 탐색 중 `mockActionMatrixFromRole`의 비도달 분기를 바꾼 두 시도는 실제 account endpoint 셀을 바꾸지 않아 green이었다. 이는 동결 판정에서 제외했고, `_mockPermissionCells` 및 account endpoint의 도달 분기로 다시 수행해 위 RED를 확정했다.

## 3. DB 부재와 CI 경계

존재하지 않는 컨테이너로 실행:

```text
powershell -File scripts/refresh-accounting-permission-db-snapshot.ps1 -Container r9-sol-no-auth-db
SCRIPT_EXIT=1
HASH_UNCHANGED=True
docker: error: no such object: r9-sol-no-auth-db
```

갱신 도구 자체는 출력 파일을 쓰지 않고 중단한다. 다만 `$ErrorActionPreference='Stop'` 때문에 `docker inspect`의 native error가 `:16-17`의 맞춤 메시지보다 먼저 종료시킨다. 종료 1과 무쓰기 계약은 유지된다.

CI는 design-system 빌드 후 `npm test`만 실행한다(`ci.yml:779-806`). QA E2E도 Playwright와 silent-skip 가드만 실행한다(`qa-e2e.yml:92-114`). 저장소 전체 workflow 검색에서 `refresh-accounting-permission-db-snapshot` 호출은 0건이다. 따라서 CI의 DB 부재는 red가 아니라 체크인 스냅샷 기반 green이며, 현재 projection의 생성 신선도는 CI가 증명하지 않는다.

## 4. 354 실측

실 `samhan-postgres/auth_db`에 SELECT만 수행했다. 비트 순서는 view/create/update/delete/restore/download/print다.

SQL 집계:

```text
scope_cells=1342 (11 × 122)
present_rows=1095
missing_rows=247
DB row query exit=0
checked-in DB projection × current auth_db mismatch=0
```

체크인 동결 목록을 현재 DB 행 존재 여부로 다시 분류:

```text
DIVERGENCES_TOTAL=354
PRESENT_ROW_DIVERGENCES=340
ABSENT_ROW_DIVERGENCES=14
```

따라서 R8의 `340 + 14`와 R9의 `354`는 같은 집합이며, 이번 실측 기준 정답은 **354**다.

## 5. 회계 exact 및 R8 전수 검사 생존

- 회계 화면/route exact: 기준 테스트 8/8 green.
- 회계 셀 변경: `MASTER × accounting.sales-slip.accounting` 뮤테이션이 기존 RED-A와 R8 전수 검사 두 곳에서 동시에 red.
- 11역할 × 122 page × 7 action 전수: `permission-contract-checker.ts:95-119` 순회 유지.
- 동결 목록 우회 없음: 셀 기대값 적용 뒤에도 `:121-135`에서 실제 DB↔mock 차이를 독립 재산출하고 역할별/전체 배열을 exact 비교.

## 6. 라이브 QA

실행 전 환경:

```text
worktree=C:\dev\Samhan-Public\.claude\worktrees\t1144
HEAD=c1f884fada655ef531f6cc0f455a4d22bcaf5442
port=127.0.0.1:15176
mode=VITE_MOCK_MODE=1 (실 API 아님)
gateway=127.0.0.1:8080, auth-service=127.0.0.1:8081, postgres=127.0.0.1:5432 (기존 실 스택)
```

기동 관측:

```text
VITE v5.4.21 ready in 438 ms
GET http://127.0.0.1:15176/ -> HTTP 200, 1283 bytes
Browser runtime selection -> No browser is available
bootstrap 진단 후 agent.browsers.list() -> []
```

따라서 GUI 클릭, 사용자 화면 권한 진입, 스크린샷은 **관측 불가**다. 독립 Playwright로 대체하지 않았다. QA 서버는 종료했고 15176 listen=false를 확인했다. 캡처 디렉터리에는 기동 로그만 있으며 PNG는 없다.

## 7. V99 및 원격 상태

V99 working-tree diff는 0이고 blob은 HEAD와 모두 `bab045b30d26ae77de7652c677dec61ebfe87232`다.

원격 PR head는 로컬과 같은 `c1f884fada655ef531f6cc0f455a4d22bcaf5442`다. 종료 시 `Frontend Desktop (typecheck + lint + build)` 포함 대부분 check는 pass였으나 `Desktop Playwright (mock 회귀 hard gate)`는 pending이었다. 원격 CI 완결도 아직 머지 근거가 아니다.

## 신규 파일 목록

- `docs/dev-reports/2026-08-09-1145-r9-sol-reconv.md`
- `docs/qa/1145-r9-real-qa/vite.stdout.log`
- `docs/qa/1145-r9-real-qa/vite.stderr.log`

스크린샷 신규 파일은 없다. Browser 런타임 0개로 GUI가 관측 불가였기 때문이다.
