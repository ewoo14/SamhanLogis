# PR #1174 rebase 및 auth migration V102 보고

작성일: 2026-08-12  
대상: `fix/1163-uuid-history-leak` / PR #1174  
현재 HEAD: `35a6da052`

## 결론

- `origin/main` 최신 `f73be2670`으로 rebase 완료. 충돌은 없었고, 이 브랜치의 7개 커밋을 재생성했다.
- auth migration은 `V101__preserve_permission_change_actor_id.sql`에서 `V102__preserve_permission_change_actor_id.sql`로 이동했다.
- SQL 본문 12줄은 변경하지 않았다. 구 파일명 참조는 0건이며, 문서 3건을 V102로 갱신했다.
- V102와 겹치는 열린 PR은 확인되지 않았다.
- 격리 PostgreSQL/서비스에서 6화면 real-QA는 6/6 통과했고 UUID 노출은 0건이었다. renderer·격리 스택·격리 볼륨은 종료·삭제했다.

## migration 네 축 재확인

| 축 | 확인 결과 |
|---|---|
| ① 실 DB 적용분 | 공유 DB 로그인/조회는 하지 않았다. 로그인 자체가 write이므로 사용자 계약상 판정하지 않았다. 대신 격리 DB Flyway 로그에서 `V101 seed sales commission` 후 `V102 preserve permission change actor id`, 최종 v102를 확인했다. |
| ② `origin/main` | auth `V101__seed_sales_commission_settlement_page_permission.sql`이 점유. auth의 다음 번호는 V102. slip-service의 V101/V102/V103은 별도 서비스 namespace다. |
| ③ 이 브랜치 | auth seed V101 + actor 보존 V102. 기존 actor migration의 SQL 내용은 그대로다. |
| ④ 열린 PR #1172·#1162·#1158·#1128·#1125 | V102 migration SQL 파일 없음. #1162에는 migration 이름을 다루는 테스트 파일만 있고 V migration은 없다. |

## 제거·변경 목록

1. 제거: `services/auth-service/src/main/resources/db/migration/V101__preserve_permission_change_actor_id.sql` 경로.
2. 추가: 같은 SQL 본문의 `V102__preserve_permission_change_actor_id.sql`.
3. 문서 참조 갱신:
   - `docs/dev-reports/2026-08-12-1163-fix2.md`
   - `docs/dev-reports/2026-08-12-1163-sol-review3.md`
   - `docs/dev-reports/2026-08-12-dg1-s4a-sol-review8.md`
4. rebase로 제품 기능을 제거하지 않았다. main에서 #1166·#1169·#1170·#1171·#1173의 변경을 함께 수용했고, 특히 #1170의 auth V101 seed와 충돌하지 않도록 actor migration만 V102로 이동했다.

## 전량 검증

| 영역 | 결과 |
|---|---|
| applied-migration guard 및 fixture 시나리오 | PASS. `origin/main` 기준 guard PASS, `Flyway applied-migration guard scenarios: PASS` |
| 파일/참조 | 제품 코드·기존 문서의 구 basename 0건, 기존 문서의 V102 참조 3건(본 보고서의 변경 목록 인용 제외), `git diff --check` PASS. 구/신 SQL 본문 동일 |
| RED-B 구조 검사 | `node --test clients/desktop/scripts/actor-display-boundary.test.cjs`: 5/5 PASS. 공격 8형 전부 RED, 정상 구성 GREEN |
| 결합문자·정상 이름 | Java/TypeScript U+0301 우회 차단 PASS. 정상 한글·자모 분해형 이름 원문 보존 PASS |
| auth | actorId PUT → DB actor_id → GET/화면 비노출 회귀군 PASS. auth 398 tests, failures/errors 0 |
| shared/common | 12 tests, failures/errors 0 |
| 아로로지스 | 593 tests, failures/errors 0 |
| slip | 요청한 focused 143 기준군 포함 현재 HEAD focused 실행 149 cases, failures/errors 0. main 이후 추가된 6 cases도 함께 통과 |
| design-system | 29 files / 270 tests PASS, build PASS, typecheck PASS |
| mobile-staff | 4 suites / 12 tests PASS, typecheck PASS |
| desktop typecheck | PASS |
| desktop 전체 Vitest | 254 files PASS, 2,220 passed / 2 skipped. 단, `harness-false-green-guard` 1건 실패: 새 `1163-fix3-real-qa/1163-fix3-live-real-qa.spec.ts`의 등록 누락을 지적하는 하네스 계약 실패. 제품 테스트 실패로 오인하지 않았고, 범위 밖 하네스 수정은 하지 않았다. |

## 격리 live-QA

- 스펙: `clients/desktop/playwright/1163-fix3-real-qa/1163-fix3-live-real-qa.spec.ts`
- API: 격리 gateway `127.0.0.1:18080`; 공유 `127.0.0.1:8080` 및 공유 DB 로그인 미사용.
- 증거 경로: `docs/qa/2026-08-12-1163-fix3-real-qa/screenshots/_local/` (`resolveQaShotsDir()` 경유)
- 확인 화면: 창고 이력, 홈택스 이력, 홈택스 제외 목록, 협업 이력, 권한 변경 로그, 삭제 배지.
- 결과: `REAL_QA_FIX3 six_screens=PASS uuid_exposure=0`, Playwright 1 passed.
- 격리 데이터: 창고 PATCH 후 audit 1건, 거래처 soft-delete 후 삭제행 1건 확인.
- 정리 확인: `sol1163-fix3` 컨테이너/네트워크/볼륨 없음, renderer 56349 listener 없음. 공유 `samhan-*` 스택은 중지하지 않았다.
- 격리 auth-service jar에는 V102가 있고 actor V101은 없다.

commit·push·PR 조작은 하지 않았다. 머지는 PM 권한으로 남겨 둔다.
