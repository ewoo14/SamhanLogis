# #1001 거래처별 원장 표시 사양 정합 — 트랙 착수 (조기 PR)

- 이슈: #1001
- 브랜치: `feat/1001-partner-ledger-spec`
- 착수: 2026-07-31
- 단계: **정찰(recon)** — 구현 전

## 이 트랙이 다루는 것

이슈 #1001 이 요구하는 3가지의 **현재 상태와 요구 사이 격차**를 실측하고, 독립 머지 가능한 슬라이스로 쪼갠다.

| # | 요구 | 상태 |
|---|---|---|
| ① | 부가세 포함 단가 | 정찰 중 |
| ② | 배송주소 데이터 | 정찰 중 |
| ③ | 판매전표 / 입금보고서 2종 구성 | 정찰 중 |

## 이번 라운드의 경계

- 산출물은 **정찰 보고서 1개**. 소스 코드 변경 없음.
- 공유 DB 는 **읽기 전용 조회만**. Docker 이미지 재빌드·서비스 재기동 금지 — 같은 시각 백엔드 스택을 쓰는 트랙이 있다.
- 새로 정할 것처럼 보이는 항목은 **기존 결정과 교차검증**하고, 충돌이 있으면 임의로 정하지 않고 충돌 자체를 보고한다.

## 용어 규율

- 할인 = **고정DC**(`products.fixed_discount_rate`) / **전역DC**(`dc_configs`) / **기본 할인율**(`partners`). "약정DC" 는 이 프로젝트에 없는 말이며, 이슈 제목의 표현을 그대로 옮기지 않는다.
- **전표**(한글) — "슬립" 금지. 영문 식별자 `slipId` 는 유지.
- 권한 표기 풀네임. 화면 UUID 노출 금지.

## 다음 단계

1. 정찰 보고서 게시 (본 PR 코멘트, 1:1)
2. 슬라이스 확정 → 구현 라운드
3. 적대검증 → 라이브QA → 머지
