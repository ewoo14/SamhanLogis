# PR #1127 R12 CODEX SOL 5.6 적대검증 — R11 비고 fix 재수렴

## 환경 확인

```text
worktree=C:\dev\Samhan-Public\.claude\worktrees\t978c
branch=feat/978-fast-path-carryover
HEAD=440cc46e52be1acca4fecc07e85d7eac92afe1ee
renderer=http://127.0.0.1:5234
renderer command=node ... vite.js src/renderer --config vite.renderer.dev.config.ts --host 127.0.0.1 --port 5234 --strictPort
renderer HTTP=200
browser=저장소 Playwright Chromium 직접 기동, headless, 1440x900
real API proxy=http://127.0.0.1:18085
product upstream=http://127.0.0.1:18084
auth API=http://127.0.0.1:8080
product container=pr1127-r7-real-qa-product (healthy)
product image=pr1127-r10-real-qa-product:ef6775061
DB=pr1127-r7-real-qa-db/product_db
```

배포본 대조:

```text
HEAD SheetSyncPage source blob SHA1=0f54108701b0a0c3edc5b50fb7759153b43638a3
HEAD SheetSyncPage source SHA256=d0f7a0e8d11f4f892502cb640fb98b9e766fcd7a6cdb51ed391491017027670b
served Vite module SHA256=e67b75fcf9fefcd7df9bd63be2e524541b7f722d1a43f8608d543eabeba394e5
served marker: parts.push(result.error)=true
served old marker: if (result.error) return result.error=false
deployed product JAR SHA256=c36d4454b7e1cedb1097bf1c00028ce11cce635d08c14baf208feda905fd0a56
product-service diff ef6775061(R10) -> 440cc46e5(R12)=0 files
```

따라서 화면 배포본은 R11 fix가 포함된 `440cc46e5` 소스이고, product JAR은 R10 이후 변경 대상이 없어 R10에서 HEAD와 대조한 동일 JAR이다.

네트워크로 확인한 실 호출:

```text
POST http://127.0.0.1:8080/auth/login                         -> 200
GET  http://127.0.0.1:18085/auth/admin/permissions/my         -> 200
GET  http://127.0.0.1:18085/api/v1/products/admin/sync/last   -> 200
POST http://127.0.0.1:18085/api/v1/products/admin/sync        -> proxy가 :18084로 전달, upstream sync 완료
GET  http://127.0.0.1:18084/api/v1/products/admin/sync/last   -> 200, lastSyncAt 전이 확인
GET  http://127.0.0.1:18085/api/v1/products/admin/sync/last   -> 200, 화면 reload 결과 투영
```

R10과 동일하게 임시 proxy의 장시간 POST response는 브라우저에 끝까지 반환되지 않았다. 첫 실행은 Playwright 240초 timeout이 났지만 서버 snapshot은 `18:09:14 KST`로 실제 갱신됐다. 이를 제품 결함으로 세지 않고, 두 번째 실행은 GUI POST 발화 뒤 upstream `/sync/last`의 시각 전이를 poll하고 화면을 reload했다. 두 번째 fresh sync는 46초에 완료됐고 Playwright `1 passed`였다. 서버 원문은 다음과 같다.

```text
18:14:25.948 [ProductSheetSync] sync 완료: insertedRows=0, updatedRows=121,
softDeletedProductRows=0, 구성품 linkedOccurrences=1601, bundle markedProducts=342,
duration=40263ms
18:14:27.643 [ProductLookupSheetSync] sync 완료: insertedRows=0, updatedRows=0,
unchangedRows=66, softDeletedLookupRows=0, skippedOccurrences=94, duration=1695ms
```

## 판정

```text
도달 가능한 결함: 0건
머지 가능
```

## 발화 조건 카운트와 공유 DB 잔재 분리

실 동기화 전에 먼저 read-only로 표본을 셌다. 표본은 0이 아니다.

```text
table_name|active|deleted
material_price|28|0
odu_recommendation_lookup|32|0
branch_pipe_lookup|6|0
ACTIVE_TOTAL|66
```

실 동기화 뒤에도 `28/32/6`, 활성 합계 `66`, 삭제 `0/0/0`이 유지됐다.

```text
branch_pipe_lookup|created_by=00000000-0000-0000-0000-000000000001|2026-07-28 20:34:17.024332~20:34:17.032183|6
material_price|created_by=00000000-0000-0000-0000-000000000001|2026-07-28 20:34:15.991420~20:34:16.028670|28
odu_recommendation_lookup|created_by=00000000-0000-0000-0000-000000000001|2026-07-28 20:34:16.524110~20:34:16.564685|32
```

세 lookup 표의 전건은 기존 system actor 행이다. 이번 검증 시각의 `QA-`·`S18`·`QA797` 행은 없으며 공유 QA 잔재를 이 PR 결함으로 세지 않았다.

## 각도 1 — 원 결함 해소

MASTER로 `/admin/sheet-sync`에 진입해 `지금 동기화`를 눌렀고, 실제 Google Sheets → DB sync 완료 뒤 같은 화면을 다시 조회했다.

추천실외기 응답 원문:

```text
insertedRows=0
updatedRows=0
unchangedRows=32
softDeletedProductRows=0
skippedOccurrences=1
error=추천실외기 natural key 중복: key=HOME_MULTI|null|7|2.5HP, firstRow=3, duplicateRow=4
```

화면 비고 원문:

```text
추천실외기 natural key 중복: key=HOME_MULTI|null|7|2.5HP, firstRow=3, duplicateRow=4 / 변경 없음 32 / skip occurrence 1
```

오류와 `변경 없음 32`가 한 셀에 함께 보인다. R10 원 결함은 해소됐다.

근거:

- `docs/qa/2026-08-09-978-r12-sol-reconv/03-after-sync-summary.png`
- `docs/qa/2026-08-09-978-r12-sol-reconv/04-result-table.png`
- `docs/qa/2026-08-09-978-r12-sol-reconv/sync-response.json`

## 각도 2 — 오류 안내 보존

화면을 원본 해상도로 직접 확인했다. 다음 오류 요소가 축약·생략 없이 모두 보였다.

```text
key=HOME_MULTI|null|7|2.5HP
firstRow=3
duplicateRow=4
```

비고 셀은 두 줄로 wrap되지만 `duplicateRow=4` 뒤에 카운트가 이어지고, 오류 문구를 밀어내거나 잘라내지 않는다. API 원문과 화면 원문이 동일하다. 반대급부 결함은 없다.

근거: `docs/qa/2026-08-09-978-r12-sol-reconv/04-result-table.png`

## 각도 3 — 오류 없는 탭과 기존 집계 보존

오류 없는 lookup 비고는 기존 형식을 유지했다.

```text
lookup:싱글 자재가격 = 변경 없음 28
lookup:분기계산 = 변경 없음 6 / skip occurrence 93
```

같은 화면의 기존 집계:

```text
총 신규 row=0
총 변경 row=121
총 삭제=0
구성품 연결=1447+154=1601
구성품 삭제=0+0=0
```

구성품 두 행은 각각 `연결 1447 / Bundle Product 271 / 삭제 0`, `연결 154 / Bundle Product 71 / 삭제 0`으로 신규·변경·삭제 열에 그대로 표시됐다. `formatTabRemark` export 변경이 다른 화면 호출부나 오류 없는 비고를 바꾼 사용자 도달 회귀는 없다.

근거:

- `docs/qa/2026-08-09-978-r12-sol-reconv/03-after-sync-summary.png`
- `docs/qa/2026-08-09-978-r12-sol-reconv/04-result-table.png`

## 라이브 QA 원문

```text
Running 1 test using 1 worker
1 passed (46.0s)

lookup:싱글 자재가격 unchangedRows=28
lookup:추천실외기 unchangedRows=32
lookup:분기계산 unchangedRows=6
totalInsertedRows=0
totalUpdatedRows=121
totalComponentLinkOccurrences=1601
totalSoftDeletedComponentRows=0
```

QA 안전 규약 보완:

- 캡처 목적지는 `resolveQaShotsDir()`를 경유한다.
- `resolveQaCredential('QA_DEV_DEFAULT_PASSWORD')`는 테스트 본문 `try/catch` 안에서 호출하며 실패 시 `test.skip`한다.
- 확정 R12 산출물 실행 때만 `QA_SHOTS_DIR`과 `QA_ALLOW_OVERWRITE=1`을 명시했다. 기본 재실행은 `_local`로 간다.
- 현재 미추적 상태에서는 공식 scope 계약이 스펙 미추적을 의도적으로 차단한다. 실제 index는 건드리지 않고 임시 index에 PM의 동시 추가 상태를 모사한 뒤 `real-qa-scope.test.cjs`를 실행해 `50 passed, 0 failed`를 확인했다.

## 신규·수정 파일 목록

이번 라운드 수정(작업 시작 전 미추적이던 R10 스펙):

```text
clients/desktop/playwright/1127-r10-sol-reconv-real-qa/1127-r10-sol-reconv-real-qa.config.ts
clients/desktop/playwright/1127-r10-sol-reconv-real-qa/1127-r10-sol-reconv-real-qa.spec.ts
```

이번 라운드 신규 생성:

```text
docs/dev-reports/2026-08-09-978-r12-sol-reconv.md
docs/qa/2026-08-09-978-r12-sol-reconv/01-before-sync.png
docs/qa/2026-08-09-978-r12-sol-reconv/02-sync-in-progress.png
docs/qa/2026-08-09-978-r12-sol-reconv/03-after-sync-summary.png
docs/qa/2026-08-09-978-r12-sol-reconv/04-result-table.png
docs/qa/2026-08-09-978-r12-sol-reconv/last-response.json
docs/qa/2026-08-09-978-r12-sol-reconv/network-calls.json
docs/qa/2026-08-09-978-r12-sol-reconv/sync-response.json
```

git commit·push는 하지 않았다.
