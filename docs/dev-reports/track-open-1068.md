연관 Issue: #1068, #1071

## 트랙 개설 — 착수 전 확인

2026-08-04 등록. **같은 화면**(판매전표)이라 한 트랙으로 묶습니다.
- `#1068` 헤더 거래처 자동채움 + 전잔·후잔 (기사 필드 배치 정정 포함)
- `#1071` 수정 화면 품목 추가 — `/sales/:id/edit` 신설

### 정찰이 실측할 것
1. `SlipFormPage`·`SlipDetailPage` 의 현재 구조. 🚨 `SlipDetailPage` 는 **4,700줄 lifecycle 허브**라 선재 결함을 떠안기 쉽습니다(`#874` 가 R21 이후 그것만 했습니다) — 범위를 명확히 자를 것
2. **전잔·후잔** 의 계산 정본이 어디인가 — 회계인가 전표인가. 🚨 금액에 닿으므로 **정밀 경로**입니다
3. `#1071`: 수정 화면에 품목 선택기가 없다는 전제를 **코드로 확인**하십시오. `#1069` 에서 PM 이 그 전제를 안 보고 지시해 도달 결함 2건을 만든 전례가 있습니다
4. 🚨 **자동 빈행 규칙** — 행 입력 화면은 전부 자동 빈행이고 "행 추가" 버튼이 없습니다. 기준은 `SlipFormPage.tsx:678-698`(증식) `:1101-1123`(빈행 저장 제외) `:710-719`(마지막 행 삭제). **재구현 금지 · 재사용**

### 🚨 업무 판단
전잔·후잔의 정의(어느 시점 기준) · 자동채움이 사용자 입력을 덮는지 — **추측 금지**