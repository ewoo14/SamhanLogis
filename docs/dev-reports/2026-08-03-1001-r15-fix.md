# PR #1061 R15 fix 기록

- 시작일: 2026-08-03 (KST)
- 기준 커밋: `11f551af3` (라이브QA R5)
- 범위: 결함 2(거래처별 금액 오염), 결함 3(사업자번호 exact lookup)
- 제한: commit/push/Docker build·up·restart/실 데이터 변경/전체 스위트 실행 금지

## 진행 원칙

- 각 결함은 RED 원문을 먼저 남긴 뒤 수정한다.
- 집계·상세·인쇄가 동일한 거래처 귀속 기준을 사용하도록 확인한다.
- 결함 1·4의 R5 PASS를 회귀 검증한다.

## R15 조사 시작

- R5 보고서: `docs/dev-reports/2026-08-03-1001-live-qa-r5.md`
- 초기 상태: 결함 2 FAIL, 결함 3 FAIL, 결함 1·4 PASS

## 결함 2·3 RED

실행 명령:

```text
./gradlew :services:accounting-service:test --tests 'com.samhanair.logis.accounting.service.SalesAggregateServiceTest.filteredAggregateDoesNotImportSalesBelongingToAnotherPartner' --tests 'com.samhanair.logis.accounting.service.PartnerLedgerReadServiceTest.businessNumberLookupReadsSelectedPartnerLedger'
```

RED 원문:

```text
2 tests completed, 2 failed

SalesAggregateServiceTest.filteredAggregateDoesNotImportSalesBelongingToAnotherPartner
expected: 100
 but was: 1000

PartnerLedgerReadServiceTest.businessNumberLookupReadsSelectedPartnerLedger
PotentialStubbingProblem: this invocation of 'find' method:
salesClient.find(2026-06-01, 2026-06-30, "1653510155", null)
has following stubbing:
salesClient.find(2026-06-01, 2026-06-30, null, <partnerId>)
```

원인 코드:

- `SalesAggregateService.applyLedgerSalesTotal`가 `partnerId`로 받은 원장 전표의 귀속을 재검증하지 않고 전부 합산한다.
- `PartnerLedgerReadService.read`가 `findByPartnerCode` 실패 후 사업자번호 directory exact fallback을 수행하지 않아 입력값을 그대로 `partnerCode` 필터로 전달한다.

## 결함 2·3 GREEN (단위 회귀)

실행 명령:

```text
./gradlew :services:accounting-service:test --tests 'com.samhanair.logis.accounting.service.SalesAggregateServiceTest.filteredAggregateDoesNotImportSalesBelongingToAnotherPartner' --tests 'com.samhanair.logis.accounting.service.PartnerLedgerReadServiceTest.businessNumberLookupReadsSelectedPartnerLedger'
```

GREEN 원문:

```text
BUILD SUCCESSFUL in 12s
2 tests completed, 0 failed
```

고친 내용:

- 선택 거래처 원장 응답은 `partnerCode` 또는 공란 코드의 고유 사업자번호가 선택 거래처와 일치하는 전표만 합산·상세화한다.
- 무필터 집계의 사업자번호 후보도 중복이면 ambiguous로 표시해 어떤 거래처에도 귀속하지 않고 식별 불가 묶음에 남긴다.
- 상세 조회에 사업자번호 exact directory fallback을 추가하고, 선택 거래처로 반환된 판매전표도 동일한 귀속 검증을 통과시킨다.

## 회귀 검증

Accounting 관련 테스트:

```text
./gradlew :services:accounting-service:test --tests 'com.samhanair.logis.accounting.service.SalesAggregateServiceTest' --tests 'com.samhanair.logis.accounting.service.PartnerLedgerReadServiceTest'

BUILD SUCCESSFUL in 5s
SalesAggregateServiceTest: 16 tests, 0 failures
PartnerLedgerReadServiceTest: 4 tests, 0 failures
```

프런트 집계·상세·인쇄 회귀:

```text
npm test -- --run src/renderer/routes/PartnerLedgerPage.print.test.tsx src/renderer/print/PartnerLedgerView.test.tsx src/renderer/api/partnerLedgerApi.test.ts

Test Files  3 passed (3)
Tests       9 passed (9)
```

확인 결과:

- 결함 1 유지: `-` 행이 `식별 불가 판매전표`로 표시되고 상세·인쇄 진입 버튼이 disabled인 기존 테스트가 통과했다. 집계의 식별 불가 금액을 제거하는 코드는 추가하지 않았다.
- 결함 4 유지: 프런트 API/인쇄 경로가 `partnerBusinessNo`를 전달하는 기존 회귀 테스트가 통과했다.
- 거래처별 금액 실측(로컬 회귀 fixture): 대상 거래처 `100`, 섞인 다른 거래처 `900`을 원장 응답으로 주입했을 때 대상 결과 `100`만 산출되어 오염 `0`이었다.
- R5 실 스택 금액(`197,476,400원`)의 재배포 후 실 데이터 재측정은 이 세션에서 Docker/재배포가 금지되어 수행하지 않았다. PM 재배포 후 `P-2026-0005/0017/0026`, `-` 행의 집계·상세·인쇄 금액을 다시 세어야 한다.

최종 accounting 재검증:

```text
./gradlew :services/accounting-service:test --tests 'com.samhanair.logis.accounting.service.SalesAggregateServiceTest' --tests 'com.samhanair.logis.accounting.service.PartnerLedgerReadServiceTest'
BUILD SUCCESSFUL in 7s
git diff --check
exit code 0
```

## 새 파일 목록

- `docs/dev-reports/2026-08-03-1001-r15-fix.md`
- `services/accounting-service/src/test/java/com/samhanair/logis/accounting/service/SalesAggregateServiceTest.java` (R15 회귀 테스트 추가)
- `services/accounting-service/src/test/java/com/samhanair/logis/accounting/service/PartnerLedgerReadServiceTest.java` (R15 회귀 테스트 추가)

수정 파일:

- `services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/SalesAggregateService.java`
- `services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/PartnerLedgerReadService.java`

초기 상태부터 존재한 미추적 `clients/desktop/playwright/1001-r5-ledger-real-qa/` 디렉터리는 변경·삭제하지 않았다.
