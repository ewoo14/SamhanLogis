# S16 fix 보고서 — PR #1078 · 이슈 #1075

## 결론

저장 경계를 넘는 규격 provenance를 닫았다. 자동 규격만 zero-width marker(`U+2060`)를 저장 문자열에 운반하고, coedit에는 `specificationSource` 셀을 함께 동기화한다. 표식 없는 저장 규격은 자동값으로 추정하지 않고 USER로 복원한다.

## RED 원문

[2026-08-06-1075-s16-red.txt](2026-08-06-1075-s16-red.txt)에 실제 RED 출력을 저장했다. 저장 요청에는 `USER-SAVED-SPEC`가 있었지만 재개방 후 품목 해제 결과는 `''`였다.

## 변경 파일

- `clients/desktop/src/renderer/routes/EstimateFormPage.tsx:83-84,275-276,337,377,385-389,1155,1494-1497,1561,1677` — hydrate/저장/coedit provenance 연결.
- `clients/desktop/src/renderer/utils/estimateSpecificationProvenance.ts:1-23` — 저장 marker encode/decode.
- `clients/desktop/src/renderer/api/mock.ts:16939` — persisted catalog mock에 marker 반영.
- `clients/desktop/src/renderer/routes/EstimateFormPage.coedit.test.tsx:434,505-632` — 저장→재개방 경계 RED-A 2종 및 자동값 회수 RED-B.
- `clients/desktop/src/renderer/utils/estimateSpecificationProvenance.test.ts:1-17` — marker round-trip/원문 복귀 단정.
- `clients/desktop/src/renderer/realtime/createCoeditProvider.test.ts:456-485` — 두 coedit 소비자 provenance 수렴·동시 회수.

## ① 새로 가능해진 상태·화면 조합과 결과

- 자동 규격 → 저장 → 동일 상세 응답 재개방 → 품목 해제: 로컬/coedit 모두 자동 규격만 공란, 기존 단가는 유지.
- 사용자 규격을 한 글자 수정 → 저장 → 재개방 → 품목 해제: 모델/품목/식별자만 해제되고 사용자 규격·단가는 로컬/coedit 모두 유지.
- 자동 규격을 카탈로그 원문으로 되돌려 입력 → 저장 → 재개방 → 품목 해제: 문자열이 원문과 같아도 USER로 유지.
- 두 coedit 소비자에서 자동 규격·`CATALOG` provenance 동기화 후 한쪽 해제: 양쪽 규격과 provenance가 함께 공란으로 수렴.
- 두 coedit 소비자에서 한쪽 규격을 직접 수정: `USER` provenance로 승격되어 다른 쪽도 사용자값을 본다.
- 기존 판매전표 공용 ProductAutocomplete, 단건 자동확정/다건 후보 모달, 취소, 라인 구조 잠금, 신규 자동 빈행, F1 blur 차단, UUID 비노출: 지정 Playwright 17건 PASS.

## ② 제거·이동·개명 식별자 grep 전수 확인

- 구 hydrate 단정 `(line.specification ?? '').trim() ? 'CATALOG' : null`: 0 matches.
- 신규 `decodeEstimateSpecification`, `encodeEstimateSpecification`, `CATALOG_MARKER`, `specificationSource`는 변경 코드·테스트에만 의도한 참조가 존재.
- `git diff --check`: PASS.

## ③ 바꾼 파일을 참조하는 테스트 전부 실행

- 직접/간접 참조 unit·contract 묶음: 8 files / 106 tests PASS.
- coedit provider 양소비자 회귀: 1 file / 30 tests PASS.
- S15 지정 desktop 회귀: `EstimateFormPage.coedit` + `SlipFormPage` 2 files / 103 tests PASS.
- design-system 지정 회귀: 2 files / 40 tests PASS.
- 지정 Playwright: 17 passed (12.8s) PASS.

## 게이트 결과 원문 요약

```text
design-system build                         PASS (built in 5.01s)
AsyncAutocomplete + ProductAutocomplete    2 files / 40 tests PASS
EstimateFormPage.coedit + SlipFormPage      2 files / 103 tests PASS
changed-file reference unit/contract        8 files / 106 tests PASS
coedit provider two-consumer                 1 file / 30 tests PASS
1062 + bundle Playwright                    17 passed (12.8s)
tsc -p tsconfig.web.json --noEmit           PASS
eslint changed files                         0 errors / existing test warnings only
git diff --check                             PASS
```

Playwright는 처음 `PLAYWRIGHT_SKIP_WEB_SERVER=1`로 실행했을 때 5173 listener가 없어 17건 모두 `ERR_CONNECTION_REFUSED`였다. 이후 worktree Vite mock server를 기동하고, 같은 `PLAYWRIGHT_SKIP_WEB_SERVER=1` 설정으로 listener를 재사용하여 17/17 PASS했다.

## 신규 파일 목록

- `docs/dev-reports/2026-08-06-1075-s16-red.txt`
- `docs/dev-reports/2026-08-06-1075-s16-fix.md`
- `clients/desktop/src/renderer/utils/estimateSpecificationProvenance.ts`
- `clients/desktop/src/renderer/utils/estimateSpecificationProvenance.test.ts`

커밋·push는 수행하지 않았다. 현재 변경은 PM 대행 커밋 대상이다.
