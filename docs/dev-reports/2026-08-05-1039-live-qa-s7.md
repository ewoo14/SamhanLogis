# PR #1045 / 이슈 #1039 라이브 QA S7

## 실행 시작

- 실행일: 2026-08-05 (Asia/Seoul)
- 작업 디렉터리: `C:\dev\Samhan-Public\.claude\worktrees\t1039`
- 브랜치: `feat/1039-provisional-dispatch`
- HEAD: `fccb7f7075c6831d9d13f87fdfb629163d883909`
- 원칙: 실 백엔드·실 서버·실 GUI 클릭. mock 및 DB 직접 쓰기 금지.

## 환경 확인

- 상태: 진행 중
- 비고: 아래에 측정 결과를 append한다.

## ①~② 삼한 실 GUI

### 환경 확인 결과 — PASS

- 실제 작업 디렉터리 확인: `C:\dev\Samhan-Public\.claude\worktrees\t1039`
- 브랜치/HEAD: `feat/1039-provisional-dispatch` / `fccb7f7075c6831d9d13f87fdfb629163d883909`
- 삼한 renderer: `http://127.0.0.1:18507`, `clients/desktop/vite.renderer.dev.config.ts`, `VITE_API_BASE_URL=http://127.0.0.1:8080`, `VITE_APP_VERSION=2026/08/05-1039`
- 아로로지스 renderer: `http://localhost:18508`, `clients/arologis-desktop/vite.renderer.dev.config.ts`, Vite proxy → `http://localhost:8097`, `VITE_APP_VERSION=2026/08/05-1039`
- 화면 API/컨테이너: 삼한 `/auth/**`, `/slips/**`, `/admin/dispatch-groups`, `/admin/carriers` → API Gateway `:8080` → auth/slip-service; 아로로지스 `/auth/**`, `/admin/arologis/**` → arologis-service `:8097`
- `samhan-slip-service`: `2026-08-04T22:13:20.425967767Z`, healthy, 기준 충족
- `samhan-arologis-service`: `2026-08-04T22:13:20.44841083Z`, healthy, 기준 충족
- 실 백엔드 증거: 삼한 `POST /auth/login` 200, `GET /auth/password/policy` 200, `GET /auth/admin/permissions/my` 200, `GET /slips?...` 200. mock fixture 사용 없음.

### ① 가배차 — 미판정

- 클릭 경로: 삼한 로그인 후 `배차 > 가배차리스트` 클릭은 API 대기로 timeout. 같은 실 renderer에서 `#/arologis/pre-classify`를 열어 실제 화면 렌더링을 확인.
- 기대: `배차 > 가배차리스트`에서 지정일 판매전표를 배송주소 권역으로 8모드 분류.
- 실제: A안 신규 전표 발행 경로는 이번 라운드 후순위로 남김.
- 실제: 화면에 `가배차 (권역)`, `지방가배차 (시도)`, 8개 실행 모드, `조회중...`, `총 0건`이 표시됨. 기존 전표 positive는 발화하지 않음. 신규 전표 발행은 수행하지 않음.
- 캡처: `docs/qa/1039-live-qa-real-qa/01-preclassify-unfired.png`
- 판정: 미판정 — 기존 전표는 발화하지 않고, 신규 전표 실 API 발행이 필요함.

### ② 배차 그룹 생성 — BLOCK

- 클릭 경로: 삼한 로그인(`dev_manager`) → `배차` 메뉴 클릭 → `배차 그룹` 클릭.
- 기대: 그룹 목록과 활성 운송사 목록을 실 API에서 불러오고 그룹을 생성할 수 있어야 함.
- 실제: 화면 진입은 성공했으나 실 응답 `GET /admin/dispatch-groups?dispatchDate=2026-08-04` → `404`, `GET /admin/carriers` → `404`. 화면에는 `배차 그룹을 불러오지 못했습니다.` 및 `활성 운송사 0개` 표시.
- 캡처: `docs/qa/1039-live-qa-real-qa/02-dispatch-groups.png`
- 판정: BLOCK — GUI에서 다음 단계로 진행할 실 그룹 생성 진입점이 없음. 직접 서비스 포트 `18086`이 존재하지만 실행 조건의 게이트웨이 `:8080`을 우회하므로 사용하지 않음.

### ③ 현장 담기 — BLOCK

- 클릭 경로: ②의 `배차 그룹` 화면에서 `구매전표 검색` 영역 확인.
- 기대: 판매전표는 가배차 결과에서, 구매전표(INBOUND)는 검색으로만 선택하여 그룹에 담을 수 있어야 함.
- 실제: 화면 자체에는 `구매전표 검색`과 `편입 대상 선택` UI 문구가 있으나 그룹 목록 API가 실 404/로드 실패하여 선택 대상·그룹이 생성되지 않음. 구매전표 검색 및 편입 클릭까지 진행할 수 없음.
- 캡처: `docs/qa/1039-live-qa-real-qa/03-add-inbound-blocked.png`
- 판정: BLOCK — INBOUND 46건의 검색 편입은 미검증.

### ④ 운송사 설정 — BLOCK

- 클릭 경로: `배차 그룹` 화면의 운송사 영역 확인.
- 기대: 그룹에 AROLOGIS 운송사를 지정하고, 미지정·비활성 운송사 사유가 표시되어야 함.
- 실제: 실 `GET /admin/carriers`가 404여서 화면에 `활성 운송사 0개`가 표시됨. 운송사 선택/지정 UI를 조작할 수 없음.
- 캡처: `docs/qa/1039-live-qa-real-qa/04-carrier-assign-blocked.png`
- 판정: BLOCK — 운송사 지정 및 차단 사유 미검증.

### ⑤ 선택 전송·멱등 — BLOCK

- 클릭 경로: `배차 그룹` 화면의 전송/조작 영역 확인.
- 기대: `is_arologis=true` 그룹만 전송하고 그 외 그룹은 전송 시도 자체가 없어야 하며, 동일 그룹 2회 전송은 중복 생성하지 않아야 함.
- 실제: 생성된 그룹이 없고 운송사 목록도 404라 전송 버튼·전송 상태·비아로로지스 운송사 경로를 실행할 수 없음.
- 캡처: `docs/qa/1039-live-qa-real-qa/05-transfer-blocked.png`
- 판정: BLOCK — 선택 전송, 비아로로지스 비시도, 멱등 모두 미검증.

### ⑥ 아로로지스 수신 확인 — PASS (빈 목록)

- 클릭 경로: 아로로지스 renderer `http://localhost:18508` → 자체 관리자 로그인(`admin` / `admin1234`) → 실제 라우트 `ReceivedGroupsPage` (`#/dispatches/received-groups`). 배차 메뉴에는 수신 그룹 링크가 노출되지 않아 라우트로 직접 진입.
- 기대: 전송받은 그룹이 표시되고, 아로로지스에서는 그룹 수정·재분류가 불가능해야 함.
- 실제: 실 API `GET http://localhost:8097/admin/arologis/dispatch-groups?dispatchDate=2026-08-04` → 200, 화면에 `수신된 배차 그룹이 없습니다.` 표시. 안내문에 `아로로지스에서는 그룹을 수정하거나 재분류할 수 없습니다.`가 표시되고, 화면 버튼은 `로그아웃`뿐이며 편집/재분류 컨트롤이 없음.
- 캡처: `docs/qa/1039-live-qa-real-qa/06-received-groups.png`
- 판정: PASS — 읽기 전용 성격은 확인. 단, 수신 데이터가 없어 전송 그룹 표시 자체와 중복 여부는 미판정.

### 운송사 목록 — BLOCK

- 클릭 경로: 삼한 `인사` 메뉴 클릭 → 노출된 메뉴 확인. `운송사 목록`은 메뉴에 없음. 지정 경로 `#/admin/carriers` 직접 진입 시 홈으로 redirect.
- 기대: 운송사 등록·수정·비활성화가 실 화면에서 가능해야 함.
- 실제: `GET /admin/carriers` 404, UI 진입점 부재 및 직접 경로 redirect.
- 캡처: `docs/qa/1039-live-qa-real-qa/07-carriers.png`
- 판정: BLOCK — AROLOGIS 운송사 및 비활성 운송사 생성/수정 검증 불가.

## 이 라운드가 보지 않은 것

- ① 신규 판매전표 실 API 발행 후 가배차 8모드 positive 발화.
- ② 그룹 생성 성공 경로와 그룹 삭제 차단(전표가 담긴 그룹 삭제 사유).
- ③ INBOUND 구매전표 검색 편입 실제 성공.
- ④ AROLOGIS 운송사 지정, 미지정·비활성 운송사 차단 사유.
- ⑤ 비아로로지스 그룹 전송 시도 자체가 없는지, AROLOGIS 선택 전송, 동일 그룹 2회 전송 멱등.
- ⑥ 수신 그룹에 실제 전송 데이터가 표시되는지(현재 수신 목록 0건).
- 인사 > 운송사 목록 등록·수정·비활성화(메뉴/라우트/API 404로 BLOCK).

## 결론

- ①~⑤ 한 줄 흐름: BLOCK. 삼한 GUI에서 `배차 그룹` 진입은 가능하지만 핵심 API가 게이트웨이에서 404라 ② 이후 진행 불가.
- ⑥ 수신 화면: PASS(읽기 전용 UI 및 실 200 빈 목록), 실제 전송 데이터 수신은 미판정.
- 신규 파일: `docs/dev-reports/2026-08-05-1039-live-qa-s7.md`, `docs/qa/1039-live-qa-real-qa/` 캡처 6개. 커밋/푸시하지 않음.
