# R30 실서버 라이브 QA 보고서

## 환경 확인

① 어느 워크트리에서 띄웠는가 (절대경로) → `D:\dev\Samhan-Public\.claude\worktrees\w1057`

② 어느 포트인가 (빈 포트를 strictPort 로) → `127.0.0.1:5187` (`vite --strictPort`, PID 23084)

③ 화면이 실제로 어느 API 를 호출하는가 — 브라우저 네트워크로 확인 → `http://localhost:8080` gateway. 로그인 `POST /auth/login → 200`, 권한 `GET /auth/admin/permissions/my → 200`, 전표 조회 `GET /slips?... → 200`을 브라우저 Network에서 확인.

④ 배포본 나이 → `slip-service`: `2026-08-04T01:39:40Z` 배포. 기준 시각 `2026-08-04` (PM 확인 인용).

대상 HEAD: `9ac2586f0f4677672534ba1f8cc58c7151993da7` (사용자 지정 HEAD와 일치)

PM 확인 인용:

```text
slip-service 배포본   2026-08-04T01:39:40Z · 실행 jar 에 sourceType 심볼 확인
R30 은 프런트 전용    (SlipDetailPage.tsx · SlipCollaborationPanel.tsx)
                     → 백엔드 재배포 불필요. 현 배포본이 맞다.
```

## QA 범위 및 규칙

- 실서버 실제 실행 및 GUI 확인.
- 기존 전표 행은 수정·삭제하지 않음.
- DB 쓰기 SQL, Docker 재빌드·재배포·컨테이너 중지, 코드 수정, 커밋을 하지 않음.
- 시나리오 B(동시 사용자)를 최우선 검증.
- 증거 캡처: `docs/qa/874-riusage-global-dc-real-qa/r30-*`

## 만든 전표 목록

| 전표번호 | 유형 | 최종 상태 | 비고 |
|---|---|---|---|
| `2026/08/04-5` | OUTBOUND | `COMPLETED` | `R30-LIVE-QA-2026-08-04-A`, 화면에서 생성 후 lifecycle 완주 |

생성 전표 `2026/08/04-5`는 화면에서 다음 버튼 경로로 완주했다. 브라우저 Network에서 각 전이 `200`을 확인했다.

`POST /slips` → `/save` → `/send` → `/accept` → `/process` → `/complete` → `/inspect` → `/ship` → `/deliver` → `/confirm`.

단계 캡처: `r30-03` 저장 전, `r30-04` DRAFT 목록, `r30-05` DRAFT 상세, `r30-06` SENT, `r30-07` 수락 전, `r30-08` ACCEPTED, `r30-09` 처리 시작 전, `r30-10` 검수 대기, `r30-12` SHIPPING, `r30-14` 배송완료 전, `r30-15` 최종 확정.

## 시나리오 A — 본인 경로

### A-1 협업 수정

상태: 진행 중 — 완료 전표 생성 후 검증 예정

### A-2 직접수정

상태: 진행 중 — 완료 전표 생성 후 검증 예정

### A-3 기사 폼

상태: 진행 중 — 완료 전표 생성 후 검증 예정

## 시나리오 B — 동시 사용자

상태: 진행 중 — 완료 전표 생성 후 검증 예정

## 시나리오 C — 전이 실패 시 폼 유지

상태: 미실행

## 시나리오 D — SENT 전표 주문전환 취소 미노출

상태: 미발화 — 아직 실행 전

## 마지막에 이 라운드가 보지 않은 것

- QA 종료 시 실제로 확인하지 못한 항목을 여기에 기록한다.
