# S28 RED 원문 — 2026-08-06

명령:

```text
npx vitest run src/renderer/routes/EstimateFormPage.coedit.test.tsx -t "S28 RED-A"
```

수정 전 결과:

```text
Test Files  1 failed (1)
Tests       1 failed | 45 skipped (46)
AssertionError: expected '' to be '9평형 / R32 / 인버터 / 윈드프리'
위치: src/renderer/routes/EstimateFormPage.coedit.test.tsx:492
```

의미: 품목이 이미 확정되고 `specificationSource=CATALOG`인 라인에서, 규격 없는 후속 blur lookup 응답을 적용한 뒤 화면 규격이 빈 문자열이 되었다. 저장 경계로 진행하기 전에 이 단언에서 실패했으므로 회귀 테스트가 결함을 잡는 것을 확인했다.
