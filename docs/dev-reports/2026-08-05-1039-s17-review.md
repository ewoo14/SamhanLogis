# 2026-08-05 · #1039 S17 2차 적대검증 보고서 (SOL)

대상 워크트리: `C:\dev\Samhan-Public\.claude\worktrees\t1039`  
브랜치: `feat/1039-provisional-dispatch`  
검증 HEAD: `4c5491a16b9fac1558e5d222b05cf8618de0262e`

## 1. 단 하나의 답

**실 사용자 경로로 재현 가능한 결함은 0건이다. 머지를 권고한다.**

S15의 도달 결함 4건은 S16 변경 표면에서 다시 밟았고 모두 닫혔다. S16 때문에 정상 경로가 차단되는 현재 집PC 실데이터는 **0건**이다.

다만 증거 용어 1건은 바로잡아야 한다. V107은 상일·초월 행만 골라 큐에 승계하지 않는다. SQL 조건에 맞는 전 행을 큐에 승계한 뒤 worker가 실제 창고 코드를 채우며, 그 결과 코드가 `2` 또는 `00003`인 행만 가배차 업무 대상으로 회복된다. 따라서 회사PC의 표현은 **“119건 큐 승계 = 116건 비대상 확정 + 3건 업무 회복”**이어야 한다. “상일·초월 3건만 승계”는 SQL 원문과 맞지 않는다.

이 집PC에서는 V106·V107이 적용되지 않았고 `slip_db` 최고 Flyway 버전은 V105다. 미적용 마이그레이션을 적용하거나 DB를 쓰지 않고 코드와 읽기 SQL로 판별했다.

## 2. S15 결함 4건 재수렴

### 2-1. 권한 없는 회계 계정의 수신 그룹 열람

닫혔다.

- 서버 `GET /admin/arologis/dispatch-groups`는 `arologis.dispatch.ops + VIEW`를 요구한다: `services/arologis-service/src/main/java/com/samhanair/logis/arologis/web/ReceivedDispatchGroupController.java:28-30`.
- 아로로지스 desktop의 `/dispatches/pre-classify`와 `/dispatches/received-groups`도 같은 page-code의 `PermissionGuard` 안에 있다: `clients/arologis-desktop/src/renderer/routes/index.tsx:157-173`.
- `PermissionGuard`는 비허용 page-code에서 보호 내용을 렌더하지 않고 `/`로 이동한다: `clients/arologis-desktop/src/renderer/components/PermissionGuard.tsx:41-45`.
- 좁은 검증: `ArologisPageCodesTest` 6개, `PermissionGuard.test.tsx` 2개 모두 실패 0.

### 2-2. 원격 수신 성공 뒤 응답 유실 시 로컬 FAILED까지 롤백

닫혔다.

- `transfer()`는 `noRollbackFor = BusinessException.class`이며 두 번 모두 응답을 받지 못하면 그룹을 `PENDING`으로 만든 뒤 예외를 반환한다: `services/slip-service/src/main/java/com/samhanair/logis/slip/service/dispatchgroup/DispatchGroupService.java:118-145`.
- scheduler는 PENDING 그룹을 같은 `groupNo` 요청으로 재전송하고 성공하면 SENT로 수렴시킨다: 같은 파일 `162-177`.
- PENDING 동안 그룹·전표 편입·운송사 변경을 잠가 원격 snapshot과 다른 요청으로 바뀌지 않게 한다: 같은 파일 `180-184`, `services/slip-service/src/main/java/com/samhanair/logis/slip/domain/dispatchgroup/DispatchGroup.java:90-94`.
- 좁은 검증: `DispatchGroupLifecycleIT` 4개 중 응답 유실 → PENDING → scheduler SENT 수렴 경로 포함, 실패 0.

### 2-3. SENT 뒤 운송사 마스터 변경으로 양쪽 표시 불일치

닫혔다.

- 운송사 수정 전에 해당 운송사를 참조하는 활성 SENT/PENDING 그룹 존재 여부를 확인하고 있으면 변경을 거부한다: `services/slip-service/src/main/java/com/samhanair/logis/slip/service/dispatchgroup/CarrierService.java:34-48`.
- 집PC 실데이터에서 활성 SENT/PENDING 그룹과 그 때문에 수정이 차단되는 운송사는 각각 0건이다. 따라서 이 fix가 현재 정상 운송사 관리 경로를 막는 건수도 0건이다.

실행 SQL 원문:

```sql
SELECT count(DISTINCT carrier_id) FILTER (
         WHERE carrier_id IS NOT NULL
           AND transfer_status IN ('SENT','PENDING')
       ) AS carriers_blocked_by_s16,
       count(*) FILTER (
         WHERE transfer_status IN ('SENT','PENDING')
       ) AS locking_groups
FROM dispatch_groups
WHERE is_deleted = FALSE;
```

실행 결과:

```text
carriers_blocked_by_s16 = 0
locking_groups          = 0
```

### 2-4. UNKNOWN 창고 경고와 상일·초월 회복

코드상 닫혔다. 집PC에는 업무 양성 표본이 없어 실제 회복 건수는 0건이다.

- 모드 결과에 포함된 UNKNOWN STACK을 경고 수에서 차감해 “화면에 보이면서 제외됐다고 세는” S15 모순을 제거했다: `services/slip-service/src/main/java/com/samhanair/logis/slip/service/preclassify/PreClassifyService.java:31-42`.
- V107은 기존 OUTBOUND의 미확정 snapshot을 worker 큐로 승계한다: `services/slip-service/src/main/resources/db/migration/V107__queue_legacy_source_warehouse_code_recovery.sql:4-16`.
- 좁은 검증: `PreClassifyServiceTest` 5개 모두 실패 0.

## 3. 집PC V107 직접 계수와 증거 정정

### 3-1. Flyway 적용 상태

실행 SQL 원문:

```sql
SELECT installed_rank, version, description, success
FROM flyway_schema_history
ORDER BY installed_rank DESC
LIMIT 5;
```

`slip_db` 결과의 최고 행:

```text
installed_rank=67, version=105, description=add dispatch group versions, success=true
```

따라서 V106·V107은 이 PC에 미적용이다. 적용을 시도하지 않았다.

### 3-2. V107이 실제로 큐에 승계할 행

실행 SQL 원문(V107 WHERE 절 그대로):

```sql
SELECT count(*) AS v107_would_update,
       count(*) FILTER (WHERE status = 'DRAFT') AS draft,
       count(*) FILTER (WHERE status <> 'DRAFT') AS non_draft
FROM slips
WHERE is_deleted = FALSE
  AND slip_type = 'OUTBOUND'
  AND source_warehouse_id IS NOT NULL
  AND source_warehouse_code IS NULL
  AND source_warehouse_code_snapshot_status = 'NOT_REQUESTED';
```

집PC 결과:

```text
v107_would_update = 2309
draft             = 2162
non_draft         = 147
```

창고 ID별 원문 계수:

```sql
SELECT source_warehouse_id, count(*)
FROM slips
WHERE is_deleted = FALSE
  AND slip_type = 'OUTBOUND'
  AND source_warehouse_id IS NOT NULL
  AND source_warehouse_code IS NULL
  AND source_warehouse_code_snapshot_status = 'NOT_REQUESTED'
GROUP BY source_warehouse_id
ORDER BY count(*) DESC;
```

```text
11111111-1111-1111-1111-000000000001 = 877  (inventory: HQ-001)
11111111-1111-1111-1111-000000000003 = 502  (inventory: CS-001)
11111111-1111-1111-1111-000000000002 = 490  (inventory: VH-001)
11111111-1111-1111-1111-000000000004 = 436  (inventory: VR-001)
11111111-1111-1111-1111-111111111111 =   4  (inventory에 없음)
합계                                      2309
```

inventory 업무 대상 창고 계수 원문:

```sql
SELECT count(*) FILTER (WHERE code IN ('2','00003')) AS business_target_warehouses,
       count(*) FILTER (WHERE code NOT IN ('2','00003')) AS non_target_warehouses
FROM warehouses
WHERE is_deleted = FALSE;
```

```text
business_target_warehouses = 0
non_target_warehouses      = 6
```

따라서 집PC 판별은 다음과 같다.

```text
V107 큐 승계 예정 = 2309
상일·초월 업무 회복 = 0
정상 대상인데 fix로 차단 = 0
```

회사PC의 `119 = 116 제외 + 3 회복`은 이 PC 데이터로 재현할 수 없다. 데이터 차이 자체는 결함이 아니다. 다만 큐 **승계**와 가배차 업무 **회복**은 반드시 구분해야 한다. V107 SQL은 warehouse code를 알기 전에 실행되므로 회사PC에서도 3건만 선별 승계할 수 없고 조건에 맞는 119건 전부를 승계한다.

## 4. 실행한 좁은 게이트

```powershell
.\gradlew.bat :services:slip-service:test `
  --tests 'com.samhanair.logis.slip.it.dispatchgroup.DispatchGroupLifecycleIT' `
  --tests 'com.samhanair.logis.slip.service.preclassify.PreClassifyServiceTest' `
  :services:arologis-service:test `
  --tests 'com.samhanair.logis.arologis.security.ArologisPageCodesTest'
```

결과:

```text
DispatchGroupLifecycleIT  4 tests, failures 0, errors 0
PreClassifyServiceTest    5 tests, failures 0, errors 0
ArologisPageCodesTest     6 tests, failures 0, errors 0
BUILD SUCCESSFUL in 44s
```

```powershell
cd clients/arologis-desktop
npm test -- --run src/renderer/components/PermissionGuard.test.tsx
```

결과:

```text
Test Files 1 passed (1)
Tests      2 passed (2)
```

전체 Gradle suite와 전체 Playwright gate는 실행하지 않았다.

## 5. 조사하지 않은 범위

- V106·V107 실적용과 적용 뒤 worker의 실시간 처리 장면은 조사하지 않았다. 이 PC DB에 두 migration이 미적용이고 적용 금지였기 때문이다.
- POST/PUT/PATCH/DELETE, DB 쓰기, 장애 주입은 하지 않았다.
- Docker 컨테이너 재빌드·재배포·중지는 하지 않았다.
- mode 5 양성 표본, 가배차 화면 2:1 레이아웃·드래그·차량 테두리는 조사하지 않았다.
- 다른 트랙 `#1057`·`#1061`·`#1063`·`#1066`은 조사하지 않았다.
- Electron GUI를 새로 기동하지 않았다. 권한 화면은 route 코드와 좁은 Vitest로 확인했다.

## 6. 최종 판정

**도달 결함 0건. 머지 권고.**

단, PR 코멘트와 후속 보고에서는 V107을 “상일·초월 3건만 승계”라고 쓰지 말고, 회사PC 기준 **“119건 큐 승계 후 116건 비대상 확정·3건 업무 회복”**으로 정정해야 한다. 집PC 실측은 **2,309건 큐 승계 예정·업무 회복 0건·정상 대상 차단 0건**이다.
