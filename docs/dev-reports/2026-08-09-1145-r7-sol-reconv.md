# PR #1145 R7 SOL 재수렴 — mock 비트 정합과 감시 폐쇄성 적대검증

- 검증자: CODEX SOL 5.6
- 검증 HEAD: `cea344eae3888274fb4b1a886c815a14f63c03d3`
- 브랜치: `feat/1144-accounting-slip-spec`
- 비트 순서: `V/C/U/D/R/DL/P`(조회/생성/수정/삭제/복원/다운로드/인쇄)
- 제약 준수: V97 미적용, 실 DB 쓰기 없음, Docker 재배포 없음, commit/push 없음. 일시 mock 뮤테이션과 probe는 모두 원복/삭제했다.

## 머지 가부 판정

**머지 불가 — BLOCKING 1건.**

HEAD의 현재 mock 값은 PR이 손댄 PageCode와 11개 역할 전수에서 실 enforcement 모델과 일치한다. 초과 집합과 누락 집합은 모두 비었다. 그러나 새 7비트 exact 테스트는 역할×PageCode 전수 곱을 보지 않는다. `PARTNER × messenger.send`를 실 모델 `0000000`에서 mock `1111000`으로 일시 과부여해도 계약 테스트가 **7 passed / 0 failed, exit 0**이었다.

심각도는 **BLOCKING(회귀 감시 결함)**이다. 현재 운영 권한이나 HEAD mock에 잘못된 양수 비트가 남은 결함은 아니지만, 이후 mock 역할 목록 한 줄 변경만으로 실 서버에서는 거절되는 경로를 mock/Playwright가 허용하고도 PR의 exact 계약이 green이 된다. R6 결함의 재발 경로가 그대로 도달 가능하다.

## 사용자 요약 선확인

다음은 사실과 일치한다.

- MANAGER 회계전표 mock은 `1111011 → 1111000`, SALES는 `1000011 → 1000000`으로 download/print 초과가 제거됐다.
- MANAGER의 `accounting.tax-invoice.inbound.manage` 누락이 `1111000`으로 복원됐다.
- 계약 테스트에 7비트 객체 동등성(`toEqual`)이 추가됐고 V97은 R6 이후 변경 0이다.

그러나 **“7비트 exact 검증이 모든 역할을 본다”는 요약은 사실과 다르다.** exact 기대표는 양수 역할 6개만 열거한다. `DRIVER/PARTNER/STAFF/DISPATCH`의 음수 셀은 해당 PageCode 전수에서 빠져 있다. 기존 RED-B가 이 역할들을 보지만 회계전표 2개와 앞 4비트만 본다. 이것이 이번 셋째 가능성이며 실제 통과 뮤테이션으로 확정됐다.

## (a) mock 비트 ↔ 실 모델 전수 대조

### 측정 방법

- 2026-08-09 07:31:29 KST, 공유 `auth_db`의 `role_page_permission_templates`를 SELECT만 했다.
- 회계전표 두 코드는 현재 `.list` 행을 V97 SELECT 규칙으로 `.accounting`에 투영했고, inbound MANAGER도 기존 `.inbound`를 `.inbound.manage`에 투영했다. V97 SQL 자체는 실행하지 않았다.
- 2026-08-09 07:33:26~07:33:30 KST, 임시 Vitest probe가 실제 `getMockResponse` 계정 응답의 비-MASTER 10역할 × 6페이지 = 60셀을 출력했다. 종료코드 0 후 probe 파일을 삭제했다.
- MASTER는 account selector 대상이 아니라 시스템 bypass다. 실 `PermissionAdminController` 계약과 mock `/permissions/my` 모두 모든 PageCode 전권 `1111111`을 반환한다. DB template 행은 `1111000`이어도 enforcement는 bypass `1111111`이다.

아래 역할 그룹은 11역할(`MASTER/MANAGER/ACCOUNTANT/SALES/WAREHOUSE/DISPATCH/INVENTORY/DEVELOPER/PARTNER/STAFF/DRIVER`)을 빠짐없이 분할한다. `DB/V97 post`는 DB template 투영값, `실 enforcement`는 MASTER bypass까지 반영한 값이다.

| PageCode | 역할 | DB/V97 post | 실 enforcement | mock | 초과 | 누락 |
|---|---|---:|---:|---:|---|---|
| `accounting.tax-invoice.inbound.manage` | MASTER | `1111000` | `1111111` | `1111111` | 없음 | 없음 |
|  | MANAGER, ACCOUNTANT | `1111000` | `1111000` | `1111000` | 없음 | 없음 |
|  | SALES, WAREHOUSE, DISPATCH, INVENTORY, DEVELOPER, PARTNER, STAFF, DRIVER | `0000000` | `0000000` | `0000000` | 없음 | 없음 |
| `accounting.sales-slip.accounting` | MASTER | `1111000` | `1111111` | `1111111` | 없음 | 없음 |
|  | MANAGER, ACCOUNTANT | `1111000` | `1111000` | `1111000` | 없음 | 없음 |
|  | SALES | `1000000` | `1000000` | `1000000` | 없음 | 없음 |
|  | WAREHOUSE, DISPATCH, INVENTORY, DEVELOPER, PARTNER, STAFF, DRIVER | `0000000` | `0000000` | `0000000` | 없음 | 없음 |
| `accounting.purchase-slip.accounting` | MASTER | `1111000` | `1111111` | `1111111` | 없음 | 없음 |
|  | MANAGER, ACCOUNTANT | `1111000` | `1111000` | `1111000` | 없음 | 없음 |
|  | SALES | `1000000` | `1000000` | `1000000` | 없음 | 없음 |
|  | WAREHOUSE, DISPATCH, INVENTORY, DEVELOPER, PARTNER, STAFF, DRIVER | `0000000` | `0000000` | `0000000` | 없음 | 없음 |
| `messenger.send` | MASTER | `1111000` | `1111111` | `1111111` | 없음 | 없음 |
|  | MANAGER, ACCOUNTANT, SALES, WAREHOUSE, INVENTORY, DEVELOPER | `1111000` | `1111000` | `1111000` | 없음 | 없음 |
|  | DISPATCH, PARTNER, STAFF, DRIVER | `0000000` | `0000000` | `0000000` | 없음 | 없음 |
| `ecount.mig.ops-dashboard` | MASTER | `1111000` | `1111111` | `1111111` | 없음 | 없음 |
|  | MANAGER | `1111000` | `1111000` | `1111000` | 없음 | 없음 |
|  | ACCOUNTANT | `1000000` | `1000000` | `1000000` | 없음 | 없음 |
|  | SALES, WAREHOUSE, DISPATCH, INVENTORY, DEVELOPER, PARTNER, STAFF, DRIVER | `0000000` | `0000000` | `0000000` | 없음 | 없음 |
| `system.permission-admin` | MASTER | `1111000` | `1111111` | `1111111` | 없음 | 없음 |
|  | 나머지 10역할 | `0000000` | `0000000` | `0000000` | 없음 | 없음 |

**(a) 판정: 현재 HEAD 값 PASS.** 역할×페이지 양방향 차집합은 `mock - real = ∅`, `real - mock = ∅`다. R7에서 초과와 누락을 모두 바로잡은 값 자체는 정확하다.

## (b) 7비트 감시 커버리지와 뮤테이션

### 실제 커버리지

`R7: every mock page changed by R6 preserves the real model 7-bit action boundary`의 exact 기대표는 다음 16개 양수 셀만 본다.

| 역할 | exact 검사 PageCode 수 | 검사 대상 |
|---|---:|---|
| MANAGER | 5 | inbound.manage, sales/purchase accounting, messenger.send, ops-dashboard |
| SALES | 3 | sales/purchase accounting, messenger.send |
| ACCOUNTANT | 5 | inbound.manage, sales/purchase accounting, messenger.send, ops-dashboard |
| WAREHOUSE | 1 | messenger.send |
| INVENTORY | 1 | messenger.send |
| DEVELOPER | 1 | messenger.send |
| DRIVER, PARTNER, STAFF, DISPATCH | 0 | 없음 |

별도 단정은 MANAGER `system.permission-admin=0000000` 7비트와 MASTER의 `canView/canEdit` 2비트뿐이다. RED-B는 `DEVELOPER/DRIVER/PARTNER/STAFF/DISPATCH/INVENTORY/WAREHOUSE × 회계전표 2개`에서 `view/create/update/delete`만 검사한다. 따라서 음수 역할 × `inbound.manage/messenger.send/ops-dashboard/system.permission-admin` 및 모든 음수 셀의 `restore/download/print`가 전수 exact 감시 대상이 아니다.

### 뮤테이션 원문

원본 mock blob: `9bf7050b87e5792e9880513c9b6190d9612a8eb0`

```diff
 const SP_D1_DEFAULT_VIEW = {
+  PARTNER: ['messenger.send'],
 }
 const SP_D1_DEFAULT_EDIT = {
+  PARTNER: ['messenger.send'],
 }
```

실 DB/V30의 `PARTNER × messenger.send`는 `0000000`이다. 위 변경은 실제 account mock을 `1111000`으로 만든다.

2026-08-09 07:32:11.552~07:32:15.242 KST 실행:

```text
npx vitest run src/renderer/test-utils/accounting-slip-permission-contract.test.ts --reporter=verbose
Test Files 1 passed (1)
Tests      7 passed (7)
MUTATION_EXIT_CODE=0
```

즉 **초과 권한이 생겨도 exact 검사가 통과했다. 감시 공백 확정.**

### 복구 증명

2026-08-09 07:32:31.662 KST 역패치 직후:

```text
mock.ts blob = 9bf7050b87e5792e9880513c9b6190d9612a8eb0
contract test blob = f9e5c65c42b288e16447ba092700948eb8f743a6
두 파일 git diff = 0
git status --short = 출력 없음
```

임시 probe도 삭제됐고 2026-08-09 07:33:57 KST `Test-Path`는 `False`였다.

**(b) 판정: BLOCKING.** 모든 역할 × 이번 PR 대상 PageCode × 7액션의 exact 표가 필요하다. 양수 셀만 열거하면 음수축 과부여를 막지 못한다.

## (c) V97 회귀 확인

2026-08-09 07:33:57 KST 측정:

```text
git diff --exit-code 28a8989c0..HEAD -- V97...sql = exit 0
V97 blob = bab045b30d26ae77de7652c677dec61ebfe87232
INSERT INTO = 8
ON CONFLICT = 8
DELETE FROM = 0
flyway_schema_history version=97 = 0행
```

R7은 V97을 변경하지 않았다. 허용 역할 제한, 4개 저장소 전표 이관, inbound MANAGER 한정 이관, partial unique upsert 구조가 R6와 동일하다. **(c) 판정: PASS.** 실제 적용/재적용은 금지 조건 때문에 하지 않았다.

## (d) Playwright

실행 명령:

```text
npx playwright test playwright/datagrid/narrow-action-column.spec.ts --reporter=line
```

2026-08-09 07:34 KST 시작 후 테스트/Vite 출력 0줄 상태로 184.1초 정지했고 실행기가 직접 종료코드 `124`로 끝냈다. assertion 도달 증거가 없어 **로컬 관측 불가**다.

원인은 로컬 의존 산출물 누락으로 좁혔다.

```text
clients/desktop/node_modules/@samhan/design-system -> clients/web/design-system (junction 정상)
clients/web/design-system/package.json exports -> ./dist/index.js
dist/index.js 존재 = False
dist/style.css 존재 = False
node import('@samhan/design-system')
  -> ERR_MODULE_NOT_FOUND: .../@samhan/design-system/dist/index.js
```

Playwright webServer는 이 의존성을 읽는 Vite를 5173에서 띄우므로 시작 전 해석 단계가 닫히지 않는다. timeout 후 남은 Vite PID `20756`은 명령줄과 07:37:38 생성 시각으로 이번 진단 프로세스임을 확인한 뒤 종료했고, 5173 LISTEN과 새 git 변경이 모두 0임을 확인했다.

다만 원격 CI는 선행 build 환경을 갖고 있으며 2026-08-09 07:38 KST 조회 시 `Desktop Playwright (mock 회귀 hard gate)`가 **pass(12m39s)**, `Playwright (web + electron + mobile emul)`도 **pass(2m45s)**였다. 따라서 이 로컬 관측 불가는 현재 원격 CI 머지를 별도로 막는 실패는 아니다. (b)의 감시 공백은 CI도 7/7 green으로 놓치므로 여전히 BLOCKING이다.

## (e) 증거 무결성 — 7 passed / 0 failed

원복 후 2026-08-09 07:39:39.338~07:39:42.723 KST 재실행:

```text
npx vitest run src/renderer/test-utils/accounting-slip-permission-contract.test.ts --reporter=verbose
Test Files 1 passed (1)
Tests      7 passed (7)
FINAL_CONTRACT_EXIT_CODE=0
```

요청된 **“7 passed / 0 failed”는 재현됐다.** 그러나 같은 결과가 과부여 뮤테이션에서도 재현됐으므로, 이 수치만으로 모든 역할의 7비트 exact 폐쇄성을 증명하지 못한다.

## 이 라운드가 보지 않은 것

- V97 실제 적용·rollback·재적용과 적용 후 materialization/cache reload는 금지 조건으로 보지 않았다.
- 로컬 Playwright assertion/UI 결과는 design-system `dist` 부재와 timeout으로 보지 못했다. 원격 CI 결과만 확인했다.
- account/group 저장소의 13계정·2그룹 전수 및 실제 로그인 후 API 200/403은 R6 PASS 범위이고 V97 0변경이므로 재실행하지 않았다.
- 이번 판정은 권한 정책 자체의 업무 타당성이 아니라 mock 정합 및 회귀 감시 폐쇄성을 대상으로 했다.

## 신규 파일 경로 목록

- `docs/dev-reports/2026-08-09-1145-r7-sol-reconv.md`

일시 뮤테이션과 probe 파일은 신규 파일 목록에 남지 않는다.
