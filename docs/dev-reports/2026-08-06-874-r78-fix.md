# R78 fix 보고 — PR #1057 · 이슈 #874

구현자: LUNA  
기준 HEAD: `b4364f41a`  
범위: R77 지시서 F1/F2만 수정. 커밋·Docker·배포·DB 쓰기·vendor 호출 없음.

## RED 실패 원문

선행 RED 명령:

```text
npx vitest run src/renderer/utils/slipDiscount.test.ts src/renderer/routes/SlipFormPage.test.tsx --reporter=dot
```

```text
slipDiscount.test.ts (6 tests | 1 failed)
× 변동DC 비대상 품목은 물리 카테고리 키가 있어도 전역DC를 적용하지 않는다
  → expected { unitPrice: 106080, rate: 48, … } to deeply equal
    { unitPrice: 204000, rate: 0, … }

SlipFormPage.test.tsx (64 tests | 2 failed)
× does not apply global DC to a non-variable-discount product with a physical fallback category
  → expected '520' to be '204000'
× confirms the selected product at catalog price before a pending DC request completes
  → expected '' to be 'Product A'

Test Files 2 failed (2); Tests 3 failed | 67 passed (70)
```

RED 원인은 계산 입력에 `hasVariableDiscount`가 없고, 품목 선택이 DC GET 완료 전까지 `updateLine`을 실행하지 않았기 때문이다.

## 변경 파일 및 줄

- `clients/desktop/src/renderer/utils/slipDiscount.ts:7,31-33`
  - 변동DC 자격 입력을 추가하고 `hasVariableDiscount === true`일 때만 전역DC를 선택한다.
  - 고정DC 우선 순서는 유지한다.
- `clients/web/design-system/src/components/ProductAutocomplete/ProductAutocomplete.tsx:40`
  - `ProductOption.hasVariableDiscount`를 공개 계약에 추가했다.
- `clients/desktop/src/renderer/api/productApi.ts:39,90`
  - BE 응답 플래그를 FE 타입·매핑에 보존했다.
- `clients/desktop/src/renderer/routes/SlipFormPage.tsx:613,772-856,1034,1067`
  - 정가/productId 라인을 DC 조회 전에 확정한다.
  - DC GET에 기존 5초 유한 제한시간을 적용하고, 성공 시에만 현재 라인을 할인 단가로 보정한다.
  - 거래처 변경 시 sequence와 partner id를 검증해 늦은 응답의 가격·`discountInfo` 오염을 차단한다.
  - 최근단가가 정상 전역DC 단가를 덮지 않는 권위 규칙을 유지한다.
- `clients/desktop/src/renderer/utils/slipDiscount.test.ts:7-50`
  - F1 RED-A와 고정DC/정상 전역DC 반대급부를 고정했다.
- `clients/desktop/src/renderer/routes/SlipFormPage.test.tsx:624-654`
  - 비대상 상품 정가·무문구, DC pending 중 품목 확정을 고정했다.
- `services/product-service/src/test/java/com/samhanair/logis/product/web/dto/ProductSummaryResponseTest.java:42-54`
  - 파생 `categoryKey`와 `hasVariableDiscount=false`가 별도 보존됨을 고정했다.

## 게이트 결과 원문

```text
cd clients/desktop
npx vitest run src/renderer/utils/slipDiscount.test.ts src/renderer/utils/usePartnerPriceRefresh.test.ts src/renderer/routes/SlipFormPage.test.tsx --reporter=dot

Test Files 3 passed (3)
Tests 79 passed (79)
```

```text
.\gradlew.bat :services:product-service:test --tests '*ProductSummaryResponseTest' --no-daemon --console=plain

BUILD SUCCESSFUL in 19s
```

```text
.\gradlew.bat :services:slip-service:test --tests '*SlipServiceTest' --no-daemon --console=plain

BUILD SUCCESSFUL in 16s
```

React Router의 기존 Future Flag warning만 stderr에 있었고 테스트 실패는 없었다.

## 신규 파일 목록

- `docs/dev-reports/2026-08-06-874-r78-fix.md` (본 보고서)

이번 수정에서는 별도 소스 신규 파일을 만들지 않았다. 기존 working tree의 R71/R76 QA 변경·미추적 파일은 건드리지 않았다.
