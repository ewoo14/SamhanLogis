# `#910` + `#935` 잔여 — 웹 3앱 · Expo 3앱 자동 업데이트

> Electron 3앱은 `#1211`(`99ab8fbb5`)에서 닫혔다. 이 트랙이 나머지를 닫고 두 이슈를 종료한다.

## 닫힌 것 (#1211)

```
신뢰 루트   저장된 플래그를 믿지 않고 Cert:\CurrentUser\Root 에서 실제 조회
            없으면 installed=false 로 정정하고 다시 묻는다
알림 UI     design-system AppUpdateNotice — 4계열을 색상 레일로 구분
릴리스      아로로지스 wrapper 가 fresh checkout 에서 실패하던 것 해소
            오류를 삼키던 최상위 catch 를 재전파로 정정
실행 확인   아로로지스·사내메신저·삼한 데스크톱 3앱 전부 9101→9102 · 데이터 보존
```

## 남은 것

```
웹 3앱     릴리스 wrapper 신설 (결정 5)
           명시 버전 wrapper 로 Electron 과 같은 fail-closed 운영 계약
Expo 3앱   EAS Update OTA + 사용자 동의 (결정 2)
           🚨 즉시 reloadAsync() 는 그대로 두면 안 된다
              저장되지 않은 입력을 조용히 잃게 하는 것이 가장 나쁜 실패다
           ⏳ 선행 — EAS project 소유 계정·비용 정책 확정
사내 메신저 배너를 AppUpdateNotice 로 통일
           현재 기본 aside 1개 + 기본 버튼 (문구 계약만 같고 시각 구분 없음)
인증서      정본 thumbprint 기록 (실제 발급 후)
            🚨 만료 30일 전 알림 장치 — 놓치면 자동 설치가 전부 멈춘다
```

## 🔒 착수 시 보존할 불변식 (#1211 이 세운 것)

```
1  한 앱의 릴리스가 다른 앱의 정책이나 feed 를 오염시키지 않는다
2  🚨 사용 중 자동 install/reload/새로고침이 저장되지 않은 입력을 조용히 잃게 하지 않는다
3  버전 정책·feed 실패가 앱 사용을 무조건 차단하지 않되 사용자에게는 드러난다
4  내부 오류 코드·경로·UUID 를 사용자 화면에 노출하지 않는다
5  Electron 릴리스는 서명 없이 빌드 실패하고 signer 와 배포된 신뢰 루트가 일치한다
6  하네스 성공은 실제 updater 경로·실제 installer·실제 재기동·cleanup 성공을 포함한다
7  9개 앱은 각각의 실행 환경에서 별도로 검증한다
```

## ⏳ 미결 하나

알림 카드의 **제목과 본문 첫 문장이 겹친다**(두 줄인데 정보는 한 줄치).
🚩 `#1214` 결정 7 로 그 트랙에 흡수됐다 — 여기서는 손대지 않는다.
