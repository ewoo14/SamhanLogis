# 트랙 개설 — 단가·할인 축 (#1090 + #1140)

> 둘 다 등록 2026-08-06 / 2026-08-08 · **두 이슈를 한 트랙으로 묶음**

## 왜 묶었나
둘 다 **견적/주문의 단가·할인 계산 표면**을 건드린다. 같은 계산기와 같은 화면 옵션을 만지므로 따로 가면 충돌한다.

| 이슈 | 범위 |
|---|---|
| `#1090` | 정액 할인 판별을 **모델명에서 품목 분류로** 전환 — 분류 단독 정본 (개발책임자 결정 **A안**) |
| `#1140` | **'인상 전 단가' → '단가변동'** 명칭 통일 · 기본값 설정 가능 · 구형에도 토글 신설 |

## 🚨 정찰이 **가장 먼저** 확인할 것 — 이미 됐는지
`#1132`(세트 전개 기본구성품 + **정액DC 분류축**) 과 `#1166`(제품구분 정비 + 주문 40% 규칙) 이 **이미 머지됐습니다.**
`#1090` 이 말하는 "분류 정본 전환" 이 **거기서 이미 됐을 수 있습니다.**
⟹ 남은 부분만 골라내십시오. 🚫 이미 된 것을 또 하지 마십시오.

## 재논의 대상이 아닌 것 (기존 결정)
```
견적 옵션 명칭은 DB 속성축을 따른다
'약정DC' 는 없는 말이다 — 할인 용어는 확정된 것을 쓸 것
정액 할인 판별 = 분류 단독 정본 (A안)
```

## 정찰이 확정할 것
1. `#1090` 잔여분이 무엇인가 (위 대조 결과)
2. `#1140` 의 **기본값 설정**이 어디에 저장되는가 — 사용자별? 전역? 거래처별?
3. **구형에도 토글 신설** — "구형" 이 무엇을 가리키는가. 좌표로 확정
4. 금액이 바뀌는가 — 바뀐다면 **실 데이터 몇 건이 달라지는지 세라**

## 🚨 3축 대조 의무
①코드 ②이슈(CLOSED 포함) ③기존 결정.
