# 트랙 #1238 — GAS 격차 '다름' 파리티 배치 2 이후

## 실측 정정

이슈 제목의 "37건" 은 오계수였다. 실제는 6개 보고서 합계 **91건**이며 분류 결과:

```text
Ⓐ 구현하면 되는 것          43건
Ⓑ 실제 충돌                  5건 → 그중 진짜 충돌 1건 (EST-R03 카드 수수료)
                                    → 08-15 결정 유지(설정 기반)로 확정
Ⓒ 레거시가 답이 아닌 것      42건 → 전부 기존 결정으로 답이 있음 (아래)
Ⓓ UI 디자인                   1건 → 격차 아님 (skip)

⟹ 개발책임자 판단이 새로 필요한 것 0건. 남은 것은 전부 구현이다.
```

## 이미 확정된 Ⓒ 축

```text
① 가격·상품 정본        우리 DB (시트/Notion 은 이관 원천이었을 뿐)
② 완료 상태·일마감 통제  수동 각인·예외를 다시 넣는다 (누가·언제·왜가 남아야 함)
③ 배차·DPS 원천         배차는 우리 출고전표 DB · DPS 자료만 엑셀 업로드
```

근거: `docs/decisions/2026-08-16-gas-c-axis-decisions.md` · `docs/dev-reports/2026-08-16-gas-difference-triage.md`

## 범위

배치 1(주문서웹 Ⓐ 6건 · PR #1241) 이후의 Ⓐ 나머지 — **종합견적서부터**.

프로그램 단위로 배치를 끊는다. 규칙 ID 접두(OW-* / EST-* / DS-*)가 다르면 다른 프로그램이고 결정도 따로다.

## 완료 조건

배치별 — 레거시 원문 경로 · 현행 경로 · 같은 입력/같은 출력 대조 원문 + 화면 스크린샷.
