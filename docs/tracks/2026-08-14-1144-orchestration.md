# `#1144` 3·4순위 — 생성 orchestration + 삭제/재생성 수명주기

> 1·2순위는 `#1213`(`4498d1f20`)에서 닫혔다. 이 트랙이 나머지를 닫고 이슈를 종료한다.

## 닫힌 것

```
1순위  정책·데이터 게이트 — V101 적용 확인 · 4자리 373행 · 미매핑 0
2순위  공통 연결 read model + eligibility
       LEGACY_READ_ONLY · SOURCE_DATA_INTEGRITY_BLOCKED 명시 상태
       legacy 19건이 API 409 로 실제 차단됨
```

## 3순위 — 생성 orchestration 1개로 3경로 수렴

```
"원천 전표 1건 → 회계전표 1건" 명령을 만든다
  전표 상세   1회 호출
  외부 일괄   N회 원자 호출   (Q1: N건→N건 · 전부 성공 또는 전부 실패)
  일마감      판단 결과와 함께 같은 명령 호출
🔑 화면별로 VAT/거래처/과배분 로직을 복제하지 않는다
```

## 4순위 — 삭제/재생성 수명주기

```
Q3  DRAFT 는 soft-delete 후 재생성 · 전기된 것은 VOIDED 처리 후 재생성
    연결된 세금계산서는 먼저 취소·연결해제
Q2 파생  총계정원장 분개는 역분개가 아니라 전기 취소 후 재전기
         (명세 11번 "역처리가 아니라 삭제 후 재생성" 과 정합)
```

## 🔒 지켜야 할 것

```
1  #1213 이 세운 read model 판정을 모든 경로가 공유한다 (따로 판단하지 않는다)
2  legacy marker 19건은 계속 읽기전용이다
3  Q5  일마감 미검증 시 회계전표 생성·마감 차단
4  Q6  회계 권한 = 회계담당자 + 관리자 + 마스터 (현행 유지)
5  🚨 삭제가 hard delete 가 되지 않는다 — BaseEntity 7 audit + soft delete
```

## ⏳ 함께 처리 — 계정과목 미매핑 6건

PM 이 이카운트 정본 표에서 대응값을 찾았다(개발책임자 위임).

```
141 토지            → 2019
163 소프트웨어       → 2374 컴퓨터소프트웨어
114 대여금          → 1082 단기대여금
120                → 1209 미수금
148 (투자활동)      → 2224 건설중인자산
230 (재무활동)      → 2515 단기차입금
```

🚩 `900` 과 `100·200·300·400·500·800` 은 **대분류 코드**다. leaf 가 아니므로 매핑 대상이 아니다.
🚩 `103·104·105` 는 V101 이 **이미 매핑**했다(`1029` · `1059` · `1059`).
