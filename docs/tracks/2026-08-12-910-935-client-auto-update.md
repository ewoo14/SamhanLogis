# 트랙 개설 — 클라이언트 자동 업데이트 (#910 + #935)

> #910 등록 2026-07-22 · #935 등록 2026-07-25 · **두 이슈를 한 트랙으로 묶음**

## 왜 묶었나
둘 다 **electron-updater 배포 피드·코드서명·버전 확인 UI** 라는 같은 표면을 건드린다.
따로 진행하면 같은 파일을 두 PR 이 만지고 머지 충돌이 난다.

| 이슈 | 범위 |
|---|---|
| `#910` | 전 사용자 대면 클라이언트 **8앱** 자동 업데이트 · 사용 중 알림 |
| `#935` | **아로로지스 데스크톱** 버전 확인·자동 업데이트 (`#928` 후속) |

## 🚩 선행 조건 — 제목에 이미 적혀 있다
`#935` 는 **코드서명·피드 선행**이다. 정찰이 가장 먼저 답할 것:
- 코드서명 인증서가 확보돼 있는가. 없으면 이 트랙은 **자격 대기**다
- 배포 피드(`latest.yml`)를 어디에 올리는가

## 정찰이 확정할 것
1. **8앱이 무엇인지 목록으로** — `clients/` 아래 실제 패키징 대상
2. 이미 있는 자동 업데이트 배선 — `#928` 이 무엇까지 했는가 (🚨 없는 걸 만들지 말 것)
3. **사용 중 알림**의 UX — 업데이트 중 작업 중이던 사용자를 어떻게 다루는가
4. 아로로지스는 **독립 운영 단위**다. 삼한 퍼블릭 피드와 분리인가 공유인가

## 🚨 3축 대조 의무
①코드 ②이슈(CLOSED 포함) ③기존 결정(`docs/dev-reports/`).
