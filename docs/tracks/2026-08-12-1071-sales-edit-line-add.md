# 트랙 개설 — #1071 판매전표 수정 화면 품목 추가 (`/sales/:id/edit` 신설)

> 등록 2026-08-04 · `#1062`/PR `#1063` 에서 분리된 건 · 단독 트랙

## 🚨 정찰이 **가장 먼저** 확인할 것 — 이미 됐는지
`#1063`(전표 라인 입력 UX) 이 머지됐고, 그 뒤 `#1131`(판매전표 계보)·`#1172`(헤더 자동채움 + 전잔/후잔) 도 머지됐다.
**그 사이에 이 화면이 이미 생겼을 수 있다.**
⟹ `/sales/:id/edit` 라우트가 지금 존재하는지, 품목 추가가 되는지 **먼저 실행해 확인**하고,
   이미 되면 **고치지 말고 그 사실을 보고**할 것.

## 정찰이 확정할 것
1. 지금 판매전표를 수정하는 경로가 무엇인가 (신규 화면 재사용? 별도 편집?)
2. 품목 추가 시 **자동 빈행** 규칙이 이 화면에도 적용되는가 (저장소 전역 규칙)
3. 수정이 **전잔/후잔**에 미치는 영향 (`#1172` 계약과 충돌하지 않는가)
4. 계보(`instanceKey`) 와의 관계 (`#1131`)

## 🚨 3축 대조 의무
①코드 ②이슈(CLOSED 포함) ③기존 결정. *"이름이 다르다"* 를 *"기능이 없다"* 로 읽지 말 것.
