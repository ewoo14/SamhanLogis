# 트랙 개설 — #1086 활성 품목 모델코드 부재 (**전제 미재현 · 처분 필요**)

> 등록 2026-08-06 · 정찰 완료 2026-08-08 · **구현 트랙이 아니라 이슈 처분 트랙**

## 🚩 정찰 결론 — 이슈 본문의 전제가 재현되지 않습니다
근거: `feat/1086-product-model-code` 브랜치의 `#1086 S1 정찰` (2026-08-08)

| 이슈 주장 | 실측 |
|---|---:|
| 활성 품목 **33개**에 불변 모델코드 부재 | **0건** (`is_deleted=false AND status='ACTIVE'` 3,083건 중 `model_code` NULL/공백 0) |
| **4,225 line** 이 이름 fallback 에 의존 | 현재 line 중 `product_id IS NULL` **0건** |

## 다만 남아 있는 것
```
products.name 동일 이름 활성 그룹 170개 (652행 · 초과 482행)
⟹ 이름만으로 해소하는 계약이 어딘가 있다면 오인 가능성은 실재한다
   반면 실제 fallback 에 쓰이는 model_name 은 활성 중복 0그룹
```

## 이 트랙이 할 일
1. **이름 기반 해소 계약이 코드에 남아 있는지** 전수 확인 — 있으면 그게 진짜 결함이고 범위를 그것으로 다시 씀
2. 없으면 **이슈를 닫자고 개발책임자께 상신**
3. 🚫 이슈 제목의 33건·4,225건 수치를 **그대로 인용하지 말 것** — 재현되지 않았다

## 🔑 이 트랙의 값
*"이름이 다르다"* 를 *"기능이 없다"* 로 읽지 않는 것과 같은 이유로,
**"이슈에 적혀 있다" 를 "지금도 그렇다" 로 읽지 않습니다.**
