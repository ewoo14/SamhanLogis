# 트랙 개설 — 회계 정본 (#1072 + #1144)

> #1072 등록 2026-08-05 · #1144 등록 2026-08-08 · **두 이슈를 한 트랙으로 묶음**

## 왜 묶었나
`#1144`(회계전표 생성·연결)가 만드는 분개는 **`#1072` 가 정하는 계정과목 체계 위에서** 만들어진다.
계정과목을 나중에 바꾸면 이미 만든 분개를 다시 손봐야 한다. **순서가 있는 한 덩어리**다.

| 이슈 | 범위 | 순서 |
|---|---|---|
| `#1072` | 계정과목 정본을 **이카운트 체계**로 전환 — 앱 생성 경로·기존 라인 정규화·chart 정리 (개발책임자 결정 **C**) | 선행 |
| `#1144` | **회계전표(매출·매입) 생성·연결 체계** — 개발책임자 명세 전수 반영 | 후행 |

## 정찰이 확정할 것
1. **개발책임자 결정 C 의 원문** — `#1072` 본문과 `docs/dev-reports/` 결정 문서를 대조해 그대로 옮길 것. 🚫 재해석 금지
2. **`#1144` 의 "명세 전수"** 가 무엇인지 목록화 — 빠짐없이
3. 🚨 **이미 있는 것** — `#1168`(정산 그룹웨어 참조)·`#1169`(정산서 확정 취소)·`#1170`(정산 S4a) 이 최근 머지됐다. 회계 표면이 이미 상당히 움직였다. **무엇이 이미 있는지 먼저 세라**
4. **기존 라인 정규화의 영향 범위** — 실 데이터 몇 건이 바뀌는가. 🚨 되돌릴 수 있는가
5. **마이그레이션 번호** — 파일명·DB 적용분·다른 브랜치 미머지분 **셋 다** 셀 것

## 🚨 잃으면 안 되는 것
회계는 금액에 닿는다. **기존 분개·잔액이 바뀌면 안 된다.** 정규화가 과거 수치를 흔들면 그건 결함이다.

## 🚨 3축 대조 의무
①코드 ②이슈(CLOSED 포함) ③기존 결정.
