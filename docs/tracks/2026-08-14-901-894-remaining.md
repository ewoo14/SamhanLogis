# `#901` + `#894` 잔여 — 클로드 도구 호출 · 채팅방 신설/편집

> 창 구조·세션 제목 요약·presence 는 `#1180`(`bac45f4ea`)에서 닫혔다.
> 이 트랙이 나머지를 닫고 두 이슈를 종료한다.

## 닫힌 것 (#1180)

```
창 구조    메인 창 = 목록 전용 · 방별 별도 BrowserWindow · 중복 창 없음
           방별 위치·크기 복원 · 메인 창을 닫아도 방 창이 남으면 접속 유지
클로드     세션별 창 · 제목이 요약으로 붙는다 (217자 질문 → 8자 제목)
           목록 재진입 시 모델 호출 경로 없음
           REAL / VIRTUAL / CREDENTIAL_UNAVAILABLE 구분
권한       계약 14/14 · 가상 에이전트 상태에서도 우회 불가
presence   6상태 · 만료 스케줄러 실제 동작 (AVAILABLE → ABSENT · 52초)
```

## 남은 것 — `#901` 클로드 도구 호출

지금은 **대화·요약·가상 에이전트까지**다. 계정 권한 안에서 **실제 작업을 수행하는 부분이 미구현**이다.

```
🔒 권한 3축 (2026-08-13 결정)
  축 0  그 계정이 클로드를 쓸 수 있는가
  축 1  그 계정이 그 업무를 할 수 있는가   ← 여기가 핵심
  축 2  클로드가 대신 실행할 때도 축 1 이 그대로 적용되는가
🚨 클로드가 축 1 을 우회하는 통로가 되면 안 된다

되묻기 흐름 (docs/decisions/2026-08-14-claude-clarify-before-answer.md)
  모호하면 선택지를 제시하고 사용자가 고른 뒤 결과를 낸다
  🚨 선택지는 실제로 존재하는 메뉴여야 하고
     그 계정의 권한 안이어야 한다 (없는 메뉴를 보여 주면 축 1 우회다)

외부 전송 범위  사용자 질문 + 클로드가 호출한 API 응답만
```

## 남은 것 — `#894` 채팅방 신설·편집 (S3~S7)

```
방 신설 · 참여자 편집 · 방 이름 변경 · 나가기 등
🚩 #1180 이 만든 창 구조와 presence 계약을 깨지 않아야 한다
```

## ⏳ 함께 볼 것

```
본체 데스크톱 채팅 화면이 방 이름 없이 "채팅방" 만 반복되는 원시 목록이다
🚩 #1214 결정 7 로 디자인은 그 트랙이 맡는다 — 여기서는 기능만 본다
REAL 모델 라이브 요약  Anthropic 자격 주입 후 확인 필요
```

## ⏳ 미결 4건 (개발책임자 대기)

```
안읽음 칩/+ 위치 · 목록별 안읽음 · 입사일 공백 정렬 · 수동 상태 해제 시점
```
