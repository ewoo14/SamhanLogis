# 트랙 개설 — 세트 구성품 (#1111 + #1143)

> #1111 등록 2026-08-07 · #1143 등록 2026-08-08 · **두 이슈를 한 트랙으로 묶음**

## 왜 묶었나
둘 다 **세트(BUNDLE) 구성품의 데이터 모델과 편집 화면**을 건드린다. 소관을 옮기면서(#1111)
비중·반올림을 데이터로 빼는(#1143) 것이 자연스럽고, 따로 하면 같은 엔티티를 두 번 만진다.

| 이슈 | 범위 |
|---|---|
| `#1111` | 세트 구성품 **편집 소관을 기초품목으로 이동** — 견적품목에는 세트 검색·납품가만 (개발책임자 정정) |
| `#1143` | 세트 구성품 **비중·반올림 단위를 데이터로** — `#1093` 의 데이터 모델이 소관 없이 남았다 |

## 🚨 정찰이 가장 먼저 확인할 것
`#1132`(세트 전개 기본구성품 + 정액DC 분류축) 가 **이미 머지**됐습니다. 세트 전개 표면이 이미 움직였습니다.
`#1093` 이 남긴 데이터 모델이 **정확히 무엇인지** 좌표로 확정하십시오. 🚫 없는 걸 만들지 말 것.

## 정찰이 확정할 것
1. 지금 세트 구성품을 **어디서** 편집하는가 (견적품목? 기초품목? 둘 다?)
2. `#1093` 의 비중·반올림 데이터 모델 좌표와 **현재 사용처**
3. 소관 이동 시 **기존 세트가 깨지는가** — 실 데이터 몇 건이 영향받는지 세라
4. 마이그레이션이 필요한가 — 필요하면 **번호를 셋 다** 세라

## 재논의 대상이 아닌 것
```
개발책임자 정정: 견적품목에는 세트 검색·납품가만 둔다
수량 동기화 = 본체(실내기·실외기) → 부속 칩 방향
```

## 🚨 3축 대조 의무
①코드 ②이슈(CLOSED 포함) ③기존 결정.
