# 트랙 개설 — 품목 식별자 정합 (`#1051` · `#1086`)

> 2026-08-10 개설. **같은 뿌리**(품목을 무엇으로 가리키는가)라 한 트랙으로 묶습니다.

## 묶은 이유

| 이슈 | 등록 | 내용 |
|---|---|---|
| `#1051` | 2026-08-01 | 전표가 참조하는 품목이 품목 DB 에 없다 — 연결 끊김 전수 조사 |
| `#1086` | 2026-08-06 | 활성 품목 33개의 **불변 모델코드 부재** — 4,225 line 이 이름 fallback |

둘 다 **"품목을 무엇으로 가리키는가"** 입니다. 따로 열면 같은 테이블·같은 조인을 두 번 만지고 머지 충돌이 그만큼 납니다.

## 🚨 이 트랙에 앞서 알아 둘 것

오늘(2026-08-10) 머지된 것이 이 표면에 닿습니다.

```text
#1156  partner_code 를 조인 키로 쓰되 사업자번호와 섞지 않는다
       → 같은 종류의 문제를 거래처 축에서 이미 겪었다
       → "전수했다" 가 다섯 번 틀렸고, write 축만 보고 read 축을 안 본 것이 원인
#1133  품목 상태(단종·미판매·품절)가 후보 노출을 가른다
#1154  기초거래처 7,253건 적재
```

🔑 **`#1156` 의 교훈을 그대로 가져옵니다** — 식별자를 바꾸거나 채우는 변경은 **write 축과 read 축을 모두** 훑어야 합니다.

## 정찰이 실측할 것

1. **연결 끊김 실 분포** — 전표·견적·재고가 참조하는 product 중 활성 카탈로그에 없는 것이 몇 건인가. 삭제된 행과 일치하는가. SQL 원문.
   - `#1113` 진단에서 **재고 201행의 product UUID 101개 중 100개**가 없다는 관측이 있었습니다. 지금 수치를 다시 세십시오.
2. **모델코드가 없는 활성 품목 33개** — 지금도 33개인가. 그 33개를 참조하는 line 이 몇 건인가.
3. **이름 fallback 이 어디서 도는가** — `파일:줄`. 이름이 바뀌면 무엇이 깨지는가.
4. 🚨 **채번 규칙** — 모델코드를 새로 부여해야 한다면 형식이 무엇인가. 이 저장소의 기존 노출 코드 선례를 표로(`P-2026-0001` · `2026/08/10-1` 등).

## 🚨 업무 판단이 필요할 것 — 추측 금지

- 모델코드가 없는 품목에 **코드를 새로 부여할 것인가, 그 품목을 정리할 것인가**
- 끊긴 참조를 **복구할 것인가 끊긴 채로 표시할 것인가**
- 🚨 `#1085` 의 QA797 잔재(전표 549 · 견적 63)와 **겹치는 행이 있는지** 먼저 가를 것 — 잔재를 복구 대상으로 세면 안 됩니다

## 범위 밖

- `#1085` QA 잔재 정리 (별도 chore)
- 품목 상태 축 (`#1133` 머지 완료)

연관 Issue: #1051, #1086
