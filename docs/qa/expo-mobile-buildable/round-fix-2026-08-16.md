# PR #1246 / 이슈 #1235 라운드 fix 보고서

## ① 환경 확인

요청된 명령과 원문 출력:

```text
cd C:\dev\Samhan-Public\.claude\worktrees\w1235
git rev-parse HEAD                 # 8e1672cbb (main 병합 직후)
git rev-parse --abbrev-ref HEAD    # feat/expo-mobile-buildable
git status --porcelain             # 비어 있어야 한다
```

실행 원문:

```text
8e1672cbbd5c4fb1d966347b86404710c1e9997a
feat/expo-mobile-buildable

```

초기 status는 빈 출력이었다.

## ② RED 원문

### A — `BUILD_ENV` 미지정 우회

RED 테스트 실행 원문:

```text
✖ sales release export rejects a token when BUILD_ENV is not specified
  AssertionError [ERR_ASSERTION]: ...
  actual: 0
  expected: 0
```

같은 실행에서 확인된 기존 경로:

```text
✔ production web export rejects a sales access token before it can enter the bundle
✔ preview sales web export rejects a sales access token before it can enter the bundle
✔ development web export still permits the sales access token for local QA
✔ release export rejects a token when APP_VARIANT is not specified
```

즉 미지정 `BUILD_ENV`만 `exit 0`으로 export되고 sentinel bundle이 생성되었다.

### B — 거래처 검색

추가한 RED 통합 테스트는 다음 계약을 요구한다.

```text
GET /api/v1/partners/quick-search?q=P-2026-QUICK&size=20
SALES → 200 + $.data[0].id/partnerCode/partnerName
```

수정 전 소스에는 해당 `@GetMapping("/quick-search")`가 없었고, 기존 검색 매핑은 `/admin/partners/search`, 내부 directory는 `/internal/partners/list`뿐이었다. 따라서 404의 근원은 클라이언트 URL이 아니라 partner-service 컨트롤러 부재였다. 게이트웨이에는 `/api/v1/partners/**` no-strip 라우트가 있었다.

## ③ 근원(파일:줄)

- A: `clients/mobile-staff/app.config.js:29-39` — `BUILD_ENV`/`APP_VARIANT` fallback 및 `APP_VARIANT === 'sales'` 조건이 release guard를 우회시켰다.
- A 테스트: `clients/mobile-staff/scripts/release-sales-token.test.cjs:102` — 미지정·대소문자·공백·prod 계열 sweep.
- B: `services/partner-service/src/main/java/com/samhanair/logis/partner/web/PartnerQuickSearchController.java:30` — 누락되어 있던 public sales quick-search 매핑.
- B 응답 계약: `services/partner-service/src/main/java/com/samhanair/logis/partner/dto/PartnerQuickSearchResponse.java:7`.
- B 회귀: `services/partner-service/src/test/java/com/samhanair/logis/partner/it/PartnerAdminControllerIT.java:140`.

## ④ 고친 것

- `EXPO_PUBLIC_SALES_ACCESS_TOKEN`은 `BUILD_ENV`가 명시적으로 `development`일 때만 허용한다. `BUILD_ENV` 미지정, production/PRODUCTION/prod, preview, 빈 문자열, 공백 변형은 모두 차단한다.
- guard를 `APP_VARIANT`와 무관하게 적용해 staff 기본 분기(`EstimateWebViewScreen`)를 유지하면서도 token bundle 우회를 차단했다.
- `GET /api/v1/partners/quick-search?q=&size=`를 추가하고 `PartnerService.listDirectory`의 ACTIVE 거래처 검색을 재사용했다. 응답은 모바일 계약(`id`, `partnerCode`, `partnerName`, `representativeName`, `phone`)으로 변환한다.
- 커밋, push, `git add`는 수행하지 않았다.

## ⑤ GREEN 양방향 회귀표

| 영역 | 차단/정상 경로 | 결과 |
|---|---|---|
| release guard | production + token | PASS — export non-zero, sentinel 0 |
| release guard | preview + token | PASS — export non-zero, sentinel 0 |
| release guard | BUILD_ENV unset + token | PASS — export non-zero, sentinel 0 |
| release guard | APP_VARIANT unset + production + token | PASS — export non-zero, sentinel 0 |
| release guard | sales + development + token | PASS — export 0, sentinel 유지 |
| mobile Jest | 전체 | PASS — 6 suites / 17 tests |
| partner-service | 전체 Gradle test | PASS — BUILD SUCCESSFUL |
| Web export | staff + production | PASS — exit 0 |
| Web export | sales + production, token unset | PASS — exit 0 |
| client typecheck | mobile-staff `npm run typecheck` | PASS — exit 0 |
| source hygiene | `git diff --check` | PASS — 출력 없음 |

실제 공유 게이트웨이 확인:

```text
GET http://localhost:8080/api/v1/partners/quick-search?q=삼한&size=20
STATUS=401
```

무인증 요청은 인증 계층에서 401로 종료되었고, 수정된 partner-service의 SALES MockMvc/DB 통합 테스트에서 실제 목록 200 응답을 확인했다. 따라서 404 라우팅 결함은 해소되고 인증은 유지된다.

## ⑥ 계열 sweep 카운트

총 8개 조합을 export 실측했다.

| 조합 계열 | 차단 | 허용 |
|---|---:|---:|
| BUILD_ENV unset / production / PRODUCTION / ` prod ` / preview / empty | 6 | 0 |
| 명시적 development (` development `, `DEVELOPMENT`) | 0 | 2 |
| 합계 | 6 | 2 |

모든 6개 비-development 계열은 sentinel 0이며, 2개 development 계열만 sentinel이 유지된다. APP_VARIANT는 unset, sales, SALES, 공백 sales, staff, 빈 문자열을 포함해 guard가 variant에 의존하지 않음을 확인했다.

## ⑦ 증거 무결성 자기 고지

- RED를 수정 전에 실행했고, `BUILD_ENV` 미지정 경로의 실제 `exit 0`과 bundle sentinel 생성을 확인했다.
- GREEN은 수정 후 새로 실행한 원문 결과만 사용했다.
- attestation이 필요한 Gradle 실행에는 최신 `infrastructure/.env.local`의 값만 프로세스 환경으로 주입했으며 비밀값은 출력하지 않았다.
- 공유 GUI/Expo 서버는 동시 라운드 소유 가능성이 있어 종료하지 않았다. 이번 세션이 기동한 headless export, Gradle/Testcontainers는 종료되었다.

## ⑧ `git status --porcelain` 원문

최종 원문:

```text
 M clients/mobile-staff/app.config.js
 M clients/mobile-staff/scripts/release-sales-token.test.cjs
 M services/partner-service/src/test/java/com/samhanair/logis/partner/it/PartnerAdminControllerIT.java
?? services/partner-service/src/main/java/com/samhanair/logis/partner/dto/PartnerQuickSearchResponse.java
?? services/partner-service/src/main/java/com/samhanair/logis/partner/web/PartnerQuickSearchController.java
?? docs/qa/expo-mobile-buildable/round-fix-2026-08-16.md
```

PM이 위 파일만 검토 후 커밋하면 된다. 이번 세션 잔여 프로세스: **이번 세션 기동 기준 0개**. 공유 프로세스는 Expo web 1개와 Jest worker 집합이 남아 있으나 동시 라운드 소유 가능성이 있어 회수하지 않았다.
