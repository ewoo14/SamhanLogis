# #1016 알리고 주소록 라이브QA R1 — **PASS 아님**

PR #1037 · HEAD `40270ac02` · CI 42/42 green · SOL 도달 결함 0 · 머지 권고
그러나 **라이브QA 가 그 권고를 되돌렸습니다.**

## 0. 환경 확인

    컨테이너 Created           2026-08-06T13:48:45.697440695Z
    jar 내 AligoAddressBook*   8개
      AligoAddressBookClient(2) · MockAligoAddressBookClient · AligoAddressBookController
      AligoAddressBookDeliveryStatus · AligoAddressBookSyncResponse · AligoAddressBookSyncService
    notification_db flyway     10
    compose 프로젝트            infrastructure
    배포 jar 클래스 ↔ HEAD 40257... 일치 확인

실제 호출 경로

    프런트 baseURL  http://localhost:8080
    POST /api/v1/auth/login                              → 200
    GET  /admin/partners/export/aligo-csv                → 200
    POST /admin/notification/aligo/address-book/sync     → 200
    /api/v1/admin/... 경로                                → 404

## 1. 발화 조건

    알리고 CSV 데이터 행        4,988건 (전체 4,989행, 헤더 포함)
    동기화 chunk                100개 (50×99 + 38)
    주소록 동기화 이력          없음
    notification_audit_logs     0건
    QA 데이터 생성              없음

## 2. 시나리오 결과

### S1 주소록 동기화 정상 경로 — 부분 통과

    LOGIN_STATUS=200
    CSV_STATUS=200 · CSV_CONTENT_TYPE=text/csv;charset=UTF-8
    CSV_LINES=4989 · CSV_DATA_ROWS=4988
    CSV_HEADER=그룹명,이름,휴대폰,비고
    SYNC_STATUS=200
    SYNC_BODY={"success":true,"code":"OK","message":"성공",
               "data":{"added":0,"updated":0,"skipped":0,"failed":[],
                       "deliveryStatus":"NOT_DELIVERED"}}

컨테이너 로그

    [MockAligoAddressBookClient] dryRun chunk size=38
    AligoAddressBookSync 완료 — totalContacts=4988 chunks=100 added=0 updated=0 skipped=0 failed=0

### S2 실호출 여부 — 🔴 FAIL · 실호출 아님

    [MockAligoAddressBookClient] dryRun
    TODO: 알리고 주소록 API spec 확정 후 실 구현체 교체.
    deliveryStatus=NOT_DELIVERED

컨테이너 환경에 알리고 자격증명 변수가 존재하지 않습니다. 주소록은 외부에 등록되지 않았습니다.
응답이 `NOT_DELIVERED` 를 명시하므로 **조용한 성공 오표시는 피했습니다**. 그러나 PR 제목의
*"현행 mock client 실호출 전환"* 은 라이브 환경에서 충족되지 않았습니다.

### S3 실패 경로 — API 부분 통과 · GUI 판정 불가

    dev_accountant  SYNC_ERROR_STATUS=403  {"success":false,"code":"FORBIDDEN",...}
    dev_dispatch    SYNC_ERROR_STATUS=403
    INVALID_TOKEN_ERROR_STATUS=401

5xx·네트워크 오류와 화면의 오류 표시·재시도 동작은 실행하지 않았습니다.

### S4 대상 0건 대조군 — 미실행

DB 직접 INSERT 없이 0건 표본을 만들 수 없어 실행하지 않았습니다. **통과로 세지 않습니다.**

### S5 레거시 계승 대조 — 🔴 미충족

    라이브 CSV 항목   그룹명, 이름, 휴대폰, 비고        (4개)
    dev-report 의 레거시 CSV 항목   전화번호, 이름, 그룹  (3개)

또한 레거시의 지역·요일 세그먼트, 중복 제거, 작업 이력이 이번 라이브 경로에서 확인되지 않았습니다.
⚠️ 완전계승 정의상 *"화면이 다르게 생겼다"* 는 미계승 근거가 아니지만, **항목이 다른 것은 미계승**입니다.

### 스크린샷 — 미생성

브라우저 런타임에 연결 가능한 브라우저가 없었고 프런트 개발 서버도 열려 있지 않았습니다.
필수 GUI 캡처를 만들지 못했습니다. **텍스트 로그를 화면 증거로 대체하지 않았습니다.**

## 3. 최종 판정

**라이브QA PASS 아님. 머지 권고를 유지할 수 없습니다.**

    S1  API 정상 실행 · NOT_DELIVERED 확인
    S2  🔴 실 알리고 호출 없음
    S3  API 401/403 은 실패로 반환 · GUI·5xx·네트워크 미검증
    S4  미실행
    S5  🔴 레거시 완전계승 미확인 · CSV 항목 불일치 (4 vs 3)
    GUI 스크린샷  미생성

QA: CODEX LUNA 5.6 · 검증: OPUS 5 (PM)
