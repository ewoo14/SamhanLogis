# 라이브 QA blocker

이 디렉터리는 `dc-config-service` 정상/중단 양쪽의 실제 주문 확정 화면 캡처를 위한 위치다.

이번 세션에서는 공유 DB write 금지와 현재 실행 컨테이너가 다른 워크트리 이미지라는
조건이 동시에 있어 confirm 저장을 실행하지 않았다. P-QA-40 seed/인증도 현재 환경에
없어 라이브 화면을 가짜 이미지로 대체하지 않았다.

재개 조건: 현재 worktree 이미지로 격리 서버를 띄우고 write 가능한 전용 QA DB를 사용한 뒤,
Chromium-1217로 정상 600,000원과 dc-config 중단 503 메시지를 각각 캡처한다.
