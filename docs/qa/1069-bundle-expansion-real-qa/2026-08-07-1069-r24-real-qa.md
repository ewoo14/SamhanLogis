# PR #1077 / 이슈 #1069 판매전표 BUNDLE 전개 라이브QA R24

- 실행일: 2026-08-07 (Asia/Seoul)
- 워크트리: `C:\dev\Samhan-Public\.claude\worktrees\t1069`
- HEAD: `e6cfe932c`
- 원칙: 실 GUI 클릭·실 API만 사용. DB 직접 쓰기와 컨테이너 재시작 없음.

## 0. 환경 확인

```text
docker inspect -f '{{.Created}}' samhan-product-service
2026-08-06T17:15:21.32275051Z

docker inspect -f '{{.Created}}' samhan-slip-service
2026-08-06T15:31:39.484631273Z
```

두 컨테이너 모두 기대 생성 시각과 일치했고 `running/healthy`였다. 렌더러는 `VITE_MOCK_MODE`를 설정하지 않았고, GUI는 `http://127.0.0.1:8080` 실 API를 호출했다. 따라서 `hasMock=false`이다.

발화 조건 카운트는 PM 재확인 수치를 기준으로 기록한다.

```text
BUNDLE 총 344건
레거시 규칙 발화: is360 10건 · is4way 27건 · is1way 11건
```

이번 표본의 실 상품 API 응답:

```text
modelCode       AC060CS6PBH1SY
productType     BUNDLE
bundleMode      EXPAND
categoryKey     singleSets
selling_price   2,516,800원
delivery_price  1,660,000원
discountFlags   000000
```

`categoryKey=singleSets`는 `SlipFormPage`에서 `HOMEMULTI`/`COMMERCIAL_MULTI`가 아닌 `OTHER`로 매핑된다. 따라서 `calculateSlipDiscount`의 360 정액 분기 대상이다. 모델 코드가 `AC`로 시작하고 8번째·9번째 문자가 `6P`이므로 레거시 `is360=true`도 확인했다.

정액 거래처 실 API 응답:

```text
2568700899 · 주식회사 제이앤피공조 · threeSixty=₩70,000
2188601069 · (주)삼성에스에이씨비투비(더블유케이) · threeSixty=₩20,000
```

## S-A. 전개 base 재확인 — PASS

① 발화 조건: `BUNDLE 344건` 중 레거시 `is360` 발화 표본 `AC060CS6PBH1SY`, `bundleMode=EXPAND`, `categoryKey=singleSets`.

② 클릭 경로: 로그인 → 판매 → 판매관리 → 신규 판매전표 → 출고창고 `HQ-001` 선택 → 거래처 `1023108393` 선택 → 모델명 `AC060CS6PBH1SY` 입력·선택 → 자동 전개.

③ 실제 API·상태코드:

```text
GET 200 /admin/partners/search?q=1023108393&size=20
GET 200 /admin/partners/1023108393
GET 200 /api/v1/partner-dc-configs/1023108393
GET 200 /api/products?q=AC060CS6PBH1SY&size=20&usageScope=PARTNER_ORDER
GET 204 /slips/price-memory?... 
POST 200 /slips/expand-line
```

④ GUI 실제 표시 금액 (VAT 포함, `delivery_price` 기준):

```text
구성행: 616,975 + 925,050 + 104,060 + 13,915 = 1,660,000원
부모 delivery_price 컬럼 = 1,660,000원
부모 selling_price 컬럼 = 2,516,800원
```

⑤ 기대값 계산식: `delivery_price 1,660,000 = 616,975 + 925,050 + 104,060 + 13,915`.

⑥ 스크린샷: [r24-S-A-expanded-delivery-price-real-qa.png](screenshots/r24-S-A-expanded-delivery-price-real-qa.png)

⑦ 판정: **PASS**.

## S-B. 거래처 전환 재가격 — FAIL

① 발화 조건: 동일한 `AC060CS6PBH1SY`의 레거시 `is360=true`, 전환 거래처 `2568700899`의 실 `threeSixty=70,000원`, `categoryKey=singleSets → OTHER`.

② 클릭 경로: S-A 전개 상태에서 거래처 입력을 `2568700899`로 변경 → 자동완성 거래처 선택 → 재가격 완료 대기.

③ 실제 API·상태코드:

```text
GET 200 /admin/partners/search?q=2568700899&size=20
GET 200 /admin/partners/2568700899
GET 200 /api/v1/partner-dc-configs/2568700899
POST 200 /slips/expand-line
```

④ GUI 실제 표시 금액 (VAT 포함, 재가격 후): 구성행은 `616,975원 + 925,050원 + 104,060원 + 13,915원 = 1,660,000원`으로 그대로 표시됐다. `delivery_price` 컬럼 기준 부모 금액도 `1,660,000원`으로 그대로였다.

⑤ 기대값 계산식:

```text
delivery_price 1,660,000 − 360 정액DC 70,000 = 기대 재가격 1,590,000원
```

⑥ 스크린샷: [r24-S-B-partner-switch-70000-real-qa.png](screenshots/r24-S-B-partner-switch-70000-real-qa.png)

⑦ 판정: **FAIL** — DC API와 전개 API는 200이지만 GUI 재가격에 70,000원이 반영되지 않았다.

## S-C. 삭제행 보존 — PASS(삭제 보존) / FAIL(가격 재계산)

① 발화 조건: S-B와 동일한 `is360=true` 표본. 구성행 2번 `AC060CXAPBH1` 삭제 후 `2188601069`로 전환하며 `threeSixty=20,000원`을 확인했다.

② 클릭 경로: S-A 전개 → 거래처 `2568700899` 전환 → 구성행 2번 삭제 → 거래처 입력을 `2188601069`로 변경 → 자동완성 거래처 선택.

③ 실제 API·상태코드:

```text
GET 200 /admin/partners/search?q=2188601069&size=20
GET 200 /admin/partners/2188601069
GET 200 /api/v1/partner-dc-configs/2188601069
POST 200 /slips/expand-line
```

④ GUI 실제 표시 금액 (VAT 포함, 재가격 후): 삭제한 `AC060CXAPBH1` 행은 다시 나타나지 않았다. 남은 행은 `616,975원 + 104,060원 + 13,915원 = 734,950원`으로 표시됐다. `delivery_price` 기준으로 기대한 360 정액 차감은 화면에 반영되지 않았다.

⑤ 기대값 계산식:

```text
삭제 보존: AC060CXAPBH1 미존재 유지
가격 기대: 남은 구성행 합계 734,950 − 360 정액DC 20,000 = 714,950원
```

⑥ 스크린샷: [r24-S-C-delete-preserved-after-switch-real-qa.png](screenshots/r24-S-C-delete-preserved-after-switch-real-qa.png)

⑦ 판정: **PASS(삭제행 보존)** / **FAIL(거래처 전환 재가격)**.

## S-D. 저장 왕복 — PASS(보존) / FAIL(재가격 저장값)

① 발화 조건: S-C 상태. 배송주소를 입력해 저장 가능 상태로 만든 뒤, 삭제행 보존 및 거래처 `2188601069` 정액DC 조건을 유지했다.

② 클릭 경로: 배송주소 `서울특별시 강남구 테헤란로 152` 입력 → 저장 클릭 → 판매 목록에서 최신 `2026/08/07-10` 상세 클릭.

③ 실제 API·상태코드:

```text
POST 201 /slips
GET 200 /slips/query?slipType=OUTBOUND&...
GET 200 /slips/62283143-916f-48cf-827b-7a24049a71d9
GET 200 /api/v1/slips/.../audit-logs
GET 200 /api/v1/slips/.../revisions
```

④ GUI 실제 표시 금액 (VAT 포함, 저장 후 재오픈): 상세 화면에는 3행이 보존됐고 단가는 `616,975원`, `104,060원`, `13,915원`, 합계 `734,950원`이었다. 재오픈 후에도 동일했다. 상세 이력에는 `거래처 싱글세트 정액DC 20000원 적용`이 표시됐지만, 실제 라인 금액은 차감되지 않았다. 배송주소도 재오픈 화면에 표시됐다.

⑤ 기대값 계산식: 저장 왕복 보존 자체는 `734,950원 → 734,950원`으로 PASS. 다만 재가격 기대값은 `734,950 − 20,000 = 714,950원`이며 저장값은 이를 반영하지 않아 FAIL.

⑥ 스크린샷: [r24-S-D-after-save-real-qa.png](screenshots/r24-S-D-after-save-real-qa.png), [r24-S-D-roundtrip-reopened-real-qa.png](screenshots/r24-S-D-roundtrip-reopened-real-qa.png)

⑦ 판정: **PASS(저장·재오픈 금액 보존)** / **FAIL(정액 재가격 저장값)**.

## 종합 판정

- S-A: **PASS** — 구성행 합계가 `selling_price`가 아닌 `delivery_price` 컬럼과 일치.
- S-B: **FAIL** — `1,660,000 − 70,000 = 1,590,000원` 기대이나 실제 `1,660,000원`.
- S-C: **PASS(삭제행 보존)** / **FAIL(재가격)** — `AC060CXAPBH1` 삭제 상태는 유지됐으나 `734,950 − 20,000 = 714,950원`으로 재계산되지 않음.
- S-D: **PASS(저장 왕복 보존)** / **FAIL(재가격 저장값)** — 저장 전후 `734,950원` 보존은 됐으나 기대 `714,950원`이 아님.

종합 판정: **FAIL**. 이번 R24에서 PM이 정정한 레거시 발화 조건과 `delivery_price` 전개 base는 실제 GUI/API에서 일치했다. 별도 셋째 가능성으로 `categoryKey=singleSets`가 관측됐지만 FE의 실제 매핑은 `OTHER`이고 `AC060CS6PBH1SY`는 레거시 `is360=true`이므로 정액 경로를 배제하는 값은 아니다. API 응답과 GUI에는 정액 정보가 표시되지만 라인·부모 금액에는 차감이 반영되지 않았다.

## 신규 파일 목록

- `docs/qa/1069-bundle-expansion-real-qa/2026-08-07-1069-r24-real-qa.md`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r24-initial-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r24-after-login-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r24-login-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r24-new-slip-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r24-S-A-expanded-delivery-price-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r24-S-B-partner-switch-70000-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r24-S-C-delete-preserved-after-switch-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r24-S-D-after-save-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r24-S-D-roundtrip-reopened-real-qa.png`
