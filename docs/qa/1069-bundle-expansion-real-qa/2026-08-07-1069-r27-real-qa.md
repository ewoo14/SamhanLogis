# PR #1077 / 이슈 #1069 판매전표 BUNDLE 확장 라이브QA R27

- 실행일: 2026-08-07 (Asia/Seoul)
- 워크트리: `C:\dev\Samhan-Public\.claude\worktrees\t1069`
- 기준 HEAD: `d473ac888`
- 실행 수단: Playwright `_electron.launch()`로 `clients/desktop`을 직접 기동. Electron은 `out/main/index.js`와 `out/renderer/index.html`을 로드했다.

## 0. 번들 확인 및 환경 전제

실행 전에 다음을 확인했다.

```text
out/renderer/assets/index-CScraO8x.js  6,206,630 bytes  2026-08-07 03:25:11
grep -rl calculateBundleParentDiscount out/renderer/ → out/renderer/assets/index-CScraO8x.js
calculateBundleParentDiscount occurrences → 3
deliveryPrice marker → 포함
```

R25 헬퍼가 0건이 아니므로 중단 조건은 발생하지 않았다. 컨테이너 생성시각도 기대값과 일치했다.

```text
samhan-product-service: 2026-08-06T17:15:21.32275051Z
samhan-slip-service:    2026-08-06T15:31:39.484631273Z
```

`VITE_MOCK_MODE=1` 없이 실행했고 Electron renderer에서 `hasMock=false`를 확인했다. 로그인, 거래처/품목 조회, 세트 확장, 저장/재조회는 실 `localhost:8080` API로 발생했다. JWT 원문은 출력하거나 저장하지 않았다.

참고로 공통 부팅 API인 `/app/version`, `/app/notices/active`, `/logs/front`에는 503이 있었으나, 본 시나리오의 인증·거래처·품목·확장·저장·상세 조회 API는 아래에 기록한 상태로 응답했다.

## 발화 조건

```text
AC060CS6PBH1SY / categoryKey=singleSets / productType=BUNDLE
delivery_price 컬럼 = 1,660,000원
selling_price 컬럼 = 2,516,800원
fixedDiscountRate=None / hasVariableDiscount=true
legacy getModelFlags: AC + m[7]=6 + m[8]=P → is360=true

2568700899 주식회사 제이앤피공조
partner-dc-config: threeSixty=70,000원, fourWay=70,000원,
oneWay=50,000원, stand=70,000원

AM360AXVGHC1SY / delivery_price 컬럼 = 10,821,635원
AM 시작이므로 legacy 360 플래그 없음
```

## S-A. AC BUNDLE 전개 기본값

1. 발화 조건: `AC060CS6PBH1SY`를 `singleSets` BUNDLE로 선택. 부모의 권위 기준은 `delivery_price` 컬럼 1,660,000원이다.
2. 클릭 경로: 로그인 → 새 판매전표 → `HQ-001 본사창고` → 거래처 `1023108393` → 모델명 `AC060CS6PBH1SY` 입력/선택·자동 전개.
3. 실제 API·상태코드: `GET 200 /admin/partners/search?q=1023108393&size=20`, `GET 200 /api/v1/partner-dc-configs/1023108393`, `GET 200 /admin/partners/1023108393`, `GET 200 /api/products?q=AC060CS6PBH1SY&size=20&usageScope=PARTNER_ORDER`, `GET 204 /slips/price-memory`, `POST 200 /slips/expand-line`.
4. GUI 실제 표시 금액: 화면 `단가` 컬럼 기준 `616,975 + 925,050 + 104,060 + 13,915 = 1,660,000원`; 하단 `총` 컬럼도 `₩1,660,000`. `selling_price` 2,516,800원은 표시되지 않았다.
5. 기대값 계산식: 구성행 합계 = 부모 `delivery_price` 컬럼 = `1,660,000원`.
6. 스크린샷: [r27-S-A-expanded-real-qa.png](screenshots/r27-S-A-expanded-real-qa.png)
7. 판정: **PASS**.

## S-B. 거래처 전환 및 70,000원 DC

1. 발화 조건: 전개된 AC 구성행에서 거래처를 `2568700899 주식회사 제이앤피공조`로 전환. `threeSixty` DC 컬럼은 70,000원이다.
2. 클릭 경로: 거래처 입력란에 `2568700899` 입력 → 자동완성 항목 선택 → 거래처 전환 완료 대기.
3. 실제 API·상태코드: `GET 200 /admin/partners/search?q=2568700899&size=20`, `GET 200 /admin/partners/2568700899`, `GET 200 /api/v1/partner-dc-configs/2568700899`. 전환 후 가격 재적용 `POST /slips/price-memory/bulk`는 발생하지 않았고, 화면 단가도 변경되지 않았다.
4. GUI 실제 표시 금액: 화면 `단가` 컬럼 기준 구성행 합계와 하단 `총` 컬럼 모두 `1,660,000원`.
5. 기대값 계산식: 부모 기준 단가 = `delivery_price 1,660,000 - threeSixty DC 70,000 = 1,590,000원`.
6. 스크린샷: [r27-S-B-partner-switch-real-qa.png](screenshots/r27-S-B-partner-switch-real-qa.png)
7. 판정: **FAIL** — 거래처 DC API는 200이지만 화면 표시값은 1,590,000원이 아닌 1,660,000원이다.

## S-C. 구성행 삭제 상태 유지 및 재가격

1. 발화 조건: AC 구성행 2번을 삭제한 뒤 거래처를 다시 `1023108393`으로 바꾸고 `2568700899`로 전환.
2. 클릭 경로: `라인 2 삭제` → 거래처 `1023108393` 선택 → 거래처 `2568700899` 선택 → 가격 반영 대기.
3. 실제 API·상태코드: 두 거래처 전환에 대해 각 `GET 200 /admin/partners/search`, `GET 200 /admin/partners/{code}`, `GET 200 /api/v1/partner-dc-configs/{code}`. 전환 후 `price-memory/bulk` 재가격 API는 발생하지 않았다.
4. GUI 실제 표시 금액: 화면 `단가` 컬럼 기준 남은 1·3·4행은 `616,975 + 104,060 + 13,915 = 734,950원`; 하단 `총` 컬럼 `₩734,950`. 삭제된 2번 행은 남지 않아 삭제 상태 자체는 유지됐다.
5. 기대값 계산식: 삭제 상태를 유지하면서 부모 기준 재가격이 반영되어야 하며, 거래처 전환 후 부모 기준은 `1,660,000 - 70,000 = 1,590,000원`이어야 한다.
6. 스크린샷: [r27-S-C-delete-preserved-real-qa.png](screenshots/r27-S-C-delete-preserved-real-qa.png)
7. 판정: **FAIL** — 삭제 상태는 유지됐지만 거래처 DC 재가격이 반영되지 않았다.

## S-D. 저장 후 재오픈

1. 발화 조건: S-C 상태에서 배송주소를 입력하고 저장. R24의 disabled 상태 재확인을 위해 저장 버튼 상태를 먼저 확인했다.
2. 클릭 경로: 배송주소 `서울특별시 강남구 테헤란로 152` 입력 → 저장 버튼 상태 확인(`disabled=false`) → 저장 클릭 → 판매관리 첫 행의 `상세` 클릭으로 재오픈.
3. 실제 API·상태코드: `POST 201 /slips`; 저장 후 `GET 200 /slips/query?...`; 재오픈 시 `GET 200 /slips/{id}`, `GET 200 /api/v1/slips/{id}/audit-logs`, `GET 200 /api/v1/slips/{id}/revisions` 및 상세 협업 API 200.
4. GUI 실제 표시 금액: 저장 직전 화면 `총` 컬럼 `₩734,950`. 판매관리 목록의 금액 컬럼 `734,950원`. 재오픈 상세의 `합계(VAT포함)` 컬럼은 `616,975 + 104,060 + 13,915 = 734,950원`.
5. 기대값 계산식: 저장 후 재조회 부모 기준 단가 = `delivery_price 1,660,000 - threeSixty DC 70,000 = 1,590,000원` 보존.
6. 스크린샷: [r27-S-D-save-ready-real-qa.png](screenshots/r27-S-D-save-ready-real-qa.png), [r27-S-D-after-save-real-qa.png](screenshots/r27-S-D-after-save-real-qa.png), [r27-S-D-reopened-real-qa.png](screenshots/r27-S-D-reopened-real-qa.png)
7. 판정: **FAIL** — 저장 버튼 disabled 문제는 해소되어 실제 저장은 201이었으나, 저장 후 재조회값은 1,590,000원이 아닌 734,950원이다. 상세 audit log에는 “거래처 싱글세트 정액DC 70000원 적용” 문구가 있으나 실제 라인 `합계(VAT포함)` 합계와 목록 금액은 734,950원이다.

## S-E. AM 반대급부

1. 발화 조건: `AM360AXVGHC1SY` 선택 후 동일 거래처 `2568700899`로 전환. 부모 `delivery_price` 컬럼은 10,821,635원이며 `AM` 시작이라 legacy 360 플래그가 없다.
2. 클릭 경로: 새 판매전표 → `HQ-001 본사창고` → 거래처 `1023108393` → `AM360AXVGHC1SY` 입력/선택·전개 → 거래처 `2568700899` 선택.
3. 실제 API·상태코드: `GET 200 /api/products?q=AM360AXVGHC1SY&size=20&usageScope=PARTNER_ORDER`, `GET 204 /slips/price-memory`, `POST 200 /slips/expand-line`, 거래처 전환 `GET 200 /admin/partners/search`, `GET 200 /admin/partners/2568700899`, `GET 200 /api/v1/partner-dc-configs/2568700899`.
4. GUI 실제 표시 금액: 화면 `단가` 컬럼 기준 `4,418,315 + 6,403,320 = 10,821,635원`; 하단 `총` 컬럼 `₩10,821,635`. 70,000원 차감도 0원 전환도 발생하지 않았다.
5. 기대값 계산식: AM legacy 플래그 없음 → `delivery_price 10,821,635 - 0 = 10,821,635원`.
6. 스크린샷: [r27-S-E-am360-no-discount-real-qa.png](screenshots/r27-S-E-am360-no-discount-real-qa.png)
7. 판정: **PASS**.

## 종합 판정

**R27 FAIL** — S-A와 S-E의 번들 기준·반대급부는 PASS했지만, 거래처 DC 전환 후 AC BUNDLE 재가격이 GUI와 저장 재조회에 반영되지 않았다. S-B/S-C/S-D가 기대값과 불일치한다. 이번 결과는 최신 2026-08-07 03:25 번들을 직접 Electron으로 실행한 실측이며, R26의 낡은 번들 문제와는 별개다.

## 신규 파일 목록

- `docs/qa/1069-bundle-expansion-real-qa/2026-08-07-1069-r27-real-qa.md`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-boot-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-after-login-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-new-slip-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-S-A-expanded-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-S-B-partner-switch-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-S-C-delete-preserved-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-S-D-save-ready-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-S-D-after-save-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-S-D-sales-list-reopen-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-S-D-reopened-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r27-S-E-am360-no-discount-real-qa.png`
- `clients/desktop/playwright/r27-live-qa.mjs`
- `clients/desktop/playwright/r27-reopen.mjs`
