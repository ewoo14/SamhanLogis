# PR #1077 / 이슈 #1069 판매전표 BUNDLE 전개 라이브QA R23

- 실행일: 2026-08-07 (Asia/Seoul)
- 워크트리: `C:\dev\Samhan-Public\.claude\worktrees\t1069`
- HEAD: `658f4f758`
- 원칙: 실 GUI 클릭·실 API·실 DB 읽기만 사용. DB 직접 쓰기/컨테이너 재시작 없음.

## 0. 환경 확인

```text
docker inspect -f '{{.Created}}' samhan-product-service
2026-08-06T17:15:21.32275051Z

docker inspect -f '{{.Created}}' samhan-slip-service
2026-08-06T15:31:39.484631273Z
```

두 값 모두 브리핑 기대 시각과 일치했다. 렌더러는 `VITE_MOCK_MODE`를 설정하지 않았고, 화면 동작은 `http://127.0.0.1:8080` 실 API를 호출했다. 따라서 `hasMock=false`이다.

코드(`VariableDiscountDetectorTest.java`)에서 `discountFlags` 첫 비트는 `is360`에 대응한다. 표본은 `AM360AXVGHC1SY`를 사용했다.

```text
product_db BUNDLE 전체                         344
  discount_flags <> '000000'                    7
  delivery_price <> selling_price              344
  둘 다 만족                                      7

실 API 표본
modelName      AM360AXVGHC1SY
productType    BUNDLE
bundleMode     EXPAND
sellingPrice   19,675,700원
deliveryPrice  10,821,635원
discountFlags  100000  (첫 비트 = 360)
```

정액 거래처는 `2568700899`를 사용했다. 실 DC API가 `threeSixty=₩70,000`을 반환했다. 비교용 무정액 거래처는 `1023108393`, 삭제 후 왕복용 거래처는 `2188601069`(`threeSixty=₩20,000`)를 사용했다.

## S-A. BUNDLE 전개 base — PASS

① 발화 조건: 위 조건의 실 발화 BUNDLE 7건 중 `AM360AXVGHC1SY` 선택. `discountFlags=100000`이고 첫 비트가 360임을 코드와 실 API로 확인했다.

② 클릭 경로: 로그인 → 판매 → 새 판매전표 → 출고창고 `HQ-001 본사창고` → 거래처 `1023108393` 선택 → 모델명 `AM360AXVGHC1SY` 입력/선택 → 자동 전개.

③ 실제 API·상태코드:

```text
GET 200 /admin/partners/search?q=1023108393&size=20
GET 200 /admin/partners/1023108393
GET 200 /api/v1/partner-dc-configs/1023108393
GET 200 /api/products?q=AM360AXVGHC1SY&size=20&usageScope=PARTNER_ORDER
GET 204 /slips/price-memory?...  (기억 단가 없음)
POST 200 /slips/expand-line
```

④ GUI 실제 표시 금액: 구성행은 `4,418,315원 + 6,403,320원 = 10,821,635원`으로 표시됐다. 이 합계는 부모 `selling_price`가 아니라 부모 `delivery_price` 컬럼 기준이다.

⑤ 기대값: `delivery_price 10,821,635 = 4,418,315 + 6,403,320`.

⑥ [S-A 전개 캡처](screenshots/r23-S-A-expanded-delivery-price-real-qa.png)

⑦ 판정: **PASS**.

## S-B. 거래처 전환 재가격 — FAIL

① 발화 조건: `discountFlags=100000` → 360 정액 경로. 거래처 `2568700899` 실 API 응답은 `threeSixty=70,000원`이다.

② 클릭 경로: S-A 전개 상태에서 거래처 입력을 `2568700899 주식회사 제이앤피공조`로 변경하고 자동완성 항목을 클릭.

③ 실제 API·상태코드:

```text
GET 200 /admin/partners/search?q=2568700899&size=20
GET 200 /admin/partners/2568700899
GET 200 /api/v1/partner-dc-configs/2568700899
POST 200 /slips/expand-line
```

④ GUI 실제 표시 금액: 거래처 전환 후에도 구성행은 `4,418,315원`, `6,403,320원`, 총 `10,821,635원`으로 그대로였다. 70,000원 차감이 화면에 반영되지 않았다.

⑤ 기대값 계산식:

```text
delivery_price 10,821,635 − 360 정액DC 70,000
= 기대 부모 재가격 10,751,635원
```

⑥ [S-B 거래처 전환 캡처](screenshots/r23-S-B-partner-switch-70000-real-qa.png)

⑦ 판정: **FAIL**. DC 설정 응답과 재전개 응답은 200이었으나 FE 표시·저장 단가가 재가격되지 않았다.

## S-C. 삭제행 보존 — PASS(삭제 상태 한정) / FAIL(가격 축)

① 발화 조건: S-B 표본과 동일한 `AM360AXVGHC1SY`, 이후 거래처 `2188601069`의 실 `threeSixty=20,000원`도 확인했다.

② 클릭 경로: 전개 2행에서 2번 구성행 `AM220AXVGHC1`을 삭제 → 거래처 `2188601069`로 전환.

③ 실제 API·상태코드:

```text
GET 200 /admin/partners/search?q=2188601069&size=20
GET 200 /admin/partners/2188601069
GET 200 /api/v1/partner-dc-configs/2188601069
POST 200 /slips/expand-line
```

④ GUI 실제 표시 금액: 삭제한 2번 구성행은 다시 나타나지 않았다. 남은 1번 행은 `4,418,315원`, 총액도 `4,418,315원`이었다. 이는 삭제 상태는 보존됐지만 정액 재가격은 적용되지 않은 상태다.

⑤ 기대값 계산식: 삭제 상태는 `AM220AXVGHC1` 미존재 유지. 가격까지 포함하면 전환 거래처의 360 정액 `20,000원`을 적용한 값이어야 하나 실제 1번 행은 `4,418,315원`으로 변하지 않았다.

⑥ [S-C 삭제행 보존 캡처](screenshots/r23-S-C-delete-preserved-after-switch-real-qa.png)

⑦ 판정: **PASS(삭제행 보존)**, **FAIL(거래처 전환 재가격)**.

## S-D. 저장 왕복 — PASS(실 저장 왕복) / 기대 재가격은 FAIL

① 발화 조건: S-C 상태. R22와 달리 저장 버튼의 직접 원인을 확인하기 위해 필수 배송주소를 입력했다.

② 클릭 경로: 배송주소 입력란에 `서울특별시 강남구 테헤란로 152` 입력 → 저장 버튼 활성화 확인 → 저장 클릭 → 판매 목록에서 `2026/08/07-9` 상세 버튼 클릭.

③ 실제 API·상태코드:

```text
POST 201 /slips
GET 200 /slips/query?...           (목록 갱신)
GET 200 /slips/70489f2f-8555-4310-9ac3-13f09494a245
GET 200 /api/v1/slips/.../audit-logs
GET 200 /api/v1/slips/.../revisions
```

저장 전에는 배송주소 미입력으로 저장 버튼이 disabled였다. 배송주소 입력 후 enabled가 되었고 `POST /slips`가 발행됐다. 생성 전표는 `2026/08/07-9`, 상태 `DRAFT`였다.

④ GUI 실제 표시 금액: 저장 전·상세 재오픈 후 모두 남은 구성행 `AM140AXVGHC1`의 단가/합계 `4,418,315원`이었다. 재오픈 화면에도 동일 금액이 표시됐다. 단 S-B 기대값인 정액 차감 가격은 저장되지 않았다.

⑤ 기대값 계산식: 저장 왕복 자체는 `4,418,315원 → 4,418,315원`으로 보존. 다만 S-B가 요구한 부모 기준은 `10,821,635 − 70,000 = 10,751,635원`이며 실제 저장 전표는 해당 재가격을 반영하지 않았다.

⑥ [S-D 저장 전 필수 입력 캡처](screenshots/r23-S-D-save-ready-required-input-real-qa.png), [S-D 저장 후 목록 캡처](screenshots/r23-S-D-after-save-real-qa.png), [S-D 상세 재오픈 캡처](screenshots/r23-S-D-roundtrip-reopened-real-qa.png)

⑦ 판정: **PASS(필수 입력 보완 후 POST/재오픈 왕복)**, **FAIL(재가격 기대값)**.

## 종합 판정

- S-A: **PASS** — `delivery_price` 기준 전개 base가 GUI 구성행 합계에 반영됨.
- S-B: **FAIL** — 360 정액 70,000원 응답과 `expand-line` 200 이후에도 총액이 10,821,635원 그대로임.
- S-C: **PASS(삭제 보존)** / **FAIL(재가격)**.
- S-D: **PASS(저장 왕복)** / **FAIL(재가격 저장값)**. R22의 disabled 원인은 배송주소 필수 입력 누락이었다.

종합 판정: **FAIL**. 이번 R23에서 백엔드 배포 시각 전제와 발화 조건은 모두 맞았고, S-A는 배포 갱신 후 정상화됐다. 남은 결함은 거래처 전환 시 `discountFlags=100000`(360) 정액을 delivery 기준 금액에 반영하지 않는 재가격 경로다.

## 신규 파일 목록

- `docs/qa/1069-bundle-expansion-real-qa/2026-08-07-1069-r23-real-qa.md`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r23-initial-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r23-S-A-start-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r23-S-A-selection-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r23-S-A-expanded-delivery-price-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r23-S-B-partner-switch-70000-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r23-S-C-delete-preserved-after-switch-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r23-S-D-save-ready-required-input-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r23-S-D-after-save-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r23-S-D-roundtrip-reopened-real-qa.png`
