# PR #1077 / 이슈 #1069 판매전표 BUNDLE 전개 라이브QA R22

- 실행일: 2026-08-07 (Asia/Seoul)
- 워크트리: `C:\dev\Samhan-Public\.claude\worktrees\t1069`
- HEAD: `3259c8076`
- 원칙: 실 Electron GUI·실 API·실 DB 읽기만 사용. mock 사용 없음. DB 직접 쓰기 없음.

## 0. 환경 확인 — 발화 조건을 먼저 확인

### 백엔드 재배포 금지 전제

요청받은 PM 확인 전제대로 백엔드 재배포·재빌드·재시작을 하지 않았다.

```text
docker inspect -f '{{.Created}}' samhan-slip-service
2026-08-06T15:31:39.484631273Z
```

`samhan-slip-service`는 위 시각의 배포본을 그대로 사용했다.

### 발화 조건 카운트

```text
product_db
조건: deleted_at IS NULL AND product_type='BUNDLE'
      AND delivery_price IS NOT NULL
      AND selling_price IS DISTINCT FROM delivery_price
결과: 344건

dc_config_db (is_deleted=false)
d360=45 · d4way=46 · dstand=45 · home_rate=159
```

PM 제시값 `45 · 46 · 45 · 159`는 일치했다. BUNDLE은 PM의 최소 조건 `8건 이상`을 충족했다.

### 표본과 분류

표본은 `AC052CS1PBH1SY`를 사용했다.

```text
products.selling_price  = 2,151,600원
products.delivery_price = 1,460,000원
product_type             = BUNDLE
product_category         = SINGLE_SET
category_id              = ...1004
```

구성품은 `AC052CN1PBH1`, `AC052CX1PBH1`, `PC1BWSK3NW`, `AR-EH05`가 전개되었고 모두 동일한 `category_id=...1004` 분류였다. 따라서 분류 없는 조합이 아닌 실제 발화 조합이다. S-B 거래처 `1060818309`의 실 설정은 `oneWay=30,000원`, `threeSixty=30,000원`, `fourWay=30,000원`, `stand=30,000원`, `homeMultiDc=45%`였다.

### Electron·mock 여부·실 API 확인

워크트리의 Electron 바이너리만 로컬 복구한 뒤 Electron 앱을 실행했다. `VITE_MOCK_MODE`는 빈 값으로 실행했고, renderer가 호출한 실 API는 모두 `http://localhost:8080`이었다. 따라서 이 라운드 캡처의 `hasMock=false`이다.

주요 실 API 상태:

```text
POST 200 /auth/login
GET 200  /inventory/warehouses
GET 200  /admin/partners/search?q=1023108393&size=20
GET 200  /admin/partners/1023108393
GET 200  /api/v1/partner-dc-configs/1023108393
GET 200  /api/products?q=AC052CS1PBH1SY&size=20&usageScope=PARTNER_ORDER
POST 200 /slips/expand-line
GET 200  /admin/partners/search?q=1060818309&size=20
GET 200  /admin/partners/1060818309
GET 200  /api/v1/partner-dc-configs/1060818309
POST 200 /slips/price-memory/bulk
GET 204  /slips/price-memory?...  (저장 단가 없음)
```

부가적으로 `/app/version`, `/app/notices/active`, `/logs/front`에 503이 있었으나 로그인·상품 조회·BUNDLE 전개 경로와 무관했다. JWT 원문은 출력·저장하지 않았다.

## S-A. BUNDLE 전개 base

### ① 발화 조건 카운트

위 환경 조건 충족: BUNDLE 344건, 정액 360 45건 등. 선택 표본은 분류가 있는 `AC052CS1PBH1SY`이다.

### ② 실제 GUI 클릭 경로

Electron 로그인 → `판매 > 새 판매전표` → 출고창고 `본사` → DC가 없는 거래처 `1023108393 에어컨J-김경진` → 품목 검색 `AC052CS1PBH1SY` 선택 → 자동 전개.

### ③ 네트워크 실제 API·상태코드

`GET /api/products` 200 응답에 두 가격이 함께 있었다. `POST /slips/expand-line` 200 응답 구성행 단가는 `807,895 + 1,211,210 + 118,580 + 13,915 = 2,151,600원`이었다.

### ④ GUI 실제 표시 금액

화면 구성행은 다음과 같이 표시됐다. 모두 `selling_price` 축 합계다.

```text
807,895 + 1,211,210 + 118,580 + 13,915 = 2,151,600원
화면 총액 = 2,151,600원
```

### ⑤ 기대값 계산식

```text
기대: products.delivery_price 1,460,000원
실제: products.selling_price 2,151,600원
차이: 691,600원
```

### ⑥ 스크린샷

[r22-S-A-expanded-delivery-price-real-qa.png](screenshots/r22-S-A-expanded-delivery-price-real-qa.png)

### ⑦ 판정: **FAIL**

실 API가 `deliveryPrice=1,460,000`을 제공했지만 실제 전개 및 GUI 합계는 `selling_price=2,151,600`이었다. R21 기대 동작을 만족하지 않는다.

## S-B. 거래처 전환 재가격

### ① 발화 조건 카운트

정액 360 45건, 4way 46건, stand 45건, home 요율 159건. 선택 거래처 `1060818309`는 oneWay/360/4way/stand 각 30,000원을 실제 보유했다.

### ② 실제 GUI 클릭 경로

S-A 전개 상태에서 거래처 입력을 `랜드유통(최경호) / 1060818309`로 전환했다.

### ③ 네트워크 실제 API·상태코드

거래처 검색·상세·DC 설정은 모두 200, `POST /slips/price-memory/bulk`도 200이었다. 그러나 거래처 전환 후 구성품 상품을 다시 조회하는 `GET /api/products` 또는 재전개 `POST /slips/expand-line`는 발생하지 않았다. `price-memory` 조회는 204였다.

### ④ GUI 실제 표시 금액

거래처 전환 직후 4개 구성행의 단가가 모두 `0`으로 바뀌었고 화면에 “카탈로그 판매가를 확인할 수 없습니다. 단가를 직접 입력해 주세요.”가 표시됐다. 화면 총액은 `0원`이었다.

### ⑤ 기대값 계산식

요청된 delivery 기준 기대값:

```text
products.delivery_price 1,460,000원 − oneWay 정액DC 30,000원
= 기대 부모 재가격 1,430,000원
```

실제 GUI: `0원`.

### ⑥ 스크린샷

[r22-S-B-partner-switch-delivery-minus-fixed-real-qa.png](screenshots/r22-S-B-partner-switch-delivery-minus-fixed-real-qa.png)

### ⑦ 판정: **FAIL**

기대값 `1,430,000원`으로 재가격되지 않았고, 오히려 구성행 단가가 0으로 소실되어 저장도 불가능했다. 추가로 확인된 셋째 상태는 “백엔드 DC 응답 실패”가 아니라, 거래처 전환 후 FE가 구성품 가격을 재조회하지 않고 0으로 만든 상태다.

## S-C. 삭제행 보존

### ① 발화 조건 카운트

S-B와 동일한 실 정액 거래처·분류 있는 BUNDLE 조합이다.

### ② 실제 GUI 클릭 경로

전개된 4행에서 `라인 3 삭제`를 클릭해 `PC1BWSK3NW`를 삭제 → 거래처를 다시 `1060818309`로 전환.

### ③ 네트워크 실제 API·상태코드

거래처 DC 설정 GET 200, `price-memory/bulk` POST 200. 전환 시 삭제행을 다시 만드는 `expand-line` 호출은 없었다.

### ④ GUI 실제 표시 금액

삭제 후 구성 모델은 `AC052CN1PBH1`, `AC052CX1PBH1`, `AR-EH05` 3개로 유지됐다. 삭제한 `PC1BWSK3NW`는 다시 나타나지 않았다. 다만 S-B의 영향으로 남은 행 단가는 0원이었다.

### ⑤ 기대값 계산식

삭제 보존 기대는 모델 행 수 기준이며, 재가격까지 포함하면 `1,460,000 − 30,000 = 1,430,000원` 부모 가격에서 삭제행 제외 상태여야 한다. 실제는 3행 보존은 했지만 가격이 0원이다.

### ⑥ 스크린샷

[r22-S-C-delete-preserved-after-switch-real-qa.png](screenshots/r22-S-C-delete-preserved-after-switch-real-qa.png)

### ⑦ 판정: **PASS (삭제 상태 한정) / 가격 축은 FAIL**

삭제한 구성행은 거래처 전환 뒤에도 재생성되지 않아 S-C의 “삭제 상태 유지” 조건은 PASS다. 단, 같은 화면에서 가격이 0원으로 소실된 것은 S-B FAIL의 연쇄 결과이며 별도 FAIL로 기록한다.

## S-D. 저장 왕복

### ① 발화 조건 카운트

S-B와 동일하다.

### ② 실제 GUI 클릭 경로

S-C 상태에서 메모를 입력하고 `저장` 버튼을 확인·클릭 시도 → 거래처 전환 후 가격 소실로 저장 버튼이 disabled 상태였다.

### ③ 네트워크 실제 API·상태코드

저장 시도에서 `POST /slips`는 발생하지 않았다. 따라서 저장 성공/상세 재조회 API도 없다.

### ④ GUI 실제 표시 금액

저장 전 화면 총액은 0원이며 저장 버튼은 disabled였다. 저장 후 재오픈할 전표가 생성되지 않았다.

### ⑤ 기대값 계산식

저장 가능 상태라면 S-B 기대 부모 금액은 `1,460,000 − 30,000 = 1,430,000원`이고, 삭제행을 제외한 구성행 합계도 그 저장 금액을 보존해야 한다. 실 저장이 발행되지 않아 왕복 비교값이 없다.

### ⑥ 스크린샷

[r22-S-D-save-attempt-real-qa.png](screenshots/r22-S-D-save-attempt-real-qa.png)

### ⑦ 판정: **판정불가**

가격 0원으로 인해 저장 버튼이 disabled였고 `POST /slips`가 발생하지 않았다. 저장 후 재오픈 금액을 검증할 수 없으므로 PASS/FAIL 대신 판정불가로 남긴다. 단 저장 불가의 직접 원인은 S-B의 재가격 실패다.

## 종합 판정

실 BUNDLE 조합과 실 정액 거래처는 발화했다. 백엔드 전제와 DC 카운트도 맞았고 renderer는 mock이 아닌 실 API를 호출했다. 그러나:

1. S-A: `delivery_price`가 아니라 `selling_price` 축으로 전개되어 FAIL.
2. S-B: 거래처 전환 후 기대 `1,430,000원`이 아니라 구성행 단가 0원, 저장 불가로 FAIL.
3. S-C: 삭제행 자체는 보존되어 PASS이나 가격 소실은 FAIL.
4. S-D: 저장 요청 자체가 발행되지 않아 판정불가.

따라서 R22 종합은 **FAIL**이다. 이번 라운드에서는 백엔드 재배포·재시작·DB 직접 변경을 하지 않았다.

## 신규 파일 목록

- `docs/qa/1069-bundle-expansion-real-qa/2026-08-07-1069-r22-real-qa.md`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r22-A-initial-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r22-S-A-expanded-delivery-price-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r22-S-B-partner-switch-delivery-minus-fixed-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r22-S-C-delete-preserved-after-switch-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r22-S-D-save-attempt-real-qa.png`

모든 `-real-qa` 캡처는 Electron 실 GUI에서 생성했으며 `hasMock=false`이다.
