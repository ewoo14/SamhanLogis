# PR #1077 / 이슈 #1069 판매전표 BUNDLE 전개 라이브QA R26 재시도

- 실행일: 2026-08-07 (Asia/Seoul)
- 워크트리: `C:\dev\Samhan-Public\.claude\worktrees\t1069`
- 요청 HEAD: `5691e17ea`
- 원칙: 실 Electron GUI 클릭·실 API 관찰. DB 직접 쓰기·컨테이너 재빌드/재시작 없음.

## 실행 수단

1. **Playwright `_electron.launch({ args: ['.'] })` 직접 기동 성공** (`clients/desktop`에서 실행).
2. 앱 창과 preload가 연결된 `#/login` 및 `#/sales/new`를 확인하고 실제 GUI 입력·선택·삭제를 수행했다.
3. Vite+Chromium fallback 및 인앱 브라우저 discovery는 사용하지 않았다.

## 0. 환경 확인

```text
docker inspect -f '{{.Created}}' samhan-product-service
2026-08-06T17:15:21.32275051Z

docker inspect -f '{{.Created}}' samhan-slip-service
2026-08-06T15:31:39.484631273Z
```

두 컨테이너 생성 시각은 PM 기대값과 일치했다. 재빌드·재시작하지 않았다.

이번 Electron 실행은 `VITE_MOCK_MODE=1` 없이 기동했고, 로그인 및 품목/거래처 조회가 실제 `localhost` API로 발생했다. 따라서 이번 실행의 renderer `hasMock=false`이다. JWT 원문은 출력·저장하지 않았다.

## 발화 조건

```text
AC060CS6PBH1SY
categoryKey=singleSets, productType=BUNDLE
delivery_price 컬럼=1,660,000원, selling_price 컬럼=2,516,800원
fixedDiscountRate=None, hasVariableDiscount=true
legacy getModelFlags: AC + m[7]=6 + m[8]=P -> is360=true

2568700899 주식회사 제이앤피공조
GET /api/v1/partner-dc-configs/2568700899
threeSixty=70,000원, fourWay=70,000원, oneWay=50,000원,
stand=70,000원, homeMultiDc=None, commercialMultiDc=None

AM360AXVGHC1SY
delivery_price 컬럼=10,821,635원, AM 시작 -> legacy 360 플래그 없음
```

## S-A. AC 세트 전개 base

① 발화 조건: `AC060CS6PBH1SY`, BUNDLE, `singleSets`, `delivery_price` 컬럼 1,660,000원, `selling_price` 컬럼 2,516,800원, legacy `is360=true`.

② 클릭 경로: 로그인 → 대시보드 `새 판매전표` → 출고창고 `HQ-001 본사창고` 선택 → 거래처 `1023108393` 선택 → 모델명 `AC060CS6PBH1SY` 입력·선택 → 세트 구성 옵션 확인.

③ 실제 API·상태코드:

```text
GET 200 /admin/partners/search?q=1023108393&size=20
GET 200 /api/v1/partner-dc-configs/1023108393
GET 200 /admin/partners/1023108393
GET 200 /api/products?q=AC060CS6PBH1SY&size=20&usageScope=PARTNER_ORDER
GET 403 /slips/price-memory?partnerId=...&productId=...
POST 200 /slips/expand-line
```

④ GUI 실제 표시 금액 (화면의 `단가`/`합계` 컬럼 기준): `959,175 + 1,439,650 + 104,060 + 13,915 = 2,516,800원`. 화면 총액도 `₩2,516,800`이었다. 이는 기대한 부모 `delivery_price` 컬럼 1,660,000원이 아니라 부모 `selling_price` 컬럼 2,516,800원과 일치한다.

⑤ 기대값 계산식: 구성행 합계는 부모 `delivery_price` 컬럼 기준 `1,660,000원`이어야 한다.

⑥ 스크린샷: [r26-S-A-expanded-delivery-price-real-qa.png](screenshots/r26-S-A-expanded-delivery-price-real-qa.png)

⑦ 판정: **FAIL** — 실제 GUI 구성행 합계가 `delivery_price`가 아닌 `selling_price` 컬럼 기준이다.

## S-B. 2568700899 전환 재가격

① 발화 조건: S-A의 AC BUNDLE 상태에서 거래처를 `2568700899 주식회사 제이앤피공조`로 전환. DC API의 `threeSixty`는 70,000원.

② 클릭 경로: 전개 완료 상태 → 거래처 입력란에 `2568700899` 입력 → 자동완성 항목 선택 → “거래처 변경으로 최근단가 재적용” 완료 대기.

③ 실제 API·상태코드:

```text
GET 200 /admin/partners/search?q=2568700899&size=20
GET 200 /api/v1/partner-dc-configs/2568700899
GET 200 /admin/partners/2568700899
POST 200 /slips/expand-line   (S-A 전개)
GET 403 /slips/price-memory/bulk
```

전환 후 추가 `expand-line` 성공 응답은 관찰하지 못했다. 화면에는 카탈로그 판매가 누락 경고가 나타났다.

④ GUI 실제 표시 금액 (화면의 `단가`/`합계` 컬럼 기준): 전환 후 AC 구성행 4개가 모두 `단가 변경`, `0원`으로 표시되고 총액은 `₩0`이었다. 기대값 1,590,000원은 표시되지 않았다.

⑤ 기대값 계산식: `delivery_price 1,660,000 − threeSixty DC 70,000 = 1,590,000원`.

⑥ 스크린샷: [r26-S-B-partner-switch-70000-real-qa.png](screenshots/r26-S-B-partner-switch-70000-real-qa.png)

⑦ 판정: **FAIL** — `1,590,000원`이 아니라 4행 모두 0원이며, R25 fix가 실 화면에서 정상 동작한다고 볼 수 없다.

## S-C. 구성행 일부 삭제 후 전환

① 발화 조건: S-A와 동일한 AC BUNDLE. 2번 구성행을 GUI에서 삭제한 뒤 `2568700899`로 전환.

② 클릭 경로: S-A 전개 → 2번 구성행 삭제 버튼 클릭 → 거래처 입력 `2568700899` → 자동완성 항목 선택.

③ 실제 API·상태코드: 거래처 검색·상세·DC 설정은 각각 `GET 200`; 전환 뒤 가격 기억 bulk 경로는 `POST /slips/price-memory/bulk 403`으로 관찰됐다. DB 직접 조회나 쓰기는 하지 않았다.

④ GUI 실제 표시 금액 (화면의 `단가`/`합계` 컬럼 기준): 삭제한 2번 구성행은 다시 나타나지 않았다. 남은 1·2·3번 구성행은 각각 `0원`, 총액은 `₩0`이었다.

⑤ 기대값 계산식: 삭제 상태 유지 + 남은 구성행의 부모 기준 재가격이 적용되어야 한다. 전체 부모 기대값은 `1,660,000 − 70,000 = 1,590,000원`이며, 실제 화면은 0원이다.

⑥ 스크린샷: [r26-S-C-delete-preserved-after-switch-real-qa.png](screenshots/r26-S-C-delete-preserved-after-switch-real-qa.png)

⑦ 판정: **FAIL** — 삭제 상태는 PASS이나, 재가격 및 표시 금액은 FAIL이다.

## S-D. 저장 후 재오픈

① 발화 조건: S-C 상태에서 배송주소를 입력하고 저장 시도.

② 클릭 경로: 삭제·거래처 전환 상태 → 배송주소 입력 → 저장 버튼 상태 확인 → 저장 클릭 시도.

③ 실제 API·상태코드: 가격 재적용 경로에서 `POST /slips/price-memory/bulk 403`을 관찰했다. 저장 버튼은 disabled였고 `POST /slips`는 발생하지 않았다. 따라서 저장 후 상세 GET/재오픈까지 진행할 수 없었다.

④ GUI 실제 표시 금액 (화면의 `단가`/`합계` 컬럼 기준): 저장 전 화면은 남은 3행 모두 0원, 총액 `₩0`. 저장·재오픈 표시값은 생성되지 않았다.

⑤ 기대값 계산식: 저장 가능한 상태라면 부모 `delivery_price` 기준 `1,590,000원`이 저장·재조회되어야 한다.

⑥ 스크린샷: [r26-S-D-save-attempt-real-qa.png](screenshots/r26-S-D-save-attempt-real-qa.png)

⑦ 판정: **FAIL** — 저장 버튼 disabled로 저장 왕복 및 1,590,000원 보존을 달성하지 못했다.

## S-E. AM 반대급부

① 발화 조건: `AM360AXVGHC1SY`, `delivery_price` 컬럼 10,821,635원, `AM` 시작으로 legacy 360 플래그 없음. 동일 거래처 `2568700899`로 전환.

② 클릭 경로: 로그인 → 새 판매전표 → `HQ-001` 선택 → 거래처 `1023108393` 선택 → `AM360AXVGHC1SY` 입력·선택 → 거래처 `2568700899` 입력·선택.

③ 실제 API·상태코드:

```text
GET 200 /admin/partners/search?q=1023108393&size=20
GET 200 /api/v1/partner-dc-configs/1023108393
GET 200 /admin/partners/1023108393
GET 200 /api/products?q=AM360AXVGHC1SY&size=20&usageScope=PARTNER_ORDER
GET 403 /slips/price-memory?partnerId=...&productId=...
POST 200 /slips/expand-line
GET 200 /admin/partners/search?q=2568700899&size=20
GET 200 /api/v1/partner-dc-configs/2568700899
GET 200 /admin/partners/2568700899
POST 403 /slips/price-memory/bulk
```

④ GUI 실제 표시 금액 (화면의 `단가`/`합계` 컬럼 기준): 전환 후 2개 구성행이 `0원`, 총액 `₩0`으로 표시됐다. 기대한 10,821,635원 그대로도 아니며, 70,000원 차감 후 10,751,635원도 아니다.

⑤ 기대값 계산식: AM 시작·legacy 플래그 없음이므로 `10,821,635원 − 0원 = 10,821,635원`.

⑥ 스크린샷: [r26-S-E-am360-no-discount-real-qa.png](screenshots/r26-S-E-am360-no-discount-real-qa.png)

⑦ 판정: **FAIL** — 70,000원 차감 회귀 여부 이전에 가격이 0원으로 소실됐다.

## 종합 판정

**R26 FAIL.** Electron 직접 기동과 실 API 연결은 성공했지만, PM이 의심한 fix는 실 화면에서 확인되지 않았다.

- S-A: **FAIL** — 화면 합계 2,516,800원(`selling_price`) vs 기대 1,660,000원(`delivery_price`).
- S-B: **FAIL** — 기대 1,590,000원 대신 4행 0원.
- S-C: **FAIL** — 삭제행은 유지됐지만 남은 행·총액 0원.
- S-D: **FAIL** — 저장 disabled, `POST /slips` 및 재오픈 불가.
- S-E: **FAIL** — 기대 10,821,635원 그대로가 아니라 0원.

이번 결과는 단위 테스트 132/132 GREEN이나 R25 코드 변경이 실 Electron 경로에서 동작함을 증명하지 못한다는 PM 우려와 일치한다. 특히 `price-memory` 경로의 403과 전환 후 카탈로그 단가 0원 상태가 관찰되어, “코드는 맞지만 화면은 다르다”가 아니라 이번 라운드에서는 실제 화면이 명확히 기대값을 충족하지 못했다.

## 신규 파일 목록

- `docs/qa/1069-bundle-expansion-real-qa/2026-08-07-1069-r26-real-qa.md`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r26-electron-boot-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r26-after-login-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r26-new-slip-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r26-S-A-expanded-delivery-price-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r26-S-B-partner-switch-70000-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r26-S-C-delete-preserved-after-switch-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r26-S-D-save-attempt-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r26-S-E-am360-no-discount-real-qa.png`
