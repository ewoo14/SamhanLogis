# PR #1077 / Issue #1069 — 라이브QA R31

## 번들·컨테이너 확인 결과 및 실행 수단

- 확인 시각: 2026-08-07 (Asia/Seoul)
- PM 전달 상태: 데스크톱 번들 `out/` 2026-08-07 04:14 재빌드, `calculateBundleParentDiscount` 3건, `expansionPending` 7건; product-service 2026-08-06T19:14:52Z 재배포 및 R29 계측 제거; slip-service 2026-08-06T15:31:39Z 무변경.
- 번들 재확인: `clients/desktop/out/renderer/assets/index-mhlRYlx-.js` 수정 시각 2026-08-07 04:14:21, `expansionPending` 7건. 다른 작은 `index-*.js` 파일에는 0건이었으나 대형 renderer bundle에는 PM 기대값과 일치하는 7건이 있었다.
- 컨테이너/서비스: 재빌드·재기동하지 않았다. PM 전달 배포 시각은 위와 같이 기록했으며, QA 실행 중 변경하지 않았다.
- renderer mock 상태: `hasMock=false`로 판정. 근거는 로그인 시 fixture 응답이 아니라 실제 `http://localhost:8080/auth/login` 호출이 발생했고, 401이 반환된 것이다. mock fixture로 시나리오를 대체하지 않았다.
- 실행 수단: 기존 Electron CDP 세션은 아로로지스 배차 renderer(`:5175`)였고 대상 `/sales/new` 라우트가 없어 404였다. 실제 Samhan Public renderer(`http://127.0.0.1:5199`)를 Playwright Chromium으로 열어 UI를 실행했다.
- 인증 전제: 핸드오프에 기록된 `dev_manager/dev_p05_pass`, `dev_master/dev_p05_pass`를 각각 1회 시도했으나 둘 다 실제 `POST /auth/login` 401. 로그인 화면을 벗어나지 못해 아래 시나리오는 실행하지 않았다.

## 인증 차단 네트워크 증거

- `GET http://localhost:8080/auth/me` — 401
- `POST http://localhost:8080/auth/login` (`dev_manager`) — 401
- `POST http://localhost:8080/auth/login` (`dev_master`) — 401
- `GET http://localhost:8080/app/version?clientType=DESKTOP&currentVersion=2026%2F08%2F06-1` — 503
- expand-line 호출: 0회 (인증 전 단계에서 차단)

## 시나리오 판정

### S-A — AC060CS6PBH1SY 전개

1. 발화 조건: `categoryKey=singleSets`, `product_category=SINGLE_SET`, `product_type=BUNDLE`, `bundle_mode=EXPAND`.
2. 클릭 경로: 로그인 → 매출 전표 신규 → 품목 검색/전개 경로. 로그인 401로 진입 불가.
3. 실제 API·상태코드: expand-line 0회. 인증 API `POST /auth/login` 401.
4. GUI 표시 금액: 확인 불가. 기준 컬럼은 부모 `delivery_price`.
5. 기대값: 구성행 합계 `delivery_price` 1,660,000.
6. 스크린샷: 시나리오 증거 없음.
7. 판정: **판정불가** — 인증 전제 불일치.

### S-B — 거래처 2568700899 전환 및 재가격

1. 발화 조건: 거래처 `2568700899` 주식회사 제이앤피공조.
2. 클릭 경로: S-A 이후 거래처 선택/전환. 진입 불가.
3. 실제 API·상태코드: expand-line 0회; 인증 401.
4. GUI 표시 금액: 확인 불가. 기준은 재가격 후 `delivery_price`/화면 표시값.
5. 기대값: `1,660,000 - 70,000(threeSixty) = 1,590,000`.
6. 스크린샷: 시나리오 증거 없음.
7. 판정: **판정불가**.

### S-C — 구성행 일부 삭제 후 거래처 전환

1. 발화 조건: S-A 전개 후 구성행 일부 삭제, 이후 거래처 전환.
2. 클릭 경로: 전개 → 구성행 삭제 → 거래처 전환. 진입 불가.
3. 실제 API·상태코드: expand-line 0회; 인증 401.
4. GUI 표시 금액: 확인 불가. 기준은 삭제 유지 후 재가격된 `delivery_price`.
5. 기대값: 삭제 상태 유지 + 거래처 전환에 따른 S-B 재가격 반영.
6. 스크린샷: 시나리오 증거 없음.
7. 판정: **판정불가**.

### S-D — 저장 후 재오픈

1. 발화 조건: S-B 상태에서 저장 후 같은 전표 재오픈.
2. 클릭 경로: 저장 → 전표 목록/저장내역 → 재오픈. 진입 불가.
3. 실제 API·상태코드: 저장/조회 API 0회; 인증 401.
4. GUI 표시 금액: 확인 불가. 기준은 재오픈 후 재가격된 `delivery_price`.
5. 기대값: 재오픈 후에도 `1,590,000`.
6. 스크린샷: 시나리오 증거 없음.
7. 판정: **판정불가**.

### S-E — AM360AXVGHC1SY 반대급부

1. 발화 조건: `AM360AXVGHC1SY`, delivery `10,821,635`, 같은 거래처.
2. 클릭 경로: 품목 교체/추가 후 거래처 전환. 진입 불가.
3. 실제 API·상태코드: expand-line 0회; 인증 401.
4. GUI 표시 금액: 확인 불가. 기준 컬럼 `delivery_price`.
5. 기대값: `10,821,635` 그대로.
6. 스크린샷: 시나리오 증거 없음.
7. 판정: **판정불가**.

### S-F — 거래처 A → B → A 빠른 전환

1. 발화 조건: 동일 품목에서 거래처를 A → B → A로 빠르게 연속 전환.
2. 클릭 경로: 거래처 전환 3회 연속. 진입 불가.
3. 실제 API·상태코드: expand-line 0회; 인증 401.
4. GUI 표시 금액: 확인 불가. 기준은 최종 A 거래처 재가격 후 `delivery_price`.
5. 기대값: 최종 A의 DC가 반영된 금액.
6. 스크린샷: 시나리오 증거 없음.
7. 판정: **판정불가**.

## 신규 파일

- `docs/qa/1069-bundle-expansion-real-qa/2026-08-07-1069-r31-real-qa.md`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r31-preflight-renderer-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r31-preflight-sales-new-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r31-preflight-sales-slip-new-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r31-login-real-qa.png`

결론: **R31 라이브QA는 인증 전제 불일치로 판정불가**. 코드가 맞는지/화면이 맞는지/실 저장·재오픈이 맞는지는 이번 라운드에서 확인하지 못했다.
