# PR #1077 / Issue #1069 — 라이브QA R32

## 번들·컨테이너 확인 및 실행 수단

- 데스크톱 번들: `clients/desktop/out/renderer/assets/index-mhlRYlx-.js`, 6,208,517 bytes, `2026-08-07 04:14:21 KST` (`2026-08-06T19:14:21Z`). 브리핑 기준 `expansionPending 7건` 환경을 사용했고, 이번 실행은 전개 완료 후 GUI에서 pending 문구가 남지 않은 상태로 측정했다.
- `product-service`: `2026-08-07 04:14:52 KST` (`2026-08-06T19:14:52Z`). R29 계측 로그 제거 후 배포본.
- `slip-service`: `2026-08-07 00:31:39 KST` (`2026-08-06T15:31:39Z`). 무변경.
- `hasMock=false`: 번들의 `isMockMode()`가 `VITE_MOCK_MODE === "1" || false`로 평가되며, 실행 중 실제 `localhost:8080` API의 200/201/204 응답을 관측했다. `GET /app/version` 503은 별개 경고로 판정에서 제외했다.
- 실행 수단: Playwright Chromium으로 `http://localhost:5199`의 Samhan Public renderer를 열었다. `127.0.0.1:5199`에서는 `POST /auth/login` 200 뒤 인증 화면이 유지되지 않았고, `localhost:5199`에서는 같은 `:5199` renderer에서 로그인 후 정상 진입했다. 이는 renderer 호스트명별 인증 쿠키 scope 차이로 보이는 셋째 환경 요인이다. 계정은 `dev_manager / dev_p05_pass!`를 사용했다.
- API 원문 JWT와 내부 UUID는 보고서에 출력하지 않았다.

## 공통 발화 조건

- `AC060CS6PBH1SY`: `delivery_price` 컬럼 1,660,000원, `selling_price` 컬럼 2,516,800원, `product_category=SINGLE_SET`, `bundle_mode=EXPAND`, 레거시 `getModelFlags is360=true`.
- 거래처 `2568700899 주식회사 제이앤피공조`: DC API의 `threeSixty='₩70,000'`.
- 기대 계산: `delivery_price 1,660,000 - threeSixty 70,000 = 1,590,000원`.
- 화면 금액은 별도 표기가 없을 때 전표 하단 `총` 컬럼(부가세 포함 합계)을 기준으로 기록했다. 재오픈 상세는 라인 `합계(VAT포함)` 컬럼 합산을 기준으로 기록했다.

## S-A — AC060CS6PBH1SY 전개

1. 발화 조건: 무정액 거래처 `1023108393 에어컨J-김경진`, `AC060CS6PBH1SY` 선택.
2. 클릭 경로: 로그인 → `판매` → `새 판매전표` → 출고창고 `HQ-001 본사창고` 선택 → 거래처 `1023108393` 선택 → 라인 1에 모델명 입력 → 품목 자동완성 선택.
3. 실제 API·상태코드: `GET 200 /admin/partners/search?q=1023108393&size=20`, `GET 200 /admin/partners/1023108393`, `GET 200 /api/products?q=AC060CS6PBH1SY&size=20&usageScope=PARTNER_ORDER`, `GET 204 /slips/price-memory`, `POST 200 /slips/expand-line`. 전개당 `expand-line` 1회.
4. GUI 실제 표시 금액: 하단 `총` 컬럼 `₩1,660,000`; 구성행 `합계` 컬럼은 616,975 + 925,050 + 104,060 + 13,915 = 1,660,000원.
5. 기대값: `delivery_price 컬럼 1,660,000원`.
6. 스크린샷: [r32-S-A-AC060-expanded-real-qa.png](screenshots/r32-S-A-AC060-expanded-real-qa.png)
7. 판정: **PASS**.

## S-B — 거래처 전환 및 부모 재가격

1. 발화 조건: S-A 전개 상태에서 `2568700899 주식회사 제이앤피공조`로 전환, DC `threeSixty` 70,000원.
2. 클릭 경로: 거래처 입력란에 `2568700899` 입력 → 자동완성 `주식회사 제이앤피공조` 클릭 → 전환 완료 대기.
3. 실제 API·상태코드: `GET 200 /admin/partners/search?q=2568700899&size=20`, `GET 200 /admin/partners/2568700899`, `GET 200 /api/v1/partner-dc-configs/2568700899`, `POST 200 /slips/expand-line`. 전환당 `expand-line` 1회. 요청·응답 모두 200.
4. GUI 실제 표시 금액: 하단 `총` 컬럼 `₩1,660,000` (기대 1,590,000원보다 70,000원 높음). 구성행 합계 컬럼도 1,660,000원.
5. 기대값: `delivery_price 1,660,000 - threeSixty 70,000 = 1,590,000원`.
6. 스크린샷: [r32-S-B-JNP-reprice-real-qa.png](screenshots/r32-S-B-JNP-reprice-real-qa.png)
7. 판정: **FAIL** — `expand-line` 200과 DC 200 응답은 있으나 화면 재가격 미반영.

## S-C — 구성행 삭제 후 전환

1. 발화 조건: AC 구성행 2번(실외기, `합계` 컬럼 925,050원)을 GUI에서 삭제한 뒤 거래처를 `1023108393` → `2568700899`로 전환.
2. 클릭 경로: `라인 2 삭제` → 거래처 `1023108393` 선택 → 거래처 `2568700899` 선택 → 전환 완료 대기.
3. 실제 API·상태코드: 각 거래처 전환마다 `GET 200 /admin/partners/search`, `GET 200 /admin/partners/{businessNumber}`, `GET 200 /api/v1/partner-dc-configs/{businessNumber}`, `POST 200 /slips/expand-line`. A 전환 1회 + B 전환 1회, 총 `expand-line` 2회, 모두 200. 삭제 상태 자체는 클라이언트에서 유지됨.
4. GUI 실제 표시 금액: 구성행 3건이 유지되고 삭제한 실외기 행은 재생성되지 않았으나, 하단 `총` 컬럼은 `₩734,950` (616,975 + 104,060 + 13,915)이며 기대 재가격은 삭제 후 기준 `1,590,000 - 925,050 = 664,950원`으로 보는 것이 아니라, 구현 계약상 전개 부모의 재가격을 반영해야 한다. 결과적으로 DC 재가격이 전혀 반영되지 않았다.
5. 기대값: 삭제 상태 유지 + 전환 후 가격 재적용. DC 적용 부모 기준은 `1,590,000원`이어야 한다.
6. 스크린샷: [r32-S-C-delete-switch-real-qa.png](screenshots/r32-S-C-delete-switch-real-qa.png)
7. 판정: **FAIL** — 삭제 상태는 유지됐지만 재가격 미반영.

## S-D — 저장 후 재오픈

1. 발화 조건: S-C의 삭제 3행 상태와 `2568700899` 거래처 상태에서 저장 후 같은 전표를 재오픈.
2. 클릭 경로: `저장` → 판매 목록에서 가장 최근 `2026/08/07-15` 전표의 `상세` 클릭.
3. 실제 API·상태코드: `POST 201 /slips`; 재오픈 시 `GET 200 /slips/{id}`, `GET 200 /api/v1/slips/{id}/realtime`, `GET 200 /api/v1/slips/{id}/audit-logs`, `GET 200 /api/v1/slips/{id}/redline`. 저장 payload는 3개 라인으로 생성됐고 저장 성공 201.
4. GUI 실제 표시 금액: 재오픈 상세의 `합계(VAT포함)` 컬럼은 616,975 + 104,060 + 13,915 = `734,950원`. audit 화면에는 `거래처 싱글세트 정액DC 70000원 적용`이 기록됐지만 라인 금액에는 반영되지 않았다.
5. 기대값: 저장 payload와 재오픈 화면 모두 S-B의 부모 재가격 `1,590,000원` 기준을 보존해야 한다.
6. 스크린샷: [r32-S-D-reopen-real-qa.png](screenshots/r32-S-D-reopen-real-qa.png)
7. 판정: **FAIL** — 저장은 201이지만 재오픈 금액은 `1,590,000원`이 아니라 삭제 후 합계 `734,950원`이다. R27의 재오픈 `734,950` 문제가 재현됐다.

## S-E — AM 품목 반대급부

1. 발화 조건: `AM360AXVGHC1SY`, `delivery_price` 컬럼 10,821,635원, 모델명이 `AM`으로 시작해 legacy 360 플래그 없음. 이후 거래처 `2568700899`로 전환.
2. 클릭 경로: 새 판매전표 → `HQ-001 본사창고` → 거래처 `1023108393` → `AM360AXVGHC1SY` 선택·전개 → 거래처 `2568700899` 선택.
3. 실제 API·상태코드: 품목 조회 `GET 200`, 초기 전개 `POST 200 /slips/expand-line` 1회, 전환 `GET 200 /admin/partners/search`, `GET 200 /admin/partners/2568700899`, `GET 200 /api/v1/partner-dc-configs/2568700899`, `POST 200 /slips/expand-line` 1회. 전환당 `expand-line` 1회.
4. GUI 실제 표시 금액: 하단 `총` 컬럼 `₩10,821,635` (구성행 합계 컬럼 4,418,315 + 6,403,320).
5. 기대값: `AM` 품목은 legacy 360 플래그가 없으므로 `10,821,635원` 그대로.
6. 스크린샷: [r32-S-E-AM-unchanged-real-qa.png](screenshots/r32-S-E-AM-unchanged-real-qa.png)
7. 판정: **PASS**.

## S-F — 늦은 응답 경쟁 A→B→A

1. 발화 조건: A=`2568700899`(DC 70,000원), B=`1023108393`(무정액), AC bundle 전개 후 A → B → A를 짧은 간격으로 연속 전환.
2. 클릭 경로: AC 전개 → 거래처 `2568700899` 선택 → 즉시 `1023108393` 선택 → 즉시 `2568700899` 선택 → 모든 응답 도착 대기.
3. 실제 API·상태코드 및 순서: 전환당 `expand-line` 호출은 A 1회, B 1회, A 1회, 총 3회이며 모두 `POST 200 /slips/expand-line`이다. 호출/응답 도착 순서(상대 ms)는 A 요청 368 → A 응답 391, B 요청 748 → B 응답 767, 최종 A 요청 1121 → 최종 A 응답 1140. 중첩 응답은 발생하지 않았지만 최종 context 경로를 확인했다. 각 전환의 partner search/detail/DC API는 모두 200.
4. GUI 실제 표시 금액: 최종 A 상태인데 하단 `총` 컬럼 `₩1,660,000`; A의 기대 금액 `₩1,590,000`이 아니다.
5. 기대값: 마지막 A context가 승리하고 `1,660,000 - 70,000 = 1,590,000원`.
6. 스크린샷: [r32-S-F-A-B-A-race-real-qa.png](screenshots/r32-S-F-A-B-A-race-real-qa.png)
7. 판정: **FAIL** — 최종 거래처는 A로 돌아왔지만 A DC가 반영되지 않았다. 호출 순서는 정상이나 가격 결과가 실패했다.

## 종합 판정

**FAIL**. S-A 전개와 S-E legacy 비발화 반대급부는 PASS였으나, 핵심인 거래처 전환 재가격(S-B), 삭제 상태 전환(S-C), 저장 후 재오픈(S-D), A→B→A 경쟁(S-F)이 모두 `threeSixty 70,000원`을 금액에 반영하지 못했다. 특히 저장 API는 201이고 audit에는 DC 적용 문구가 있으나 재오픈 라인 `합계(VAT포함)` 컬럼은 `734,950원`으로 남아 R27 문제가 재현됐다.

## 신규 파일

- `docs/qa/1069-bundle-expansion-real-qa/2026-08-07-1069-r32-real-qa.md`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r32-S-A-AC060-expanded-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r32-S-B-JNP-reprice-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r32-S-C-delete-switch-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r32-S-D-reopen-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r32-S-E-AM-unchanged-real-qa.png`
- `docs/qa/1069-bundle-expansion-real-qa/screenshots/r32-S-F-A-B-A-race-real-qa.png`
