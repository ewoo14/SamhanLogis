# PR #1268 재판정 2회차 — CODEX SOL 리모컨 축

검증일: 2026-08-18  
판정 대상: `feat/option-naming-unify` / `1f4d8ce36b59fd6b76a42d41dde31965d768231d`

## ① 검증 SHA·main 병합

- 시작 HEAD와 PR head OID가 모두 `1f4d8ce36b59fd6b76a42d41dde31965d768231d`임을 확인했다.
- 시작 전 `git fetch origin` 후 `git merge origin/main --no-edit`를 실행했다. 결과는 `Already up to date.`였고 충돌은 없었다.
- 병합 시점 `origin/main`: `0ac75e010`.
- 브랜치 JAR로 `product-service`, `partner-order-service`를 각각 빌드했다. 결과: `BUILD SUCCESSFUL`.
- 코드 수정, `git add`, commit, push는 하지 않았다.

## ② 신규 variant 실험 ①~④ — 양쪽 웹 실측

공유 DB에는 쓰지 않았다. 격리 PostgreSQL의 `product_clone`에서 리모컨 설정 65행의 variant를, 실험 전 저장소 전체 고정문자 검색 결과가 0건인 `해오라기824731`로 바꾸고 `AWR-WG00N` 납품가를 `137,531원`으로 설정했다. 브랜치 `product-service`·`partner-order-service`와 종합견적서 renderer·주문서웹 renderer를 별도 포트로 띄웠고, `SAMHAN_GATEWAY_ATTESTATION`을 주입한 뒤 headless Chromium Playwright로 밟았다.

대상 세트는 양쪽 모두 `AC060CS6PBH1SY`이며, 리모컨 외 구성 소계는 `1,516,000원`이다.

| 확인 항목 | 종합견적서 | 주문서웹 | 판정 |
|---|---:|---:|---|
| ① 목록 | `기본 / 해오라기824731 / 유선` | `기본 / 해오라기824731 / 유선` | 양쪽 노출 |
| ② 선택 적용 | 선택값 `해오라기824731` | 선택값 `해오라기824731` | 양쪽 적용 |
| ③ 세트가 | `1,653,531원` | `1,653,531원` | 기준 대비 `+137,531원` |
| ④ 상세 단가 | `AWR-WG00N × 1`, `137,531원` | `AWR-WG00N × 1`, `137,531원` | 양쪽 설정 납품가 일치 |
| 상세 소계 | `1,653,531원` | `1,653,531원` | 양쪽 헤더와 일치 |

따라서 이번 라운드의 핵심인 **코드에 없던 신규 이름의 목록 → 선택 → 세트가 → 상세 단가 파이프라인은 양쪽 웹에서 도달 성공**했다.

## ③ 하드코딩 잔여 수

검사 기준은 단순히 이전 정규식 6개가 사라졌는지가 아니라, 리모컨 variant 이름을 모델 상수/모델 코드로 바꾸는 fallback 분기를 독립 검색하는 방식으로 잡았다. `기본`·`무선`·`유선`·`컬러` 이름을 `AR-EH05`·`AWR-WE13N`·`AWR-WG00N` 또는 리모컨 모델 상수로 대응하는 블록을 셌다.

- 종합견적서: **3블록** — `clients/web/estimate-app/views/index.ejs:4195`, `:4213`, `:8475`
- 주문서웹: **3블록** — `clients/web/order-app/index.html:2541`, `:2559`, `:5636`
- `legacyQuantityBoundary.js`: **0블록**
- 합계: **6블록**

이전의 선택 제한형 `if (opt === '유선' || opt === '컬러')` 분기는 0개이고 설정 resolver가 fallback보다 먼저 실행되므로, 위 신규 variant 라이브 실험은 통과했다. 그러나 이름→모델 하드코딩 자체는 6블록 남아 있어 fix 보고서의 광의의 “정적 잔여 0개”와 개발책임자의 “하드코딩만 안 되면 돼” 기준에는 맞지 않는다.

## ④ 범위 이탈 처리 — 판넬·360판넬

**부분 원복이다.**

- 360판넬 화면 목록은 양쪽 웹에서 다시 `원형 / 사각` 고정 목록이다. 라이브 화면에서도 `원형`을 확인했다.
- 판넬 목록 동적화는 원복되지 않았다. `comm_panel`, `home_panel`, `ss_panel`이 여전히 `d03ConfiguredVariants_(..., 'PANEL')` 결과로 만들어진다.
- 실제 화면에서 양쪽 모두 `판넬제외 / 기본 / 블랙 / 승강 / 공청`을 확인했다. 종전 고정 표기인 `기본판넬 / 블랙판넬 / 승강판넬 / 공청판넬`에서 표시·값이 바뀌며, 향후 설정 행 추가·누락이 판넬 선택 목록에 그대로 반영되는 변경이다.
- 이번 라이브 대상에서 판넬 선택·가격 계산이 깨지는 현상은 재현되지 않았다. 따라서 이 범위 이탈 자체를 도달 결함으로 세지는 않지만, “판넬 목록 원복 완료” 주장은 사실이 아니다.

## ⑤ CI 귀속 및 golden 판정

PR head `1f4d8ce36`의 GitHub Checks를 직접 확인했다.

| 작업 | 결과 | 실측 |
|---|---|---|
| 빌드 검증 + 단위 테스트 | 실패 | 견적 앱 `2 suites / 15 tests` 실패, `345/360` 통과 |
| Frontend Order-App | 실패 | `6 files / 49 tests` 실패, `208/257` 통과 |
| Desktop Playwright mock hard gate | 통과 | `*-real-qa.spec.ts`는 전역 `testIgnore`에 의해 mock 게이트에서 제외됨 |
| Flyway 불변 가드 및 나머지 완료 작업 | 통과 | 완료 작업 green |
| Playwright (web + electron + mobile emul) | 진행 중 | 보고서 작성 시점 pending |

- 두 실패 작업 모두 `Set up job` 실패가 아니다. 테스트가 실제 실행된 뒤 실패했으므로 GitHub 인프라 장애 귀속이 아니다.
- 로컬 exact 실행도 견적 `15 실패 / 345 통과`, 주문서 `49 실패 / 208 통과`로 CI와 일치했다.
- golden 실패는 원격 모델 `AR-EH05`, `AWR-WE13N`, `AWR-WG00N`의 기대 수량·target 보존 계약을 잃은 차이를 포함한다. 이 기대값을 새 출력에 맞춰 바꾸면 기존 동작 손실을 숨기므로 **기대값 변경은 옳지 않다**.
- 따라서 CI 전체 green은 아니며 현재 산출물은 merge gate를 통과하지 못한다.

## ⑥ 잃으면 안 되는 것 재현

격리 DB를 원래 이름·가격으로 되돌린 뒤 동일 세트를 양쪽 웹에서 다시 선택했다.

| 상태 | 리모컨 상세 | 종합견적서 세트가 = 상세 소계 | 주문서웹 세트가 = 상세 소계 |
|---|---:|---:|---:|
| 기본(무선) | `AR-EH05 × 1`, `16,000원` | `1,532,000원` | `1,532,000원` |
| 유선 | `AWR-WE13N × 1`, `56,000원` | `1,572,000원` | `1,572,000원` |
| 컬러 | `AWR-WG00N × 1`, `91,000원` | `1,607,000원` | `1,607,000원` |
| 제외 | 리모컨 없음, `0원` | `1,516,000원` | `1,516,000원` |

그 뒤 `AWR-WG00N` 설정 납품가만 `91,000 → 123,456원`으로 바꿨다.

- 종합견적서: 상세 `123,456원`, 세트가·상세 소계 `1,639,456원`
- 주문서웹: 상세 `123,456원`, 세트가·상세 소계 `1,639,456원`
- 세트가 변화: 양쪽 `+32,456원`, 설정 납품가 변화와 정확히 동일

헤더와 상세 구성품 소계는 모든 상태에서 같았고, #1241의 천원 단위 본체 배분을 포함한 나머지 소계는 유지됐다. 판넬 목록과 자재 `포함 / 별도`도 양쪽 화면에서 유지됨을 확인했다.

## ⑦ 가격 대조표

리모컨 축에서는 종전값과 DB값의 불일치가 없었다. 수정 요구 없이 실측표만 남긴다.

| 모델 | 종전값 | DB값 | 영향 세트수 |
|---|---:|---:|---:|
| AR-EH05 | 16,000 | 16,000 | 50 |
| AWR-WE13N | 56,000 | 56,000 | 65 |
| AWR-WG00N | 91,000 | 91,000 | 65 |

임의 실험값 `137,531원`과 관통 확인값 `123,456원`도 양쪽 웹에 그대로 반영됐으므로 리모컨 축의 추가 가격 불일치는 없다.

## ⑧ 마이그레이션 V48 3중 확인·fresh

| 확인면 | 마이그레이션 파일 수 | 최대 번호 | V48 수 | 중복 번호 |
|---|---:|---:|---:|---:|
| 검증 브랜치 | 47 | 48 | 1 | 0 |
| 최신 `origin/main` | 46 | 47 | 0 | 0 |
| 전체 `origin/*` 중 V48 보유 ref | 해당 ref 1개 | 48 | `origin/feat/option-naming-unify`만 1 | 충돌 ref 0 |

새 격리 DB `product_fresh`에 브랜치 JAR를 직접 기동해 fresh migration을 수행했다. Flyway 결과는 `성공 48 / 현재 48 / 실패 0`, V48 설명은 `remote color context delivery price`, 성공 여부 `true`였다. 기존 clone DB도 `성공 48 / 현재 48 / 실패 0`을 재확인했다.

## ⑨ 스크린샷 — 행 수·경로

모든 캡처는 `resolveQaShotsDir()`이 반환한 `docs/qa/1268-sol-reverdict-2/screenshots/_local/`에 저장했다. 총 **18장**이며, 생성 뒤 18장을 직접 열어 선택값·가격·상세 행을 확인했다.

| 묶음 | 장수 | 종합견적서 행 수 | 주문서웹 행 수 | 파일 |
|---|---:|---:|---:|---|
| 신규 variant | 3 | 목록 14 | 목록 1, 상세 3 | `variant-estimate-해오라기824731.png`, `variant-order-list-해오라기824731.png`, `variant-order-detail-해오라기824731.png` |
| 기존 4상태 | 12 | 상태별 목록 14 | 상태별 목록 1, 상세 `3 / 3 / 3 / 2` | `baseline-estimate-*`, `baseline-order-list-*`, `baseline-order-detail-*` |
| 123,456원 관통 | 3 | 목록 14 | 목록 1, 상세 3 | `changed-estimate-컬러.png`, `changed-order-list-컬러.png`, `changed-order-detail-컬러.png` |

수치 원문은 같은 디렉터리의 `variant-measurement.json`, `baseline-measurement.json`, `changed-measurement.json`에 있다.

## ⑩ 판정 불가 축

- 요청 핵심인 신규 variant ①~④ 및 기존 리모컨 4상태: 판정 불가 축 없음.
- GitHub `Playwright (web + electron + mobile emul)`: 보고서 작성 시점에도 진행 중이므로 최종 결과는 **판정 불가**.
- CI가 지적한 15개·49개 실패 시나리오 전체를 각각 실 사용자 화면으로 재현했는지는 **판정 불가**다. 라이브로 밟은 리모컨·판넬·자재 대상에서는 화면 결함을 재현하지 못했으며, 못 밟은 시나리오를 결함 0으로 환산하지 않았다.

## ⑪ 최종 판정

**머지 불가 — 이번 라이브에서 확인된 실사용자 도달 결함 0건.**

신규 임의 variant는 양쪽 웹에서 정상 관통했고 기존 리모컨 동작도 보존됐다. 그러나 다음 두 사유로 merge 가능 판정은 내릴 수 없다.

1. 개발책임자의 명시 기준과 달리 이름→모델 하드코딩 fallback이 **6블록** 남아 있다.
2. GitHub CI 필수 작업 **2개가 실제 테스트 실패**이고 전체 CI 1개는 아직 진행 중이다.

판넬 목록 범위 이탈도 완전 원복되지 않았지만, 이번 라이브에서 그것이 실제 화면 결함을 만드는 증거는 없으므로 도달 결함 수에는 넣지 않았다.

## ⑫ 프로세스 회수

- 기동한 포트 `28084`, `28094`, `38088`, `5183`, `5180`, `38100`: listener 0개 확인.
- 격리 컨테이너 `sol1268r2-pg`: 제거 후 잔존 0개 확인.
- 자체 임시 `.err`·`.log`·`.pid`: 잔존 0개 확인.
- 임시 Playwright spec/config 및 `test-results`: 제거.
- 공유 `samhan-*` 실행 컨테이너: 시작 상태와 같은 **24개 실행 중**. 중지·변경하지 않았다.
- 최종 `git status --short`: `?? docs/qa/1268-sol-reverdict-2/` 1건. 지정 보고서·스크린샷·수치 증거만 untracked이며 코드 파일 변경은 0건이다.
