# PR #1003 (Issue #1001) 기능 회귀 검토 2차

- 대상 SHA: `03624dcd65c7508fdb1e83b38369c515f9dfc20d`

## 조사 로그

- 중간 저장 파일을 조사 전에 생성했고 제목과 검증 대상 SHA만 먼저 기록했다.
- 적용 절차 확인: `superpowers:using-superpowers`, `superpowers:systematic-debugging`, `superpowers:verification-before-completion`. 구현·수정 없이 재현과 증거 확인만 수행한다.
- 대상 일치 확인: `git rev-parse HEAD`가 지정 SHA `03624dcd65c7508fdb1e83b38369c515f9dfc20d`와 일치한다. 조사 시작 시 작업트리 변경은 이 검토 파일(`??`)뿐이다.
- 직전 보고서와 fix 보고서를 대조했다. fix 커밋의 변경 범위는 `SlipPublishService`와 단건·병합 IT, fix 보고서뿐이며, 보고서 자체도 실데이터 16개 각각의 HTTP 재요청은 하지 않았다고 명시한다. 따라서 이번 검토에서는 16개 키를 개별 판정하는 별도 재현이 필요하다.
- 코드 경계 확인: 단건·병합 모두 요청 `deliveryAddress == null`일 때만 legacy 지문을 계산하고(`SlipPublishService.java:197-205`, `297-305`), 저장 전표의 `delivery_address IS NULL`이며 저장 지문과 legacy 지문이 같을 때 replay한다(`590-608`). 새 배송주소가 있는 요청은 legacy fallback에 들어가지 않으므로 주소가 실제로 다르면 strict 지문 불일치로 409가 된다.
- 추가 위험 후보: 단건/병합 현행·legacy 지문은 `shippingAddress`, `receiverPhone`, `partnerName`, 단건 `warehouseId`·`orderApprovedAt`, 병합 `sourceOrders.orderNo`, 라인의 `productName`·`sourceOrderLineId` 등을 포함하지 않는다(`SlipPublishService.java:788-878`). 이 필드만 다른 같은 키 요청은 지문상 동일할 가능성이 있어 직접 판정이 필요하다.
- 공유 DB 읽기 전용 재측정: 활성 `PARTNER_ORDER` audit는 **16행 / 지문 보유 16 / 서로 다른 키 16 / 단건 13 / 병합 3**이다. Docker 컨테이너에는 재빌드·재기동을 하지 않았고 `psql -c "SELECT ..."`만 실행했다.
- 노출 16개를 행별 replay 조건으로 다시 판정한 집계는 **전체 16 / legacy replay 자격 16 / 비자격 0 / 단건 13 / 병합 3**이다. 각 행은 저장 지문이 있고 저장 전표 `delivery_address IS NULL`이어서, 원래의 배포 전 본문을 같은 키로 보내면 exact HEAD의 legacy 지문과 저장 지문이 일치해 `assertReplayOrConflict`가 replay를 반환한다. HTTP 라이브 재요청은 exact SHA 미배포로 하지 않았다.
- fix 커밋의 서비스 diff는 기존 strict 비교에 legacy 일치 분기만 추가했고 새 거부 조건은 추가하지 않았다. 노출 16개 중 fix로 새로 막힌 정상 원문 replay는 **0건(단건 0, 병합 0)**이며, 반대로 직전 409 대상 16개가 replay 자격을 회복했다.
- 구조화 `deliveryAddress`가 실제로 달라지는 경우: 요청 주소가 non-null이면 legacy 지문을 계산하지 않고(`SlipPublishService.java:199-205`, `299-305`), 현행 지문에는 주소가 포함된다(`797`, `827`). 기존 지문과 달라 `601-605`에서 409가 된다. 단건·병합 IT도 각각 이를 명시한다(`SlipPublishControllerIT.java:206-234`, `SlipPublishMergeIT.java:517-545`).
- **BLOCK 발견**: 같은 키에서 `shippingAddress`만 바꾸면 단건은 전표 `shipping_address` 저장값이 달라지고(`SlipPublishService.java:233-241`), 병합도 동일하다(`331-337`). 그러나 단건 현행/legacy 지문(`820-849`)과 병합 현행/legacy 지문(`788-802`, `853-866`) 어디에도 `shippingAddress`가 없다. 따라서 같은 원문에서 이 필드만 바꾼 두 발행은 같은 SHA-256이 되고, 기존 전표가 있으면 `590-608`에서 409가 아니라 기존 전표 200 replay로 합쳐진다.
- 같은 결함은 `receiverPhone`, `partnerName`, `warehouseId`, 단건 `orderApprovedAt`, 병합 `sourceOrders.orderNo`, 라인 `productName`·`sourceOrderLineId`에도 성립한다. 이 필드들은 요청 계약과 저장/업무 의미가 있으나(`PublishFromPartnerOrderRequest.java:27-42`, `PublishFromOrdersMergeRequest.java:41-56`, `PublishLineRequest.java:30-41`) 지문 canonical에는 없다(`SlipPublishService.java:788-878`). 노출 16개도 원문에 이 중 하나만 다른 요청을 만들면 지문이 그대로이므로 **16/16이 서로 다른 발행을 조용히 replay할 수 있다**.
- 원장 데이터 재측정: 활성 OUTBOUND 중 `COMPLETED 7문서/17라인`, `CONFIRMED 4/10`, `DELIVERED 10/35`, 합계 **21문서/62라인**이다. 세 상태 문서의 비어 있지 않은 `delivery_address`는 **0문서**다.
- 슬라이스 1 계약 파일 4개(controller/DTO/repository/IT)는 `5fc3c27ad..03624dcd6` diff가 **0줄**이다. 상태 상수는 `SlipInternalController.java:74-78`, 조회·mapping은 `361-383`, DB 조건은 `SlipRepository.java:80-94` 그대로다.
- 응답 projection은 헤더 7필드와 라인 5필드만 가지며 UUID 필드가 없다(`PartnerLedgerSalesResponse.java:16-39`). entity의 UUID를 mapping하지 않고 업무 식별자와 표시값만 옮긴다(`48-68`). DTO 단위 테스트를 exact HEAD에서 재실행했고 `BUILD SUCCESSFUL`(18 tasks, exit 0)이었다.
- 공유 서버는 exact SHA/슬라이스 1이 미배포라 해당 GET이 `NoResourceFoundException`으로 500이었다. 이는 배포 슬롯 없음 조건이므로 라이브 QA 증거로 사용하지 않았고 최종에 **미실행**으로 기록한다.
- 최종 검증: 산출물 전체를 다시 읽어 대상 SHA, (1) BLOCK, (2) PASS, 16개 replay 판정, 신규 차단 0건, 21문서/62라인·주소 0, UUID 비노출, 라이브 QA 미실행, “내가 보지 않은 것”을 확인했다. `git status --short`는 이 산출물 1개만 `??`로 표시했고 HEAD도 지정 SHA와 일치했다.

## 판정

### (1) 멱등 키 fix가 정상 발행을 막거나 서로 다른 발행을 합치지 않는가? — **BLOCK**

- 배포 전 키 원문 replay: **16/16 replay 자격 복원**(단건 13, 병합 3).
- 실제 구조화 배송주소 `deliveryAddress`가 다른 같은 키 요청: 단건·병합 모두 **409**.
- fix로 새로 막힌 정상 발행: **0/16**(단건 0, 병합 0).
- 그러나 `shippingAddress`, 수령인 전화, 거래처명, 창고 UUID 등 지문 누락 필드만 다른 발행은 동일 지문으로 기존 전표에 합쳐진다. 이는 서로 다른 발행을 구분하지 못하는 기능 결함이므로 BLOCK이다.
- 근거: `services/slip-service/src/main/java/com/samhanair/logis/slip/publish/SlipPublishService.java:197-205, 233-241, 297-305, 331-337, 590-608, 788-878`.

### (2) 슬라이스 1 원장 read 계약이 여전히 온전한가? — **PASS**

- 계약 파일 변경 0줄, 포함 상태 `CONFIRMED`·`DELIVERED`·`COMPLETED` 유지.
- 공유 DB 읽기 전용 실측 **21문서/62라인**, 배송주소 값 **0문서**.
- 응답 DTO에 UUID 필드가 없고 entity UUID도 mapping하지 않는다.
- 근거: `services/slip-service/src/main/java/com/samhanair/logis/slip/web/SlipInternalController.java:74-78, 361-383`; `services/slip-service/src/main/java/com/samhanair/logis/slip/repository/SlipRepository.java:80-94`; `services/slip-service/src/main/java/com/samhanair/logis/slip/web/dto/PartnerLedgerSalesResponse.java:16-68`.

## 라이브 QA

- **미실행**. exact SHA가 공유 서비스에 배포되지 않았고 배포 슬롯이 없으며, Docker 재빌드·재기동 금지 조건을 준수했다.

## 내가 보지 않은 것

- exact SHA가 배포된 서비스에 대한 16개 키 실제 HTTP POST replay. 대신 exact HEAD 코드 분기와 공유 DB의 16개 행을 개별 자격 조건으로 대조했다.
- Testcontainers 기반 단건·병합 IT 및 원장 controller IT 재실행. 새 Docker 컨테이너 시작을 피하기 위해 실행하지 않았고, Docker 없는 DTO 단위 테스트만 재실행했다.
- 공유 DB 쓰기, V14 적용·rollback, 동시 race 실험. 모두 금지 조건 때문에 수행하지 않았다.
- 슬라이스 3·4·5, 인쇄 `MOCK_DATA`, 과거 배송주소 backfill. 이번 질문 범위에서 제외했다.
