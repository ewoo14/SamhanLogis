# PR #1088 (이슈 #1013) 라이브QA R3

- 실행일: 2026-08-07 (Asia/Seoul)
- 워크트리 HEAD: `23613b191`
- 실행 표면: `clients/arologis-desktop` Electron GUI
- 판정 원칙: 직접 URL 진입은 GUI 클릭 증거로 인정하지 않고 보조 관측으로만 기록

## 0. 환경 확인 및 발화 조건

```text
docker inspect -f '{{.Created}}' samhan-arologis-service
2026-08-06T16:20:46.583064293Z
```

기대값 `2026-08-06T16:20:46Z`와 일치했고 컨테이너는 `healthy`였다. Electron도 다음 전제와 일치했다.

```text
clients/arologis-desktop/node_modules/electron/dist/electron.exe
188,784,128 bytes (33.4.11)
clients/arologis-desktop/node_modules/electron/path.txt = electron.exe
```

읽기 전용 DB 확인 결과:

| 항목 | count |
|---|---:|
| 수신 배차 그룹 (`deleted_at IS NULL`) | 1 |
| 운영 배차 (`deleted_at IS NULL`) | 26 |
| `driver_locations` | 0 |
| `dispatch_notifications` | 0 |

수신 그룹 표본은 `S19-20260805-01`이었다. DB 직접 쓰기는 하지 않았다.

renderer는 `VITE_MOCK_MODE=0`으로 실행했고, `isMockMode()` 조건(`=== '1'`)이 false였다. 실제 API 응답이 관측되어 mock 응답이 사용되지 않았음도 확인했다.

## S1. 수신 배차 그룹

1. 발화 조건: 수신 그룹 1건, 운영 배차 26건.
2. 클릭 경로: Electron 로그인 화면에서 `admin / admin1234` 입력 후 로그인 버튼 클릭. 로그인 후 사이드바에서 `수신 배차 그룹`을 찾았으나 메뉴/링크가 존재하지 않았다. 따라서 GUI 클릭만으로 해당 화면에 진입할 수 없었다. 보조 확인으로 동일 Electron 창에서 route를 열고 날짜를 `2026-08-05`로 실제 입력했다.
3. 네트워크: `POST /auth/admin/login` 200, `GET /auth/me` 200, `GET /admin/arologis/permissions/my` 200, `GET /admin/arologis/dispatch-groups?dispatchDate=2026-08-05` 200.
4. GUI 표시(보조 관측): `S19-20260805-01`, 차량 `S19 QA 차량 01`, 운송사 `AROLOGIS · 아로로지스`, 운영 `수신 전용`이 표시됐다. 수정/재분류 UI는 표시되지 않았다.
5. 스크린샷: [r3-S1-received-groups-real-qa.png](screenshots/r3-S1-received-groups-real-qa.png)
6. 판정: **판정불가** — 데이터 표시 자체는 정상이나, 요구된 GUI 클릭 진입 경로(사이드바 메뉴)가 없어 직접 클릭 시나리오를 완주할 수 없다.

## S2. 조건 불일치 날짜 0건

1. 발화 조건: 동일 표본 조건에서 날짜를 `2026-08-04`로 변경.
2. 클릭 경로: S1의 수신 그룹 화면 보조 진입 후 날짜 입력 컨트롤을 실제 클릭/입력했다. 단, S1과 같은 GUI 진입 경로 누락이 있다.
3. 네트워크: `GET /admin/arologis/dispatch-groups?dispatchDate=2026-08-04` 200.
4. GUI 표시(보조 관측): `수신된 배차 그룹이 없습니다.` 빈 목록이 표시됐다.
5. 스크린샷: [r3-S2-empty-date-real-qa.png](screenshots/r3-S2-empty-date-real-qa.png)
6. 판정: **판정불가** — 200 + 빈 목록 동작은 정상 관측됐지만, 요구된 GUI 클릭 진입을 S1 메뉴 누락으로 완주할 수 없다.

## S3. 배차 상세·GPS·회신

1. 발화 조건: 운영 배차 26건, `driver_locations` 0건, `dispatch_notifications` 0건.
2. 클릭 경로: 배차 목록에서 상세로 가는 링크/버튼을 확인했으나 `tbody` 내 `a=0`, `button=0`이었다. 따라서 목록 행 클릭으로 상세 진입할 수 없었다. 보조 관측으로 기존 운영 배차 1건의 상세 route를 Electron에서 열었다.
3. 네트워크: `GET /admin/arologis/dispatches/{기존 운영 배차}` 200 (React StrictMode로 동일 GET 2회 관측).
4. GUI 표시(보조 관측): `배차 상세`, 배차 일자/유형/차량 2대와 각 차량의 `대기 중`, `매칭이 시도되지 않았습니다`가 표시됐다. GPS 및 회신 데이터 행은 표본 0으로 표시할 값이 없었다.
5. 스크린샷: [r3-S3-detail-zero-sample-real-qa.png](screenshots/r3-S3-detail-zero-sample-real-qa.png)
6. 판정: **판정불가** — 목록 클릭 진입 affordance가 없고, GPS·회신은 표본 0이라 결함 0으로 판정할 수 없다. 실 관리자 화면/API를 통한 표본 생성도 이번 실행에서 수행하지 않았으며 DB 직접 INSERT는 금지 조건상 하지 않았다.

## S4. 수동 배차

1. 발화 조건: 로그인 완료, 운영 배차 26건.
2. 클릭 경로: Electron 배차 화면에서 `미배차` 화면으로 이동한 뒤 `수동 배차` 사이드바 링크 클릭 → `+ 차량 추가` 버튼 클릭.
3. 네트워크: 화면 진입 및 차량 추가는 로컬 상태 동작으로 API 호출 없음.
4. GUI 표시: 수동 배차 폼, 도착일/유형/기사 코드, 차량 1 기본 폼이 표시됐고 `+ 차량 추가` 클릭 후 `차량 2` 폼이 추가됐다.
5. 스크린샷: [r3-S4-manual-dispatch-real-qa.png](screenshots/r3-S4-manual-dispatch-real-qa.png)
6. 판정: **PASS** — Electron GUI 클릭 및 기본 차량 추가 동작 확인.

## 종합 판정

**R3 전체 PASS 아님.** 로그인·실 API·S2의 200/빈 목록·S4 기본 동작은 정상이다. 그러나 수신 배차 그룹 메뉴가 사이드바에 없어 S1/S2의 요구된 GUI 진입이 막혔고, 배차 목록에 상세 클릭 affordance가 없어 S3의 목록 클릭 진입이 막혔다. GPS·회신은 `driver_locations=0`, `dispatch_notifications=0`이라 판정불가다.

## 신규 파일

- `docs/qa/pr-1088/2026-08-07-1088-live-qa-r3.md`
- `docs/qa/pr-1088/screenshots/r3-S1-received-groups-real-qa.png`
- `docs/qa/pr-1088/screenshots/r3-S2-empty-date-real-qa.png`
- `docs/qa/pr-1088/screenshots/r3-S3-detail-zero-sample-real-qa.png`
- `docs/qa/pr-1088/screenshots/r3-S4-manual-dispatch-real-qa.png`
- `clients/arologis-desktop/qa-electron-r3.log`
- `clients/arologis-desktop/qa-electron-r3.err.log`
