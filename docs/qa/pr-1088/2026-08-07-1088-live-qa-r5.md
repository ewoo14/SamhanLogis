# PR #1088 (이슈 #1013) 머지 전 라이브QA R5

- 실행일: 2026-08-07 (Asia/Seoul)
- 기준 HEAD: `ab4ad0a2f`
- 실행 표면: `clients/arologis-desktop` Electron 33.4.11
- mock: `VITE_MOCK_MODE=0`; renderer `isMockMode()`는 값이 `1`일 때만 true이므로 `hasMock=false`. 실 API 응답을 확인했다.
- 금지 범위 준수: git 명령 없음, DB 직접 쓰기 없음, 컨테이너 재빌드·재시작 없음, 배차 목록에서 상세 진입 없음.

## 0. 환경 확인 및 발화 조건

```text
docker inspect -f '{{.Created}}' samhan-arologis-service
2026-08-06T16:20:46.583064293Z
```

기대값 `2026-08-06T16:20:46Z`와 일치했다. `samhan-arologis-service`는 healthy 상태였다.

| 발화 조건 | 실측 |
|---|---:|
| 수신 배차 그룹 | 1건 (`S19-20260805-01`) |
| 운영 배차 | 26건 |
| `driver_locations` | 0건 — 표본 0, 판정 불가 |
| `dispatch_notifications` | 0건 — 표본 0, 판정 불가 |
| 계정 | `admin / ${QA_AROLOGIS_ADMIN_PASSWORD}` → `AROLOGIS_MASTER` |
| renderer `hasMock` | `false` |

## S1. 사이드바 수신 배차 그룹 진입

1. 발화 조건: admin 로그인, 수신 배차 그룹 1건, 운영 배차 26건.
2. 클릭 경로: Electron 로그인 → `로그인` → 배차 메뉴의 `수신 배차 그룹` 클릭 → `배차 지정일`을 `2026-08-05`로 변경.
3. 네트워크 실제 API·상태코드: `POST /auth/admin/login` 200, `GET /auth/me` 200, `GET /admin/arologis/permissions/my` 200, `GET /admin/arologis/dispatch-groups?dispatchDate=2026-08-05` 200.
4. GUI 실제 표시: `S19-20260805-01`, 차량 `S19 QA 차량 01`, 운송사 `AROLOGIS · 아로로지스`, 운영 `수신 전용`이 표시됐다. 수신 그룹 화면의 수정/재분류 UI는 없었다.
5. 스크린샷: [r5-S1-received-groups-real-qa.png](screenshots/r5-S1-received-groups-real-qa.png)
6. 판정: **PASS**

## S2. 2026-08-04 빈 날짜

1. 발화 조건: S1의 수신 배차 그룹 화면에서 날짜를 `2026-08-04`로 변경.
2. 클릭 경로: `배차 지정일` 입력 → `2026-08-04` 입력 → 조회 결과 대기.
3. 네트워크 실제 API·상태코드: `GET /admin/arologis/dispatch-groups?dispatchDate=2026-08-04` 200, 응답 데이터가 빈 배열이었다.
4. GUI 실제 표시: `수신된 배차 그룹이 없습니다.`
5. 스크린샷: [r5-S2-empty-date-real-qa.png](screenshots/r5-S2-empty-date-real-qa.png)
6. 판정: **PASS**

## S6. 권한 조회 실패 복구 및 fail-closed

1. 발화 조건: S1 로그인 세션. 개발자도구 네트워크 차단 대신 Electron renderer의 요청 interception으로 `GET /admin/arologis/permissions/my`를 서버/DB 변경 없이 403으로 응답시켰다.
2. 클릭 경로: 수신 배차 그룹 화면에서 permissions 요청 403 차단 → 화면 새로고침/배차 화면 재렌더 → 차단 해제 → `권한 다시 확인` 클릭 → 다시 403 차단 상태에서 차단 해제 후 Electron 창 포커스 이탈·복귀.
3. 네트워크 실제 API·상태코드:
   - 차단 상태: `GET /admin/arologis/permissions/my` 403 (TanStack Query retry 포함 2회).
   - 버튼 재시도: 같은 API 200, 이어서 `GET /admin/arologis/dispatch-groups?dispatchDate=2026-08-06` 200.
   - 포커스 복귀: 같은 API 200.
4. GUI 실제 표시:
   - 403 상태에서 `권한을 확인하지 못했습니다. 잠시 후 다시 시도해 주세요.`와 `권한 다시 확인` 버튼이 표시됐다.
   - 403 상태에서 `수신 배차 그룹` 링크는 숨겨졌다(fail-closed).
   - 차단 해제 후 버튼 클릭으로 권한을 재조회하자 `수신 배차 그룹` 링크가 다시 나타났다.
   - 차단 해제 후 포커스 복귀만으로도 200 재조회되고 링크가 복구됐다.
5. 스크린샷: [403 fail-closed](screenshots/r5-S6-permission-failed-real-qa.png), [버튼 재시도](screenshots/r5-S6-permission-retry-real-qa.png), [포커스 복귀](screenshots/r5-S6-focus-recovery-real-qa.png)
6. 판정: **PASS**

## S7. 미배차 화면 포커스 복귀 시 전역 재조회 회귀 확인

1. 발화 조건: Electron에서 `/dispatches/unassigned` 화면 진입. `driver_locations=0`, `dispatch_notifications=0`은 표본 0으로 내용 판정에 사용하지 않았다.
2. 클릭 경로: 배차 메뉴의 `미배차` 클릭 → 초기 목록 렌더 완료 → Electron 창 포커스 이탈·복귀.
3. 네트워크 실제 API·상태코드:
   - 화면 진입 초기 요청: permissions 200, `GET /admin/arologis/dispatches/unassigned?date=2026-08-07` 200, history/latest/history 조회 200, 화면 진입 시 저장내역 복원 POST 200.
   - 포커스 이탈·복귀 후 추가 요청: **0건**. 특히 `POST /admin/arologis/dispatches/history` 자동저장 요청은 발생하지 않았다.
4. GUI 실제 표시: `미배차 리스트`, 조회일 `2026-08-07`, 출고전표 8건 중 미배차 8건과 `수동 배차로 이동` 버튼이 표시됐다. 포커스 복귀 후 화면 상태는 유지됐다. 배차 상세 진입은 시도하지 않았다.
5. 스크린샷: [r5-S7-unassigned-focus-real-qa.png](screenshots/r5-S7-unassigned-focus-real-qa.png)
6. 판정: **PASS**

## 종합 판정

게이트 ③ 최종 SHA `ab4ad0a2f` 기준 S1·S2·S6·S7은 모두 **PASS**다. S6의 403 복구는 권한 없음과 조회 실패를 구분하고 링크를 fail-closed 처리했으며, 버튼 재시도와 포커스 복귀 모두 200으로 복구됐다. S7에서는 포커스 복귀 후 권한 쿼리 이외의 전역 재조회나 자동저장이 발생하지 않았다.

`driver_locations=0`, `dispatch_notifications=0`은 표본 0이므로 해당 데이터 내용은 판정하지 않았다.

## 신규 파일

- `docs/qa/pr-1088/2026-08-07-1088-live-qa-r5.md`
- `docs/qa/pr-1088/screenshots/r5-S1-received-groups-real-qa.png`
- `docs/qa/pr-1088/screenshots/r5-S2-empty-date-real-qa.png`
- `docs/qa/pr-1088/screenshots/r5-S6-permission-failed-real-qa.png`
- `docs/qa/pr-1088/screenshots/r5-S6-permission-retry-real-qa.png`
- `docs/qa/pr-1088/screenshots/r5-S6-focus-recovery-real-qa.png`
- `docs/qa/pr-1088/screenshots/r5-S7-unassigned-focus-real-qa.png`
- `clients/arologis-desktop/qa-electron-r5.log`
- `clients/arologis-desktop/qa-electron-r5.err.log`
