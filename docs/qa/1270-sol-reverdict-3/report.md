# PR #1270 CODEX SOL 적대검증 재판정 보고서 — 3회차

## ① main 병합 결과 — #1264 포함 여부·충돌

- 검증 브랜치: `fix/daily-closing-parity`
- 시작 SHA: `af435d45f16f9583552941f75c33df526d3b6b57`
- 첫 병합 main: `3e4f44cc0e3312172a6f7ca8b3d87d875fd69428`
- 첫 병합 결과: 충돌 없이 merge commit `a341b55ea909ad607601734390ae7b77de462273` 생성.
- 첫 병합 시점 PR #1264: `open`, `merged=false`, head `2e0cdaecc58909e0bb45227b30fd598a7fc2aad7`. 따라서 첫 병합본에는 #1264가 없었다.
- 검증 종료 직전 PR #1264가 `merged=true`로 바뀌어 최신 main `0ac75e01083302cd24c1ecbc391b042701aaa3f9`을 다시 fetch·merge했다.
- 두 번째 병합 결과: **자동 병합 실패. 다음 3파일 content conflict**.

| 충돌 파일 | 서로 겹친 계약 |
|---|---|
| `clients/desktop/src/renderer/routes/DailyClosingPage.tsx` | #1270의 draft 기반 17열 필터값 함수와 #1264의 탭 분류식·회계전표 생성 UI가 같은 블록을 변경 |
| `clients/desktop/src/renderer/routes/DailyClosingPage.test.tsx` | #1270의 17열 DOM 회귀·편집 구조 테스트와 #1264의 회계전표 API mock·레거시 탭 의미 테스트가 같은 import/테스트 블록을 변경 |
| `services/slip-service/src/main/java/com/samhanair/logis/slip/web/dto/DailyClosingRowResponse.java` | #1270의 `categoryKey·deliveryPrice·expectedRate`와 #1264의 `partnerId·slipNo·productCode·sourceLineNo·taxType`가 record 꼬리·생성자·복사 생성 호출에 서로 배타적으로 추가됨 |

지시대로 충돌은 해결하지 않았다. 현재 워크트리는 위 3파일이 `UU`인 병합 중 상태다. 따라서 #1264 포함 최종 결합본의 build·renderer·라이브 결과는 **판정 불가**다.

## ② 탭 분류 — 두 탭 행 수·생성 버튼

첫 번째 main 병합본(`#1264` 미포함, `a341b55ea`)은 branch product/slip JAR와 renderer를 별도 포트로 띄워 headless Chromium에서 실측했다.

| 날짜 | API 원천 | 회계반영일자 있음 | 회계반영일자 없음 | 화면 「결과」 | 화면 「선발행」 | 생성 버튼 |
|---|---:|---:|---:|---:|---:|---:|
| 2026-08-14 | 13 | 1 | 12 | **1** | **12** | 결과 0 / 선발행 0 |
| 2026-08-03 | 4 | 0 | 4 | **0** | **4** | 결과 0 / 선발행 0 |

레거시 정본은 `Code.js:738`의 회계반영일자 있음→`pre`, `Index.html:211-212`의 `main=결과`, `pre=선발행`이다. 즉 **있음→선발행, 없음→결과**여야 한다. 그러나 첫 병합본 `DailyClosingPage.tsx:779-782`는 `RESULT ? Boolean(accountingPostedAt) : !accountingPostedAt`이어서 반대다.

- 2026-08-14 기존 매출행에서 반영 1/1은 「결과」, 미반영 12/12는 「선발행」에 있었다.
- #1264가 제공하는 매입 14행까지 포함한 정본 모집단 `미반영 26 → 결과`, `반영 1 → 선발행`은 첫 병합본에 매입 토글·생성 기능 자체가 없어 밟을 수 없었다.
- 회계전표 「생성」 버튼도 두 탭 모두 0개였다. #1264 미포함 때문이며 별도 도달 결함으로 중복 계산하지 않는다.
- 최신 #1264 포함 main은 위 3파일에서 충돌했으므로, 충돌 해결 후 실제 `결과 26 / 선발행 1`과 생성 버튼의 결과 탭 배치를 다시 확인해야 한다.

## ③ 17열 대조표

대조 행은 새 QA 데이터가 아닌 기존 행 `2026/08/03-6`의 `무풍 1way 냉방전용 실내기`다. 첫 main 병합본에서 Chromium DOM 17개 셀을 읽고 공유 DB를 `BEGIN READ ONLY`로 대조했다.

| 열번호 | 헤더명 | 화면값 | DB 원본·원본 계산값 | 일치여부 |
|---:|---|---|---|:---:|
| 1 | DC | `홈45% / 360 -50000 / 4way -80000 / 1way -60000 / 스탠드 -20000 / 디럭스 -10000 / 1등급 -30000` | `dc_configs`: 0.4500 / 50,000 / 80,000 / 60,000 / 20,000 / 10,000 / 30,000 | 일치 |
| 2 | 일자 | `2026-08-03` | `slips.slip_date=2026-08-03` | 일치 |
| 3 | 번호 | `6` | `slips.seq_no=6` | 일치 |
| 4 | 창고명 | 빈칸 | `destination_warehouse_name=NULL` | 일치 |
| 5 | 품목명 | `무풍 1way 냉방전용 실내기` | `slip_lines.product_name` 동일 | 일치 |
| 6 | 수량 | `1` | `quantity=1` | 일치 |
| 7 | 단가(VAT포함) | `641,480` | `unit_price_with_vat=641,480.00` | 일치 |
| 8 | 공급가액 | `583,164` | `supply_amount=583,164.00` | 일치 |
| 9 | 부가세 | `58,316` | `vat_amount=58,316.00` | 일치 |
| 10 | 합계 | `641,480` | 공급가액+부가세=`641,480.00` | 일치 |
| 11 | 거래처명 | `(주)삼한공조시스템` | `slips.partner_name` 동일 | 일치 |
| 12 | 거래처코드 | `2148720659` | `slips.partner_code` 동일 | 일치 |
| 13 | 출고가 | `475,200` | `price_history`, 2026-04-01 적용 `release_price=475,200.00` | 일치 |
| 14 | 할인율 | `-35%` | `round((1-641480/475200)*100)=-35%` | 일치 |
| 15 | 총계 | `641,480` | 단가×수량=`641,480.00` | 일치 |
| 16 | 확인 | `확인` | 적용 출고가 양수에 따른 API 파생 `CONFIRMED` | 일치 |
| 17 | 회계반영일자 | 빈칸 | `2026/08/03-6`의 활성 posted allocation 0건 | 일치 |

**첫 main 병합본에서 17/17 일치했다.** 최신 #1264 포함 main은 충돌 때문에 병합 후 열 이동 여부를 다시 렌더링할 수 없어 최종 결합 상태는 판정 불가다.

## ④ 견적품목 표시

- 기존 모델 `PC1BWCK3NW`의 product DB 원본: 출고가 520,300원, 납품가 **286,165원**, 활성 카테고리 `COMMERCIAL_MULTI, HOME_MULTI`.
- 2026-08-14 API 13행 중 카테고리와 0이 아닌 납품가 원천이 있는 같은 원인 대상은 **1행**이었다.
- 화면 상세에서 모델 `PC1BWCK3NW`, 카테고리 `COMMERCIAL_MULTI, HOME_MULTI`, 기준 납품가 **286,165원**을 확인했다.
- 결과: 첫 main 병합본은 **대상 1/1 표시 유지**, 같은 원인으로 다시 0이 된 행 0건.
- 최신 #1264 포함 main에서는 DTO record 꼬리가 바로 충돌했으므로, 양쪽 필드 세트를 함께 보존한 해결 뒤 1/1을 재검증해야 한다.

## ⑤ 금액 축 정합

기존 2026-08-14 번호 6 `한경희 선풍기` 행을 화면·API·DB로 대조했다.

| 축 | 화면 | API/DB | 계약 |
|---|---:|---:|---|
| 단가(VAT 포함) | 11,000 | 11,000 | 총액축 |
| 공급가액 | 10,000 | 10,000 | `Math.round(11000/1.1)` |
| VAT | 1,000 | 1,000 | 11,000-10,000 |
| 합계 | 11,000 | 11,000 | 공급가+VAT |
| 총계 | 11,000 | 11,000 | 단가×수량 1 |
| 출고가 | 0 | 원천 없음 | 회계 총액과 별도 기준축 |
| 할인율 | 0% | 출고가 원천 없음 | 총계에 영향 없음 |

17열 기준행 641,480원도 `Math.round(641480/1.1)=583,164`, VAT 58,316, 합계·총계 641,480원이다. 따라서 첫 main 병합본의 금액 축은 #1264의 `10,000+VAT 1,000=11,000` 및 #1265의 레거시 총액축과 일치했다. 최신 #1264 포함 충돌 해결본은 판정 불가다.

## ⑥ 기존 행 호환

- 공유 DB 기존 행만 사용했다. 새 fixture·새 전표를 만들지 않았다.
- 2026-08-03: API 4행, 화면 결과 0+선발행 4=4행.
- 2026-08-14: API 13행, 화면 결과 1+선발행 12=13행.
- 두 날짜 모두 header-only 또는 0행 stub이 아니며 17열·견적 상세·금액이 실제 렌더링됐다.

첫 main 병합본의 조회 호환은 유지됐다. 최신 #1264 포함 main은 merge conflict로 build 이전에 막혀 최종 호환은 판정 불가다.

## ⑦ 스크린샷 — 행 수·경로

모든 PNG는 `resolveQaShotsDir()` 기본 `_local` 경로로 저장했고, 3600×1400 원본 4장을 직접 열어 한글·17열·데이터행·상세값을 확인했다.

| 캡처 | DOM에서 직접 센 데이터행 | 확인 내용 |
|---|---:|---|
| `_local/01-2026-08-14-result-real-qa.png` | 1 | 반영된 기존 번호 17이 잘못 「결과」에 있음, 회계반영일자 표시 |
| `_local/02-2026-08-14-preissued-real-qa.png` | 12 | 미반영 기존 12행이 잘못 「선발행」에 있음 |
| `_local/03-2026-08-14-estimate-detail-real-qa.png` | 12 + 상세 1 | `PC1BWCK3NW`, 두 카테고리, 286,165원 표시 |
| `_local/04-2026-08-03-17cols-real-qa.png` | 4 | 17개 헤더·17개 셀, 기존 번호 6의 4개 품목 |

![2026-08-14 결과 1행](_local/01-2026-08-14-result-real-qa.png)

![2026-08-14 선발행 12행](_local/02-2026-08-14-preissued-real-qa.png)

![견적품목 상세](_local/03-2026-08-14-estimate-detail-real-qa.png)

![2026-08-03 17열](_local/04-2026-08-03-17cols-real-qa.png)

기계 관측 원문은 `_local/live-evidence.json`이다. 자격과 내부 UUID는 기록하지 않았다. 이 캡처는 충돌 전 첫 main 병합본의 증거이며, #1264 포함 충돌 해결본의 증거로 오인하면 안 된다.

## ⑧ 판정 불가 축

- **#1264 포함 최종 결합본 전체**: 3파일 content conflict로 build·renderer·Playwright 실행 불가.
- 정본 최종 모집단 `결과 26 / 선발행 1`과 생성 버튼의 「결과」 탭 배치: 충돌 해결 전 판정 불가.
- 회계전표 생성·재진입 잠금: 공유 DB write 금지 및 최종 결합 충돌 때문에 이번 라운드 판정 불가.
- 최신 main merge SHA `0ac75e010`의 CI는 최종 완료됐지만, 충돌한 로컬 결합 상태 자체의 CI는 아니므로 post-#1264 결합 성공의 근거로 쓰지 않는다.

위 항목은 결함 0건의 근거로 사용하지 않았다.

## ⑨ CI 귀속

- PR #1270 head `af435d45f`: check-run **46/46 성공**, 실패 0, pending 0.
- PR #1264 head `2e0cdaecc`: 46개 중 저장소 check 45 성공, 실패 1은 외부 `GitGuardian Security Checks`다. 제품 CI 실패로 귀속하지 않는다.
- #1264 merge main `0ac75e010`: REST check-run 최종 **29/29 성공**, 실패 0, pending 0.
- 알려진 main 기존 `SlipSalesUpdateIT R9 (expected: 2 / was: 1)`은 이번 최신 main의 완료된 `slip-it-core`에 재현되지 않았다.
- 충돌 전 결합 HEAD 로컬 보강: product/slip `bootJar` 성공, `DailyClosingPage.test.tsx` **35/35 통과**, 최종 채택 live Playwright **1/1 통과**.
- 충돌 후 결합 상태는 컴파일 대상이 아니므로 로컬 성공 수치로 상쇄하지 않는다.

## ⑩ 머지 가능/불가 — 도달 결함 1건

**머지 불가 — 실 사용자가 화면으로 재현 가능한 도달 결함 1건 + #1264 포함 결합 판정 불가.**

1. 충돌 전 실행 가능한 첫 main 병합본에서 회계반영일자 있는 1행이 「결과」, 없는 12행이 「선발행」으로 정본과 반대였다. 사용자는 기존 일마감 화면에서 두 탭을 눌러 재현할 수 있다.

#1264가 이 의미와 생성 버튼을 고쳤지만, 최신 main을 병합하자 3파일이 자동 병합되지 않았다. 충돌 자체는 화면 도달 결함 수에 넣지 않았으나 명백한 머지 차단 사유다. 충돌을 해결하면서 #1270의 17열·견적 필드와 #1264의 전표 생성 필드를 모두 보존한 뒤, `결과 26 / 선발행 1`, 생성 버튼 결과 탭, 17/17, 견적 1/1을 다시 라이브 검증해야 한다.

## ⑪ 프로세스 회수

- 종료: branch product-service PID 110756(38084), branch slip-service PID 104476(38086), 읽기 전용 프록시 PID 105776(5176), Vite renderer PID 104876(5175).
- 종료 후 38084·38086·5176·5175 listener 각 0.
- Playwright/Chromium 잔여 0, 임시 `1270-sol-reverdict-3-real-qa.spec.ts`·config·proxy 삭제, `%TEMP%` 전용 결과·로그·PID 삭제.
- 이번 라운드 격리 컨테이너 생성 0, `sol1270r3*` 잔여 0.
- 공유 `samhan-*` 컨테이너 **24개 실행 상태 그대로**. 타 검증 컨테이너를 중지·변경하지 않았다.
- 공유 DB는 `BEGIN READ ONLY` 조회만 했고 저장 요청은 읽기 전용 프록시에서 차단했다.
- 다른 워크트리는 변경하지 않았고 `wdc70`은 삭제하지 않았다.
- `git add`·별도 commit·push는 실행하지 않았다. 사용자 지시의 main merge가 만든 첫 merge commit과, 현재 해결하지 않은 두 번째 merge conflict만 존재한다.
