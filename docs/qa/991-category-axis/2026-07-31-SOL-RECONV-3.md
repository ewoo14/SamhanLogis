# PR #991 기능 회귀 검토 — SOL-RECONV-3

- 대상 SHA: `a1d40d10734a2301b14571c5dc66aa23c09b5f1c`

## 검증 기준

- `git rev-parse HEAD` 실측 결과가 대상 SHA와 일치함을 확인했다.

## (1) `VatAmountCalculator` 소비자 전수 확인

`rg -n --glob '*.java' "VatAmountCalculator" services shared`로 생산 코드 호출부를 전수 검색했다. 테스트 호출부를 제외한 결과는 아래와 같다.

| 소비자/경로 | 호출 | 실제 모드 | 근거 |
|---|---|---|---|
| accounting `VatCalculator` VAT 포함 역산 | `splitVatInclusive(total)` | `DOWN` 기본 | `services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/VatCalculator.java:28`, `shared/common/src/main/java/com/samhanair/logis/common/financial/VatAmountCalculator.java:34-45` |
| partner-order `PartnerOrderLine` PRICE 권위 | `splitVatInclusive(resolvedTotal)` | `DOWN` 기본 | `services/partner-order-service/src/main/java/com/samhanair/logis/partnerorder/domain/PartnerOrderLine.java:183` |
| partner-order `PartnerOrderLine` TOTAL 권위 | `splitVatInclusive(lineTotal)` | `DOWN` 기본 | `services/partner-order-service/src/main/java/com/samhanair/logis/partnerorder/domain/PartnerOrderLine.java:207` |
| partner-order 인쇄 legacy fallback | `splitVatInclusive(total)` | `DOWN` 기본 | `services/partner-order-service/src/main/java/com/samhanair/logis/partnerorder/service/PartnerOrderPrintService.java:113` |
| slip `SlipLine.createFromVatInclusive` | `splitVatInclusive(..., HALF_UP)` | `HALF_UP` 명시 | `services/slip-service/src/main/java/com/samhanair/logis/slip/domain/SlipLine.java:275-276` |
| slip `EstimateLine.createFromVatInclusive` | `splitVatInclusive(..., HALF_UP)` | `HALF_UP` 명시 | `services/slip-service/src/main/java/com/samhanair/logis/slip/estimate/domain/EstimateLine.java:193-194` |
| accounting `JournalSeeder` | `fromSupply(net)` | `DOWN` 고정 | `services/accounting-service/src/main/java/com/samhanair/logis/accounting/seed/JournalSeeder.java:206`, `VatAmountCalculator.java:25-31` |
| accounting `TaxInvoiceLine` | `fromSupply(supply)` | `DOWN` 고정 | `services/accounting-service/src/main/java/com/samhanair/logis/accounting/domain/TaxInvoiceLine.java:233`, `VatAmountCalculator.java:25-31` |
| accounting `TaxInvoice` fallback | `fromSupply(supplySum)` | `DOWN` 고정 | `services/accounting-service/src/main/java/com/samhanair/logis/accounting/domain/TaxInvoice.java:601`, `VatAmountCalculator.java:25-31` |
| partner-order `PartnerOrderLine` SUPPLY 권위 | `fromSupply(supplyAmount)` | `DOWN` 고정 | `services/partner-order-service/src/main/java/com/samhanair/logis/partnerorder/domain/PartnerOrderLine.java:190`, `VatAmountCalculator.java:25-31` |
| slip `SlipLine` 공급가 기준 계산 | `fromSupply(supplyAmount)` | `DOWN` 고정 | `services/slip-service/src/main/java/com/samhanair/logis/slip/domain/SlipLine.java:537`, `VatAmountCalculator.java:25-31` |
| slip `EstimateLine.recompute` 공급가 기준 계산 | `fromSupply(supplyAmount)` | `DOWN` 고정 | `services/slip-service/src/main/java/com/samhanair/logis/slip/estimate/domain/EstimateLine.java:273`, `VatAmountCalculator.java:25-31` |
| slip `SlipRevisionService` 공급가 기준 합계 | `fromSupply(supply)` | `DOWN` 고정 | `services/slip-service/src/main/java/com/samhanair/logis/slip/revision/service/SlipRevisionService.java:671`, `VatAmountCalculator.java:25-31` |

확인 시점 결론: 명시적 `HALF_UP`을 받는 생산 호출부는 VAT 포함 견적 생성과 VAT 포함 전표 생성 두 곳뿐이다. `fromSupply()` 구현은 이번 R3에서도 `DOWN` 고정이다.

저장소 전체를 빌드 산출물·문서를 제외하고 다시 검색해도 위 생산 호출부 외 소비자는 없었다. 따라서 의도치 않게 `HALF_UP`을 받는 경로는 없고, `HALF_UP`이 필요한 VAT 포함 견적·발행 경로가 기본 `DOWN`으로 빠지는 곳도 없다.

### `EstimateLine` 실데이터 변화액

공유 `slip_db`에 `docker exec samhan-postgres psql -U samhan -d slip_db -c "SQL"` 형식의 읽기 전용 SQL을 실행했다. `estimate_lines`와 `estimates`가 모두 활성이고 `unit_price_with_vat IS NOT NULL`인 라인을 대상으로 다음 값을 비교했다.

- R3 재계산: `합계=ROUND(unit_price_with_vat×quantity, 0)`, `공급가=ROUND(합계/1.10, 0)`, `VAT=합계-공급가`
- 기존 저장: `supply_amount`, `vat_amount`, `line_total`
- 범위: 활성 VAT 포함 견적 **100라인**, 모두 `QUOTE_DRAFT`

| 비교 | 변화 라인 | 공급가 변화액 합계 | VAT 변화액 합계 | 합계 변화액 합계 |
|---|---:|---:|---:|---:|
| 현재 저장값 → R3 `HALF_UP` | **0라인** | **0원** | **0원** | **0원** |
| `DOWN` → R3 `HALF_UP` 산식 비교 | 24라인 | +24원 | -24원 | 0원 |

따라서 `EstimateLine`에 `HALF_UP`을 명시한 것은 현재 견적 원천 금액을 바꾸지 않는다. 반대로 이 경로가 기본 `DOWN`을 받으면 현재 100라인 중 24라인의 공급가/VAT 구성이 원천과 달라진다. 근거 구현: `services/slip-service/src/main/java/com/samhanair/logis/slip/estimate/domain/EstimateLine.java:188-203`.

## (2) R-01·R-02 재확인

### R-01 — 일마감 상세 표시 단가 대 실제 전표 단가

공유 `accounting_db`에 읽기 전용 SQL을 다시 실행했다. 화면 도달 경계는 `tax_invoices.status='ISSUED'`, `sales_accounting_slips.status='POSTED'`, `purchase_accounting_slips.status='POSTED'`와 각 헤더·라인 `is_deleted=FALSE`다. 비교식은 다음과 같다.

```text
표시 단가 = (supply_amount + vat_amount) / quantity
실제 전표 단가 = line_total / quantity
```

| 도달 원천 | 라인 | 수량 0 | 단가 불일치 | 최대 차이 |
|---|---:|---:|---:|---:|
| `TAX_INVOICE` | 13 | 0 | **0** | **0원** |
| `SALES_SLIP` | 0 | 0 | 0 | 0원 |
| `PURCHASE_SLIP` | 0 | 0 | 0 | 0원 |

실제 단가를 집계 key에 포함한 현재 코드와 같은 방식으로 `TAX_INVOICE` 13라인을 다시 묶어도 표시 행은 13행, 원천 라인과 표시 행의 단가 불일치는 **0라인**, 최대 차이는 **0원**이었다. 구현 근거: `services/accounting-service/src/main/java/com/samhanair/logis/accounting/service/MonthEndCloseService.java:346-357,379-398,638-665`.

### 반올림 소비자 회귀 실행

다음 두 타깃을 `--rerun-tasks --no-build-cache --no-daemon`으로 함께 다시 실행했고 종료코드 0, `BUILD SUCCESSFUL`을 확인했다.

- `com.samhanair.logis.common.financial.VatAmountCalculatorTest`: 기본 `DOWN`과 명시적 `HALF_UP` 계약
- `com.samhanair.logis.partnerorder.domain.PartnerOrderLineSupplyVatTest`: partner-order PRICE/SUPPLY/VAT/TOTAL 및 800,000원 기존 계약

근거 테스트: `shared/common/src/test/java/com/samhanair/logis/common/financial/VatAmountCalculatorTest.java:20-43`, `services/partner-order-service/src/test/java/com/samhanair/logis/partnerorder/domain/PartnerOrderLineSupplyVatTest.java:14-72`.

### R-02 — 견적 110,005원 원천·발행 금액

아래 후보 테스트를 `--rerun-tasks --no-build-cache --no-daemon`으로 다시 실행했다. 격리 테스트 DB를 사용했고 공유 `slip_db`에는 쓰지 않았다. JUnit XML 실측은 **5 tests / 0 failures / 0 errors / 0 skipped**, Gradle 종료코드 0이었다.

| 경로 | 재확인 결과 |
|---|---|
| 도메인 VAT 포함 전표 생성 | 110,005원 → 공급가 100,005원 / VAT 10,000원 |
| 공개 견적 발행 | 원천과 같은 100,005원 / 10,000원 저장 |
| 내부 견적 발행 | 원천과 같은 100,005원 / 10,000원 저장 |
| 단일 partner-order 발행 | 원천과 같은 100,005원 / 10,000원 저장 |
| 병합 partner-order 발행 | 원천과 같은 100,005원 / 10,000원 저장 |

근거: `services/slip-service/src/test/java/com/samhanair/logis/slip/domain/SlipLineAuthoritativeAmountsTest.java:208-218`, `services/slip-service/src/test/java/com/samhanair/logis/slip/publish/SlipPublishControllerIT.java:210-268`, `services/slip-service/src/test/java/com/samhanair/logis/slip/publish/InternalSlipPublishControllerIT.java:117-149`, `services/slip-service/src/test/java/com/samhanair/logis/slip/publish/SlipPublishMergeIT.java:219-253`.

## R-03 존속 여부 — 판정 제외

R-03 선재 VAT 재가산 19건은 직전 판정대로 **존속**한다. R3는 해당 데이터/backfill을 건드리지 않았으며 이번 PASS/BLOCK 판정에는 포함하지 않았다.

## 판정

| 질문 | 판정 | 사용자 영향 근거 |
|---|---|---|
| (1) 반올림 범위 축소가 다른 경로를 깨뜨렸는가 | **PASS** | 기본 overload는 `DOWN`, 명시 overload만 전달 모드를 사용한다(`shared/common/src/main/java/com/samhanair/logis/common/financial/VatAmountCalculator.java:25-45`). 명시 `HALF_UP`은 VAT 포함 `SlipLine`과 `EstimateLine` 두 곳뿐이다(`SlipLine.java:270-277`, `EstimateLine.java:188-195`). partner-order PRICE/TOTAL과 인쇄 fallback은 기본 `DOWN`이다(`PartnerOrderLine.java:178-208`, `PartnerOrderPrintService.java:101-116`). 견적 실데이터 100라인은 R3 대비 변화 0라인/공급가 0원/VAT 0원/합계 0원이었다. 의도치 않은 `HALF_UP` 경로나 필요한 `HALF_UP` 누락 경로는 확인되지 않았다. |
| (2) R-01·R-02가 여전히 해소 상태인가 | **PASS** | R-01은 실제 단가를 집계 key에 넣어 단가별 행을 분리한다(`MonthEndCloseService.java:346-357,379-398,638-665`). 공유 DB 재측정은 도달 13라인, 수량 0은 0라인, 불일치 0라인, 최대 차이 0원이었다. R-02는 견적·발행 생성이 `HALF_UP`을 명시하고(`EstimateLine.java:188-203`, `SlipLine.java:270-283`), 110,005원 후보 5 tests 재실행에서 공급가 100,005원/VAT 10,000원으로 원천과 발행이 일치했다. |

두 질문의 최종 기능 회귀 판정은 **PASS**다.

## 라이브QA

**미실행** — 배포 슬롯이 없어 대상 SHA의 라이브 서버 검증을 실행하지 않았다.

## 내가 보지 않은 것

- R-03의 원인·정정/backfill 타당성은 조사하지 않았고, 직전 19건의 존속 여부만 기록했다.
- B-03 ~ B-10은 조사하지 않았다.
- 변경 파일 전수 조사와 증거 무결성 전면 재현은 하지 않았다.
- 배포 슬롯이 없어 라이브QA, 대상 SHA 이미지 배포, 실제 사용자 화면 조작은 하지 않았다.
- Docker 서비스 재빌드·재기동과 공유 DB INSERT/UPDATE/DELETE는 하지 않았다.
- 이번 두 질문과 무관한 UI, CSV, 인쇄, 정렬, 합계 경로는 재검증하지 않았다. 단, `VatAmountCalculator`의 직접 생산 소비자인 partner-order 인쇄 fallback의 반올림 모드가 `DOWN`임은 코드로 확인했다.
