# PR #1125 (#894) SOL 라이브 QA 증거

- 격리 desktop: `http://127.0.0.1:41275`
- 격리 gateway: `http://127.0.0.1:41280`
- 공유 `samhan-*` 스택 로그인·쓰기: 수행하지 않음
- UTF-8 복제: custom dump 파일 경유, 복제 전후 한글 SELECT 동일

## 스크린샷

없음. in-app Browser 연결 결과가 `No browser is available`, 가용 브라우저 목록이 `[]`여서 증거 무결성을 위해 합성 이미지나 다른 브라우저 제어 수단으로 대체하지 않았다.

상세 API·SSE·테스트 원문은 `docs/dev-reports/2026-08-12-894-sol-review.md`에 있다.
