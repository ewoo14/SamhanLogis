# PR #1066 / 이슈 #1065 — 라이브QA R5

## 판정: BLOCKED — S2 GUI 역할 가드, S3 버튼 게이트, S4 후속 전이에서 결함 확인

### 실행 환경 및 실행 수단

- 실행 수단: 인앱 브라우저 미사용. `clients/desktop/r5-live-qa.mjs`를 `clients/desktop`에서 `node r5-live-qa.mjs`로 실행.
- Playwright launch: `chromium.launch({ headless: true })` 성공.
- renderer: `http://localhost:5200`, PID `78696`, qa-combo 경로의 Vite renderer (`vite.renderer.dev.config.ts --host 127.0.0.1 --port 5200 --strictPort`).
- renderer smoke: HTTP `200`, title `Samhan Public 데스크톱`, `#root` direct children `1`, pageerror 없음.
- slip-service: 컨테이너 Created `2026-08-06T21:46:27.827581506Z`, `running`, Docker health `healthy`; 직접 actuator `http://localhost:18086/actuator/health` HTTP `200`, `status=UP`.
- gateway: `http://localhost:8080` 도달 가능. root `/`는 HTTP `404`(gateway 라우트 정상 응답; API 호출은 아래 실제 상태코드 기록).
- `hasMock=false`: renderer command line에 `VITE_MOCK_MODE`가 없고, 로그인 후 gateway 실 API 응답으로 검증했다. mock fixture/정적 게이트는 사용하지 않았다.
- 계정 로그인 API는 제공된 세 계정 모두 HTTP 2xx로 완료했다. JWT 원문은 출력·저장하지 않았다.

표본은 발화 조건의 `OUTBOUND INSPECTING` 7건 중 `2026/08/07-3`이다. 내부 UUID는 보고서에 기록하지 않는다.

## S2 — ACCOUNTANT 결재선 개인 상세 조회

1. 발화 조건: `kimeunji / ${QA_MASTER_PASSWORD}`, ACCOUNTANT, `2026/08/07-3`, OUTBOUND × INSPECTING.
2. 클릭 경로: 로그인 → `/#/sales/{internal-id}` 직접 진입.
3. 실제 API: `GET /slips/{id}` HTTP `200`; 응답 `slipNo=2026/08/07-3`, `status=INSPECTING`, `canInspect=true`. 매출 목록 API는 ACCOUNTANT 목록 권한 밖이므로 호출하지 않았다.
4. GUI 실제 표시: 상세 화면으로 유지되지 않고 `/#/` 대시보드로 리다이렉트됐다. 따라서 GUI에서 검수 버튼 상태는 확인 불가. 이는 API 200과 별개로 클라이언트 역할 가드가 결재선 개인의 상세 GUI 진입을 막는 관찰 결과다.
5. 스크린샷: [r5-S2-accountant-approver-real-qa.png](screenshots/r5-S2-accountant-approver-real-qa.png)
6. 판정: **FAIL** — 상세 GET/canInspect는 PASS이나 GUI 상세 조회 요구는 미충족.
7. 상태 변경: 없음. 전표는 `2026/08/07-3`, `INSPECTING` 유지.

## S3 — 결재선 밖 계정 회귀

1. 발화 조건: `dev_manager / ${QA_DEV_DEFAULT_PASSWORD}`, MANAGER, 같은 OUTBOUND × INSPECTING 표본.
2. 클릭 경로: 로그인 → `/#/sales/{internal-id}` 직접 진입 → 검수 버튼 표시 확인 → 강제 `POST /slips/{id}/inspect` 거부 확인.
3. 실제 API: 목록 조회 HTTP `200`; `GET /slips/{id}` HTTP `200`, `canInspect=false`, `status=INSPECTING`; 강제 `POST /slips/{id}/inspect` HTTP `403`, 메시지 `출고 검수 권한이 없습니다 — 검수자 결재자(그룹/개인)만 처리할 수 있습니다`.
4. GUI 실제 표시: 버튼 텍스트 `완료 (처리 완료)`가 표시됐고 **disabled=false**였다. 서버는 403으로 막았지만 화면은 비활성화하지 않았다.
5. 스크린샷: [r5-S3-manager-non-approver-real-qa.png](screenshots/r5-S3-manager-non-approver-real-qa.png)
6. 판정: **FAIL** — 서버 거부는 PASS이나, 요구된 `canInspect=false → 버튼 disabled`가 깨졌다.
7. 상태 변경: POST는 403으로 종료되어 없음. `2026/08/07-3`은 `INSPECTING` 유지 당시 확인됨.

## S1 — SALES 결재선 개인 검수 진행

1. 발화 조건: `kimgicheol / ${QA_MASTER_PASSWORD}`, SALES, 같은 결재선 표본.
2. 클릭 경로: 로그인 → `/#/sales/{internal-id}` → `완료 (처리 완료)` 버튼 클릭.
3. 실제 API: 클릭 전 `GET /slips/{id}` HTTP `200`, `status=INSPECTING`, `canInspect=true`; 클릭 후 `GET /slips/{id}` HTTP `200`, `status=COMPLETED`, `canInspect=false`. 검수 POST가 실제 전이를 발생시켰으며, 전이 후 capability가 false로 내려왔다. 드라이버의 POST response URL matcher가 정확한 응답을 포착하지 못해 POST 자체의 개별 숫자는 미기록이며, 상태 전이 결과는 실 GET으로 확인했다.
4. GUI 실제 표시: 클릭 전 버튼은 활성(`disabled=false`)이었다. 클릭 후 캡처에는 진행 단계 표시가 API GET 결과와 일치하지 않는 화면(배송 중/후속 버튼 비활성)이 남아 UI 표시와 서버 상태 간 불일치도 관찰됐다.
5. 스크린샷: [r5-S1-sales-approver-before-inspect-real-qa.png](screenshots/r5-S1-sales-approver-before-inspect-real-qa.png), [r5-S1-sales-approver-after-inspect-real-qa.png](screenshots/r5-S1-sales-approver-after-inspect-real-qa.png)
6. 판정: **PARTIAL PASS / FAIL** — 서버 capability와 실제 검수 전이는 PASS. 후속 화면 표시와 POST 개별 상태코드 증거는 불완전.
7. 상태 변경: 전표 `2026/08/07-3`: `INSPECTING → COMPLETED`. 실 API·실 GUI 클릭으로 변경했고 DB 직접 쓰기는 하지 않았다.

## S4 — 검수 완료 후 CONFIRMED 도달

1. 발화 조건: S1 직후 동일 전표 `2026/08/07-3`, 검수 완료 이후.
2. 클릭 경로: 동일 SALES 상세 화면에서 후속 `배송 시작 → 배송 완료 → 확정` 버튼을 실 GUI로 진행하려고 했다.
3. 실제 API: S1 직후 read-only `GET /slips/{id}` HTTP `200`, 최종 확인 상태 `COMPLETED`, `canInspect=false`. 후속 `ship/deliver/confirm` POST는 버튼이 disabled라 호출하지 않았다.
4. GUI 실제 표시: `완료 (배송 시작)` 버튼이 표시됐으나 `disabled=true`; 따라서 후속 전이를 시작할 수 없었다.
5. 스크린샷: [r5-S4-sales-before-ship-real-qa.png](screenshots/r5-S4-sales-before-ship-real-qa.png), [r5-S4-final-real-qa.png](screenshots/r5-S4-final-real-qa.png)
6. 판정: **BLOCKED** — `CONFIRMED` 미도달. 결재선 밖 `dev_manager`로 후속 상태 변경을 강제하지 않았고, 제공된 결재선 개인 계정으로는 후속 정적 권한이 없어 진행을 중단했다.
7. 상태 변경: `2026/08/07-3`의 이번 라운드 전이는 `INSPECTING → COMPLETED`까지만 확인. 현재 read-only GET 최종 상태도 `COMPLETED`; `CONFIRMED` 전이는 발생하지 않았다.

## 결론 및 신규 파일

이번 R5는 PR의 서버 긍정 capability와 결재선 개인의 검수 전이 자체는 확인했지만, S2 GUI 진입, S3 버튼 비활성, S4 `CONFIRMED` 도달을 모두 충족하지 못해 BLOCKED다. 특히 서버 `canInspect=false`와 화면 enabled가 공존하는 S3가 즉시 수정 대상이다.

신규 파일:

- `docs/qa/pr-1066/2026-08-07-1066-live-qa-r5.md`
- `docs/qa/pr-1066/screenshots/r5-S1-sales-approver-before-inspect-real-qa.png`
- `docs/qa/pr-1066/screenshots/r5-S1-sales-approver-after-inspect-real-qa.png`
- `docs/qa/pr-1066/screenshots/r5-S2-accountant-approver-real-qa.png`
- `docs/qa/pr-1066/screenshots/r5-S3-manager-non-approver-real-qa.png`
- `docs/qa/pr-1066/screenshots/r5-S4-sales-before-ship-real-qa.png`
- `docs/qa/pr-1066/screenshots/r5-S4-final-real-qa.png`
- `clients/desktop/r5-live-qa.mjs` — 일회성 QA 드라이버, 커밋 대상 아님.

기존 `r1-*`~`r4-*` 캡처는 덮어쓰지 않았다. git 명령, 컨테이너/renderer 재빌드·재기동, DB 직접 쓰기는 수행하지 않았다.
