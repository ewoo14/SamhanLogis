# 재고수불부 정본 — 이카운트 실 엑셀본 기준 (개발책임자)

> 2026-08-14 · 이슈 `#999` · 참조자료 `.claude/uploads/재고수불부_AP145BNPPHH1.xlsx`

## 개발책임자 지시

> *"재고수불부 모달이 너무 작은데? 내가 참조하라고 준 **이카운트 엑셀본대로** 만들어줘야지.
> **해당 전표번호로 전표도 볼 수 있어야** 하고 말야"*

---

## 1. 실 엑셀본 구조 (직접 파싱해 확인)

### 머리글 (1행)

```
회사명 : (주)삼한공조시스템 / 2026/05/01 ~ 2026/08/13 / 재고수불부 I / AP145BNPPHH1 [BN프리미엄 실내기] (AP145BNPPHH1)
```

⟹ **회사명 · 기간 · 표 제목 · 대상 품목**이 한 줄에 들어갑니다.

### 열 (2행) — **9열**

| | 열 | 비고 |
|---|---|---|
| A | **일자** | `2026/05/02` |
| B | **품목명** | `AP145BNPPHH1 [BN프리미엄 실내기]` |
| C | **품목코드** | `AP145BNPPHH1` |
| D | **창고명** | `삼성창고 (초월 무갑)` |
| E | **거래처명** | `삼성전자(주)` · `꽃달시스템-이종승` |
| F | **적요** | 자유 텍스트 — 아래 참조 |
| G | **입고수량** | |
| H | **출고수량** | |
| I | **재고수량** | 누적 잔량 |

### 첫 데이터 행 앞에 **전일재고**

```
전일재고 | 81
```

⟹ 기간 시작 시점의 이월 잔량이 **맨 위 한 줄**로 들어갑니다. 이게 없으면 누적 잔량이 어디서 시작했는지 알 수 없습니다.

### 규모

```
총 481행 (머리글 1 + 열 1 + 전일재고 1 + 데이터 478)
🚩 합계·누계 행은 없습니다
🚩 금액 열이 없습니다 — 수량만
```

### 🚩 `적요` 는 한 열에 여러 성격이 섞여 있습니다

```
7343889694                                    ← 전표번호로 보이는 숫자 (삼성전자 입고 행)
지방/울산광역시 북구 사청6길 6 (화봉동) 1층상가   ← 배송주소 (지역 태그 포함)
야적/인천 연수구 센트럴로160 …                  ← 야적 태그
48%/-7만                                      ← 할인 메모
-6만                                          ← 금액 메모
계산서X 충청북도 충주시 예성로 152 (성서동)      ← 계산서 여부 + 주소
```

---

## 2. 🔴 결정 — 우리 화면

### 2-1. 크기

**모달을 엑셀본 9열이 잘리지 않고 들어가는 폭**으로 키웁니다. 지금은 너무 작습니다.

### 2-2. 열 구성

**엑셀본 9열을 그대로** 씁니다. 🚫 임의로 줄이거나 합치지 마십시오.

### 2-3. 전표번호 — 🆕 열을 추가합니다

🚩 **이카운트 엑셀본에는 전표번호 전용 열이 없습니다.** 전표번호로 보이는 값이 `적요` 에 섞여 있을 뿐입니다.

우리 시스템은 전표번호를 정식으로 갖고 있으므로 **전용 열을 추가**하고, 개발책임자 지시대로 **클릭하면 그 전표를 볼 수 있게** 합니다.

### 2-4. 전표 열기 방식 — 개발책임자 결정

> **수불부를 닫고 전표 화면으로 이동한다.** (모달 위 모달 ❌ · 새 창 ❌)

### 2-5. 전표 종류별로 맞는 화면

```
판매전표 · 입고전표 · 이동전표 · 재고실사 — 종류마다 가는 곳이 다릅니다
🚨 종류를 코드에서 확인할 수 없으면 지어내지 말고 보고할 것
```

### 2-6. UUID 비노출

URL 에 UUID 를 쓰지 않습니다. **전표번호나 opaque 식별자**를 씁니다.
→ [`2026-08-13-uuid-never-exposed.md`](2026-08-13-uuid-never-exposed.md)

---

## 3. 🔴 나머지 결정 (2026-08-14 개발책임자)

### 결정 5 — 행 구성: **전일재고 + 합계 행 둘 다**

```
맨 위    전일재고 행     ← 엑셀본에 있음. 누적 잔량의 시작점이라 없으면 설명이 안 됩니다
맨 아래  합계 / 누계 행   ← 🆕 엑셀본에는 없지만 우리는 넣습니다
                          기간 내 총 입고·총 출고를 한눈에 보기 위함
```

🚩 **엑셀본과 행 수가 달라집니다.** 이카운트와 나란히 대조할 때 헷갈리지 않도록 합계 행은 **시각적으로 구분**되게 하십시오.

### 결정 6 — 적요: **전표번호만 빼고 나머지는 한 열에 그대로**

```
✅ 전표번호      전용 열로 분리 (결정 2-3)
✅ 나머지        적요 한 열에 그대로
     배송주소(지방/야적 태그 포함) · 할인메모(48%/-7만) · 계산서X · 그 밖의 자유 텍스트
```

🔑 **미리 성격을 정해 나누지 않습니다.** 적요에 무엇이 들어갈지 확정돼 있지 않고 형식도 일정하지 않습니다. 특정 성격을 자주 쓰게 되면 그때 뽑습니다.

### 결정 7 — 기간 기본값: **최근 3개월**

엑셀본이 `2026/05/01 ~ 08/13`(약 3개월 보름)이었고, 품목 하나당 478행 정도라 한 화면에서 훑어볼 만합니다.

---

## 3-1. ⏳ 남은 것

없습니다. 결정 1~7 로 화면 정본이 확정됐습니다.

🚩 다만 **모달 폭은 언제나 실제 렌더 폭으로 판정**하십시오. 열이 늘면 다시 좁아집니다 — `XL(1280px)` 이 10열에서 140px 모자라 `xxl(1320px)` 로 올린 전례가 이 트랙 안에 있습니다. 합계 행은 폭에 영향이 없지만, 열을 더 추가하면 다시 재야 합니다.

---

## 4. 관련

```
docs/dev-reports/2026-08-13-stock-ledger-recon.md   수불 정본은 public.stock_movements
PR #1199 (머지)  시리얼 입출고가 수불에 남게
PR #1203         재고이동 확정이 재고·수불을 바꾸게 + 확정 후 캐시 무효화
```

🚨 **이 화면 작업은 새 PR 을 열지 않고 `#1203` 트랙 안에서 단계별로** 처리합니다(개발책임자 지시).
