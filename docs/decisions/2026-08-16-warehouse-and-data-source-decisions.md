# 창고 판정 · 데이터 정본 — 개발책임자 확정 (2026-08-16)

야간 세션에서 올린 결정 4건이다.

---

## ① 미분류 품목의 창고 — **초월(00003)** (현재 구현 유지)

```text
확정   품목분류가 없는 품목은 레거시 some 판정에서 "걸리지 않은 것" 으로 본다
       ⟹ 그 품목만 있는 주문은 초월(00003)
실측   활성 상품 3084개 중 중분류 없음 2194개 (71%)
```

🔑 **이미 구현된 대로다.** 미분류 건수와 모델코드는 실행 로그·history payload 에 남는다.

### 🚩 유지해야 할 구분

```text
분류 없음 / 미지 분류   → 미분류로 기록 (사람이 손봐야 할 대상)
정상 분류 · 9조건 비적중 → 미분류 아님. 그냥 초월이다
```
이 구분은 `ca9049a35` 에서 닫았다. 되돌아가면 로그가 거짓이 된다.

---

## ② 레거시와 갈리는 상품 32개 — **예외로 레거시 유지**

```text
확정   판정은 품목분류 기준으로 하되, 이 32개만 레거시 창고를 강제한다
대상   상일→초월 27 · 초월→상일 5
목록   docs/dev-reports/2026-08-15-order-web-warehouse-category-mapping.md
```

### 🚩 구현 시 반드시 지킬 것

```text
🚨 예외 목록이 코드에 묻히면 안 된다 — 무엇이 왜 예외인지 읽히게 남겨라
🚨 판정이 두 갈래가 된다. 어느 상품이 예외로 걸렸는지 실행 중에 식별 가능해야 한다
   ⟹ 나중에 분류가 정리되면 예외를 지울 수 있어야 한다
🚨 32개 목록은 실측 산출물이다. 손으로 고치지 말고 대조 보고서를 원천으로 삼아라
```

---

## ③ 상품 마스터에 없는 모델 — **현재 동작 유지 (확정 실패)**

```text
확정   가격표·할인·DC 근거 없는 주문이 저장되지 않도록 막는 것이 맞다
       사용자는 주문 전에 상품을 마스터에 등록해야 한다
근거   가격 계산 방어선 (6a219fa8a7)
```

🚩 **#1229 보고서의 "미매칭도 확정 허용" 서술은 사실이 아니다.** 그 문장을 고쳐야 한다.

```text
현 개발 DB 실측 미등록 모델 2개
  AR05TXEAAWKNEU-11 · AR15TXEAAWKNEU-07
```

---

## ④ 레거시 데이터 정본 — **CSV(Notion)가 정본**

```text
확정   Notion 원본을 정본으로 보고 DB 를 맞춘다
       누락 92코드를 넣고 · drift 43코드/67필드를 CSV 값으로 덮는다
```

### 🚩 착수 전 반드시 확인할 것

```text
🚨 DB 에서 사람이 수정한 값이 있으면 사라진다
   ⟹ 덮기 전에 "CSV 와 다른 현행 값" 전량을 백업·기록하고 시작하라
🚨 금액에 닿는 것이 126코드다 — 덮은 뒤 총액 비교를 남겨라
🚨 적재기 파싱 결함 2건은 이미 닫혔다 (#1228)
   DC 0.45 → 0.4500 보존 · 발송금지 ISO 시각 파싱 · 시간축 KST 통일
   ⟹ 지금 재실행하면 100배 왜곡이나 6행 거부는 나지 않는다
🚨 원천이 바뀌면 현행이 따라가는 구조가 아니다 (일회성 적재)
   같은 drift 가 다시 쌓인다. 그 구조를 바꿀지는 별도 결정이다
```

근거 보고서: [레거시 데이터 적재 격차](../dev-reports/2026-08-16-legacy-data-load-gaps.md)
