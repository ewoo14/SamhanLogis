# 전표 명칭 통일 + 배송태그 전면 재구성 (2026-08-14 개발책임자)

> 앞선 결정: [`2026-08-14-inbound-delivery-tag-purchase-default.md`](2026-08-14-inbound-delivery-tag-purchase-default.md) — **이 문서가 대체한다**
> 관련: [`2026-08-14-qr-lifecycle-correction.md`](2026-08-14-qr-lifecycle-correction.md) — QR 수명주기
> 트랙: `#1210`

---

## 개발책임자 원문

> *"구매전표 → 입고전표, 판매전표 → 출고전표로 **메뉴 등의 명칭을 모두 변경**.
> 그리고 입고전표의 배송 태그는 기본 '구매' 그리고 '차용', '대여반납', '반품', '착하반품', '회차'로 구성 (**QR은 '구매', '차용', '대여반납'** 건에 대해서 생성)
> 출고전표의 배송 태그는 기본 '판매' 그리고 '대여', '차용반납', '불량반납', '직배', '착하선조치', '로젠택배', '경동택배', '경동화물', '야적', '지방'"*

---

## 1. 전표 명칭 통일

```
구매전표  →  입고전표
판매전표  →  출고전표
```

**메뉴 · 화면 제목 · 버튼 · 안내문 · 출력물 전부** 바꾼다.

### 🔒 권한 키(page-code)는 바꾸지 않는다

```
바뀐다 (사용자가 보는 것)   메뉴 · 제목 · 버튼 · 안내문 · 인쇄물
🚫 안 바뀐다 (내부 키)      purchases.slip.list · purchases.slip.edit
                            accounting.sales-slip.list · accounting.purchase-slip.list
```

🔑 이들은 `account_page_permissions` · `group_page_permissions` · `role_page_permissions` **세 테이블의 키**다. 바꾸면 전 계정 권한을 함께 옮겨야 하고, **하나라도 누락되면 그 화면이 안 열린다.** 표시와 키를 분리해 위험을 없앤다.

---

## 2. 배송태그 전면 재구성

### 입고전표 — 6종 (기본 `구매`)

| 태그 | QR |
|---|---|
| **구매** (기본) | ✅ 생성 |
| **차용** | ✅ 생성 |
| **대여반납** | ✅ 생성 |
| 반품 | ❌ |
| 착하반품 | ❌ |
| 회차 | ❌ |

### 출고전표 — 11종 (기본 `판매`)

```
판매 (기본) · 대여 · 차용반납 · 불량반납 · 직배 · 착하선조치
로젠택배 · 경동택배 · 경동화물 · 야적 · 지방
```

🔑 **입고·출고가 짝을 이룬다.** `차용`(입고) ↔ `차용반납`(출고) · `대여`(출고) ↔ `대여반납`(입고).

---

## 3. 🚩 `당일`(DAY) 폐기 — 기존 13건은 `판매`로

새 출고 목록에 `당일`이 없다. 실측하면 **살아 있는 전표 13건**이 쓰고 있고 마감시각 `00:01` 설정도 걸려 있다.

```
결정  폐기한다. 기존 13건은 기본값 판매 로 내린다
      마감시각 설정의 DAY 행도 함께 정리한다
```

🚩 **당일 배송이라는 구분이 이력에서 사라진다.** 되돌리기 어려우므로, 옮기기 전에 13건을 식별 가능하게 남긴다.

---

## 4. 🚩 `대여반납` QR 생성 — 앞선 원칙과의 관계를 기록해 둔다

PM 이 확인 요청했고 **개발책임자가 "지시대로 대여반납도 생성" 으로 확정**했다.

```
앞서 세운 원칙
  "이미 QR 이 생성되어 출고된 인스턴스가 다시 창고로 되돌아 오는 거니까"
  ⟹ 반품 · 회차 는 생성하지 않는다

대여반납은 다르게 다룬다
  ⟹ 대여 출고 시점에는 QR 을 붙이지 않고, 돌아올 때 발급하는 흐름으로 본다
```

🚨 **구현이 반드시 지킬 것** — 대여반납 스캔 시 **이미 시리얼키를 가진 개체면 새로 발급하지 않는다.**
같은 물리 개체에 시리얼키가 둘 생기면 그 순간부터 재고와 이력이 어긋나고 되돌리기 어렵다. *"대여반납은 생성 대상"* 과 *"이미 있으면 재사용"* 은 함께 성립해야 한다.

---

## 5. 진행 방식 — `#1210` 트랙에 함께

명칭 변경이 **131개 파일**에 걸치지만 배송태그와 같은 화면이라 한 트랙에서 끝낸다.

🚩 대신 **커밋을 분리**한다 — 명칭 일괄 치환과 기능 변경이 한 diff 에 섞이면 리뷰에서 결함이 문자열 치환 속에 묻힌다.

---

## 6. 이 결정이 정리하는 기존 미결

| 미결 | 해소 |
|---|---|
| 출고 태그없음 132건 | 기본값 `판매` 로 채운다 |
| `대여`·`반납` 처리 | `대여` 유지 · `반납` 은 `차용반납`/`대여반납`/`불량반납` 으로 분화 |
| 입고 태그없음 61건 | 기본값 `구매` (앞선 결정 유지) |

## 7. 🚩 구현 전 확인해야 할 것

```
① 마감시각 설정(slip_outbound_cutoff)은 6종만 등록돼 있다
   새 11종 중 어디에 마감시각이 필요한지 확인이 필요하다 (DAY 폐기분 포함)
② 야적 · 지방 은 autoMemo=true (하차일 자동계산) — 재구성에서 유실되면 안 된다
③ page-code 는 그대로이므로 권한 마이그레이션은 없다
```
