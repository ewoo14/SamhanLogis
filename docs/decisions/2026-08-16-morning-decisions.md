# 개발책임자 확정 — 2026-08-16 아침

야간 세션에서 올린 결정 4건.

---

## ① 일마감 상세가 전부 0 인 것 — **데이터부터 채운다**

```text
확정   화면에서 빼거나 '-' 로 바꾸지 않는다. 값이 나오도록 데이터를 채운다
현황   모델        13/13 존재 — 응답에서만 빠졌다
       카테고리    0/13 — 데이터 자체가 없다
       기준 납품가  시점 이력 1/13
       기대율      유효 근거 0/13
       DC액        파생 가능 1/13 · 결과도 0
       확인 사유    응답에는 있으나 관측과 현재 로직이 모순
```

### 🚩 착수 전 확인이 필요한 것

```text
🚨 카테고리·기준 납품가·기대율을 어디서 가져오는가
   product_db 에 있는가 · 레거시 GAS 에 있는가 · 사람이 입력하는가
   ⟹ 원천이 정해져야 채울 수 있다
🚨 "채운다" 가 일회성 적재인가, 앞으로 계속 유지되는 계산인가
   일회성이면 DC drift 처럼 다시 어긋난다
🚨 모델은 응답만 고치면 즉시 나온다 — 그것부터 분리해서 낼 수 있다
```

근거: [일마감 상세 0 정찰](../dev-reports/2026-08-16-daily-closing-detail-zero-recon.md)

---

## ② 레거시 CSV 적재 — **전체를 한 번에**

```text
확정   누락 92코드 + drift 43코드/67필드를 한 번에 적재한다
정본   CSV(Notion)  (2026-08-16 ④ 확정)
```

### 🚩 반드시 지킬 것

```text
🚨 덮기 전에 "CSV 와 다른 현행 DB 값" 전량을 백업하고 목록을 남긴다
   ⟹ DB 에서 사람이 수정한 값이 있으면 사라진다
🚨 금액에 닿는 126코드는 적재 전후 총액 비교를 남긴다
🚨 적재기 파싱 결함 2건은 이미 닫혔다 (#1228 — DC 100배 · 발송금지 ISO 시각 · KST 통일)
🚨 원천이 바뀌면 현행이 따라가는 구조가 아니다 (일회성)
   같은 drift 가 다시 쌓인다. 구조 변경은 별도 결정
```

근거: [레거시 데이터 적재 격차](../dev-reports/2026-08-16-legacy-data-load-gaps.md)

---

## ③ #1162 자격 로테이션 — **지금 머지 + 14개 동시 재배포**

```text
확정   보류 해제. 머지하고 즉시 14개를 함께 재배포한다
이유   compose 가 SAMHAN_INTERNAL_TOKEN 등을 :?required 로 요구하므로
       반쪽 재배포는 mesh 를 깬다 (야간에 두 번 겪음)
상태   머지 완료 (0cd6b0027) · 14개 재배포 진행
```

---

## ④ Expo 모바일 — **설계 대비 구현 격차부터 재기**

```text
확정   방향을 정하기 전에 무엇이 없는지 먼저 전수로 잰다
현황   @samhan/mobile-staff v0.4.0 · Expo 53 · RN 0.79.6
       APP_VARIANT staff · sales 2변형
       EAS development · preview 프로필 (production 없음)
설계   SALES-APP-DESIGN.md · PHOTO-ATTACHMENT-DESIGN.md (둘 다 2026-05-11)
```

🚩 판정 기준은 **"파일이 있다" 가 아니라 "사용자가 도달하는 경로가 있다"** 다.
라우팅·탭에 안 걸린 화면은 없는 것으로 센다.
