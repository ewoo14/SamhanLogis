/**
 * F1 skeleton 시점의 DriverManagementPage placeholder.
 *
 * 실제 list / create / soft delete 구현은 F4 (별도 commit) 에서 추가한다.
 */
export function DriverManagementPage(): JSX.Element {
  return (
    <section>
      <h1 style={{ fontSize: 'var(--font-size-xl)', marginTop: 0 }}>기사 관리</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>
        phoneNumber 사전 등록 화면 — F4 에서 구현됩니다.
      </p>
    </section>
  )
}
